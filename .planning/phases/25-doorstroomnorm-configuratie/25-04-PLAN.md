---
phase: 25-doorstroomnorm-configuratie
plan: 04
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - src/components/DoortstroomPrognoseSection.tsx
autonomous: true
requirements: [NORM-01, NORM-02, NORM-03, NORM-04, NORM-05]
tags: [ui, prognosis, react, parameterization]

must_haves:
  truths:
    - "DoortstroomPrognoseSection.tsx reads current normen via getNormenSync() and uses them to construct all explanatory label strings"
    - "No hardcoded threshold values (6, 2, 13, 15) remain in label string templates in DoortstroomPrognoseSection.tsx"
    - "When a mentor changes normen in Settings, the student detail view shows the updated norm values in the explanatory text (not stale hardcoded numbers)"
    - "The component does NOT call loadNormen() — it uses the sync accessor because main.tsx pre-warmed the cache at startup"
  artifacts:
    - path: "src/components/DoortstroomPrognoseSection.tsx"
      provides: "Parameterized norm text using getNormenSync() — no hardcoded 6/2/13/15 threshold strings"
      contains: "getNormenSync"
  key_links:
    - from: "src/components/DoortstroomPrognoseSection.tsx"
      to: "utils/normen.ts getNormenSync"
      via: "import { getNormenSync } from '../utils/normen'; const n = getNormenSync();"
      pattern: "getNormenSync\\(\\)"
---

<objective>
Parameterize all hardcoded norm-text strings in `src/components/DoortstroomPrognoseSection.tsx` by replacing literal threshold values with reads from `getNormenSync()`.

Purpose: After Plans 02 and 03 ship, the prognose engine computes correct RAG colours using configured normen, and SettingsPage allows editing those normen. However, the student detail view (DoortstroomPrognoseSection) still shows hardcoded strings like "max. 6 totaal", "norm ≥13", "norm ≥15". This creates a visible inconsistency: tile colours may have changed but the explanatory copy still says the old CIOS defaults. This plan closes that gap so the entire visible surface (tiles + detail text) reflects the live configured normen.

The component uses `getNormenSync()` (not `loadNormen()`) because `main.tsx` pre-warmed the cache at startup (Plan 03, Task 1). Sync read is safe here and avoids async complexity in render functions.

Output: DoortstroomPrognoseSection.tsx with all threshold references parameterized. No new files. No new state. Full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-doorstroomnorm-configuratie/25-CONTEXT.md
@.planning/phases/25-doorstroomnorm-configuratie/25-RESEARCH.md
@.planning/phases/25-doorstroomnorm-configuratie/25-02-SUMMARY.md

<interfaces>
<!-- getNormenSync() is available after Plan 02 ships -->

From utils/normen.ts (created in Plan 02):
```typescript
export function getNormenSync(): Normen;
// Returns _cache ?? DEFAULT_NORMEN — sync, no await, safe to call in render functions.
// Cache is pre-warmed by main.tsx at startup, so this returns live configured normen.

export interface Normen {
  sbl: number;                 // default 13 — BJ2 SBL threshold
  sbc: number;                 // default 15 — BJ2 SBC threshold
  negatiefTotaal: number;      // default 6  — max total onvoldoendes before negatief advies
  negatiefPerLeerlijn: number; // default 2  — max onvoldoendes per leerlijn before negatief
  bj1Positief: number;         // default 13 — BJ1→BJ2 doorstroom threshold
  versneldLesgeven: number;    // default 4
  versneldOrganiseren: number; // default 3
  versneldProfHandelen: number;// default 5
}
```

<!-- Current hardcoded strings in DoortstroomPrognoseSection.tsx to replace: -->
<!--
  Line 27: `max. 6 totaal, max. 2 per leerlijn`
           → `max. ${n.negatiefTotaal} totaal, max. ${n.negatiefPerLeerlijn} per leerlijn`

  Line 34: `(${p.totaalOnvoldoende}/6 O)`
           → `(${p.totaalOnvoldoende}/${n.negatiefTotaal} O)`

  Line 40: `max. 2 O per leerlijn bereikt`
           → `max. ${n.negatiefPerLeerlijn} O per leerlijn bereikt`

  Line 53: `norm ≥13)` (SBL gehaald label)
           → `norm ≥${n.sbl})`

  Line 54: `norm ≥13)` (SBL nodig label)
           → `norm ≥${n.sbl})`

  Line 64: `≥15 ≥V + alle kerndeelgebieden)` (SBC gehaald label)
           → `≥${n.sbc} ≥V + alle kerndeelgebieden)`

  Line 68: `norm ≥15)` (SBC nodig label)
           → `norm ≥${n.sbc})`

  Line 85: `norm ≥13)` (BJ1→BJ2 label)
           → `norm ≥${n.bj1Positief})`
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize DoortstroomPrognoseSection.tsx norm text using getNormenSync()</name>
  <files>src/components/DoortstroomPrognoseSection.tsx</files>
  <read_first>
    - src/components/DoortstroomPrognoseSection.tsx (entire file — read before touching anything; all 8 hardcoded sites are listed in the interfaces block above with exact line numbers and replacement targets)
    - utils/normen.ts (Plan 02 output — confirm `getNormenSync` export exists and returns `Normen`)
    - .planning/phases/25-doorstroomnorm-configuratie/25-CONTEXT.md (D-12: berekenPrognose uses getNormenSync; same sync pattern applies here)
    - .planning/phases/25-doorstroomnorm-configuratie/25-RESEARCH.md (Pitfall 4 — sync read is correct here because main.tsx pre-warmed the cache)
  </read_first>
  <action>
    Edit `src/components/DoortstroomPrognoseSection.tsx`:

    1. Add import at the top of the file alongside the existing imports:
       `import { getNormenSync } from '../utils/normen';`
       (Match the relative path: DoortstroomPrognoseSection.tsx is in `src/components/`, normen.ts is in `utils/` — from the component's perspective the path is `'../utils/normen'`. Confirm this matches the import style used by other utils imports in the same file.)

    2. At the top of the `computeAlgemeneItems(p: any)` function body (before the `const items` declaration), add:
       `const n = getNormenSync();`

    3. Apply the following replacements inside `computeAlgemeneItems`:
       - Line 27 template literal: change `max. 6 totaal, max. 2 per leerlijn` to `max. ${n.negatiefTotaal} totaal, max. ${n.negatiefPerLeerlijn} per leerlijn`.
       - Line 34 template literal: change `/6 O)` to `/${n.negatiefTotaal} O)`.
       - Line 40 template literal: change `max. 2 O per leerlijn bereikt` to `max. ${n.negatiefPerLeerlijn} O per leerlijn bereikt`.

    4. At the top of the `computeSBLItems(p: any)` function body (before the `if` guard), add:
       `const n = getNormenSync();`

    5. Apply the following replacements inside `computeSBLItems`:
       - Line 53: change `norm ≥13)` to `norm ≥${n.sbl})` — in the `nodigSBL === 0` branch label string.
       - Line 54: change `norm ≥13)` to `norm ≥${n.sbl})` — in the `nodigSBL > 0` branch label string.

    6. At the top of the `computeSBCItems(p: any)` function body (before the `if` guard), add:
       `const n = getNormenSync();`

    7. Apply the following replacements inside `computeSBCItems`:
       - Line 64: change `≥15 ≥V + alle kerndeelgebieden)` to `≥${n.sbc} ≥V + alle kerndeelgebieden)`.
       - Line 68: change `norm ≥15)` to `norm ≥${n.sbc})`.

    8. At the top of the `computeBJ1Items(p: any)` function body (before the `const` destructuring), add:
       `const n = getNormenSync();`

    9. Apply the following replacement inside `computeBJ1Items`:
       - Line 85: change `norm ≥13)` to `norm ≥${n.bj1Positief})` — in the `nodigBJ2 === 0` branch label string.

    10. Do NOT add `getNormenSync()` to the main exported `DoortstroomPrognoseSection` component body — all helper functions already call it and the component delegates to them. Adding it again would be redundant.

    11. Do NOT add any new React state, useEffect, or async logic. The sync accessor is the correct approach here (pre-warmed cache from main.tsx).

    12. Do NOT change the component's props interface or return structure. Only the string literal content of label fields changes.

    After all edits: verify no remaining hardcoded threshold values appear in label string positions using the check in the acceptance criteria.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '../utils/normen'" src/components/DoortstroomPrognoseSection.tsx` = 1 (import added).
    - `grep -c "getNormenSync()" src/components/DoortstroomPrognoseSection.tsx` = 4 (one call at the top of each of the 4 helper functions: computeAlgemeneItems, computeSBLItems, computeSBCItems, computeBJ1Items).
    - No hardcoded `6` threshold remains in label string templates: `grep -n "max\. 6" src/components/DoortstroomPrognoseSection.tsx | grep -v '^\s*//'` returns zero lines.
    - No hardcoded `2` threshold remains in label string templates: `grep -n "max\. 2" src/components/DoortstroomPrognoseSection.tsx | grep -v '^\s*//'` returns zero lines.
    - No hardcoded `≥13` remains in label string templates: `grep -n "≥13" src/components/DoortstroomPrognoseSection.tsx | grep -v '^\s*//'` returns zero lines.
    - No hardcoded `≥15` remains in label string templates: `grep -n "≥15" src/components/DoortstroomPrognoseSection.tsx | grep -v '^\s*//'` returns zero lines.
    - Template literals use `n.negatiefTotaal`, `n.negatiefPerLeerlijn`, `n.sbl`, `n.sbc`, `n.bj1Positief`:
      - `grep -c "n\.negatiefTotaal" src/components/DoortstroomPrognoseSection.tsx` ≥ 2 (line 27 + line 34 replacements).
      - `grep -c "n\.negatiefPerLeerlijn" src/components/DoortstroomPrognoseSection.tsx` ≥ 2 (line 27 + line 40 replacements).
      - `grep -c "n\.sbl" src/components/DoortstroomPrognoseSection.tsx` ≥ 2 (lines 53 + 54).
      - `grep -c "n\.sbc" src/components/DoortstroomPrognoseSection.tsx` ≥ 2 (lines 64 + 68).
      - `grep -c "n\.bj1Positief" src/components/DoortstroomPrognoseSection.tsx` ≥ 1 (line 85).
    - `npx vitest run` full suite green — no regressions introduced by this change.
    - Component exports are unchanged: `DoortstroomPrognoseSection` default export still exists with the same props interface (`student`, `status`).
  </acceptance_criteria>
  <done>DoortstroomPrognoseSection.tsx fully parameterized — all 8 hardcoded threshold string sites replaced with getNormenSync() reads; no async logic added; full test suite green. The student detail view now reflects live configured normen, completing the end-to-end visible surface for Phase 25.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getNormenSync() return value → JSX string interpolation | Sync read of integer values from a validated cache — values are numbers, interpolated as strings via template literals |
| Component render (hot path) | getNormenSync() is O(1) cache read; no async, no I/O in render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-14 | Information Disclosure | getNormenSync() in render functions | accept | Returns only integer threshold values (sbl, sbc, etc.) from the validated LazyStore cache. No PII. No sensitive data. Values were set by the mentor in Settings or fall back to DEFAULT_NORMEN. |
| T-25-15 | Tampering | Stale cache read showing default after norm change | accept | getNormenSync() reads the module-level `_cache` which is updated synchronously by saveNormen before the async write (instant-apply pattern from Plan 02). As long as Plan 03 wires onNormenChanged to trigger re-render (via refreshKey bump), the next render calls getNormenSync() after the cache update. The Re-render chain: saveNormen → _cache updated → onNormenChanged → refreshKey++ → KlasOverzicht re-renders → student detail view re-renders → getNormenSync() returns new value. This is the existing pattern for all cached utils in this app. |

No high-severity threats. Read-only component consuming a pre-validated sync cache.
</threat_model>

<verification>
- `grep -c "getNormenSync" src/components/DoortstroomPrognoseSection.tsx` = 4 (one per helper function).
- `grep -n "≥13\|≥15\|max\. 6\|max\. 2" src/components/DoortstroomPrognoseSection.tsx | grep -v '^\s*//'` returns zero lines (all hardcoded thresholds replaced).
- `npx vitest run` → full suite green.
- Manual spot-check (can be combined with Plan 03 checkpoint): change SBL to 10 in Settings, open a student detail view, confirm the explanatory text shows "norm ≥10" instead of "norm ≥13".
</verification>

<success_criteria>
- NORM-01: SBL norm text in DoortstroomPrognoseSection.tsx uses `n.sbl` — shows configured value, not hardcoded 13.
- NORM-02: SBC norm text uses `n.sbc` — shows configured value, not hardcoded 15.
- NORM-03: Negatief totaal text uses `n.negatiefTotaal` — shows configured value, not hardcoded 6.
- NORM-04: Negatief per leerlijn text uses `n.negatiefPerLeerlijn` — shows configured value, not hardcoded 2.
- NORM-05: BJ1 doorstroom text uses `n.bj1Positief` — shows configured value, not hardcoded 13.
- No async logic added to the component.
- Full test suite green.
- Component props interface unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/25-doorstroomnorm-configuratie/25-04-SUMMARY.md` describing:
- The 8 hardcoded sites replaced (with before/after string examples).
- The 4 helper functions that each received a `const n = getNormenSync();` call.
- Confirmation that no async logic was added.
- Final test count and pass rate.
- Whether the manual spot-check was performed and what was observed.
</output>
