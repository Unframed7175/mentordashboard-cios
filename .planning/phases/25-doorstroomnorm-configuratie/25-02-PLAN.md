---
phase: 25-doorstroomnorm-configuratie
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - utils/normen.ts
  - utils/prognosis.ts
autonomous: true
requirements: [NORM-01, NORM-02, NORM-03, NORM-04, NORM-05, NORM-06, NORM-07]
tags: [persistence, prognosis, settings, refactor]

must_haves:
  truths:
    - "utils/normen.ts exists with LazyStore-backed Normen persistence (load, save, reset, sync accessor)"
    - "loadNormen() uses Number.isFinite() + per-field range enforcement — typeof checks are NOT sufficient"
    - "saveNormen() logs console.error when the store write fails so a failed save is never silent"
    - "berekenPrognose() accepts an optional 4th `normen` parameter and falls back to getNormenSync() when omitted"
    - "All 8 hardcoded threshold constants in utils/prognosis.ts (per the PATTERNS.md table, lines ~127–207) are replaced with normen.* reads — in both the main conditionals AND the gaps object"
    - "All 12 RED tests from Plan 01 turn GREEN"
  artifacts:
    - path: "utils/normen.ts"
      provides: "LazyStore-backed Normen persistence + sync cache + DEFAULT_NORMEN + Number.isFinite validation + field-level error logging"
      exports: ["Normen", "DEFAULT_NORMEN", "getNormenSync", "loadNormen", "saveNormen", "resetNormen"]
      contains: "doorstroom_normen"
    - path: "utils/prognosis.ts"
      provides: "berekenPrognose with optional normen parameter; VERSNELD_BJ1 removed"
      contains: "getNormenSync"
  key_links:
    - from: "utils/prognosis.ts"
      to: "utils/normen.ts"
      via: "import { getNormenSync, type Normen } from './normen'"
      pattern: "from\\s+['\"]\\./normen['\"]"
    - from: "utils/normen.ts"
      to: "@tauri-apps/plugin-store"
      via: "new LazyStore('store.json', { defaults: {}, autoSave: false })"
      pattern: "new LazyStore"
    - from: "utils/normen.ts"
      to: "saveNormen pairing"
      via: "store.set(STORE_KEY, normen) followed by store.save() — VERPLICHT (Phase 12 pitfall)"
      pattern: "store\\.save\\(\\)"
---

<objective>
Turn the Plan 01 RED tests GREEN by creating `utils/normen.ts` (modelled byte-for-byte on `utils/verzuimDrempels.ts`) and refactoring `utils/prognosis.ts` to replace all 8 hardcoded threshold constants with reads from the new `Normen` source of truth.

Purpose: This is the parameterisation refactor at the heart of Phase 25. Once complete, every threshold used in `berekenPrognose()` — both in the main conditional logic AND in the gaps sub-object — is sourced from `utils/normen.ts` via a sync cache. Plan 03 (UI) then wires the SettingsPage inputs into this persistence layer.

Key strengthening vs. original plan:
- `loadNormen()` validation uses `Number.isFinite()` (not just `typeof === 'number'`) AND enforces per-field min/max ranges. Values outside range fall back to DEFAULT_NORMEN for that field (or full fallback — see task action for the chosen strategy).
- When the type/range guard fails for a specific field, a field-level error is logged: `[normen.ts] invalid field 'sbl': <value>`.
- `saveNormen()` logs a `console.error('[normen.ts] saveNormen failed — settings not persisted')` when it catches an error and returns false. Failed saves are NEVER silent.

Output: Two files updated/created, all 12 Plan 01 tests green, full test suite green, no behavioural regression for existing prognosis tests (which call berekenPrognose with no 4th arg → fallback to defaults → identical numbers to today).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-doorstroomnorm-configuratie/25-CONTEXT.md
@.planning/phases/25-doorstroomnorm-configuratie/25-RESEARCH.md
@.planning/phases/25-doorstroomnorm-configuratie/25-PATTERNS.md
@.planning/phases/25-doorstroomnorm-configuratie/25-01-SUMMARY.md

<interfaces>
<!-- Contract defined by Plan 01 RED tests. Implementation must match exactly. -->

```typescript
// utils/normen.ts
export interface Normen {
  sbl: number;
  sbc: number;
  negatiefTotaal: number;
  negatiefPerLeerlijn: number;
  bj1Positief: number;
  versneldLesgeven: number;
  versneldOrganiseren: number;
  versneldProfHandelen: number;
}

export const DEFAULT_NORMEN: Normen; // { sbl:13, sbc:15, negatiefTotaal:6, negatiefPerLeerlijn:2, bj1Positief:13, versneldLesgeven:4, versneldOrganiseren:3, versneldProfHandelen:5 }
export function getNormenSync(): Normen;
export async function loadNormen(): Promise<Normen>;
export async function saveNormen(normen: Normen): Promise<boolean>;
export async function resetNormen(): Promise<Normen>;
```

```typescript
// utils/prognosis.ts (modified signature)
export function berekenPrognose(
  student: any,
  traject?: string,
  activeDeelgebiedenIds?: string[],
  normen?: Normen,
): any;
```

// Per-field valid ranges (same ranges used by SettingsPage Plan 03 inputs — D-09):
// sbl:                 min=1, max=19
// sbc:                 min=1, max=19
// negatiefTotaal:      min=1, max=19
// negatiefPerLeerlijn: min=1, max=6
// bj1Positief:         min=1, max=19
// versneldLesgeven:    min=1, max=6
// versneldOrganiseren: min=1, max=6
// versneldProfHandelen:min=1, max=6
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create utils/normen.ts (LazyStore + sync cache + reset + Number.isFinite validation + field-level logging)</name>
  <files>utils/normen.ts</files>
  <read_first>
    - utils/verzuimDrempels.ts (line-for-line template — copy and adapt; validation guard is at lines 48–61)
    - utils/deelgebieden.ts (reset pattern reference)
    - .planning/phases/25-doorstroomnorm-configuratie/25-PATTERNS.md (section `utils/normen.ts` — adaptations to make)
    - .planning/phases/25-doorstroomnorm-configuratie/25-RESEARCH.md (Pattern 1 — full snippet; Pitfall 2, 5)
    - tests/normen.test.ts (the GREEN target — written in Plan 01)
  </read_first>
  <behavior>
    The new module must satisfy all 5 tests from tests/normen.test.ts. Specifically:
    - `getNormenSync()` returns `DEFAULT_NORMEN` when the cache is null (tests 1, 3).
    - `loadNormen()` reads `'doorstroom_normen'` from LazyStore, validates that all 8 fields pass `Number.isFinite()` AND are within their per-field min/max range, caches the result, and returns it; on missing/invalid data falls back to `{ ...DEFAULT_NORMEN }` (not a direct reference). Logs which field failed when the guard rejects a value.
    - `saveNormen(n)` sets `_cache = n` BEFORE the await (instant-apply per pitfall 5), then awaits `store.set(STORE_KEY, n)` followed by `store.save()` (Phase 12 pitfall — VERPLICHT pairing). Returns true on success. On catch: logs `console.error('[normen.ts] saveNormen failed — settings not persisted')` AND returns false (tests 2, 4).
    - `resetNormen()` calls `saveNormen({ ...DEFAULT_NORMEN })` and returns `DEFAULT_NORMEN` (test 5).
    - STORE_KEY is the literal string `'doorstroom_normen'`.
  </behavior>
  <action>
    Copy `utils/verzuimDrempels.ts` into a new file `utils/normen.ts`. Adapt:

    1. Change `STORE_KEY` from `'verzuim_drempels'` to `'doorstroom_normen'`.

    2. Replace the `VerzuimDrempels` interface with the 8-field `Normen` interface.

    3. Replace `DEFAULT_VERZUIM_DREMPELS` with `DEFAULT_NORMEN = { sbl:13, sbc:15, negatiefTotaal:6, negatiefPerLeerlijn:2, bj1Positief:13, versneldLesgeven:4, versneldOrganiseren:3, versneldProfHandelen:5 }`.

    4. Rename all functions to the Normen equivalents.

    5. Replace the `loadNormen` validation guard with a strengthened version. Use `Number.isFinite()` instead of `typeof === 'number'` for each field. Additionally enforce per-field range: sbl (1–19), sbc (1–19), negatiefTotaal (1–19), negatiefPerLeerlijn (1–6), bj1Positief (1–19), versneldLesgeven (1–6), versneldOrganiseren (1–6), versneldProfHandelen (1–6). When any field fails either check, log the specific failure: `console.warn('[normen.ts] invalid field 'FIELDNAME': ', raw.FIELDNAME)` (replace FIELDNAME with the actual field name string literal), then fall back to `{ ...DEFAULT_NORMEN }` for the entire object (full fallback strategy — simpler than per-field patching). The log fires once per invalid field before the fallback return.

    6. In `saveNormen`: the catch block must call `console.error('[normen.ts] saveNormen failed — settings not persisted')` (change from `console.warn` used in verzuimDrempels.ts — the distinction signals a persistence failure, not merely a read issue) and return false.

    7. Add a new exported `resetNormen()` function: `async function resetNormen(): Promise<Normen> { await saveNormen({ ...DEFAULT_NORMEN }); return DEFAULT_NORMEN; }`.

    8. Preserve every other pattern from the template: LazyStore instantiation options (`{ defaults: {}, autoSave: false }`), instant-apply `_cache = normen` BEFORE the first await in saveNormen, `store.set(...)` followed by `store.save()` (both awaited), `_cache` typed as `Normen | null` initialised to null.

    9. Keep the same export ordering and JSDoc comment style as verzuimDrempels.ts.

    10. Do not add any new functionality beyond the 5 exports listed in the interfaces block.
  </action>
  <verify>
    <automated>npx vitest run tests/normen.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `utils/normen.ts` exists.
    - `grep -c "from '@tauri-apps/plugin-store'" utils/normen.ts` = 1.
    - `grep -c "STORE_KEY = 'doorstroom_normen'" utils/normen.ts` = 1.
    - `grep -c "export const DEFAULT_NORMEN" utils/normen.ts` = 1.
    - `grep -c "export function getNormenSync" utils/normen.ts` = 1.
    - `grep -c "export async function loadNormen" utils/normen.ts` = 1.
    - `grep -c "export async function saveNormen" utils/normen.ts` = 1.
    - `grep -c "export async function resetNormen" utils/normen.ts` = 1.
    - `grep -c "await store.save()" utils/normen.ts` ≥ 1 (Phase 12 pitfall mitigation).
    - The line setting `_cache = normen` inside `saveNormen` appears BEFORE the first `await` in that function (instant-apply — verify by reading the saveNormen body).
    - Validation uses `Number.isFinite`: `grep -c "Number.isFinite" utils/normen.ts` ≥ 8 (one per field).
    - Field-level error logging exists: `grep -c "invalid field" utils/normen.ts` ≥ 1 (the logging template from the action step).
    - Persistence failure logging uses console.error: `grep -c "console.error.*saveNormen failed" utils/normen.ts` = 1.
    - `npx vitest run tests/normen.test.ts`: all 5 tests PASS (GREEN).
  </acceptance_criteria>
  <done>utils/normen.ts shipped; all 5 normen unit tests green; Number.isFinite + range validation; field-level error logging; saveNormen logs console.error on failure; LazyStore patterns from verzuimDrempels.ts replicated correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor utils/prognosis.ts — accept normen param, replace all 8 hardcoded threshold sites (main logic + gaps)</name>
  <files>utils/prognosis.ts</files>
  <read_first>
    - utils/prognosis.ts (entire file — must understand every threshold occurrence; the PATTERNS.md table cites lines ~127–207)
    - .planning/phases/25-doorstroomnorm-configuratie/25-PATTERNS.md (the line-by-line replacement table in `utils/prognosis.ts` section — this is the implementation checklist)
    - .planning/phases/25-doorstroomnorm-configuratie/25-RESEARCH.md (Pitfall 1 — gaps object hardcoding is the highest-risk failure mode)
    - tests/prognosis.normen.test.ts (the 7 new GREEN targets from Plan 01 Task 2)
    - utils/normen.ts (created in Task 1 — exports getNormenSync, type Normen)
  </read_first>
  <behavior>
    After this task:
    - `berekenPrognose` signature changes to `(student, traject?, activeDeelgebiedenIds?, normen?: Normen)`.
    - Inside the function body, a single line `const n = normen ?? getNormenSync();` is added at the top (before any threshold reference).
    - The `VERSNELD_BJ1` module-level constant (~lines 28–32) is removed — all references are replaced by `n.versneldLesgeven`, `n.versneldOrganiseren`, `n.versneldProfHandelen`.
    - Every literal `13` used as a threshold in BJ1-positief or BJ2-SBL contexts becomes `n.bj1Positief` or `n.sbl` per the replacement table.
    - Every literal `15` used as the SBC threshold becomes `n.sbc`.
    - Every literal `6` used as negatief-totaal threshold becomes `n.negatiefTotaal`.
    - Every literal `2` used as negatief-per-leerlijn threshold becomes `n.negatiefPerLeerlijn`.
    - The `gaps` sub-object (Pitfall 1) gets the same treatment — `gaps.nodigBJ2`, `gaps.nodigSBL`, `gaps.nodigSBC_deelgebieden`, `gaps.onvoldoendeRuimte`, `gaps.onvoldoendeRuimtePerLeerlijn.*` — every hardcoded number is replaced.
    - All existing tests in tests/prognosis.test.ts continue to pass (calls without 4th arg fall back to `getNormenSync()` which returns DEFAULT_NORMEN → numerically identical to today).
    - The 7 new tests in tests/prognosis.normen.test.ts all turn GREEN.
  </behavior>
  <action>
    Edit `utils/prognosis.ts`. Apply changes in this order:

    1. At the top of the file, add `import { getNormenSync, type Normen } from './normen';` alongside existing imports.

    2. Update the `berekenPrognose` function signature to accept an optional fourth parameter `normen?: Normen`.

    3. As the first statement inside the function body, declare `const n = normen ?? getNormenSync();`.

    4. Walk the function top-to-bottom and apply EVERY replacement from the 25-PATTERNS.md table — do not stop at the main conditionals; the `gaps` object replacements are mandatory (Pitfall 1). Reference identifiers: `n.sbl`, `n.sbc`, `n.negatiefTotaal`, `n.negatiefPerLeerlijn`, `n.bj1Positief`, `n.versneldLesgeven`, `n.versneldOrganiseren`, `n.versneldProfHandelen`.

    5. Remove the `VERSNELD_BJ1` module-level constant (~lines 28–32). Replace all remaining references with the appropriate `n.versneld*` identifier.

    6. DO NOT touch `KERN_SBC` (it is a label list, not a configurable threshold — see RESEARCH.md note).

    7. After making all edits, search for any remaining literal threshold values in conditional or gap-arithmetic positions: `grep -nE '> ?6\b|> ?2\b|>= ?13\b|>= ?15\b|13 ?-|15 ?-|6 ?-|2 ?-' utils/prognosis.ts | grep -v '^\s*//'` — every hit must be explained (e.g., it is part of an unrelated calculation), or replaced.

    8. Do not change the function's return shape. The keys of `result` and `result.gaps` must remain identical to today.
  </action>
  <verify>
    <automated>npx vitest run tests/prognosis.normen.test.ts tests/prognosis.test.ts tests/normen.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `utils/prognosis.ts` contains the import `from './normen'` (`grep -c "from './normen'" utils/prognosis.ts` = 1).
    - `utils/prognosis.ts` contains `const n = normen ?? getNormenSync();` (exact or with whitespace variation — `grep -cE "normen \?\? getNormenSync\(\)" utils/prognosis.ts` = 1).
    - The `berekenPrognose` signature has 4 parameters where the 4th is typed as optional `Normen`: `grep -nE "function berekenPrognose\(.*normen\?:\s*Normen" utils/prognosis.ts | wc -l` ≥ 1.
    - `VERSNELD_BJ1` is removed: `grep -c "VERSNELD_BJ1" utils/prognosis.ts` = 0.
    - The 8 normen field references appear in the file:
      - `grep -c "n\.sbl\b" utils/prognosis.ts` ≥ 2 (main + gaps)
      - `grep -c "n\.sbc\b" utils/prognosis.ts` ≥ 2 (main + gaps)
      - `grep -c "n\.negatiefTotaal\b" utils/prognosis.ts` ≥ 2
      - `grep -c "n\.negatiefPerLeerlijn\b" utils/prognosis.ts` ≥ 2
      - `grep -c "n\.bj1Positief\b" utils/prognosis.ts` ≥ 2 (conditional + gap)
      - `grep -c "n\.versneldLesgeven\b" utils/prognosis.ts` ≥ 1
      - `grep -c "n\.versneldOrganiseren\b" utils/prognosis.ts` ≥ 1
      - `grep -c "n\.versneldProfHandelen\b" utils/prognosis.ts` ≥ 1
    - `npx vitest run tests/prognosis.normen.test.ts`: all 7 tests GREEN (including Tests A–G with exact label assertions).
    - `npx vitest run tests/prognosis.test.ts`: all existing prognosis tests GREEN (fallback path to getNormenSync=DEFAULT_NORMEN ensures behavioural equivalence).
    - `npx vitest run tests/normen.test.ts`: still green (no regression from Task 1).
    - `npx vitest run`: full suite green — no other test file fails.
    - The result shape of `berekenPrognose` is unchanged: fields `result.gaps.nodigSBL`, `result.gaps.onvoldoendeRuimte`, `result.label`, `result.isNegatief` all still exist.
  </acceptance_criteria>
  <done>utils/prognosis.ts refactored — all 8 hardcoded threshold sites parametrised (main conditionals AND gaps); VERSNELD_BJ1 removed; 12/12 Plan 01 tests GREEN; full suite green; no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → app (store.json read) | Untrusted JSON deserialised at startup via `store.get('doorstroom_normen')` — could be corrupt or hand-edited |
| settings UI → utils/normen.ts | Input validated upstream (Plan 03 — min/max clamp + Math.round), but normen.ts must not assume clean input |
| utils/normen.ts → utils/prognosis.ts (hot path) | Sync read — cached value after loadNormen pre-warm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Tampering | store.json `doorstroom_normen` key | mitigate | `loadNormen` validation guard uses `Number.isFinite()` for all 8 fields AND enforces per-field min/max ranges (sbl 1–19, negatiefPerLeerlijn 1–6, etc.). On any field failing either check, the offending field name is logged (`[normen.ts] invalid field 'X': value`) and the entire object falls back to DEFAULT_NORMEN. Acceptance criterion verifies Number.isFinite usage count ≥ 8. |
| T-25-04 | Denial of Service | berekenPrognose hot path | accept | Function is pure + sync; reading `n.*` integer fields is O(1). No remote calls, no I/O. |
| T-25-05 | Information Disclosure | console.warn on store read errors | mitigate | Error messages prefixed `[normen.ts]`; no user data logged — only the error message string from caught exception. Same posture as verzuimDrempels.ts. |
| T-25-06 | Repudiation / Tampering | store.set without store.save | mitigate | Acceptance criterion verifies `store.save()` follows `store.set()` (Phase 12 VERPLICHT pairing) — prevents silent loss of saved normen across app restart. |
| T-25-07 | Tampering | Silent persistence failure in saveNormen | mitigate | saveNormen catch block calls `console.error('[normen.ts] saveNormen failed — settings not persisted')` and returns false. Acceptance criterion `grep -c "console.error.*saveNormen failed" utils/normen.ts` = 1. |
| T-25-08 | Tampering | Negative / non-finite values bypass typeof guard | mitigate | `Number.isFinite()` rejects NaN, Infinity, -Infinity. Range checks (min=1) reject negative and zero values. These inputs cannot be stored as valid normen. |

No high-severity threats. ASVS V5 Input Validation satisfied at the persistence boundary.
</threat_model>

<verification>
- `npx vitest run tests/normen.test.ts` → 5/5 green.
- `npx vitest run tests/prognosis.normen.test.ts` → 7/7 green (includes exact label assertions: 'sbl', 'sbc', 'naar_bj2', 'versneld_sbc').
- `npx vitest run tests/prognosis.test.ts` → all existing tests green (no regression).
- `npx vitest run` → full suite green (132+ tests baseline per STATE.md Phase 23 line).
- `grep -c "VERSNELD_BJ1" utils/prognosis.ts` = 0 (constant removed).
- `grep -c "Number.isFinite" utils/normen.ts` ≥ 8 (one per field in the validation guard).
- `grep -c "console.error.*saveNormen failed" utils/normen.ts` = 1 (persistence failure is not silent).
- `grep -c "invalid field" utils/normen.ts` ≥ 1 (field-level error logging present).
- `grep -nE '\b(>=|>) ?(13|15)\b' utils/prognosis.ts | grep -v '^\s*//'` returns no threshold-conditional lines (or every hit is explained as unrelated to norm logic).
</verification>

<success_criteria>
- utils/normen.ts created, modelled on verzuimDrempels.ts, with all 5 expected exports.
- loadNormen uses Number.isFinite + range enforcement — rejecting negative, non-finite, and out-of-range values.
- Field-level logging fires for each invalid field before falling back to DEFAULT_NORMEN.
- saveNormen failure is surfaced via console.error — no silent persistence loss.
- utils/prognosis.ts berekenPrognose accepts optional Normen parameter and uses `n.*` for every threshold (main + gaps).
- All RED tests from Plan 01 are now GREEN (5 normen unit tests + 7 prognosis.normen integration tests).
- No regression in any other existing test file.
- berekenPrognose return shape unchanged — only the values driving it are now configurable.
- File contains no remaining literal threshold magic numbers (13, 15, 6, 2) in conditional positions for SBL/SBC/negatief logic.
</success_criteria>

<output>
After completion, create `.planning/phases/25-doorstroomnorm-configuratie/25-02-SUMMARY.md` describing:
- The exact set of 8 hardcoded sites replaced in utils/prognosis.ts (with line numbers after refactor, for downstream verification).
- Confirmation that VERSNELD_BJ1 is removed.
- Validation strategy used in loadNormen (Number.isFinite + range) and the field-name logging format.
- Pass count for tests/normen.test.ts (5/5), tests/prognosis.normen.test.ts (7/7), tests/prognosis.test.ts (full green), full suite.
- Any deviation from the PATTERNS.md table (with rationale).
</output>
