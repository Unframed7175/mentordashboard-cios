---
phase: 25-doorstroomnorm-configuratie
plan: 03
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - src/main.tsx
  - src/App.tsx
  - src/components/SettingsPage.tsx
  - src/index.css
  - tests/SettingsPage.test.tsx
autonomous: false
requirements: [NORM-01, NORM-02, NORM-03, NORM-04, NORM-05, NORM-06, NORM-07]
tags: [ui, settings, react, css, test]

must_haves:
  truths:
    - "main.tsx pre-warms loadNormen() inside the existing Promise.all block before React mounts"
    - "App.tsx exposes handleNormenChanged which calls setRefreshKey(k => k + 1) and passes it to SettingsPage"
    - "SettingsPage renders a new Section 5 'Doorstroomdrempels' with two sub-blocks (BJ2 + BJ1) totaling 8 number inputs"
    - "Each input saves on blur AND on Enter; decimals are rounded; values are clamped to per-field min/max from D-09"
    - "saveNormen failure (returns false) is detected and logged — it is never silently swallowed in the UI handler"
    - "SBC < SBL displays orange inline warning text below the SBC row — no blocking, no save abort"
    - "A 'Herstel standaard' two-step confirm button restores DEFAULT_NORMEN and triggers onNormenChanged"
    - "After threshold change in Settings, returning to KlasOverzicht shows recomputed RAG colours without app restart"
    - "Saved thresholds survive app restart (round-trip through store.json doorstroom_normen)"
    - "tests/SettingsPage.test.tsx extended with Section 5 coverage: section renders, blur triggers save, Enter triggers blur, SBC<SBL warning appears, reset confirmation flow, onNormenChanged callback fires"
  artifacts:
    - path: "src/main.tsx"
      provides: "loadNormen() added to Promise.all pre-warm"
      contains: "loadNormen"
    - path: "src/App.tsx"
      provides: "handleNormenChanged + onNormenChanged prop on SettingsPage"
      contains: "handleNormenChanged"
    - path: "src/components/SettingsPage.tsx"
      provides: "Section 5 with 8 inputs + reset + SBC<SBL warning + onNormenChanged callback + persistence-failure logging"
      contains: "Doorstroomdrempels"
    - path: "src/index.css"
      provides: ".settings-sub-heading + .norm-warning CSS rules"
      contains: ".norm-warning"
    - path: "tests/SettingsPage.test.tsx"
      provides: "Section 5 automated coverage: render, blur-save, Enter-blur, warning, reset flow, callback"
      contains: "Doorstroomdrempels"
  key_links:
    - from: "src/main.tsx"
      to: "utils/normen.ts loadNormen"
      via: "Promise.all([... , loadNormen()])"
      pattern: "loadNormen\\(\\)"
    - from: "src/App.tsx handleNormenChanged"
      to: "setRefreshKey"
      via: "setRefreshKey(k => k + 1)"
      pattern: "setRefreshKey\\(k => k \\+ 1\\)"
    - from: "src/App.tsx <SettingsPage>"
      to: "SettingsPage onNormenChanged prop"
      via: "<SettingsPage ... onNormenChanged={handleNormenChanged} />"
      pattern: "onNormenChanged=\\{handleNormenChanged\\}"
    - from: "SettingsPage Section 5 blur handler"
      to: "utils/normen.ts saveNormen"
      via: "const ok = await saveNormen(updatedNormen); if (!ok) { console.error('[SettingsPage] saveNormen failed'); } onNormenChanged();"
      pattern: "saveNormen\\("
---

<objective>
Ship the UI surface and the runtime wiring for Phase 25. After this plan completes:
1. App startup pre-warms the normen sync cache (no first-render flicker / wrong RAG colour).
2. SettingsPage has a new Section 5 "Doorstroomdrempels" with 8 number inputs, an inline SBC<SBL warning, and a two-step reset button — per the 25-UI-SPEC.md contract.
3. Editing any input and blurring / pressing Enter saves to plugin-store, updates the sync cache instantly, and triggers `onNormenChanged()` which bumps refreshKey in App.tsx — causing KlasOverzicht to recompute RAG colours next render.
4. Persistence failures from `saveNormen()` returning false are detected and logged via console.error — they are never silently swallowed.
5. The "Herstel standaard" button restores DEFAULT_NORMEN with a confirm step.
6. Automated tests in tests/SettingsPage.test.tsx cover the Section 5 behaviour (render, blur-save, Enter-blur, SBC<SBL warning, reset confirmation, onNormenChanged callback firing).

Purpose: This closes NORM-01 through NORM-07 end-to-end — UI to persistence to prognose-engine to RAG tile colour. The utils refactor from Plan 02 is invisible to the user without this UI layer. Plan 04 (DoortstroomPrognoseSection.tsx) completes the final visible layer by parameterizing hardcoded norm text in the student detail view.

Output: Working settings UI, wired callback, pre-warmed cache, new CSS rules, extended test coverage, manual human-verification checkpoint confirming the round-trip works visually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-doorstroomnorm-configuratie/25-CONTEXT.md
@.planning/phases/25-doorstroomnorm-configuratie/25-RESEARCH.md
@.planning/phases/25-doorstroomnorm-configuratie/25-PATTERNS.md
@.planning/phases/25-doorstroomnorm-configuratie/25-UI-SPEC.md
@.planning/phases/25-doorstroomnorm-configuratie/25-02-SUMMARY.md

<interfaces>
<!-- SettingsPage prop interface extension required by App.tsx wiring -->

```typescript
interface SettingsPageProps {
  onBack: () => void;
  onNavigateToImport: () => void;
  isDark: boolean;
  onToggleDark: (isDark: boolean) => void;
  onNormenChanged: () => void; // NEW — invoked after each saveNormen + after resetNormen
}
```

<!-- The full UI structure, copy, validation table, and CSS are defined in 25-UI-SPEC.md.
     Executor must follow it byte-for-byte. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-warm loadNormen in main.tsx + add handleNormenChanged + Section 5 wiring scaffold in App.tsx</name>
  <files>src/main.tsx, src/App.tsx</files>
  <read_first>
    - src/main.tsx (entire file — see existing Promise.all pre-warm block ~lines 43–54 per PATTERNS.md)
    - src/App.tsx (entire file — see existing refreshKey usage on lines ~18, 52–55, 76–78 and existing SettingsPage mount on ~lines 144–154 per PATTERNS.md)
    - utils/normen.ts (Plan 02 — confirm `loadNormen` export and signature)
    - .planning/phases/25-doorstroomnorm-configuratie/25-PATTERNS.md (section `src/main.tsx` and `src/App.tsx`)
    - .planning/phases/25-doorstroomnorm-configuratie/25-RESEARCH.md (Pitfall 4 — refreshKey is the single correct invalidation hook for KlasOverzicht's useMemo)
  </read_first>
  <action>
    main.tsx:
    - Add import `import { loadNormen } from '../utils/normen';` alongside the existing loadVerzuimDrempels / getBpvConfig / etc. imports (match the existing relative path and import style used by those imports).
    - In the existing `await Promise.all([...])` pre-warm block, append `loadNormen()` as a new entry. Add an inline comment `// Phase 25 — pre-warm doorstroom normen sync cache` on the same line or directly above.
    - Do not modify anything else in main.tsx.

    App.tsx:
    - Locate the existing `setRefreshKey` usage near handleImportComplete / handleKlasSwitch (PATTERNS.md cites lines 52–55, 76–78). Define a new handler function alongside them: `function handleNormenChanged() { setRefreshKey(k => k + 1); }`.
    - In the SettingsPage mount block (PATTERNS.md cites lines 144–154), add the prop `onNormenChanged={handleNormenChanged}` to `<SettingsPage ... />`.
    - Do NOT add a new state variable — refreshKey is sufficient (RESEARCH.md Open Question 2 resolution).
    - Do not modify any other parts of App.tsx.

    Note: SettingsPage will receive a prop its current TypeScript interface does not yet declare — Task 2 adds that prop to SettingsPageProps. To avoid a dangling typecheck gap, complete Task 1 and Task 2 in the same session so the SettingsPageProps gap is closed before the test suite runs.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "loadNormen" src/main.tsx` ≥ 2 (one import line + one call inside Promise.all).
    - `grep -c "Promise.all" src/main.tsx` ≥ 1, and `loadNormen()` appears between `Promise.all([` and the closing `])` of the pre-warm block (verify by reading the edited file).
    - `grep -c "handleNormenChanged" src/App.tsx` ≥ 2 (one function declaration, one prop pass).
    - `grep -c "onNormenChanged={handleNormenChanged}" src/App.tsx` = 1.
    - No new `useState` is introduced in App.tsx for normen (RESEARCH Open Question 2 resolution — refreshKey is reused).
    - `npx vitest run` full suite passes (if SettingsPage tests fail with TS prop errors only, that is acceptable as long as Task 2 is applied in the same session to restore green).
  </acceptance_criteria>
  <done>main.tsx pre-warms loadNormen; App.tsx has handleNormenChanged passed as onNormenChanged to SettingsPage; refreshKey reused; tests still pass (after Task 2 completes the SettingsPageProps extension).</done>
</task>

<task type="auto">
  <name>Task 2: SettingsPage Section 5 — 8 inputs + warning + reset + onNormenChanged prop + persistence-failure logging + new CSS rules</name>
  <files>src/components/SettingsPage.tsx, src/index.css</files>
  <read_first>
    - src/components/SettingsPage.tsx (entire file — must understand current state, existing Section 4 number-input pattern around lines 312–384, Section 3 reset two-button pattern around lines 289–309, props interface around lines 25–30)
    - .planning/phases/25-doorstroomnorm-configuratie/25-UI-SPEC.md (the canonical visual + copy + validation + CSS contract — follow byte-for-byte)
    - .planning/phases/25-doorstroomnorm-configuratie/25-PATTERNS.md (section `src/components/SettingsPage.tsx` — JSX snippets, state shape, handler shape)
    - .planning/phases/25-doorstroomnorm-configuratie/25-CONTEXT.md (D-01 placement, D-02 reset, D-03 two blocks, D-05 BJ1 includes both bj1Positief and versneld trio, D-06 blur/Enter trigger, D-07 instant-save, D-09 input ranges + Math.round, D-10 warning copy)
    - utils/normen.ts (Plan 02 — confirm exports: loadNormen, saveNormen, resetNormen, DEFAULT_NORMEN, type Normen)
    - src/index.css (find CSS section for Phase 18 settings styles — Section 5 styles append here)
  </read_first>
  <action>
    SettingsPage.tsx — additive changes only, do not modify existing Sections 1–4:

    1. Add import after the existing utils imports:
       `import { loadNormen, saveNormen, resetNormen, DEFAULT_NORMEN, type Normen } from '../../utils/normen';`
       (match the relative path used by existing utils imports such as `loadVerzuimDrempels`; adjust `../../` vs `../` to match the existing import paths.)

    2. Extend `SettingsPageProps` with `onNormenChanged: () => void;` (D-08 callback). Existing props are unchanged. Update any existing SettingsPage test invocations that now receive a missing-prop TypeScript error — pass `onNormenChanged={() => {}}` as a no-op stub in those test render calls (this is done in Task 3, but note the dependency so Tasks 2 and 3 are applied in the same session).

    3. Inside the component body, add two new state declarations near the existing Section 4 state:
       `const [normen, setNormen] = useState<Normen>(DEFAULT_NORMEN);`
       `const [confirmingResetNormen, setConfirmingResetNormen] = useState(false);`

    4. Add a separate `useEffect(() => { loadNormen().then(setNormen).catch(err => console.warn('[SettingsPage] section 5 load failed:', err)); }, []);` near the existing Section 4 load effect. Keep the Section 4 load effect untouched.

    5. Add a single shared blur handler for all 8 inputs:
       `async function handleNormenBlur(field: keyof Normen, rawValue: number, min: number, max: number) {`
         `  const rounded = Math.round(Number.isFinite(rawValue) ? rawValue : DEFAULT_NORMEN[field]);`
         `  const clamped = Math.max(min, Math.min(max, rounded));`
         `  const updated = { ...normen, [field]: clamped };`
         `  setNormen(updated);`
         `  const ok = await saveNormen(updated);`
         `  if (!ok) { console.error('[SettingsPage] saveNormen returned false — doorstroom norm niet opgeslagen'); }`
         `  onNormenChanged();`
       `}`
       (D-09 clamp + round; D-07 instant-save via saveNormen which updates sync cache before await per Plan 02; persistence failure is detected and logged, not silently swallowed.)

    6. Add a reset handler:
       `async function handleResetNormen() { const fresh = await resetNormen(); setNormen(fresh); setConfirmingResetNormen(false); onNormenChanged(); }`

    7. Compute the warning predicate once per render: `const sbcWaarschuwing = normen.sbc < normen.sbl;` (D-10).

    8. Append a new `<section className="detail-section">` AFTER Section 4's closing `</section>` (D-01). The section structure must follow 25-UI-SPEC.md "Layout: Section 5 Structure" exactly:
       - `<h2 className="detail-section-title">Doorstroomdrempels</h2>`
       - Sub-block 1 with heading `<p className="settings-sub-heading">BJ2-drempels</p>` inside a `<div className="settings-threshold-group">` containing 4 `<div className="settings-threshold-row">` rows: sbl (min=1, max=19, unit `≥V`), sbc (min=1, max=19, unit `≥V`), negatiefTotaal (min=1, max=19, unit `O totaal`), negatiefPerLeerlijn (min=1, max=6, unit `O per leerlijn`).
       - The SBC warning paragraph `{sbcWaarschuwing && (<p className="norm-warning" role="status">Let op: SBC-drempel is normaal hoger dan SBL-drempel (standaard: 15 vs 13).</p>)}` rendered AFTER the SBC row but inside the BJ2 group.
       - A `<hr style={{ border: 'none', borderTop: '1px solid var(--border-light)', margin: '16px 0' }} />` divider.
       - Sub-block 2 with heading `<p className="settings-sub-heading">BJ1-drempels</p>` inside another `<div className="settings-threshold-group">` with 4 rows: bj1Positief (min=1, max=19, unit `≥V`), versneldLesgeven (min=1, max=6, unit `≥G`), versneldOrganiseren (min=1, max=6, unit `≥G`), versneldProfHandelen (min=1, max=6, unit `≥G`).
       - Two-step reset block using `confirmingResetNormen` state — follow the exact Section 3 pattern from 25-PATTERNS.md (btn btn-ghost "Herstel standaard" → confirm row with "Alles terugzetten naar CIOS-standaard?" + "Niet herstellen" + "Ja, herstel" buttons).

    9. Every `<input>` follows this pattern (adapt field name, min, max):
       `<input type="number" className="settings-number-input" id="norm-{field}" min={min} max={max} step={1} value={normen.{field}} onChange={e => setNormen(n => ({ ...n, {field}: Number(e.target.value) }))} onBlur={() => handleNormenBlur('{field}', normen.{field}, {min}, {max})} onKeyDown={e => { if (e.key === 'Enter') (e.target as HTMLInputElement).blur(); }} />`
       Each input has a matching `<label htmlFor="norm-{field}">…</label>` per the 25-UI-SPEC.md Accessibility Contract. All copy strings come from the UI-SPEC Copywriting Contract table.

    src/index.css — append to the end of the existing Section 25 / Settings styles block:
    - `.settings-sub-heading { font-size: 0.875rem; font-weight: 600; color: var(--text-secondary); margin-bottom: 8px; }`
    - `.norm-warning { font-size: 0.875rem; color: var(--status-oranje-text); margin-top: 4px; }`
    Do not modify any existing CSS rule.
  </action>
  <verify>
    <automated>npx vitest run src/components/SettingsPage.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '../../utils/normen'" src/components/SettingsPage.tsx` ≥ 1 (or matching path from existing imports).
    - `grep -c "Doorstroomdrempels" src/components/SettingsPage.tsx` = 1 (the h2 title text).
    - `grep -c "BJ2-drempels" src/components/SettingsPage.tsx` = 1.
    - `grep -c "BJ1-drempels" src/components/SettingsPage.tsx` = 1.
    - `grep -cE "normen\.(sbl|sbc|negatiefTotaal|negatiefPerLeerlijn|bj1Positief|versneldLesgeven|versneldOrganiseren|versneldProfHandelen)" src/components/SettingsPage.tsx` ≥ 8.
    - `grep -c "handleNormenBlur" src/components/SettingsPage.tsx` ≥ 9 (1 declaration + 8 onBlur call sites).
    - `grep -c "Math.round" src/components/SettingsPage.tsx` ≥ 1 inside handleNormenBlur.
    - `grep -c "saveNormen" src/components/SettingsPage.tsx` ≥ 1.
    - `grep -c "console.error.*saveNormen returned false" src/components/SettingsPage.tsx` = 1 (persistence failure not silently swallowed).
    - `grep -c "resetNormen" src/components/SettingsPage.tsx` ≥ 1.
    - `grep -c "onNormenChanged" src/components/SettingsPage.tsx` ≥ 3 (props interface, save handler call, reset handler call).
    - `grep -c "sbcWaarschuwing" src/components/SettingsPage.tsx` ≥ 2 (declaration + render guard).
    - `grep -c "Let op: SBC-drempel is normaal hoger dan SBL-drempel" src/components/SettingsPage.tsx` = 1.
    - `grep -c "Alles terugzetten naar CIOS-standaard" src/components/SettingsPage.tsx` = 1.
    - `grep -c "confirmingResetNormen" src/components/SettingsPage.tsx` ≥ 3 (state declaration, render guard, button click).
    - `grep -cE 'htmlFor="norm-(sbl|sbc|negatiefTotaal|negatiefPerLeerlijn|bj1Positief|versneldLesgeven|versneldOrganiseren|versneldProfHandelen)"' src/components/SettingsPage.tsx` = 8.
    - `grep -c "\.norm-warning" src/index.css` = 1.
    - `grep -c "\.settings-sub-heading" src/index.css` = 1.
  </acceptance_criteria>
  <done>Section 5 fully rendered with 8 inputs + warning + reset + onNormenChanged callback + persistence-failure logging; new CSS rules appended; TypeScript interface extended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests/SettingsPage.test.tsx with Section 5 coverage</name>
  <files>tests/SettingsPage.test.tsx</files>
  <read_first>
    - tests/SettingsPage.test.tsx (entire file — understand existing test structure, render helpers, mock patterns, and which props are already provided; identify all existing render calls that now require the new `onNormenChanged` prop)
    - src/components/SettingsPage.tsx (Task 2 output — understand the Section 5 structure: state, handlers, blur/Enter logic, warning predicate, reset flow)
    - utils/normen.ts (Plan 02 — exports to mock: loadNormen, saveNormen, resetNormen, DEFAULT_NORMEN)
  </read_first>
  <behavior>
    Add a new `describe('Section 5: Doorstroomdrempels', ...)` block. Existing tests in this file are NOT removed.

    Required coverage:
    - Test 1 — section renders: render SettingsPage with `onNormenChanged={() => {}}`, assert element with text "Doorstroomdrempels" is in the document. Assert "BJ2-drempels" and "BJ1-drempels" sub-headings are present.
    - Test 2 — blur triggers saveNormen + onNormenChanged: render with `onNormenChanged` spy. Simulate user changing the SBL input value (fireEvent.change to set value to '10'), then fireEvent.blur on the SBL input. Assert saveNormen mock was called with an object where sbl=10. Assert onNormenChanged spy was called once.
    - Test 3 — Enter triggers blur (and thus save): render with spies. Simulate fireEvent.keyDown on the SBL input with key='Enter'. Assert the input loses focus (or that handleNormenBlur logic fires — can assert saveNormen was called, since blur follows Enter per the onKeyDown handler).
    - Test 4 — SBC<SBL warning appears: render with normen state seeded so sbc < sbl (override the loadNormen mock to return `{ ...DEFAULT_NORMEN, sbc: 10, sbl: 13 }`). Assert the warning paragraph "Let op: SBC-drempel is normaal hoger dan SBL-drempel" is in the document. Then simulate setting sbc ≥ sbl value and blur, assert warning is gone (or is not in the document).
    - Test 5 — reset confirmation flow: render. Click "Herstel standaard" button. Assert confirm row with text "Alles terugzetten naar CIOS-standaard?" appears. Click "Niet herstellen". Assert confirm row disappears. Click "Herstel standaard" again. Click "Ja, herstel". Assert `resetNormen` mock was called. Assert `onNormenChanged` spy was called.
    - Test 6 — onNormenChanged callback fires after blur save: already covered partially by Test 2, but add explicit assertion that `onNormenChanged` fires exactly once per blur event (not zero times, not multiple times).

    All existing render calls in the file that pass SettingsPageProps must be updated to include `onNormenChanged={() => {}}` as a no-op stub if TypeScript now requires it. Identify these in the read_first step and patch them.

    Mock strategy for normen utils: use `vi.mock('../utils/normen', () => ({ loadNormen: vi.fn().mockResolvedValue(DEFAULT_NORMEN), saveNormen: vi.fn().mockResolvedValue(true), resetNormen: vi.fn().mockResolvedValue(DEFAULT_NORMEN), DEFAULT_NORMEN: { sbl:13, sbc:15, negatiefTotaal:6, negatiefPerLeerlijn:2, bj1Positief:13, versneldLesgeven:4, versneldOrganiseren:3, versneldProfHandelen:5 }, }))` (or adapt to match the import path used by SettingsPage.tsx). Mock must be at the top of the new describe block or at file level if consistent with the existing mock pattern.
  </behavior>
  <action>
    Open `tests/SettingsPage.test.tsx`.

    1. First, scan ALL existing `render(<SettingsPage ...` call sites. Add `onNormenChanged={() => {}}` to every render that does not already have it. This prevents TypeScript errors from the newly required prop.

    2. Add a `vi.mock` for `../../utils/normen` (or the correct relative path from the test file's perspective — verify by checking the existing import in SettingsPage.tsx and the test file's location). Mock all four exports: `loadNormen`, `saveNormen`, `resetNormen`, `DEFAULT_NORMEN`.

    3. Append a `describe('Section 5: Doorstroomdrempels', () => { ... })` block after the existing describe blocks. Inside, add the 6 tests from the behavior section above.

    4. Use `@testing-library/react` query methods consistent with the existing test file (`getByText`, `getByRole`, `getByLabelText`, `queryByText`, `fireEvent.change`, `fireEvent.blur`, `fireEvent.keyDown`, `userEvent.*` if present).

    5. Each test wraps async operations in `act(...)` or uses `await` with `waitFor(...)` per the existing test patterns.

    6. Do NOT modify the Section 1–4 test logic.
  </action>
  <verify>
    <automated>npx vitest run tests/SettingsPage.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/SettingsPage.test.tsx` passes — all tests green (both existing and new Section 5 tests).
    - `grep -c "Section 5: Doorstroomdrempels" tests/SettingsPage.test.tsx` = 1 (the describe block label).
    - `grep -c "Doorstroomdrempels" tests/SettingsPage.test.tsx` ≥ 2 (describe + at least one assertion).
    - `grep -c "saveNormen" tests/SettingsPage.test.tsx` ≥ 1 (mock + assertion that saveNormen is called on blur).
    - `grep -c "resetNormen" tests/SettingsPage.test.tsx` ≥ 1 (reset flow test assertion).
    - `grep -c "onNormenChanged" tests/SettingsPage.test.tsx` ≥ 2 (no-op prop in render calls + spy assertion in blur test).
    - `grep -c "Alles terugzetten naar CIOS-standaard" tests/SettingsPage.test.tsx` ≥ 1 (reset confirmation flow test).
    - `grep -c "SBC-drempel is normaal hoger" tests/SettingsPage.test.tsx` ≥ 1 (SBC<SBL warning test).
    - `npx vitest run` full suite green (all plans passing, no regressions).
  </acceptance_criteria>
  <done>tests/SettingsPage.test.tsx extended with 6 Section 5 tests; all existing tests still pass; saveNormen, resetNormen, onNormenChanged all covered by automated assertions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify end-to-end Doorstroomdrempels round-trip</name>
  <what-built>
    Phase 25 settings UI is live. Mentor can edit 8 doorstroomdrempels in Settings, the prognose engine recomputes instantly using those values, RAG colours in KlasOverzicht update without app restart, the values persist across app restart, and "Herstel standaard" restores the CIOS defaults. Note: DoortstroomPrognoseSection.tsx (the detail-view norm text) is parameterized in Plan 04 — this checkpoint covers the settings and KlasOverzicht layers.
  </what-built>
  <how-to-verify>
    Prerequisite: At least one klas with one student that has a prognose label other than "groen" (so threshold changes are observable).

    1. Run `npm run dev`. App opens in Tauri window.
    2. Open Settings (gear icon in KlasTabStrip). Scroll past Section 4 — confirm new Section 5 titled "Doorstroomdrempels" appears.
    3. Verify visual layout matches 25-UI-SPEC.md:
       - Two sub-block headings "BJ2-drempels" (top) and "BJ1-drempels" (bottom), separated by a thin divider line.
       - 8 number inputs total — 4 per block — each with a visible label and unit hint.
       - Defaults: SBL=13, SBC=15, Negatief totaal=6, Negatief per leerlijn=2, BJ1-positief=13, Versneld lesgeven=4, organiseren=3, prof. handelen=5.
       - "Herstel standaard" button appears below the BJ1 block.
    4. Test blur-trigger save (D-06, D-07):
       - Click into the SBL input. Type "8". DO NOT blur yet — confirm KlasOverzicht colours have NOT changed.
       - Click outside (blur). Navigate back to KlasOverzicht. Verify at least one student tile colour changed.
    5. Test Enter-trigger save: re-open Settings → Section 5. Change SBC to "10". Press Enter. Confirm save (no error).
    6. Test SBC < SBL warning (D-10):
       - Set SBL=15, SBC=10 (blur each). Confirm orange warning paragraph appears below the SBC row: "Let op: SBC-drempel is normaal hoger dan SBL-drempel (standaard: 15 vs 13)." — save still proceeds.
       - Restore SBC=15. Warning disappears.
    7. Test decimal rounding (D-09 / Pitfall 5): enter "13.7" in SBL, blur. Confirm input snaps to "14".
    8. Test clamping (D-09): enter "999" in any input, blur. Confirm value clamps to the field max. Enter "0", blur. Confirm value clamps to 1.
    9. Test persistence (NORM-06): set SBL=10, SBC=12. Close Tauri app entirely. Re-open. Open Settings → Section 5. Confirm SBL=10, SBC=12 are still shown.
    10. Test reset (NORM-07): click "Herstel standaard". Confirm confirm row appears with "Alles terugzetten naar CIOS-standaard?" and two buttons. Click "Ja, herstel". Confirm all 8 inputs revert to defaults (13, 15, 6, 2, 13, 4, 3, 5). Navigate back to KlasOverzicht — RAG colours reflect default thresholds.
    11. Accessibility quick check: Tab through Section 5 inputs. Each input gets a visible focus ring. Each label is clickable and focuses the input.
    12. Dark mode check: toggle dark mode. Section 5 inputs, labels, warning text, and reset buttons all render legibly.

    If any step fails, describe exactly what failed (step number + observed behaviour).
  </how-to-verify>
  <files>n/a — human-verification only, no files modified by this task</files>
  <action>Pause for human verification. Executor must not proceed past this task until the user types "approved" or describes failures. See how-to-verify for the 12-step manual test plan.</action>
  <verify>
    <automated>echo "Checkpoint task — manual human verification. See how-to-verify for steps."</automated>
  </verify>
  <done>User has typed "approved" after walking through all 12 verification steps (or all reported failures have been remediated and re-verified).</done>
  <resume-signal>Type "approved" if all 12 steps pass, otherwise describe failures so Plan 03 can be amended.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mentor → number input | Free-form input — could be decimal, negative, out-of-range, non-numeric (NaN from `Number("")`) |
| Settings UI → utils/normen.ts saveNormen | Goes through clamp + round before persistence |
| utils/normen.ts → store.json (plugin-store) | Tauri IPC, atomic write per LazyStore guarantees |
| saveNormen return value → SettingsPage | false return (write failure) must be detected, not silently dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Tampering | Mentor input — decimal / negative / huge values | mitigate | handleNormenBlur applies `Math.round()` (with `Number.isFinite` NaN guard) and clamps to per-field min/max (D-09) before saveNormen. Acceptance criterion verifies Math.round and console.error guard usage. Manual checkpoint steps 7+8 confirm. |
| T-25-10 | Denial of Service | Empty / non-numeric input causes NaN | mitigate | `Number.isFinite(rawValue) ? rawValue : DEFAULT_NORMEN[field]` fallback in handleNormenBlur ensures NaN never reaches saveNormen. ASVS V5 Input Validation. |
| T-25-11 | Spoofing / Information Disclosure | onNormenChanged firing during partial typing | accept | Save is gated on blur/Enter only (D-06). onChange updates local state only — never calls saveNormen. No external observer of intermediate state. |
| T-25-12 | Tampering | Stale React closure in handleNormenBlur captures old `normen` | mitigate | Handler spreads `{ ...normen, [field]: clamped }` from the latest render's closure. Blur is synchronous from user action — closure is current. Manual checkpoint step 4 (multi-edit + restart) detects any stale-write regression. |
| T-25-13 | Repudiation | saveNormen returns false with no visible indication | mitigate | handleNormenBlur checks the boolean return of saveNormen and calls `console.error('[SettingsPage] saveNormen returned false — doorstroom norm niet opgeslagen')`. Acceptance criterion verifies the console.error call. |

No high-severity threats. ASVS V5 Input Validation satisfied at the UI boundary; persistence boundary covered by Plan 02 threat model.
</threat_model>

<verification>
- Visual + functional manual checkpoint passes all 12 steps (see Task 4).
- `npx vitest run tests/SettingsPage.test.tsx` → all tests green (existing + 6 new Section 5 tests).
- `npx vitest run` full suite green (132+ tests).
- `grep -c "loadNormen" src/main.tsx` ≥ 2.
- `grep -c "handleNormenChanged" src/App.tsx` ≥ 2.
- `grep -c "Doorstroomdrempels" src/components/SettingsPage.tsx` = 1.
- `grep -c "saveNormen" src/components/SettingsPage.tsx` ≥ 1.
- `grep -c "console.error.*saveNormen returned false" src/components/SettingsPage.tsx` = 1.
- `grep -c "resetNormen" src/components/SettingsPage.tsx` ≥ 1.
- `grep -c "\.norm-warning" src/index.css` = 1.
- `grep -c "\.settings-sub-heading" src/index.css` = 1.
</verification>

<success_criteria>
- NORM-01: SBL input live, editable, persisted, recomputes prognose.
- NORM-02: SBC input live, editable, persisted, recomputes prognose.
- NORM-03: Negatief totaal input live, editable, persisted, recomputes prognose.
- NORM-04: Negatief per leerlijn input live, editable, persisted, recomputes prognose.
- NORM-05: BJ1-positief + versneld trio inputs live, editable, persisted, recomputes prognose.
- NORM-06: Custom values survive Tauri app restart (checkpoint step 9).
- NORM-07: "Herstel standaard" two-step button restores DEFAULT_NORMEN and recomputes prognose (checkpoint step 10).
- Persistence failures are surfaced via console.error in handleNormenBlur — not silently ignored.
- Manual checkpoint approved (all 12 steps).
- tests/SettingsPage.test.tsx has 6 new Section 5 tests, all green.
- No regression in existing test suite.
- Section 5 visual contract matches 25-UI-SPEC.md exactly (copy, layout, colours, typography, validation ranges).
</success_criteria>

<output>
After completion, create `.planning/phases/25-doorstroomnorm-configuratie/25-03-SUMMARY.md` describing:
- main.tsx Promise.all entries before/after.
- App.tsx handleNormenChanged added; SettingsPage prop wired.
- SettingsPage Section 5 final structure (count of inputs, blocks, warning render, reset flow, persistence-failure logging) — confirm against 25-UI-SPEC.md.
- src/index.css new rule names added.
- tests/SettingsPage.test.tsx additions: count of new tests, which behaviours covered.
- Manual checkpoint result (all steps passed or list of failures + remediation).
- Final test count and pass rate.
</output>
