---
phase: 25-doorstroomnorm-configuratie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/normen.test.ts
  - tests/prognosis.test.ts
autonomous: true
requirements: [NORM-01, NORM-02, NORM-03, NORM-04, NORM-05, NORM-06, NORM-07]
tags: [tdd, settings, prognosis, persistence]

must_haves:
  truths:
    - "tests/normen.test.ts exists with 5 RED tests covering cold cache default, save round-trip, sync accessor, instant-apply, reset"
    - "tests/prognosis.test.ts has new integration tests asserting berekenPrognose honors custom normen for sbl, sbc, negatiefTotaal, negatiefPerLeerlijn, bj1Positief, and versneld-SBC trio"
    - "Tests are RED before Plan 02 ships (normen.ts does not exist yet)"
  artifacts:
    - path: "tests/normen.test.ts"
      provides: "RED test scaffold for NORM-01..07 unit coverage"
      contains: "loadNormen, saveNormen, getNormenSync, resetNormen, DEFAULT_NORMEN"
    - path: "tests/prognosis.test.ts"
      provides: "Integration RED tests for berekenPrognose normen parameter"
      contains: "berekenPrognose("
  key_links:
    - from: "tests/normen.test.ts"
      to: "utils/normen.ts (does not yet exist)"
      via: "import { loadNormen, saveNormen, getNormenSync, resetNormen, DEFAULT_NORMEN, type Normen } from '../utils/normen'"
      pattern: "from '\\.\\./utils/normen'"
    - from: "tests/prognosis.test.ts"
      to: "utils/prognosis.ts berekenPrognose normen param"
      via: "berekenPrognose(student, 'bj2', undefined, customNormen)"
      pattern: "berekenPrognose\\(.*,.*normen"
---

<objective>
Lay the failing-test foundation for Phase 25. Create `tests/normen.test.ts` (new file) modelled byte-for-byte on `tests/verzuimDrempels.test.ts`, and extend `tests/prognosis.test.ts` with integration tests that assert `berekenPrognose()` honors a custom `normen` parameter for every NORM-XX requirement.

Purpose: Establish the RED gate that Plan 02 (utils refactor) must turn GREEN. This pins the contract for `utils/normen.ts` (interface, exports, behaviour) and `utils/prognosis.ts` (optional fourth parameter) BEFORE any implementation begins. Per the project's established TDD pattern (Phases 14, 18, 19, 23), Wave 0 tests RED → Wave 1 implementation GREEN.

Output: Two test files with failing tests that document the exact API surface and behaviour the implementation in Plan 02 must satisfy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-doorstroomnorm-configuratie/25-CONTEXT.md
@.planning/phases/25-doorstroomnorm-configuratie/25-RESEARCH.md
@.planning/phases/25-doorstroomnorm-configuratie/25-PATTERNS.md

<interfaces>
<!-- Contract that Plan 02 will implement. Tests are written against this signature now. -->

utils/normen.ts (to be created in Plan 02) must export:

```typescript
export interface Normen {
  sbl: number;                 // default 13
  sbc: number;                 // default 15
  negatiefTotaal: number;      // default 6
  negatiefPerLeerlijn: number; // default 2
  bj1Positief: number;         // default 13
  versneldLesgeven: number;    // default 4
  versneldOrganiseren: number; // default 3
  versneldProfHandelen: number;// default 5
}

export const DEFAULT_NORMEN: Normen;
export function getNormenSync(): Normen;
export async function loadNormen(): Promise<Normen>;
export async function saveNormen(normen: Normen): Promise<boolean>;
export async function resetNormen(): Promise<Normen>;
```

utils/prognosis.ts berekenPrognose (to be modified in Plan 02) must accept an optional fourth parameter:

```typescript
export function berekenPrognose(
  student: any,
  traject?: string,
  activeDeelgebiedenIds?: string[],
  normen?: Normen
): any;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/normen.test.ts (5 RED tests, NORM-06/07 + unit contract)</name>
  <files>tests/normen.test.ts</files>
  <read_first>
    - tests/verzuimDrempels.test.ts (exact template — copy structure, adapt to Normen interface)
    - .planning/phases/25-doorstroomnorm-configuratie/25-PATTERNS.md (section: `tests/normen.test.ts (test — NEW file)` — lists the 5 cases to replicate)
    - .planning/phases/25-doorstroomnorm-configuratie/25-RESEARCH.md (section: Pattern 1 — defines DEFAULT_NORMEN and validation guard)
    - .planning/STATE.md (line 64: STO-02 LazyStore mock mandate — vi.hoisted + ES6 class)
  </read_first>
  <behavior>
    Test cases (all initially RED — `utils/normen.ts` does not exist yet):
    - Test 1 — cold cache default: `loadNormen()` returns object equal to DEFAULT_NORMEN when store is empty (sbl=13, sbc=15, negatiefTotaal=6, negatiefPerLeerlijn=2, bj1Positief=13, versneldLesgeven=4, versneldOrganiseren=3, versneldProfHandelen=5). Covers NORM-06 default branch.
    - Test 2 — save round-trip persists (NORM-06): call `saveNormen({ ...DEFAULT_NORMEN, sbl: 10, sbc: 12 })`, run `vi.resetModules()`, re-import normen module, call `loadNormen()`, assert returned object has sbl=10 and sbc=12.
    - Test 3 — getNormenSync cold returns defaults: without calling loadNormen, `getNormenSync()` returns DEFAULT_NORMEN (the cache fallback branch).
    - Test 4 — instant-apply (pitfall 5): after `saveNormen({ ...DEFAULT_NORMEN, sbl: 7 })`, `getNormenSync()` immediately returns sbl=7 (cache updated before await store.save completes).
    - Test 5 — resetNormen restores defaults (NORM-07): after `saveNormen({ ...DEFAULT_NORMEN, sbl: 9, sbc: 9 })`, call `resetNormen()`, then assert `getNormenSync()` and the returned value both equal DEFAULT_NORMEN.
  </behavior>
  <action>
    Copy `tests/verzuimDrempels.test.ts` verbatim into `tests/normen.test.ts` and adapt:
    - Replace `verzuim_drempels` STORE_KEY references with `doorstroom_normen` in any mocked Map seed.
    - Replace `VerzuimDrempels` / `DEFAULT_VERZUIM_DREMPELS` / `loadVerzuimDrempels` / `saveVerzuimDrempels` / `getVerzuimDrempelsSync` identifiers with the Normen equivalents (`Normen`, `DEFAULT_NORMEN`, `loadNormen`, `saveNormen`, `getNormenSync`, `resetNormen`).
    - Import path: `from '../utils/normen'`.
    - Keep the `vi.hoisted` LazyStore mock exactly as in the source template (Map-backed `get`/`set`/`save`/`delete`).
    - Keep `beforeEach(() => { setStoreMap(new Map()); vi.resetModules(); })`.
    - Add one additional test (#5 above) for resetNormen since verzuimDrempels.test.ts has no reset test.
    - All eight Normen fields (sbl, sbc, negatiefTotaal, negatiefPerLeerlijn, bj1Positief, versneldLesgeven, versneldOrganiseren, versneldProfHandelen) MUST be asserted in Test 1 — do not abbreviate with `.toEqual(DEFAULT_NORMEN)` only; assert each field explicitly so failure messages are diagnostic.
    - Use `await import('../utils/normen')` inside each test after `vi.resetModules()` so cache is fresh per case.
  </action>
  <verify>
    <automated>npx vitest run tests/normen.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/normen.test.ts` exists and is parseable TypeScript (no syntax errors blocking vitest discovery).
    - `npx vitest run tests/normen.test.ts` shows exactly 5 test cases.
    - All 5 tests FAIL with module-not-found error for `../utils/normen` (this is the expected RED state — module does not exist yet).
    - File contains the string `vi.hoisted` (LazyStore mock pattern from STATE.md line 64).
    - File contains the literal `DEFAULT_NORMEN` referenced from import.
    - File contains separate assertions for each of: sbl, sbc, negatiefTotaal, negatiefPerLeerlijn, bj1Positief, versneldLesgeven, versneldOrganiseren, versneldProfHandelen (grep count `grep -v '^\s*//' tests/normen.test.ts | grep -c 'versneldProfHandelen'` ≥ 1).
  </acceptance_criteria>
  <done>5 RED unit tests in tests/normen.test.ts, all failing on missing module import — Plan 02 will turn them GREEN by creating utils/normen.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/prognosis.test.ts with normen-parameter integration tests</name>
  <files>tests/prognosis.test.ts</files>
  <read_first>
    - tests/prognosis.test.ts (current state — see what fixtures and helpers exist; reuse setup)
    - utils/prognosis.ts (current berekenPrognose signature — read lines 1–220 to confirm the 8 hardcoded sites in 25-PATTERNS.md table)
    - .planning/phases/25-doorstroomnorm-configuratie/25-PATTERNS.md (hardcoded constant replacement checklist table in `utils/prognosis.ts` section)
    - .planning/phases/25-doorstroomnorm-configuratie/25-RESEARCH.md (Pitfall 1: gaps object hardcoding — every gap must use n.* after refactor)
  </read_first>
  <behavior>
    Add a new `describe('berekenPrognose normen parameter', ...)` block with these integration tests (all initially RED — current signature does not accept a 4th param):

    - Test A — SBL custom (NORM-01): call `berekenPrognose(studentJustAtThreshold, 'bj2', undefined, { ...DEFAULT_NORMEN, sbl: 10 })` for a student with totaalVoldoendeOfHoger = 10. Assert prognose label crosses into SBL territory (positive label, not "rood"). With default sbl=13, same student would be below SBL. Verify `result.gaps.nodigSBL === 0` (was `Math.max(0, 13 - 10) = 3` with hardcoded threshold).
    - Test B — SBC custom (NORM-02): same student fixture, pass `{ ...DEFAULT_NORMEN, sbc: 10 }`, traject 'bj2', assert prognose now reaches SBC label (was below 15 default). Verify `result.gaps.nodigSBC_deelgebieden === 0`.
    - Test C — negatiefTotaal custom (NORM-03): student with 7 onvoldoende deelgebieden, default thresholds would flag negatief (>6). Pass `{ ...DEFAULT_NORMEN, negatiefTotaal: 10 }` — assert NOT flagged as negatief, and `result.gaps.onvoldoendeRuimte === 3` (10 - 7).
    - Test D — negatiefPerLeerlijn custom (NORM-04): student with 3 onvoldoendes in lesgeven leerlijn. Default (>2) flags negatief. Pass `{ ...DEFAULT_NORMEN, negatiefPerLeerlijn: 5 }` — assert NOT flagged, and `result.gaps.onvoldoendeRuimtePerLeerlijn.lesgeven === 2` (5 - 3).
    - Test E — BJ1 versneld trio (NORM-05): for traject 'bj1' student with lesgeven=3, organiseren=2, prof_handelen=4 voldoendes, pass `{ ...DEFAULT_NORMEN, versneldLesgeven: 3, versneldOrganiseren: 2, versneldProfHandelen: 4 }` — assert versneld-SBC label is granted. With defaults (4/3/5) it would not.
    - Test F — BJ1 positief threshold (NORM-05 supplement): traject 'bj1', student totaalVoldoendeOfHoger = 11, pass `{ ...DEFAULT_NORMEN, bj1Positief: 10 }` — assert positive prognose label (was negative under default 13). Verify `result.gaps.nodigBJ2 === 0`.
    - Test G — undefined normen falls back to sync cache: call `berekenPrognose(student, 'bj2')` (no 4th param). Test must not throw and must return a result. (This proves the fallback path exists — once Plan 02 wires `getNormenSync()` as fallback, this passes; until then it depends on default param handling.)

    Each test must be standalone (no shared mutation between tests). Reuse existing fixture helpers in tests/prognosis.test.ts where present; if none exist, inline minimal student objects with the precise `deelgebiedScores` shape required by berekenPrognose (verify by reading utils/prognosis.ts lines 110–230).
  </behavior>
  <action>
    Open `tests/prognosis.test.ts`, append a new `describe` block at the end of the file (do NOT modify existing tests). Import `DEFAULT_NORMEN` and `type Normen` from `'../utils/normen'` at the top of the file alongside existing imports. Construct minimal student fixtures inline per the behavior list above — the existing student-construction patterns in tests/prognosis.test.ts are the model; copy the same `deelgebiedScores` keying convention.

    For each test, capture `result.label` (or whatever the prognose label field is — confirm by reading the current berekenPrognose return shape in utils/prognosis.ts) and `result.gaps.*` to verify both the label change and the gap-arithmetic correction (Pitfall 1 — gaps must also be parametrised in Plan 02).

    All 7 tests will initially RED because:
    1. `utils/normen.ts` does not exist (DEFAULT_NORMEN import fails) — top-level import error fails all tests in this file.
    2. Once Plan 02 creates normen.ts, tests A–G still fail because berekenPrognose does not accept a 4th argument and still uses hardcoded constants.

    Do not write any production code in this task. Do not modify utils/prognosis.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/prognosis.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `tests/prognosis.test.ts` contains a new `describe('berekenPrognose normen parameter'` block with exactly 7 test cases (Tests A through G).
    - Each test calls `berekenPrognose(...)` with a 4th argument that is a partial `Normen` override applied to `DEFAULT_NORMEN`.
    - Test A asserts `result.gaps.nodigSBL` equals an expected number derived from the custom sbl (proves gap-object refactor is verified, not only the label).
    - Test C asserts `result.gaps.onvoldoendeRuimte` reflects the custom negatiefTotaal (Pitfall 1 coverage).
    - File contains import `from '../utils/normen'` for `DEFAULT_NORMEN` (or `'../../utils/normen'` depending on existing path resolution in the test file — match the existing import style used by the file's current imports of utils modules).
    - `npx vitest run tests/prognosis.test.ts` runs and shows the 7 new tests FAILING (RED) — the failures must be due to (a) missing utils/normen module OR (b) berekenPrognose ignoring the 4th arg / gaps still using hardcoded constants — NOT due to syntax errors in the test file itself.
    - Pre-existing tests in tests/prognosis.test.ts continue to pass (no regression — the new block is purely additive).
  </acceptance_criteria>
  <done>7 RED integration tests appended to tests/prognosis.test.ts covering NORM-01..05 label changes AND gap-arithmetic correction; existing prognosis tests still pass; Plan 02 must turn the new 7 GREEN by refactoring berekenPrognose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → mocked LazyStore | Map-backed in-memory mock — no real plugin-store IPC; no untrusted input |
| test → utils modules | All code under test is project-local; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | tests/normen.test.ts Map mock | accept | Test-only scaffold; mock is isolated per `beforeEach` via `setStoreMap(new Map())` + `vi.resetModules()`. No persistence. |
| T-25-02 | Information Disclosure | tests/prognosis.test.ts fixtures | accept | Test fixtures use synthetic numbers (e.g. totaalVoldoendeOfHoger=10), not real student data. No PII. |

No high-severity threats. This plan only creates test scaffolds; no production code paths altered.
</threat_model>

<verification>
- `npx vitest run tests/normen.test.ts` shows 5 test cases, all RED (module-not-found).
- `npx vitest run tests/prognosis.test.ts` shows existing tests passing + 7 new tests RED.
- `npx vitest run` shows the rest of the suite still passing (no regressions in unrelated tests).
- `grep -c 'describe' tests/normen.test.ts` ≥ 1.
- `grep -c "berekenPrognose normen parameter" tests/prognosis.test.ts` = 1.
</verification>

<success_criteria>
- tests/normen.test.ts created with 5 RED unit tests covering NORM-06 (persist round-trip), NORM-07 (reset), and the utils/normen.ts contract (cold default, sync cache, instant-apply).
- tests/prognosis.test.ts extended with 7 RED integration tests covering NORM-01..05 (each threshold parametrised, both label and gaps).
- Existing test suite passes (no regressions introduced).
- All new tests fail for the right reason: missing module / unchanged signature — NOT syntax errors.
- Plan 02 has a precise GREEN target.
</success_criteria>

<output>
After completion, create `.planning/phases/25-doorstroomnorm-configuratie/25-01-SUMMARY.md` describing:
- The 5 + 7 = 12 new RED tests added.
- The exact API contract pinned for utils/normen.ts (interface, exports).
- The exact berekenPrognose signature pinned (4th parameter, fallback to getNormenSync).
- Confirmation that the rest of the suite still passes (test count before/after).
</output>
