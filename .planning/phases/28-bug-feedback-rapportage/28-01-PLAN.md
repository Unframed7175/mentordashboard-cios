---
phase: 28-bug-feedback-rapportage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - utils/feedback.ts
  - tests/feedbackUtils.test.ts
  - src/main.tsx
autonomous: true
requirements:
  - FEED-02
  - FEED-03
  - FEED-04

must_haves:
  truths:
    - "buildMailtoUrl() returns a properly encoded mailto: string containing OS, app version, last import, and console errors"
    - "setLastImport() stores the last import and it appears in the next mailto: URL"
    - "If ring buffer is empty, email body contains 'Geen console errors geregistreerd'"
    - "If no import has happened, email body contains 'Geen importactie geregistreerd'"
    - "console.error and window.onerror are patched in main.tsx before ReactDOM.createRoot"
  artifacts:
    - path: "utils/feedback.ts"
      provides: "Ring buffer, setLastImport, buildMailtoUrl, getSystemInfo"
      exports: ["setLastImport", "buildMailtoUrl", "getSystemInfo", "pushConsoleError"]
    - path: "tests/feedbackUtils.test.ts"
      provides: "TDD tests for buildMailtoUrl, setLastImport, empty-buffer, no-import cases"
    - path: "src/main.tsx"
      provides: "console.error patch + window.onerror handler installed before ReactDOM.createRoot"
  key_links:
    - from: "utils/feedback.ts"
      to: "buildMailtoUrl()"
      via: "encodeURIComponent on full body"
      pattern: "encodeURIComponent"
    - from: "src/main.tsx"
      to: "utils/feedback.ts"
      via: "import pushConsoleError"
      pattern: "pushConsoleError"
---

<objective>
Create utils/feedback.ts (ring buffer, setLastImport, buildMailtoUrl, getSystemInfo) and patch console.error + window.onerror in main.tsx before React mounts. Write RED TDD tests first, then GREEN implementation.

Purpose: This foundation provides the data-collection layer that FeedbackModal (Plan 28-02) depends on. All technical context captured here — OS info, version, imports, errors — feeds directly into the mailto: URL.

Output: utils/feedback.ts (fully tested), tests/feedbackUtils.test.ts (passing), src/main.tsx (patched with ring buffer hooks).
</objective>

<execution_context>
@D:/Users/rafae/.claude/get-shit-done/workflows/execute-plan.md
@D:/Users/rafae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-bug-feedback-rapportage/28-CONTEXT.md
</context>

<interfaces>
<!-- Key patterns the executor must replicate — extracted from codebase. -->

From src/main.tsx (current top of file, before async IIFE):
```
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";
// ... other imports
(async () => {
  // ... init logic ...
  ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(...)
})();
```
The ring buffer install must appear BEFORE the async IIFE — at module top-level — so errors during async init (loadKlassen, etc.) are captured.

From tests/normen.test.ts — plugin mock pattern:
- Uses vi.hoisted() + vi.mock() for @tauri-apps/plugin-store
- utils/feedback.ts does NOT use plugin-store; it is pure in-memory module state
- Tests can mock @tauri-apps/api/app and tauri-plugin-os with vi.mock()

Tauri API paths (already in project):
- `@tauri-apps/api/app` → named export `getVersion(): Promise<string>`
- `tauri-plugin-os` → named exports `platform(): Promise<string>`, `version(): Promise<string>`
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for utils/feedback.ts</name>
  <files>tests/feedbackUtils.test.ts</files>
  <read_first>
    - tests/normen.test.ts (vi.hoisted + vi.mock pattern for Tauri APIs)
    - tests/feedback.test.ts (understand existing file is unrelated — different module)
    - .planning/phases/28-bug-feedback-rapportage/28-CONTEXT.md (D-07, D-10, D-11, D-12, D-13, specifics section for email body format)
  </read_first>
  <behavior>
    - Test "buildMailtoUrl includes recipient": result starts with `mailto:ralvarezstam@cioszuidwest.nl?`
    - Test "buildMailtoUrl encodes subject with version and OS": decoded subject matches `[Bug] Mentordashboard v1.0.0 — mock-platform`
    - Test "buildMailtoUrl body includes description when provided": decoded body contains the description string
    - Test "buildMailtoUrl body includes OS section": decoded body contains `OS: mock-platform mock-os-version`
    - Test "buildMailtoUrl body includes app version": decoded body contains `App-versie: 1.0.0`
    - Test "buildMailtoUrl body — no import — shows fallback": decoded body contains `Geen importactie geregistreerd`
    - Test "setLastImport then buildMailtoUrl — shows filename": after setLastImport({ filename: 'test.pdf', type: 'PDF' }), decoded body contains `test.pdf (PDF)`
    - Test "buildMailtoUrl body — empty ring buffer — shows fallback": decoded body contains `Geen console errors geregistreerd`
    - Test "pushConsoleError then buildMailtoUrl — includes error": after pushConsoleError('ERR: boom'), decoded body contains `ERR: boom`
    - Test "ring buffer caps at 10 entries": push 12 entries, decoded body contains entry 3..12 but NOT entries 1 and 2
    - Test "pushConsoleError truncates at 200 chars": push a 300-char string, stored string length is 200
    - Mock @tauri-apps/api/app: `getVersion` returns `'1.0.0'`
    - Mock tauri-plugin-os: `platform` returns `'mock-platform'`, `version` returns `'mock-os-version'`
    - Reset module state between tests using beforeEach that calls a reset helper (or re-imports with vi.resetModules)
  </behavior>
  <action>
    Create tests/feedbackUtils.test.ts. Use vi.mock('@tauri-apps/api/app', ...) and vi.mock('tauri-plugin-os', ...) at the top. Because utils/feedback.ts does not yet exist, all imports will fail at this stage — that is the expected RED state. Add a comment at the top: "Phase 28 Plan 01 — RED: utils/feedback.ts does not exist yet. All tests must fail." Each test must await buildMailtoUrl(description) and decode the URL params with decodeURIComponent to assert on the body/subject content. Export a resetFeedbackState() function stub that tests call in beforeEach — the real implementation will be provided by utils/feedback.ts in Task 2.
  </action>
  <verify>
    <automated>npx vitest run tests/feedbackUtils.test.ts 2>&1 | findstr /i "FAIL\|Cannot find\|failed"</automated>
  </verify>
  <done>tests/feedbackUtils.test.ts exists and all tests FAIL with "Cannot find module '../utils/feedback'" or equivalent import error — RED state confirmed.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement utils/feedback.ts</name>
  <files>utils/feedback.ts</files>
  <read_first>
    - tests/feedbackUtils.test.ts (the test contracts created in Task 1 — implement against these exactly)
    - .planning/phases/28-bug-feedback-rapportage/28-CONTEXT.md (D-07, D-08, D-09, D-10, D-11, D-12, D-13, specifics section for email body format)
  </read_first>
  <behavior>
    - Module-level ring buffer: `const errorBuffer: string[] = []` (max 10 entries)
    - Module-level last import: `let lastImport: { filename: string; type: 'PDF' | 'Excel' | 'zip'; timestamp: string } | null = null`
    - `pushConsoleError(msg: string): void` — pushes `msg.slice(0, 200)` to errorBuffer; if buffer length > 10, removes oldest entry (shift)
    - `resetFeedbackState(): void` — clears buffer and lastImport (for tests only; exported)
    - `setLastImport(info: { filename: string; type: 'PDF' | 'Excel' | 'zip' }): void` — sets lastImport with current ISO timestamp via `new Date().toISOString().slice(0,16).replace('T',' ')`
    - `getSystemInfo(): Promise<{ platform: string; osVersion: string; appVersion: string }>` — calls `platform()` from tauri-plugin-os, `version()` from tauri-plugin-os, `getVersion()` from @tauri-apps/api/app; returns object
    - `buildMailtoUrl(description: string): Promise<string>` — assembles full email body with sections in this exact order: "Beschrijving:\n{description or blank}\n\n--- Technische info ---\nOS: {platform} {osVersion}\nApp-versie: {appVersion}\nLaatste import: {lastImport string or 'Geen importactie geregistreerd'}\n\n--- Console errors (laatste 10) ---\n{errors joined by \n or 'Geen console errors geregistreerd'}"; subject: `[Bug] Mentordashboard v{appVersion} — {platform}`; recipient: DEVELOPER_EMAIL constant = `'ralvarezstam@cioszuidwest.nl'`; returns `mailto:${DEVELOPER_EMAIL}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`
    - Last import display format: `{filename} ({type}), {timestamp}` e.g. `rapport-2B.pdf (PDF), 2026-05-26 19:35`
  </behavior>
  <action>
    Create utils/feedback.ts as a pure TypeScript module (no React imports). Place it at utils/feedback.ts (same directory level as utils/klassen.ts). Import `getVersion` from `'@tauri-apps/api/app'` and `platform`, `version` from `'tauri-plugin-os'`. Export the DEVELOPER_EMAIL constant, pushConsoleError, resetFeedbackState, setLastImport, getSystemInfo, and buildMailtoUrl. The file has zero side effects at module load time — only plain variable declarations. No default export.
  </action>
  <verify>
    <automated>npx vitest run tests/feedbackUtils.test.ts</automated>
  </verify>
  <done>npx vitest run tests/feedbackUtils.test.ts exits 0 with all tests passing — GREEN state confirmed. utils/feedback.ts exists at utils/feedback.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Patch console.error and window.onerror in main.tsx</name>
  <files>src/main.tsx</files>
  <read_first>
    - src/main.tsx (read the FULL current file — patch must be inserted at the very top, after the last import statement and before the async IIFE)
    - utils/feedback.ts (confirm pushConsoleError export signature)
    - .planning/phases/28-bug-feedback-rapportage/28-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    Add import `{ pushConsoleError }` from `'../utils/feedback'` at the top of src/main.tsx alongside the other imports. Immediately after all import statements and BEFORE the async IIFE block, insert two patches at module top-level:

    1. console.error patch: save original as `const _origConsoleError = console.error`; reassign `console.error = (...args: unknown[]) => { pushConsoleError(args.map(String).join(' ')); _origConsoleError(...args); }`.
    2. window.onerror handler: assign `window.onerror = (message, _source, _lineno, _colno, _error) => { pushConsoleError(String(message)); return false; }`.

    Do NOT modify the async IIFE body, the existing imports, or ReactDOM.createRoot call. Only add the import line and the two patch assignments between the imports and the IIFE.
  </action>
  <verify>
    <automated>npx vitest run tests/feedbackUtils.test.ts && node -e "const fs=require('fs'); const c=fs.readFileSync('src/main.tsx','utf8'); if(!c.includes('pushConsoleError')) process.exit(1); if(!c.includes('window.onerror')) process.exit(1); console.log('OK');"</automated>
  </verify>
  <done>src/main.tsx contains import of pushConsoleError from '../utils/feedback', a console.error patch that calls pushConsoleError, and a window.onerror handler that calls pushConsoleError. All existing tests still pass (npx vitest run exits 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| utils/feedback.ts → mailto: URL | Module-level string concatenation produces a URL opened in the OS mail client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | buildMailtoUrl body assembly | mitigate | encodeURIComponent() applied to the entire body string and subject string before URL assembly — prevents header injection via crafted description or console error content |
| T-28-02 | Information Disclosure | ring buffer in-memory | accept | Buffer is process-local, never written to disk, cleared on app restart — acceptable for a debug tool |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No new npm/Tauri dependencies added in this plan — @tauri-apps/api/app and tauri-plugin-os already in package.json/Cargo.toml |
</threat_model>

<verification>
- npx vitest run tests/feedbackUtils.test.ts exits 0 (all GREEN)
- npx vitest run exits 0 (full suite — no regressions)
- utils/feedback.ts exists and exports: DEVELOPER_EMAIL, pushConsoleError, resetFeedbackState, setLastImport, getSystemInfo, buildMailtoUrl
- src/main.tsx contains `pushConsoleError` reference and `window.onerror` assignment before the async IIFE
</verification>

<success_criteria>
- utils/feedback.ts is created with ring buffer (max 10), setLastImport, buildMailtoUrl (encodeURIComponent on body and subject), getSystemInfo, DEVELOPER_EMAIL = 'ralvarezstam@cioszuidwest.nl'
- buildMailtoUrl email body sections appear in correct order: description, technische info, last import, console errors
- Empty buffer → "Geen console errors geregistreerd"; no import → "Geen importactie geregistreerd"
- console.error and window.onerror patched in main.tsx at module top-level before the async IIFE
- All 158 existing tests + new feedback tests pass
</success_criteria>

<output>
Create `.planning/phases/28-bug-feedback-rapportage/28-01-SUMMARY.md` when done
</output>
