---
phase: 28-bug-feedback-rapportage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - src-tauri/Cargo.toml
  - src-tauri/capabilities/default.json
  - utils/feedback.ts
  - tests/feedbackUtils.test.ts
  - src/main.tsx
autonomous: true
requirements:
  - FEED-02
  - FEED-03
  - FEED-04

must_haves:
  truths:
    - "buildMailtoUrl() returns a properly encoded mailto: string containing OS, app version, last import, and console errors"
    - "setLastImport() stores the last import and it appears in the next mailto: URL"
    - "If ring buffer is empty, email body contains 'Geen console errors geregistreerd'"
    - "If no import has happened, email body contains 'Geen importactie geregistreerd'"
    - "Ring buffer entries include a timestamp prefix [HH:MM:SS]"
    - "Multi-arg console.error calls with Error objects use .stack in the stored entry"
    - "Total mailto: body is capped at 1500 chars with [ingekort wegens e-mail limiet] suffix"
    - "console.error and window.onerror and unhandledrejection are patched in main.tsx before ReactDOM.createRoot"
    - "HMR guard prevents double-wrapping console.error in dev mode"
  artifacts:
    - path: "utils/feedback.ts"
      provides: "Ring buffer, setLastImport, buildMailtoUrl, getSystemInfo, initSystemInfo"
      exports: ["setLastImport", "buildMailtoUrl", "getSystemInfo", "initSystemInfo", "pushConsoleError", "resetFeedbackState"]
    - path: "tests/feedbackUtils.test.ts"
      provides: "TDD tests covering all behaviors including body truncation, multi-arg serialization, unhandledrejection wiring"
    - path: "src/main.tsx"
      provides: "console.error patch (with HMR guard) + window.addEventListener('error') + window.addEventListener('unhandledrejection') + initSystemInfo() call before ReactDOM.createRoot"
  key_links:
    - from: "utils/feedback.ts"
      to: "buildMailtoUrl()"
      via: "encodeURIComponent on full body"
      pattern: "encodeURIComponent"
    - from: "src/main.tsx"
      to: "utils/feedback.ts"
      via: "import pushConsoleError, initSystemInfo"
      pattern: "pushConsoleError"
    - from: "utils/feedback.ts"
      to: "@tauri-apps/plugin-os"
      via: "initSystemInfo pre-cache"
      pattern: "plugin-os"
---

<objective>
Install missing Tauri dependencies, create utils/feedback.ts (ring buffer, setLastImport, buildMailtoUrl, getSystemInfo, initSystemInfo), and patch main.tsx before React mounts. Write RED TDD tests first, then GREEN implementation.

Purpose: This foundation provides the data-collection layer that FeedbackModal (Plan 28-02) depends on. All technical context — OS info, version, imports, errors — feeds directly into the mailto: URL. All HIGH and MEDIUM review issues from 28-REVIEWS.md are addressed in this plan.

Output: utils/feedback.ts (fully tested), tests/feedbackUtils.test.ts (passing), src/main.tsx (patched with ring buffer hooks, HMR guard, unhandledrejection listener), dependency files updated.
</objective>

<execution_context>
@D:/Users/rafae/.claude/get-shit-done/workflows/execute-plan.md
@D:/Users/rafae/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-bug-feedback-rapportage/28-CONTEXT.md
</context>

<interfaces>
<!-- Key patterns the executor must replicate — extracted from codebase. -->

From src/main.tsx (current top of file, before async IIFE):
The ring buffer install must appear BEFORE the async IIFE — at module top-level — so errors during async init (loadKlassen, etc.) are captured.

From tests/normen.test.ts — plugin mock pattern:
- Uses vi.hoisted() + vi.mock() for @tauri-apps/plugin-store
- utils/feedback.ts does NOT use plugin-store; it is pure in-memory module state
- Tests mock @tauri-apps/api/app and @tauri-apps/plugin-os with vi.mock()

Verified package names (from package.json + node_modules probe):
- JS import: `@tauri-apps/api/app` (getVersion) — already installed
- JS import: `@tauri-apps/plugin-os` (platform, version) — NOT YET INSTALLED (Task 0)
- JS import: `@tauri-apps/plugin-opener` (open) — NOT YET INSTALLED (Task 0)
- Rust crate: `tauri-plugin-os = "2"` — NOT YET in Cargo.toml (Task 0)
- Rust crate: `tauri-plugin-opener = "2"` — already in Cargo.toml

Capabilities file location: src-tauri/capabilities/default.json
Current permissions: ["core:default", "store:default", "secure-storage:default"]
Must add: "os:default", "opener:default"

New exports from utils/feedback.ts (created in Task 2):
- pushConsoleError(args: unknown[]): void — serializes multiple args, prefers Error.stack
- resetFeedbackState(): void — clears ring buffer and lastImport (for tests only)
- setLastImport(info: { filename: string; type: 'PDF' | 'Excel' | 'zip' }): void
- getSystemInfo(): Promise<{ platform: string; osVersion: string; appVersion: string }>
- initSystemInfo(): Promise<void> — pre-fetches and caches system info at startup
- buildMailtoUrl(description: string): Promise<string> — reads from cache, falls back to live call
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Install missing Tauri dependencies</name>
  <files>package.json, src-tauri/Cargo.toml, src-tauri/capabilities/default.json</files>
  <read_first>
    - src-tauri/Cargo.toml (read the [dependencies] section — verify tauri-plugin-opener is present, tauri-plugin-os is absent)
    - src-tauri/capabilities/default.json (read the full file — verify current permissions array)
    - package.json (check devDependencies/dependencies for @tauri-apps/plugin-os and @tauri-apps/plugin-opener)
  </read_first>
  <action>
    Three changes:

    1. Run `npm install @tauri-apps/plugin-os @tauri-apps/plugin-opener` in the project root. Both packages are required: plugin-os for OS detection (platform, version), plugin-opener for mailto: handoff via the OS (Plan 28-02). Confirm both appear under dependencies in package.json after install.

    2. Add `tauri-plugin-os = "2"` to the [dependencies] section of src-tauri/Cargo.toml. tauri-plugin-opener = "2" is already present — do not duplicate it. Write the updated Cargo.toml.

    3. Add `"os:default"` and `"opener:default"` to the permissions array in src-tauri/capabilities/default.json. The existing array contains "core:default", "store:default", "secure-storage:default" — append after those. Write the updated JSON.

    Do not change any other file in this task.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies?.['@tauri-apps/plugin-os'] && !p.devDependencies?.['@tauri-apps/plugin-os']) process.exit(1); if(!p.dependencies?.['@tauri-apps/plugin-opener'] && !p.devDependencies?.['@tauri-apps/plugin-opener']) process.exit(1); const fs=require('fs'); const c=fs.readFileSync('src-tauri/Cargo.toml','utf8'); if(!c.includes('tauri-plugin-os')) process.exit(1); const cap=JSON.parse(fs.readFileSync('src-tauri/capabilities/default.json','utf8')); if(!cap.permissions.includes('os:default')) process.exit(1); if(!cap.permissions.includes('opener:default')) process.exit(1); console.log('OK');"</automated>
  </verify>
  <done>package.json lists @tauri-apps/plugin-os and @tauri-apps/plugin-opener. src-tauri/Cargo.toml [dependencies] contains tauri-plugin-os = "2". src-tauri/capabilities/default.json permissions contains "os:default" and "opener:default".</done>
</task>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for utils/feedback.ts</name>
  <files>tests/feedbackUtils.test.ts</files>
  <read_first>
    - tests/normen.test.ts (vi.hoisted + vi.mock pattern for Tauri APIs — replicate exactly)
    - .planning/phases/28-bug-feedback-rapportage/28-CONTEXT.md (D-07 through D-13 for email format, decision specifics)
  </read_first>
  <behavior>
    Core behaviors (from original plan, unchanged):
    - Test "buildMailtoUrl includes recipient": result starts with `mailto:ralvarezstam@cioszuidwest.nl?`
    - Test "buildMailtoUrl encodes subject with version and OS": decoded subject matches `[Bug] Mentordashboard v1.0.0 — mock-platform`
    - Test "buildMailtoUrl body includes description when provided": decoded body contains the description string
    - Test "buildMailtoUrl body includes OS section": decoded body contains `OS: mock-platform mock-os-version`
    - Test "buildMailtoUrl body includes app version": decoded body contains `App-versie: 1.0.0`
    - Test "buildMailtoUrl body — no import — shows fallback": decoded body contains `Geen importactie geregistreerd`
    - Test "setLastImport then buildMailtoUrl — shows filename": after setLastImport({ filename: 'test.pdf', type: 'PDF' }), decoded body contains `test.pdf (PDF)`
    - Test "buildMailtoUrl body — empty ring buffer — shows fallback": decoded body contains `Geen console errors geregistreerd`
    - Test "pushConsoleError then buildMailtoUrl — includes error": after pushConsoleError(['ERR: boom']), decoded body contains `ERR: boom`
    - Test "ring buffer caps at 10 entries": push 12 single-string entries, decoded body does NOT contain entries 1 and 2
    - Test "pushConsoleError truncates at 200 chars per entry": push a 300-char string as single arg, stored entry length is at most 200 (excluding timestamp prefix)

    NEW behaviors (from review feedback — all required):
    - Test "ring buffer entries include timestamp prefix": after pushConsoleError(['hello']), the stored string starts with `[` and matches pattern /^\[\d{2}:\d{2}:\d{2}\] /
    - Test "pushConsoleError with Error object uses .stack": push [new Error('fail')] where the mock Error has stack='Error: fail\n  at test:1:1'; decoded body contains 'Error: fail'
    - Test "pushConsoleError with multiple args joins them": push ['label:', new Error('boom')] with stack; decoded body contains 'label:' and 'Error: boom'
    - Test "pushConsoleError with plain object JSON-stringifies it": push [{ code: 42 }]; decoded body contains '"code":42' or '{"code":42}'
    - Test "buildMailtoUrl body truncates at 1500 chars": set up 10 ring buffer entries of 200 chars each (2000+ chars total with metadata), call buildMailtoUrl(''); verify decoded body contains '[ingekort wegens e-mail limiet]'
    - Test "buildMailtoUrl body under 1500 chars — no truncation marker": with empty ring buffer and no import, decoded body does NOT contain '[ingekort wegens e-mail limiet]'

    Mock setup:
    - Mock @tauri-apps/api/app: `getVersion` returns `'1.0.0'`
    - Mock @tauri-apps/plugin-os: `platform` returns `'mock-platform'`, `version` returns `'mock-os-version'`
    - Reset module state between tests using beforeEach that calls resetFeedbackState() imported from utils/feedback.ts
  </behavior>
  <action>
    Create tests/feedbackUtils.test.ts. Use vi.hoisted() + vi.mock('@tauri-apps/api/app', ...) and vi.mock('@tauri-apps/plugin-os', ...) at the top, following the exact pattern in tests/normen.test.ts. Note: the JS package is @tauri-apps/plugin-os (not tauri-plugin-os). Because utils/feedback.ts does not yet exist, all imports will fail at this stage — that is the expected RED state. Add a comment at the top: "Phase 28 Plan 01 — RED: utils/feedback.ts does not exist yet. All tests must fail."

    Each test that checks buildMailtoUrl must await buildMailtoUrl(description) and decode URL params with decodeURIComponent to assert on the body and subject content. For the body truncation test: push 10 entries via a loop where each entry is a string of 200 'x' characters, then call buildMailtoUrl('') and assert decodeURIComponent(body).includes('[ingekort wegens e-mail limiet]').

    The pushConsoleError function accepts an array of unknown[] (multiple args) — call it as pushConsoleError(['ERR: boom']) not pushConsoleError('ERR: boom'). Update all test call sites accordingly.

    Import resetFeedbackState from '../utils/feedback' and call it in beforeEach.
  </action>
  <verify>
    <automated>npx vitest run tests/feedbackUtils.test.ts 2>&1 | findstr /i "FAIL\|Cannot find\|failed"</automated>
  </verify>
  <done>tests/feedbackUtils.test.ts exists and all tests FAIL with "Cannot find module '../utils/feedback'" or equivalent import error — RED state confirmed.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement utils/feedback.ts</name>
  <files>utils/feedback.ts</files>
  <read_first>
    - tests/feedbackUtils.test.ts (the test contracts created in Task 1 — implement against these exactly)
    - .planning/phases/28-bug-feedback-rapportage/28-CONTEXT.md (D-07 through D-13 and specifics section for email body format)
    - utils/klassen.ts (reference for module-level variable pattern and TypeScript style in this codebase)
  </read_first>
  <behavior>
    Module-level state:
    - `const errorBuffer: string[] = []` (max 10 entries, each entry is the serialized+timestamped string)
    - `let lastImport: { filename: string; type: 'PDF' | 'Excel' | 'zip'; timestamp: string } | null = null`
    - `let systemInfoCache: { platform: string; osVersion: string; appVersion: string } | null = null`

    Serializer (args: unknown[]): string — converts multiple args to a single string:
    - For each arg: if instanceof Error → use arg.stack ?? String(arg); if typeof arg === 'object' and arg !== null → JSON.stringify(arg); else → String(arg)
    - Join all parts with a single space
    - Truncate result to 200 characters
    - Prepend timestamp: `[${new Date().toLocaleTimeString('nl-NL')}] ` before the 200-char content (timestamp is outside the 200-char limit)

    pushConsoleError(args: unknown[]): void — serializes args using the serializer, pushes to errorBuffer; if buffer length > 10, shift oldest entry.

    resetFeedbackState(): void — sets errorBuffer.length = 0 and lastImport = null and systemInfoCache = null. Exported for tests only.

    setLastImport(info: { filename: string; type: 'PDF' | 'Excel' | 'zip' }): void — sets lastImport with timestamp via `new Date().toISOString().slice(0, 16).replace('T', ' ')`.

    getSystemInfo(): Promise<{ platform: string; osVersion: string; appVersion: string }> — calls platform() from @tauri-apps/plugin-os, version() from @tauri-apps/plugin-os, getVersion() from @tauri-apps/api/app; returns object with keys platform, osVersion, appVersion.

    initSystemInfo(): Promise<void> — calls getSystemInfo() and stores result in systemInfoCache. Call at startup to pre-populate cache so buildMailtoUrl is synchronous for the user-visible data.

    buildMailtoUrl(description: string): Promise<string> — reads from systemInfoCache if available, otherwise calls getSystemInfo() live. Assembles body in this exact order:
    1. "Beschrijving:\n{description.trim() or blank line}\n\n"
    2. "--- Technische info ---\n"
    3. "OS: {platform} {osVersion}\n"
    4. "App-versie: {appVersion}\n"
    5. "Laatste import: {filename} ({type}), {timestamp}" or "Geen importactie geregistreerd"
    6. "\n\n--- Console errors (laatste 10) ---\n"
    7. errors joined by "\n" or "Geen console errors geregistreerd"

    Body budget enforcement: after assembling the full body string, if body.length > 1500, rebuild the errors section with at most 5 entries truncated to 100 chars each (strip the timestamp prefix length from the budget, then take first 100 chars of the message part), then re-assemble. If body still > 1500, truncate to 1497 and append "...". Always append "[ingekort wegens e-mail limiet]" on a new line when any truncation occurred.

    Subject: `[Bug] Mentordashboard v{appVersion} — {platform}`
    Recipient constant: `const DEVELOPER_EMAIL = 'ralvarezstam@cioszuidwest.nl'`
    Return: `mailto:${DEVELOPER_EMAIL}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`

    Last import display format: `{filename} ({type}), {timestamp}` e.g. `rapport-2B.pdf (PDF), 2026-05-26 19:35`
  </behavior>
  <action>
    Create utils/feedback.ts as a pure TypeScript module (no React imports). Place it at the project root utils/ directory, same level as utils/klassen.ts. Import getVersion from '@tauri-apps/api/app' and platform, version from '@tauri-apps/plugin-os'. Export: DEVELOPER_EMAIL constant, pushConsoleError, resetFeedbackState, setLastImport, getSystemInfo, initSystemInfo, buildMailtoUrl. The file has zero side effects at module load time — only plain variable/constant declarations. No default export. Do not use fenced code blocks in implementation decisions — follow the behavior block above for every behavioral contract.
  </action>
  <verify>
    <automated>npx vitest run tests/feedbackUtils.test.ts</automated>
  </verify>
  <done>npx vitest run tests/feedbackUtils.test.ts exits 0 with all tests passing — GREEN state confirmed. utils/feedback.ts exists at utils/feedback.ts with all required exports.</done>
</task>

<task type="auto">
  <name>Task 3: Patch console.error, error listener, unhandledrejection, and initSystemInfo in main.tsx</name>
  <files>src/main.tsx</files>
  <read_first>
    - src/main.tsx (read the FULL current file — identify the last import statement line and the start of the async IIFE; patches must be inserted between those two points)
    - utils/feedback.ts (confirm pushConsoleError and initSystemInfo export signatures)
    - .planning/phases/28-bug-feedback-rapportage/28-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    Four changes to src/main.tsx — all added between the last import statement and the async IIFE block:

    1. Import line: add `import { pushConsoleError, initSystemInfo } from '../utils/feedback'` at the top alongside other imports.

    2. console.error patch with HMR guard: check `if (!(console.error as any).__gsd_patched)` before patching. Inside the if-block: save `const _origConsoleError = console.error`. Reassign `console.error = (...args: unknown[]) => { pushConsoleError(args); _origConsoleError.apply(console, args); }`. Set `(console.error as any).__gsd_patched = true` after the reassignment. This prevents double-wrapping on HMR dev reloads.

    3. Error event listener (replaces window.onerror assignment): use `window.addEventListener('error', (event) => { pushConsoleError([event.message ?? 'Unknown error']); })` — this is safer than direct window.onerror = assignment because it does not clobber existing handlers.

    4. Unhandled promise rejection listener: add `window.addEventListener('unhandledrejection', (event) => { pushConsoleError([event.reason instanceof Error ? event.reason : String(event.reason ?? 'Unhandled rejection')]); })` — this captures async failures that window.onerror misses.

    5. Inside the async IIFE, as the first statement (before loadKlassen and any other init), add `await initSystemInfo().catch(() => { /* ignore — buildMailtoUrl falls back to live call */ })` — this pre-caches OS info so FeedbackModal's handleVerstuur is fast.

    Do NOT modify the async IIFE's existing logic beyond inserting that one line at the top. Do NOT modify ReactDOM.createRoot or any existing import.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/main.tsx','utf8'); if(!c.includes('pushConsoleError')) process.exit(1); if(!c.includes('initSystemInfo')) process.exit(1); if(!c.includes('unhandledrejection')) process.exit(1); if(!c.includes('__gsd_patched')) process.exit(1); if(!c.includes(\"addEventListener('error'\")) process.exit(1); console.log('OK');"</automated>
  </verify>
  <done>src/main.tsx imports pushConsoleError and initSystemInfo from '../utils/feedback'. Contains HMR-guarded console.error patch, window.addEventListener('error', ...) handler, window.addEventListener('unhandledrejection', ...) handler, and initSystemInfo() call inside the async IIFE. All existing tests still pass (npx vitest run exits 0).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| utils/feedback.ts → mailto: URL | Module-level string concatenation produces a URL opened in the OS mail client |
| console.error patch → ring buffer | Arbitrary runtime error messages enter the ring buffer and eventually appear in a mailto: URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | buildMailtoUrl body assembly | mitigate | encodeURIComponent() applied to the entire body string and subject string before URL assembly — prevents header injection via crafted description or console error content |
| T-28-02 | Information Disclosure | ring buffer in-memory | accept | Buffer is process-local, never written to disk, cleared on app restart — acceptable for a debug tool. File paths in error messages may contain username — acceptable for local-first tester tool. |
| T-28-03 | Denial of Service | mailto: URL length | mitigate | Hard 1500-char body budget enforced in buildMailtoUrl; tested in TDD suite |
| T-28-SC | Tampering | npm/cargo installs | mitigate | Task 0 installs @tauri-apps/plugin-os and @tauri-apps/plugin-opener — both are official Tauri 2 packages from tauri-apps org. Verify on npmjs.com before installing if uncertain. |
</threat_model>

<verification>
- npx vitest run tests/feedbackUtils.test.ts exits 0 (all GREEN)
- npx vitest run exits 0 (full suite — no regressions)
- utils/feedback.ts exports: DEVELOPER_EMAIL, pushConsoleError, resetFeedbackState, setLastImport, getSystemInfo, initSystemInfo, buildMailtoUrl
- src/main.tsx contains: pushConsoleError import, initSystemInfo import, __gsd_patched HMR guard, addEventListener('error'), addEventListener('unhandledrejection'), initSystemInfo() call inside async IIFE
- src-tauri/Cargo.toml [dependencies] contains tauri-plugin-os = "2"
- src-tauri/capabilities/default.json permissions contains "os:default" and "opener:default"
- package.json dependencies contain @tauri-apps/plugin-os and @tauri-apps/plugin-opener
</verification>

<success_criteria>
- utils/feedback.ts is created with: ring buffer (max 10, timestamp-prefixed entries), improved multi-arg serializer (Error.stack, JSON.stringify objects), 1500-char body budget with [ingekort wegens e-mail limiet] truncation, setLastImport, buildMailtoUrl (encodeURIComponent on body and subject), getSystemInfo, initSystemInfo (pre-cache), DEVELOPER_EMAIL = 'ralvarezstam@cioszuidwest.nl'
- buildMailtoUrl email body sections appear in correct order: description, technische info, last import, console errors
- Empty buffer → "Geen console errors geregistreerd"; no import → "Geen importactie geregistreerd"
- console.error patched with HMR guard in main.tsx; window.addEventListener('error') and window.addEventListener('unhandledrejection') installed before the async IIFE
- All existing tests + new feedback tests pass
</success_criteria>

<output>
Create `.planning/phases/28-bug-feedback-rapportage/28-01-SUMMARY.md` when done
</output>
