---
phase: 19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/spider.test.ts
autonomous: true
requirements: [POL-02]
tags: [test, spider-chart, jsx-refactor]

must_haves:
  truths:
    - "tests/spider.test.ts asserts JSX return type (not string) for buildSpiderSVG"
    - "tests/spider.test.ts contains coverage for axis label <text> elements"
    - "tests/spider.test.ts contains coverage for hit-circle elements"
    - "All updated tests are RED until utils/spider.ts is refactored in Plan 03"
  artifacts:
    - path: "tests/spider.test.ts"
      provides: "Test suite for spider chart JSX refactor"
      contains: "React.isValidElement"
  key_links:
    - from: "tests/spider.test.ts"
      to: "utils/spider.ts (buildSpiderSVG)"
      via: "import { SpiderChart }"
      pattern: "React\\.isValidElement"
---

<objective>
Update `tests/spider.test.ts` so that all assertions describe the post-refactor JSX return type of `buildSpiderSVG`. This is the Wave 0 RED scaffold for the spider chart refactor in Plan 03 (per D-09 and Pitfall 1 in RESEARCH).

Purpose: Lock the test contract for the JSX refactor BEFORE the refactor happens, so that Plan 03 has a clear GREEN target and the four existing string-based assertions do not silently mask regressions.

Output: Updated `tests/spider.test.ts` with 4 updated tests (string → JSX) and 2 new tests (axis labels, hit circles). All 6 tests must FAIL until Plan 03 ships the JSX refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ui-polish/19-CONTEXT.md
@.planning/phases/19-ui-polish/19-RESEARCH.md
@.planning/phases/19-ui-polish/19-PATTERNS.md
@.planning/phases/19-ui-polish/19-UI-SPEC.md

<interfaces>
<!-- Current SpiderChart.buildSpiderSVG signature (utils/spider.ts lines 46-51): -->
buildSpiderSVG(
  axes: Array<{ key: string; label: string }>,
  scores: Record<string, string | null>,
  fillVar: string,
  strokeVar: string
): string

<!-- Target signature after Plan 03 (will return JSX.Element): -->
buildSpiderSVG(
  axes: Array<{ key: string; label: string }>,
  scores: Record<string, string | null>,
  fillVar: string,
  strokeVar: string,
  onHover?: (state: { axisIndex: number; x: number; y: number } | null) => void
): JSX.Element

<!-- Current tests/spider.test.ts has 4 tests (lines 15-37) all asserting typeof svg === 'string' -->
<!-- Use react-dom/server renderToStaticMarkup to render JSX to inspectable HTML strings -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite spider tests for JSX return type</name>
  <files>tests/spider.test.ts</files>
  <read_first>
    - tests/spider.test.ts (current 4 tests asserting string return)
    - utils/spider.ts (current string-returning implementation — verify axes/scores/fillVar/strokeVar argument shape used in tests)
    - .planning/phases/19-ui-polish/19-PATTERNS.md (section "tests/spider.test.ts" — exact target test bodies)
    - .planning/phases/19-ui-polish/19-RESEARCH.md (Pitfall 1 — Spider Test Breakage)
  </read_first>
  <action>
    Update tests/spider.test.ts per D-09 and Pitfall 1.

    Add imports at top of file: `import React from 'react';` and `import { renderToStaticMarkup } from 'react-dom/server';` (preserve existing imports of SpiderChart and DEELGEBIEDEN).

    Replace the 4 existing tests with JSX-aware versions:
    1. "buildSpiderSVG geeft een JSX element terug" — assert `React.isValidElement(svg)` is `true` (replaces the `typeof svg === 'string'` assertion).
    2. "output rendert naar svg element" — call `renderToStaticMarkup(svg)` and assert the result contains `'<svg'`.
    3. "alle excellent scores geeft polygon terug" — same setup as before with all-excellent scores; call `renderToStaticMarkup(svg)` and assert the HTML contains `'polygon'`.
    4. "buildSpiderSVG gooit geen fout bij lege scores" — preserve existing no-throw assertion as-is (still valid regardless of return type).

    Add 3 NEW tests:
    5. "axis labels worden gerenderd als text elementen" — call buildSpiderSVG, render to HTML, assert HTML contains `'<text'` AND contains `axes[0].label`.
    6. "hit circles worden gerenderd voor tooltip interactie" — call buildSpiderSVG, render to HTML, assert HTML contains `'spider-hit-circle'` (the className on the new hit-area circles).
    7. "onHover callback wordt aangeroepen bij mouseenter op hit circle" — call `buildSpiderSVG(axes, scores, '--fill', '--stroke', mockOnHover)` where `mockOnHover` is a `vi.fn()`. The returned JSX element is a React element tree; traverse it using `React.Children.toArray` and a recursive helper to find the first element with `className === 'spider-hit-circle'`. Call its `onMouseEnter` prop directly. Assert `mockOnHover` was called once with an object matching `{ axisIndex: expect.any(Number), x: expect.any(Number), y: expect.any(Number) }`. This locks the onHover contract (D-11 tooltip data shape) — this test will be RED until Plan 03 ships (no hit circles in current string implementation, so the JSX traversal will find nothing). This test intentionally stays RED alongside tests 1–4 and 6.

    Keep the test file's axes fixture (mapped from DEELGEBIEDEN) and other shared setup unchanged. Do NOT modify utils/spider.ts in this plan — these tests intentionally stay RED until Plan 03 refactors the implementation. (Note: this is a single-developer project with local Vitest runs only — no shared CI blocking on this RED state. The RED scaffold is local discipline to ensure Plan 03 has a verifiable GREEN target.)
  </action>
  <verify>
    <automated>npm test -- tests/spider.test.ts 2>&1 | tee /tmp/spider-test.log; grep -E "(6 failed|failed.*6|FAIL.*spider)" /tmp/spider-test.log</automated>
  </verify>
  <acceptance_criteria>
    - tests/spider.test.ts imports `React` from 'react' and `renderToStaticMarkup` from 'react-dom/server'
    - tests/spider.test.ts contains the literal token `React.isValidElement`
    - tests/spider.test.ts contains the literal token `renderToStaticMarkup`
    - tests/spider.test.ts contains the literal string `'spider-hit-circle'`
    - tests/spider.test.ts contains the literal token `'<text'`
    - tests/spider.test.ts contains exactly 7 `test(` calls (4 updated + 3 new + 0 deleted; the 4th preserved no-throw test remains)
    - tests/spider.test.ts contains the literal token `vi.fn()` (for the onHover callback spy in test 7)
    - `npm test -- tests/spider.test.ts` exits non-zero with at least 4 failures referencing spider.test.ts (RED state — expected until Plan 03)
    - `npm test` excluding spider.test.ts still has the same pass count as before this plan (no other suites broken)
  </acceptance_criteria>
  <done>tests/spider.test.ts contains 7 tests aligned with JSX return contract and onHover callback contract; suite is RED on spider.test.ts only; rest of the test suite is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Test file only — no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | tests/spider.test.ts | accept | Test code; failure of these tests blocks merge in Plan 03 — that is the intended defense |
</threat_model>

<verification>
- `npm test -- tests/spider.test.ts` runs and fails with assertion errors on the 6 (or 5) string-vs-JSX checks
- Other test files (`npm test` excluding spider) still pass at the same count as before
- No source files in `src/` or `utils/` were modified
</verification>

<success_criteria>
- 7 tests defined in tests/spider.test.ts
- All JSX-related assertions (`React.isValidElement`, `renderToStaticMarkup`, `<text`, `spider-hit-circle`) are present
- onHover callback shape test (vi.fn() spy, asserting axisIndex/x/y) is present (test 7)
- Spider test suite is RED (acts as gate for Plan 03)
- No other test suite regressions
</success_criteria>

<output>
After completion, create `.planning/phases/19-ui-polish/19-01-SUMMARY.md`
</output>
