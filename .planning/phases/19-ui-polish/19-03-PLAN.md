---
phase: 19
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - utils/spider.tsx
  - src/components/SpiderChartCard.tsx
  - src/index.css
autonomous: true
requirements: [POL-02, POL-03]
tags: [spider-chart, jsx-refactor, tooltip, accessibility]

must_haves:
  truths:
    - "buildSpiderSVG returns JSX.Element (not string) and renders to a real <svg> in tests"
    - "Every axis renders a visible <text> label using axis.label (D-10)"
    - "Hovering a data-point hit-circle shows a tooltip with '{label}: {scoreDisplay}' (D-11)"
    - "SpiderChartCard no longer uses dangerouslySetInnerHTML"
    - "Spider tooltip is positioned inside .spider-card via position: relative + position: absolute"
    - "All 6 tests in tests/spider.test.ts pass (RED → GREEN)"
    - ".spider-card has width: 160px; position: relative (matches SVG width exactly so tooltip coordinates require no offset correction)"
  artifacts:
    - path: "utils/spider.tsx"
      provides: "JSX-returning buildSpiderSVG with axis labels and hit circles"
      contains: "JSX.Element"
    - path: "src/components/SpiderChartCard.tsx"
      provides: "Tooltip-aware spider card without dangerouslySetInnerHTML"
      contains: "useState"
    - path: "src/index.css"
      provides: ".spider-tooltip and .spider-hit-circle and .spider-card rules"
      contains: ".spider-tooltip"
  key_links:
    - from: "src/components/SpiderChartCard.tsx"
      to: "utils/spider.ts buildSpiderSVG"
      via: "direct JSX render with onHover callback"
      pattern: "SpiderChart\\.buildSpiderSVG\\("
    - from: "SpiderChartCard tooltip <div>"
      to: ".spider-card position: relative"
      via: "absolute positioning within relative ancestor"
      pattern: "position:\\s*relative"
---

<objective>
Refactor the spider chart end-to-end per D-09/D-10/D-11 (POL-02) and apply the spider-card responsive fix from the POL-03 audit. Change `buildSpiderSVG` return type from `string` → `JSX.Element`, add SVG `<text>` axis labels and `<circle>` hit areas, remove `dangerouslySetInnerHTML` from `SpiderChartCard.tsx`, add tooltip state + rendering, and add the spider tooltip/hit-circle/.spider-card CSS rules.

Purpose: POL-02 cannot ship without this refactor — React event handlers are unavailable on injected HTML. Removing `dangerouslySetInnerHTML` also eliminates the SVG injection surface noted in the Phase 14 security comment.

Output: Updated `utils/spider.ts`, `src/components/SpiderChartCard.tsx`, and `src/index.css`. The 6 RED tests from Plan 01 go GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ui-polish/19-CONTEXT.md
@.planning/phases/19-ui-polish/19-RESEARCH.md
@.planning/phases/19-ui-polish/19-PATTERNS.md
@.planning/phases/19-ui-polish/19-UI-SPEC.md
@.planning/phases/19-ui-polish/19-01-SUMMARY.md
@.planning/phases/19-ui-polish/19-02-SUMMARY.md

<interfaces>
<!-- Target signature (Plan 03 produces this — tests in Plan 01 already assert against it): -->
buildSpiderSVG(
  axes: Array<{ key: string; label: string }>,
  scores: Record<string, string | null>,
  fillVar: string,
  strokeVar: string,
  onHover?: (state: { axisIndex: number; x: number; y: number } | null) => void
): JSX.Element

<!-- SpiderChartCard current key facts (lines verified during planning):
     - imports: React, SpiderChart, DEELGEBIEDEN, getDeelgebiedenConfigSync
     - uses dangerouslySetInnerHTML at ~line 48 with rawSvg = SpiderChart.buildSpiderSVG(...)
     - inline style on wrapper: maxWidth: '180px'
     - empty-state string "Geen scores beschikbaar" (PRESERVE — see Copywriting Contract)
     - security comment at lines 41-44 (REMOVE — JSX eliminates the injection surface) -->

<!-- Score display formatter (per UI-SPEC Copywriting Contract): -->
function scoreDisplay(score: string | null): string {
  switch (score) {
    case 'onvoldoende': return 'Onvoldoende';
    case 'voldoende':   return 'Voldoende';
    case 'goed':        return 'Goed';
    case 'excellent':   return 'Excellent';
    default:            return 'Geen score';
  }
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor utils/spider.ts to return JSX.Element with axis labels and hit circles</name>
  <files>utils/spider.ts</files>
  <read_first>
    - utils/spider.ts (full current implementation — 123 lines, math is preserved)
    - .planning/phases/19-ui-polish/19-PATTERNS.md (section "utils/spider.ts" — exact JSX patterns for gridRings, gridLines, axisLabels, hitCircles, polygon)
    - .planning/phases/19-ui-polish/19-RESEARCH.md (Pattern 1, Pitfall 6 SVG → DOM coord mapping)
    - tests/spider.test.ts (updated by Plan 01 — these are the GREEN target)
  </read_first>
  <behavior>
    - Returning value is a JSX.Element (React.isValidElement returns true)
    - renderToStaticMarkup output contains '<svg', 'polygon', '<text', 'spider-hit-circle'
    - Empty axes array returns an empty <svg /> JSX element (no throw)
    - axes[i].label appears as the text content of the i-th <text> element
  </behavior>
  <action>
    Rewrite the body of `SpiderChart.buildSpiderSVG` in `utils/spider.ts` per D-09 and 19-PATTERNS.md.

    RENAME `utils/spider.ts` to `utils/spider.tsx` (required — tsconfig.json sets `"jsx": "react-jsx"` but TypeScript only allows JSX syntax in `.tsx` files, not `.ts` files). Update the single import in `src/components/SpiderChartCard.tsx` from `'../utils/spider'` to `'../utils/spider'` (the extension-less import resolves to the renamed file automatically — no change needed unless the import already uses a `.ts` extension). If `tests/spider.test.ts` imports `utils/spider` by relative path, no import change is needed (TypeScript resolves the rename via the same extension-less path). Add `import React from 'react';` at the top of the renamed file.

    Define a local type alias at the top of the file for clarity (cross-AI review suggestion):
    `type HoverState = { axisIndex: number; x: number; y: number } | null;`

    Update the function signature to:
      `buildSpiderSVG(axes, scores, fillVar, strokeVar, onHover?: (state: HoverState) => void): JSX.Element`

    Replace string-concatenation loops with `.map()` returning JSX:
    - `gridRings` → array of `<polygon key="ring-{li}" points={ringPoints} fill="none" stroke="#d1d5db" strokeWidth="0.75" opacity="0.7" />`. Note camelCase `strokeWidth` (not stroke-width).
    - `gridLines` → array of `<line key="spoke-{i}" x1={centerX} y1={centerY} x2={x} y2={y} stroke="#d1d5db" strokeWidth="0.75" opacity="0.7" />`.
    - NEW `axisLabels` → for each axis, `<text key="label-{i}" x={labelX} y={labelY} textAnchor="middle" dominantBaseline="middle" fontSize="10" fill="var(--text-secondary)">{axis.label}</text>` where `labelX = centerX + cos(angle) * (maxRadius + 12)`, `labelY = centerY + sin(angle) * (maxRadius + 12)`.
    - Data `<polygon points={pointsStr} fill={`var(${safeFill})`} stroke={`var(${safeStroke})`} strokeWidth="2" fillOpacity="0.4" />`.
    - NEW `hitCircles` → for each axis, `<circle key="hit-{i}" cx={cx} cy={cy} r={6} className="spider-hit-circle" onMouseEnter={() => onHover?.({ axisIndex: i, x: cx * (160 / 200), y: cy * (160 / 200) })} onMouseLeave={() => onHover?.(null)} />`.
      Coordinate system note (addresses cross-AI review concern): `cx`/`cy` are in SVG viewBox space (0–200). The `160/200` scale factor converts them to rendered pixel space (0–160), matching the SVG's `width="160"` attribute. The resulting `x`/`y` are then used by `SpiderChartCard` as `style={{ top: tooltip.y, left: tooltip.x }}` on an absolutely-positioned `<div>` inside `.spider-card` (which has `position: relative`). The tooltip is positioned relative to `.spider-card`, NOT the viewport — so no viewport coordinate conversion is needed.

    Return root JSX: `<svg width="160" height="160" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">{gridRings}{gridLines}{axisLabels}<polygon ... />{hitCircles}</svg>`.

    Empty-state branch (n === 0): return `<svg width="160" height="160" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg" />` JSX.

    Preserve `sanitizeCssVar` (still applied to fillVar/strokeVar). Preserve `scoreToRadius`. Preserve the angle formula `(2 * Math.PI * i / n) - (Math.PI / 2)`. Update the JSDoc @returns line to `JSX.Element` and remove the "as string" wording.
  </action>
  <verify>
    <automated>npm test -- tests/spider.test.ts 2>&1 | tee /tmp/spider-green.log; grep -E "(6 passed|✓.*6)" /tmp/spider-green.log</automated>
  </verify>
  <acceptance_criteria>
    - utils/spider.ts has been renamed to utils/spider.tsx (required for JSX syntax — confirmed by tsconfig.json `"jsx": "react-jsx"` requiring .tsx extension)
    - utils/spider.tsx imports React
    - File contains the literal `JSX.Element` in the function return type
    - File contains the `HoverState` type definition
    - File contains the literal `axisLabels` and `hitCircles` identifiers (or equivalent map results assigned to those names)
    - File contains the literal `className="spider-hit-circle"`
    - File contains `onMouseEnter` and `onMouseLeave`
    - File does NOT contain the literal `+ '<polygon'` or `'<line'` string concatenations
    - `npm test -- tests/spider.test.ts` exits 0 with all 7 tests green (including the onHover callback shape test from Plan 01 test 7)
    - `npm run typecheck-migrated` (if defined) exits 0; otherwise `npm run build` continues to succeed
  </acceptance_criteria>
  <done>buildSpiderSVG returns JSX.Element with grid rings, spokes, axis labels, data polygon, and hit circles. Plan 01's 6 tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor SpiderChartCard.tsx — remove dangerouslySetInnerHTML, add tooltip state + render, add .spider-card / .spider-tooltip / .spider-hit-circle CSS</name>
  <files>src/components/SpiderChartCard.tsx, src/index.css</files>
  <read_first>
    - src/components/SpiderChartCard.tsx (full file — 54 lines; confirm current imports, axes/scores derivation, security comment at lines 41-44, inline maxWidth at line 47, dangerouslySetInnerHTML at line 48)
    - utils/spider.ts (post-Task-1 state — new JSX signature)
    - .planning/phases/19-ui-polish/19-PATTERNS.md (section "src/components/SpiderChartCard.tsx" and "Spider chart CSS additions")
    - .planning/phases/19-ui-polish/19-UI-SPEC.md (Spider Chart Refactor contract — exact tooltip CSS values)
    - src/index.css (confirm where new spider-related rules will be appended; choose a new section number at end of file or insert near existing spider-related selectors if any)
  </read_first>
  <behavior>
    - SpiderChartCard renders the JSX from buildSpiderSVG directly (no dangerouslySetInnerHTML).
    - When tooltip state is null, no tooltip <div> is in the DOM.
    - When user hovers a hit circle, tooltip <div> appears with text "{axis.label}: {scoreDisplay(score)}" (e.g., "V&A: Goed").
    - On mouse leave, tooltip <div> disappears.
    - Empty-scores state still renders the "Geen scores beschikbaar" message (preserved string).
    - .spider-card wrapper has position: relative and width: 100%; max-width: 180px.
  </behavior>
  <action>
    A) Edit `src/components/SpiderChartCard.tsx`:
    - Change `import React from 'react';` to `import React, { useState } from 'react';`.
    - Remove the security comment block at the current lines 41-44 entirely (JSX eliminates the injection surface, per D-09 and Pitfall in UI-SPEC).
    - Remove the `rawSvg` const, the `null` check that returns early, the `.replace(/<script.../, '')` script-strip, and the `dangerouslySetInnerHTML` JSX usage at the bottom of the component.
    - Replace the inline `style={{ maxWidth: '180px' }}` on the wrapper with className-only `.spider-card` (the CSS class added in step B will own width/max-width/position).
    - Import `HoverState` from `'../utils/spider'` (exported from spider.tsx) OR re-declare it locally as `type HoverState = { axisIndex: number; x: number; y: number } | null`. Add `const [tooltip, setTooltip] = useState<HoverState>(null);` near the top of the component body.
    - Add a local `scoreDisplay(score)` helper as shown in the interfaces block above OR import it from a small shared util — local helper is acceptable for Phase 19 scope.
    - Keep the empty-state branch returning `<div className="spider-card-empty">Geen scores beschikbaar</div>` (or equivalent existing wording — preserve exactly).
    - New return JSX for the populated case:
      `<div className="spider-card">{SpiderChart.buildSpiderSVG(axes, scores, fillVar, strokeVar, setTooltip)}{tooltip && (<div className="spider-tooltip" style={{ top: tooltip.y, left: tooltip.x }}>{axes[tooltip.axisIndex].label}: {scoreDisplay(scores[axes[tooltip.axisIndex].key] ?? null)}</div>)}<div className="spider-leerlijn-title">{title}</div></div>`
      (the trailing leerlijn-title div preserves existing behavior — see current file for exact element).

    B) Append a new section (e.g. "/* Section 26 — Spider chart (Phase 19) */") to `src/index.css` containing:
    - `.spider-card { width: 160px; position: relative; }` — NOTE: width is 160px (exactly matching the SVG `width="160"` attribute) so that the pre-scaled tooltip coordinates (cx * 160/200, cy * 160/200) map directly to card-relative pixel positions with zero horizontal offset. This replaces the `width: 100%; max-width: 180px` value that was in the original plan; the 160px fixed width wraps the chart precisely and still satisfies POL-03 (the parent flex row already wraps at narrow widths per DetailWeergave's existing `flexWrap: 'wrap'`).
    - `.spider-tooltip { position: absolute; background: var(--bg-surface); border: 1.5px solid var(--border-default); border-radius: var(--radius-sm); padding: 4px 8px; font-size: 12px; color: var(--text-primary); box-shadow: var(--shadow-md); pointer-events: none; white-space: nowrap; z-index: 10; transform: translate(-50%, -100%); opacity: 1; transition: opacity var(--transition-fast); }`
    - `.spider-hit-circle { fill: transparent; cursor: pointer; transition: fill var(--transition-fast); }`
    - `.spider-hit-circle:hover { fill: rgba(0, 0, 0, 0.08); }`

    Do NOT modify App.tsx, DetailWeergave.tsx, or KlasTabStrip.tsx in this plan — Plan 04 owns those. Do NOT add animation/keyframes or nav-stripe CSS — Plan 04 owns those sections of index.css.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -10 && powershell -Command "$f = Get-Content src/components/SpiderChartCard.tsx -Raw; if ($f -match 'dangerouslySetInnerHTML') { Write-Host 'FAIL: dangerouslySetInnerHTML still present'; exit 1 }; if ($f -notmatch 'useState') { Write-Host 'FAIL: useState missing'; exit 1 }; $c = Get-Content src/index.css -Raw; if ($c -notmatch '\.spider-tooltip') { Write-Host 'FAIL: .spider-tooltip CSS missing'; exit 1 }; if ($c -notmatch '\.spider-hit-circle') { Write-Host 'FAIL: .spider-hit-circle CSS missing'; exit 1 }; Write-Host OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SpiderChartCard.tsx does NOT contain the literal `dangerouslySetInnerHTML`
    - src/components/SpiderChartCard.tsx does NOT contain a script-strip regex `<script` literal
    - src/components/SpiderChartCard.tsx contains `useState<{ axisIndex: number; x: number; y: number } | null>`
    - src/components/SpiderChartCard.tsx contains `SpiderChart.buildSpiderSVG(axes, scores, fillVar, strokeVar, setTooltip)`
    - src/components/SpiderChartCard.tsx still renders the literal string `Geen scores beschikbaar` for the empty state
    - src/index.css contains the literal `.spider-tooltip { position: absolute;`
    - src/index.css contains the literal `.spider-hit-circle`
    - src/index.css contains `.spider-card { width: 160px; position: relative; }` (or equivalent multi-line — 160px matches SVG width exactly for correct tooltip coordinate mapping)
    - `npm test` exits 0 — full suite green (Plan 01 RED tests are now GREEN, no other regressions)
    - `npm run build` exits 0 (TypeScript + Vite build succeeds)
  </acceptance_criteria>
  <done>SpiderChartCard renders JSX directly, tooltip appears on hover via .spider-card relative + tooltip absolute, CSS rules are in place, full test suite green, build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG render path | Spider chart SVG was previously injected as HTML string; now rendered as JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Injection (XSS) | utils/spider.ts → SpiderChartCard.tsx | mitigate | JSX rendering replaces dangerouslySetInnerHTML; React auto-escapes attribute values; axis labels (axis.label) come from trusted DEELGEBIEDEN schema; tooltip text composed from enum-derived scoreDisplay values — no user input embedded |
| T-19-05 | Tampering | sanitizeCssVar for fillVar/strokeVar | mitigate | Preserve existing sanitizeCssVar — still applied to fillVar/strokeVar values embedded in `var(...)` attribute strings |
</threat_model>

<verification>
- All 6 spider tests GREEN
- Full `npm test` suite GREEN (no regressions in actiepunten/excel/pdf/storage/etc)
- `npm run build` succeeds (typecheck + Vite bundle)
- Manual (executor's option): run `npm run dev`, open a klas detail view, hover a spider chart point → tooltip appears with label+score
</verification>

<success_criteria>
- POL-02 fully implemented: axis labels visible, hover tooltips functional, dangerouslySetInnerHTML removed
- .spider-card CSS supports responsive width (POL-03 audit item)
- 0 test regressions outside the intended spider GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/19-ui-polish/19-03-SUMMARY.md`
</output>
