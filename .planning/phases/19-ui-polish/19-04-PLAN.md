---
phase: 19
plan: 04
type: execute
wave: 3
depends_on: [19-02, 19-03]
files_modified:
  - src/App.tsx
  - src/components/SettingsPage.tsx
  - src/components/KlasTabStrip.tsx
  - src/components/DetailWeergave.tsx
  - src/assets/logo-light.svg
  - src/assets/logo-dark.svg
  - src/index.css
autonomous: false
requirements: [POL-03, POL-04]
tags: [animation, nav, logo, dark-mode-lift, responsive]

user_setup:
  - service: logo-assets
    why: "D-06 — Logo files (light + dark) live outside the repo; user must export SVG/PNG from design source"
    files_to_create:
      - target: "src/assets/logo-light.svg"
        description: "Full 'cios ZUIDWEST-NL' logo for light mode nav (blue cios italic + bold ZUIDWEST-NL)"
      - target: "src/assets/logo-dark.svg"
        description: "'cios' mark only for dark mode nav (blue text on dark/transparent bg)"
    blocking_checkpoint: true

must_haves:
  truths:
    - "isDark state is owned by App.tsx and passed as prop to KlasTabStrip and SettingsPage"
    - "Opening settings triggers slide-in-from-right animation (~200ms ease) every time"
    - "Settings re-opens cleanly (key prop forces remount → animation re-fires)"
    - "KlasTabStrip renders logo-light.svg when isDark=false and logo-dark.svg when isDark=true"
    - "#main-nav shows a blue diagonal stripe in the top-right via ::after pseudo-element"
    - "Nav tabs render above the diagonal stripe (z-index)"
    - ".aanvullend-grid children carry min-width: 0 to prevent overflow at 1024px (POL-03)"
    - "Settings view transition does not produce a horizontal scrollbar flash"
  artifacts:
    - path: "src/App.tsx"
      provides: "isDark + settingsOpenCount state, slide-in wrapper, prop wiring"
      contains: "settingsOpenCount"
    - path: "src/components/KlasTabStrip.tsx"
      provides: "isDark prop, logo <img>"
      contains: "isDark"
    - path: "src/components/SettingsPage.tsx"
      provides: "Lifted isDark + onToggleDark props"
      contains: "onToggleDark"
    - path: "src/assets/logo-light.svg"
      provides: "Light mode logo asset"
    - path: "src/assets/logo-dark.svg"
      provides: "Dark mode logo asset"
    - path: "src/index.css"
      provides: ".view-slide-in-right keyframes, #main-nav::after diagonal, .nav-tab z-index, .aanvullend-grid > * min-width fix"
      contains: "@keyframes slideInFromRight"
  key_links:
    - from: "App.tsx handleOpenSettings"
      to: "SettingsPage key={settingsOpenCount}"
      via: "increment + key remount"
      pattern: "setSettingsOpenCount"
    - from: "App.tsx isDark"
      to: "KlasTabStrip isDark prop -> logo <img src>"
      via: "prop drilling"
      pattern: "isDark={isDark}"
    - from: "#main-nav::after"
      to: ".nav-tab z-index: 1"
      via: "stacking context"
      pattern: "z-index:\\s*1"
---

<objective>
Wire the App-level animation, branded nav, and responsive completeness work that requires component code changes. Lift `isDark` from SettingsPage to App.tsx (Pitfall 2), add `settingsOpenCount` counter and `.view-slide-in-right` wrapper for the settings panel (D-16), add the logo to KlasTabStrip with light/dark switching (D-06), add the `#main-nav::after` diagonal accent stripe (D-03), and finish the POL-03 responsive audit by adding `min-width: 0` to `.aanvullend-grid` children.

Purpose: Complete the corporate identity refresh (logo + diagonal stripe) and ship the settings slide-in animation that makes the panel feel like a Material side panel. Closes POL-03 and POL-04 completely.

Output: Updated `src/App.tsx`, `src/components/SettingsPage.tsx`, `src/components/KlasTabStrip.tsx`, `src/components/DetailWeergave.tsx` (only if a JSX class adjustment for `.aanvullend-grid` children is needed — likely CSS-only), `src/index.css` (animation + nav stripe + .aanvullend-grid responsive rule), and 2 new logo assets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ui-polish/19-CONTEXT.md
@.planning/phases/19-ui-polish/19-RESEARCH.md
@.planning/phases/19-ui-polish/19-PATTERNS.md
@.planning/phases/19-ui-polish/19-UI-SPEC.md
@.planning/phases/19-ui-polish/19-02-SUMMARY.md
@.planning/phases/19-ui-polish/19-03-SUMMARY.md

<interfaces>
<!-- Current KlasTabStripProps (src/components/KlasTabStrip.tsx lines 3-10): -->
interface KlasTabStripProps {
  klassen: Array<{ id: string; naam: string }>;
  activeKlasId: string | null;
  onSwitch: (klasId: string) => void;
  onCreateKlas: () => void;
  onSettings: () => void;
  isSettingsActive: boolean;
}
// Phase 19 adds: isDark: boolean

<!-- Current SettingsPageProps (src/components/SettingsPage.tsx lines 25-28): -->
interface SettingsPageProps {
  onBack: () => void;
  onNavigateToImport: () => void;
}
// Phase 19 adds: isDark: boolean; onToggleDark: (isDark: boolean) => void

<!-- Settings currently owns dark mode via loadSettings() / applyTheme().
     Plan 04 lifts the source of truth to App.tsx; SettingsPage receives current value as prop
     and notifies parent on toggle via onToggleDark. SettingsPage still calls applyTheme + saveSettings. -->

<!-- utils/settings.ts exports: loadSettings, saveSettings, applyTheme, Theme -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 (Checkpoint): User provides exported logo SVG/PNG files</name>
  <what-built>Plan 04 cannot proceed past Task 3 without the two logo image files. Per D-06, the user has the source images from the design session; they must be exported and placed at src/assets/logo-light.svg and src/assets/logo-dark.svg before KlasTabStrip can reference them.</what-built>
  <how-to-verify>
    1. Export the light mode logo (full "cios ZUIDWEST-NL" — blue italic 'cios' + bold black 'ZUIDWEST-NL') as `src/assets/logo-light.svg` (preferred) or `.png` @2x.
    2. Export the dark mode logo ("cios" mark only — blue text on dark/transparent bg) as `src/assets/logo-dark.svg` (preferred) or `.png` @2x.
    3. Both files should render cleanly at ~36px height in the nav (SVG ideal; PNG should be at least 72px tall).
    4. Confirm files exist: `Test-Path src/assets/logo-light.svg` and `Test-Path src/assets/logo-dark.svg`.
  </how-to-verify>
  <resume-signal>Type "logos in place" once both files exist at src/assets/logo-light.svg (or .png) and src/assets/logo-dark.svg (or .png). Mention the extension if it is .png so the executor adjusts the imports.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift isDark to App.tsx, add settingsOpenCount, wire slide-in wrapper, pass props to KlasTabStrip and SettingsPage</name>
  <files>src/App.tsx, src/components/SettingsPage.tsx</files>
  <read_first>
    - src/App.tsx (full file — confirm current state declarations, handleOpenSettings, KlasTabStrip and SettingsPage call sites)
    - src/components/SettingsPage.tsx (find the internal dark-mode useState, the loadSettings/applyTheme/saveSettings calls, and the toggle onChange handler that must now notify parent)
    - utils/settings.ts (exports loadSettings, saveSettings, applyTheme, Theme)
    - .planning/phases/19-ui-polish/19-PATTERNS.md (sections "src/App.tsx" and "Component Prop Interface Extension")
    - .planning/phases/19-ui-polish/19-RESEARCH.md (Pitfall 2 — Dark Mode Theme State Not Lifted)
  </read_first>
  <behavior>
    - App.tsx mounts and loadSettings() resolves; isDark reflects stored theme (resolving 'system' via prefers-color-scheme)
    - applyTheme runs once on mount so body.dark class matches isDark
    - Clicking gear icon calls handleOpenSettings, which increments settingsOpenCount and sets view='settings'
    - SettingsPage renders inside a div with className "view-slide-in-right" and inline style { overflow: 'hidden' }, with key={settingsOpenCount}
    - Re-opening settings remounts SettingsPage and replays slide-in
    - Toggling dark mode inside SettingsPage updates App.tsx via onToggleDark, persists via saveSettings + applyTheme
    - KlasTabStrip receives current isDark (consumed in Task 3)
  </behavior>
  <action>
    A) Edit src/App.tsx:
    - Add imports for loadSettings and applyTheme from the existing settings util (use the same relative path as SettingsPage uses for ../../utils/settings, adjusted to App.tsx location: `'../utils/settings'`).
    - Add state: `const [settingsOpenCount, setSettingsOpenCount] = useState(0);` and `const [isDark, setIsDark] = useState<boolean>(false);`.
    - Add a useEffect that calls loadSettings on mount: resolve theme to dark when `s.theme === 'dark'` OR (`s.theme === 'system'` AND `window.matchMedia('(prefers-color-scheme: dark)').matches`). Call setIsDark with the result and applyTheme(s.theme ?? 'light').
    - Update handleOpenSettings to call `setSettingsOpenCount(c => c + 1)` BEFORE `setView('settings')`.
    - Add `function handleToggleDark(next: boolean) { setIsDark(next); }`. Persistence remains inside SettingsPage; App.tsx only mirrors the value.
    - In JSX: pass `isDark={isDark}` to the existing KlasTabStrip call site (new prop).
    - Wrap the SettingsPage mount: `{view === 'settings' && (<div className="view-slide-in-right" style={{ overflow: 'hidden' }}><SettingsPage key={settingsOpenCount} onBack={handleBackFromSettings} onNavigateToImport={handleNavigateToImportFromSettings} isDark={isDark} onToggleDark={handleToggleDark} /></div>)}`.

    B) Edit src/components/SettingsPage.tsx:
    - Extend interface SettingsPageProps with `isDark: boolean;` and `onToggleDark: (isDark: boolean) => void;`.
    - Destructure the two new props in the component signature.
    - REMOVE the local useState that owned dark mode (the current internal source of truth). Replace internal reads/writes with the `isDark` prop.
    - In the dark-mode toggle onChange: after persisting via saveSettings({ theme: nextTheme }) and calling applyTheme(nextTheme), call `onToggleDark(nextTheme === 'dark' || (nextTheme === 'system' && window.matchMedia('(prefers-color-scheme: dark)').matches))` so App.tsx stays in sync.
    - Preserve all Phase 17/18 logic (deelgebieden table, leerlijnen, thresholds, BPV) untouched. Only the dark-mode data flow changes.

    Do NOT modify visual layout of SettingsPage in this task — only data flow.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20 && powershell -Command "$a = Get-Content src/App.tsx -Raw; if ($a -notmatch 'settingsOpenCount') { exit 1 }; if ($a -notmatch 'view-slide-in-right') { exit 1 }; if ($a -notmatch 'isDark=') { exit 1 }; $s = Get-Content src/components/SettingsPage.tsx -Raw; if ($s -notmatch 'onToggleDark') { exit 1 }; if ($s -notmatch 'isDark') { exit 1 }; Write-Host OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains literal `useState(0)` for settingsOpenCount and `useState<boolean>(false)` (or equivalent) for isDark
    - src/App.tsx imports `loadSettings` and `applyTheme` from the settings util
    - src/App.tsx calls `setSettingsOpenCount(c => c + 1)` inside handleOpenSettings
    - src/App.tsx wraps SettingsPage in a div with className="view-slide-in-right"
    - src/App.tsx passes `key={settingsOpenCount}`, `isDark={isDark}`, `onToggleDark={handleToggleDark}` to SettingsPage
    - src/App.tsx passes `isDark={isDark}` to KlasTabStrip
    - src/components/SettingsPage.tsx interface SettingsPageProps declares both `isDark: boolean` and `onToggleDark: (isDark: boolean) => void`
    - src/components/SettingsPage.tsx does NOT contain a local useState that initialises a dark/theme value independently of the props (verify the previous internal source of truth is removed)
    - `npm test` exits 0 — full suite still green (any Phase 17/18 SettingsPage tests still pass; if they break because of the prop signature change, update those tests to provide the new props with sensible defaults)
  </acceptance_criteria>
  <done>App owns isDark + settingsOpenCount, SettingsPage is a controlled child for dark mode, slide-in wrapper is in JSX, test suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: KlasTabStrip logo + isDark prop; CSS for slide animation, nav diagonal stripe, nav-tab z-index, .aanvullend-grid responsive</name>
  <files>src/components/KlasTabStrip.tsx, src/index.css, src/components/DetailWeergave.tsx</files>
  <read_first>
    - src/components/KlasTabStrip.tsx (current props interface lines 3-10; current nav JSX — confirm where to insert logo img BEFORE the first klas tab and AFTER any existing left-side elements)
    - src/components/DetailWeergave.tsx (locate the .aanvullend-grid usage — confirm whether the grid template is set in CSS or as inline style; this determines whether the min-width:0 fix lands in CSS only or needs a JSX className adjustment)
    - src/index.css (confirm current #main-nav rule around line 236; .nav-tab rule around lines 251-264; find the existing .aanvullend-grid rule and its child selectors)
    - src/assets/ (confirm logo-light.svg and logo-dark.svg exist — Task 1 checkpoint output)
    - .planning/phases/19-ui-polish/19-PATTERNS.md (sections "src/components/KlasTabStrip.tsx", "Section 4 — Nav #main-nav additions", "Settings slide-in animation")
    - .planning/phases/19-ui-polish/19-UI-SPEC.md (Corporate Identity Refresh — confirmed nav design)
    - .planning/phases/19-ui-polish/19-RESEARCH.md (Pitfall 4 — sticky + overflow:hidden compat)
  </read_first>
  <behavior>
    - KlasTabStrip renders an <img alt="CIOS Zuidwest logo" /> as the leftmost element in #main-nav with src=logoLight when isDark=false and src=logoDark when isDark=true; image height 36px, width auto, marginRight 16px
    - Tabs render to the right of the logo with the existing layout
    - #main-nav has overflow: hidden and a ::after diagonal blue stripe in the top-right corner; tabs render above the stripe (z-index)
    - Opening settings triggers .view-slide-in-right animation in App.tsx wrapper
    - .aanvullend-grid children have min-width: 0 so the 2-column grid never bleeds at 1024px (POL-03 audit item)
    - Reduced-motion users see no animation (existing media query at index.css line 168 already handles this)
  </behavior>
  <action>
    A) Edit src/components/KlasTabStrip.tsx:
    - Add `isDark: boolean` to the KlasTabStripProps interface (no default — required prop).
    - Destructure `isDark` in component signature.
    - At the top of the file, add asset imports: `import logoLight from '../assets/logo-light.svg';` and `import logoDark from '../assets/logo-dark.svg';` (if user provided .png in Task 1, swap the extensions accordingly).
    - In the <nav id="main-nav"> JSX, insert as the FIRST child (before the klas tab buttons and the existing nav buttons): `<img src={isDark ? logoDark : logoLight} alt="CIOS Zuidwest logo" style={{ height: '36px', width: 'auto', marginRight: '16px' }} />`.

    B) Edit src/index.css — append these additions in their appropriate sections (do NOT duplicate existing rules — extend the existing #main-nav rule, add the new ::after as a new rule, etc.):

    1. Extend the existing #main-nav rule (around line 236-249): add `overflow: hidden;` to its declarations block. (Sticky behavior remains unaffected per Pitfall 4.)

    2. Add a new rule immediately after #main-nav:
       `#main-nav::after { content: ''; position: absolute; top: 0; right: 0; width: 120px; height: 52px; background: linear-gradient(to bottom-left, #009FE3 0%, transparent 65%); pointer-events: none; z-index: 0; }`

    3. Extend the existing .nav-tab rule (around line 251-264): add `position: relative;` and `z-index: 1;` so tabs render above the diagonal stripe. If the existing rule already has position, keep it; only ensure z-index: 1 is present.

    4. Add the settings slide-in animation as a new section near the end of the file:
       ```
       .view-slide-in-right { animation: slideInFromRight var(--transition-base) ease forwards; }
       @keyframes slideInFromRight {
         from { transform: translateX(100%); opacity: 0.7; }
         to   { transform: translateX(0);    opacity: 1;   }
       }
       ```

    5. POL-03 responsive fix for .aanvullend-grid children: add a rule `.aanvullend-grid > * { min-width: 0; }`. Place it adjacent to the existing .aanvullend-grid rule (search the file first to locate that section). If .aanvullend-grid does not exist as a CSS class yet (it may be inline style), then add the rule that scopes to the inline-styled container in DetailWeergave by giving the container a className `.aanvullend-grid` — adjust DetailWeergave.tsx accordingly to apply the className.

    C) Conditionally edit src/components/DetailWeergave.tsx ONLY IF the .aanvullend-grid container is currently an inline-styled element without that className. In that case: add `className="aanvullend-grid"` to the container element so the CSS rule in step B.5 applies. If the className already exists, no JSX change is required.

    RESPONSIVE AUDIT NOTE (cross-AI review): The RESEARCH.md responsive audit (Overflow Risk Summary) already verified all views at 1024px. Only `.aanvullend-grid` has a confirmed medium-risk overflow. All other components (KlasOverzicht grid, klas-toolbar, spider row, settings page, BpvProgressSection) are confirmed OK — no additional responsive CSS is needed beyond B.5. Do NOT add min-width rules, flex-wrap, or overflow changes to other components based on speculation.

    POL-04 HOVER AUDIT NOTE (cross-AI review): The RESEARCH.md hover audit confirmed that `.kpi-tile:hover` (line 354), `.nav-tab:hover` (line 265), and `.klas-tile:hover` (line 416) already exist in `src/index.css`. POL-04 is satisfied by G1–G4 from Plan 02 (new Phase 16–18 selectors) plus the nav-tab z-index fix here. No new hover rules for tiles or nav-tabs are required in Plan 04.

    Do NOT touch utils/spider.tsx, SpiderChartCard.tsx, or any tokens added in Plan 02 — those are already in place.
  </action>
  <verify>
    <automated>powershell -Command "npm test; if ($LASTEXITCODE -ne 0) { exit 1 }; npm run build; if ($LASTEXITCODE -ne 0) { exit 1 }; $k = Get-Content src/components/KlasTabStrip.tsx -Raw; if ($k -notmatch 'logoLight') { Write-Host 'FAIL: logoLight import missing'; exit 1 }; if ($k -notmatch 'isDark') { Write-Host 'FAIL: isDark prop missing'; exit 1 }; $c = Get-Content src/index.css -Raw; if ($c -notmatch '@keyframes slideInFromRight') { Write-Host 'FAIL: animation missing'; exit 1 }; if ($c -notmatch '#main-nav::after') { Write-Host 'FAIL: nav stripe missing'; exit 1 }; if ($c -notmatch 'aanvullend-grid') { Write-Host 'FAIL: responsive fix missing'; exit 1 }; Write-Host OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/KlasTabStrip.tsx contains `isDark: boolean` in the props interface
    - src/components/KlasTabStrip.tsx imports both `logoLight` and `logoDark` from `../assets/`
    - src/components/KlasTabStrip.tsx renders `<img` with `alt="CIOS Zuidwest logo"` and `src={isDark ? logoDark : logoLight}`
    - src/index.css contains the literal `@keyframes slideInFromRight`
    - src/index.css contains the literal `#main-nav::after`
    - src/index.css contains `linear-gradient(to bottom-left, #009FE3 0%, transparent 65%)` (CIOS blue diagonal)
    - src/index.css `#main-nav` rule contains `overflow: hidden`
    - src/index.css `.nav-tab` rule contains `z-index: 1` (or higher)
    - src/index.css contains `.aanvullend-grid > * { min-width: 0; }` or an equivalent rule applying min-width: 0 to .aanvullend-grid children
    - `npm test` exits 0 (full suite green)
    - `npm run build` exits 0 (TypeScript + Vite + Tauri build succeeds — confirms logo asset imports resolve)
    - If logo files were provided as .png in Task 1: KlasTabStrip.tsx imports use `.png` extensions (not `.svg`); build confirms correct resolution regardless of format
  </acceptance_criteria>
  <done>Logo renders in nav with light/dark switching, diagonal stripe is visible, settings panel slides in on open, .aanvullend-grid no longer overflows at 1024px, tests + build succeed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (Checkpoint): Human verify nav, settings animation, and responsive layout in Tauri</name>
  <what-built>The brand refresh is now visible: logo in nav, diagonal CIOS blue stripe, settings slide-in animation, hover effects on all settings inputs and detail-section cards, responsive layout at 1024px.</what-built>
  <how-to-verify>
    Run `npm run dev` to open the Tauri dev window. Verify each of:
    1. Light mode: logo "cios ZUIDWEST-NL" appears at top-left of nav. Switch to dark mode in settings → logo swaps to "cios" mark only.
    2. Top-right of nav shows a diagonal blue (#009FE3) accent stripe; nav tabs are visible above it (not clipped).
    3. Click the gear icon → settings page slides in from the right over ~200ms; close and reopen → animation replays.
    4. In settings, hover the leerlijn dropdown and the number inputs → border color shifts (G1/G2 hover gaps from Plan 02).
    5. Hover a detail-section card on the detail view → shadow lifts (G4 hover gap from Plan 02).
    6. Hover a spider chart data point → tooltip shows "{label}: {Score}" e.g. "V&A: Goed" (POL-02 from Plan 03).
    7. Resize Tauri window to 1024px wide → no horizontal scrollbar appears on any view (klas overzicht, detail, settings, import); .aanvullend-grid in detail does not overflow.
    8. Industry font is loaded (DevTools → Network → confirm IndustryTest-Book.otf was fetched 200; text in app renders in Industry, not system sans-serif).
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass. If any fail, list the failing item(s) and what you observed; the executor will diagnose and fix before resuming.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logo assets | New static SVG/PNG bundled with app |
| Dark mode prop wiring | State source moved from child to parent; downstream consumers depend on the new prop contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Information Disclosure | logo SVG files | accept | Static brand assets; no PII; user-provided |
| T-19-07 | Tampering | App.tsx isDark prop drilling | mitigate | TypeScript interface enforces presence of isDark on KlasTabStrip + SettingsPage; build fails on missing prop |
| T-19-08 | Denial of Service | view-slide-in-right animation | mitigate | Reduced-motion media query at index.css line 168 globally zeros animations; users with motion sensitivity unaffected |
</threat_model>

<verification>
- Full `npm test` suite GREEN
- `npm run build` exits 0 (logo imports resolve, no missing assets)
- Human verification checklist (Task 4) all 8 items PASS
</verification>

<success_criteria>
- POL-03 closed: 1024px width has no horizontal scroll on any view; .aanvullend-grid handles narrow widths
- POL-04 closed (combined with Plan 02): all hover gaps fixed; settings slide-in animation in place; logo + nav stripe show the CIOS identity
- Dark mode lift is complete; SettingsPage is a controlled child for theme
- All four POL-2/3/4 requirements observable to user in Tauri window
</success_criteria>

<output>
After completion, create `.planning/phases/19-ui-polish/19-04-SUMMARY.md`
</output>
