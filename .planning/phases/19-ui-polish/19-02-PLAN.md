---
phase: 19
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/assets/fonts/IndustryTest-Book.otf
  - src/assets/fonts/IndustryTest-Bold.otf
  - src/assets/fonts/IndustryTest-Demi.otf
autonomous: true
requirements: [POL-04]
tags: [css, design-tokens, typography, fonts, brand]

user_setup:
  - service: font-assets
    why: "Industry OTF font files (D-08) live outside the repo and must be copied into src/assets/fonts/ for Vite bundling"
    files_to_copy:
      - source: "C:\\Users\\rafae\\Desktop\\bestanden voor dashboard testing\\design\\industry font\\IndustryTest-Book.otf"
        target: "src/assets/fonts/IndustryTest-Book.otf"
      - source: "C:\\Users\\rafae\\Desktop\\bestanden voor dashboard testing\\design\\industry font\\IndustryTest-Bold.otf"
        target: "src/assets/fonts/IndustryTest-Bold.otf"
      - source: "C:\\Users\\rafae\\Desktop\\bestanden voor dashboard testing\\design\\industry font\\IndustryTest-Demi.otf"
        target: "src/assets/fonts/IndustryTest-Demi.otf"

must_haves:
  truths:
    - "All accent token usages (buttons, focus rings, active tabs) render in CIOS blue #009FE3 instead of indigo #4F46E5"
    - "All four missing hover gaps (G1-G4) have CSS rules in src/index.css"
    - "html/body/#root font-family is 'Industry' first; Google Fonts @import is removed"
    - "Three hardcoded rgba(79,70,229,0.12) focus rings are updated to rgba(0,159,227,0.12)"
    - "Material elevation values applied to --shadow-sm/--shadow-md/--shadow-lg"
    - "body.dark block contains updated accent token overrides"
    - "Industry OTF files are present at src/assets/fonts/"
  artifacts:
    - path: "src/index.css"
      provides: "Updated design tokens, @font-face, hover gaps, focus-ring rgba fixes"
      contains: "@font-face"
    - path: "src/assets/fonts/IndustryTest-Book.otf"
      provides: "Industry Regular 400 OTF"
    - path: "src/assets/fonts/IndustryTest-Bold.otf"
      provides: "Industry Bold 700 OTF"
    - path: "src/assets/fonts/IndustryTest-Demi.otf"
      provides: "Industry Demi 600 OTF"
  key_links:
    - from: "src/index.css :root --accent"
      to: "all components consuming var(--accent)"
      via: "CSS custom property inheritance"
      pattern: "--accent:\\s*#009FE3"
    - from: "src/index.css @font-face Industry"
      to: "src/assets/fonts/IndustryTest-*.otf"
      via: "url('./assets/fonts/...')"
      pattern: "@font-face"
---

<objective>
Apply the Phase 19 brand refresh and hover-gap fixes purely in CSS + asset copy. Replaces indigo accent tokens with CIOS blue `#009FE3` (D-05), swaps Google Fonts Inter for bundled Industry OTF (D-07, D-08), lifts the shadow tokens to Material elevation values (D-04), updates 3 hardcoded rgba focus rings (Pitfall 3), and adds the 4 confirmed hover gaps G1–G4 (POL-04).

Purpose: Establish the design foundation that every subsequent plan consumes. No component code changes here — pure tokens, typography, and missing hover rules.

Output: Updated `src/index.css`; new `src/assets/fonts/` directory containing 3 OTF files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ui-polish/19-CONTEXT.md
@.planning/phases/19-ui-polish/19-RESEARCH.md
@.planning/phases/19-ui-polish/19-PATTERNS.md
@.planning/phases/19-ui-polish/19-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy Industry OTF font files into src/assets/fonts/</name>
  <files>src/assets/fonts/IndustryTest-Book.otf, src/assets/fonts/IndustryTest-Bold.otf, src/assets/fonts/IndustryTest-Demi.otf</files>
  <read_first>
    - .planning/phases/19-ui-polish/19-UI-SPEC.md (section "Typography System (Industry Font)" — confirms exact filenames and source path)
    - .planning/phases/19-ui-polish/19-RESEARCH.md (Pitfall 5 — OTF relative path)
  </read_first>
  <action>
    Create the directory `src/assets/fonts/` if it does not exist. Copy the following 3 OTF files from the user's design folder `C:\Users\rafae\Desktop\bestanden voor dashboard testing\design\industry font\` into `src/assets/fonts/`:

    - IndustryTest-Book.otf  (weight 400)
    - IndustryTest-Bold.otf  (weight 700)
    - IndustryTest-Demi.otf  (weight 600)

    Use PowerShell `Copy-Item` (preferred on Windows) or `cp` via Bash. Preserve exact filenames — `src/index.css` @font-face URLs in Task 2 reference them literally.

    Do NOT copy italic/medium/light variants; only the 3 weights above are used by Phase 19.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path src/assets/fonts/IndustryTest-Book.otf -and (Test-Path src/assets/fonts/IndustryTest-Bold.otf) -and (Test-Path src/assets/fonts/IndustryTest-Demi.otf)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/assets/fonts/IndustryTest-Book.otf` with size > 10 KB
    - File exists at `src/assets/fonts/IndustryTest-Bold.otf` with size > 10 KB
    - File exists at `src/assets/fonts/IndustryTest-Demi.otf` with size > 10 KB
    - No additional Industry*.otf files copied (Medium, Light, italic variants omitted)
  </acceptance_criteria>
  <done>The 3 OTF files are present at src/assets/fonts/ with their exact filenames.</done>
</task>

<task type="auto">
  <name>Task 2: Update src/index.css — brand tokens, @font-face, shadows, hover gaps, focus-ring rgba</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (full file — confirm current line numbers for the targets below; line numbers in this plan are from research and may have drifted by ±a few lines)
    - .planning/phases/19-ui-polish/19-PATTERNS.md (section "src/index.css" — exact target values and locations)
    - .planning/phases/19-ui-polish/19-UI-SPEC.md (section "Color" and "Typography" — confirmed token values)
    - .planning/phases/19-ui-polish/19-RESEARCH.md (CSS Token Audit and Hover & Transition Audit)
  </read_first>
  <action>
    Edit `src/index.css` in place. Make these grouped edits — extend only, never replace the file. Per D-05, D-07, D-08, D-04, D-14, D-15.

    A) FONT IMPORT (around line 9):
    - Remove the `@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');` line.
    - Insert three `@font-face` blocks (Industry Book 400, Bold 700, Demi 600) BEFORE the `:root` block. Use `src: url('./assets/fonts/IndustryTest-Book.otf') format('opentype');` style relative paths, `font-display: swap;`, `font-style: normal;`. See 19-PATTERNS.md for the exact block bodies.

    B) ACCENT TOKENS (:root block, around lines 16–20 and 37):
    Replace exactly these 6 values in place — do NOT add a duplicate token, edit the existing declaration:
    - `--accent`        : `#4F46E5` → `#009FE3`
    - `--accent-hover`  : `#4338CA` → `#007DBF`
    - `--accent-light`  : `#EEF2FF` → `#E0F5FD`
    - `--accent-border` : `#C7D2FE` → `#99D9F4`
    - `--accent-text`   : `#3730A3` → `#00547A`
    - `--border-focus`  : `#4F46E5` → `#009FE3`
    Preserve `--nav-bg: #FFFFFF` (D-03: nav stays white) and legacy aliases `--accent-blue`/etc. that reference `var(--accent)`.

    C) SHADOW TOKENS — MATERIAL ELEVATION (around lines 41–43):
    Replace `--shadow-sm`, `--shadow-md`, `--shadow-lg` values in place with:
    - `--shadow-sm: 0 2px 4px rgba(15, 23, 42, 0.12), 0 1px 2px rgba(15, 23, 42, 0.08);`
    - `--shadow-md: 0 4px 8px rgba(15, 23, 42, 0.16), 0 2px 4px rgba(15, 23, 42, 0.10);`
    - `--shadow-lg: 0 8px 16px rgba(15, 23, 42, 0.20), 0 4px 8px rgba(15, 23, 42, 0.12);`
    Preserve `--shadow-xs` unchanged.

    D) FONT-FAMILY (around line 181 on `html, body, #root` rule):
    Change the leading family from `'Inter'` to `'Industry'`. Preserve the rest of the fallback chain unchanged.

    E) DARK MODE ACCENT OVERRIDES (inside the existing `body.dark { }` block around lines 111–141):
    Add or update these 6 declarations (keep them grouped near the existing accent-related dark overrides, do not create a second body.dark block):
    - `--accent: #009FE3;`
    - `--accent-hover: #007DBF;`
    - `--accent-light: #003B57;`
    - `--accent-border: #005F8E;`
    - `--accent-text: #7DD4F5;`
    - `--border-focus: #009FE3;`

    F) HARDCODED rgba FOCUS RINGS (Pitfall 3 — search for the literal `rgba(79, 70, 229, 0.12)` or `rgba(79,70,229,0.12)`; should appear 3× around lines 304, 1112, 1183 in `.klas-zoek:focus`, `.dg-naam-input:focus`, `.settings-number-input:focus` — verify before editing):
    Replace every occurrence with `rgba(0, 159, 227, 0.12)`.
    Also search for and update the `.dg-leerlijn-select:focus` box-shadow if it uses the same indigo rgba (per PATTERNS line-1130 note).

    G) HOVER GAP FIXES (POL-04, G1–G4) — add these 4 new rules into the appropriate sections (each new rule placed adjacent to its existing `:focus` or base rule):
    - `.dg-leerlijn-select:hover { border-color: var(--accent-border); }`
    - `.settings-number-input:hover { border-color: var(--accent-border); }`
    - `.toggle-switch:hover .toggle-track:not(.on) { background: var(--text-faint); }`
    - `.detail-section:hover { box-shadow: var(--shadow-md); transition: box-shadow var(--transition-base); }`

    H) DARK MODE THEME TRANSITION (cross-AI review suggestion — prevents jarring flash on theme toggle):
    Add a transition rule to the `html` or `body` rule in `src/index.css` (place it in Section 9 — Body/Global rules, adjacent to the existing `body` rule):
    - `body { transition: background-color var(--transition-fast), color var(--transition-fast); }`
    This ensures the dark/light switch animates smoothly rather than snapping. It is separate from `body.dark { }` and applies globally.
    NOTE: `.kpi-tile:hover` (line 354), `.nav-tab:hover` (line 265), and `.klas-tile:hover` (line 416) already exist in `src/index.css` per the RESEARCH.md hover audit — no action needed for those selectors. G1–G4 are the only confirmed missing hover rules.

    Do NOT touch the spider chart CSS, animation/keyframes, or nav `::after` stripe — those belong to Plans 03 and 04. Do NOT replace the file; only the edits above.
  </action>
  <verify>
    <automated>powershell -Command "npm test; if ($LASTEXITCODE -ne 0) { Write-Host 'FAIL: test suite'; exit 1 }; $c = Get-Content src/index.css -Raw; if ($c -notmatch '@font-face') { Write-Host 'FAIL: @font-face missing'; exit 1 }; if ($c -match '#4F46E5') { Write-Host 'FAIL: indigo accent still present'; exit 1 }; if ($c -match 'rgba\(79,\s*70,\s*229') { Write-Host 'FAIL: hardcoded indigo rgba still present'; exit 1 }; if ($c -notmatch '#009FE3') { Write-Host 'FAIL: CIOS blue missing'; exit 1 }; if ($c -notmatch 'detail-section:hover') { Write-Host 'FAIL: G4 hover gap missing'; exit 1 }; if ($c -notmatch '--nav-bg:\s*#FFFFFF') { Write-Host 'FAIL: --nav-bg token overwritten'; exit 1 }; Write-Host 'OK'"</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains the literal `@font-face` declaration at least 3 times (Book, Bold, Demi)
    - src/index.css contains `url('./assets/fonts/IndustryTest-Book.otf')`
    - src/index.css `:root` block sets `--accent: #009FE3;` (verify via grep `--accent:\s*#009FE3`)
    - src/index.css does NOT contain the literal token `#4F46E5` or `#4338CA` or `#EEF2FF` or `#C7D2FE` or `#3730A3` anywhere (old indigo values fully removed)
    - src/index.css does NOT contain `rgba(79, 70, 229` or `rgba(79,70,229` (old hardcoded indigo focus rgba removed)
    - src/index.css contains `rgba(0, 159, 227, 0.12)` at least 3 times
    - src/index.css `body.dark` block contains `--accent: #009FE3` (case-sensitive)
    - src/index.css contains `'Industry',` in the html/body/#root font-family rule
    - src/index.css does NOT contain the Google Fonts `@import url('https://fonts.googleapis.com/css2?family=Inter` line
    - src/index.css contains all 4 new hover selectors: `.dg-leerlijn-select:hover`, `.settings-number-input:hover`, `.toggle-switch:hover .toggle-track:not(.on)`, `.detail-section:hover`
    - src/index.css contains the new `--shadow-sm: 0 2px 4px rgba(15, 23, 42, 0.12)` Material value
    - src/index.css contains `font-display: swap` in all 3 @font-face blocks (prevents invisible text during font load)
    - src/index.css `:root` block still contains `--nav-bg: #FFFFFF` (D-03: nav stays white — verify not accidentally removed during accent token edits)
    - src/index.css `body` rule (global, not body.dark) contains `transition: background-color` (dark mode flash prevention)
    - `npm test` exits 0 with the same pass count as before this plan EXCEPT for the spider.test.ts failures introduced by Plan 01 (those remain RED)
  </acceptance_criteria>
  <done>Brand tokens, fonts, shadows, dark mode overrides, focus rgba and hover gaps are all in src/index.css; test suite green except for the expected Plan 01 spider RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset bundle | OTF font files become part of the shipped Tauri binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Information Disclosure | OTF font files | accept | "Test" font files are licensed per user; risk is licensing, not security; no PII in font assets |
| T-19-03 | Tampering | src/index.css design tokens | accept | Local file edits only; no remote fetch (Google Fonts removed → reduced supply-chain surface) |
</threat_model>

<verification>
- `npm test` passes (spider.test.ts failures from Plan 01 are the only expected RED)
- `npm run dev` (manual) — Tauri window shows Industry font and CIOS blue accent (manual check at executor's option)
- No new component code changes in this plan
</verification>

<success_criteria>
- All 6 accent tokens updated in :root and body.dark
- All 3 shadow tokens updated to Material values
- @font-face declares Industry 400/700/600 referencing OTF files
- Google Fonts @import removed
- 3 hardcoded indigo rgba focus rings replaced with CIOS blue rgba
- 4 hover gap rules added
- 3 OTF files present in src/assets/fonts/
</success_criteria>

<output>
After completion, create `.planning/phases/19-ui-polish/19-02-SUMMARY.md`
</output>
