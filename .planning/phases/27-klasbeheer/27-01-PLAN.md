---
phase: 27-klasbeheer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/klassen.ts
  - tests/storage.test.ts
autonomous: true
requirements:
  - KLS-02
  - KLS-03

must_haves:
  truths:
    - "renameKlas(klasId, newNaam) exists as an exported async function in utils/klassen.ts"
    - "renameKlas updates the naam field on the klas object in klassenState.klassen"
    - "renameKlas calls saveKlassen() and persists the new name across a simulated app restart"
    - "renameKlas returns false for unknown klasId and does not call saveKlassen"
    - "After rename + save + reload, existing student data on the klas is unchanged (data integrity)"
  artifacts:
    - path: "utils/klassen.ts"
      provides: "renameKlas export"
      exports: ["renameKlas"]
    - path: "tests/storage.test.ts"
      provides: "RNM-01, RNM-02 unit tests"
      contains: "renameKlas"
  key_links:
    - from: "utils/klassen.ts"
      to: "tests/storage.test.ts"
      via: "named import renameKlas"
      pattern: "renameKlas"
---

<objective>
Add the renameKlas(klasId, newNaam) utility function to utils/klassen.ts and cover it with unit tests.

Purpose: KlasTabStrip (Plan 02) will call this function on inline-input commit. The function must exist and be tested before the UI is wired.
Output: Exported renameKlas function + 2 green tests in tests/storage.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-klasbeheer/27-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add renameKlas() to utils/klassen.ts and test it</name>
  <files>utils/klassen.ts, tests/storage.test.ts</files>

  <read_first>
    - utils/klassen.ts — read entirely before editing; understand deleteKlas (lines 94-115) and saveKlassen patterns to replicate correctly
    - tests/storage.test.ts — read entirely; understand the existing mock setup (LazyStore class mock, invoke btoa encoding, beforeEach reset) to add tests in the same style without duplicating mock declarations
  </read_first>

  <behavior>
    - RNM-01: renameKlas('klas_1', 'Nieuwe Naam') updates klassenState.klassen['klas_1'].naam to 'Nieuwe Naam', calls saveKlassen(), and a subsequent loadKlassen() restores naam as 'Nieuwe Naam'. Additionally, after the full save + reset + load cycle, klassenState.klassen['klas_1'].students must still equal the value it had before the rename (assert data integrity — renaming must not touch any field other than naam).
    - RNM-02: renameKlas('nonexistent_id', 'X') returns false without modifying klassenState.klassen and without calling saveKlassen
  </behavior>

  <action>
    In utils/klassen.ts, after the deleteKlas function (after line 115) add a new exported async function renameKlas(klasId: string, newNaam: string): Promise<boolean>.

    Implementation contract:
    - Guard: if (!klassenState.klassen[klasId]) return false immediately
    - Mutation: klassenState.klassen[klasId].naam = newNaam (direct field update — no object spread needed, same pattern as deleteKlas mutates the klassen Record)
    - Persist: await saveKlassen() and return true
    - No uniqueness check (per D-06 — non-empty trim validation is the caller's responsibility, not this function's)
    - Function header comment: "// ── renameKlas(klasId, newNaam) — KLS-02/KLS-03 ─────────────────────────────"

    In tests/storage.test.ts, add renameKlas to the existing import statement on the line that imports from '../utils/klassen'. Then append two tests after the last existing test block:

    Test RNM-01: In beforeEach or at the start of the test, set klassenState.klassen to have klas_1 with naam 'Oud Naam' and students set to a known value (e.g., [] or a small array — match whatever beforeEach establishes, but ensure students is explicitly set so it can be asserted afterward). Call await renameKlas('klas_1', 'Nieuwe Naam'), then assert klassenState.klassen['klas_1'].naam === 'Nieuwe Naam'. Then call await saveKlassen() + reset klassenState (set klassen back to {}) + await loadKlassen() and assert the restored naam is 'Nieuwe Naam'. Also assert klassenState.klassen['klas_1'].students deep-equals the value it had before the rename call — this proves renameKlas did not mutate any field except naam.

    Test RNM-02: klassenState.klassen is empty (set in beforeEach). Call await renameKlas('nonexistent', 'X') and assert the return value is false. Assert Object.keys(klassenState.klassen).length === 0 (no side effects).

    Run tests RED first (test file edited, function not yet added), verify RNM-01 and RNM-02 fail, then add the function to make them GREEN.
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npx vitest run tests/storage.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - tests/storage.test.ts has tests named RNM-01 and RNM-02 that both pass
    - RNM-01 asserts naam changed AND students unchanged after persist/reload cycle (both assertions present in test source)
    - utils/klassen.ts exports renameKlas: grep -c "export async function renameKlas" utils/klassen.ts returns 1
    - Full test suite passes: npx vitest run exits with 150+ tests passing, 0 failures
    - renameKlas does not contain any trim or non-empty guard internally (validation is caller's responsibility per D-06); grep -c "\.trim()" utils/klassen.ts returns the same count as before adding the function
  </acceptance_criteria>

  <done>
    - tests/storage.test.ts has tests named RNM-01 and RNM-02 that both pass
    - RNM-01 explicitly asserts students field is unchanged after the rename cycle
    - utils/klassen.ts exports renameKlas (grep -c "export async function renameKlas" utils/klassen.ts returns 1)
    - Full test suite passes: npx vitest run exits with 150+ tests passing, 0 failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → renameKlas | newNaam string is caller-supplied; function trusts that caller has already validated non-empty trim (D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | klassenState.klassen mutation | accept | klassenState is an in-process singleton; no external surface exposed in this function |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
npx vitest run tests/storage.test.ts -- RNM tests pass.
npx vitest run (full suite) -- all 150+ tests pass, 0 failures.
grep -c "export async function renameKlas" utils/klassen.ts returns 1.
RNM-01 test source contains an assertion on .students after reload (data integrity coverage).
</verification>

<success_criteria>
renameKlas is exported from utils/klassen.ts. RNM-01 and RNM-02 tests pass. RNM-01 proves that students data is intact after a rename + persist + reload cycle. Full suite green.
</success_criteria>

<output>
Create .planning/phases/27-klasbeheer/27-01-SUMMARY.md when done.
</output>
