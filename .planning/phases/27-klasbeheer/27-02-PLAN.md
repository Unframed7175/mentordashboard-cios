---
phase: 27-klasbeheer
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - src/components/KlasTabStrip.tsx
  - src/components/KlasOverzicht.tsx
  - src/App.tsx
  - src/index.css
  - tests/KlasTabStrip.test.tsx
autonomous: true
requirements:
  - KLS-01
  - KLS-02
  - KLS-03

must_haves:
  truths:
    - "Each tab is a <div role='tab'> element (NOT a <button>), so the inner × <button> is valid HTML with no nested interactive content"
    - "A × button appears on hover (and on :focus-within) inside each tab whose klas has canDelete === true, and is invisible otherwise"
    - "Clicking × triggers window.confirm with the klas name, then calls deleteKlas and setRefreshKey on confirm"
    - "Double-clicking a tab's name span replaces it with a focused inline <input> pre-filled with the current naam; input.select() fires so text is pre-selected"
    - "Pressing Enter or blurring the input saves the new name (if non-empty trim); pressing Escape cancels and restores original text"
    - "commitRename() is guarded by an isCommittingRef so Enter-then-blur does not double-submit"
    - "Saving a rename calls onRenameKlas then setRefreshKey so the tab strip and all consumers re-render with the new name"
    - "The delete button and footer block are removed from KlasOverzicht.tsx"
    - "CSS hides .delete-tab-btn by default, shows it on .nav-tab:hover and .nav-tab:focus-within"
    - "Component tests TAB-01, TAB-02, TAB-03 pass in tests/KlasTabStrip.test.tsx"
  artifacts:
    - path: "src/components/KlasTabStrip.tsx"
      provides: "hover × delete + double-click inline rename with valid HTML structure"
      exports: ["default"]
      contains: "editingKlasId"
    - path: "src/components/KlasOverzicht.tsx"
      provides: "delete button removed (D-07)"
    - path: "src/App.tsx"
      provides: "handleDeleteKlas and handleRenameKlas wired to KlasTabStrip"
    - path: "src/index.css"
      provides: "CSS for .delete-tab-btn visibility (hover + focus-within) and inline tab input"
    - path: "tests/KlasTabStrip.test.tsx"
      provides: "TAB-01, TAB-02, TAB-03 component tests"
      contains: "KlasTabStrip"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/KlasTabStrip.tsx"
      via: "onDeleteKlas and onRenameKlas props"
      pattern: "onDeleteKlas|onRenameKlas"
    - from: "src/components/KlasTabStrip.tsx"
      to: "utils/klassen.ts"
      via: "onRenameKlas callback (which calls renameKlas)"
      pattern: "onRenameKlas"
---

<objective>
Wire the tab-strip delete and rename UI: add hover × icon to empty-klas tabs (using valid HTML structure), add double-click inline rename input, remove the legacy KlasOverzicht delete button, add the required CSS, and cover the new UI behaviors with component tests.

Purpose: Delivers KLS-01 (tab × delete for empty klassen), KLS-02 (rename without data loss), KLS-03 (name change immediately visible in tab strip). Removes the old delete path (D-07). Fixes nested-button HTML invalidity from the pre-review plan.
Output: KlasTabStrip with valid markup + delete + rename, App.tsx with two new handlers, KlasOverzicht without legacy delete, CSS for hover/focus × and inline input, 3 passing component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-klasbeheer/27-CONTEXT.md
@.planning/phases/27-klasbeheer/27-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From utils/klassen.ts (after Plan 01):
  export async function deleteKlas(klasId: string): Promise<boolean>
  export async function renameKlas(klasId: string, newNaam: string): Promise<boolean>
  export const klassenState: { klassen: Record<string, any>; activeKlasId: string | null; onboardingCompleted: boolean }

From src/App.tsx (line 18):
  const [refreshKey, setRefreshKey] = useState(0);
  // setRefreshKey(k => k + 1) is the pattern used after every mutation

From src/components/KlasTabStrip.tsx (current props interface):
  interface KlasTabStripProps {
    klassen: Array<{ id: string; naam: string }>;
    activeKlasId: string | null;
    onSwitch: (klasId: string) => void;
    onCreateKlas: () => void;
    onSettings: () => void;
    isSettingsActive: boolean;
    isDark: boolean;
  }

From src/components/KlasOverzicht.tsx (lines 135-148, 243-250 — to be removed in Task 2):
  // handleDelete function (lines 135-148) — remove entirely
  // Footer JSX block (lines 243-250) — remove entirely

From src/index.css (existing .nav-tab rules around lines 287-311):
  .nav-tab { display: inline-flex; align-items: center; padding: 0.375rem 0.875rem; ... }
  .nav-tab:hover { color: var(--text-primary); background: var(--bg-surface-alt); border-color: var(--border-default); }
  .nav-tab.active { background: var(--accent-light); color: var(--accent-text); border-color: var(--accent-border); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend KlasTabStrip and wire App.tsx callbacks</name>
  <files>src/components/KlasTabStrip.tsx, src/App.tsx</files>

  <read_first>
    - src/components/KlasTabStrip.tsx — read entirely; understand the current props interface, the map over klassen, and the existing button structure before adding any code
    - src/App.tsx — read entirely; understand the KlasTabStrip render site (around line 114-122), the setRefreshKey pattern, and existing handler function signatures to model handleDeleteKlas and handleRenameKlas correctly
    - utils/klassen.ts — read lines 93-115 (deleteKlas) and the new renameKlas function from Plan 01 to confirm exact function signatures before calling them
    - .planning/phases/27-klasbeheer/27-CONTEXT.md — re-read D-01 through D-06 before implementing; the inline decisions must be honored exactly
  </read_first>

  <action>
    KlasTabStrip.tsx — full rewrite of the component:

    1. Extend KlasTabStripProps with two new required props:
       - onDeleteKlas: (klasId: string) => void
       - onRenameKlas: (klasId: string, newNaam: string) => void
       Change the klas array item type to use canDelete: boolean instead of students: any[].
       The new klas item type is: { id: string; naam: string; canDelete: boolean }
       KlasTabStrip never needs to inspect the students array — it only needs to know whether deletion is permitted.

    2. Add local state inside the component using React.useState:
       - editingKlasId: string | null (initially null)
       - editValue: string (initially '')
       Also add a ref to guard against double-submit on commitRename:
       - isCommittingRef: React.useRef<boolean>(false) — initialized to false

    3. Add a useEffect that fires when editingKlasId changes. When editingKlasId is a non-null string, find the input element inside the active editing tab and call .select() on it so the full current name is pre-selected for immediate overwrite. Use a selector like document.querySelector('.tab-rename-input') or use a React ref attached to the input element — either approach is acceptable. This fires immediately after React renders the input into the DOM.

    4. The commitRename helper (define inside the component, before the return):
       - Signature: function commitRename(klasId: string): void
       - Guard against double-submit: if (isCommittingRef.current) return; set isCommittingRef.current = true at the top of the function body.
       - If editValue.trim().length === 0 → setEditingKlasId(null); isCommittingRef.current = false; return (revert, per D-05 and D-06)
       - Otherwise → onRenameKlas(klasId, editValue.trim()); setEditingKlasId(null); isCommittingRef.current = false
       - Pattern: both branches reset isCommittingRef.current to false before returning so the guard is never permanently locked.

    5. In the klassen.map() render, DO NOT use a <button> as the outer tab wrapper. Instead use:
       <div role="tab" tabIndex={0} className={`nav-tab${klas.id === activeKlasId ? ' active' : ''}`}
            onClick={() => { if (editingKlasId !== klas.id) onSwitch(klas.id); }}
            onKeyDown={e => { if ((e.key === 'Enter' || e.key === ' ') && editingKlasId !== klas.id) onSwitch(klas.id); }}>
       This structure is valid HTML: the outer element is a div with ARIA role, not a button, so the inner × button is not nested inside an interactive button.

    6. Inside each <div role="tab">, render two children:
       a. Conditional naam / input child:
          - When editingKlasId === klas.id: render <input type="text" value={editValue} autoFocus className="tab-rename-input" onChange={e => setEditValue(e.target.value)} onKeyDown={e => { if (e.key === 'Enter') commitRename(klas.id); if (e.key === 'Escape') { setEditingKlasId(null); isCommittingRef.current = false; } }} onBlur={() => commitRename(klas.id)} onClick={e => e.stopPropagation()} />
          - When not editing: render <span onDoubleClick={e => { e.stopPropagation(); setEditingKlasId(klas.id); setEditValue(klas.naam); isCommittingRef.current = false; }}>{klas.naam}</span>
       b. Conditional delete button (per D-01 — only when canDelete is true):
          <button className="delete-tab-btn" title="Klas verwijderen" aria-label={`Klas ${klas.naam} verwijderen`} onClick={e => { e.stopPropagation(); onDeleteKlas(klas.id); }}>×</button>
          Only render this button when klas.canDelete === true.

    App.tsx — two new async handler functions and updated KlasTabStrip props:

    1. Add import of deleteKlas and renameKlas to the existing import from '../utils/klassen' (deleteKlas was already imported by KlasOverzicht but App.tsx does not yet import it directly).

    2. Add handleDeleteKlas async function:
       - Signature: async function handleDeleteKlas(klasId: string): Promise<void>
       - Body: const confirmed = window.confirm(`Klas '${klassenState.klassen[klasId]?.naam ?? klasId}' verwijderen? Dit kan niet ongedaan worden gemaakt.`); if (!confirmed) return; await deleteKlas(klasId); setRefreshKey(k => k + 1);
       - Per D-02: confirm message uses the klas naam; on confirm calls deleteKlas then setRefreshKey

    3. Add handleRenameKlas async function:
       - Signature: async function handleRenameKlas(klasId: string, newNaam: string): Promise<void>
       - Body: await renameKlas(klasId, newNaam); setRefreshKey(k => k + 1);

    4. Update the <KlasTabStrip ...> JSX at the render site (~line 114) to pass:
       - onDeleteKlas={handleDeleteKlas}
       - onRenameKlas={handleRenameKlas}
       Update the klassen prop construction: for each klas in Object.values(klassenState.klassen), compute canDelete as (klassenState.klassen[klas.id]?.students?.length ?? 1) === 0, and pass { id: klas.id, naam: klas.naam, canDelete } to the array.
       Do NOT pass students: any[] — the tabstrip only receives canDelete: boolean per the updated prop contract.
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - npx tsc --noEmit exits 0 (no type errors)
    - KlasTabStrip.tsx does NOT contain "button" as the outer tab wrapper: grep for the pattern "role=\"tab\"" in src/components/KlasTabStrip.tsx returns >= 1; grep -c "<button" src/components/KlasTabStrip.tsx returns exactly 1 (the inner × button only, no outer tab button)
    - KlasTabStrip.tsx prop type uses canDelete: boolean (grep -c "canDelete" src/components/KlasTabStrip.tsx returns >= 2); does NOT contain "students: any" (grep -c "students: any" src/components/KlasTabStrip.tsx returns 0)
    - KlasTabStrip.tsx contains editingKlasId state (grep -c "editingKlasId" src/components/KlasTabStrip.tsx returns >= 3)
    - KlasTabStrip.tsx contains isCommittingRef (grep -c "isCommittingRef" src/components/KlasTabStrip.tsx returns >= 3)
    - KlasTabStrip.tsx contains delete-tab-btn class (grep -c "delete-tab-btn" src/components/KlasTabStrip.tsx returns >= 1)
    - App.tsx contains handleDeleteKlas and handleRenameKlas functions (grep -c "handleDeleteKlas" src/App.tsx returns >= 2)
    - App.tsx klassen prop construction uses canDelete and does NOT pass students (grep -c "canDelete" src/App.tsx returns >= 1)
  </acceptance_criteria>

  <done>
    - npx tsc --noEmit exits 0 (no type errors)
    - KlasTabStrip outer tab wrapper is <div role="tab"> not <button>
    - KlasTabStrip prop type uses canDelete: boolean, not students: any[]
    - isCommittingRef guard present in commitRename
    - App.tsx contains handleDeleteKlas and handleRenameKlas, and passes canDelete prop
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy delete from KlasOverzicht and add CSS</name>
  <files>src/components/KlasOverzicht.tsx, src/index.css</files>

  <read_first>
    - src/components/KlasOverzicht.tsx — read entirely; locate the handleDelete function (lines 135-148) and the footer JSX block (lines 243-250) precisely before removing anything; also check the import line (line 8) which imports deleteKlas — remove that import too if deleteKlas is no longer used in this file after the removal
    - src/index.css — read lines 280-320 to see the full existing .nav-tab ruleset so the new CSS rules integrate correctly without duplicating selectors
    - .planning/phases/27-klasbeheer/27-CONTEXT.md — re-read D-07 and the specifics section (× icon CSS and inline input width) before writing CSS
  </read_first>

  <action>
    KlasOverzicht.tsx — two removals (per D-07):

    1. Remove the handleDelete function entirely (lines 135-148, inclusive of the async function handleDelete() { ... } block).

    2. Remove the footer JSX block entirely (lines 243-250, the {/* Footer: delete klas */} comment and the conditional div that wraps the "Klas verwijderen" button).

    3. After removal, check the import on line 8: if deleteKlas is no longer referenced anywhere in the file, remove deleteKlas from the import statement. If klassenState is still used elsewhere in the component (it is — used in the trendMap memo and sortAsc logic), keep it. Only remove deleteKlas from the named imports.

    4. Also remove the onKlasDeleted prop from KlasOverzichtProps interface and the destructured prop list since it is no longer called. Verify that KlasOverzicht.tsx still compiles without onKlasDeleted. Then update App.tsx: remove the onKlasDeleted={handleBack} prop from the <KlasOverzicht> JSX — it no longer exists. (handleBack function itself stays in App.tsx as it is used by DetailWeergave.)

    src/index.css — append after the existing .nav-tab.active ruleset (around line 311), add the following rules:

    Rule 1 — hide × by default:
    Selector: .nav-tab .delete-tab-btn
    Properties: opacity: 0; pointer-events: none; transition: opacity 0.15s ease; background: transparent; border: none; cursor: pointer; font-size: 0.85rem; line-height: 1; padding: 0 0 0 0.35rem; color: var(--text-muted);

    Rule 2 — show × on tab hover:
    Selector: .nav-tab:hover .delete-tab-btn
    Properties: opacity: 1; pointer-events: auto;

    Rule 3 — show × on keyboard focus (accessibility — so keyboard users can reach the × without a mouse):
    Selector: .nav-tab:focus-within .delete-tab-btn
    Properties: opacity: 1; pointer-events: auto;

    Rule 4 — inline rename input (do NOT set a fixed width property; use min-width and max-width only so the input grows to content rather than clipping longer class names):
    Selector: .tab-rename-input
    Properties: background: transparent; border: none; border-bottom: 1px solid var(--accent); outline: none; font-size: inherit; font-family: inherit; color: inherit; min-width: 5ch; max-width: 20ch; padding: 0;
    (Omit any "width: Nch" line entirely — the field should stretch between min and max based on content. Using a hard width: 8ch clips longer names.)
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npx tsc --noEmit 2>&1 | head -20 && npx vitest run 2>&1 | tail -6</automated>
  </verify>

  <acceptance_criteria>
    - handleDelete function is gone from KlasOverzicht.tsx: grep -c "handleDelete" src/components/KlasOverzicht.tsx returns 0
    - "Klas verwijderen" text is gone from KlasOverzicht.tsx: grep -c "Klas verwijderen" src/components/KlasOverzicht.tsx returns 0
    - onKlasDeleted is gone from KlasOverzicht.tsx: grep -c "onKlasDeleted" src/components/KlasOverzicht.tsx returns 0
    - .delete-tab-btn appears in src/index.css for both :hover and :focus-within rules: grep -c "delete-tab-btn" src/index.css returns >= 3 (one base rule + hover + focus-within)
    - .tab-rename-input is in src/index.css: grep -c "tab-rename-input" src/index.css returns >= 1
    - .tab-rename-input does NOT contain a bare "width:" property (only min-width and max-width): grep -A 10 "tab-rename-input" src/index.css must NOT show a line matching "^\s*width:" (only min-width and max-width lines present)
    - :focus-within rule exists: grep -c "focus-within" src/index.css returns >= 1
    - npx tsc --noEmit exits 0
    - npx vitest run exits 0 with 150+ tests passing
  </acceptance_criteria>

  <done>
    - handleDelete function and "Klas verwijderen" JSX are gone from KlasOverzicht.tsx
    - onKlasDeleted removed from props and App.tsx JSX
    - .delete-tab-btn CSS has 3 rules: base (opacity:0), :hover (opacity:1), :focus-within (opacity:1)
    - .tab-rename-input CSS uses min-width/max-width only (no hard width property)
    - npx tsc --noEmit exits 0
    - npx vitest run exits 0 with 150+ tests passing
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write KlasTabStrip component tests</name>
  <files>tests/KlasTabStrip.test.tsx</files>

  <read_first>
    - src/components/KlasTabStrip.tsx — read the final version (after Task 1) to understand exact prop names, class names, and element structure before writing tests
    - tests/storage.test.ts — read the first 40 lines to understand the existing Vitest + Testing Library setup patterns (imports, describe block style, render approach) used in this project
    - vitest.config.ts or vite.config.ts — read to confirm jsdom environment is configured (or check existing component test files if any exist) so you know whether to add @vitest-environment jsdom pragma
  </read_first>

  <behavior>
    - TAB-01: When a klas has canDelete === true, the × button renders. When canDelete === false, no × button is present.
    - TAB-02: Double-clicking the naam span enters editing state — the span disappears and a text input with className "tab-rename-input" appears, pre-filled with the klas naam.
    - TAB-03: When in editing state, pressing Enter calls onRenameKlas with the trimmed input value; pressing Escape does NOT call onRenameKlas and the input disappears.
  </behavior>

  <action>
    Create tests/KlasTabStrip.test.tsx as a new file. Do not create it inside src/ — it goes in the top-level tests/ directory alongside storage.test.ts.

    Import structure:
    - import { render, screen, fireEvent } from '@testing-library/react' (or userEvent if the project already uses it)
    - import { describe, it, expect, vi, beforeEach } from 'vitest'
    - import KlasTabStrip from '../src/components/KlasTabStrip'

    Shared test props (define a helper makeProps() or a beforeEach):
    - klassen: [{ id: 'k1', naam: 'Klas A', canDelete: true }, { id: 'k2', naam: 'Klas B', canDelete: false }]
    - activeKlasId: 'k1'
    - onSwitch: vi.fn()
    - onCreateKlas: vi.fn()
    - onSettings: vi.fn()
    - onDeleteKlas: vi.fn()
    - onRenameKlas: vi.fn()
    - isSettingsActive: false
    - isDark: false

    Test TAB-01: Render with the shared props. Query for elements with aria-label matching /verwijderen/i. Assert that exactly 1 × button is found (for k1 which has canDelete: true). Assert that no × button exists for 'Klas B' (canDelete: false). Concrete assertion: expect(screen.getAllByRole('button', { name: /verwijderen/i })).toHaveLength(1).

    Test TAB-02: Render with shared props. Find the span containing 'Klas A' (the naam text for the active tab). Fire a doubleClick event on it. Assert that an input element with className "tab-rename-input" is now in the document. Assert that the input's value equals 'Klas A'. Concrete assertion: expect(screen.getByRole('textbox')).toHaveValue('Klas A').

    Test TAB-03 (Enter saves): Render with shared props. Fire doubleClick on the 'Klas A' span. Get the input. Change its value to 'Klas Nieuw' via fireEvent.change. Fire keyDown Enter on the input. Assert onRenameKlas was called with ('k1', 'Klas Nieuw'). Assert the input is no longer in the document (editing state cleared).

    Test TAB-03 (Escape cancels): In a separate it() block (or as TAB-03b), render, double-click, change value to 'Iets Anders', fire keyDown Escape. Assert onRenameKlas was NOT called. Assert the input is no longer in the document.

    Use fireEvent.keyDown(input, { key: 'Enter', code: 'Enter' }) and fireEvent.keyDown(input, { key: 'Escape', code: 'Escape' }) for key events.

    If the project requires @vitest-environment jsdom pragma for component tests (check by looking at vitest.config.ts), add // @vitest-environment jsdom as the very first line of the file.

    Run tests RED first before Task 1 is wired (import will succeed but behaviors will fail), then confirm GREEN after Task 1's component is complete.
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npx vitest run tests/KlasTabStrip.test.tsx 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - tests/KlasTabStrip.test.tsx exists and contains TAB-01, TAB-02, TAB-03 test names (grep -c "TAB-0" tests/KlasTabStrip.test.tsx returns >= 3)
    - All three test cases pass when running npx vitest run tests/KlasTabStrip.test.tsx
    - TAB-01 uses a role-based or aria-label query (not a className query) to assert × button count
    - TAB-02 asserts that an input with value 'Klas A' appears after double-click (screen.getByRole('textbox') present)
    - TAB-03 asserts onRenameKlas.mock.calls.length === 1 with correct arguments after Enter; asserts onRenameKlas was not called after Escape
    - Full suite still passes: npx vitest run exits 0 with all tests green (including the new component tests)
  </acceptance_criteria>

  <done>
    - tests/KlasTabStrip.test.tsx exists with TAB-01, TAB-02, TAB-03 tests
    - All three tests pass
    - Full vitest suite green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user double-click → inline input | User-supplied rename string enters React state; validated on commit (non-empty trim per D-06) |
| user × click → window.confirm | Confirmation dialog prevents accidental delete; deleteKlas only called on explicit confirm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02 | Tampering | renameKlas call from KlasTabStrip | mitigate | commitRename validates trim().length > 0 before calling onRenameKlas; empty strings revert silently; isCommittingRef prevents double-submit |
| T-27-03 | Tampering | deleteKlas via × button | mitigate | window.confirm dialog gates the deletion; e.stopPropagation() prevents double-fire with onSwitch |
| T-27-04 | Spoofing | × button accessibility | mitigate | aria-label on delete button identifies the klas by name for screen readers; :focus-within CSS makes × reachable via keyboard |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
npx tsc --noEmit exits 0 — no TypeScript errors across all modified files.
npx vitest run exits 0 — all 150+ tests pass including TAB-01, TAB-02, TAB-03.
Manual smoke: double-click a tab name → input appears with autoFocus and text pre-selected. Type new name → Enter saves and tab strip updates. Escape cancels. Hover empty-klas tab → × appears. Tab-focus a tab → × appears via :focus-within. Click × → confirm dialog → on confirm tab is removed.
</verification>

<success_criteria>
KLS-01: Empty-klas tabs (canDelete: true) show × on hover and on keyboard focus; click × + confirm → klas deleted, tab strip refreshes.
KLS-02: Double-click tab → inline input with pre-selected text → Enter/blur saves new name via renameKlas; Escape cancels; empty trim reverts; isCommittingRef prevents double-submit.
KLS-03: After rename, setRefreshKey fires and all consumers (tab strip, KlasOverzicht header) show updated naam.
D-07: No "Klas verwijderen" button exists in KlasOverzicht. Full test suite green. No TypeScript errors. TAB-01/02/03 component tests pass.
HTML validity: outer tab element is <div role="tab">, inner × is a standalone <button> — no nested interactive elements.
</success_criteria>

<output>
Create .planning/phases/27-klasbeheer/27-02-SUMMARY.md when done.
</output>
