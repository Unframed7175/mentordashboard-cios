---
phase: 17
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/settings.ts
  - src/components/SettingsPage.tsx
  - tests/SettingsPage.test.tsx
  - tests/vitest-setup.js
  - vitest.config.ts
autonomous: true
requirements: [SET-01, POL-01]
must_haves:
  truths:
    - "Toggling the dark mode switch immediately adds or removes the `dark` class on document.body"
    - "Toggling the switch persists `{ theme: 'dark' | 'light' }` under the 'settings' key in the plugin-store"
    - "On mount, when a saved theme exists, it is applied and the toggle reflects it"
    - "On mount, when no saved theme exists, the OS preference (matchMedia) is applied but NOT persisted"
    - "On first paint, the toggle reflects the hydrated theme — no flicker from a default `isDark=false` while async loadSettings() runs (Codex MEDIUM concern resolved by initializing isDark from `document.body.classList.contains('dark')` which main.tsx hydrates BEFORE React mounts in Plan 17-03)"
    - "Vitest discovers the SettingsPage test file (BLOCKER 1 resolved by placing the test at `tests/SettingsPage.test.tsx` so the current include pattern `tests/**/*.test.{js,ts,tsx}` picks it up)"
    - "Clicking the 'Bestanden toevoegen' button invokes onNavigateToImport"
    - "Clicking '← Terug' invokes onBack"
    - "Phase 18 placeholder section cards render with 'Komt in een volgende versie.' text"
  artifacts:
    - path: "utils/settings.ts"
      provides: "loadSettings(), saveSettings(), applyTheme() — settings-store helpers (mirror utils/klassen.ts pattern)"
      exports: ["loadSettings", "saveSettings", "applyTheme"]
    - path: "src/components/SettingsPage.tsx"
      provides: "SettingsPage component with dark mode toggle, Bestanden CTA, and Phase 18 placeholders"
      exports: ["default SettingsPage"]
    - path: "tests/SettingsPage.test.tsx"
      provides: "Vitest coverage for SET-01 (toggle + persist + restore + OS fallback) and SET-02 (import nav callback)"
      contains: "describe"
    - path: "tests/vitest-setup.js"
      provides: "jest globals shim + window.matchMedia stub (added in this plan so jsdom tests can render components that read OS theme preference)"
      contains: "matchMedia"
  key_links:
    - from: "SettingsPage onChange handler"
      to: "document.body.classList"
      via: "classList.toggle('dark', checked)"
      pattern: "classList\\.toggle\\(['\"]dark['\"]"
    - from: "SettingsPage onChange handler"
      to: "plugin-store 'settings' key"
      via: "saveSettings({ theme })"
      pattern: "saveSettings"
    - from: "SettingsPage useState initializer"
      to: "document.body.classList"
      via: "synchronous read of hydrated dark class (avoids flicker)"
      pattern: "document\\.body\\.classList\\.contains\\(['\"]dark['\"]"
    - from: "SettingsPage mount useEffect"
      to: "window.matchMedia"
      via: "OS-preference fallback when key absent"
      pattern: "matchMedia\\(['\"]\\(prefers-color-scheme: dark\\)['\"]"
---

<objective>
Create the `SettingsPage` React component, a `utils/settings.ts` helper that wraps the plugin-store `'settings'` key, a Vitest test file at `tests/SettingsPage.test.tsx` (NOT under `src/`, so the existing `vitest.config.ts` include pattern picks it up), and extend `tests/vitest-setup.js` with a `window.matchMedia` mock. The component renders the four section cards from the UI-SPEC: Weergave (toggle), Bestanden (CTA), and two Phase 18 placeholders.

Purpose: SET-01 requires a user-controlled toggle with persistent preference. POL-01 requires the toggle visual to use the new `.toggle-switch` / `.toggle-track` / `.toggle-thumb` classes from Plan 01. The settings store helper is split into `utils/settings.ts` so it can be imported by both the SettingsPage component and the App.tsx startup hydration in Plan 03 — without duplicating LazyStore wiring.

Resolves two cross-AI review issues:
- BLOCKER (Codex + Gemini): test files previously placed under `src/components/` would NOT be discovered by the current `vitest.config.ts` (`include: ['tests/**/*.test.{js,ts}']`). This plan places the test at `tests/SettingsPage.test.tsx` instead, and the include pattern is widened to also accept `.tsx` in Task 3's verify step (single-character change). No move of any existing test files.
- MEDIUM (Codex): the SettingsPage previously initialized `isDark` to `false`, which causes a brief incorrect-toggle render until the async `loadSettings()` resolves on mount — especially visible when `main.tsx` already hydrated `body.dark` in Plan 17-03. This plan initializes `isDark` from `document.body.classList.contains('dark')` so the visible toggle state matches the hydrated DOM from the very first paint.

Output: One new helper module, one new React component, one new test file, one tiny edit to `tests/vitest-setup.js`, and one tiny edit to `vitest.config.ts` (extend include to `.tsx`). Zero changes to existing production source files outside the new component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-settings-panel-foundation/17-CONTEXT.md
@.planning/phases/17-settings-panel-foundation/17-RESEARCH.md
@.planning/phases/17-settings-panel-foundation/17-UI-SPEC.md
@.planning/phases/17-settings-panel-foundation/17-VALIDATION.md
@.planning/phases/17-settings-panel-foundation/17-REVIEWS.md

@utils/klassen.ts
@tests/storage.test.ts
@tests/vitest-setup.js
@vitest.config.ts

<interfaces>
<!-- LazyStore pattern + existing test mocks -->

From @tauri-apps/plugin-store (already installed, version ^2.4.3):
```typescript
class LazyStore {
  constructor(filename: string, options?: { defaults?: object; autoSave?: boolean });
  get<T>(key: string): Promise<T | null | undefined>;
  set(key: string, value: unknown): Promise<void>;  // in-memory; MUST call save()
  save(): Promise<void>;                              // writes to disk
}
```

From utils/klassen.ts (replicate this pattern):
```typescript
import { LazyStore } from '@tauri-apps/plugin-store';
const store = new LazyStore('store.json', { defaults: {}, autoSave: false });
// On read: await store.get<...>('klassen')
// On write: await store.set('klassen', payload); await store.save();
```

LazyStore mock pattern from tests/storage.test.ts (Phase 12 — STATE.md line 64):
```typescript
// Must be ES6 class, not vi.fn() — required for `new LazyStore(...)` constructor call
vi.mock('@tauri-apps/plugin-store', () => ({
  LazyStore: class { /* get/set/save as methods */ }
}));
```

Current vitest.config.ts include pattern: `['tests/**/*.test.{js,ts}']`. Task 3 changes this to
`['tests/**/*.test.{js,ts,jsx,tsx}']` so a single `.tsx` test under `tests/` is discovered.
This is additive — every existing `.test.js`/`.test.ts` still matches.

Current tests/vitest-setup.js has NO matchMedia mock. jsdom does not provide
`window.matchMedia`, so SettingsPage tests (which read it during OS-fallback)
would throw. Task 3 extends the setup file with a configurable stub.

SettingsPage component contract (consumed by App.tsx in Plan 03):
```typescript
interface SettingsPageProps {
  onBack: () => void;
  onNavigateToImport: () => void;
}
export default function SettingsPage(props: SettingsPageProps): JSX.Element;
```

utils/settings.ts contract (consumed by App.tsx + SettingsPage):
```typescript
export type Theme = 'dark' | 'light';
export async function loadSettings(): Promise<{ theme: Theme } | null>;
export async function saveSettings(settings: { theme: Theme }): Promise<void>;
export function applyTheme(theme: Theme): void;  // toggles document.body.classList
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create utils/settings.ts helper module</name>
  <files>utils/settings.ts</files>
  <read_first>
    - utils/klassen.ts (LazyStore initialization + get/set/save pattern at lines ~11 and ~127–143)
    - .planning/phases/17-settings-panel-foundation/17-RESEARCH.md (Pattern 2 — plugin-store settings key + Pitfall 1)
  </read_first>
  <action>
    Create `utils/settings.ts` exporting three symbols: type alias `Theme = 'dark' | 'light'`; async function `loadSettings(): Promise<{ theme: Theme } | null>` that returns `await store.get<{ theme: Theme }>('settings')` from a module-level `LazyStore('store.json', { defaults: {}, autoSave: false })` instance (per D-07: same store file as klassen, separate key per RESEARCH Assumption A3); async function `saveSettings(settings: { theme: Theme }): Promise<void>` that calls `await store.set('settings', settings)` THEN `await store.save()` (Pitfall 1 — save() is mandatory); sync function `applyTheme(theme: Theme): void` that calls `document.body.classList.toggle('dark', theme === 'dark')`. Use the SAME `'store.json'` filename string as `utils/klassen.ts` so both keys share one disk file (D-07 + Assumption A3). Do not import React. Export each symbol with `export` so the test file can import them by name.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('utils/settings.ts','utf8');const checks=[/export\s+(type|async\s+function|function)\s+Theme/.test(src.replace(/type\s+/,'type '))||/export\s+type\s+Theme/.test(src),/export\s+async\s+function\s+loadSettings/.test(src),/export\s+async\s+function\s+saveSettings/.test(src),/export\s+function\s+applyTheme/.test(src),/LazyStore/.test(src),/['\"]store\.json['\"]/.test(src),/await\s+\w+\.save\(\)/.test(src),/classList\.toggle\(['\"]dark['\"]/.test(src)];if(checks.some(c=>!c))throw new Error('Missing exports/patterns: '+JSON.stringify(checks));console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - utils/settings.ts exists and exports: `Theme` (type), `loadSettings`, `saveSettings`, `applyTheme`
    - File contains `new LazyStore('store.json'` (same file as klassen)
    - `saveSettings` body contains `await store.set('settings'` followed by `await store.save()` (Pitfall 1)
    - `applyTheme` calls `document.body.classList.toggle('dark', ...)` — no other classList mutations
    - `npx tsc --noEmit` (or `npm run typecheck-migrated` if defined) reports no errors for this file
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Module created with three exports following the Phase 12 LazyStore pattern; save() always follows set(); typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create SettingsPage component with flicker-free initial state</name>
  <files>src/components/SettingsPage.tsx</files>
  <read_first>
    - utils/settings.ts (just created — confirm exports)
    - src/components/DetailWeergave.tsx (back-button pattern — `.detail-nav-btn` usage)
    - .planning/phases/17-settings-panel-foundation/17-UI-SPEC.md (Component Inventory sections 2–5 + Copywriting Contract)
    - .planning/phases/17-settings-panel-foundation/17-CONTEXT.md (D-05 through D-11)
    - .planning/phases/17-settings-panel-foundation/17-REVIEWS.md (Codex MEDIUM "Toggle flicker on open" concern — resolved by initializing isDark from document.body.classList)
  </read_first>
  <action>
    Create `src/components/SettingsPage.tsx` as a default-exported React function component with props `{ onBack: () => void; onNavigateToImport: () => void }`. Import `useEffect`, `useState` from React and `loadSettings`, `saveSettings`, `applyTheme`, type `Theme` from `../../utils/settings`.

    **Internal state — flicker-free initializer (Codex MEDIUM resolution):**
    `const [isDark, setIsDark] = useState<boolean>(() => typeof document !== 'undefined' && document.body.classList.contains('dark'));`

    Rationale: by the time SettingsPage mounts, Plan 17-03's startup hydration in `main.tsx` has already added or removed `body.dark` based on the persisted preference (or the OS fallback). Reading `document.body.classList.contains('dark')` synchronously during state initialization makes the toggle visually correct on the very first paint, eliminating the brief incorrect-state flash that would happen if we initialized with `false` and corrected via the async `loadSettings()` mount effect. The lazy initializer (`useState(() => ...)`) is required so the `document` access happens only on first render, not on every render. The `typeof document !== 'undefined'` guard is defensive for any future SSR scenario — under jsdom in tests, `document` is always defined.

    **On mount (useEffect, []):** call `loadSettings()`. If the returned object has a `theme` field, call `applyTheme(saved.theme)` and `setIsDark(saved.theme === 'dark')` — this re-syncs in case main.tsx hydration was skipped (e.g., older app version) or the persisted value changed between startup and Settings mount. If null/undefined (key absent), read `window.matchMedia('(prefers-color-scheme: dark)').matches`, call `applyTheme(prefersDark ? 'dark' : 'light')` and `setIsDark(prefersDark)` — but DO NOT call `saveSettings` (per D-06 / Pitfall 4: only persist explicit user choice). Defensive error handling: wrap the `loadSettings()` call in a try/catch and on failure log `console.warn('[SettingsPage] loadSettings failed:', err)` and leave state untouched (the lazy initializer already gave a sensible default).

    **Toggle handler `async function handleToggle(checked: boolean)`:** call `applyTheme(checked ? 'dark' : 'light')` THEN `setIsDark(checked)` THEN `await saveSettings({ theme: checked ? 'dark' : 'light' })`. Both DOM update and React state update happen in the same synchronous handler scope (Pitfall 6).

    **Render markup per UI-SPEC Component Inventory section 2:**
    - Outer: `<main className="settings-page">`
    - Header: `<div className="settings-header"><button className="detail-nav-btn" onClick={onBack}>← Terug</button><h1>Instellingen</h1></div>`
    - Section 1 "Weergave": `<section className="detail-section"><h2 className="detail-section-title">Weergave</h2>` containing `<label className="toggle-switch">` with hidden `<input type="checkbox" className="sr-only" checked={isDark} onChange={e => handleToggle(e.target.checked)} aria-label="Donkere modus" />`, a `<span className={` + "`toggle-track${isDark ? ' on' : ''}`" + `}><span className="toggle-thumb" /></span>`, and `<span>Donkere modus</span>`.
    - Section 2 "Bestanden": `<section className="detail-section"><h2 className="detail-section-title">Bestanden</h2><button className="btn btn-primary" onClick={onNavigateToImport}>Bestanden toevoegen</button>` followed by helper `<p>` with inline style `fontSize: '0.875rem', color: 'var(--text-muted)', marginTop: '8px'` containing the text `Voeg PDFs of een verzuim-Excel toe aan de actieve klas.` (UI-SPEC Copywriting).
    - Section 3 "Deelgebieden & Leerlijnen": `<section className="detail-section"><h2 className="detail-section-title">Deelgebieden &amp; Leerlijnen</h2><p className="settings-placeholder-text">Komt in een volgende versie.</p></section>`
    - Section 4 "Drempelwaarden & BPV-uren": same structure with title `Drempelwaarden &amp; BPV-uren` and the same placeholder text.

    All Dutch copy strings are EXACT per UI-SPEC Copywriting Contract — no English fallback.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('src/components/SettingsPage.tsx','utf8');const checks=[/export\s+default\s+function\s+SettingsPage/.test(src),/onBack/.test(src)&&/onNavigateToImport/.test(src),/loadSettings/.test(src)&&/saveSettings/.test(src)&&/applyTheme/.test(src),/useState[^(]*\(\s*\(\)\s*=>/.test(src),/document\.body\.classList\.contains\(['\"]dark['\"]\)/.test(src),/matchMedia\(['\"]\(prefers-color-scheme: dark\)['\"]\)/.test(src),/className=\"settings-page\"/.test(src),/className=\"settings-header\"/.test(src),/className=\"toggle-switch\"/.test(src),/className=\"toggle-track/.test(src),/className=\"toggle-thumb\"/.test(src),/className=\"settings-placeholder-text\"/.test(src),/← Terug/.test(src),/Instellingen/.test(src),/Donkere modus/.test(src),/Bestanden toevoegen/.test(src),/Komt in een volgende versie/.test(src),/aria-label=\"Donkere modus\"/.test(src),/try\s*\{[\s\S]*loadSettings[\s\S]*catch/.test(src)];const failed=checks.map((c,i)=>c?null:i).filter(x=>x!==null);if(failed.length)throw new Error('Failed checks: '+failed.join(','));console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SettingsPage.tsx` exports default function `SettingsPage` accepting `{ onBack, onNavigateToImport }`
    - Imports `loadSettings`, `saveSettings`, `applyTheme` from `../../utils/settings`
    - `isDark` is initialized via a lazy `useState(() => ...)` initializer that reads `document.body.classList.contains('dark')` (NOT a plain `useState(false)`)
    - useEffect mount handler calls `loadSettings()` inside try/catch; on success branches: persisted theme → apply + setIsDark; absent → `window.matchMedia('(prefers-color-scheme: dark)').matches` + apply + setIsDark, NO saveSettings call; on failure: console.warn and leave state alone
    - Toggle handler calls `applyTheme` AND `setIsDark` AND `saveSettings` (all three required)
    - Rendered elements include literal Dutch strings: `← Terug`, `Instellingen`, `Weergave`, `Donkere modus`, `Bestanden`, `Bestanden toevoegen`, `Komt in een volgende versie.`
    - Toggle input has `aria-label="Donkere modus"` and `className="sr-only"`
    - `npx vitest run` exits 0
    - `npx tsc --noEmit` (or equivalent) reports no errors for this file
  </acceptance_criteria>
  <done>SettingsPage renders with all four sections per UI-SPEC; mount logic respects D-06 (no persist on OS fallback); toggle synchronously updates DOM + React state + plugin-store; initial isDark matches hydrated body.dark so the toggle has no flicker on open.</done>
</task>

<task type="auto">
  <name>Task 3: Add matchMedia mock + extend vitest include pattern + create tests/SettingsPage.test.tsx</name>
  <files>tests/vitest-setup.js, vitest.config.ts, tests/SettingsPage.test.tsx</files>
  <read_first>
    - tests/vitest-setup.js (current 16-line file — see what is shimmed; add matchMedia alongside the jest globals)
    - vitest.config.ts (current include pattern — confirm widening to `.tsx` does not break existing tests)
    - tests/storage.test.ts (ES6-class LazyStore mock pattern — STATE.md line 64 mandate)
    - src/components/SettingsPage.tsx (just created — confirm props + import paths)
    - utils/settings.ts (just created — confirm export names for module mock)
    - .planning/phases/17-settings-panel-foundation/17-VALIDATION.md (Per-Task Verification Map)
    - .planning/phases/17-settings-panel-foundation/17-REVIEWS.md (BLOCKER 1 vitest discovery + MEDIUM matchMedia mock)
  </read_first>
  <action>
    Three coordinated edits — all in test infrastructure, no production code touched:

    **A. Extend `vitest.config.ts` include pattern:**
    Change the `include` line from `include: ['tests/**/*.test.{js,ts}']` to `include: ['tests/**/*.test.{js,ts,jsx,tsx}']`. This is additive — every existing `.test.js` and `.test.ts` file still matches. Leave every other config field (`environment: 'jsdom'`, `globals: true`, `setupFiles`, `coverage`, `pool: 'vmForks'`) untouched.

    **B. Add `window.matchMedia` stub to `tests/vitest-setup.js`:**
    Append a section after the existing `globalThis.jest = { ... }` block that defines `window.matchMedia` on the jsdom window with a configurable default. Use `Object.defineProperty(window, 'matchMedia', { writable: true, value: vi.fn().mockImplementation((query) => ({ matches: false, media: query, onchange: null, addListener: vi.fn(), removeListener: vi.fn(), addEventListener: vi.fn(), removeEventListener: vi.fn(), dispatchEvent: vi.fn() })) })`. The default `matches: false` means tests that do NOT override the stub get a "light mode" reading, which matches the prior implicit expectation. Tests can call `window.matchMedia.mockImplementation(() => ({ matches: true, ... }))` to simulate dark-mode OS preference. Do NOT remove the existing `globalThis.jest` shim — both blocks coexist.

    **C. Create `tests/SettingsPage.test.tsx`:**
    Vitest + `@testing-library/react`. Mock `@tauri-apps/plugin-store` using an ES6 class mock (NOT `vi.fn()` — STATE.md mandates this for `new LazyStore()` constructor calls). The mock class must expose `get`, `set`, `save` as methods backed by an in-memory `Map` keyed by store key so that round-trip persistence is observable. Import paths from `tests/` are relative — use `../src/components/SettingsPage` for the component and `../utils/settings` is not imported directly (the component imports it internally).

    Required test cases (each in its own `it(...)`):

    1. **SET-01 persistence — toggle adds body.dark and persists `{ theme: 'dark' }`:** ensure mock store map is empty AND `document.body.className = ''` (no pre-existing dark class); ensure `window.matchMedia` stub returns `matches: false`; render SettingsPage with stub callbacks; await mount effect; assert `document.body.classList.contains('dark') === false` initially; fire click on the checkbox to check it; assert `document.body.classList.contains('dark') === true`; await microtasks; assert the mock store's internal map contains key `'settings'` with value `{ theme: 'dark' }`; assert the mock's `save` method was called at least once after the `set`.

    2. **SET-01 restore — saved 'dark' theme applied on mount:** preload the mock store map with `{ settings: { theme: 'dark' } }`; ensure `document.body.className = ''`; render SettingsPage; await mount; assert `document.body.classList.contains('dark') === true`; assert the toggle checkbox is checked.

    3. **SET-01 OS fallback — empty store + matchMedia true → dark applied but NOT persisted:** ensure mock store map is empty; ensure `document.body.className = ''`; override `window.matchMedia.mockImplementation` to return `{ matches: true, media: '...', addListener: vi.fn(), removeListener: vi.fn(), addEventListener: vi.fn(), removeEventListener: vi.fn(), dispatchEvent: vi.fn(), onchange: null }`; render SettingsPage; await mount; assert `document.body.classList.contains('dark') === true`; assert the mock store map does NOT contain key `'settings'` (D-06 / Pitfall 4 — OS preference is NOT persisted).

    4. **Flicker-free initializer — body.dark already set before mount → toggle starts checked:** set `document.body.className = 'dark'` BEFORE render (simulating main.tsx startup hydration in Plan 17-03); preload mock store with `{ settings: { theme: 'dark' } }` so the mount effect does not flip state; render SettingsPage; immediately (before awaiting microtasks) read the toggle checkbox's `checked` attribute via `screen.getByRole('checkbox', { name: 'Donkere modus' })` — it MUST be true on the first paint, not after the async loadSettings settles. This test specifically guards the Codex MEDIUM "Toggle flicker on open" concern.

    5. **SET-02 — clicking "Bestanden toevoegen" invokes onNavigateToImport:** render SettingsPage with a `vi.fn()` for `onNavigateToImport`; query the button by text `Bestanden toevoegen`; click; assert the mock was called exactly once.

    6. **Back button — clicking ← Terug invokes onBack:** same approach as test 5 with `onBack` spy and button text `← Terug`.

    Use `beforeEach(() => { document.body.className = ''; /* reset matchMedia stub to default `matches: false` + clear store map */ })` to isolate tests. Use `@testing-library/react` `render`, `screen`, `fireEvent` (or `userEvent`). The component imports `loadSettings`/`saveSettings`/`applyTheme` from `../../utils/settings` — those functions are NOT mocked directly (they use the mocked LazyStore underneath). The mock module path is `@tauri-apps/plugin-store`.
  </action>
  <verify>
    <automated>npx vitest run tests/SettingsPage.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` include line literally reads `include: ['tests/**/*.test.{js,ts,jsx,tsx}']`
    - `tests/vitest-setup.js` contains `matchMedia` and uses `Object.defineProperty(window, 'matchMedia'`
    - `tests/vitest-setup.js` still contains the original `globalThis.jest` shim (regression guard)
    - File `tests/SettingsPage.test.tsx` exists (NOT `src/components/SettingsPage.test.tsx`)
    - File contains `vi.mock('@tauri-apps/plugin-store'` with `LazyStore: class` (ES6 class mock, not `vi.fn()`)
    - File defines exactly 6 `it(...)` blocks covering: toggle persists, restore on mount, OS fallback no-persist, flicker-free initializer, Bestanden toevoegen callback, ← Terug callback
    - `npx vitest run tests/SettingsPage.test.tsx` exits 0 with all 6 tests passing
    - Full suite `npx vitest run` exits 0 (no regression of existing 43 tests; total ≥ 49 tests)
    - Test 3 (OS fallback) explicitly asserts the store map does NOT contain key `'settings'` after mount
    - Test 4 (flicker-free) asserts the checkbox is checked on the first synchronous read (no `await` before the assertion)
  </acceptance_criteria>
  <done>matchMedia mock added; include pattern widened; 6 tests green at tests/SettingsPage.test.tsx; LazyStore mock uses ES6 class; full suite remains green.</done>
</task>

</tasks>

<verification>
- `npx vitest run` exits 0 with at least 49 tests passing (43 existing + 6 new)
- `utils/settings.ts`, `src/components/SettingsPage.tsx`, `tests/SettingsPage.test.tsx` all exist
- `tests/vitest-setup.js` contains a `matchMedia` mock; `vitest.config.ts` include matches `.tsx`
- `loadSettings`, `saveSettings`, `applyTheme` exported from `utils/settings.ts`
- SettingsPage uses `<label className="toggle-switch">` markup matching Plan 01 CSS classes
- SettingsPage's `isDark` state initializer reads `document.body.classList.contains('dark')` (no flicker on open)
</verification>

<success_criteria>
1. Three new files created (`utils/settings.ts`, `src/components/SettingsPage.tsx`, `tests/SettingsPage.test.tsx`); two test-infra files modified (`tests/vitest-setup.js`, `vitest.config.ts`)
2. SET-01 coverage: toggle persists, restore on mount, OS fallback does NOT persist, flicker-free initializer (4 tests)
3. SET-02 coverage: onNavigateToImport callback fires on Bestanden toevoegen click (1 test)
4. Back button: onBack callback fires on ← Terug click (1 test)
5. All Dutch strings exact per UI-SPEC Copywriting Contract
6. LazyStore mocked as ES6 class (Phase 12 mandate)
7. Test file in `tests/`, NOT `src/components/`, so the existing vitest include pattern discovers it (Codex BLOCKER resolved)
8. `window.matchMedia` available under jsdom via vitest-setup (Codex MEDIUM resolved)
</success_criteria>

<output>
After completion, create `.planning/phases/17-settings-panel-foundation/17-02-SUMMARY.md`
</output>
