---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
autonomous: true
requirements: [POL-01]
must_haves:
  truths:
    - "Adding the `dark` class to body switches the entire app to the dark theme via CSS custom properties"
    - "Removing the `dark` class from body returns the app to the light theme"
    - "OS `prefers-color-scheme: dark` no longer forces dark mode — JS controls it exclusively"
    - "Pure-CSS toggle switch renders correctly in both states with accessible focus outline"
    - "Settings page layout (header + section cards) has dedicated CSS classes available"
  artifacts:
    - path: "src/index.css"
      provides: "body.dark token overrides + section 24 (SettingsPage + toggle) styles"
      contains: "body.dark"
  key_links:
    - from: "body.dark"
      to: "CSS custom properties (--accent, --bg-page, --text-primary, etc.)"
      via: "selector override"
      pattern: "body\\.dark\\s*\\{"
    - from: "input.sr-only:focus-visible"
      to: ".toggle-track"
      via: "adjacent sibling selector"
      pattern: "\\.sr-only:focus-visible \\+ \\.toggle-track"
---

<objective>
Convert the existing `@media (prefers-color-scheme: dark)` block in `src/index.css` to a JS-controllable `body.dark { }` selector, then add a new section 24 of CSS for the SettingsPage layout and the dark-mode toggle switch. This phase delivers the CSS foundation that Plans 02 and 03 build on.

Purpose: POL-01 requires dark mode to apply consistently to all components through CSS custom properties. The selector swap is mechanical (all 20 tokens already exist), but it MUST be in place before any JS toggle code can apply a theme. The new `.settings-page`, `.settings-header`, `.toggle-switch`, `.toggle-track`, `.toggle-thumb`, and `.settings-placeholder-text` classes are required by SettingsPage.tsx (Plan 02).

Output: A single modified `src/index.css` containing the `body.dark { }` block, the removed/commented media query, and a new section 24 with 6 new CSS classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-settings-panel-foundation/17-CONTEXT.md
@.planning/phases/17-settings-panel-foundation/17-RESEARCH.md
@.planning/phases/17-settings-panel-foundation/17-UI-SPEC.md

@src/index.css

<interfaces>
<!-- Existing CSS tokens and classes that MUST be respected. -->

Existing :root tokens in src/index.css (light mode — keep unchanged):
  --accent, --accent-hover, --accent-light, --accent-border, --accent-text,
  --bg-page, --bg-surface, --bg-surface-alt, --nav-bg,
  --text-primary, --text-secondary, --text-muted, --text-faint,
  --border-default, --border-light,
  --shadow-xs, --shadow-sm, --shadow-md, --shadow-lg,
  --status-groen-bg/text, --status-oranje-bg/text, --status-rood-bg/text,
  --status-blauw-bg/text, --status-grijs-bg/text,
  --transition-base

Existing @media (prefers-color-scheme: dark) block at lines ~97–130 holds the dark
values for the same tokens — these MUST be moved verbatim into `body.dark { }`.

Existing classes reused (DO NOT REDEFINE):
  .detail-section, .detail-section-title, .detail-nav-btn,
  .nav-tab, .nav-tab.active, .nav-tab:hover,
  .btn, .btn-primary, .btn-primary:hover,
  .sr-only

@media (prefers-reduced-motion: reduce) block — DO NOT TOUCH (unrelated).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert dark mode media query to body.dark selector</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (full file — confirm current token list and locate lines ~97–130)
    - .planning/phases/17-settings-panel-foundation/17-UI-SPEC.md (Dark Mode Token Mapping section)
    - .planning/phases/17-settings-panel-foundation/17-RESEARCH.md (Pattern 3 + Pitfall 2)
  </read_first>
  <action>
    Replace the `@media (prefers-color-scheme: dark) { :root { ... } }` block in `src/index.css` with a `body.dark { }` block (per D-05). The new block lives at the same file location (immediately after the `:root` block where the media query currently sits). Copy every token value from the media query verbatim into the new `body.dark { }` block — do NOT change any color values, shadows, or status tokens. Required tokens to override (complete list): `--accent`, `--accent-hover`, `--accent-light`, `--accent-border`, `--accent-text`, `--bg-page`, `--bg-surface`, `--bg-surface-alt`, `--nav-bg`, `--text-primary`, `--text-secondary`, `--text-muted`, `--text-faint`, `--border-default`, `--border-light`, `--shadow-xs`, `--shadow-sm`, `--shadow-md`, `--shadow-lg`, `--status-groen-bg`, `--status-groen-text`, `--status-oranje-bg`, `--status-oranje-text`, `--status-rood-bg`, `--status-rood-text`, `--status-blauw-bg`, `--status-blauw-text`, `--status-grijs-bg`, `--status-grijs-text`. The original `@media (prefers-color-scheme: dark)` block MUST be removed entirely (Pitfall 2: leaving it produces conflicts). The `@media (prefers-reduced-motion: reduce)` block at line ~133 stays UNTOUCHED. The `:root` block stays UNCHANGED as the light-mode default.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('src/index.css','utf8');const hasBodyDark=/body\.dark\s*\{[\s\S]*--accent[\s\S]*--status-grijs-text[\s\S]*\}/.test(css);const noPrefersDark=!/@media\s*\(prefers-color-scheme:\s*dark\)/.test(css);const hasReducedMotion=/@media\s*\(prefers-reduced-motion:\s*reduce\)/.test(css);if(!hasBodyDark)throw new Error('body.dark block missing or incomplete');if(!noPrefersDark)throw new Error('prefers-color-scheme media query still present');if(!hasReducedMotion)throw new Error('prefers-reduced-motion block accidentally removed');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains exactly one `body.dark {` block
    - src/index.css does NOT contain any `@media (prefers-color-scheme: dark)` substring
    - src/index.css still contains `@media (prefers-reduced-motion: reduce)`
    - The `body.dark` block defines all 29 listed dark tokens (--accent through --status-grijs-text)
    - The `:root` block remains structurally unchanged (light values intact)
    - `npx vitest run` exits 0 (no regression in current 43 tests)
  </acceptance_criteria>
  <done>Media query removed; body.dark block in place with all 29 token overrides; reduced-motion block preserved; existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add section 24 CSS for SettingsPage layout and toggle switch</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (locate the last numbered section to determine where section 24 begins)
    - .planning/phases/17-settings-panel-foundation/17-UI-SPEC.md (Component Inventory sections 2–5 + CSS Classes to Create table)
    - .planning/phases/17-settings-panel-foundation/17-RESEARCH.md (Pattern 4 — Pure CSS Toggle Switch)
  </read_first>
  <action>
    Append a new "Section 24. SettingsPage" CSS block at the end of `src/index.css` (after all existing numbered sections, before any closing comments). Add EXACTLY these 6 classes — values are normative from 17-UI-SPEC.md:

    - `.settings-page` — `max-width: 640px; margin: 0 auto; padding: 32px 16px;`
    - `.settings-header` — `display: flex; align-items: center; gap: 12px; margin-bottom: 24px;` and nested `.settings-header h1` with `font-size: 1.25rem; font-weight: 700; color: var(--text-primary); margin: 0;`
    - `.toggle-switch` — `display: flex; align-items: center; gap: 12px; cursor: pointer;`
    - `.toggle-track` — `position: relative; width: 44px; height: 24px; border-radius: 12px; background: var(--border-default); border: 1.5px solid var(--border-default); transition: background var(--transition-base), border-color var(--transition-base); flex-shrink: 0;` PLUS a sibling rule `.toggle-track.on { background: var(--accent); border-color: var(--accent); }`
    - `.toggle-thumb` — `position: absolute; top: 2px; left: 2px; width: 18px; height: 18px; border-radius: 50%; background: #fff; transition: transform var(--transition-base); box-shadow: var(--shadow-xs);` PLUS sibling rule `.toggle-track.on .toggle-thumb { transform: translateX(22px); }` (UI-SPEC authoritative value, NOT 20px from RESEARCH — UI-SPEC overrides per Assumption A2)
    - `.settings-placeholder-text` — `font-size: 0.875rem; color: var(--text-faint); font-style: italic; margin: 0;`

    Also add the keyboard focus rule: `.sr-only:focus-visible + .toggle-track { outline: 2.5px solid var(--accent); outline-offset: 2px; }`. Do NOT redefine `.sr-only` itself — it already exists. Do NOT add any media queries (per UI-SPEC: minimum viewport 900px, no mobile breakpoints in this phase).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('src/index.css','utf8');const required=['.settings-page','.settings-header','.toggle-switch','.toggle-track','.toggle-thumb','.settings-placeholder-text','.toggle-track.on','translateX(22px)','.sr-only:focus-visible + .toggle-track'];for(const r of required){if(!css.includes(r))throw new Error('Missing: '+r);}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains all 6 new class selectors literally: `.settings-page`, `.settings-header`, `.toggle-switch`, `.toggle-track`, `.toggle-thumb`, `.settings-placeholder-text`
    - `.toggle-track.on` rule present (sets `background: var(--accent)`)
    - Thumb ON-state transform is exactly `translateX(22px)` (UI-SPEC authoritative)
    - Focus rule `.sr-only:focus-visible + .toggle-track` present with `outline: 2.5px solid var(--accent)`
    - No `@media` queries added in section 24
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>Section 24 appended with all 6 new classes; toggle ON-state uses translateX(22px); focus outline rule present; tests pass.</done>
</task>

</tasks>

<verification>
- `body.dark` is the sole dark-mode trigger (no `prefers-color-scheme` media query)
- All 6 new CSS classes from UI-SPEC component inventory are present
- `npx vitest run` exits 0
- Adding `<body class="dark">` in a browser DevTools session visually inverts the theme (manual smoke check available, not gated)
</verification>

<success_criteria>
1. `body.dark { }` block defines all 29 dark-mode token overrides (verbatim copies of the removed media query values)
2. `@media (prefers-color-scheme: dark)` no longer appears anywhere in `src/index.css`
3. `@media (prefers-reduced-motion: reduce)` still present
4. Section 24 contains the 6 new classes plus the `.toggle-track.on` and focus-visible rules
5. Existing test suite (43 tests) passes unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/17-settings-panel-foundation/17-01-SUMMARY.md`
</output>
