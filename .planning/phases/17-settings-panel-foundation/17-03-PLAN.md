---
phase: 17
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - src/App.tsx
  - src/main.tsx
  - src/components/KlasTabStrip.tsx
  - tests/KlasTabStrip.test.tsx
autonomous: true
requirements: [SET-01, SET-02, POL-01]
must_haves:
  truths:
    - "The ⚙ gear icon is visible on the far-right edge of the KlasTabStrip nav bar"
    - "Clicking the gear icon opens the SettingsPage (view === 'settings') and pins prevView to the previous view"
    - "Clicking ← Terug from SettingsPage restores the previous view (klas/import/detail)"
    - "Clicking 'Bestanden toevoegen' navigates to the ImportPage; completing the import lands on the klas view"
    - "On app startup (before the first paint of any view), the persisted theme is applied to body.dark so dark mode survives restart — and a try/catch around the hydration logic ensures a plugin-store failure cannot block app boot (Codex MEDIUM startup-resilience concern resolved, mirroring the loadKlassen() pattern already in main.tsx)"
    - "The gear icon shows the .nav-tab.active state while view === 'settings'"
    - "The previous '↑ Importeer' button is removed from KlasTabStrip — import is reached via Settings (Open Question #1 resolved)"
    - "SET-02 add-to-active-class is guaranteed by Phase 16's auto-detect-skip: when ImportPage.handlePDFs sees `activeKlasId !== null && students.length > 0`, it bypasses autoDetectKlas and appends students to the active class verbatim — the Settings → Bestanden toevoegen path lands in ImportPage with the same `klassenState.activeKlasId` and `students` that were active in the pre-Settings view, so the existing class is augmented (Codex BLOCKER on SET-02 resolved by acknowledgment, no code change needed)"
    - "Vitest discovers the KlasTabStrip test (BLOCKER 1 resolved by placing the file at `tests/KlasTabStrip.test.tsx` plus the include-pattern widening done in Plan 17-02)"
  artifacts:
    - path: "src/App.tsx"
      provides: "4th view state 'settings', prevView tracking, SettingsPage mount, onSettings/onBack/onNavigateToImport handlers"
      contains: "view === 'settings'"
    - path: "src/main.tsx"
      provides: "Startup theme hydration before ReactDOM.render, wrapped in try/catch (Research Open Question #2 resolved + Codex MEDIUM startup resilience resolved)"
      contains: "loadSettings"
    - path: "src/components/KlasTabStrip.tsx"
      provides: "Gear icon button with onSettings prop + isSettingsActive prop; '↑ Importeer' button removed"
      contains: "onSettings"
    - path: "tests/KlasTabStrip.test.tsx"
      provides: "Vitest coverage for gear icon presence + onSettings callback + active class"
      contains: "describe"
  key_links:
    - from: "src/main.tsx startup IIFE"
      to: "utils/settings.ts loadSettings + applyTheme"
      via: "await inside try/catch before ReactDOM.createRoot"
      pattern: "loadSettings\\(\\)"
    - from: "KlasTabStrip gear button onClick"
      to: "App.tsx handleOpenSettings"
      via: "onSettings prop"
      pattern: "onSettings"
    - from: "App.tsx handleBackFromSettings"
      to: "setView(prevView)"
      via: "prevView state"
      pattern: "setView\\(prevView\\)"
    - from: "App.tsx handleNavigateToImportFromSettings"
      to: "ImportPage (mounted by view === 'import' branch)"
      via: "setView('import') — activeKlasId in klassenState is unchanged, so ImportPage.handlePDFs skips autoDetect when students > 0 (SET-02 contract)"
      pattern: "setView\\(['\"]import['\"]\\)"
---

<objective>
Wire the SettingsPage into App.tsx as the 4th view state, add `prevView` tracking, add startup theme hydration in main.tsx (so dark mode applies before the first paint — Research Open Question #2) wrapped in a try/catch that mirrors the existing `loadKlassen()` pattern (Codex MEDIUM resolution), add the ⚙ gear icon to KlasTabStrip with its `onSettings` callback and `isSettingsActive` prop, remove the legacy '↑ Importeer' button from KlasTabStrip (Research Open Question #1 — import is now reached via Settings → Bestanden toevoegen per D-09), and add a KlasTabStrip test at `tests/KlasTabStrip.test.tsx` (NOT under `src/`) covering the gear icon.

Purpose: This plan converts Plans 01 (CSS) and 02 (component) into a working end-to-end flow. Three review-driven adjustments versus the prior draft:
- Test file moved from `src/components/KlasTabStrip.test.tsx` to `tests/KlasTabStrip.test.tsx` so the existing Vitest include pattern (widened to `.tsx` in Plan 17-02 Task 3) discovers it (BLOCKER 1 resolution).
- Startup theme hydration explicitly wrapped in `try/catch` with `console.error` logging, matching the existing `loadKlassen()` defensive pattern in `main.tsx`, so a plugin-store failure cannot block app boot (Codex MEDIUM "Startup resilience" resolution).
- SET-02 add-to-active-class behavior explicitly documented in the must_haves and acceptance_criteria below, leaning on Phase 16's auto-detect-skip logic (ImportPage.handlePDFs lines 78–112: when the active class already has students, autoDetectKlas is bypassed and addStudent appends to the existing class verbatim). Per the reviews-mode directive, no `activeKlasId` prop pass-through to ImportPage is added — the existing `klassenState.activeKlasId` singleton already provides the context Phase 16 reads. This resolves the Codex BLOCKER on SET-02 by acknowledgment + verification, not by code change.

Output: Modifications to 3 existing production files (src/App.tsx, src/main.tsx, src/components/KlasTabStrip.tsx) + 1 new test file at `tests/KlasTabStrip.test.tsx`. Depends on Plan 01 (CSS classes referenced) and Plan 02 (SettingsPage component imported, utils/settings.ts imported by main.tsx, vitest config widened to `.tsx`, matchMedia mock added).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-settings-panel-foundation/17-CONTEXT.md
@.planning/phases/17-settings-panel-foundation/17-RESEARCH.md
@.planning/phases/17-settings-panel-foundation/17-UI-SPEC.md
@.planning/phases/17-settings-panel-foundation/17-VALIDATION.md
@.planning/phases/17-settings-panel-foundation/17-REVIEWS.md

@src/App.tsx
@src/main.tsx
@src/components/KlasTabStrip.tsx
@src/components/ImportPage.tsx

<interfaces>
<!-- Contracts from Plan 02 -->

From utils/settings.ts (created in Plan 02):
```typescript
export type Theme = 'dark' | 'light';
export async function loadSettings(): Promise<{ theme: Theme } | null>;
export async function saveSettings(settings: { theme: Theme }): Promise<void>;
export function applyTheme(theme: Theme): void;
```

From src/components/SettingsPage.tsx (created in Plan 02):
```typescript
interface SettingsPageProps {
  onBack: () => void;
  onNavigateToImport: () => void;
}
export default function SettingsPage(props: SettingsPageProps): JSX.Element;
```

Current KlasTabStrip props (src/components/KlasTabStrip.tsx — to be extended):
```typescript
interface KlasTabStripProps {
  klassen: Array<{ id: string; naam: string }>;
  activeKlasId: string | null;
  onSwitch: (klasId: string) => void;
  onCreateKlas: () => void;
  onImport: () => void;                  // REMOVE — legacy import button
  // ADD:
  onSettings: () => void;
  isSettingsActive: boolean;
}
```

Current App.tsx view state (src/App.tsx line 10):
```typescript
const [view, setView] = useState<'import' | 'klas' | 'detail'>('import');
// Extend to:
const [view, setView] = useState<'import' | 'klas' | 'detail' | 'settings'>('import');
const [prevView, setPrevView] = useState<'import' | 'klas' | 'detail'>('klas');
```

ImportPage.handlePDFs SET-02 contract (Phase 16 behavior, src/components/ImportPage.tsx
lines 77–112) — read-only reference, NOT modified by this plan:
```typescript
async function handlePDFs(files: File[]) {
  const activeId = klassenState.activeKlasId;
  const hasStudents = activeId !== null && (klassenState.klassen[activeId]?.students?.length ?? 0) > 0;
  if (!hasStudents) {
    // ... auto-detect creates/reuses a klas ...
  }
  // ... then addStudent(parseResult) per PDF — appends to the active class regardless of
  // whether the class was just auto-detected or was already populated.
}
```
Translation for SET-02: when the mentor opens Settings from a klas view that has
students, `klassenState.activeKlasId` already points at that klas and
`klassenState.klassen[activeId].students` has at least one entry. Clicking
"Bestanden toevoegen" calls `handleNavigateToImportFromSettings` which only flips
`view` to `'import'` — it does NOT touch `klassenState`. ImportPage mounts, reads the
unchanged `klassenState`, sees `hasStudents === true`, skips autoDetectKlas, and
appends new students to the existing class. SET-02 satisfied without any prop
plumbing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gear icon + remove legacy import button in KlasTabStrip and create tests/KlasTabStrip.test.tsx</name>
  <files>src/components/KlasTabStrip.tsx, tests/KlasTabStrip.test.tsx</files>
  <read_first>
    - src/components/KlasTabStrip.tsx (current 44-line file — confirm props and JSX shape; note that Plan 17-01 Task 3 has already swapped the `+` button's inline color to `var(--accent)` and left the `↑ Importeer` button untouched, so the import-button removal here is the only remaining structural change)
    - vitest.config.ts (confirm Plan 17-02 widened include to `.tsx`)
    - tests/vitest-setup.js (confirm Plan 17-02 added matchMedia mock — not used by this test, but ensures the setup file is current)
    - .planning/phases/17-settings-panel-foundation/17-UI-SPEC.md (Component Inventory section 1 — Gear Icon)
    - .planning/phases/17-settings-panel-foundation/17-RESEARCH.md (Pattern 5 + Open Question #1 resolution)
    - .planning/phases/17-settings-panel-foundation/17-REVIEWS.md (BLOCKER 1 — test file location)
  </read_first>
  <action>
    Update `src/components/KlasTabStrip.tsx`:
    - Update `KlasTabStripProps` interface: REMOVE `onImport: () => void`; ADD `onSettings: () => void` and `isSettingsActive: boolean`.
    - Update the destructured function signature to drop `onImport` and add `onSettings`, `isSettingsActive`.
    - REMOVE the existing `<button className="nav-tab" style={{ marginLeft: 'auto', color: '#6b7280' }} title="Bestanden importeren" onClick={onImport}>↑ Importeer</button>` block entirely (Research Open Question #1 resolution: import is reached via Settings → Bestanden toevoegen per D-09).
    - ADD in its place a gear icon button: `<button className={` + "`nav-tab${isSettingsActive ? ' active' : ''}`" + `} style={{ marginLeft: 'auto', fontSize: '18px' }} title="Instellingen" aria-label="Instellingen openen" onClick={onSettings}>⚙</button>` — Unicode gear character U+2699 (per UI-SPEC section 1 + D-02). `marginLeft: 'auto'` pins the button to the far-right edge of `#main-nav`. The `.active` class engages when `isSettingsActive` is true so the gear visually reflects the open Settings view. Do NOT add a hardcoded color — the `.nav-tab` and `.nav-tab.active` rules in `src/index.css` already provide themed colors that respond to `body.dark`.

    Also create `tests/KlasTabStrip.test.tsx` (NOT `src/components/KlasTabStrip.test.tsx` — BLOCKER 1 resolution):
    - Import KlasTabStrip via relative path from `tests/`: `import KlasTabStrip from '../src/components/KlasTabStrip';`.
    - Render `<KlasTabStrip klassen={[]} activeKlasId={null} onSwitch={vi.fn()} onCreateKlas={vi.fn()} onSettings={onSettingsSpy} isSettingsActive={false} />`.
    - Test 1: button with aria-label "Instellingen openen" is present in the DOM.
    - Test 2: clicking that button calls `onSettings` exactly once.
    - Test 3: when re-rendered with `isSettingsActive={true}`, the gear button's className contains `active`.
    - Test 4: '↑ Importeer' text is NOT present in the rendered output (regression guard for the removal).
  </action>
  <verify>
    <automated>npx vitest run tests/KlasTabStrip.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/KlasTabStrip.tsx` no longer references `onImport` (neither in the interface nor in the JSX)
    - `src/components/KlasTabStrip.tsx` does NOT contain the literal string `↑ Importeer`
    - `src/components/KlasTabStrip.tsx` contains the `⚙` character and an element with `aria-label="Instellingen openen"`
    - `src/components/KlasTabStrip.tsx` interface declares `onSettings: () => void` and `isSettingsActive: boolean`
    - The gear `<button>` style includes `marginLeft: 'auto'`
    - The gear `<button>` does NOT include a hardcoded `color:` in its inline style (themed via `.nav-tab` CSS)
    - `tests/KlasTabStrip.test.tsx` exists (NOT `src/components/KlasTabStrip.test.tsx`)
    - `tests/KlasTabStrip.test.tsx` has 4 `it(...)` blocks covering: gear present, onSettings called, active class, legacy text absent
    - `npx vitest run tests/KlasTabStrip.test.tsx` exits 0 with all 4 tests passing
  </acceptance_criteria>
  <done>Gear icon replaces the legacy import button; KlasTabStrip test file lives in `tests/` (discoverable by current vitest config) and covers presence + callback + active state + regression.</done>
</task>

<task type="auto">
  <name>Task 2: Wire settings view + prevView + SettingsPage mount in App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current 102-line file — confirm view state shape, handler signatures, JSX conditional render pattern)
    - src/components/SettingsPage.tsx (Plan 02 — confirm export and prop shape)
    - src/components/KlasTabStrip.tsx (Task 1 just modified — confirm new props)
    - src/components/ImportPage.tsx (read lines 77–112 specifically — confirm the auto-detect-skip behavior that makes SET-02 work without a prop pass-through)
    - .planning/phases/17-settings-panel-foundation/17-UI-SPEC.md (App Routing Contract section)
    - .planning/phases/17-settings-panel-foundation/17-REVIEWS.md (SET-02 reasoning — no prop change needed because Phase 16 auto-detect-skip handles it)
  </read_first>
  <action>
    Modify `src/App.tsx`:
    - Add import: `import SettingsPage from './components/SettingsPage';`
    - Extend the view state type: change `useState<'import' | 'klas' | 'detail'>('import')` to `useState<'import' | 'klas' | 'detail' | 'settings'>('import')`.
    - Add a new state `const [prevView, setPrevView] = useState<'import' | 'klas' | 'detail'>('klas');` directly below the `view` declaration.
    - Add three handler functions before the `return`:
      - `function handleOpenSettings() { setPrevView(view as 'import' | 'klas' | 'detail'); setView('settings'); }` — only reachable when `view !== 'settings'` because the gear icon is rendered by KlasTabStrip, which exists in all three non-settings views; this prevents prevView from ever holding 'settings' (Pitfall 3).
      - `function handleBackFromSettings() { setView(prevView); }`
      - `function handleNavigateToImportFromSettings() { setView('import'); }` — SET-02 contract: this MUST only flip view state. Do NOT touch `klassenState`, `klassenState.activeKlasId`, or any other singleton. ImportPage's Phase 16 logic reads `klassenState.activeKlasId` on mount and, if `students.length > 0`, skips auto-detect and appends to the active klas verbatim. Any reset of `klassenState` here would defeat that and create a new klas instead.
    - Update the `<KlasTabStrip ...>` JSX: REMOVE the `onImport={handleImportOpen}` prop and ADD `onSettings={handleOpenSettings}` and `isSettingsActive={view === 'settings'}`.
    - **Remove `handleImportOpen` from App.tsx entirely.** Delete the `handleImportOpen` function declaration in its entirety. After Task 1 removed the legacy import button from KlasTabStrip and the JSX prop is removed here, no call site references this handler. Leaving it in place would produce a TS6133 "declared but never used" warning under `noUnusedLocals` and confuse future readers about how import is reached (the answer is: Settings → Bestanden toevoegen → `handleNavigateToImportFromSettings`).
    - The KlasTabStrip is still rendered unconditionally (above the view-specific conditionals), so the gear icon is visible in all views.
    - Add a 4th conditional render block AFTER the existing `view === 'detail'` block: `{view === 'settings' && (<SettingsPage onBack={handleBackFromSettings} onNavigateToImport={handleNavigateToImportFromSettings} />)}`.
    - Do NOT touch the `useEffect` that prevents browser drag navigation, `handleImportComplete`, `handleKlasSwitch`, `handleStudentSelect`, `handleBack`, or `handleKlasCreated`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('src/App.tsx','utf8');const checks=[/import\s+SettingsPage\s+from\s+['\"]\.\/components\/SettingsPage['\"]/.test(src),/useState<['\"]import['\"]\s*\|\s*['\"]klas['\"]\s*\|\s*['\"]detail['\"]\s*\|\s*['\"]settings['\"]>/.test(src),/setPrevView/.test(src)&&/prevView/.test(src),/handleOpenSettings/.test(src),/handleBackFromSettings/.test(src),/handleNavigateToImportFromSettings/.test(src),/onSettings=\{handleOpenSettings\}/.test(src),/isSettingsActive=\{view === ['\"]settings['\"]\}/.test(src),/view === ['\"]settings['\"]\s*&&/.test(src),/<SettingsPage/.test(src),!/onImport=\{handleImportOpen\}/.test(src),!/handleImportOpen/.test(src)];const failed=checks.map((c,i)=>c?null:i).filter(x=>x!==null);if(failed.length)throw new Error('Failed checks: '+failed.join(','));console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` imports `SettingsPage` from `'./components/SettingsPage'`
    - View state union literally contains all four values: `'import' | 'klas' | 'detail' | 'settings'`
    - A `prevView` state variable exists with initial value `'klas'`
    - Three new handlers exist: `handleOpenSettings`, `handleBackFromSettings`, `handleNavigateToImportFromSettings`
    - `handleNavigateToImportFromSettings` body contains ONLY `setView('import')` — does NOT touch `klassenState` or any other module-level singleton (SET-02 prerequisite)
    - `<KlasTabStrip>` receives `onSettings={handleOpenSettings}` and `isSettingsActive={view === 'settings'}`
    - `<KlasTabStrip>` no longer receives `onImport={handleImportOpen}` (regression guard)
    - **`handleImportOpen` is removed from `src/App.tsx` entirely — the identifier does not appear anywhere in the file**
    - JSX contains the conditional `{view === 'settings' && (<SettingsPage` rendering block
    - `npx vitest run` exits 0 (full suite green, including the new SettingsPage and KlasTabStrip tests)
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>App.tsx routes the 4th 'settings' view; gear-icon-to-Settings-to-prevView-back flow wired; `handleImportOpen` deleted; existing klassenState left untouched on navigate-to-import so Phase 16's auto-detect-skip can satisfy SET-02.</done>
</task>

<task type="auto">
  <name>Task 3: Add startup theme hydration in main.tsx wrapped in try/catch</name>
  <files>src/main.tsx</files>
  <read_first>
    - src/main.tsx (current 20-line file — confirm IIFE order: loadKlassen → createRoot; note that loadKlassen is already wrapped in try/catch with `console.error('[main.tsx] ...mislukt:', err)` — this task replicates that exact pattern for theme hydration)
    - utils/settings.ts (Plan 02 — confirm loadSettings + applyTheme signatures)
    - .planning/phases/17-settings-panel-foundation/17-RESEARCH.md (Open Question #2 — startup hydration)
    - .planning/phases/17-settings-panel-foundation/17-REVIEWS.md (Codex MEDIUM "Startup resilience" — explicit try/catch mirroring loadKlassen)
  </read_first>
  <action>
    Modify `src/main.tsx` to hydrate the persisted theme BEFORE the first React render (resolves Research Open Question #2 — without this, dark mode flashes light on every restart until the user opens Settings). Wrap the entire hydration block in `try/catch` so a plugin-store read failure does NOT block app boot (Codex MEDIUM resolution — the existing `loadKlassen()` call in the same IIFE already uses this defensive pattern; the theme hydration MUST match).

    - Add import: `import { loadSettings, applyTheme } from '../utils/settings';`
    - Inside the existing `async` IIFE, AFTER the `await loadKlassen()` try/catch block but BEFORE `ReactDOM.createRoot(...)`, add a new try/catch block with this exact shape:
      ```
      try {
        const saved = await loadSettings();
        if (saved?.theme) {
          applyTheme(saved.theme);
        } else {
          const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
          applyTheme(prefersDark ? 'dark' : 'light');
        }
      } catch (err) {
        console.error('[main.tsx] thema hydratie mislukt:', err);
        // Fall back to OS preference so the app still renders with a sensible theme
        // even if the plugin-store cannot be read. Wrap this fallback in its own
        // try/catch because matchMedia could in principle also fail under exotic
        // runtime conditions — but if it does, swallow silently and let the app
        // start in light mode.
        try {
          const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
          applyTheme(prefersDark ? 'dark' : 'light');
        } catch {
          // Light mode (the default) — no further action needed.
        }
      }
      ```
    - The catch-branch fallback to matchMedia mirrors the user-visible behavior of D-06 (when no preference is persisted, use OS) and is a strict superset of the prior plan: an unrecoverable store failure now still gives the user a themed UI rather than a stuck light/dark mismatch.
    - The OS-preference branch (both in the try and the catch fallback) applies but does NOT call saveSettings (D-06).
    - The `ReactDOM.createRoot(...).render(...)` call stays unchanged; just runs AFTER the new hydration block.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('src/main.tsx','utf8');const checks=[/import\s+\{[^}]*loadSettings[^}]*applyTheme[^}]*\}\s+from\s+['\"]\.\.\/utils\/settings['\"]/.test(src)||(/loadSettings/.test(src)&&/applyTheme/.test(src)&&/from\s+['\"]\.\.\/utils\/settings['\"]/.test(src)),/await\s+loadSettings\(\)/.test(src),/applyTheme\(/.test(src),/matchMedia\(['\"]\(prefers-color-scheme: dark\)['\"]\)/.test(src),src.indexOf('loadSettings')<src.indexOf('ReactDOM.createRoot'),!/saveSettings/.test(src),/try\s*\{[\s\S]*loadSettings[\s\S]*\}\s*catch/.test(src),/console\.error\(['\"]\[main\.tsx\]\s*thema/.test(src)];const failed=checks.map((c,i)=>c?null:i).filter(x=>x!==null);if(failed.length)throw new Error('Failed checks: '+failed.join(','));console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.tsx` imports both `loadSettings` and `applyTheme` from `'../utils/settings'`
    - `await loadSettings()` is called BEFORE `ReactDOM.createRoot(...)` (textual order in file)
    - The `await loadSettings()` call sits inside a `try { ... } catch (err) { ... }` block — verified by regex matching the try/catch wrapper around `loadSettings`
    - The catch branch contains a `console.error('[main.tsx] thema hydratie mislukt:'` log call mirroring the existing `loadKlassen()` defensive pattern
    - `applyTheme(...)` is called in BOTH the happy-path branches (saved theme exists AND OS-preference fallback) AND in the catch-branch's nested matchMedia fallback (so the app always paints with some theme, even if the store is unreadable)
    - `window.matchMedia('(prefers-color-scheme: dark)')` is called in the OS-fallback branch(es) only
    - `saveSettings` is NOT called from main.tsx (D-06 — OS preference is not persisted on startup either)
    - `npx vitest run` exits 0 (full suite green — 43 existing + 6 SettingsPage + 4 KlasTabStrip ≈ 53 tests)
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>Theme is applied to document.body BEFORE React renders, eliminating the light-to-dark flash on restart; a plugin-store failure cannot block app boot (defensive try/catch with OS-preference fallback in the catch branch); behavior mirrors the existing loadKlassen() defensive pattern.</done>
</task>

</tasks>

<verification>
- `npx vitest run` exits 0 with all tests passing (existing 43 + 6 SettingsPage + 4 KlasTabStrip ≈ 53)
- `npx tsc --noEmit` reports no errors
- Manual smoke check (NOT gated, captured in 17-VALIDATION.md manual list): toggle dark mode in Settings, close + reopen app, dark mode persists with no light flash
- Manual SET-02 smoke check (NOT gated): with an active klas that already has students, open Settings → Bestanden toevoegen → ImportPage → drop a new PDF. The student count of the existing klas should INCREASE by 1 (autoDetectKlas skipped because hasStudents was true). No new klas should appear in the KlasTabStrip.
- Manual startup-resilience smoke check (NOT gated): with the app's plugin-store file deliberately corrupted, the app should still render in either dark or light (per OS preference) instead of failing to boot — a console.error is logged but the UI is usable.
- Full settings flow available end-to-end: gear icon → SettingsPage → toggle → Bestanden toevoegen → ImportPage → klas
</verification>

<success_criteria>
1. SET-01: full toggle + persistence + startup hydration path works (dark mode survives restart with no flash); startup hydration is defensive (plugin-store failure does not block boot)
2. SET-02: Settings → Bestanden toevoegen → ImportPage → klas navigation works end-to-end; the active klas is augmented (NOT duplicated) when it already has students, by virtue of Phase 16's auto-detect-skip — no code change in ImportPage required
3. POL-01: body.dark applied at startup means all components paint with dark tokens from the first frame
4. Legacy '↑ Importeer' button removed from KlasTabStrip — single entry point via Settings
5. Gear icon shows .nav-tab.active state when view === 'settings'
6. prevView never holds 'settings' (Pitfall 3 — only set in handleOpenSettings which is unreachable from settings view)
7. KlasTabStrip test lives at `tests/KlasTabStrip.test.tsx` (discoverable by the include pattern widened in Plan 17-02)
</success_criteria>

<output>
After completion, create `.planning/phases/17-settings-panel-foundation/17-03-SUMMARY.md`
</output>
