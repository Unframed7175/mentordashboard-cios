---
phase: 18
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - utils/prognosis.ts
  - src/utils/status.ts
  - src/components/VerzuimSection.tsx
autonomous: true
requirements: [SET-03, SET-04, SET-05]
tags: [react, typescript, vitest, prognosis, status, verzuim]
must_haves:
  truths:
    - "berekenPrognose(student, traject, activeDeelgebiedenIds) filters all three norm checks to active deelgebieden only (D-05, D-06, D-07)"
    - "berekenPrognose(student) without third arg behaves identically to current production output (backward compatible)"
    - "berekenPrognose internally uses getLeerlijnenMappingSync — no async-without-await bug (Critical Discovery §1 fixed)"
    - "berekenStatus(student, traject, thresholds?) accepts optional thresholds; defaults to {geoorloofd:900, ongeoorloofd:600}"
    - "berekenStatus returns oranje/Verzuim when ongeoorloofd > ongeoorloofd-threshold OR when geoorloofd > geoorloofd-threshold (NEW per D-08)"
    - "berekenStatus(student, traject) WITHOUT thresholds arg internally calls getVerzuimDrempelsSync() so all existing tile call sites (KlasOverzicht, LeerlingTegel) see runtime thresholds automatically (SC-4 wiring)"
    - "VerzuimSection no longer uses literal 600 — reads runtime threshold via getVerzuimDrempelsSync"
    - "All existing 43+ tests still pass (no regression)"
  artifacts:
    - path: utils/prognosis.ts
      provides: "berekenPrognose with activeDeelgebiedenIds parameter + sync mapping fix"
      contains: "activeDeelgebiedenIds"
    - path: src/utils/status.ts
      provides: "berekenStatus with thresholds parameter + internal getVerzuimDrempelsSync() fallback; hardcoded VERZUIM_DREMPEL_MIN removed"
      contains: "getVerzuimDrempelsSync"
    - path: src/components/VerzuimSection.tsx
      provides: "Runtime threshold instead of hardcoded > 600"
      contains: "getVerzuimDrempelsSync"
  key_links:
    - from: utils/prognosis.ts
      to: utils/leerlijnen.ts
      via: "import { getLeerlijnenMappingSync } — replaces broken sync call to async getLeerlijnenMapping"
      pattern: "getLeerlijnenMappingSync"
    - from: src/utils/status.ts
      to: src/components/VerzuimSection.tsx
      via: "shared VerzuimDrempels shape (caller passes thresholds)"
      pattern: "thresholds\\?: \\{ geoorloofd"
    - from: src/utils/status.ts
      to: utils/verzuimDrempels.ts
      via: "getVerzuimDrempelsSync() - default thresholds fallback when caller omits the thresholds argument (SC-4)"
      pattern: "getVerzuimDrempelsSync"
    - from: src/components/VerzuimSection.tsx
      to: utils/verzuimDrempels.ts
      via: "getVerzuimDrempelsSync() — sync cache accessor for ongeoorloofd threshold check"
      pattern: "getVerzuimDrempelsSync"
---

<objective>
Wave 1 — Modify the two synchronous backend-logic functions (berekenPrognose, berekenStatus) and the one component with a hardcoded 600 threshold (VerzuimSection). This is the only plan in Wave 1 that touches existing production logic; it is intentionally scoped narrowly to prognosis + status + their direct consumer to keep the blast radius small. Wave 2 UI plans then call these new signatures.

Purpose: Make prognose calculations respect the runtime active-deelgebieden filter (D-05/06/07); make verzuim status respect runtime thresholds (D-08/09/10); fix the latent async-without-await bug in prognosis.ts line 59 (RESEARCH §1).
Output: Two utility files and one component file modified. All Plan 18-01 tests for status / prognosis turn green. No regressions in the existing 43-test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-settings-panel-advanced/18-CONTEXT.md
@.planning/phases/18-settings-panel-advanced/18-RESEARCH.md
@.planning/phases/18-settings-panel-advanced/18-PATTERNS.md
@utils/prognosis.ts
@src/utils/status.ts
@src/components/VerzuimSection.tsx
@utils/leerlijnen.ts
@utils/schema.ts

<interfaces>
<!-- This plan modifies three functions. Their new signatures and the EXACT files that read each are documented below. -->

utils/prognosis.ts (modified):
  // Imports change:
  //   REMOVE: import { getLeerlijnenMapping } from './leerlijnen'
  //   ADD:    import { getLeerlijnenMappingSync } from './leerlijnen'
  //
  // Line 59 change:
  //   FROM: const mapping = getLeerlijnenMapping();
  //   TO:   const mapping = getLeerlijnenMappingSync();
  //
  // Signature changes:
  function telLeerlijnen(scores: any, activeDeelgebiedenIds?: string[]): any
    // First line of body:
    //   const deelgebieden = (activeDeelgebiedenIds && activeDeelgebiedenIds.length)
    //     ? DEELGEBIEDEN.filter(dg => activeDeelgebiedenIds.includes(dg.id))
    //     : DEELGEBIEDEN;
    //   Replace existing `const deelgebieden = DEELGEBIEDEN;` line.
  export function berekenPrognose(
    student: any,
    traject?: string,
    activeDeelgebiedenIds?: string[]
  ): any
    // Pass activeDeelgebiedenIds into every internal telLeerlijnen call AND any other
    // place that iterates DEELGEBIEDEN for norm calculations (≥13 voldoende count, negatief count).
    // The active filter applies to ALL three norms per D-05, D-06, D-07.

src/utils/status.ts (modified):
  // REMOVE line 15: const VERZUIM_DREMPEL_MIN = 600;
  // ADD import: import { getVerzuimDrempelsSync } from '../../utils/verzuimDrempels';
  // Signature change:
  export function berekenStatus(
    student: any,
    traject?: string,
    thresholds?: { geoorloofd: number; ongeoorloofd: number }
  ): StatusResult
  // Inside body, FIRST resolve thresholds against the runtime cache when caller omits them (SC-4):
  //   const resolvedThresholds = thresholds ?? getVerzuimDrempelsSync();
  // Then replace the hardcoded `> 600` check with both checks per D-08:
  //   const ongeoorloofd = student.verzuim?.ongeoorloofd ?? 0;
  //   const geoorloofd   = student.verzuim?.geoorloofd   ?? 0;
  //   const ongeoorloofdrempel = resolvedThresholds.ongeoorloofd;
  //   const geoorloofdrempel   = resolvedThresholds.geoorloofd;
  //   if (ongeoorloofd > ongeoorloofdrempel || geoorloofd > geoorloofdrempel)
  //     return { kleur:'oranje', label:'Verzuim', prognose: p };
  // The order of checks in the existing chain (Onbekend → Risico → Let op → Verzuim → ...)
  // is PRESERVED. Only the Verzuim condition expands per D-08.
  // RATIONALE: by resolving thresholds inside berekenStatus, every call site
  // (KlasOverzicht, LeerlingTegel, DetailWeergave, etc.) automatically picks up
  // the runtime threshold WITHOUT any call-site changes. This is the SC-4 wiring.

src/components/VerzuimSection.tsx (modified):
  // Line 43: `const ongeoorloofdhoogVerzuim = (v.ongeoorloofd || 0) > 600;`
  // Change to read the runtime threshold via the sync accessor:
  //   import { getVerzuimDrempelsSync } from '../../utils/verzuimDrempels';
  //   const ongeoorloofdrempel = getVerzuimDrempelsSync().ongeoorloofd;
  //   const ongeoorloofdhoogVerzuim = (v.ongeoorloofd || 0) > ongeoorloofdrempel;
  // The visual styling logic that depends on ongeoorloofdhoogVerzuim is UNCHANGED.

NOTE on call sites:
  This plan does NOT update KlasOverzicht.tsx / LeerlingTegel.tsx / DoortstroomPrognoseSection.tsx
  callers to PASS thresholds or activeDeelgebiedenIds. They continue to call
  berekenStatus(student) and berekenPrognose(student, traject) with no extra args. Because
  berekenStatus internally falls back to getVerzuimDrempelsSync() when thresholds is undefined,
  all existing tile call sites automatically reflect runtime threshold edits (SC-4). For
  activeDeelgebiedenIds, Wave 3 (Plan 18-05) threads getActiveDGIds into the matrix/spider call sites.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modify utils/prognosis.ts — add activeDeelgebiedenIds + fix sync mapping call</name>
  <files>utils/prognosis.ts</files>
  <read_first>
    - utils/prognosis.ts (FULL — read lines 1-150 to understand telLeerlijnen, berekenPrognose, the 3 norm checks, and the line 59 broken sync call)
    - utils/leerlijnen.ts (to confirm getLeerlijnenMappingSync exists after Plan 18-02 lands — this plan depends on 18-02 being merged)
    - utils/schema.ts (DEELGEBIEDEN array shape — id, label, group)
    - tests/prognosis.test.ts (created by Plan 18-01 — the 4 it() blocks for activeDeelgebiedenIds filter)
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (section "utils/prognosis.ts (MODIFY)" lines 248-310)
    - .planning/phases/18-settings-panel-advanced/18-RESEARCH.md (Critical Discovery §1 + Pattern 3)
    - .planning/phases/18-settings-panel-advanced/18-CONTEXT.md (D-05, D-06, D-07 — all three norm checks filter to active pool)
  </read_first>
  <behavior>
    - berekenPrognose(student) — current callers unchanged — produces SAME result as before this plan
    - berekenPrognose(student, 'sbc') — same
    - berekenPrognose(student, undefined, ['va','mm']) — only counts deelgebieden with id 'va' or 'mm' in totaalVoldoende / totaalOnvoldoende
    - berekenPrognose(student, undefined, []) — treated as "no filter" (same as undefined; an empty array means no active pool would be insane, so we coerce empty → no filter for safety; document in code comment per D-05 spirit)
    - Internal mapping is read via getLeerlijnenMappingSync (no await, no Promise leak)
    - Negatief count, voldoende count, SBC leerlijn-per-group counts ALL use the filtered pool
  </behavior>
  <action>
    Change import at top of utils/prognosis.ts: remove `getLeerlijnenMapping` from the leerlijnen import; add `getLeerlijnenMappingSync` to the same import statement. Replace the line `const mapping = getLeerlijnenMapping();` (currently at line 59) with `const mapping = getLeerlijnenMappingSync();`.

    Update the telLeerlijnen function signature to accept an optional `activeDeelgebiedenIds?: string[]` second parameter. Inside telLeerlijnen, replace `const deelgebieden = DEELGEBIEDEN;` with `const deelgebieden = (activeDeelgebiedenIds && activeDeelgebiedenIds.length > 0) ? DEELGEBIEDEN.filter(dg => activeDeelgebiedenIds.includes(dg.id)) : DEELGEBIEDEN;`.

    Update the berekenPrognose function signature to add `activeDeelgebiedenIds?: string[]` as the third parameter. At the top of berekenPrognose body, compute `const activeDGs = (activeDeelgebiedenIds && activeDeelgebiedenIds.length > 0) ? DEELGEBIEDEN.filter(dg => activeDeelgebiedenIds.includes(dg.id)) : DEELGEBIEDEN;`. Use activeDGs in place of DEELGEBIEDEN for the ≥13 voldoende count, the negatief onvoldoende count, AND the SBC leerlijn-per-group threshold checks (per D-05, D-06, D-07). Pass activeDeelgebiedenIds through to telLeerlijnen calls inside berekenPrognose.

    Do NOT change the public output shape of berekenPrognose. Do NOT change any callers in this task. Empty-array third arg is treated as "no filter" (defensive coercion).
  </action>
  <verify>
    <automated>npm test -- --run tests/prognosis.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - utils/prognosis.ts no longer imports `getLeerlijnenMapping` (grep `from './leerlijnen'` — only getLeerlijnenMappingSync appears in the import list)
    - utils/prognosis.ts contains the literal string `getLeerlijnenMappingSync()` (no await, no Promise)
    - utils/prognosis.ts berekenPrognose signature contains `activeDeelgebiedenIds?: string[]` (grep)
    - utils/prognosis.ts telLeerlijnen signature contains `activeDeelgebiedenIds?: string[]`
    - utils/prognosis.ts body contains the filter expression `DEELGEBIEDEN.filter(dg => activeDeelgebiedenIds` (grep allowing whitespace)
    - `npm test -- --run tests/prognosis.test.ts` exits 0 — all 4 new activeDeelgebiedenIds tests pass
    - `npm test -- --run` exits 0 — no regression in any other test file
    - `npm run typecheck-migrated` exits 0
  </acceptance_criteria>
  <done>berekenPrognose accepts active filter; sync mapping bug fixed; prognosis tests green; no regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modify src/utils/status.ts + src/components/VerzuimSection.tsx — runtime thresholds</name>
  <files>src/utils/status.ts, src/components/VerzuimSection.tsx</files>
  <read_first>
    - src/utils/status.ts (FULL — to identify the VERZUIM_DREMPEL_MIN constant at line 15 and the existing chain of return-statements lines 121-132)
    - src/components/VerzuimSection.tsx (FULL — to find the hardcoded `> 600` at line 43 and confirm the styling-dependent variable name `ongeoorloofdhoogVerzuim`)
    - utils/verzuimDrempels.ts (created in Plan 18-02 — confirm getVerzuimDrempelsSync export exists and the relative import path from src/components/)
    - tests/status.test.ts (Plan 18-01 added the 5 thresholds-parameter tests)
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (section "src/utils/status.ts (MODIFY)" lines 312-354)
    - .planning/phases/18-settings-panel-advanced/18-RESEARCH.md (Pattern 2 + Critical Discovery §4, §5)
    - .planning/phases/18-settings-panel-advanced/18-CONTEXT.md (D-08, D-09, D-10)
  </read_first>
  <behavior>
    - berekenStatus(student, undefined, undefined) reads runtime thresholds via getVerzuimDrempelsSync() - when the cache holds default 900/600 the result is identical to current production behavior
    - berekenStatus(student, traject, { geoorloofd: 60, ongeoorloofd: 60 }) returns oranje/Verzuim when student.verzuim.ongeoorloofd > 60 (explicit thresholds override runtime cache)
    - berekenStatus(student, traject, { geoorloofd: 60, ongeoorloofd: 9999 }) returns oranje/Verzuim when student.verzuim.geoorloofd > 60 (NEW path per D-08)
    - SC-4 wiring: berekenStatus(student) WITHOUT thresholds arg returns oranje/Verzuim when the runtime cache contains a custom geoorloofd threshold (e.g., 120) that student.verzuim.geoorloofd exceeds - no call-site change required
    - VerzuimSection's bold-red styling activates when ongeoorloofd > the runtime threshold (not hardcoded 600); when threshold default is 600, behavior matches pre-change
  </behavior>
  <action>
    In src/utils/status.ts: delete the line `const VERZUIM_DREMPEL_MIN = 600;`. Add the import `import { getVerzuimDrempelsSync } from '../../utils/verzuimDrempels';` at the top of the file (verify the relative path matches existing util imports in src/utils/status.ts). Change berekenStatus signature to add a third parameter `thresholds?: { geoorloofd: number; ongeoorloofd: number }`. At the top of the body (immediately after the existing initial setup), FIRST resolve thresholds against the runtime cache: `const resolvedThresholds = thresholds ?? getVerzuimDrempelsSync();` - this is the SC-4 wiring: when callers (KlasOverzicht tiles, LeerlingTegel, etc.) omit the argument, berekenStatus pulls the runtime threshold from the verzuimDrempels sync cache instead of falling back to a hardcoded literal. Then compute `const ongeoorloofdrempel = resolvedThresholds.ongeoorloofd;` and `const geoorloofdrempel = resolvedThresholds.geoorloofd;`. Read both verzuim values from the student: `const ongeoorloofd = student.verzuim?.ongeoorloofd ?? 0;` and `const geoorloofd = student.verzuim?.geoorloofd ?? 0;`. Replace the existing Verzuim-detection if-statement (currently `if (ongeoorloofd > VERZUIM_DREMPEL_MIN)` or similar) with: `if (ongeoorloofd > ongeoorloofdrempel || geoorloofd > geoorloofdrempel) return { kleur: 'oranje', label: 'Verzuim', prognose: p };`. Keep the position of this check in the return-chain identical to its current position (between "Let op" and "sbc"). Do NOT change any other return paths.

    In src/components/VerzuimSection.tsx: add import at top `import { getVerzuimDrempelsSync } from '../../utils/verzuimDrempels';` (verify the relative path matches other utility imports in this file). Replace line 43 `const ongeoorloofdhoogVerzuim = (v.ongeoorloofd || 0) > 600;` with two lines: `const ongeoorloofdrempel = getVerzuimDrempelsSync().ongeoorloofd;` followed by `const ongeoorloofdhoogVerzuim = (v.ongeoorloofd || 0) > ongeoorloofdrempel;`. Do NOT change any rendering/styling logic — only the threshold source changes.
  </action>
  <verify>
    <automated>npm test -- --run tests/status.test.ts && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/status.ts no longer contains the literal `VERZUIM_DREMPEL_MIN` (grep returns 0 matches in the file)
    - src/utils/status.ts berekenStatus signature contains `thresholds?: { geoorloofd: number; ongeoorloofd: number }` (grep)
    - src/utils/status.ts imports `getVerzuimDrempelsSync` from utils/verzuimDrempels (grep returns the import line)
    - src/utils/status.ts body contains the literal `thresholds ?? getVerzuimDrempelsSync()` (grep - exactly one match, the resolvedThresholds line)
    - berekenStatus() returns oranje/Verzuim when a custom geoorloofd threshold in the runtime verzuimDrempels cache is exceeded EVEN WHEN no thresholds argument is passed (tests/status.test.ts MUST cover this case - extend Plan 18-01 test list if not already covered; assert by stubbing the verzuimDrempels module before importing status)
    - src/utils/status.ts body contains an if-statement of the form `if (ongeoorloofd > ongeoorloofdrempel || geoorloofd > geoorloofdrempel)` returning `kleur: 'oranje'` with `label: 'Verzuim'`
    - src/components/VerzuimSection.tsx imports getVerzuimDrempelsSync (grep `getVerzuimDrempelsSync` returns ≥2 matches: 1 import line + 1 usage line)
    - src/components/VerzuimSection.tsx no longer contains the literal `> 600` (grep -v '^//' src/components/VerzuimSection.tsx | grep -c '> 600' returns 0)
    - `npm test -- --run tests/status.test.ts` exits 0 — all 5 new thresholds tests pass
    - `npm test -- --run` exits 0 — no regression in any other test file (43+ existing + new from 18-01 all green for owned modules)
    - `npm run typecheck-migrated` exits 0
  </acceptance_criteria>
  <done>Hardcoded 600 removed from production code in both status.ts and VerzuimSection.tsx; runtime threshold path operational; tests green.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` exits 0 — full suite passes
- `npm run typecheck-migrated` exits 0
- `grep -rn 'VERZUIM_DREMPEL_MIN' src/ utils/` returns 0 matches (hardcoded constant fully eradicated)
- `grep -rn '> 600' src/utils/ src/components/VerzuimSection.tsx` returns 0 matches (no literal 600 thresholds remain in changed files)
- The latent async-without-await bug from prognosis.ts is gone: `grep -n 'getLeerlijnenMapping()' utils/prognosis.ts` returns 0 matches (only getLeerlijnenMappingSync() remains)
</verification>

<success_criteria>
- berekenPrognose accepts and respects activeDeelgebiedenIds for all three norm checks (D-05, D-06, D-07)
- berekenStatus accepts and respects optional thresholds for both geoorloofd and ongeoorloofd (D-08, D-09, D-10)
- VerzuimSection reads runtime threshold (no hardcoded 600)
- All Plan 18-01 tests for status + prognosis turn green
- No regressions in the existing 43-test suite
- Default behavior preserved when optional args are omitted — Wave 2 callers can adopt incrementally
</success_criteria>

<output>
After completion, create `.planning/phases/18-settings-panel-advanced/18-03-SUMMARY.md` documenting:
- Diff summary for each of the 3 files
- Confirmation grep outputs (the 4 grep gates from <verification>)
- Number of Plan 18-01 tests that flipped red→green (should be 5 status + 4 prognosis = 9)
- Confirmation that npm test -- --run reports the same total file count as before, with ≥9 more passing tests than before this plan
</output>
