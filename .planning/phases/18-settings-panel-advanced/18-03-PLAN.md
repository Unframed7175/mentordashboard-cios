---
phase: 18-settings-panel-advanced
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - utils/prognosis.ts
  - src/utils/status.ts
  - src/components/VerzuimSection.tsx
autonomous: true
requirements: [SET-03, SET-04, SET-05]
tags: [prognosis, status, verzuim, runtime-thresholds, sync-bug-fix]

must_haves:
  truths:
    - "berekenPrognose no longer calls getLeerlijnenMapping without await (sync-call bug fixed)"
    - "berekenPrognose accepts an optional activeDeelgebiedenIds parameter and filters all three norms (≥13 voldoende, SBC per-leerlijn, negatief) to that pool"
    - "berekenStatus accepts an optional thresholds parameter; when omitted it reads getVerzuimDrempelsSync() internally"
    - "berekenStatus returns oranje/Verzuim when geoorloofd exceeds the geoorloofd threshold (D-08 new check)"
    - "VerzuimSection no longer contains the hardcoded > 600 check; it reads the runtime ongeoorloofd threshold via getVerzuimDrempelsSync()"
    - "All Wave 0 RED tests for status.ts and prognosis.ts (8 new it() blocks) now pass"
    - "All pre-existing tests still pass — no regression"
  artifacts:
    - path: utils/prognosis.ts
      provides: "Sync-bug-fixed prognose with activeDeelgebiedenIds filtering"
      contains: "getLeerlijnenMappingSync"
    - path: src/utils/status.ts
      provides: "berekenStatus with optional thresholds parameter and dual geoorloofd/ongeoorloofd check"
      contains: "thresholds?: { geoorloofd: number; ongeoorloofd: number }"
    - path: src/components/VerzuimSection.tsx
      provides: "VerzuimSection using runtime ongeoorloofd threshold"
      contains: "getVerzuimDrempelsSync"
  key_links:
    - from: utils/prognosis.ts
      to: utils/leerlijnen.ts
      via: "import { getLeerlijnenMappingSync } from './leerlijnen'"
      pattern: "getLeerlijnenMappingSync"
    - from: src/utils/status.ts
      to: utils/verzuimDrempels.ts
      via: "import { getVerzuimDrempelsSync } from '../../utils/verzuimDrempels'"
      pattern: "getVerzuimDrempelsSync"
    - from: src/components/VerzuimSection.tsx
      to: utils/verzuimDrempels.ts
      via: "import { getVerzuimDrempelsSync } from '../../utils/verzuimDrempels'"
      pattern: "getVerzuimDrempelsSync"
---

<objective>
Wave 2 — Backend logic refactor. Fix the verified async/sync bug in `utils/prognosis.ts` line 59, add `activeDeelgebiedenIds` filtering to `berekenPrognose`, replace the hardcoded `VERZUIM_DREMPEL_MIN = 600` constant in `src/utils/status.ts` with runtime thresholds (now dual: geoorloofd + ongeoorloofd per D-08/D-10), and remove the hardcoded `> 600` check at `VerzuimSection.tsx` line 43.

Purpose: All downstream consumers (DeelgebiedenMatrix, SpiderChartCard, BpvProgressSection, KlasOverzicht tiles) call `berekenStatus(student)` and `berekenPrognose(student, traject)` without passing config. The internal sync-cache fallback pattern means runtime thresholds and active-deelgebied filtering propagate everywhere with **zero changes to existing call sites**. Plan 18-05 then wires the active-filter parameter where it is explicitly consumed (matrix / spider).
Output: Three modified files; the 8 new it() blocks added in 18-01 (4 in `tests/status.test.ts` + 4 in `tests/prognosis.test.ts`) turn GREEN; the full suite remains green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-settings-panel-advanced/18-CONTEXT.md
@.planning/phases/18-settings-panel-advanced/18-RESEARCH.md
@.planning/phases/18-settings-panel-advanced/18-PATTERNS.md
@.planning/phases/18-settings-panel-advanced/18-02-SUMMARY.md
@utils/prognosis.ts
@src/utils/status.ts
@src/components/VerzuimSection.tsx
@utils/leerlijnen.ts
@utils/verzuimDrempels.ts
@utils/schema.ts

<interfaces>
<!-- LOCKED CONTRACTS from 18-01 tests + 18-02 modules. Implement against these. -->

Existing exports this plan CONSUMES (from 18-02):
  utils/leerlijnen.ts:        function getLeerlijnenMappingSync(): Record<string, string>
  utils/verzuimDrempels.ts:   function getVerzuimDrempelsSync(): VerzuimDrempels
                              interface VerzuimDrempels { geoorloofd: number; ongeoorloofd: number }
                              const DEFAULT_VERZUIM_DREMPELS = { geoorloofd: 900, ongeoorloofd: 600 }

New / modified exports this plan PRODUCES:

  utils/prognosis.ts:
    export function berekenPrognose(
      student: any,
      traject?: string,
      activeDeelgebiedenIds?: string[]
    ): any
    // When activeDeelgebiedenIds provided, DEELGEBIEDEN is filtered to those ids
    // BEFORE all three norm computations (≥13 voldoende, SBC per-leerlijn, negatief).

  src/utils/status.ts:
    export function berekenStatus(
      student: any,
      traject?: string,
      thresholds?: { geoorloofd: number; ongeoorloofd: number }
    ): StatusResult
    // When thresholds omitted, reads getVerzuimDrempelsSync() internally.
    // Returns { kleur: 'oranje', label: 'Verzuim', prognose: p } when EITHER
    //   ongeoorloofd > thresholds.ongeoorloofd OR geoorloofd > thresholds.geoorloofd.

  src/components/VerzuimSection.tsx:
    No exported API change. The local boolean `ongeoorloofdhoogVerzuim` now reads
    its threshold from getVerzuimDrempelsSync().ongeoorloofd at render time
    instead of the literal 600.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix sync-call bug + add activeDeelgebiedenIds filter in utils/prognosis.ts (SET-03, SET-04)</name>
  <files>utils/prognosis.ts</files>
  <read_first>
    - utils/prognosis.ts (entire file — small, ~130 lines) — to identify the existing imports near lines 14-15, the telLeerlijnen function around line 55, the bad sync call at line 59, the berekenPrognose signature at line 110, and the three norm computations (≥13 voldoende, SBC per-leerlijn thresholds, negatief criteria)
    - utils/leerlijnen.ts — to confirm getLeerlijnenMappingSync was added in 18-02
    - utils/schema.ts — DEELGEBIEDEN shape (id, label, group) used by the filter
    - tests/prognosis.test.ts — the 4 RED tests in the "berekenPrognose activeDeelgebiedenIds filter (Phase 18)" describe block, which dictate exact behavior
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (section "utils/prognosis.ts (MODIFY)") — verbatim refactor pattern
    - .planning/phases/18-settings-panel-advanced/18-RESEARCH.md (Critical Code Discovery §1 + Pattern 3)
  </read_first>
  <behavior>
    - Test: berekenPrognose called with no third argument counts all 19 DEELGEBIEDEN (existing behavior preserved)
    - Test: berekenPrognose called with activeDeelgebiedenIds = [id1, id2, id3] only counts those 3 deelgebieden in totaalVoldoendeOfHoger
    - Test: berekenPrognose called with [] yields totaalVoldoendeOfHoger === 0 AND totaalOnvoldoende === 0
    - Test: berekenPrognose returns a defined label (not undefined) — proves getLeerlijnenMappingSync returns a real object (not a Promise)
    - Test: SBC per-leerlijn thresholds (≥4 lesgeven goed, ≥3 organiseren goed, ≥5 prof. handelen goed) apply only to active deelgebieden in each leerlijn pool (D-06)
    - Test: negatief norm (>6 onvoldoende globally, OR >2 onvoldoende within one leerlijn) counts only active deelgebieden (D-07)
  </behavior>
  <action>
    Edit utils/prognosis.ts with two coordinated changes:

    Change A — sync-bug fix (line 59 area):
    - Locate the import line `import { getLeerlijnenMapping } from './leerlijnen';` (around line 14-15) and change it to `import { getLeerlijnenMappingSync } from './leerlijnen';`. If `getLeerlijnenMapping` is also still needed elsewhere in this file, add it back to the same import — but search first; if no other call site exists in this file, replace the single import.
    - Locate the call site `const mapping = getLeerlijnenMapping();` (line 59) and change it to `const mapping = getLeerlijnenMappingSync();`. The downstream usage `mapping[dg.id] || dg.group` does not change.

    Change B — activeDeelgebiedenIds filtering (D-05, D-06, D-07):
    - Modify the signature of `berekenPrognose` from `export function berekenPrognose(student: any, traject?: string): any` to `export function berekenPrognose(student: any, traject?: string, activeDeelgebiedenIds?: string[]): any`.
    - Inside berekenPrognose, derive the active pool once near the top of the function body:
      Let activeDGs = activeDeelgebiedenIds ? DEELGEBIEDEN.filter(dg => activeDeelgebiedenIds.includes(dg.id)) : DEELGEBIEDEN;
    - Modify the signature of the internal helper `telLeerlijnen(scores)` (around line 55) to `telLeerlijnen(scores, activeDeelgebiedenIds?: string[])`. Inside it, replace the assignment `const deelgebieden = DEELGEBIEDEN;` with: `const deelgebieden = activeDeelgebiedenIds ? DEELGEBIEDEN.filter(dg => activeDeelgebiedenIds.includes(dg.id)) : DEELGEBIEDEN;`.
    - Update the call to `telLeerlijnen(student.deelgebiedScores)` inside berekenPrognose to forward the parameter: `telLeerlijnen(student.deelgebiedScores, activeDeelgebiedenIds)`.
    - The three norm computations must all use the filtered pool:
      1. ≥13 voldoende: count voldoende-or-higher scores ONLY over activeDGs (D-05)
      2. SBC per-leerlijn (≥4 lesgeven goed / ≥3 organiseren goed / ≥5 prof. handelen goed): the `dgs` filter inside telLeerlijnen already iterates over the filtered `deelgebieden` constant — confirm it does (D-06)
      3. negatief criteria (>6 onvoldoende total OR >2 onvoldoende per leerlijn): both counts run over activeDGs and the telLeerlijnen-filtered pool respectively (D-07)
    - Preserve the existing exact label semantics: 'sbc' | 'sbl' | 'negatief' | 'neutraal'. Preserve `totaalVoldoendeOfHoger` and `totaalOnvoldoende` field names in the returned object — tests assert on these exact keys.
    - Preserve backward compatibility: when activeDeelgebiedenIds is undefined, behavior MUST equal the pre-Phase-18 behavior bit-for-bit (existing tests still pass).
    - Do NOT touch the public signature of any other exported function.
  </action>
  <verify>
    <automated>npm test -- --run tests/prognosis.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - utils/prognosis.ts contains the substring `getLeerlijnenMappingSync` AND does NOT contain the literal `const mapping = getLeerlijnenMapping();` (the async sync-call bug at line 59 is removed)
    - utils/prognosis.ts contains the substring `activeDeelgebiedenIds?: string[]` (signature change applied)
    - utils/prognosis.ts contains the literal `activeDeelgebiedenIds.includes(dg.id)` (filter applied)
    - `npm test -- --run tests/prognosis.test.ts` exits 0 — all pre-existing prognosis tests pass AND the 4 new "activeDeelgebiedenIds filter (Phase 18)" tests pass
    - `npm run typecheck-migrated` exits 0 (no new TypeScript errors)
    - berekenPrognose called without the third argument yields the same return shape as before Phase 18 (verified via pre-existing tests staying green)
  </acceptance_criteria>
  <done>utils/prognosis.ts uses getLeerlijnenMappingSync, accepts activeDeelgebiedenIds, and filters all three norms; tests/prognosis.test.ts is fully GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace hardcoded VERZUIM_DREMPEL_MIN in src/utils/status.ts with runtime thresholds (SET-05, D-08, D-10)</name>
  <files>src/utils/status.ts</files>
  <read_first>
    - src/utils/status.ts (entire file) — to find the `const VERZUIM_DREMPEL_MIN = 600;` at line 15, the StatusResult type, the berekenStatus signature at line 115, and the existing status chain at lines 121-132 (order: !heeftScores → grijs/Onbekend, p.label='negatief' → rood/Risico, p.label='neutraal' → oranje/Let op, ongeoorloofd > 600 → oranje/Verzuim, then sbc/sbl/versneld_sbc/naar_bj2/fallback)
    - utils/verzuimDrempels.ts — getVerzuimDrempelsSync() signature and DEFAULT_VERZUIM_DREMPELS shape (geoorloofd: 900, ongeoorloofd: 600)
    - tests/status.test.ts — the 4 RED tests in the "berekenStatus thresholds (Phase 18)" describe block
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (section "src/utils/status.ts (MODIFY)") — verbatim refactor pattern
    - .planning/phases/18-settings-panel-advanced/18-RESEARCH.md (Pattern 2 and Critical Code Discovery §5)
  </read_first>
  <behavior>
    - Test: berekenStatus(student, undefined, { geoorloofd:1500, ongeoorloofd:600 }) with verzuim.ongeoorloofd=700 returns { kleur:'oranje', label:'Verzuim', ... } (ongeoorloofd > 600 triggers)
    - Test: berekenStatus with thresholds { geoorloofd:900, ongeoorloofd:600 } and verzuim.geoorloofd=1000, verzuim.ongeoorloofd=0 returns oranje/Verzuim (new D-08 geoorloofd check)
    - Test: berekenStatus(student) without third arg reads from a mocked getVerzuimDrempelsSync()={geoorloofd:100, ongeoorloofd:50}; student with verzuim.ongeoorloofd=51 returns oranje/Verzuim (internal sync fallback works)
    - Test: berekenStatus with both verzuim values under thresholds returns prognose-driven status (groen/Op koers for sbl prognose)
    - Pre-existing status tests must continue passing — the verzuim chain still triggers BEFORE the prognose-label branches, and default thresholds when none provided match previous behavior (ongeoorloofd > 600).
  </behavior>
  <action>
    Edit src/utils/status.ts with three coordinated changes:

    Change A — remove the hardcoded constant:
    - Delete the line `const VERZUIM_DREMPEL_MIN = 600;` (line 15). Run a project-wide grep first; if any file outside this plan imports `VERZUIM_DREMPEL_MIN`, halt and report. Expected: no other consumer (VerzuimSection uses a local literal, not this constant).

    Change B — add the import + signature change:
    - Add near the top of the file, after the existing imports: `import { getVerzuimDrempelsSync } from '../../utils/verzuimDrempels';`. (Path: src/utils/status.ts → ../../utils/verzuimDrempels.ts — verify relative depth before writing.)
    - Change the berekenStatus signature at line 115 from `export function berekenStatus(student: any, traject?: string): StatusResult` to `export function berekenStatus(student: any, traject?: string, thresholds?: { geoorloofd: number; ongeoorloofd: number }): StatusResult`.

    Change C — implement dual-threshold check using `resolvedThresholds`:
    - At the top of the function body (before the existing status chain), add: `const resolvedThresholds = thresholds ?? getVerzuimDrempelsSync();`
    - Replace the existing hardcoded ongeoorloofd check (the branch around lines 121-125 that previously read `ongeoorloofd > VERZUIM_DREMPEL_MIN`) with a single combined branch that fires when EITHER threshold is exceeded:
      `if ((student.verzuim?.ongeoorloofd ?? 0) > resolvedThresholds.ongeoorloofd || (student.verzuim?.geoorloofd ?? 0) > resolvedThresholds.geoorloofd) return { kleur: 'oranje', label: 'Verzuim', prognose: p };`
    - Preserve the order of the existing chain (no scores → grijs/Onbekend, negatief → rood/Risico, neutraal → oranje/Let op, THEN this verzuim check, THEN sbc/sbl/versneld_sbc/naar_bj2/fallback).
    - Do NOT modify any other branch's literal labels or kleur values.
    - Backward compatibility: when thresholds is omitted AND no caller has yet written to plugin-store, getVerzuimDrempelsSync() returns DEFAULT_VERZUIM_DREMPELS = { geoorloofd:900, ongeoorloofd:600 }, preserving pre-Phase-18 behavior on the ongeoorloofd branch and adding the new geoorloofd branch (which had no equivalent before but uses a sensible default).
  </action>
  <verify>
    <automated>npm test -- --run tests/status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/status.ts does NOT contain the literal `VERZUIM_DREMPEL_MIN` (constant removed)
    - src/utils/status.ts contains the literal `thresholds?: { geoorloofd: number; ongeoorloofd: number }` in the berekenStatus signature
    - src/utils/status.ts contains the literal `getVerzuimDrempelsSync` (import + call)
    - src/utils/status.ts contains the literal `resolvedThresholds` (or the equivalent `thresholds ?? getVerzuimDrempelsSync` source-of-truth substring)
    - The Verzuim branch condition references both `resolvedThresholds.ongeoorloofd` AND `resolvedThresholds.geoorloofd` joined by `||`
    - `npm test -- --run tests/status.test.ts` exits 0 — all pre-existing status tests pass AND the 4 new "berekenStatus thresholds (Phase 18)" tests pass
    - `npm run typecheck-migrated` exits 0
  </acceptance_criteria>
  <done>src/utils/status.ts uses runtime dual thresholds, internal sync fallback works, tests/status.test.ts is fully GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Replace hardcoded > 600 check in src/components/VerzuimSection.tsx (SET-05)</name>
  <files>src/components/VerzuimSection.tsx</files>
  <read_first>
    - src/components/VerzuimSection.tsx (entire file, ~150 lines) — to confirm the literal `const ongeoorloofdhoogVerzuim = (v.ongeoorloofd || 0) > 600;` at line 43, and identify how `ongeoorloofdhoogVerzuim` flows into the styling at line ~44 ({ color: '#991b1b', fontWeight: 700 } when true)
    - utils/verzuimDrempels.ts — getVerzuimDrempelsSync signature
    - .planning/phases/18-settings-panel-advanced/18-RESEARCH.md (Critical Code Discovery §4)
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (section "src/components/DetailWeergave.tsx (MODIFY)" — the note about VerzuimSection's hardcoded check at the end of that section)
  </read_first>
  <action>
    Edit src/components/VerzuimSection.tsx:

    - Add an import at the top of the file alongside the existing imports: `import { getVerzuimDrempelsSync } from '../../utils/verzuimDrempels';`. Confirm path depth: src/components → ../../utils/verzuimDrempels.
    - Replace the line `const ongeoorloofdhoogVerzuim = (v.ongeoorloofd || 0) > 600;` (around line 43) with:
      `const ongeoorloofdhoogVerzuim = (v.ongeoorloofd || 0) > getVerzuimDrempelsSync().ongeoorloofd;`
    - Do NOT change any other line. Specifically: preserve the section wrapper, the empty-state branch, the verzuim-bar (44px) layout, the pA/pG/pO percentages, the styling object that consumes ongeoorloofdhoogVerzuim (`{ color: '#991b1b', fontWeight: 700 }`), and any helper function (minNaarUren).
    - The pre-warm in main.tsx (added in 18-02) guarantees that getVerzuimDrempelsSync() returns the persisted value at render time. No useEffect or state needed.
  </action>
  <verify>
    <automated>npm test -- --run && npm run typecheck-migrated 2>&1 | grep -v '^#' | grep -iE "error|fail" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/VerzuimSection.tsx does NOT contain the literal `> 600` (the hardcoded check is removed)
    - src/components/VerzuimSection.tsx contains the literal `getVerzuimDrempelsSync().ongeoorloofd`
    - Structural elements preserved: default-export React component, React import, `detail-section-title` literal, `'Verzuim'` title literal, empty-state branch checking `!student.verzuim`
    - `npm test -- --run` exits 0 (full suite — Phase 18 tests + all pre-existing tests including SettingsPage / status / prognosis)
    - `npm run typecheck-migrated` exits 0
  </acceptance_criteria>
  <done>VerzuimSection.tsx reads the runtime ongeoorloofd threshold; full suite green; typecheck clean.</done>
</task>

</tasks>

<verification>
- `npm test -- --run tests/prognosis.test.ts tests/status.test.ts` exits 0 (the 4+4 new behavior tests turn GREEN)
- `npm test -- --run` exits 0 (full suite — Phase 18 tests + all pre-existing tests pass)
- `npm run typecheck-migrated` exits 0
- File ownership: only utils/prognosis.ts, src/utils/status.ts, src/components/VerzuimSection.tsx were touched (matches files_modified frontmatter)
- No call site of `berekenPrognose(student, traject)` or `berekenStatus(student)` in the codebase was broken (backward-compat preserved via optional parameters)
</verification>

<success_criteria>
- Sync-call bug in utils/prognosis.ts (line 59) is eliminated — getLeerlijnenMappingSync replaces the bad call
- berekenPrognose accepts activeDeelgebiedenIds and applies it to all three norms (D-05, D-06, D-07)
- berekenStatus accepts thresholds with internal getVerzuimDrempelsSync() fallback (D-10)
- berekenStatus returns oranje/Verzuim on the geoorloofd branch (D-08 — new behavior)
- VerzuimSection.tsx reads runtime ongeoorloofd threshold (Critical Code Discovery §4)
- All 8 new it() blocks from 18-01 turn GREEN
- Zero pre-existing test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/18-settings-panel-advanced/18-03-SUMMARY.md` documenting:
- Final signature of berekenPrognose and berekenStatus
- Confirmation that getLeerlijnenMappingSync is the only mapping accessor used in prognosis.ts
- Confirmation that VERZUIM_DREMPEL_MIN constant is gone
- Test counts: 8/8 new Phase 18 backend tests now green; total suite N/N
</output>
