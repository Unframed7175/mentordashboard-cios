---
phase: 18-settings-panel-advanced
plan: 04
type: execute
wave: 2
depends_on: ["18-02"]
files_modified:
  - src/components/SettingsPage.tsx
  - src/index.css
autonomous: true
requirements: [SET-03, SET-04]
tags: [settings-ui, deelgebieden, leerlijnen, table, css]

must_haves:
  truths:
    - "SettingsPage section 3 placeholder is replaced with a working 19-row deelgebieden table"
    - "Each row has an inline Naam input, a Leerlijn dropdown (Lesgeven/Organiseren/Prof. handelen), and an Actief toggle"
    - "Naam edits apply on blur AND on Enter; Escape reverts to last-saved value"
    - "Leerlijn change applies onChange (instant-apply, no save button)"
    - "Actief toggle applies onChange (instant-apply, no save button)"
    - "Herstel standaard button below the table opens an inline confirmation (no modal) per D-03"
    - "'Ja, herstel' resets deelgebieden + leerlijnen back to schema defaults; 'Niet herstellen' dismisses the confirmation"
    - "All copy is Dutch per UI-SPEC Copywriting Contract"
    - "index.css section 25 exists with the deelgebieden-specific CSS classes; section 24 is untouched"
    - "All pre-existing tests still pass"
  artifacts:
    - path: src/components/SettingsPage.tsx
      provides: "Section 3 functional UI for SET-03 + SET-04"
      contains: "dg-settings-table"
    - path: src/index.css
      provides: "CSS section 25 (Phase 18 deelgebieden classes)"
      contains: "25. SettingsPage Phase 18"
  key_links:
    - from: src/components/SettingsPage.tsx
      to: utils/deelgebieden.ts
      via: "import { getDeelgebiedenConfig, saveDeelgebiedenConfig, resetDeelgebiedenConfig, type DeelgebiedConfig } from '../../utils/deelgebieden'"
      pattern: "getDeelgebiedenConfig"
    - from: src/components/SettingsPage.tsx
      to: utils/leerlijnen.ts
      via: "import { getLeerlijnenMapping, saveLeerlijnenMapping, resetLeerlijnenMapping } from '../../utils/leerlijnen'"
      pattern: "saveLeerlijnenMapping"
    - from: src/index.css
      to: src/components/SettingsPage.tsx
      via: ".dg-settings-table-wrap / .dg-naam-input / .dg-leerlijn-select / .dg-toggle class references"
      pattern: "dg-settings-table-wrap"
---

<objective>
Wave 2 — UI implementation for the "Deelgebieden & Leerlijnen" section (section 3 of SettingsPage). Build a 19-row table with three interactive columns (Naam inline input, Leerlijn dropdown, Actief toggle) plus a "Herstel standaard" button with inline confirmation. Add the deelgebieden-specific CSS to a new section 25 of index.css.

Purpose: Delivers SET-03 + SET-04 visible to the user. Plan 18-05 will run in parallel and handle section 4 (thresholds + BPV) and the matrix/spider/BPV component wiring; this plan's CSS additions are scoped to deelgebieden-specific classes only so 18-05 can append the remaining classes (`.settings-threshold-group`, `.settings-number-input`, `.bpv-bar-*`) without conflict. Both plans modify `src/index.css`, but 18-04 creates the section-25 header and the dg-* classes; 18-05 appends additional classes inside the same section. Because both plans modify index.css, the planner has placed them in the same wave only because they touch DISJOINT class names inside ONE section — the executor MUST append (not overwrite) inside section 25.
Output: SettingsPage.tsx with section 3 populated; index.css with new section 25 (dg-* + .toggle override classes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-settings-panel-advanced/18-CONTEXT.md
@.planning/phases/18-settings-panel-advanced/18-RESEARCH.md
@.planning/phases/18-settings-panel-advanced/18-PATTERNS.md
@.planning/phases/18-settings-panel-advanced/18-UI-SPEC.md
@.planning/phases/18-settings-panel-advanced/18-02-SUMMARY.md
@src/components/SettingsPage.tsx
@utils/deelgebieden.ts
@utils/leerlijnen.ts
@utils/schema.ts
@src/index.css

<interfaces>
<!-- LOCKED CONTRACTS this plan consumes (from 18-02). -->

utils/deelgebieden.ts:
  interface DeelgebiedConfig { id: string; label: string; active: boolean }
  async function getDeelgebiedenConfig(): Promise<DeelgebiedConfig[]>
  async function saveDeelgebiedenConfig(config: DeelgebiedConfig[]): Promise<boolean>
  async function resetDeelgebiedenConfig(): Promise<void>
  function buildDefaultDeelgebiedenConfig(): DeelgebiedConfig[]

utils/leerlijnen.ts (existing):
  async function getLeerlijnenMapping(): Promise<Record<string, string>>
  async function saveLeerlijnenMapping(mapping: Record<string, string>): Promise<boolean>
  async function resetLeerlijnenMapping(): Promise<void>
  // Leerlijn keys (value of the mapping object): "lesgeven" | "organiseren" | "profHandelen"

utils/schema.ts:
  DEELGEBIEDEN: ReadonlyArray<{ id: string; label: string; group: string }>  // 19 items
</interfaces>

<copywriting>
<!-- LOCKED Dutch copy from 18-UI-SPEC.md Copywriting Contract. Use verbatim. -->
Section title:           "Deelgebieden & Leerlijnen"   (already present in placeholder)
Table column headers:    "Naam" | "Leerlijn" | "Actief"
Dropdown options:        "Lesgeven" | "Organiseren" | "Prof. handelen"
Reset button:            "Herstel standaard"
Confirmation prompt:     "Alles terugzetten naar standaard?"
Confirmation cancel:     "Niet herstellen"
Confirmation confirm:    "Ja, herstel"
Toggle aria-label:       `${row.label} actief`  (template literal)
</copywriting>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS section 25 deelgebieden classes to src/index.css (SET-03, SET-04)</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css — locate the END of section 24 (around line 1063 per RESEARCH.md). Section 24 is "SettingsPage" from Phase 17. The new section 25 header must be appended AFTER section 24's last rule.
    - .planning/phases/18-settings-panel-advanced/18-UI-SPEC.md (sections "Component Inventory § Deelgebieden table" and "CSS Classes to Create")
    - .planning/phases/18-settings-panel-advanced/18-RESEARCH.md (Code Examples § Section 25 CSS — full block verbatim)
    - existing CSS for `.aanvullend-veld select` (around line 563-578) — the `.dg-leerlijn-select` rules duplicate this visual spec; confirm the existing rules to mirror
    - existing CSS for `.toggle-switch`, `.toggle-track`, `.toggle-thumb` from Phase 17 — the `.dg-toggle` override resizes these
  </read_first>
  <action>
    Append a NEW section 25 to src/index.css after the end of section 24. The section header must follow the existing section-header comment pattern used in section 24:

    /* --------------------------------------------------------------------------
       25. SettingsPage Phase 18 — Deelgebieden & Drempelwaarden
       -------------------------------------------------------------------------- */

    Inside section 25, add ONLY the deelgebieden-scoped classes in this exact list (Plan 18-05 will append the threshold + BPV classes AFTER these in the same section):

    1. `.dg-settings-table-wrap` — max-height: 420px; overflow-y: auto; border: 1.5px solid var(--border-default); border-radius: var(--radius-md);
    2. `.dg-settings-table` — width: 100%; border-collapse: collapse;
    3. `.dg-settings-row` — border-bottom: 1px solid var(--border-light); font-size: 0.875rem;
    4. `.dg-settings-row:last-child { border-bottom: none; }`
    5. `.dg-settings-row:nth-child(even) td { background: var(--bg-surface-alt); }`
    6. `.dg-settings-row:nth-child(odd)  td { background: var(--bg-surface); }`
    7. `.dg-settings-row:hover td { background: var(--accent-light) !important; transition: background var(--transition-fast); }`
    8. `.dg-naam-input` — width: 100%; padding: 4px 8px; background: transparent; border: 1.5px solid transparent; border-radius: var(--radius-sm); font-size: 0.875rem; color: var(--text-primary); transition: border-color var(--transition-base), background var(--transition-base);
    9. `.dg-naam-input:focus` — border-color: var(--border-focus); background: var(--bg-surface); box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.12); outline: none;
    10. `.dg-leerlijn-select` — copy the EXACT visual spec from `.aanvullend-veld select` (padding 0.5rem 0.75rem; 1.5px border-default; radius-sm; bg-surface; text-primary; 0.875rem; outline none; cursor pointer; transition border-color + box-shadow; shadow-xs)
    11. `.dg-leerlijn-select:focus` — border-color: var(--border-focus); box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.12);
    12. `.dg-toggle .toggle-track` — width: 32px; height: 18px; border-radius: 9px;
    13. `.dg-toggle .toggle-thumb` — width: 12px; height: 12px;
    14. `.dg-toggle .toggle-track:not(.on) .toggle-thumb` — transform: translateX(2px);
    15. `.dg-toggle .toggle-track.on .toggle-thumb` — transform: translateX(16px);
    16. Header `<th>` styling (apply via `.dg-settings-table thead th`): font-size 11px (0.6875rem); font-weight: 700; text-transform: uppercase; letter-spacing: 0.07em; color: var(--text-faint); background: var(--bg-surface-alt); padding: 8px; border-bottom: 1.5px solid var(--border-default); position: sticky; top: 0; z-index: 1;

    Do NOT touch section 24. Do NOT add the `.settings-threshold-group`, `.settings-threshold-row`, `.settings-number-input`, `.bpv-progress-wrap`, `.bpv-bar-track`, or `.bpv-bar-fill` classes — those are Plan 18-05's responsibility. Leave a clean break at the bottom of the dg-* classes so 18-05 can append cleanly.

    Verbatim CSS values are sourced from RESEARCH.md "Code Examples § Section 25 CSS" lines 541-646; copy ONLY the dg-* portion (the first ~50 lines of that block).
  </action>
  <verify>
    <automated>grep -c "dg-settings-table-wrap\|dg-naam-input\|dg-leerlijn-select\|dg-toggle" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains the literal substring `25. SettingsPage Phase 18`
    - src/index.css contains all of: `.dg-settings-table-wrap`, `.dg-settings-table`, `.dg-settings-row`, `.dg-naam-input`, `.dg-leerlijn-select`, `.dg-toggle .toggle-track`, `.dg-toggle .toggle-thumb`
    - src/index.css does NOT yet contain `.settings-threshold-group` or `.bpv-bar-track` (those belong to Plan 18-05)
    - The section-24 header comment and all its rules are unchanged (grep finds the same section-24 marker present before this task)
    - `grep -c "dg-settings-table-wrap\|dg-naam-input\|dg-leerlijn-select\|dg-toggle" src/index.css` returns >= 5 (each class definition appears at least once)
  </acceptance_criteria>
  <done>CSS section 25 exists with the 16 deelgebieden-scoped rules; sections 1-24 untouched; threshold/BPV classes intentionally deferred to 18-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement section 3 deelgebieden table in src/components/SettingsPage.tsx (SET-03, SET-04, D-01..D-04)</name>
  <files>src/components/SettingsPage.tsx</files>
  <read_first>
    - src/components/SettingsPage.tsx (entire file) — to identify the section 3 placeholder at lines 93-97 (the `<section className="detail-section">` wrapper with `<h2 className="detail-section-title">Deelgebieden &amp; Leerlijnen</h2>` and the `<p className="settings-placeholder-text">Komt in een volgende versie.</p>` paragraph), the existing useEffect at lines 24-44 (loadSettings pattern), and the existing handleToggle at lines 47-52 (instant-apply pattern)
    - utils/deelgebieden.ts — DeelgebiedConfig shape, getDeelgebiedenConfig, saveDeelgebiedenConfig, resetDeelgebiedenConfig, buildDefaultDeelgebiedenConfig signatures
    - utils/leerlijnen.ts — getLeerlijnenMapping, saveLeerlijnenMapping, resetLeerlijnenMapping signatures; confirm the value keys are `"lesgeven"`, `"organiseren"`, `"profHandelen"`
    - utils/schema.ts — DEELGEBIEDEN order (the 19 ids — table rows render in this order to keep stable visual sequence even when user renames)
    - .planning/phases/18-settings-panel-advanced/18-UI-SPEC.md (sections "Component Inventory § Deelgebieden table" + "Interaction States" + "Copywriting Contract")
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (section "src/components/SettingsPage.tsx (MODIFY)") — verbatim handler/JSX patterns
    - .planning/phases/18-settings-panel-advanced/18-RESEARCH.md (Pattern 4 — inline confirmation)
    - tests/SettingsPage.test.tsx — to verify the existing tests still pass after the change (no regression expected; this plan only ADDS content to section 3)
  </read_first>
  <behavior>
    - Initial mount: Promise.all([getDeelgebiedenConfig(), getLeerlijnenMapping()]) populates local state; UI renders 19 rows in DEELGEBIEDEN order
    - Editing the Naam input and pressing Enter (or blurring with a changed value) calls saveDeelgebiedenConfig with the mutated config; UI shows the new name immediately
    - Pressing Escape in the Naam input reverts to the last-saved label and blurs the input; no save fires
    - Empty/whitespace name on blur reverts to the last-saved label (no save)
    - Changing the Leerlijn dropdown calls saveLeerlijnenMapping with the new mapping; UI reflects the change immediately
    - Toggling Actief calls saveDeelgebiedenConfig with the row's `active` flag flipped; UI reflects immediately
    - Clicking "Herstel standaard" replaces the button with the inline confirmation row (Niet herstellen / Ja, herstel)
    - Clicking "Niet herstellen" restores the button without modifying any data
    - Clicking "Ja, herstel" calls resetDeelgebiedenConfig() and resetLeerlijnenMapping() (both), then re-fetches config + mapping into state, then restores the button
    - The dashboard (DeelgebiedenMatrix / SpiderChartCard) re-renders with the new active filter when the user toggles Actief or changes Leerlijn — Plan 18-05 wires the consumers; this plan's responsibility is only that the store is written correctly
  </behavior>
  <action>
    Edit src/components/SettingsPage.tsx:

    Imports (add to the existing import block at the top):
    - `import { getDeelgebiedenConfig, saveDeelgebiedenConfig, resetDeelgebiedenConfig, type DeelgebiedConfig } from '../../utils/deelgebieden';`
    - `import { getLeerlijnenMapping, saveLeerlijnenMapping, resetLeerlijnenMapping } from '../../utils/leerlijnen';`
    - Leave existing imports (useEffect, useState, loadSettings, saveSettings, applyTheme, Theme) untouched.

    State:
    - Add `const [dgConfig, setDgConfig] = useState<DeelgebiedConfig[]>([]);`
    - Add `const [leerlijnenMapping, setLeerlijnenMapping] = useState<Record<string, string>>({});`
    - Add `const [confirmingReset, setConfirmingReset] = useState(false);`

    Mount effect:
    - Add a NEW useEffect (separate from the existing dark-mode effect; do not merge) that runs once on mount and does:
      Promise.all([getDeelgebiedenConfig(), getLeerlijnenMapping()]).then(([cfg, mapping]) => { setDgConfig(cfg); setLeerlijnenMapping(mapping); }).catch(err => console.warn('[SettingsPage] section 3 load failed:', err));

    Handlers (define inside the component, alongside the existing handleToggle):
    - `handleNaamApply(id: string, newLabel: string)`: trim newLabel; if empty after trim, return without saving (UI reverts via the local lastValue ref); otherwise build `next = dgConfig.map(c => c.id === id ? { ...c, label: trimmed } : c)`; call `setDgConfig(next)` then `await saveDeelgebiedenConfig(next)`.
    - `handleLeerlijnChange(id: string, leerlijn: string)`: build `nextMapping = { ...leerlijnenMapping, [id]: leerlijn }`; call `setLeerlijnenMapping(nextMapping)` then `await saveLeerlijnenMapping(nextMapping)`.
    - `handleActiveToggle(id: string, active: boolean)`: build `next = dgConfig.map(c => c.id === id ? { ...c, active } : c)`; `setDgConfig(next)` then `await saveDeelgebiedenConfig(next)`.
    - `handleReset()`: await Promise.all([resetDeelgebiedenConfig(), resetLeerlijnenMapping()]); then re-fetch via `const [cfg, mapping] = await Promise.all([getDeelgebiedenConfig(), getLeerlijnenMapping()])`; setDgConfig(cfg); setLeerlijnenMapping(mapping); setConfirmingReset(false). (Note: 18-02 modules null the cache after delete, so the re-fetch will produce schema defaults.)

    JSX — replace the placeholder `<p className="settings-placeholder-text">Komt in een volgende versie.</p>` inside section 3 with the table + reset button. The `<section className="detail-section">` and the `<h2 className="detail-section-title">Deelgebieden &amp; Leerlijnen</h2>` wrappers stay unchanged.

    Inline table structure:
    - `<div className="dg-settings-table-wrap">`
    -   `<table className="dg-settings-table">`
    -     `<thead><tr><th>Naam</th><th style={{ width: 160 }}>Leerlijn</th><th style={{ width: 68 }}>Actief</th></tr></thead>`
    -     `<tbody>` mapping over dgConfig — each row uses key `row.id`, className `dg-settings-row`:
    -       Column 1 (`<td>`): a `<NaamInput>` inline component (or inline JSX with useState+useRef) that:
              - tracks a local `value` state initialized to row.label
              - on blur: if value !== row.label and value.trim() !== '', call `handleNaamApply(row.id, value.trim())`; else reset local value to row.label
              - on keydown Enter: blur the input (which triggers the same logic)
              - on keydown Escape: setValue(row.label) then blur
              - uses className `dg-naam-input`
              - re-syncs its local value when row.label changes externally (use useEffect with [row.label] dep)
    -       Column 2 (`<td>`): `<select className="dg-leerlijn-select" value={leerlijnenMapping[row.id] ?? schemaDefaultFor(row.id)} onChange={e => handleLeerlijnChange(row.id, e.target.value)}>` with three options: `<option value="lesgeven">Lesgeven</option>`, `<option value="organiseren">Organiseren</option>`, `<option value="profHandelen">Prof. handelen</option>`. The fallback `schemaDefaultFor(row.id)` reads DEELGEBIEDEN.find(d => d.id === row.id)?.group ?? 'lesgeven'.
    -       Column 3 (`<td>`): the same `<label className="toggle-switch dg-toggle">` / `<input type="checkbox" className="sr-only">` / `<span className="toggle-track">` structure from the existing dark-mode toggle (lines 66-79 pattern), with `checked={row.active}`, `onChange={e => handleActiveToggle(row.id, e.target.checked)}`, `aria-label={`${row.label} actief`}`. The span uses the conditional class `${row.active ? 'on' : ''}` joined with `toggle-track`.

    Below the table, render the Herstel-standaard button + inline confirmation:
    - When `!confirmingReset`: `<button className="btn btn-ghost" style={{ marginTop: 8 }} onClick={() => setConfirmingReset(true)}>Herstel standaard</button>`
    - When `confirmingReset === true`: a `<div style={{ display: 'flex', alignItems: 'center', gap: '8px', flexWrap: 'wrap', marginTop: '8px' }}>` containing:
        - `<span style={{ fontSize: '0.875rem', color: 'var(--text-secondary)' }}>Alles terugzetten naar standaard?</span>`
        - `<button className="btn btn-ghost" onClick={() => setConfirmingReset(false)}>Niet herstellen</button>`
        - `<button className="btn btn-primary" onClick={handleReset}>Ja, herstel</button>`

    Critical preservations:
    - Do NOT change section 1 (Weergave/dark mode), section 2 (Bestanden), or section 4 (Drempelwaarden — that placeholder stays for Plan 18-05).
    - Do NOT change the existing dark-mode handler or the existing useEffect.
    - All Dutch copy verbatim from the Copywriting contract above.
    - No new external dependencies.
  </action>
  <verify>
    <automated>npm test -- --run && npm run typecheck-migrated 2>&1 | grep -v '^#' | grep -iE "error|fail" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SettingsPage.tsx contains the literal `dg-settings-table-wrap` and `dg-settings-table` and `dg-naam-input` and `dg-leerlijn-select` and `dg-toggle`
    - src/components/SettingsPage.tsx contains the literal copy strings: `Naam`, `Leerlijn`, `Actief`, `Lesgeven`, `Organiseren`, `Prof. handelen`, `Herstel standaard`, `Alles terugzetten naar standaard?`, `Niet herstellen`, `Ja, herstel`
    - src/components/SettingsPage.tsx contains the literal `getDeelgebiedenConfig` AND `saveDeelgebiedenConfig` AND `resetDeelgebiedenConfig` AND `getLeerlijnenMapping` AND `saveLeerlijnenMapping` AND `resetLeerlijnenMapping`
    - src/components/SettingsPage.tsx contains the literal `confirmingReset` (state variable for inline confirmation)
    - src/components/SettingsPage.tsx does NOT contain the literal `Komt in een volgende versie` inside the section 3 wrapper (placeholder removed); the section 4 placeholder MAY still contain that string (Plan 18-05 handles section 4)
    - The `<h2>Deelgebieden &amp; Leerlijnen</h2>` heading is still present (section wrapper preserved)
    - `<option value="lesgeven">`, `<option value="organiseren">`, `<option value="profHandelen">` all appear (three options)
    - `<input type="checkbox" className="sr-only"` appears (toggle input pattern reused)
    - `aria-label={` appears at least once inside section 3 (template-literal aria-label for the toggle)
    - `npm test -- --run` exits 0 (full suite green — Phase 18 tests + pre-existing SettingsPage + status + prognosis tests)
    - `npm run typecheck-migrated` exits 0
  </acceptance_criteria>
  <done>SettingsPage section 3 renders the 19-row table with inline naam, leerlijn dropdown, and active toggle; Herstel standaard works with inline confirmation; full suite green.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` exits 0 (full suite — existing SettingsPage tests + Phase 18 tests all green)
- `npm run typecheck-migrated` exits 0
- File ownership: only src/components/SettingsPage.tsx and src/index.css touched
- Manual smoke (deferred to Plan 18-05's checker pass): open Settings → section 3 shows 19 rows; rename a deelgebied, switch its leerlijn, toggle Actief — all changes persist across reload; "Herstel standaard" with "Ja, herstel" returns all rows to schema defaults
- index.css section 24 unchanged (grep equivalence vs. pre-task state)
- index.css section 25 contains only dg-* / toggle-override classes; threshold/BPV classes intentionally absent (Plan 18-05 appends them)
</verification>

<success_criteria>
- SET-03 (rename + deactivate deelgebieden) and SET-04 (leerlijn reassignment) are both UI-complete in section 3
- D-01 (combined table), D-02 (inline input on blur/Enter, Escape revert), D-03 (Herstel standaard with inline confirmation), D-04 (scores preserved — implicit because toggle only flips `active`, no score deletion) are all satisfied
- Instant-apply pattern from Phase 17 dark-mode toggle is reused (Pitfall 5: state updates before async save)
- CSS section 25 dg-* classes exist; section 24 is intact; threshold/BPV CSS deferred to 18-05
- All Dutch copy verbatim from UI-SPEC Copywriting Contract
- Zero pre-existing test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/18-settings-panel-advanced/18-04-SUMMARY.md` documenting:
- Final structure of section 3 (table + reset button)
- The four handlers (handleNaamApply, handleLeerlijnChange, handleActiveToggle, handleReset) with their store-call pairs
- Confirmation that CSS section 25 contains dg-* classes only (threshold/BPV classes intentionally deferred to 18-05)
- Note for 18-05: "Append threshold and BPV CSS classes BELOW the existing dg-* rules in section 25; do NOT add a new section header."
</output>
