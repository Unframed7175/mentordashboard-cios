---
phase: 18-settings-panel-advanced
plan: 05
type: execute
wave: 3
depends_on: ["18-02", "18-03", "18-04"]
files_modified:
  - src/components/SettingsPage.tsx
  - src/components/DeelgebiedenMatrix.tsx
  - src/components/SpiderChartCard.tsx
  - src/components/DetailWeergave.tsx
  - src/components/BpvProgressSection.tsx
  - src/index.css
autonomous: true
requirements: [SET-03, SET-04, SET-05, SET-06]
tags: [settings-ui, thresholds, bpv, matrix-filter, spider-filter, detail-weergave, css]

must_haves:
  truths:
    - "SettingsPage section 4 contains two threshold number inputs (Geoorloofd / Ongeoorloofd) in hours with unit 'uur', stored as hours×60 minutes"
    - "SettingsPage section 4 contains the Verwachte BPV-uren input plus a 'BPV-uren importeren' button"
    - "DeelgebiedenMatrix filters its rendered deelgebieden by getDeelgebiedenConfigSync().active; score lookups still use the ORIGINAL schema dg.label (Pitfall 3)"
    - "SpiderChartCard filters its axes by getDeelgebiedenConfigSync().active; axis.key uses the ORIGINAL schema dg.label for score lookup; axis.label uses the custom config label for display"
    - "DetailWeergave renders BpvProgressSection between VerzuimSection and VakkenSection"
    - "BpvProgressSection shows an empty state when no BPV data is imported; shows a progress bar + stats row when data exists; bar fill turns green (#22C55E) when ≥100%"
    - "BPV Excel import button calls parseBpvExcel — the parser is the D-13 STUB returning {} (placeholder for the real parser when user supplies a sample file)"
    - "CSS section 25 is appended with the remaining classes (.settings-threshold-group, .settings-threshold-row, .settings-number-input, .bpv-progress-wrap, .bpv-bar-track, .bpv-bar-fill); the dg-* classes from 18-04 are untouched"
    - "All pre-existing tests still pass and the full Phase 18 test suite is green"
  artifacts:
    - path: src/components/SettingsPage.tsx
      provides: "Section 4 with threshold inputs + BPV expected hours + BPV import button"
      contains: "settings-number-input"
    - path: src/components/DeelgebiedenMatrix.tsx
      provides: "Active-deelgebied filter + runtime leerlijn-mapping group assignment"
      contains: "getDeelgebiedenConfigSync"
    - path: src/components/SpiderChartCard.tsx
      provides: "Filtered axes with custom-label display + schema-label score key (Pitfall 3 invariant)"
      contains: "key: dg.label"
    - path: src/components/DetailWeergave.tsx
      provides: "BpvProgressSection mounted between VerzuimSection and VakkenSection (D-12)"
      contains: "BpvProgressSection"
    - path: src/components/BpvProgressSection.tsx
      provides: "Per-student BPV progress section — empty state, progress bar, stats row"
      contains: "bpv-bar-fill"
    - path: src/index.css
      provides: "CSS section 25 completion (threshold + BPV classes appended below the dg-* rules)"
      contains: "bpv-bar-fill"
  key_links:
    - from: src/components/DeelgebiedenMatrix.tsx
      to: utils/deelgebieden.ts
      via: "getDeelgebiedenConfigSync() + getLeerlijnenMappingSync() for filter + grouping"
      pattern: "getDeelgebiedenConfigSync"
    - from: src/components/SpiderChartCard.tsx
      to: utils/deelgebieden.ts
      via: "getDeelgebiedenConfigSync() filter + custom-label lookup"
      pattern: "getDeelgebiedenConfigSync"
    - from: src/components/DetailWeergave.tsx
      to: src/components/BpvProgressSection.tsx
      via: "<BpvProgressSection leerlingId={leerlingId} /> rendered after <VerzuimSection>"
      pattern: "BpvProgressSection"
    - from: src/components/BpvProgressSection.tsx
      to: utils/bpv.ts
      via: "getBpvConfig() + getBpvData() + berekenBpvPct()"
      pattern: "berekenBpvPct"
    - from: src/components/SettingsPage.tsx
      to: utils/bpv.ts
      via: "getBpvConfig / saveBpvConfig / parseBpvExcel (D-13 stub)"
      pattern: "saveBpvConfig"
    - from: src/components/SettingsPage.tsx
      to: utils/verzuimDrempels.ts
      via: "loadVerzuimDrempels / saveVerzuimDrempels"
      pattern: "saveVerzuimDrempels"
---

<objective>
Wave 3 — Complete the Phase 18 UI: section 4 of SettingsPage (verzuim thresholds + BPV configuration + BPV import button), the active-deelgebied filtering in DeelgebiedenMatrix and SpiderChartCard, the new BpvProgressSection mounted in DetailWeergave, and the remaining CSS classes in section 25 of index.css.

Purpose: Closes the loop. After this plan the user can: (a) set both verzuim thresholds in hours and see VerzuimSection / berekenStatus respect them (D-08/D-09 — wired via 18-03's internal sync fallback), (b) set the expected BPV uren and click the import button (the parser itself is the documented D-13 stub returning {}), (c) see the matrix and spiderweb omit inactive deelgebieden, (d) see per-student BPV progress in DetailWeergave.
Output: Five modified components, one new component (BpvProgressSection), and the CSS completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-settings-panel-advanced/18-CONTEXT.md
@.planning/phases/18-settings-panel-advanced/18-RESEARCH.md
@.planning/phases/18-settings-panel-advanced/18-PATTERNS.md
@.planning/phases/18-settings-panel-advanced/18-UI-SPEC.md
@.planning/phases/18-settings-panel-advanced/18-02-SUMMARY.md
@.planning/phases/18-settings-panel-advanced/18-03-SUMMARY.md
@.planning/phases/18-settings-panel-advanced/18-04-SUMMARY.md
@src/components/SettingsPage.tsx
@src/components/DeelgebiedenMatrix.tsx
@src/components/SpiderChartCard.tsx
@src/components/DetailWeergave.tsx
@src/components/VerzuimSection.tsx
@utils/deelgebieden.ts
@utils/verzuimDrempels.ts
@utils/bpv.ts
@utils/leerlijnen.ts
@utils/schema.ts
@src/index.css

<interfaces>
<!-- LOCKED CONTRACTS this plan consumes (from 18-02 and 18-03). -->

utils/deelgebieden.ts:
  function getDeelgebiedenConfigSync(): DeelgebiedConfig[]
  function getActiveDGIds(config: DeelgebiedConfig[]): string[]

utils/leerlijnen.ts:
  function getLeerlijnenMappingSync(): Record<string, string>  // values: "lesgeven"|"organiseren"|"profHandelen"

utils/verzuimDrempels.ts:
  function getVerzuimDrempelsSync(): VerzuimDrempels  // { geoorloofd:number; ongeoorloofd:number } in MINUTES
  async function loadVerzuimDrempels(): Promise<VerzuimDrempels>
  async function saveVerzuimDrempels(d: VerzuimDrempels): Promise<boolean>
  const DEFAULT_VERZUIM_DREMPELS = { geoorloofd: 900, ongeoorloofd: 600 }  // minutes

utils/bpv.ts:
  interface BpvConfig { verwachteUren: number }   // hours
  interface BpvStudentRecord { gerealiseerdeUren: number }
  type BpvData = Record<string, BpvStudentRecord>
  async function getBpvConfig(): Promise<BpvConfig>
  async function saveBpvConfig(c: BpvConfig): Promise<boolean>
  async function getBpvData(): Promise<BpvData>
  async function saveBpvData(d: BpvData): Promise<boolean>
  function berekenBpvPct(gerealiseerd: number, verwacht: number): number  // 0..100
  function parseBpvExcel(buffer: ArrayBuffer): BpvData  // D-13 STUB — returns {}
  const DEFAULT_BPV_CONFIG = { verwachteUren: 200 }

utils/schema.ts:
  DEELGEBIEDEN: ReadonlyArray<{ id: string; label: string; group: string }>  // 19 items
</interfaces>

<invariants>
<!-- HARD INVARIANTS — violating any of these breaks dashboard rendering. -->

I1 (Score-Key Invariant, Pitfall 3 from RESEARCH.md):
  Student `deelgebiedScores` are keyed by the ORIGINAL schema dg.label (e.g., "V&A").
  - DeelgebiedenMatrix lookups MUST use the original schema dg.label.
  - SpiderChartCard axis.key MUST be the original schema dg.label.
  - The custom user-renamed label from getDeelgebiedenConfigSync() is for DISPLAY ONLY.

I2 (Threshold Storage Unit):
  Threshold inputs in the UI are HOURS (integers). Storage is MINUTES (hours × 60).
  - On read: divide stored minutes by 60 for display.
  - On save: multiply hours by 60 before calling saveVerzuimDrempels.

I3 (BPV Parser Stub, D-13):
  parseBpvExcel is a stub returning {}. The import button calls it but the resulting
  empty object writes no records. UI must show the parse-error copy
  "Onbekend BPV-bestandsformaat. Probeer een ander bestand." ONLY if the parser
  throws. The empty-but-successful case (the current stub state) renders the empty
  state in BpvProgressSection — this is intentional until the real parser ships.

I4 (CSS Append Discipline):
  src/index.css section 25 already contains the dg-* rules from Plan 18-04.
  This plan APPENDS the remaining classes inside the SAME section 25.
  Do NOT add a second "25." header. Do NOT modify the dg-* rules.
</invariants>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement section 4 (thresholds + BPV) in SettingsPage + create BpvProgressSection (SET-05, SET-06)</name>
  <files>src/components/SettingsPage.tsx, src/components/BpvProgressSection.tsx</files>
  <read_first>
    - src/components/SettingsPage.tsx (entire file, after 18-04 ran) — section 4 placeholder `<p className="settings-placeholder-text">Komt in een volgende versie.</p>` inside `<section className="detail-section"><h2 className="detail-section-title">Drempelwaarden &amp; BPV-uren</h2>` is the insertion point; do not disturb the section-3 deelgebieden table 18-04 added
    - src/components/VerzuimSection.tsx — the empty-state and detail-section pattern (lines 19-29) that BpvProgressSection mirrors
    - utils/verzuimDrempels.ts — loadVerzuimDrempels/saveVerzuimDrempels signatures and the minutes×60 convention
    - utils/bpv.ts — getBpvConfig/saveBpvConfig/getBpvData/saveBpvData/berekenBpvPct/parseBpvExcel signatures
    - .planning/phases/18-settings-panel-advanced/18-UI-SPEC.md (sections 2 "Drempelwaarden & BPV-uren" and 3 "BPV Progress Section in DetailWeergave" — verbatim layout, copy, attributes)
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (sections "src/components/SettingsPage.tsx (MODIFY)" — number input + import button, and "src/components/BpvProgressSection.tsx (NEW)")
    - .planning/phases/18-settings-panel-advanced/18-RESEARCH.md (Code Examples for BpvConfig and BpvStudentData)
  </read_first>
  <action>
    Edit src/components/SettingsPage.tsx:

    Imports (add to existing import block; do not duplicate 18-04's imports):
    - `import { loadVerzuimDrempels, saveVerzuimDrempels } from '../../utils/verzuimDrempels';`
    - `import { getBpvConfig, saveBpvConfig, parseBpvExcel, saveBpvData, getBpvData, type BpvConfig, type BpvData } from '../../utils/bpv';`

    State (add alongside existing dgConfig / leerlijnenMapping / confirmingReset state):
    - `const [geoorloofdHours, setGeoorloofdHours] = useState<number>(15);`  // default: 900 min / 60 = 15u
    - `const [ongeoorloofdHours, setOngeoorloofdHours] = useState<number>(10);` // default: 600 min / 60 = 10u
    - `const [bpvUren, setBpvUren] = useState<number>(200);`
    - `const [bpvImportError, setBpvImportError] = useState<string | null>(null);`

    Mount effect (add a SEPARATE useEffect; do not merge with 18-04's):
    - Promise.all([loadVerzuimDrempels(), getBpvConfig()]).then(([drempels, bpvConfig]) => { setGeoorloofdHours(Math.round(drempels.geoorloofd / 60)); setOngeoorloofdHours(Math.round(drempels.ongeoorloofd / 60)); setBpvUren(bpvConfig.verwachteUren); }).catch(err => console.warn('[SettingsPage] section 4 load failed:', err));

    Handlers:
    - `handleGeoorloofdChange(hours: number)`: clamp hours to [0, 200]; setGeoorloofdHours(clamped); await saveVerzuimDrempels({ geoorloofd: clamped * 60, ongeoorloofd: ongeoorloofdHours * 60 }) — apply on change (per UI-SPEC instant-apply).
    - `handleOngeoorloofdChange(hours: number)`: clamp [0, 200]; setOngeoorloofdHours(clamped); await saveVerzuimDrempels({ geoorloofd: geoorloofdHours * 60, ongeoorloofd: clamped * 60 }).
    - `handleBpvUrenChange(uren: number)`: clamp [0, 9999]; setBpvUren(clamped); await saveBpvConfig({ verwachteUren: clamped }).
    - `handleBpvImport()`: trigger an `<input type="file" accept=".xlsx,.xls" />` (use a hidden ref-attached `<input>` and call .click()); on file selection, read the file as ArrayBuffer via `await file.arrayBuffer()`; try `const records = parseBpvExcel(buffer)`; if no exception, merge with existing data via `const merged = { ...(await getBpvData()), ...records }; await saveBpvData(merged); setBpvImportError(null);`. If parseBpvExcel throws OR records is `null`/non-object, setBpvImportError('Onbekend BPV-bestandsformaat. Probeer een ander bestand.'). Note the D-13 stub will currently return {} successfully — that is OK; no error fires; no records merge.

    JSX (replace the section-4 placeholder; preserve `<section className="detail-section">` + `<h2 className="detail-section-title">Drempelwaarden &amp; BPV-uren</h2>`):

    1. Verzuim drempelwaarden subsection (`<div className="settings-threshold-group">`):
       - Row 1 (`<div className="settings-threshold-row">`):
         - `<label style={{ minWidth: 160 }}>Geoorloofd verzuim waarschuwing</label>`
         - `<input type="number" className="settings-number-input" min={0} max={200} step={1} value={geoorloofdHours} onChange={e => handleGeoorloofdChange(Number(e.target.value))} />`
         - `<span style={{ color: 'var(--text-muted)' }}>uur</span>`
       - Row 2 — same layout, label "Ongeoorloofd verzuim waarschuwing", handler handleOngeoorloofdChange.

    2. Separator: `<hr style={{ border: 'none', borderTop: '1px solid var(--border-light)', margin: '16px 0' }} />`

    3. BPV subsection:
       - `<div className="settings-threshold-row">`
         - `<label style={{ minWidth: 160 }}>Verwachte BPV-uren per periode</label>`
         - `<input type="number" className="settings-number-input" min={0} max={9999} step={1} value={bpvUren} onChange={e => handleBpvUrenChange(Number(e.target.value))} />`
         - `<span style={{ color: 'var(--text-muted)' }}>uur</span>`
       - Below the row:
         - A hidden `<input type="file" ref={fileInputRef} accept=".xlsx,.xls" style={{ display: 'none' }} onChange={handleBpvImportFile} />` (handleBpvImportFile reads the selected file).
         - `<button className="btn btn-ghost" style={{ marginTop: 8 }} onClick={() => fileInputRef.current?.click()}>BPV-uren importeren</button>`
         - `<p style={{ fontSize: '0.875rem', color: 'var(--text-muted)', marginTop: 8 }}>Importeer de BPV Excel om gerealiseerde uren per leerling bij te houden.</p>`
         - If bpvImportError: `<p style={{ fontSize: '0.875rem', color: '#EF4444', marginTop: 8 }}>{bpvImportError}</p>`

    NEW FILE — src/components/BpvProgressSection.tsx:
    - Imports: `import { useEffect, useState } from 'react'; import { getBpvConfig, getBpvData, berekenBpvPct, type BpvConfig, type BpvStudentRecord } from '../../utils/bpv';`
    - Props interface: `interface BpvProgressSectionProps { leerlingId: string }`
    - Default export functional component.
    - State: `const [bpvConfig, setBpvConfig] = useState<BpvConfig | null>(null); const [record, setRecord] = useState<BpvStudentRecord | null>(null);`
    - useEffect on [leerlingId]: `Promise.all([getBpvConfig(), getBpvData()]).then(([cfg, data]) => { setBpvConfig(cfg); setRecord(data[leerlingId] ?? null); }).catch(err => console.warn('[BpvProgressSection] load failed:', err));`
    - Render:
      - Wrapper: `<div className="detail-section">` and title `<p className="detail-section-title">BPV-uren</p>` (use `<p>` per the VerzuimSection analog pattern, not `<h2>`)
      - Empty state (when `bpvConfig === null || record === null`): `<p style={{ fontSize: '0.875rem', color: '#9ca3af' }}>Geen BPV-data — importeer de BPV Excel via Instellingen.</p>`
      - Populated state (`bpvConfig !== null && record !== null`):
        - Compute: `const gerealiseerd = record.gerealiseerdeUren; const verwacht = bpvConfig.verwachteUren; const pct = berekenBpvPct(gerealiseerd, verwacht); const verschil = verwacht - gerealiseerd; const verschilPrefix = verschil >= 0 ? '+' : '−'; const overshoot = gerealiseerd >= verwacht;`
        - `<div className="bpv-progress-wrap">`
          - Bar row: `<div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>`
            - `<div className="bpv-bar-track"><div className="bpv-bar-fill" style={{ width: \`${Math.min(100, pct)}%\`, background: overshoot ? '#22C55E' : undefined }} /></div>`
            - `<span style={{ fontSize: '0.875rem', fontWeight: 700, color: 'var(--text-primary)' }}>{pct}%</span>`
          - Stats row: `<div style={{ display: 'flex', gap: 16, flexWrap: 'wrap' }}>` with three stats:
            - Gerealiseerd: label uppercase 11px text-muted / value 14px 700 text-primary — value `${gerealiseerd}u`
            - Verwacht: same — value `${verwacht}u`
            - Verschil: same — value `${verschilPrefix}${Math.abs(verschil)}u`

    Critical preservations:
    - Section 3 (deelgebieden table from 18-04) is untouched.
    - Section 1 + Section 2 are untouched.
    - All Dutch copy verbatim from UI-SPEC Copywriting Contract.
    - Number inputs apply on change (instant-apply per UI-SPEC); no debounce.
    - Use the file-input ref pattern (no external file-picker library).
  </action>
  <verify>
    <automated>npm test -- --run && npm run typecheck-migrated 2>&1 | grep -v '^#' | grep -iE "error|fail" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SettingsPage.tsx contains the literals: `settings-threshold-group`, `settings-threshold-row`, `settings-number-input`, `Geoorloofd verzuim waarschuwing`, `Ongeoorloofd verzuim waarschuwing`, `Verwachte BPV-uren per periode`, `BPV-uren importeren`, `Importeer de BPV Excel om gerealiseerde uren per leerling bij te houden.`
    - src/components/SettingsPage.tsx contains the literal `parseBpvExcel` AND `saveBpvData` AND `saveVerzuimDrempels` AND `saveBpvConfig`
    - src/components/SettingsPage.tsx contains the literal `Onbekend BPV-bestandsformaat. Probeer een ander bestand.` (parse-error copy)
    - src/components/SettingsPage.tsx contains `* 60` (the hours×60 conversion for storage) — appears at least twice (once per threshold save)
    - src/components/SettingsPage.tsx no longer contains the literal `Komt in een volgende versie` ANYWHERE (both section-3 and section-4 placeholders are now replaced)
    - src/components/BpvProgressSection.tsx exists, is a default-export React component, contains the literals: `BPV-uren`, `Geen BPV-data — importeer de BPV Excel via Instellingen.`, `Gerealiseerd`, `Verwacht`, `Verschil`, `bpv-bar-track`, `bpv-bar-fill`, `berekenBpvPct`, `getBpvConfig`, `getBpvData`
    - src/components/BpvProgressSection.tsx contains the literal `#22C55E` (overshoot green per UI-SPEC)
    - `npm test -- --run` exits 0
    - `npm run typecheck-migrated` exits 0
  </acceptance_criteria>
  <done>Section 4 UI is functional with instant-apply thresholds and BPV import; BpvProgressSection component file exists and is ready to mount.</done>
</task>

<task type="auto">
  <name>Task 2: Wire active-deelgebied filter + leerlijn-mapping into DeelgebiedenMatrix and SpiderChartCard (SET-03, SET-04 — score-key invariant)</name>
  <files>src/components/DeelgebiedenMatrix.tsx, src/components/SpiderChartCard.tsx</files>
  <read_first>
    - src/components/DeelgebiedenMatrix.tsx (entire file) — to locate lines 4, 60-65 where DEELGEBIEDEN + GROEPEN drive the rendering; verify how `dg.label` is used downstream as the score lookup key
    - src/components/SpiderChartCard.tsx (entire file) — to locate lines 3, 14-17 where the axes are built with `dg.label` as both key and label; this is the file where the score-key vs display-label invariant matters most
    - utils/deelgebieden.ts — getDeelgebiedenConfigSync signature; DeelgebiedConfig shape
    - utils/leerlijnen.ts — getLeerlijnenMappingSync signature
    - utils/schema.ts — confirm DEELGEBIEDEN ordering and the `group` field semantics (default leerlijn assignment)
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (sections "src/components/DeelgebiedenMatrix.tsx (MODIFY)" and "src/components/SpiderChartCard.tsx (MODIFY)") — verbatim refactor patterns
    - .planning/phases/18-settings-panel-advanced/18-RESEARCH.md (Pitfall 3 — score-key invariant; Pitfall 6 — leerlijn grouping)
  </read_first>
  <action>
    Edit src/components/DeelgebiedenMatrix.tsx:

    - Add imports: `import { getDeelgebiedenConfigSync, type DeelgebiedConfig } from '../../utils/deelgebieden';` and `import { getLeerlijnenMappingSync } from '../../utils/leerlijnen';`. Confirm relative path depth: src/components → ../../utils.
    - Read configuration synchronously inside the render function body (the pre-warm in main.tsx guarantees populated caches by the time any component renders):
      `const dgConfig = getDeelgebiedenConfigSync();`
      `const mapping = getLeerlijnenMappingSync();`
      `const activeIds = new Set(dgConfig.filter(c => c.active).map(c => c.id));`
      `const labelById = new Map(dgConfig.map(c => [c.id, c.label]));`  // for DISPLAY label only — score lookups use the schema dg.label
    - Replace the existing `groepDG` construction (lines 60-65 area) with a filtered version:
      `const groepDG: Record<string, typeof DEELGEBIEDEN> = {};`
      `for (const g of GROEPEN) {`
      `  groepDG[g.key] = DEELGEBIEDEN.filter(dg => {`
      `    if (!activeIds.has(dg.id)) return false;`
      `    const dgLeerlijn = mapping[dg.id] || dg.group;`
      `    return dgLeerlijn === g.key;`
      `  });`
      `}`
      `const allDG = GROEPEN.flatMap(g => groepDG[g.key]);`
    - In the JSX rendering of each deelgebied row/cell, where the display label is shown to the user, use `labelById.get(dg.id) ?? dg.label`. Where the SCORE is looked up (any `deelgebiedScores[dg.label]` or analogous expression), KEEP `dg.label` — this is the schema label, which matches storage keys (Invariant I1 / Pitfall 3).
    - Do NOT add useState or useEffect — sync accessors plus the main.tsx pre-warm are sufficient. (If pre-warm has not run yet for any reason, the sync accessor falls back to schema defaults — graceful degradation.)

    Edit src/components/SpiderChartCard.tsx:

    - Add the same two imports: `import { getDeelgebiedenConfigSync } from '../../utils/deelgebieden';` (mapping not needed here — SpiderChartCard groups by `dg.group` schema field which is fine for axis grouping).
    - Locate the axes construction (lines 15-17 area). Replace with:
      `const dgConfig = getDeelgebiedenConfigSync();`
      `const activeIds = new Set(dgConfig.filter(c => c.active).map(c => c.id));`
      `const labelById = new Map(dgConfig.map(c => [c.id, c.label]));`
      `const axes = DEELGEBIEDEN`
      `  .filter(dg => dg.group === group)`
      `  .filter(dg => activeIds.has(dg.id))`
      `  .map(dg => ({`
      `    key: dg.label,                              // CRITICAL: schema label for score lookup (Pitfall 3 / Invariant I1)`
      `    label: labelById.get(dg.id) ?? dg.label,    // custom label for display only`
      `  }));`
    - Confirm the comment at the top of the existing axes block (the `CRITICAL: use dg.label (not dg.id)` comment) remains intact; if it does not exist in this file, add a one-line comment immediately above the .map: `// SCORE-KEY INVARIANT (Phase 18): axis.key MUST be the schema dg.label to match deelgebiedScores storage keys`.
    - Downstream code that consumes `axes` continues to look up scores by `axis.key` (which is the schema label). Do not modify the consumer logic.
  </action>
  <verify>
    <automated>npm test -- --run && npm run typecheck-migrated 2>&1 | grep -v '^#' | grep -iE "error|fail" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/DeelgebiedenMatrix.tsx contains the literal `getDeelgebiedenConfigSync` AND `getLeerlijnenMappingSync` AND `activeIds`
    - src/components/DeelgebiedenMatrix.tsx contains the literal `mapping[dg.id] || dg.group` (runtime leerlijn-mapping used for group assignment per Pitfall 6)
    - src/components/SpiderChartCard.tsx contains the literal `getDeelgebiedenConfigSync` AND `activeIds`
    - src/components/SpiderChartCard.tsx contains the literal `key: dg.label` (Invariant I1 — SCORE-KEY INVARIANT preserved; this exact substring serves as the source-of-truth assertion)
    - src/components/SpiderChartCard.tsx contains the literal `label: labelById.get(dg.id)` (custom label used for display)
    - Neither file uses `dg.id` as the score lookup key anywhere
    - `npm test -- --run` exits 0 (full suite green)
    - `npm run typecheck-migrated` exits 0
  </acceptance_criteria>
  <done>DeelgebiedenMatrix and SpiderChartCard render only active deelgebieden, respect runtime leerlijn reassignment (matrix), and preserve the score-key invariant (spider).</done>
</task>

<task type="auto">
  <name>Task 3: Mount BpvProgressSection in DetailWeergave + append CSS section 25 completion (SET-06, D-12)</name>
  <files>src/components/DetailWeergave.tsx, src/index.css</files>
  <read_first>
    - src/components/DetailWeergave.tsx — section order at lines 117-173, confirm <VerzuimSection student={student} /> at line 164 (the line BPV section is inserted AFTER), and <VakkenSection> at line 167 (BPV inserted BEFORE)
    - src/components/BpvProgressSection.tsx (created in Task 1) — confirm prop interface is `{ leerlingId: string }`
    - src/components/DetailWeergave.tsx — verify how the current student's leerlingId is available in scope (read the relevant prop or derived variable; pass it through to BpvProgressSection)
    - src/index.css — confirm section 25 from 18-04 exists with the dg-* rules ending at some line; the new classes append BELOW the existing section-25 content
    - .planning/phases/18-settings-panel-advanced/18-UI-SPEC.md (section 3 BPV Progress Section + CSS Classes to Create)
    - .planning/phases/18-settings-panel-advanced/18-RESEARCH.md (Code Examples § Section 25 CSS lines 638-682 — the threshold/BPV CSS portion)
    - .planning/phases/18-settings-panel-advanced/18-04-SUMMARY.md (the note "Append threshold and BPV CSS classes BELOW the existing dg-* rules in section 25; do NOT add a new section header.")
  </read_first>
  <action>
    Edit src/components/DetailWeergave.tsx:

    - Add an import at the top of the existing import block: `import BpvProgressSection from './BpvProgressSection';`.
    - Locate the line `<VerzuimSection student={student} />` (around line 164) and insert IMMEDIATELY AFTER it:
      `<BpvProgressSection leerlingId={leerlingId} />`
      where `leerlingId` is the variable already in scope in DetailWeergave (verify the exact identifier; in the current codebase it is a prop or destructured value — match the casing used by the file).
    - Do NOT touch any other section's ordering, props, or imports. Specifically: preserve the order DoortstroomPrognoseSection → AanvullendSection → StageSection → FeedbackActiepuntenSection → LeerlijnenSection → SpiderChartCard row → DeelgebiedenMatrix → VerzuimSection → **BpvProgressSection (new)** → VakkenSection → NotitiesTextarea.

    Edit src/index.css — append to the existing section 25 (do NOT add a new "25." header; the dg-* rules from 18-04 stay at the top of section 25, these new rules go BELOW them):

    1. `.settings-threshold-group` — display: flex; flex-direction: column; gap: 8px; margin-bottom: 16px;
    2. `.settings-threshold-row` — display: flex; align-items: center; gap: 8px;
    3. `.settings-number-input` — width: 80px; padding: 0.5rem; border: 1.5px solid var(--border-default); border-radius: var(--radius-sm); background: var(--bg-surface); color: var(--text-primary); font-size: 0.875rem; text-align: right; box-shadow: var(--shadow-xs); outline: none; transition: border-color var(--transition-base), box-shadow var(--transition-base);
    4. `.settings-number-input:focus` — border-color: var(--border-focus); box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.12);
    5. `.bpv-progress-wrap` — display: flex; flex-direction: column; gap: 8px;
    6. `.bpv-bar-track` — flex: 1; height: 8px; border-radius: 4px; background: var(--border-light); overflow: hidden;
    7. `.bpv-bar-fill` — height: 100%; border-radius: 4px; background: var(--accent); transition: width 300ms ease;

    Verbatim CSS values are sourced from RESEARCH.md "Code Examples § Section 25 CSS"; copy ONLY the threshold/BPV portion (do not duplicate the dg-* rules already present from 18-04).
  </action>
  <verify>
    <automated>npm test -- --run && npm run typecheck-migrated 2>&1 | grep -v '^#' | grep -iE "error|fail" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/DetailWeergave.tsx contains the literal `import BpvProgressSection from './BpvProgressSection';`
    - src/components/DetailWeergave.tsx contains the literal `<BpvProgressSection leerlingId={` (mount with the leerlingId prop)
    - The mount of `<BpvProgressSection` appears AFTER `<VerzuimSection` and BEFORE `<VakkenSection` in the file (order preserved per D-12)
    - src/index.css contains all of: `.settings-threshold-group`, `.settings-threshold-row`, `.settings-number-input`, `.bpv-progress-wrap`, `.bpv-bar-track`, `.bpv-bar-fill`
    - src/index.css contains EXACTLY ONE occurrence of `25. SettingsPage Phase 18` (no duplicate section header)
    - src/index.css still contains the dg-* rules from 18-04 (`.dg-settings-table-wrap`, `.dg-naam-input`, `.dg-leerlijn-select`, `.dg-toggle`) — unchanged
    - `npm test -- --run` exits 0 (full suite green)
    - `npm run typecheck-migrated` exits 0
  </acceptance_criteria>
  <done>BpvProgressSection is mounted between VerzuimSection and VakkenSection; CSS section 25 is complete with all 13+ Phase 18 classes; full suite green.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` exits 0 (full suite green — Phase 18 utility tests + status/prognosis tests + pre-existing SettingsPage tests + smoke render tests)
- `npm run typecheck-migrated` exits 0
- File ownership: only the six files in files_modified were touched
- Manual smoke (verifier checkpoint follow-up): open Settings → section 4 shows two threshold inputs in hours, BPV uren input + import button. Open DetailWeergave for any student → BPV section renders empty state. Set ongeoorloofd to 5u in settings → the verzuim section's "ongeoorloofd hoog" coloring kicks in at 5u (not 10u). Mark a deelgebied inactive → it disappears from the matrix and spider, scores still in store (visible after re-activating). Re-assign a deelgebied's leerlijn → the matrix moves it to the new column.
</verification>

<success_criteria>
- SettingsPage section 4 contains threshold inputs (hours→minutes×60), BPV expected hours input, BPV import button — all instant-apply
- BPV parser is the documented D-13 stub returning {}; the import button calls it without errors; UI shows error copy only on a real exception
- DeelgebiedenMatrix filters by active config AND respects runtime leerlijn-mapping for group assignment (Pitfall 6 addressed)
- SpiderChartCard filters by active config AND preserves the SCORE-KEY INVARIANT — axis.key is the schema dg.label, axis.label is the custom display label (Pitfall 3 / Invariant I1)
- BpvProgressSection renders empty state when no data; renders bar + stats when data exists; bar turns green at ≥100%
- DetailWeergave order is preserved with BpvProgressSection inserted between VerzuimSection and VakkenSection (D-12)
- CSS section 25 is complete (13+ classes); dg-* rules from 18-04 untouched
- All Dutch copy verbatim from UI-SPEC Copywriting Contract
- Zero pre-existing test regressions; full Phase 18 test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/18-settings-panel-advanced/18-05-SUMMARY.md` documenting:
- Final structure of section 4 (threshold inputs + BPV + import button)
- BpvProgressSection structure (empty state vs populated state, overshoot color)
- Confirmation that DeelgebiedenMatrix uses `dg.label` for score lookups (Invariant I1)
- Confirmation that SpiderChartCard `axes.key === dg.label` (Invariant I1)
- Confirmation that BPV parser is still the D-13 stub (parseBpvExcel returns {}) — flag for the user to provide a sample BPV Excel file when ready
- Note: section 4's bpvImportError state path is exercised only if parseBpvExcel throws; with the current stub it never fires
- Test counts: full Phase 18 suite N/N green; total suite M/M
</output>
