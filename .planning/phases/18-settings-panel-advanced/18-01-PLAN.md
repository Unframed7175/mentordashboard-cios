---
phase: 18-settings-panel-advanced
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/deelgebieden.test.ts
  - tests/verzuimDrempels.test.ts
  - tests/bpv.test.ts
  - tests/leerlijnen.test.ts
  - tests/status.test.ts
  - tests/prognosis.test.ts
autonomous: true
requirements: [SET-03, SET-04, SET-05, SET-06]
tags: [settings, deelgebieden, verzuim, bpv, prognose, tdd-scaffolds]

must_haves:
  truths:
    - "Failing tests exist for each new utility module before implementation begins"
    - "Failing tests exist for new behaviour added to berekenStatus and berekenPrognose"
    - "Test suite as a whole still runs (failures are isolated to the new tests)"
  artifacts:
    - path: tests/deelgebieden.test.ts
      provides: "RED tests for getDeelgebiedenConfig defaults, save, reset, getActiveDGIds"
      min_lines: 60
    - path: tests/verzuimDrempels.test.ts
      provides: "RED tests for VerzuimDrempels defaults and persistence"
      min_lines: 40
    - path: tests/bpv.test.ts
      provides: "RED tests for BpvConfig defaults, berekenBpvPct, parseBpvExcel stub"
      min_lines: 50
    - path: tests/leerlijnen.test.ts
      provides: "RED tests for getLeerlijnenMappingSync cache + cold-cache fallback"
      min_lines: 30
    - path: tests/status.test.ts
      provides: "Extended RED tests for berekenStatus geoorloofd + ongeoorloofd thresholds"
    - path: tests/prognosis.test.ts
      provides: "Extended RED tests for berekenPrognose activeDeelgebiedenIds parameter"
  key_links:
    - from: tests/deelgebieden.test.ts
      to: utils/deelgebieden.ts
      via: "import { getDeelgebiedenConfig, saveDeelgebiedenConfig, resetDeelgebiedenConfig, getActiveDGIds, type DeelgebiedConfig } from '../utils/deelgebieden'"
      pattern: "from.*utils/deelgebieden"
    - from: tests/verzuimDrempels.test.ts
      to: utils/verzuimDrempels.ts
      via: "import { loadVerzuimDrempels, saveVerzuimDrempels, getVerzuimDrempelsSync, DEFAULT_VERZUIM_DREMPELS } from '../utils/verzuimDrempels'"
      pattern: "from.*utils/verzuimDrempels"
    - from: tests/bpv.test.ts
      to: utils/bpv.ts
      via: "import { getBpvConfig, saveBpvConfig, getBpvData, saveBpvData, berekenBpvPct, parseBpvExcel } from '../utils/bpv'"
      pattern: "from.*utils/bpv"
    - from: tests/leerlijnen.test.ts
      to: utils/leerlijnen.ts
      via: "import { getLeerlijnenMappingSync, getLeerlijnenMapping } from '../utils/leerlijnen'"
      pattern: "getLeerlijnenMappingSync"
---

<objective>
Wave 0 — Establish a RED test baseline for every new module and every behaviour change Phase 18 will introduce.

Purpose: Lock the executors to a contract before any implementation lands. Wave 1 (18-02) is GREEN only when these tests pass. Per Phase 18 cross-cutting constraints, every code-producing task in later waves needs an automated verification command — this plan supplies them.
Output: Six test files (4 new + 2 extended) that fail in a known, deterministic way until the corresponding utility / refactor lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-settings-panel-advanced/18-CONTEXT.md
@.planning/phases/18-settings-panel-advanced/18-RESEARCH.md
@.planning/phases/18-settings-panel-advanced/18-PATTERNS.md
@tests/SettingsPage.test.tsx
@tests/status.test.ts
@tests/prognosis.test.ts
@utils/schema.ts
@utils/leerlijnen.ts

<interfaces>
<!-- These interfaces are LOCKED contracts. Tests are written against them. -->
<!-- They will be implemented in Plan 18-02. Do not invent alternative shapes. -->

From utils/deelgebieden.ts (created in 18-02):
  export interface DeelgebiedConfig { id: string; label: string; active: boolean }
  export function getDeelgebiedenConfig(): Promise<DeelgebiedConfig[]>
  export function getDeelgebiedenConfigSync(): DeelgebiedConfig[]
  export function saveDeelgebiedenConfig(config: DeelgebiedConfig[]): Promise<boolean>
  export function resetDeelgebiedenConfig(): Promise<void>
  export function getActiveDGIds(config: DeelgebiedConfig[]): string[]
  STORE_KEY = 'deelgebieden_config'

From utils/verzuimDrempels.ts (created in 18-02):
  export interface VerzuimDrempels { geoorloofd: number; ongeoorloofd: number }
  export const DEFAULT_VERZUIM_DREMPELS: VerzuimDrempels = { geoorloofd: 900, ongeoorloofd: 600 }
  export function loadVerzuimDrempels(): Promise<VerzuimDrempels>
  export function getVerzuimDrempelsSync(): VerzuimDrempels
  export function saveVerzuimDrempels(d: VerzuimDrempels): Promise<boolean>
  STORE_KEY = 'verzuim_drempels'

From utils/bpv.ts (created in 18-02):
  export interface BpvConfig { verwachteUren: number }   // default 200
  export interface BpvStudentRecord { gerealiseerdeUren: number }
  export type BpvData = Record<string, BpvStudentRecord>
  export function getBpvConfig(): Promise<BpvConfig>
  export function saveBpvConfig(c: BpvConfig): Promise<boolean>
  export function getBpvData(): Promise<BpvData>
  export function saveBpvData(d: BpvData): Promise<boolean>
  export function berekenBpvPct(gerealiseerd: number, verwacht: number): number
  export function parseBpvExcel(buffer: ArrayBuffer): BpvData   // D-13 STUB returns {}
  STORE_KEYS = 'bpv_config' and 'bpv_data'

New export added to utils/leerlijnen.ts in 18-02:
  export function getLeerlijnenMappingSync(): Record<string, string>
  // Returns _cachedMapping if non-null, otherwise buildDefault() (schema defaults)

berekenStatus signature change (src/utils/status.ts, in 18-03):
  export function berekenStatus(
    student: any,
    traject?: string,
    thresholds?: { geoorloofd: number; ongeoorloofd: number }
  ): StatusResult
  // When thresholds omitted, uses getVerzuimDrempelsSync() internally.

berekenPrognose signature change (utils/prognosis.ts, in 18-03):
  export function berekenPrognose(
    student: any,
    traject?: string,
    activeDeelgebiedenIds?: string[]
  ): any
  // When activeDeelgebiedenIds provided, filters DEELGEBIEDEN to those ids in all three norms.
</interfaces>

<mock_pattern>
<!-- Plugin-store mock contract. Source: tests/SettingsPage.test.tsx pattern extended with delete(). -->
<!-- Use vi.hoisted() so the in-memory map survives module evaluation order. -->

vi.hoisted block creates getStoreMap()/setStoreMap() over a Map<string, unknown>.
vi.mock('@tauri-apps/plugin-store') exposes a LazyStore class with async get/set/save/delete methods backed by getStoreMap(). The delete() method MUST be present because resetDeelgebiedenConfig() calls store.delete(). The SettingsPage.test.tsx mock omits delete() — extend it here.

Each test must call setStoreMap(new Map()) AND reset the module-level _cache by re-importing the module with vi.resetModules() in beforeEach, OR by calling reset functions that null the cache.
</mock_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/deelgebieden.test.ts (SET-03 RED scaffold)</name>
  <files>tests/deelgebieden.test.ts</files>
  <read_first>
    - tests/SettingsPage.test.tsx — for the plugin-store mock pattern (vi.hoisted + LazyStore class)
    - utils/schema.ts — to import DEELGEBIEDEN and reference id values ('va','mm',...) and array length (19)
    - utils/leerlijnen.ts — the analog the new module mirrors; tests must mirror its persistence semantics
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (section "utils/deelgebieden.ts") — exact function signatures
  </read_first>
  <action>
    Create tests/deelgebieden.test.ts as a Vitest spec file targeting utils/deelgebieden.ts (to be implemented in 18-02). Import: getDeelgebiedenConfig, getDeelgebiedenConfigSync, saveDeelgebiedenConfig, resetDeelgebiedenConfig, getActiveDGIds, type DeelgebiedConfig from '../utils/deelgebieden', and DEELGEBIEDEN from '../utils/schema'.

    Implement the vi.hoisted + vi.mock('@tauri-apps/plugin-store') LazyStore class mock per the mock_pattern in context — include async delete(key) method.

    Add beforeEach that calls vi.resetModules() and setStoreMap(new Map()) to clear cache and store. Re-import the module under test inside each test via dynamic import to ensure module-level _cache resets.

    Required test cases (each it):
    1. "getDeelgebiedenConfig returns 19 entries on first call (cold cache)" — assert result.length === DEELGEBIEDEN.length and every entry has active===true and label matching DEELGEBIEDEN entry by id.
    2. "getDeelgebiedenConfig persists round-trip via saveDeelgebiedenConfig" — save a mutated copy (one entry active:false, one label:'Custom'), invalidate cache (resetModules retained store map), reload, assert returned config equals saved config.
    3. "getActiveDGIds returns ids of active entries only" — pass a config with 3 entries inactive and assert result.length === DEELGEBIEDEN.length - 3 and excluded ids are absent.
    4. "resetDeelgebiedenConfig wipes the stored key" — save a custom config, call resetDeelgebiedenConfig(), assert getStoreMap().has('deelgebieden_config') === false AND subsequent getDeelgebiedenConfig returns schema defaults.
    5. "getDeelgebiedenConfigSync returns schema defaults when cache cold" — without calling getDeelgebiedenConfig first, call sync accessor, assert length === DEELGEBIEDEN.length and first entry id matches DEELGEBIEDEN[0].id.

    Do NOT create utils/deelgebieden.ts here. Tests are expected to FAIL on import-resolution until 18-02 lands; that is the desired RED state.
  </action>
  <verify>
    <automated>npm test -- --run tests/deelgebieden.test.ts 2>&1 | grep -v '^#' | grep -E "(Cannot find module|failed|FAIL)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File tests/deelgebieden.test.ts exists and is at least 60 lines
    - File contains the substring `from '../utils/deelgebieden'` and the substring `from '../utils/schema'`
    - File contains exactly 5 occurrences of `it(` matching the 5 required cases (names include "first call", "round-trip", "getActiveDGIds", "resetDeelgebiedenConfig", "Sync")
    - File contains `vi.mock('@tauri-apps/plugin-store')` and the mocked LazyStore exposes an async `delete` method
    - `npm test -- --run tests/deelgebieden.test.ts` exits non-zero (RED) — failure is "Cannot find module '../utils/deelgebieden'" OR test assertion failures
  </acceptance_criteria>
  <done>tests/deelgebieden.test.ts present, 5 it() blocks, plugin-store mocked with delete(), suite RED with module-not-found error.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/verzuimDrempels.test.ts + tests/bpv.test.ts + tests/leerlijnen.test.ts</name>
  <files>tests/verzuimDrempels.test.ts, tests/bpv.test.ts, tests/leerlijnen.test.ts</files>
  <read_first>
    - tests/SettingsPage.test.tsx — plugin-store mock pattern (reuse exact mock from Task 1)
    - utils/leerlijnen.ts — current async getLeerlijnenMapping and _cachedMapping pattern
    - utils/schema.ts — for buildDefault expectations in the leerlijnen sync test (mapping defaults to dg.group per id)
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (sections "utils/verzuimDrempels.ts", "utils/bpv.ts", "utils/leerlijnen.ts (MODIFY)") — exact signatures
  </read_first>
  <action>
    Create three Vitest spec files. All three use the same plugin-store LazyStore mock pattern (vi.hoisted Map + class with async get/set/save/delete) from Task 1.

    tests/verzuimDrempels.test.ts — imports loadVerzuimDrempels, saveVerzuimDrempels, getVerzuimDrempelsSync, DEFAULT_VERZUIM_DREMPELS from '../utils/verzuimDrempels'. Tests:
      1. "loadVerzuimDrempels returns DEFAULT_VERZUIM_DREMPELS on cold cache" — assert geoorloofd === 900 and ongeoorloofd === 600.
      2. "saveVerzuimDrempels persists round-trip" — save { geoorloofd: 1200, ongeoorloofd: 300 }, reload after vi.resetModules() with map retained, assert returned values match.
      3. "getVerzuimDrempelsSync returns DEFAULT_VERZUIM_DREMPELS when cache cold" — without awaiting load, assert sync result equals DEFAULT_VERZUIM_DREMPELS.
      4. "saveVerzuimDrempels updates the sync cache" — call saveVerzuimDrempels({ geoorloofd: 1500, ongeoorloofd: 450 }), then call getVerzuimDrempelsSync() and assert it returns the new values (instant-apply pitfall 5).

    tests/bpv.test.ts — imports getBpvConfig, saveBpvConfig, getBpvData, saveBpvData, berekenBpvPct, parseBpvExcel from '../utils/bpv'. Tests:
      1. "getBpvConfig returns { verwachteUren: 200 } on cold cache".
      2. "saveBpvConfig persists round-trip" — save { verwachteUren: 320 }, reload, assert match.
      3. "getBpvData returns {} on cold cache" — assert Object.keys(result).length === 0.
      4. "saveBpvData persists per-student record" — save { 'L-001': { gerealiseerdeUren: 80 } }, reload, assert structure preserved.
      5. "berekenBpvPct returns 0 when verwacht is 0" — assert berekenBpvPct(50, 0) === 0.
      6. "berekenBpvPct caps at 100 when actual exceeds verwacht" — assert berekenBpvPct(250, 200) === 100.
      7. "berekenBpvPct rounds to nearest integer" — assert berekenBpvPct(67, 200) === 34 (33.5 → 34).
      8. "parseBpvExcel STUB returns empty object" — pass new ArrayBuffer(0), assert Object.keys(result).length === 0 AND no exception thrown. Include comment in test: // D-13: parser stubbed — replace when user supplies sample BPV Excel.

    tests/leerlijnen.test.ts — imports getLeerlijnenMappingSync, getLeerlijnenMapping from '../utils/leerlijnen', DEELGEBIEDEN from '../utils/schema'. Tests:
      1. "getLeerlijnenMappingSync returns schema defaults when cache cold" — without awaiting getLeerlijnenMapping, call sync, assert mapping has a key for each DEELGEBIEDEN id and each value matches the corresponding dg.group.
      2. "getLeerlijnenMappingSync returns cached value after getLeerlijnenMapping resolves" — set store with custom mapping { 'va': 'organiseren' }, await getLeerlijnenMapping, then call sync, assert sync.va === 'organiseren'.
      3. "getLeerlijnenMappingSync never returns a Promise" — assert typeof getLeerlijnenMappingSync().then === 'undefined'.

    All three files must be RED on first run because the imports do not yet exist (verzuimDrempels.ts, bpv.ts) or the new export does not yet exist (leerlijnen.ts getLeerlijnenMappingSync).
  </action>
  <verify>
    <automated>npm test -- --run tests/verzuimDrempels.test.ts tests/bpv.test.ts tests/leerlijnen.test.ts 2>&1 | grep -v '^#' | grep -E "(Cannot find|does not provide|failed|FAIL)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File tests/verzuimDrempels.test.ts exists with 4 occurrences of `it(`, ≥40 lines, imports `from '../utils/verzuimDrempels'`
    - File tests/bpv.test.ts exists with 8 occurrences of `it(`, ≥50 lines, imports `from '../utils/bpv'`, contains substring "D-13" AND substring "stubbed"
    - File tests/leerlijnen.test.ts exists with 3 occurrences of `it(`, ≥30 lines, imports `getLeerlijnenMappingSync` from '../utils/leerlijnen'
    - All three files use the vi.hoisted LazyStore mock with async delete
    - `npm test -- --run tests/verzuimDrempels.test.ts tests/bpv.test.ts tests/leerlijnen.test.ts` exits non-zero (RED)
  </acceptance_criteria>
  <done>Three RED test files committed; failure mode is module-not-found or missing-export.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tests/status.test.ts and tests/prognosis.test.ts with new behaviour assertions</name>
  <files>tests/status.test.ts, tests/prognosis.test.ts</files>
  <read_first>
    - tests/status.test.ts — current file, to understand existing student fixture shape and how berekenStatus is invoked
    - tests/prognosis.test.ts — current file, to understand existing prognosis fixtures and assertion style
    - src/utils/status.ts — current berekenStatus signature and the hardcoded VERZUIM_DREMPEL_MIN=600 at line 15
    - utils/prognosis.ts — current berekenPrognose at line 110 and telLeerlijnen at line 55
    - utils/schema.ts — DEELGEBIEDEN ids to use for activeDeelgebiedenIds parameter test
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (sections "src/utils/status.ts (MODIFY)" and "utils/prognosis.ts (MODIFY)") — exact signature changes
  </read_first>
  <action>
    Append new it() blocks inside new describe blocks. Do NOT delete existing tests.

    Append to tests/status.test.ts a new describe('berekenStatus thresholds (Phase 18)', ...) with these it() blocks:
      1. "returns oranje/Verzuim when ongeoorloofd exceeds custom threshold" — student fixture: verzuim.ongeoorloofd=700, verzuim.geoorloofd=0, valid prognose (has scores, prognose label 'sbl'). Call berekenStatus(student, undefined, { geoorloofd: 1500, ongeoorloofd: 600 }). Assert result.kleur === 'oranje' AND result.label === 'Verzuim'.
      2. "returns oranje/Verzuim when geoorloofd exceeds custom geoorloofd threshold" — verzuim.geoorloofd=1000, verzuim.ongeoorloofd=0, thresholds { geoorloofd: 900, ongeoorloofd: 600 }. Assert oranje/Verzuim.
      3. "uses runtime thresholds via getVerzuimDrempelsSync when arg omitted" — mock utils/verzuimDrempels' getVerzuimDrempelsSync via vi.mock to return { geoorloofd: 100, ongeoorloofd: 50 }. Build student with verzuim.ongeoorloofd=51, call berekenStatus(student) WITHOUT third arg. Assert oranje/Verzuim. Reset mock at end.
      4. "returns prognose-driven status when both verzuim values stay under thresholds" — verzuim.ongeoorloofd=10, verzuim.geoorloofd=10, thresholds { geoorloofd: 900, ongeoorloofd: 600 }, prognose 'sbl'. Assert kleur === 'groen' AND label === 'Op koers'.

    Append to tests/prognosis.test.ts a new describe('berekenPrognose activeDeelgebiedenIds filter (Phase 18)', ...) with these it() blocks:
      1. "without activeDeelgebiedenIds counts all 19 deelgebieden" — student with all DEELGEBIEDEN scored 'V' (voldoende). Call berekenPrognose(student). Assert result.totaalVoldoendeOfHoger === DEELGEBIEDEN.length (19).
      2. "with activeDeelgebiedenIds filters out inactive deelgebieden" — same student, call berekenPrognose(student, undefined, [DEELGEBIEDEN[0].id, DEELGEBIEDEN[1].id, DEELGEBIEDEN[2].id]). Assert result.totaalVoldoendeOfHoger === 3.
      3. "with empty activeDeelgebiedenIds array yields zero counts" — pass []. Assert result.totaalVoldoendeOfHoger === 0 AND result.totaalOnvoldoende === 0.
      4. "uses getLeerlijnenMappingSync (no Promise leak)" — mock '../utils/leerlijnen' so getLeerlijnenMappingSync returns a known mapping with all DEELGEBIEDEN[i].id mapped to 'lesgeven'. Build student with 4 'goed' scores on lesgeven-mapped deelgebieden. Call berekenPrognose with the corresponding active ids. Assert result.label is one of 'sbc' | 'sbl' | 'negatief' | 'neutraal' (not undefined — proves the mapping was a real object, not a Promise).

    All four prognosis tests MUST fail before 18-03 lands because the activeDeelgebiedenIds parameter is not yet plumbed through. Tests in status.test.ts MUST fail because the thresholds parameter and geoorloofd check do not yet exist.
  </action>
  <verify>
    <automated>npm test -- --run tests/status.test.ts tests/prognosis.test.ts 2>&1 | grep -v '^#' | grep -E "(failed|FAIL)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/status.test.ts contains a describe block whose name matches /berekenStatus thresholds .Phase 18./ with exactly 4 `it(` occurrences inside it
    - tests/prognosis.test.ts contains a describe block whose name matches /activeDeelgebiedenIds.*Phase 18/ with exactly 4 `it(` occurrences inside it
    - tests/status.test.ts case 1 contains substrings `kleur` and `'oranje'` and `'Verzuim'` and asserts threshold 600 with ongeoorloofd=700
    - tests/prognosis.test.ts case 2 asserts totaalVoldoendeOfHoger === 3 when activeDeelgebiedenIds has 3 ids
    - `npm test -- --run tests/status.test.ts tests/prognosis.test.ts` exits non-zero (the 4+4 new tests fail; existing tests may still pass)
    - Pre-existing it() blocks in both files are preserved unchanged (no deletions)
  </acceptance_criteria>
  <done>Both files extended with the specified new describe blocks; new tests RED; pre-existing tests unmodified.</done>
</task>

</tasks>

<verification>
- All four new test files exist with the required `it(` counts
- Both extended test files contain new Phase 18 describe blocks without removing prior tests
- `npm test -- --run tests/deelgebieden.test.ts tests/verzuimDrempels.test.ts tests/bpv.test.ts tests/leerlijnen.test.ts tests/status.test.ts tests/prognosis.test.ts` exits non-zero (RED is the goal — GREEN would mean tests do not exercise the new behaviour)
- `npm test -- --run` for any pre-existing test file (e.g., SettingsPage.test.tsx) still passes — Wave 0 must not break the existing suite outside the new RED scaffolds
</verification>

<success_criteria>
- 4 new test files + 2 extended test files committed
- New tests reference the exact interface symbols specified in <interfaces> (lock-in for 18-02 implementer)
- All new tests fail with module-not-found or missing-export errors (there is no implementation yet)
- Plugin-store LazyStore mock includes the delete() method missing from the existing SettingsPage.test.tsx mock
</success_criteria>

<output>
After completion, create `.planning/phases/18-settings-panel-advanced/18-01-SUMMARY.md` documenting:
- The exact it() block names per file
- The vi.hoisted LazyStore mock body (so 18-02 implementer can read the contract without re-deriving it)
- A short note that all 4+8+3+4+4 = 23 new tests are RED and gate Wave 1 GREEN
</output>
