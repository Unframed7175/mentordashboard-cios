---
phase: 18
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/deelgebieden.test.ts
  - tests/verzuimDrempels.test.ts
  - tests/bpv.test.ts
  - tests/leerlijnen.test.ts
  - tests/status.test.ts
  - tests/prognosis.test.ts
autonomous: true
requirements: [SET-03, SET-04, SET-05, SET-06]
tags: [react, typescript, vitest, tauri-plugin-store, settings, prognosis]
must_haves:
  truths:
    - "Failing test files exist for SET-03 (deelgebieden config persistence)"
    - "Failing test files exist for SET-05 (verzuim threshold persistence)"
    - "Failing test files exist for SET-06 (BPV config + progress)"
    - "Existing status.test.ts extended with threshold-parameter cases (currently failing)"
    - "Existing prognosis.test.ts extended with activeDeelgebiedenIds cases (currently failing)"
    - "Existing leerlijnen test coverage extended with getLeerlijnenMappingSync cases"
  artifacts:
    - path: tests/deelgebieden.test.ts
      provides: "Unit tests for utils/deelgebieden.ts (defaults, save, reset, getActiveDGIds)"
      contains: "describe.*deelgebieden"
    - path: tests/verzuimDrempels.test.ts
      provides: "Unit tests for utils/verzuimDrempels.ts (defaults, sync cache, save)"
      contains: "describe.*verzuimDrempels"
    - path: tests/bpv.test.ts
      provides: "Unit tests for utils/bpv.ts (config defaults, data save, berekenBpvPct)"
      contains: "describe.*bpv"
    - path: tests/leerlijnen.test.ts
      provides: "Unit tests for getLeerlijnenMappingSync + existing mapping persistence"
    - path: tests/status.test.ts
      provides: "Extended berekenStatus tests with thresholds parameter (geoorloofd/ongeoorloofd)"
    - path: tests/prognosis.test.ts
      provides: "Extended berekenPrognose tests with activeDeelgebiedenIds parameter"
  key_links:
    - from: tests/deelgebieden.test.ts
      to: utils/deelgebieden.ts
      via: "import { getDeelgebiedenConfig, saveDeelgebiedenConfig, resetDeelgebiedenConfig, getActiveDGIds }"
      pattern: "from.*utils/deelgebieden"
    - from: tests/verzuimDrempels.test.ts
      to: utils/verzuimDrempels.ts
      via: "import { getVerzuimDrempels, getVerzuimDrempelsSync, saveVerzuimDrempels, DEFAULT_VERZUIM_DREMPELS }"
      pattern: "from.*utils/verzuimDrempels"
    - from: tests/bpv.test.ts
      to: utils/bpv.ts
      via: "import { getBpvConfig, saveBpvConfig, getBpvData, saveBpvData, berekenBpvPct, parseBpvExcel }"
      pattern: "from.*utils/bpv"
---

<objective>
Wave 0 — Create failing test scaffolds for every behavior change in Phase 18. Tests are written FIRST (Nyquist rule) so that Wave 1 implementation has a red→green target. All tests in this plan are expected to FAIL until Wave 1 lands.

Purpose: Lock the contract before the implementation. Tests double as machine-checkable acceptance criteria for SET-03/04/05/06.
Output: 3 new test files + 3 extended existing test files; all failing in a predictable, contract-shaped way.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-settings-panel-advanced/18-CONTEXT.md
@.planning/phases/18-settings-panel-advanced/18-RESEARCH.md
@.planning/phases/18-settings-panel-advanced/18-PATTERNS.md
@tests/SettingsPage.test.tsx
@tests/status.test.ts

<interfaces>
<!-- LazyStore mock pattern that all new tests must use (extracted from tests/SettingsPage.test.tsx).
     CRITICAL: the existing mock in SettingsPage.test.tsx omits `async delete()` — new tests for
     reset() functions MUST add `delete()` to the mock. -->

Mock skeleton (vi.hoisted shared store):
- vi.hoisted returns { getStoreMap, setStoreMap }
- vi.mock('@tauri-apps/plugin-store') exports class LazyStore with: async get<T>(key), async set(key, value), async save(), async delete(key)
- Tests reset the storeMap in beforeEach via setStoreMap(new Map())

Function contracts the tests must enforce (these guide Wave 1 implementation):

utils/deelgebieden.ts:
  export interface DeelgebiedConfig { id: string; label: string; active: boolean }
  export async function getDeelgebiedenConfig(): Promise<DeelgebiedConfig[]>
  export function getDeelgebiedenConfigSync(): DeelgebiedConfig[]
  export async function saveDeelgebiedenConfig(config: DeelgebiedConfig[]): Promise<boolean>
  export async function resetDeelgebiedenConfig(): Promise<void>
  export function getActiveDGIds(config?: DeelgebiedConfig[]): string[]
  STORE_KEY: 'deelgebieden_config'

utils/verzuimDrempels.ts:
  export interface VerzuimDrempels { geoorloofd: number; ongeoorloofd: number }
  export const DEFAULT_VERZUIM_DREMPELS = { geoorloofd: 900, ongeoorloofd: 600 }
  export async function getVerzuimDrempels(): Promise<VerzuimDrempels>
  export function getVerzuimDrempelsSync(): VerzuimDrempels
  export async function saveVerzuimDrempels(d: VerzuimDrempels): Promise<boolean>
  STORE_KEY: 'verzuim_drempels'

utils/bpv.ts:
  export interface BpvConfig { verwachteUren: number }
  export interface BpvStudentRecord { gerealiseerdeUren: number }
  export type BpvData = Record<string, BpvStudentRecord>
  export const DEFAULT_BPV_CONFIG = { verwachteUren: 200 }
  export async function getBpvConfig(): Promise<BpvConfig>
  export function getBpvConfigSync(): BpvConfig
  export async function saveBpvConfig(c: BpvConfig): Promise<boolean>
  export async function getBpvData(): Promise<BpvData>
  export async function saveBpvData(data: BpvData): Promise<boolean>
  export function berekenBpvPct(gerealiseerd: number, verwacht: number): number
  export function parseBpvExcel(buffer: ArrayBuffer): BpvData  // stub returns {}
  STORE_KEYS: 'bpv_config', 'bpv_data'

utils/leerlijnen.ts (NEW EXPORT only):
  export function getLeerlijnenMappingSync(): Record<string, string>
    // returns _cachedMapping or buildDefault() if cache cold

utils/prognosis.ts (NEW SIGNATURE):
  export function berekenPrognose(student: any, traject?: string, activeDeelgebiedenIds?: string[]): any
    // when activeDeelgebiedenIds non-empty, filter DEELGEBIEDEN to those ids before tellen

src/utils/status.ts (NEW SIGNATURE):
  export function berekenStatus(
    student: any,
    traject?: string,
    thresholds?: { geoorloofd: number; ongeoorloofd: number }
  ): StatusResult
    // defaults: geoorloofd 900 min, ongeoorloofd 600 min
    // returns { kleur:'oranje', label:'Verzuim' } when ongeoorloofd > ongeoorloofdrempel
    // OR when geoorloofd > geoorloofdrempel (NEW per D-08)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create 3 new utility test files (deelgebieden, verzuimDrempels, bpv)</name>
  <files>tests/deelgebieden.test.ts, tests/verzuimDrempels.test.ts, tests/bpv.test.ts</files>
  <read_first>
    - tests/SettingsPage.test.tsx (LazyStore mock pattern — lines 12-34; copy the vi.hoisted + vi.mock skeleton)
    - tests/storage.test.ts (additional reference for LazyStore mock with delete())
    - utils/leerlijnen.ts (reference for the persistence contract these new modules will mirror)
    - utils/schema.ts (DEELGEBIEDEN array — 19 items — needed for length assertions in deelgebieden tests)
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (Mock pattern lines 678-702; extend with async delete())
    - .planning/phases/18-settings-panel-advanced/18-CONTEXT.md (D-15, D-16, D-17 — store key + shape contracts)
  </read_first>
  <behavior>
    tests/deelgebieden.test.ts cases:
    - "getDeelgebiedenConfig returns 19 defaults (all active=true, labels from schema) on first call when store empty"
    - "saveDeelgebiedenConfig persists via store.set + store.save, returns true"
    - "saveDeelgebiedenConfig invalidates cache; next getDeelgebiedenConfig returns the saved value"
    - "resetDeelgebiedenConfig calls store.delete and clears cache; next get returns schema defaults"
    - "getActiveDGIds(config) returns only ids where active===true"
    - "getDeelgebiedenConfigSync returns [] (or schema defaults — match implementation) when cache cold"

    tests/verzuimDrempels.test.ts cases:
    - "DEFAULT_VERZUIM_DREMPELS equals { geoorloofd: 900, ongeoorloofd: 600 }"
    - "getVerzuimDrempels returns DEFAULT when store empty"
    - "saveVerzuimDrempels stores object via store.set(key, value) — NOT JSON.stringify — and calls store.save"
    - "getVerzuimDrempelsSync returns cached value after save; returns DEFAULT_VERZUIM_DREMPELS when cache cold"
    - "Invalid stored shape (missing keys / non-number) falls back to DEFAULT"

    tests/bpv.test.ts cases:
    - "getBpvConfig returns { verwachteUren: 200 } when store empty"
    - "saveBpvConfig persists object via store.set + store.save"
    - "getBpvData returns empty object {} when store empty"
    - "saveBpvData persists per-student records and round-trips correctly"
    - "berekenBpvPct(100, 200) === 50; berekenBpvPct(250, 200) === 100 (clamped); berekenBpvPct(0, 0) === 0"
    - "parseBpvExcel(new ArrayBuffer(0)) returns {} (stub — D-13: format TBD)"
  </behavior>
  <action>
    Create three new test files under tests/. Each file imports vi/describe/it/expect/beforeEach from 'vitest', sets up the LazyStore mock using the vi.hoisted pattern from tests/SettingsPage.test.tsx (extended with `async delete(key: string): Promise<void> { getStoreMap().delete(key); }`), and imports the functions listed in the Interfaces block of this plan from the not-yet-existing module paths. Each test case from the <behavior> list becomes exactly one it() block. Tests MUST currently fail because the modules do not exist yet — that is the expected Wave 0 outcome. Use the exact STORE_KEY strings 'deelgebieden_config', 'verzuim_drempels', 'bpv_config', 'bpv_data'. Use beforeEach to reset the storeMap. Do NOT implement the utility modules in this task — Plan 18-02 owns implementation.
  </action>
  <verify>
    <automated>npm test -- --run tests/deelgebieden.test.ts tests/verzuimDrempels.test.ts tests/bpv.test.ts 2>&1 | grep -E "(Cannot find module|fail|FAIL)"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/deelgebieden.test.ts exists; contains `STORE_KEY = 'deelgebieden_config'` literal OR imports it; contains describe block matching /deelgebieden/i; contains it() blocks covering all six behaviors listed above (≥6 it() blocks)
    - File tests/verzuimDrempels.test.ts exists; contains `'verzuim_drempels'` literal; contains it() blocks for the five behaviors listed (≥5 it() blocks); references DEFAULT_VERZUIM_DREMPELS
    - File tests/bpv.test.ts exists; contains `'bpv_config'` and `'bpv_data'` literals; contains it() blocks for the six behaviors listed (≥6 it() blocks)
    - All three test files use the vi.hoisted + LazyStore class mock pattern (grep for `vi.hoisted` returns matches in each file)
    - The LazyStore mock in each file declares an `async delete(` method (grep for `async delete(` returns ≥1 match per file)
    - Running `npm test -- --run tests/deelgebieden.test.ts tests/verzuimDrempels.test.ts tests/bpv.test.ts` exits non-zero with errors of the form "Cannot find module 'utils/deelgebieden'" (or similar) — confirming the tests fail because the modules do not yet exist, not due to syntax errors
  </acceptance_criteria>
  <done>3 new test files created, all six contracts encoded as failing tests targeting the not-yet-existing utility modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend status.test.ts, prognosis.test.ts, leerlijnen.test.ts with Phase 18 cases</name>
  <files>tests/status.test.ts, tests/prognosis.test.ts, tests/leerlijnen.test.ts</files>
  <read_first>
    - tests/status.test.ts (current file — preserve existing tests; only add new describe blocks)
    - tests/prognosis.test.ts (if it exists — if not, create it with the standard Vitest skeleton and the LazyStore mock)
    - tests/leerlijnen.test.ts (if it exists — if not, create it with the LazyStore mock pattern)
    - src/utils/status.ts (current berekenStatus signature lines 115-132 — to confirm the threshold default fallback contract)
    - utils/prognosis.ts (current berekenPrognose signature line 110 + telLeerlijnen line 55 — to confirm activeDeelgebiedenIds param shape)
    - utils/leerlijnen.ts (full file — to confirm getLeerlijnenMappingSync expected behavior with cold cache)
    - utils/schema.ts (DEELGEBIEDEN ids needed for activeDeelgebiedenIds test fixtures)
  </read_first>
  <behavior>
    tests/status.test.ts NEW cases (append to existing describe or new describe block "berekenStatus thresholds"):
    - "berekenStatus(student, undefined, undefined) uses default thresholds {geoorloofd:900, ongeoorloofd:600} (backward compatible)"
    - "berekenStatus returns oranje/Verzuim when student.verzuim.ongeoorloofd > thresholds.ongeoorloofd"
    - "berekenStatus returns oranje/Verzuim when student.verzuim.geoorloofd > thresholds.geoorloofd (NEW per D-08)"
    - "berekenStatus does NOT return Verzuim when both are below their thresholds"
    - "Custom threshold { geoorloofd: 60, ongeoorloofd: 60 } triggers Verzuim at lower minute values than defaults"

    tests/prognosis.test.ts NEW cases (new describe block "berekenPrognose active deelgebieden filter"):
    - "berekenPrognose(student) (no third arg) considers all 19 DEELGEBIEDEN — baseline matches current output"
    - "berekenPrognose(student, undefined, ['va','mm']) only counts those 2 deelgebieden in totaalVoldoendeOfHoger / totaalOnvoldoende"
    - "Inactive deelgebieden with onvoldoende scores do NOT contribute to negatief norm count"
    - "≥13 voldoende threshold check uses active pool only (D-05) — student with 12 voldoende from 14 active deelgebieden is NOT label sbl"

    tests/leerlijnen.test.ts NEW cases (extend or create):
    - "getLeerlijnenMappingSync returns schema defaults when cache is cold (never awaited get*)"
    - "After awaiting getLeerlijnenMapping, getLeerlijnenMappingSync returns the same cached value"
    - "After saveLeerlijnenMapping then await getLeerlijnenMapping, sync accessor reflects the saved mapping"
  </behavior>
  <action>
    For tests/status.test.ts: append a new describe block "berekenStatus thresholds (Phase 18 SET-05)" containing the 5 it() blocks from the behavior list above. Build student fixtures inline with `{ verzuim: { geoorloofd: N, ongeoorloofd: M }, deelgebiedScores: {...minimal voldoende/onvoldoende mix...} }`. Reference berekenStatus from '../src/utils/status' (preserve existing import path).

    For tests/prognosis.test.ts: if the file does not exist, create it with the LazyStore mock skeleton (so getLeerlijnenMappingSync returns defaults). Add the describe block "berekenPrognose active deelgebieden filter (Phase 18 SET-03/04)" with the 4 it() blocks. Import DEELGEBIEDEN from '../utils/schema' to access deelgebied ids. Use realistic student.deelgebiedScores fixtures keyed by `dg.label`.

    For tests/leerlijnen.test.ts: if the file does not exist, create it with the LazyStore mock. Add the describe block "getLeerlijnenMappingSync (Phase 18)" with the 3 it() blocks. The sync function does not yet exist — these tests fail until Plan 18-02 adds it.

    Do NOT modify the production utils/prognosis.ts, src/utils/status.ts, or utils/leerlijnen.ts in this task. Tests-only.
  </action>
  <verify>
    <automated>npm test -- --run tests/status.test.ts tests/prognosis.test.ts tests/leerlijnen.test.ts 2>&1 | grep -E "(fail|FAIL|✗|✖)"</automated>
  </verify>
  <acceptance_criteria>
    - tests/status.test.ts contains a describe block referencing "thresholds" (grep `thresholds`) and ≥5 new it() blocks added (compare diff vs git HEAD); existing test cases preserved (existing it() count is unchanged or only additive)
    - tests/prognosis.test.ts exists and contains describe block referencing "active" or "activeDeelgebieden" + ≥4 it() blocks
    - tests/leerlijnen.test.ts exists and contains describe block referencing "Sync" or "getLeerlijnenMappingSync" + ≥3 it() blocks
    - All new tests fail at run time (one of: TypeError on missing param, "thresholds is not a function", or assertion failure) — confirmed by exit code != 0 from the verify command
    - No production source files in utils/ or src/utils/ were modified by this task (git diff utils/ src/utils/ is empty)
  </acceptance_criteria>
  <done>3 existing/new test files contain all new Phase 18 behavior assertions; full test suite has more total failures than before this plan ran (failing-by-design).</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `npm test -- --run` exits non-zero
- The failure count includes ≥ the sum of new it() blocks (6 + 5 + 6 + 5 + 4 + 3 = 29 new failing tests)
- No new tests pass falsely (i.e., no test that "tests nothing")
- No existing passing tests are now failing due to changes (regressions). If status.test.ts existing tests break because `berekenStatus` signature would require 3rd arg, that is acceptable ONLY if it's a TypeScript signature failure — those existing calls pass undefined as 3rd arg implicitly.
</verification>

<success_criteria>
- Three new test files committed: tests/deelgebieden.test.ts, tests/verzuimDrempels.test.ts, tests/bpv.test.ts
- Three existing/extended test files updated: tests/status.test.ts, tests/prognosis.test.ts, tests/leerlijnen.test.ts
- All Phase 18 behaviors from SET-03/04/05/06 have at least one machine-checkable test
- The test suite fails predictably — every failure traces to either "module not found" (new utils) or "missing param/feature" (extended utils)
</success_criteria>

<output>
After completion, create `.planning/phases/18-settings-panel-advanced/18-01-SUMMARY.md` documenting:
- Number of new test files + total new it() count per file
- Verification output (grep of failure count from `npm test -- --run`)
- Any contract drift discovered while writing tests that the planner missed (must be flagged before Plan 18-02 begins)
</output>
