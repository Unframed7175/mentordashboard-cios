---
phase: 18-settings-panel-advanced
plan: 02
type: execute
wave: 1
depends_on: ["18-01"]
files_modified:
  - utils/deelgebieden.ts
  - utils/verzuimDrempels.ts
  - utils/bpv.ts
  - utils/leerlijnen.ts
  - src/main.tsx
autonomous: true
requirements: [SET-03, SET-04, SET-05, SET-06]
tags: [settings, deelgebieden, verzuim, bpv, lazystore, prewarm]

must_haves:
  truths:
    - "All four new utility modules expose the contract locked in 18-01 tests"
    - "getLeerlijnenMappingSync exists in utils/leerlijnen.ts and returns a Record (never a Promise)"
    - "src/main.tsx pre-warms the four async caches before ReactDOM.createRoot()"
    - "All Wave 0 RED tests now pass (the 23 new tests from 18-01)"
  artifacts:
    - path: utils/deelgebieden.ts
      provides: "LazyStore persistence + sync accessor + active-ids helper for deelgebieden config"
      min_lines: 80
      contains: "STORE_KEY = 'deelgebieden_config'"
    - path: utils/verzuimDrempels.ts
      provides: "LazyStore persistence + sync accessor for verzuim drempels"
      min_lines: 60
      contains: "STORE_KEY = 'verzuim_drempels'"
    - path: utils/bpv.ts
      provides: "LazyStore persistence for bpv_config + bpv_data + berekenBpvPct + parseBpvExcel stub"
      min_lines: 80
    - path: utils/leerlijnen.ts
      provides: "Existing async API + new getLeerlijnenMappingSync export"
      contains: "export function getLeerlijnenMappingSync"
    - path: src/main.tsx
      provides: "Promise.all pre-warm for all four sync caches before ReactDOM.createRoot()"
  key_links:
    - from: utils/deelgebieden.ts
      to: "@tauri-apps/plugin-store"
      via: "new LazyStore('store.json', { defaults: {}, autoSave: false })"
      pattern: "LazyStore.*store\\.json"
    - from: src/main.tsx
      to: "utils/deelgebieden, utils/verzuimDrempels, utils/bpv, utils/leerlijnen"
      via: "Promise.all([getDeelgebiedenConfig(), loadVerzuimDrempels(), getBpvConfig(), getBpvData(), getLeerlijnenMapping()])"
      pattern: "getDeelgebiedenConfig|loadVerzuimDrempels|getBpvConfig"
---

<objective>
Wave 1 — Implement the three new utility modules, add the missing sync accessor to utils/leerlijnen.ts, and pre-warm all four async caches at app startup.

Purpose: This is the foundation Wave 2 depends on. Plan 18-03 (backend logic) imports getVerzuimDrempelsSync and getLeerlijnenMappingSync. Plan 18-04 (Settings UI) imports getDeelgebiedenConfig/save and getLeerlijnenMapping/save. After this plan, all 23 RED tests from 18-01 turn GREEN.
Output: Three new TS modules, one extended TS module, and updated main.tsx pre-warm sequence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-settings-panel-advanced/18-CONTEXT.md
@.planning/phases/18-settings-panel-advanced/18-RESEARCH.md
@.planning/phases/18-settings-panel-advanced/18-PATTERNS.md
@.planning/phases/18-settings-panel-advanced/18-01-SUMMARY.md
@utils/leerlijnen.ts
@utils/settings.ts
@utils/schema.ts
@src/main.tsx

<interfaces>
<!-- LOCKED contract — must match what tests/deelgebieden.test.ts, tests/verzuimDrempels.test.ts, -->
<!-- tests/bpv.test.ts, tests/leerlijnen.test.ts import. -->

utils/deelgebieden.ts exports:
  interface DeelgebiedConfig { id: string; label: string; active: boolean }
  async function getDeelgebiedenConfig(): Promise<DeelgebiedConfig[]>
  function getDeelgebiedenConfigSync(): DeelgebiedConfig[]
  async function saveDeelgebiedenConfig(config: DeelgebiedConfig[]): Promise<boolean>
  async function resetDeelgebiedenConfig(): Promise<void>
  function getActiveDGIds(config: DeelgebiedConfig[]): string[]
  function buildDefaultDeelgebiedenConfig(): DeelgebiedConfig[]
  STORE_KEY = 'deelgebieden_config'

utils/verzuimDrempels.ts exports:
  interface VerzuimDrempels { geoorloofd: number; ongeoorloofd: number }
  const DEFAULT_VERZUIM_DREMPELS: VerzuimDrempels = { geoorloofd: 900, ongeoorloofd: 600 }
  async function loadVerzuimDrempels(): Promise<VerzuimDrempels>
  function getVerzuimDrempelsSync(): VerzuimDrempels
  async function saveVerzuimDrempels(d: VerzuimDrempels): Promise<boolean>
  STORE_KEY = 'verzuim_drempels'

utils/bpv.ts exports:
  interface BpvConfig { verwachteUren: number }
  interface BpvStudentRecord { gerealiseerdeUren: number }
  type BpvData = Record<string, BpvStudentRecord>
  const DEFAULT_BPV_CONFIG: BpvConfig = { verwachteUren: 200 }
  async function getBpvConfig(): Promise<BpvConfig>
  function getBpvConfigSync(): BpvConfig
  async function saveBpvConfig(c: BpvConfig): Promise<boolean>
  async function getBpvData(): Promise<BpvData>
  async function saveBpvData(d: BpvData): Promise<boolean>
  function berekenBpvPct(gerealiseerd: number, verwacht: number): number
  function parseBpvExcel(buffer: ArrayBuffer): BpvData   // D-13 STUB: returns {}
  STORE_KEYS = 'bpv_config' (object) + 'bpv_data' (JSON-stringified object)

utils/leerlijnen.ts new export added (preserve all existing exports):
  function getLeerlijnenMappingSync(): Record<string, string>
  // Returns _cachedMapping if non-null, otherwise calls the internal buildDefault() (one entry per DEELGEBIEDEN id, value = dg.group)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create utils/deelgebieden.ts + utils/verzuimDrempels.ts</name>
  <files>utils/deelgebieden.ts, utils/verzuimDrempels.ts</files>
  <read_first>
    - utils/leerlijnen.ts (lines 1–105) — full LazyStore pattern: imports, _cachedMapping, buildDefault, isValid, get, save, reset
    - utils/settings.ts — simpler object-shape pattern (no JSON.stringify) used by verzuimDrempels
    - utils/schema.ts — DEELGEBIEDEN array shape (19 items, each with id, label, group)
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (sections "utils/deelgebieden.ts" and "utils/verzuimDrempels.ts") — verbatim code patterns
    - tests/deelgebieden.test.ts — the contract tests must pass
    - tests/verzuimDrempels.test.ts — the contract tests must pass
  </read_first>
  <action>
    Create utils/deelgebieden.ts following the leerlijnen.ts analog exactly:
      - Import: DEELGEBIEDEN from './schema', LazyStore from '@tauri-apps/plugin-store'
      - Module constants: const store = new LazyStore('store.json', { defaults: {}, autoSave: false }); const STORE_KEY = 'deelgebieden_config'; let _cache: DeelgebiedConfig[] | null = null;
      - Export interface DeelgebiedConfig with fields id (string), label (string), active (boolean)
      - Helper buildDefaultDeelgebiedenConfig(): map DEELGEBIEDEN → { id: dg.id, label: dg.label, active: true }
      - Helper isValid(config): require Array, length === DEELGEBIEDEN.length, every DEELGEBIEDEN id appears in the array
      - async getDeelgebiedenConfig(): cache check → store.get<string>(STORE_KEY) → JSON.parse → isValid → cache → fallback to buildDefault. Wrap read in try/catch with console.warn '[deelgebieden.ts] read error:'.
      - function getDeelgebiedenConfigSync(): return _cache ?? buildDefaultDeelgebiedenConfig()
      - async saveDeelgebiedenConfig(config): update _cache = config FIRST (instant-apply per pitfall 5), then await store.set(STORE_KEY, JSON.stringify(config)), then await store.save(). Return true on success, false in catch with console.warn '[deelgebieden.ts] plugin-store write error:'. CRITICAL: store.set + store.save must both await.
      - async resetDeelgebiedenConfig(): try await store.delete(STORE_KEY) + await store.save(); catch console.warn; then set _cache = null. (Note: pattern differs from saveDeelgebiedenConfig — reset always nulls cache.)
      - function getActiveDGIds(config): return config.filter(c => c.active).map(c => c.id)

    Create utils/verzuimDrempels.ts:
      - Import LazyStore from '@tauri-apps/plugin-store'
      - Module constants: same LazyStore('store.json', { defaults: {}, autoSave: false }), STORE_KEY = 'verzuim_drempels', let _cache: VerzuimDrempels | null = null
      - Export interface VerzuimDrempels { geoorloofd: number; ongeoorloofd: number }
      - Export const DEFAULT_VERZUIM_DREMPELS: VerzuimDrempels = { geoorloofd: 900, ongeoorloofd: 600 } (frozen via Object.freeze if you want, but not required)
      - function getVerzuimDrempelsSync(): return _cache ?? DEFAULT_VERZUIM_DREMPELS
      - async loadVerzuimDrempels(): cache check → store.get<VerzuimDrempels>(STORE_KEY) → typeof guard (both fields are 'number') → _cache = raw; fallback to spread of DEFAULT_VERZUIM_DREMPELS. Try/catch with console.warn '[verzuimDrempels.ts] read error:'.
      - async saveVerzuimDrempels(drempels): _cache = drempels FIRST, then await store.set(STORE_KEY, drempels) — pass object directly, no JSON.stringify (settings.ts pattern), then await store.save(). Return true / false in catch.

    Both files: TypeScript strict, no `any` casts. Use export keyword on interface and functions per project convention.
  </action>
  <verify>
    <automated>npm test -- --run tests/deelgebieden.test.ts tests/verzuimDrempels.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - utils/deelgebieden.ts exists, ≥80 lines, contains the literal `STORE_KEY = 'deelgebieden_config'` and the literal `new LazyStore('store.json'`
    - utils/deelgebieden.ts exports the symbols DeelgebiedConfig (interface), getDeelgebiedenConfig, getDeelgebiedenConfigSync, saveDeelgebiedenConfig, resetDeelgebiedenConfig, getActiveDGIds, buildDefaultDeelgebiedenConfig
    - utils/deelgebieden.ts saveDeelgebiedenConfig pairs `await store.set(` followed by `await store.save(` within the same function body
    - utils/verzuimDrempels.ts exists, ≥60 lines, contains the literal `STORE_KEY = 'verzuim_drempels'` and `geoorloofd: 900` and `ongeoorloofd: 600`
    - utils/verzuimDrempels.ts saveVerzuimDrempels pairs `await store.set(` followed by `await store.save(` within the same function body
    - `npm test -- --run tests/deelgebieden.test.ts tests/verzuimDrempels.test.ts` exits 0 (the 5+4 RED tests from 18-01 turn GREEN)
  </acceptance_criteria>
  <done>Both utility files created; their RED tests from 18-01 now pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create utils/bpv.ts and add getLeerlijnenMappingSync to utils/leerlijnen.ts</name>
  <files>utils/bpv.ts, utils/leerlijnen.ts</files>
  <read_first>
    - utils/leerlijnen.ts (entire file) — to identify where to append getLeerlijnenMappingSync (after resetLeerlijnenMapping near line 103) and to reuse _cachedMapping + the existing internal buildDefault() function
    - utils/settings.ts — object-shape persistence pattern reused for bpv_config
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (sections "utils/bpv.ts" and "utils/leerlijnen.ts (MODIFY)") — verbatim code patterns
    - tests/bpv.test.ts — contract tests
    - tests/leerlijnen.test.ts — contract tests
  </read_first>
  <action>
    Create utils/bpv.ts with two separate store keys ('bpv_config' object, 'bpv_data' JSON-stringified). Pattern mirrors verzuimDrempels.ts (config) + deelgebieden.ts (data, since BpvData may be large):
      - Imports: LazyStore from '@tauri-apps/plugin-store'
      - Module constants: const store = new LazyStore('store.json', { defaults: {}, autoSave: false }); const CONFIG_KEY = 'bpv_config'; const DATA_KEY = 'bpv_data'; let _configCache: BpvConfig | null = null; let _dataCache: BpvData | null = null;
      - Export interfaces BpvConfig { verwachteUren: number }, BpvStudentRecord { gerealiseerdeUren: number }, BpvData = Record<string, BpvStudentRecord>
      - Export const DEFAULT_BPV_CONFIG: BpvConfig = { verwachteUren: 200 } (D-14: 200 per UI-SPEC)
      - getBpvConfig() async: cache check → store.get<BpvConfig>(CONFIG_KEY) → typeof verwachteUren === 'number' guard → cache; fallback to { ...DEFAULT_BPV_CONFIG }. Try/catch console.warn '[bpv.ts] config read error:'.
      - getBpvConfigSync(): return _configCache ?? DEFAULT_BPV_CONFIG
      - saveBpvConfig(c) async: _configCache = c FIRST, await store.set(CONFIG_KEY, c) (object, not stringified), await store.save(). Return true / catch false.
      - getBpvData() async: cache check → store.get<string>(DATA_KEY) → JSON.parse → record-shape guard (object, not array) → cache; fallback to {}. Try/catch console.warn '[bpv.ts] data read error:'.
      - saveBpvData(d) async: _dataCache = d FIRST, await store.set(DATA_KEY, JSON.stringify(d)), await store.save(). Return true / catch false.
      - berekenBpvPct(gerealiseerd, verwacht): if (!verwacht || verwacht <= 0) return 0; return Math.min(100, Math.round((gerealiseerd / verwacht) * 100));
      - parseBpvExcel(buffer: ArrayBuffer): BpvData — STUB per D-13. Body MUST contain the comment text `// D-13: BPV Excel parser stubbed — replace when user supplies sample BPV Excel file`. The implementation reference the buffer parameter (e.g., `void buffer;`) to avoid the unused-arg warning and `return {};`.

    Modify utils/leerlijnen.ts:
      - Locate the existing async function getLeerlijnenMapping() and the module-level let _cachedMapping (or equivalent name) and the internal buildDefault() helper
      - Append a new exported function `getLeerlijnenMappingSync()` after the existing resetLeerlijnenMapping export (near line 103)
      - Body: `if (_cachedMapping !== null) return _cachedMapping; return buildDefault();`
      - Include a JSDoc comment above the export: "Synchronous cache accessor. Returns cached mapping if present, otherwise schema defaults. Use in berekenPrognose() and other sync call sites instead of the async version. Pre-warm by calling getLeerlijnenMapping() at app startup."
      - Do NOT modify any other export or function in leerlijnen.ts. Preserve resetLeerlijnenMapping, saveLeerlijnenMapping, getLeerlijnenMapping unchanged.
  </action>
  <verify>
    <automated>npm test -- --run tests/bpv.test.ts tests/leerlijnen.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - utils/bpv.ts exists, ≥80 lines, contains the literals `CONFIG_KEY = 'bpv_config'` and `DATA_KEY = 'bpv_data'` and `verwachteUren: 200`
    - utils/bpv.ts exports BpvConfig, BpvStudentRecord, BpvData, DEFAULT_BPV_CONFIG, getBpvConfig, getBpvConfigSync, saveBpvConfig, getBpvData, saveBpvData, berekenBpvPct, parseBpvExcel
    - utils/bpv.ts parseBpvExcel body contains the substring `D-13` AND the substring `stubbed`
    - utils/bpv.ts saveBpvConfig and saveBpvData both pair `await store.set(` followed by `await store.save(` within their function bodies
    - utils/leerlijnen.ts contains the substring `export function getLeerlijnenMappingSync(`
    - utils/leerlijnen.ts existing exports (getLeerlijnenMapping, saveLeerlijnenMapping, resetLeerlijnenMapping) still exist unchanged
    - `npm test -- --run tests/bpv.test.ts tests/leerlijnen.test.ts` exits 0 (8+3 RED tests turn GREEN)
  </acceptance_criteria>
  <done>utils/bpv.ts and the getLeerlijnenMappingSync addition both ship; their RED tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Pre-warm new caches in src/main.tsx</name>
  <files>src/main.tsx</files>
  <read_first>
    - src/main.tsx (entire file) — to see the current pre-warm sequence (loadKlassen at line 9, loadSettings at line 16, then ReactDOM.createRoot)
    - utils/deelgebieden.ts (just-written) — to import getDeelgebiedenConfig
    - utils/verzuimDrempels.ts (just-written) — to import loadVerzuimDrempels
    - utils/bpv.ts (just-written) — to import getBpvConfig, getBpvData
    - utils/leerlijnen.ts — to import getLeerlijnenMapping (existing async function — this populates the cache used by getLeerlijnenMappingSync)
    - .planning/phases/18-settings-panel-advanced/18-PATTERNS.md (section "main.tsx Pre-warm Sequence")
  </read_first>
  <action>
    In src/main.tsx, locate the existing pre-warm async IIFE (the block that awaits loadKlassen() and loadSettings() before ReactDOM.createRoot()). Add the four new pre-warm calls in a Promise.all so they run in parallel after loadKlassen/loadSettings:

    - Add imports at the top of the file in alphabetical order alongside existing utility imports:
      `import { getDeelgebiedenConfig } from '../utils/deelgebieden';`
      `import { loadVerzuimDrempels } from '../utils/verzuimDrempels';`
      `import { getBpvConfig, getBpvData } from '../utils/bpv';`
      `import { getLeerlijnenMapping } from '../utils/leerlijnen';`

    - After the existing `await loadSettings()` (or whichever line currently precedes ReactDOM.createRoot), add:
      `await Promise.all([
         getDeelgebiedenConfig(),
         loadVerzuimDrempels(),
         getBpvConfig(),
         getBpvData(),
         getLeerlijnenMapping(),
       ]);`

    Place a brief comment immediately above this Promise.all: `// Phase 18: pre-warm sync caches (D-15, D-16, D-17) before React mounts so sync accessors are populated`.

    Do NOT remove or reorder the existing loadKlassen() / loadSettings() awaits. Do NOT change any other code in main.tsx (App import, ReactDOM.createRoot call, root element id selector, error-banner block).

    Each new pre-warm function is fail-soft (the modules already swallow errors via console.warn and fall back to defaults), so wrapping in Promise.all without try/catch is acceptable — but if main.tsx already has a try/catch around the existing pre-warm, place the Promise.all inside that same try/catch.
  </action>
  <verify>
    <automated>npm test -- --run && npm run typecheck-migrated 2>&1 | grep -v '^#' | grep -iE "error|fail" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/main.tsx contains the literal substrings `getDeelgebiedenConfig`, `loadVerzuimDrempels`, `getBpvConfig`, `getBpvData`, `getLeerlijnenMapping`
    - src/main.tsx contains the literal substring `Promise.all`
    - src/main.tsx contains the literal substring `Phase 18`
    - The existing `loadKlassen` and `loadSettings` await calls are still present (grep -c finds both)
    - `npm test -- --run` exits 0 (all 23 new Phase 18 tests + all pre-existing tests pass)
    - `npm run typecheck-migrated` exits 0 (no new TypeScript errors)
  </acceptance_criteria>
  <done>main.tsx pre-warms the four new caches; full test suite green; typecheck clean.</done>
</task>

</tasks>

<verification>
- All 23 Wave 0 RED tests turn GREEN: `npm test -- --run tests/deelgebieden.test.ts tests/verzuimDrempels.test.ts tests/bpv.test.ts tests/leerlijnen.test.ts` exits 0
- The full pre-existing suite still passes: `npm test -- --run` exits 0
- TypeScript clean: `npm run typecheck-migrated` exits 0
- File ownership: only the five files in `files_modified` were touched
</verification>

<success_criteria>
- All four new utility files exist with the LOCKED interface from 18-01
- saveDeelgebiedenConfig, saveVerzuimDrempels, saveBpvConfig, saveBpvData each pair `store.set` + `store.save` (Phase 12 pitfall)
- _cache is updated BEFORE the await store.set call in each save function (instant-apply pitfall 5)
- parseBpvExcel is the documented D-13 stub returning {} with the in-file comment
- main.tsx pre-warms all four async caches before ReactDOM.createRoot()
- All 23 new Phase 18 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-settings-panel-advanced/18-02-SUMMARY.md` documenting:
- Final signatures of all new exports (for 18-03 and 18-04 consumers)
- Confirmation that the `set + save` pair is present in every save function
- Confirmation that getLeerlijnenMappingSync exists and is exported
- Test counts: 23/23 Wave 0 tests now green; total suite N/N
</output>
