---
phase: 31
plan: "31-01"
title: "CSS sizing changes — nav, nav-stripe, spider-card"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
autonomous: true
requirements:
  - UI-05
  - UI-07
---

## Objective

Update three CSS property values in `src/index.css` to double the nav-banner height, grow the diagonal stripe to fill the nav, and widen the spider chart card.

No logic changes — CSS property value substitutions only.

## Tasks

### Task 1 — Nav-banner height

<read_first>
- src/index.css lines 261–274 (#main-nav block)
</read_first>

<action>
In `src/index.css`, inside the `#main-nav` rule block (around line 268):
Change `min-height: 52px;` to `min-height: 104px;`
</action>

<acceptance_criteria>
- `src/index.css` contains `min-height: 104px` inside the `#main-nav` block
- `src/index.css` does NOT contain `min-height: 52px` inside the `#main-nav` block
- All other properties inside `#main-nav` are unchanged
</acceptance_criteria>

---

### Task 2 — Nav-stripe height

<read_first>
- src/index.css lines 276–284 (.nav-stripe block)
</read_first>

<action>
In `src/index.css`, inside the `.nav-stripe` rule block (around line 281):
Change `height: 52px;` to `height: 100%;`
The diagonal stripe fills the full nav height via this change (the nav has `position: relative`, so 100% resolves to the nav's computed 104px).
</action>

<acceptance_criteria>
- `src/index.css` contains `height: 100%;` inside the `.nav-stripe` block
- `src/index.css` does NOT contain `height: 52px` inside the `.nav-stripe` block
- The `.nav-stripe` `width`, `background`, `pointer-events` properties are unchanged
</acceptance_criteria>

---

### Task 3 — Spider card width

<read_first>
- src/index.css line 1380 (Section 26 spider-card override)
</read_first>

<action>
In `src/index.css`, on the Section 26 override line (around line 1380):
Change `.spider-card { width: 160px; position: relative; }` to `.spider-card { width: 280px; position: relative; }`
</action>

<acceptance_criteria>
- `src/index.css` line ~1380 contains `width: 280px` in the `.spider-card` Section 26 override
- `src/index.css` does NOT contain `width: 160px` anywhere (grep `src/index.css` for `160px` gives 0 spider-card matches)
- `position: relative` on the same line is preserved
</acceptance_criteria>

---

### Task 4 — No hardcoded hex colors introduced

<acceptance_criteria>
- `grep -n "#[0-9a-fA-F]\{3,6\}" src/index.css` shows no NEW hex color values in the lines modified by Tasks 1–3
- All color references in modified lines use `var(--*)` tokens or the existing `#009FE3` in `.nav-stripe` background (unchanged)
</acceptance_criteria>

---

## Verification

```bash
# Verify all three CSS changes
grep "min-height: 104px" src/index.css
grep "height: 100%" src/index.css | grep -v "min-height\|vh\|notitie\|textarea\|wrapper"
grep "width: 280px" src/index.css

# No 52px references left in nav context
grep "52px" src/index.css  # should show 0 results

# Tests still pass
npm run test
```

## Success Criteria

- `src/index.css` contains `min-height: 104px` in `#main-nav` ✓
- `src/index.css` contains `height: 100%` in `.nav-stripe` ✓
- `src/index.css` contains `width: 280px` in `.spider-card` Section 26 override ✓
- No `height: 52px` or `min-height: 52px` remains in nav context ✓
- `npm run test` exits 0 ✓

<must_haves>
truths:
  - "#main-nav min-height is 104px in src/index.css"
  - ".nav-stripe height is 100% (not 52px) in src/index.css"
  - ".spider-card width is 280px in Section 26 override in src/index.css"
  - "npm run test exits 0 after CSS changes"
</must_haves>
