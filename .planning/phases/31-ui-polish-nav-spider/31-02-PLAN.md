---
phase: 31
plan: "31-02"
title: "Component changes — logo height, spider SVG responsive, DetailWeergave section reorder"
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - src/components/KlasTabStrip.tsx
  - utils/spider.tsx
  - src/components/DetailWeergave.tsx
autonomous: true
requirements:
  - UI-05
  - UI-06
  - UI-07
---

## Objective

Three targeted component changes:

1. **KlasTabStrip.tsx** — Logo inline style `height: '36px'` → `height: '72px'`
2. **utils/spider.tsx** — SVG `width="160" height="160"` → `width="100%"` (remove height), update hit-circle coordinate scale from `160/200` to `280/200`
3. **DetailWeergave.tsx** — Move `<FeedbackActiepuntenSection>` from its current position (after RekenenNederlandsSection) to the absolute last position in the JSX (after `<BpvProgressSection>`)

No business logic changes — pure visual/structural adjustments.

## Tasks

### Task 1 — Logo height in KlasTabStrip.tsx

<read_first>
- src/components/KlasTabStrip.tsx line 65 (logo img element)
</read_first>

<action>
In `src/components/KlasTabStrip.tsx` at line 65, update the `img` inline style:
Change `style={{ height: '36px', width: 'auto', marginRight: '16px' }}`
to     `style={{ height: '72px', width: 'auto', marginRight: '16px' }}`
Only the height value changes. Width and marginRight stay unchanged.
</action>

<acceptance_criteria>
- `src/components/KlasTabStrip.tsx` contains `height: '72px'` on the logo `img` element
- `src/components/KlasTabStrip.tsx` does NOT contain `height: '36px'` on the logo `img` element
- `width: 'auto'` and `marginRight: '16px'` are preserved on the same element
- No new hardcoded hex colors introduced (grep `KlasTabStrip.tsx` for `#[0-9a-fA-F]` gives 0 new matches in modified lines)
</acceptance_criteria>

---

### Task 2 — Spider SVG responsive sizing

<read_first>
- utils/spider.tsx (full file — understand all SVG element occurrences and hit-circle scale factor)
</read_first>

<action>
In `utils/spider.tsx` make three changes:

**Change A — Empty-chart SVG (line ~69):**
Change `<svg width="160" height="160" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg" />`
to     `<svg width="100%" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg" />`
(Remove `height="160"` and `width="160"`, add `width="100%"`)

**Change B — Main chart SVG (line ~154):**
Change `<svg width="160" height="160" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">`
to     `<svg width="100%" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">`
(Remove `height="160"` and `width="160"`, add `width="100%"`)

**Change C — Hit-circle coordinate scale (lines ~148–149):**
The `onMouseEnter` callback converts viewBox coordinates (0–200) to rendered pixel space. With the card now 280px wide:
Change `cx * (160 / 200)` to `cx * (280 / 200)` and `cy * (160 / 200)` to `cy * (280 / 200)`.
The full lines become:
`onMouseEnter={() => onHover?.({ axisIndex: i, x: cx * (280 / 200), y: cy * (280 / 200) })}`
</action>

<acceptance_criteria>
- `utils/spider.tsx` contains `width="100%"` on both SVG elements (grep gives 2 matches)
- `utils/spider.tsx` does NOT contain `width="160"` (grep gives 0 matches)
- `utils/spider.tsx` does NOT contain `height="160"` (grep gives 0 matches)
- `utils/spider.tsx` contains `(280 / 200)` in the `onMouseEnter` callback for hit circles
- `utils/spider.tsx` does NOT contain `(160 / 200)` (grep gives 0 matches)
- `viewBox="0 0 200 200"` is preserved on both SVG elements
- `npm run test` exits 0 (spider chart tests pass)
</acceptance_criteria>

---

### Task 3 — DetailWeergave.tsx section reorder

<read_first>
- src/components/DetailWeergave.tsx lines 120–175 (all section JSX blocks, from the print button to the closing div)
</read_first>

<action>
In `src/components/DetailWeergave.tsx`, move the `<FeedbackActiepuntenSection>` JSX block from its current position (between RekenenNederlandsSection and the SpiderChartCard row) to the absolute last position inside the wrapper div — after `<BpvProgressSection leerlingId={leerlingId} />`.

The desired JSX order after this change:
1. DoortstroomPrognoseSection
2. RekenenNederlandsSection
3. SpiderChartCard row (div.detail-section)
4. DeelgebiedenMatrix
5. VerzuimSection
6. BpvProgressSection
7. FeedbackActiepuntenSection ← last

This is a pure cut-and-paste reorder. Do NOT change:
- Any props on FeedbackActiepuntenSection
- Any props on BpvProgressSection
- The comment text on any section
- Any logic, imports, or state
</action>

<acceptance_criteria>
- In `src/components/DetailWeergave.tsx`, `<FeedbackActiepuntenSection` appears AFTER `<BpvProgressSection` in the file (grep line numbers confirm this ordering)
- `<FeedbackActiepuntenSection leerlingId={leerlingId} />` is the last section element inside the wrapper div (i.e., the closing `</div>` of the outer wrapper appears directly after it)
- `<BpvProgressSection leerlingId={leerlingId} />` is the penultimate section element
- No imports removed or added
- `npm run test` exits 0
</acceptance_criteria>

---

### Task 4 — No hardcoded hex colors in modified JSX files

<acceptance_criteria>
- `grep "#[0-9a-fA-F]\{3,6\}" src/components/KlasTabStrip.tsx` — no NEW hex color values in lines touched by Task 1
- `grep "#[0-9a-fA-F]\{3,6\}" utils/spider.tsx` — no NEW hex color values in lines touched by Task 2
- `grep "#[0-9a-fA-F]\{3,6\}" src/components/DetailWeergave.tsx` — no NEW hex color values in lines touched by Task 3
- All existing `#d1d5db` and similar in spider.tsx are pre-existing (not introduced by this plan)
</acceptance_criteria>

---

## Verification

```bash
# Task 1: logo height
grep "height: '72px'" src/components/KlasTabStrip.tsx

# Task 2: SVG responsive
grep 'width="100%"' utils/spider.tsx        # should show 2 matches
grep 'width="160"' utils/spider.tsx          # should show 0 matches
grep 'height="160"' utils/spider.tsx         # should show 0 matches
grep '280 / 200' utils/spider.tsx            # should show 1 match

# Task 3: section order in DetailWeergave
grep -n "FeedbackActiepuntenSection\|BpvProgressSection" src/components/DetailWeergave.tsx
# BpvProgressSection line number must be LOWER than FeedbackActiepuntenSection line number

# Full test suite
npm run test
```

## Success Criteria

- `KlasTabStrip.tsx` has logo `height: '72px'` ✓
- `utils/spider.tsx` SVG elements use `width="100%"` with no `width="160"` or `height="160"` ✓
- `utils/spider.tsx` hit-circle scale factor is `(280 / 200)` ✓
- `DetailWeergave.tsx` has FeedbackActiepuntenSection AFTER BpvProgressSection ✓
- No hardcoded hex colors in any modified JSX ✓
- `npm run test` exits 0 ✓

<must_haves>
truths:
  - "KlasTabStrip.tsx logo img has height '72px' (not '36px')"
  - "utils/spider.tsx SVG elements have width='100%' — no hardcoded width='160' or height='160'"
  - "utils/spider.tsx hit-circle onMouseEnter scale is 280/200"
  - "DetailWeergave.tsx FeedbackActiepuntenSection is the last section before the closing wrapper div"
  - "npm run test exits 0 after all component changes"
</must_haves>
