---
phase: "09"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app.js
autonomous: true
requirements:
  - VRZ-01
  - VRZ-02

must_haves:
  truths:
    - "Elke leerlingtegel toont aanwezigheidspercentage (bijv. '87% aanwezig') onder de verzuimbalk"
    - "De tekst onder de balk is altijd neutraal grijs (--text-muted) — geen rode kleur bij drempeloverschrijding"
    - "De 3-delige verzuimbalk (aanwezig / geoorloofd / ongeoorloofd) en haar kleuren zijn ongewijzigd"
    - "VERZUIM_DREMPEL_MIN en alle drempellogica buiten buildMiniVerzuimBar() zijn ongewijzigd"
  artifacts:
    - path: "app.js"
      provides: "buildMiniVerzuimBar() met percentage weergave"
      contains: "${pA}% aanwezig"
  key_links:
    - from: "app.js buildMiniVerzuimBar()"
      to: "klasoverzicht tegel HTML output"
      via: "template literal return"
      pattern: "pA.*aanwezig"
---

<objective>
Vervang de "Xu ongeoorloofd" tekst in klasoverzicht-tegels door "${pA}% aanwezig".

Purpose: Mentors zien direct het aanwezigheidspercentage per leerling in het klasoverzicht, zodat zij in één oogopslag de algehele aanwezigheid beoordelen. De informatie is neutraal informatief — geen visuele waarschuwing op basis van drempelwaarden.

Output: buildMiniVerzuimBar() in app.js retourneert de percentage-tekst in plaats van de ongeoorloofd-uren tekst. De verzuimbalk zelf is ongewijzigd.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cios-huisstijl-verzuim-weergave/09-CONTEXT.md
@.planning/phases/09-cios-huisstijl-verzuim-weergave/09-UI-SPEC.md
@.planning/phases/09-cios-huisstijl-verzuim-weergave/09-PATTERNS.md

<interfaces>
<!-- Exacte huidige functie-inhoud (bevestigd via codebase read, lines 1256–1273) -->
<!-- Executor werkt uitsluitend met onderstaande code — geen codebase exploratie nodig. -->

Huidige buildMiniVerzuimBar() — app.js lines 1256–1273:
```javascript
function buildMiniVerzuimBar(student) {
  if (!student.verzuim) return `<span style="color:var(--text-faint);font-size:0.8rem;">—</span>`;
  const v      = student.verzuim;
  const totaal = (v.aanwezigheid || 0) + (v.geoorloofd || 0) + (v.ongeoorloofd || 0);
  if (!totaal) return `<span style="color:var(--text-faint);font-size:0.8rem;">—</span>`;
  const pA = Math.round((v.aanwezigheid || 0) / totaal * 100);
  const pG = Math.round((v.geoorloofd   || 0) / totaal * 100);
  const pO = 100 - pA - pG;
  const kleurTekst = v.ongeoorloofd > 600 ? '#991b1b' : '#6b7280';       // VERWIJDEREN
  const ongeoorloofdTekst = v.ongeoorloofd > 0                            // VERWIJDEREN
    ? `<div style="font-size:0.75rem;color:${kleurTekst};margin-top:3px;">${minNaarUren(v.ongeoorloofd)} ongeoorloofd</div>`
    : '';                                                                   // VERWIJDEREN
  return `<div class="mini-verzuim-bar">
    <div class="mvb-seg mvb-aanwezig"     style="width:${pA}%"></div>
    <div class="mvb-seg mvb-geoorloofd"   style="width:${pG}%"></div>
    <div class="mvb-seg mvb-ongeoorloofd" style="width:${pO}%"></div>
  </div>${ongeoorloofdTekst}`;                                              // WIJZIGEN
}
```

Doelfunctie na wijziging:
```javascript
function buildMiniVerzuimBar(student) {
  if (!student.verzuim) return `<span style="color:var(--text-faint);font-size:0.8rem;">—</span>`;
  const v      = student.verzuim;
  const totaal = (v.aanwezigheid || 0) + (v.geoorloofd || 0) + (v.ongeoorloofd || 0);
  if (!totaal) return `<span style="color:var(--text-faint);font-size:0.8rem;">—</span>`;
  const pA = Math.round((v.aanwezigheid || 0) / totaal * 100);
  const pG = Math.round((v.geoorloofd   || 0) / totaal * 100);
  const pO = 100 - pA - pG;
  return `<div class="mini-verzuim-bar">
    <div class="mvb-seg mvb-aanwezig"     style="width:${pA}%"></div>
    <div class="mvb-seg mvb-geoorloofd"   style="width:${pG}%"></div>
    <div class="mvb-seg mvb-ongeoorloofd" style="width:${pO}%"></div>
  </div><div style="font-size:0.75rem;color:var(--text-muted);margin-top:3px;">${pA}% aanwezig</div>`;
}
```

NIET aanraken:
- VERZUIM_DREMPEL_MIN constante (line ~1039) — lees-referentie, niet wijzigen
- mvb-aanwezig / mvb-geoorloofd / mvb-ongeoorloofd CSS classes en bar-structuur
- Early-return `—` spans (lines 1257, 1260)
- pG en pO variabelen — nog steeds nodig voor balk-breedte percentages
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vervang ongeoorloofd-tekst door aanwezigheidspercentage in buildMiniVerzuimBar()</name>
  <files>app.js</files>
  <read_first>
    - app.js lines 1256–1273 (volledig functielichaam — exact huidige code voor elke Edit)
    - app.js line ~1039 (VERZUIM_DREMPEL_MIN — lees-referentie om te bevestigen dat het niet gewijzigd wordt)
  </read_first>
  <action>
Voer exact de onderstaande wijziging uit in app.js. Gebruik één Edit tool-aanroep die de volledige functie-body vervangt.

**Verwijderen (lines 1264–1267):**
```javascript
  const kleurTekst = v.ongeoorloofd > 600 ? '#991b1b' : '#6b7280';
  const ongeoorloofdTekst = v.ongeoorloofd > 0
    ? `<div style="font-size:0.75rem;color:${kleurTekst};margin-top:3px;">${minNaarUren(v.ongeoorloofd)} ongeoorloofd</div>`
    : '';
```

**Wijzigen (line 1272) — return statement:**

Oud:
```javascript
  </div>${ongeoorloofdTekst}`;
```

Nieuw:
```javascript
  </div><div style="font-size:0.75rem;color:var(--text-muted);margin-top:3px;">${pA}% aanwezig</div>`;
```

**Resultaat — doelfunctie (lines 1256–1273 na wijziging):**
```javascript
function buildMiniVerzuimBar(student) {
  if (!student.verzuim) return `<span style="color:var(--text-faint);font-size:0.8rem;">—</span>`;
  const v      = student.verzuim;
  const totaal = (v.aanwezigheid || 0) + (v.geoorloofd || 0) + (v.ongeoorloofd || 0);
  if (!totaal) return `<span style="color:var(--text-faint);font-size:0.8rem;">—</span>`;
  const pA = Math.round((v.aanwezigheid || 0) / totaal * 100);
  const pG = Math.round((v.geoorloofd   || 0) / totaal * 100);
  const pO = 100 - pA - pG;
  return `<div class="mini-verzuim-bar">
    <div class="mvb-seg mvb-aanwezig"     style="width:${pA}%"></div>
    <div class="mvb-seg mvb-geoorloofd"   style="width:${pG}%"></div>
    <div class="mvb-seg mvb-ongeoorloofd" style="width:${pO}%"></div>
  </div><div style="font-size:0.75rem;color:var(--text-muted);margin-top:3px;">${pA}% aanwezig</div>`;
}
```

WAARSCHUWING: Pas UITSLUITEND buildMiniVerzuimBar() aan. Raak geen andere functie, constante of variabele aan.
  </action>
  <verify>
    <automated>
# Nieuwe percentage tekst aanwezig
grep -n "aanwezig" app.js | grep "pA"

# Verwijderde variabelen weg
grep -n "kleurTekst\|ongeoorloofdTekst" app.js

# Verzuimbalk segment classes ongewijzigd
grep -n "mvb-aanwezig\|mvb-geoorloofd\|mvb-ongeoorloofd" app.js

# Drempellogica ongewijzigd
grep -n "VERZUIM_DREMPEL_MIN" app.js
    </automated>
  </verify>
  <acceptance_criteria>
    - app.js bevat `${pA}% aanwezig` in buildMiniVerzuimBar()
    - app.js bevat `color:var(--text-muted)` in het nieuwe div-element (niet #991b1b of #6b7280 als conditie)
    - grep "kleurTekst" app.js geeft GEEN resultaten terug
    - grep "ongeoorloofdTekst" app.js geeft GEEN resultaten terug
    - grep "ongeoorloofd" app.js toont: de functienaam-definitie (buildMiniVerzuimBar), v.ongeoorloofd in totaal-berekening (line 1259), pO berekening, en mvb-ongeoorloofd class — NIET de oude kleurTekst conditie
    - grep "mvb-aanwezig" app.js toont de segment div (balk ongewijzigd)
    - grep "mvb-geoorloofd" app.js toont de segment div (balk ongewijzigd)
    - grep "mvb-ongeoorloofd" app.js toont de segment div (balk ongewijzigd)
    - grep "VERZUIM_DREMPEL_MIN" app.js toont de constante definitie (ongewijzigd)
  </acceptance_criteria>
  <done>buildMiniVerzuimBar() retourneert "${pA}% aanwezig" in neutrale --text-muted kleur onder de 3-delige verzuimbalk. kleurTekst en ongeoorloofdTekst variabelen zijn verwijderd. Verzuimbalk structuur, VERZUIM_DREMPEL_MIN en alle andere code zijn ongewijzigd.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student.verzuim object | Berekend uit geïmporteerde Excel — waarden zijn integers (minuten); pA berekening is veilig voor 0-waarden via early return |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | VERZUIM_DREMPEL_MIN constante | mitigate | Acceptance criteria verifieert dat de constante ongewijzigd aanwezig is |
| T-09-05 | Tampering | Verzuimbalk segment classes | mitigate | Acceptance criteria verifieert dat mvb-aanwezig/geoorloofd/ongeoorloofd classes intact zijn |
| T-09-06 | Information Disclosure | pA percentage bij student zonder verzuim | accept | Early return (lines 1257, 1260) geeft "—" terug — geen percentage getoond als data ontbreekt |
</threat_model>

<verification>
Na voltooiing:

```bash
# 1. Percentage tekst aanwezig
grep -n "pA.*aanwezig" app.js
# → verwacht: één regel met `${pA}% aanwezig` in buildMiniVerzuimBar()

# 2. Verwijderde logica weg
grep -n "kleurTekst\|ongeoorloofdTekst\|#991b1b" app.js
# → verwacht: geen output

# 3. Verzuimbalk ongewijzigd
grep -n "mvb-seg" app.js
# → verwacht: 3 regels — mvb-aanwezig, mvb-geoorloofd, mvb-ongeoorloofd — allemaal aanwezig

# 4. Drempellogica ongewijzigd
grep -n "VERZUIM_DREMPEL_MIN" app.js
# → verwacht: de constante definitie, ongewijzigd
```
</verification>

<success_criteria>
- app.js buildMiniVerzuimBar() geeft "${pA}% aanwezig" terug in var(--text-muted) kleur
- kleurTekst en ongeoorloofdTekst variabelen zijn volledig verwijderd
- 3-delige verzuimbalk structuur (mvb-aanwezig, mvb-geoorloofd, mvb-ongeoorloofd) is ongewijzigd
- VERZUIM_DREMPEL_MIN en alle drempellogica buiten buildMiniVerzuimBar() zijn ongewijzigd
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/09-cios-huisstijl-verzuim-weergave/09-02-SUMMARY.md`
</output>
