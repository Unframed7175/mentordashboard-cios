---
phase: "09"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - DES-01
  - DES-02
  - DES-03
  - DES-04

must_haves:
  truths:
    - "Knoppen, actieve tabs, en highlights tonen CIOS cyaan #00AEEF als accentkleur"
    - "De header/navigatiebalk is CIOS navy #003057 in zowel light als dark mode"
    - "Dark mode heeft expliciete --accent-blue* en --bg-header tokens (geen inheritance van :root)"
    - "Structurele koppen, tabelheaders en kaartnamen gebruiken font-weight 700"
    - "Geen hardcoded #3b82f6 restanten in index.html buiten de tokens"
  artifacts:
    - path: "index.html"
      provides: "CSS token definitie in :root en body.dark"
      contains: "--accent-blue: #00AEEF"
    - path: "index.html"
      provides: "CSS token definitie in :root en body.dark"
      contains: "--bg-header: #003057"
  key_links:
    - from: "index.html :root"
      to: "alle --accent-blue* consumenten (btn-primary, sort-btn.active, import-zone, progress-bar-fill)"
      via: "CSS var() propagation"
      pattern: "var\\(--accent-blue"
    - from: "index.html body.dark"
      to: "dark mode accent overrides"
      via: "CSS cascade"
      pattern: "body\\.dark \\{"
---

<objective>
Herschrijf alle CIOS kleur- en typografie-tokens in index.html.

Purpose: Het dashboard krijgt de CIOS Zuidwest huisstijl — cyaan accentkleur, navy header, bold typografie. Alle wijzigingen zijn puur CSS (token-waarden en font-weight declaraties); geen HTML-structuur of class-namen worden gewijzigd.

Output: index.html met bijgewerkte :root tokens, expliciete body.dark overrides, gerepareerde hardcoded hex anti-patronen, en gepromote font-weight: 700 op structurele elementen.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
@/home/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-cios-huisstijl-verzuim-weergave/09-CONTEXT.md
@.planning/phases/09-cios-huisstijl-verzuim-weergave/09-UI-SPEC.md
@.planning/phases/09-cios-huisstijl-verzuim-weergave/09-PATTERNS.md

<interfaces>
<!-- Huidige token-waarden in index.html (bevestigd via codebase read) -->
<!-- Executor werkt UITSLUITEND met de waarden hieronder — geen codebase exploratie nodig. -->

Uit index.html :root {} (lines 7–39):
```css
:root {
  --bg-page: #f5f5f7;
  --bg-surface: #ffffff;
  --bg-surface-alt: #f9fafb;
  --bg-header: #1a1a2e;          /* WIJZIGEN → #003057 */
  --text-primary: #111827;
  --text-secondary: #374151;
  --text-muted: #6b7280;
  --text-faint: #9ca3af;
  --border-default: #e5e7eb;
  --border-light: #f3f4f6;
  --accent-blue: #3b82f6;        /* WIJZIGEN → #00AEEF */
  --accent-blue-hover: #2563eb;  /* WIJZIGEN → #009AD6 */
  --accent-blue-light: #eff6ff;  /* WIJZIGEN → #E6F7FD */
  --accent-blue-border: #bfdbfe; /* WIJZIGEN → #80D7F7 */
  /* status tokens en spider tokens: NIET WIJZIGEN */
  --spider-prof-handelen-stroke: #22b8c8; /* NIET WIJZIGEN */
}
```

Uit index.html body.dark {} (lines 41–68):
```css
body.dark {
  /* Bevat GEEN --accent-blue* of --bg-header — erft van :root */
  --spider-prof-handelen-stroke: #67e8f9; /* NIET WIJZIGEN */
}
```

Hardcoded hex anti-patronen te repareren:
- Line 123: `.nav-count { background: #3b82f6; }` → `background: var(--accent-blue);`
- Line 277: `body.dark .sort-btn.active { border-color: #3b82f6; }` → `border-color: var(--accent-blue);`

font-weight: 600 → 700 te promoten (14 elementen):
- Line 276: `.sort-btn.active { font-weight: 600; }` → 700
- Line 300: `#klas-tabel th { font-weight: 600; }` → 700
- Line 318: `.student-naam { font-weight: 600; }` → 700
- Line 426: `.detail-section-title { font-weight: 600; }` → 700
- Line 477: `.dg-matrix th { font-weight: 600; }` → 700
- Line 565: `.vak-header { font-weight: 600; }` → 700
- Line 675: `.aanvullend-veld label { font-weight: 600; }` → 700
- Line 704: `#leerlijn-toewijzing h2 { font-weight: 600; }` → 700
- Line 707: `.lt-table th { font-weight: 600; }` → 700
- Line 771: `.klas-tile-naam { font-weight: 600; }` → 700
- Line 786: `#klassen-leeg h2 { font-weight: 600; }` → 700
- Line 907: `.dg-matrix-tijdlijn .groep-header { font-weight: 600; }` → 700
- Line 948: `.spider-leerlijn-title { font-weight: 600; }` → 700
- Line 965: `.ap-subsection-title { font-weight: 600; }` → 700
- Inline h2/h3 in app sections (lines ~1170, ~1182, ~1198, ~1205, ~1217, ~1308): `font-weight:600` → `font-weight:700`

font-weight: 600 NIET wijzigen (decoratieve badges/pills):
- Line 126: `.nav-count { font-weight: 600; }` — LATEN STAAN
- Line 330: `.status-badge { font-weight: 600; }` — LATEN STAAN
- Line 538: `.vote-badge { font-weight: 600; }` — LATEN STAAN
- Line 720: `.traject-tag { font-weight: 600; }` — LATEN STAAN
- Line 1020: `.ap-status-badge { font-weight: 600; }` — LATEN STAAN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS kleur-tokens in :root en body.dark</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 7–68 (volledig :root en body.dark blok — exact huidige token-waarden voor elke Edit)
  </read_first>
  <action>
Voer de volgende exacte wijzigingen uit in index.html. Gebruik het Edit tool voor elke wijziging afzonderlijk.

**Stap 1 — :root tokens (5 waarden):**

Vervang in het :root blok (lines 7–39):
- `--bg-header: #1a1a2e;` → `--bg-header: #003057;`
- `--accent-blue: #3b82f6;` → `--accent-blue: #00AEEF;`
- `--accent-blue-hover: #2563eb;` → `--accent-blue-hover: #009AD6;`
- `--accent-blue-light: #eff6ff;` → `--accent-blue-light: #E6F7FD;`
- `--accent-blue-border: #bfdbfe;` → `--accent-blue-border: #80D7F7;`

NIET wijzigen: --status-* tokens, --spider-* tokens (inclusief `--spider-prof-handelen-stroke: #22b8c8`).

**Stap 2 — body.dark overrides toevoegen:**

Voeg de volgende 5 tokens toe aan het body.dark blok, VOOR de afsluitende `}`. Voeg ze in na de laatste bestaande declaratie (`--spider-prof-handelen-stroke: #67e8f9;`):

```css
  /* CIOS huisstijl — Phase 9 */
  --accent-blue: #00AEEF;
  --accent-blue-hover: #009AD6;
  --accent-blue-light: #0A2C3D;
  --accent-blue-border: #005F8A;
  --bg-header: #003057;
```

NIET wijzigen: --bg-*, --text-*, --status-*, --spider-* tokens in het dark blok. Specifiek: `--spider-prof-handelen-stroke: #67e8f9` NIET aanraken.

**Stap 3 — hardcoded hex anti-patronen repareren:**

Lijn 123 — `.nav-count`:
- Huidig: `background: #3b82f6;`
- Nieuw: `background: var(--accent-blue);`

Lijn 277 — `body.dark .sort-btn.active`:
- Huidig: `body.dark .sort-btn.active { background: #1e3a5f; border-color: #3b82f6; color: #93c5fd; }`
- Nieuw: `body.dark .sort-btn.active { background: #1e3a5f; border-color: var(--accent-blue); color: #93c5fd; }`
  </action>
  <verify>
    <automated>
# Token waarden in :root
grep -n "accent-blue:" index.html | head -5
grep -n "bg-header:" index.html | head -3

# Dark mode overrides aanwezig
grep -n "CIOS huisstijl" index.html
grep -n "0A2C3D\|005F8A" index.html

# Hardcoded hex weg
grep -n "#3b82f6" index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - index.html bevat `--accent-blue: #00AEEF;` in :root (niet #3b82f6)
    - index.html bevat `--accent-blue-hover: #009AD6;` in :root
    - index.html bevat `--accent-blue-light: #E6F7FD;` in :root
    - index.html bevat `--accent-blue-border: #80D7F7;` in :root
    - index.html bevat `--bg-header: #003057;` in :root
    - index.html bevat `--accent-blue-light: #0A2C3D;` in body.dark blok
    - index.html bevat `--accent-blue-border: #005F8A;` in body.dark blok
    - grep -n "#3b82f6" index.html geeft GEEN resultaten terug (alle hardcoded hex gerepareerd)
    - index.html bevat `--spider-prof-handelen-stroke: #22b8c8;` (NIET gewijzigd)
    - index.html bevat `--spider-prof-handelen-stroke: #67e8f9;` in body.dark (NIET gewijzigd)
  </acceptance_criteria>
  <done>Alle 5 :root tokens bijgewerkt, 5 body.dark tokens toegevoegd, 2 hardcoded #3b82f6 vervangen door var(--accent-blue). Geen spider of status tokens gewijzigd.</done>
</task>

<task type="auto">
  <name>Task 2: Promoveer font-weight 600 → 700 op structurele elementen</name>
  <files>index.html</files>
  <read_first>
    - index.html (volledig bestand — exact huidige font-weight waarden per lijn voor elke Edit)
  </read_first>
  <action>
Promoveer `font-weight: 600` naar `font-weight: 700` op de volgende 14 CSS selectors en inline stijlen. Gebruik het Edit tool.

**CSS class declaraties (gebruik Edit voor elke wijziging):**

| Lijn | Selector | Wijziging |
|------|----------|-----------|
| 276 | `.sort-btn.active` | `font-weight: 600` → `font-weight: 700` |
| 300 | `#klas-tabel th` | `font-weight: 600` → `font-weight: 700` |
| 318 | `.student-naam` | `font-weight: 600` → `font-weight: 700` |
| 426 | `.detail-section-title` | `font-weight: 600` → `font-weight: 700` |
| 477 | `.dg-matrix th` | `font-weight: 600` → `font-weight: 700` |
| 565 | `.vak-header` | `font-weight: 600` → `font-weight: 700` |
| 675 | `.aanvullend-veld label` | `font-weight: 600` → `font-weight: 700` |
| 704 | `#leerlijn-toewijzing h2` | `font-weight: 600` → `font-weight: 700` |
| 707 | `.lt-table th` | `font-weight: 600` → `font-weight: 700` |
| 771 | `.klas-tile-naam` | `font-weight: 600` → `font-weight: 700` |
| 786 | `#klassen-leeg h2` | `font-weight: 600` → `font-weight: 700` |
| 907 | `.dg-matrix-tijdlijn .groep-header` | `font-weight: 600` → `font-weight: 700` |
| 948 | `.spider-leerlijn-title` | `font-weight: 600` → `font-weight: 700` |
| 965 | `.ap-subsection-title` | `font-weight: 600` → `font-weight: 700` |

**Inline stijlen in HTML (zoek naar `font-weight:600` in HTML-attributen):**

Gebruik grep om alle inline `font-weight:600` (zonder spatie) en `font-weight: 600` (met spatie) te vinden in HTML-elementen (niet in de `<style>` tag). Wijzig elk gevonden exemplaar naar `font-weight:700` of `font-weight: 700`.

Verwachte locaties (ter verificatie):
- ~Line 1170, 1182, 1198: `<h2 style="...font-weight:600...">` in import panels
- ~Line 1205, 1217: `<h3 style="...font-weight:600...">` in import panels
- ~Line 1308: `<h3 style="...font-weight:600...">` in klas dialog

**NIET wijzigen (decoratieve badges/pills — blijven op 600):**
- Lijn 126: `.nav-count { font-weight: 600; }` — LATEN STAAN
- Lijn 330: `.status-badge { font-weight: 600; }` — LATEN STAAN
- Lijn 538: `.vote-badge { font-weight: 600; }` — LATEN STAAN
- Lijn 720: `.traject-tag { font-weight: 600; }` — LATEN STAAN
- Lijn 1020: `.ap-status-badge { font-weight: 600; }` — LATEN STAAN
  </action>
  <verify>
    <automated>
# Alle structurele 600→700 promoties bevestigen
grep -n "font-weight: 600" index.html
grep -n "font-weight:600" index.html

# Decoratieve badges nog steeds op 600
grep -n "nav-count\|status-badge\|vote-badge\|traject-tag\|ap-status-badge" index.html | grep "600"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "font-weight: 600" index.html toont ALLEEN de 5 decoratieve badge-selectors: .nav-count, .status-badge, .vote-badge, .traject-tag, .ap-status-badge
    - grep "font-weight:600" index.html (inline, geen spatie) toont GEEN resultaten
    - index.html bevat `font-weight: 700` op .sort-btn.active (lijn ~276)
    - index.html bevat `font-weight: 700` op #klas-tabel th (lijn ~300)
    - index.html bevat `font-weight: 700` op .student-naam (lijn ~318)
    - index.html bevat `font-weight: 700` op .klas-tile-naam (lijn ~771)
    - #site-header h1 is nog steeds font-weight: 700 (was al correct — niet gewijzigd)
  </acceptance_criteria>
  <done>Alle 14 structurele elementen promoveren van font-weight 600 naar 700. De 5 decoratieve badge-selectors blijven op 600. Geen enkele inline font-weight:600 op een heading of structureel label resteert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token scope | :root tokens propageren naar alle elementen — foutieve waarde infecteert het hele visuele systeem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Spider chart stroke tokens | mitigate | Expliciete grep-check in acceptance_criteria dat #22b8c8 en #67e8f9 ongewijzigd zijn |
| T-09-02 | Tampering | Decoratieve badge font-weight | mitigate | Acceptance criteria verifieert dat .nav-count, .status-badge, .vote-badge, .traject-tag, .ap-status-badge nog op 600 staan |
| T-09-03 | Denial of Service | Foutieve CSS syntax | accept | Vanilla CSS — parser-fouten degraderen graceful; geen runtime crash mogelijk |
</threat_model>

<verification>
Na voltooiing van beide taken:

```bash
# 1. Nieuwe token-waarden aanwezig
grep "accent-blue: #00AEEF" index.html
grep "bg-header: #003057" index.html

# 2. Dark mode overrides aanwezig
grep "0A2C3D" index.html
grep "005F8A" index.html

# 3. Geen resterende hardcoded #3b82f6
grep "#3b82f6" index.html
# → verwacht: geen output

# 4. Spider tokens ongewijzigd
grep "spider-prof-handelen-stroke" index.html
# → verwacht: #22b8c8 (light) en #67e8f9 (dark) — beide aanwezig en ongewijzigd

# 5. font-weight audit — alleen badges op 600
grep "font-weight: 600" index.html
# → verwacht: alleen .nav-count, .status-badge, .vote-badge, .traject-tag, .ap-status-badge
```
</verification>

<success_criteria>
- index.html :root bevat --accent-blue: #00AEEF, --accent-blue-hover: #009AD6, --accent-blue-light: #E6F7FD, --accent-blue-border: #80D7F7, --bg-header: #003057
- index.html body.dark bevat expliciete overrides voor alle 5 CIOS tokens
- grep "#3b82f6" index.html geeft geen resultaten (alle hardcoded hex gerepareerd)
- grep "spider-prof-handelen-stroke" toont #22b8c8 (light) en #67e8f9 (dark) ongewijzigd
- grep "font-weight: 600" toont alleen 5 decoratieve badge-selectors
- Alle structurele koppen, tabelheaders, kaartnamen gebruiken font-weight: 700
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/09-cios-huisstijl-verzuim-weergave/09-01-SUMMARY.md`
</output>
