---
phase: 32-rn-status-tegels
plan: 01
title: R&N statusregel in LeerlingTegel (TDD)
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/LeerlingTegel.test.tsx
  - src/components/LeerlingTegel.tsx
autonomous: true
requirements:
  - TEGEL-03
  - TEGEL-04

must_haves:
  truths:
    - "Een tegel voor een leerling met rekenResultaat='2F' toont de tekst 'R 2F'"
    - "Een tegel voor een leerling met nederlandsResultaat='3F' toont de tekst 'N 3F'"
    - "Een tegel met zowel rekenResultaat als nederlandsResultaat toont 'R 2F · N 3F' als één rij"
    - "Een tegel zonder R&N data toont geen rnRow — er is geen lege ruimte"
    - "De rnRow gebruikt uitsluitend CSS-variabelen; grep op #[0-9a-fA-F] in LeerlingTegel.tsx geeft nul matches voor rnRow"
  artifacts:
    - path: "tests/LeerlingTegel.test.tsx"
      provides: "RED tests voor TEGEL-03 en TEGEL-04 (4 tests)"
    - path: "src/components/LeerlingTegel.tsx"
      provides: "rnRow render logic + uitgebreide StudentProps interface"
  key_links:
    - from: "src/components/LeerlingTegel.tsx"
      to: "StudentProps.rekenResultaat / .nederlandsResultaat"
      via: "inline interface uitbreiding"
      pattern: "rekenResultaat\\?.*string"
    - from: "tests/LeerlingTegel.test.tsx"
      to: "src/components/LeerlingTegel.tsx"
      via: "render() + screen.getByText()"
      pattern: "R 2F"
---

<objective>
Voeg een compacte R&N statusregel toe aan LeerlingTegel. De rij toont "R {score}" en/of "N {score}"
wanneer de betreffende velden op het student-object niet null/leeg zijn en is volledig verborgen als
beide velden afwezig zijn.

Purpose: TEGEL-03 en TEGEL-04 afsluiten — mentoren zien R&N-scores direct in het klasoverzicht
zonder de detailweergave te hoeven openen.

Output:
- tests/LeerlingTegel.test.tsx — 4 RED→GREEN tests
- src/components/LeerlingTegel.tsx — StudentProps uitgebreid + rnRow render
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Schrijf falende tests voor rnRow in tests/LeerlingTegel.test.tsx</name>
  <files>tests/LeerlingTegel.test.tsx</files>

  <read_first>
    - src/components/LeerlingTegel.tsx (volledig — begrijp de huidige StudentProps interface en render output)
    - tests/RekenenNederlandsSection.test.tsx (testpatroon: import na mock, makeStudent helper, describe-blokken)
    - src/index.css regels 539-549 (de bestaande .score-telling CSS klasse die we hergebruiken)
  </read_first>

  <behavior>
    Vier tests, gegroepeerd in describe('LeerlingTegel — rnRow'):

    Test 1 — toont "R 2F" wanneer rekenResultaat="2F", nederlandsResultaat afwezig:
      render(<LeerlingTegel student={{ naam: 'T', leerlingId: 'S1', rekenResultaat: '2F' }} status={grijsStatus} onClick={vi.fn()} />)
      expect(screen.getByText(/R 2F/)).toBeTruthy()

    Test 2 — toont "N 3F" wanneer nederlandsResultaat="3F", rekenResultaat afwezig:
      render(<LeerlingTegel student={{ naam: 'T', leerlingId: 'S1', nederlandsResultaat: '3F' }} status={grijsStatus} onClick={vi.fn()} />)
      expect(screen.getByText(/N 3F/)).toBeTruthy()

    Test 3 — toont "R 2F · N 3F" als beide aanwezig zijn:
      render(<LeerlingTegel student={{ naam: 'T', leerlingId: 'S1', rekenResultaat: '2F', nederlandsResultaat: '3F' }} status={grijsStatus} onClick={vi.fn()} />)
      expect(screen.getByText(/R 2F · N 3F/)).toBeTruthy()

    Test 4 — verbergt de rij volledig als beide velden null/afwezig zijn (TEGEL-04):
      render(<LeerlingTegel student={{ naam: 'T', leerlingId: 'S1' }} status={grijsStatus} onClick={vi.fn()} />)
      expect(screen.queryByText(/^R |^N |· N/)).toBeNull()

    Hulpobject voor alle tests:
      const grijsStatus = { kleur: 'grijs' as const, label: 'Onbekend', prognose: null }

    Geen mocks vereist — LeerlingTegel is een pure presentational component zonder
    imports van utils/klassen of andere stateful modules.

    Importpatroon (bovenaan het bestand, na vi/describe/it/expect import):
      import React from 'react'
      import { render, screen } from '@testing-library/react'
      import LeerlingTegel from '../src/components/LeerlingTegel'
  </behavior>

  <action>
    Maak tests/LeerlingTegel.test.tsx aan met de vier bovenstaande tests.
    Gebruik vi, describe, it, expect van 'vitest' en render/screen van '@testing-library/react'.
    Definieer grijsStatus als constante boven de describe-blokken.
    Voeg geen implementatiecode toe in dit bestand.
    Voer na aanmaken npm test -- --reporter=verbose uit om te bevestigen dat alle vier tests ROOD zijn
    (TypeError of "Unable to find an element" — beide zijn acceptabele RED-uitkomsten op dit moment).
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npm test -- --reporter=verbose 2>&1 | grep -E "FAIL|PASS|LeerlingTegel"</automated>
  </verify>

  <acceptance_criteria>
    - tests/LeerlingTegel.test.tsx bestaat
    - Bevat describe('LeerlingTegel — rnRow', ...) met exact 4 it()-blokken
    - grep -c "R 2F" tests/LeerlingTegel.test.tsx geeft minstens 2
    - grep -c "N 3F" tests/LeerlingTegel.test.tsx geeft minstens 2
    - npm test toont de 4 nieuwe tests als FAILING (RED) — de overige tests blijven groen
  </acceptance_criteria>

  <done>4 RED tests in tests/LeerlingTegel.test.tsx; alle overige tests nog groen.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Breid StudentProps uit en render rnRow in src/components/LeerlingTegel.tsx</name>
  <files>src/components/LeerlingTegel.tsx</files>

  <read_first>
    - src/components/LeerlingTegel.tsx (volledig — huidige StudentProps interface op regel 12-21, render op regel 93-107)
    - src/index.css regels 539-549 (.score-telling klasse — flex, 0.75rem, color: var(--text-muted))
    - tests/LeerlingTegel.test.tsx (de vier tests om te weten wat exact gerenderd moet worden)
  </read_first>

  <behavior>
    Wijziging 1 — Voeg twee optionele velden toe aan de bestaande StudentProps inline interface
    (regels 12-21 in LeerlingTegel.tsx). Voeg NA het `verzuim?` blok toe:
      rekenResultaat?: string | null
      nederlandsResultaat?: string | null
    De interface heet nog steeds StudentProps en staat nog steeds inline — geen import van een
    extern type (per IN-01: "Intentionally kept inline").

    Wijziging 2 — Bereken rnRow direct BOVEN de return-statement (na de scoreTelling berekening
    op regel 56-82). Logica:
      const rPart = student.rekenResultaat ? `R ${student.rekenResultaat}` : null
      const nPart = student.nederlandsResultaat ? `N ${student.nederlandsResultaat}` : null
      const parts = [rPart, nPart].filter(Boolean)
      const rnRow: React.ReactNode = parts.length > 0
        ? <div className="score-telling">{parts.join(' · ')}</div>
        : null

    Wijziging 3 — Render {rnRow} in de return JSX, direct NA {scoreTelling} en VOOR {miniBar}.
    De volledige render volgorde wordt dus:
      <span className="klas-tile-naam">...</span>
      <span className="status-badge ...">...</span>
      {scoreTelling}
      {rnRow}
      {miniBar}

    Geen hardcoded kleuren. className="score-telling" hergebruikt de bestaande CSS-klasse
    (Phase 26) — geen nieuwe CSS vereist.
    Geen wijzigingen aan KlasOverzicht.tsx: het component geeft reeds het volledige student-object
    `s` door als `student={s}` prop. Zodra StudentProps de nieuwe velden declareert, pikt
    TypeScript de waarden automatisch op.
  </behavior>

  <action>
    1. Open src/components/LeerlingTegel.tsx.
    2. Voeg rekenResultaat?: string | null en nederlandsResultaat?: string | null toe aan StudentProps
       (na het verzuim?: blok, vóór de sluitende accolade van de interface).
    3. Voeg de rnRow berekening toe na de scoreTelling berekening (na regel ~82, vóór handleKeyDown).
    4. Voeg {rnRow} toe in de return JSX na {scoreTelling}.
    5. Voer npm test uit — alle tests inclusief de 4 nieuwe moeten nu groen zijn.
    6. Voer grep -n "#[0-9a-fA-F]" src/components/LeerlingTegel.tsx uit — er mogen geen
       hardcoded hex-kleuren in de rnRow logica staan (bestaande kleuren in miniBar zijn OK).
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npm test 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "rekenResultaat" src/components/LeerlingTegel.tsx toont de interface-declaratie EN de rnRow berekening
    - grep -c "nederlandsResultaat" src/components/LeerlingTegel.tsx geeft minstens 2
    - grep -c "rnRow" src/components/LeerlingTegel.tsx geeft minstens 3 (declaratie + render in JSX)
    - npm test exitcode 0 — alle tests groen inclusief de 4 LeerlingTegel rnRow tests
    - grep -n "score-telling" src/components/LeerlingTegel.tsx toont className="score-telling" voor de rnRow div
    - Geen nieuwe hardcoded hex-kleuren in de rnRow logica: grep op "rnRow\|rekenResultaat\|nederlandsResultaat" gevolgd door grep op "#[0-9a-fA-F]" geeft geen matches in de nieuwe regels
  </acceptance_criteria>

  <done>
    npm test exitcode 0. Alle 4 rnRow tests groen.
    LeerlingTegel.tsx heeft StudentProps uitgebreid met de twee optionele velden.
    rnRow rendert "R {score}", "N {score}" of "R {score} · N {score}" met .score-telling CSS.
    Geen hardcoded kleuren toegevoegd.
  </done>
</task>

<task type="auto">
  <name>Task 3 (VERIFY): Bevestig acceptance criteria en TypeScript typecheck</name>
  <files></files>

  <read_first>
    - src/components/LeerlingTegel.tsx (controleer de eindtoestand)
    - src/components/KlasOverzicht.tsx regels 210-224 (bevestig dat student={s} prop ongewijzigd is)
  </read_first>

  <action>
    Voer de volgende commando's uit en bevestig dat elk slaagt:

    1. npm test — volledige testsuite groen (alle vorige tests + 4 nieuwe LeerlingTegel tests)
    2. npm run typecheck 2>&1 | tail -10 — nul TypeScript fouten (of gebruik het bestaande
       typecheck-commando in package.json; als er geen is, gebruik dan npx tsc --noEmit)
    3. grep -n "rekenResultaat\|nederlandsResultaat" src/components/LeerlingTegel.tsx — bevestig
       aanwezigheid in zowel StudentProps interface (boven) als rnRow logica (midden)
    4. grep -c "score-telling" src/components/LeerlingTegel.tsx — geeft minstens 2
       (één voor scoreTelling in Phase 26, één voor rnRow)
    5. grep -rn "#[0-9a-fA-F]" src/components/LeerlingTegel.tsx | grep -v "rag-grijs\|rag-" — geeft
       nul nieuwe hardcoded hex-matches (bestaande RAG var()-references zijn OK)

    Als een commando faalt: diagnosticeer en repareer vóór het aanmaken van de SUMMARY.
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npm test 2>&1 | grep -E "passed|failed"</automated>
  </verify>

  <acceptance_criteria>
    - npm test exitcode 0, alle LeerlingTegel rnRow tests in de output zichtbaar als groen
    - TypeScript typecheck exitcode 0 (nul errors)
    - StudentProps in LeerlingTegel.tsx declareert rekenResultaat?: string | null EN nederlandsResultaat?: string | null
    - rnRow logica verbergt de rij volledig wanneer beide velden null/undefined/leeg zijn (TEGEL-04)
    - rnRow gebruikt className="score-telling" — geen inline color of andere hardcoded waarden
    - KlasOverzicht.tsx is niet gewijzigd (student={s} pass-through was al correct)
  </acceptance_criteria>

  <done>
    Volledige testsuite groen. TypeScript clean. TEGEL-03 en TEGEL-04 aantoonbaar geleverd via
    automatische tests en typecheck. SUMMARY aangemaakt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student prop → rnRow render | rekenResultaat/nederlandsResultaat zijn strings afkomstig uit geïmporteerde PDF-data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | StudentProps.rekenResultaat | accept | Weergave-only; geen mutatie in deze phase. Waarden worden al gesaniteerd bij import in Phase 23 (normalizeRekenScore). |
| T-32-SC | Tampering | npm installs | accept | Geen nieuwe npm packages in deze phase — nul install-stappen. |
</threat_model>

<verification>
Na uitvoering:
- npm test exitcode 0 — alle tests inclusief 4 nieuwe LeerlingTegel rnRow tests groen
- TypeScript typecheck exitcode 0
- Tegel met rekenResultaat="2F" toont "R 2F" in de DOM (test 1)
- Tegel met nederlandsResultaat="3F" toont "N 3F" in de DOM (test 2)
- Tegel met beide velden toont "R 2F · N 3F" (test 3)
- Tegel zonder R&N data toont geen rnRow element (test 4 / TEGEL-04)
- grep op hardcoded hex in nieuwe LeerlingTegel.tsx regels geeft nul resultaten
</verification>

<success_criteria>
1. TEGEL-03: Een tegel voor een leerling met R&N-data toont compacte statusregel "R {score}" / "N {score}" / "R {score} · N {score}"
2. TEGEL-04: Een tegel zonder R&N-data toont geen statusregel — geen lege ruimte
3. Alle bestaande tests (210+) blijven groen — nul regressies
4. TypeScript typecheck clean — StudentProps correct uitgebreid zonder breaking changes
5. CSS-variabelen only — geen hardcoded kleuren in rnRow
</success_criteria>

<output>
Maak .planning/phases/32-rn-status-tegels/32-01-SUMMARY.md aan wanneer klaar.
</output>
