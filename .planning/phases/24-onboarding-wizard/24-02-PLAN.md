---
phase: 24-onboarding-wizard
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/App.tsx
autonomous: true
requirements:
  - ONB-01
  - ONB-07
  - ONB-08

must_haves:
  truths:
    - "App.tsx view union type includes 'onboarding'"
    - "Lazy initializer sets view to 'onboarding' when no klassen exist (ONB-01, ONB-08)"
    - "Lazy initializer sets view to 'import' when klassen already exist (ONB-08)"
    - "handleOnboardingComplete calls switchActiveKlas then setView('klas') (ONB-07)"
    - "OnboardingWizard renders as full-screen overlay inside App return block"
    - "All existing tests pass — npm test exits 0"
  artifacts:
    - path: "src/App.tsx"
      provides: "Wired onboarding flow into App view state machine"
      contains: "onboarding"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/OnboardingWizard.tsx"
      via: "import + {view === 'onboarding' && <OnboardingWizard .../>}"
      pattern: "OnboardingWizard"
    - from: "lazy initializer"
      to: "klassenState.klassen"
      via: "Object.keys(klassenState.klassen).length === 0"
      pattern: "klassenState\\.klassen"
---

<objective>
Wire OnboardingWizard into App.tsx: extend view union type, add lazy initializer for first-run detection, add handleOnboardingComplete handler, render wizard in JSX. Then run all existing tests and commit.

Purpose: Makes the wizard appear automatically for mentors with no classes and skip automatically for returning mentors (ONB-01, ONB-07, ONB-08).
Output: Updated src/App.tsx; all existing tests passing; git commit.
</objective>

<execution_context>
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\phases\24-onboarding-wizard\24-02-PLAN.md
</execution_context>

<context>
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\ROADMAP.md
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\STATE.md

<interfaces>
<!-- Current App.tsx structure (read before editing) -->
Line 12:  const [view, setView] = useState<'import' | 'klas' | 'detail' | 'settings'>('import');
Line 14:  const [refreshKey, setRefreshKey] = useState(0);
Line 89:  async function handleKlasCreated(klasId: string) { ... }  // insert handleOnboardingComplete after this
Line 96:  return (                                                    // JSX return block starts here
Line 99:  <KlasTabStrip .../>
Line 143: {view === 'settings' && (...)}                             // add wizard block after this, before </>
Line 144: </>                                                        // closing fragment

From utils/klassen (already imported in App.tsx line 8):
  klassenState: { klassen: Record<string, Klas>; activeKlasId: string | null }
  switchActiveKlas(id: string): Promise<void>

Note: loadKlassen() is called in main.tsx BEFORE ReactDOM.createRoot, so klassenState.klassen
is fully populated at App mount time. The lazy initializer runs synchronously — no async needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire OnboardingWizard into App.tsx</name>
  <files>src/App.tsx</files>
  <action>
Read the full src/App.tsx first, then apply four targeted edits. Do not rewrite the file.

EDIT 1 — Add import. Insert after the existing SettingsPage import (line 7):
  import OnboardingWizard from './components/OnboardingWizard';

EDIT 2 — Extend view union type and add lazy initializer. Find (line 12):
  const [view, setView] = useState<'import' | 'klas' | 'detail' | 'settings'>('import');
Replace with:
  const [view, setView] = useState<'import' | 'klas' | 'detail' | 'settings' | 'onboarding'>(
    () => Object.keys(klassenState.klassen).length === 0 ? 'onboarding' : 'import'
  );
Rationale: lazy initializer runs once at mount; klassenState is already populated because
loadKlassen() runs in main.tsx before ReactDOM.createRoot — no async needed.

EDIT 3 — Add handleOnboardingComplete. Insert this function after the closing brace of
handleKlasCreated (currently ending around line 94), before the return statement:
  async function handleOnboardingComplete(klasId: string) {
    await switchActiveKlas(klasId);
    setRefreshKey(k => k + 1);
    setView('klas');
  }

EDIT 4 — Render wizard in JSX. Inside the return block, add BEFORE the closing </> fragment
(currently line 144), after the settings block (line 143):
  {view === 'onboarding' && (
    <OnboardingWizard onComplete={handleOnboardingComplete} />
  )}

The wizard uses position: fixed with z-index: 100 (defined in index.css), so it covers
KlasTabStrip without needing to conditionally hide it.
  </action>
  <verify>
    <automated>Select-String -Path "D:\Downloads\get-shit-done-main\dashboard-2\src\App.tsx" -Pattern "onboarding" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>App.tsx contains 'onboarding' view in union type, lazy initializer using klassenState.klassen.length === 0, handleOnboardingComplete function, and OnboardingWizard JSX render block. TypeScript compiles without errors (npx tsc --noEmit from project root).</done>
</task>

<task type="auto">
  <name>Task 2: Run tests and commit</name>
  <files>src/App.tsx</files>
  <action>
From the project root (D:\Downloads\get-shit-done-main\dashboard-2):

1. Run the test suite:
   npm test
   All tests must pass. The OnboardingWizard component is not unit-tested because it uses
   async Tauri APIs (createKlas, switchActiveKlas, saveKlassen, parseSinglePDF, etc.) that
   require the native Tauri bridge. The existing 132+ tests serve as regression gate.
   If any test fails, investigate before committing — do not commit on red.

2. Stage and commit:
   git add src/components/OnboardingWizard.tsx src/index.css src/App.tsx
   git commit -m "feat(onboarding): add 5-step onboarding wizard for first-run setup (ONB-01..08)"

Commit covers all three files modified across plans 24-01 and 24-02.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>npm test exits 0 with all existing tests passing. Git commit exists with message containing 'ONB-01..08'. Git log --oneline -1 shows the commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| klassenState at mount | Read-only check of in-memory state populated by loadKlassen() in main.tsx before React renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Tampering | lazy initializer | accept | klassenState is populated from local Tauri storage before React renders; no external input crosses this boundary. |
| T-24-04 | Elevation of Privilege | view state machine | accept | Adding 'onboarding' to view union does not expand API surface; all Tauri commands gated by existing Rust permissions. |
</threat_model>

<verification>
- App.tsx view union: 'import' | 'klas' | 'detail' | 'settings' | 'onboarding'
- Lazy initializer: empty klassen → 'onboarding'; existing klassen → 'import' (ONB-08)
- handleOnboardingComplete: calls switchActiveKlas(klasId), setRefreshKey(k => k+1), setView('klas') (ONB-07)
- OnboardingWizard rendered in JSX when view === 'onboarding'
- npm test exits 0
</verification>

<success_criteria>
- App.tsx contains all four edits with no TypeScript errors
- First-run mentor (no classes) sees the wizard on app start (ONB-01, ONB-08)
- Returning mentor (classes exist) skips the wizard and sees import view (ONB-08)
- Completing the wizard calls handleOnboardingComplete which sets view to 'klas' (ONB-07)
- All existing tests pass — npm test exits 0
- Git commit message contains ONB-01..08
</success_criteria>

<output>
After completion, create .planning/phases/24-onboarding-wizard/24-02-SUMMARY.md
</output>
