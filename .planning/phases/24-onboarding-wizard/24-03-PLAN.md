---
phase: 24-onboarding-wizard
plan: 03
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
files_modified:
  - src/components/OnboardingWizard.tsx
  - src/App.tsx
autonomous: true
gap_closure: true
requirements:
  - ONB-06

must_haves:
  truths:
    - "Stap 5 toont twee number-inputs voor geoorloofd en ongeoorloofd drempelwaarden (in uren)"
    - "Stap 5 heeft een 'Overslaan' knop die naar stap 6 gaat zonder op te slaan"
    - "Stap 5 heeft een 'Opslaan & Volgende' knop die saveVerzuimDrempels aanroept en naar stap 6 gaat"
    - "Stap 6 is het afsluitscherm (🎉) met 'Naar het dashboard' knop"
    - "De 'Naar het dashboard' knop heeft een null-guard op klasId voor aanroep van onComplete"
    - "Vanaf stap 2 is een 'Afbreken' knop zichtbaar die de wizard verlaat"
    - "App.tsx lazy init controleert op aanwezigheid van studenten, niet louter op klasbestaan"
  artifacts:
    - path: "src/components/OnboardingWizard.tsx"
      provides: "6-stap wizard met ONB-06 instellingenstap, klasId guard en abort flow"
      exports: ["default OnboardingWizard"]
    - path: "src/App.tsx"
      provides: "Ghost-class-veilige lazy init en onAbort handler"
  key_links:
    - from: "src/components/OnboardingWizard.tsx"
      to: "utils/verzuimDrempels"
      via: "loadVerzuimDrempels, saveVerzuimDrempels, DEFAULT_VERZUIM_DREMPELS"
      pattern: "loadVerzuimDrempels|saveVerzuimDrempels"
    - from: "src/App.tsx"
      to: "src/components/OnboardingWizard"
      via: "onAbort prop — setView('import')"
      pattern: "onAbort"
---

<objective>
Sluit vier review-gaps (24-REVIEWS.md) die na uitvoering van plan 01 en 02 zijn geïdentificeerd.

GAP 1 (ONB-06 — BLOCKING): Voeg stap 5 toe met drempelwaardeninstellingen; schuif het afrondscherm op naar stap 6.
GAP 2 (ghost class): Vervang de lazy-init guard in App.tsx door een students-check.
GAP 3 (klasId!): Vervang de non-null assertion door een expliciete null-guard.
GAP 4 (abort flow): Voeg een 'Afbreken' knop toe zichtbaar vanaf stap 2.

Purpose: Na deze gap-closure voldoet fase 24 volledig aan ONB-06 en sluiten de overige drie risico's die beide reviewers als MEDIUM of hoger hebben beoordeeld.
Output: Gewijzigde src/components/OnboardingWizard.tsx (6 stappen) en src/App.tsx (ghost-class fix + onAbort).
</objective>

<execution_context>
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\phases\24-onboarding-wizard\24-03-PLAN.md
</execution_context>

<context>
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\ROADMAP.md
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\STATE.md
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\phases\24-onboarding-wizard\24-REVIEWS.md

<interfaces>
<!-- Relevante exports uit bestaande utilities -->

From utils/verzuimDrempels.ts:
  export interface VerzuimDrempels { geoorloofd: number; ongeoorloofd: number; }
  export const DEFAULT_VERZUIM_DREMPELS: VerzuimDrempels = { geoorloofd: 900, ongeoorloofd: 600 };
  export async function loadVerzuimDrempels(): Promise<VerzuimDrempels>
  export async function saveVerzuimDrempels(drempels: VerzuimDrempels): Promise<boolean>

  Import path vanuit src/components/OnboardingWizard.tsx:
    import { loadVerzuimDrempels, saveVerzuimDrempels, DEFAULT_VERZUIM_DREMPELS } from '../../utils/verzuimDrempels';

From utils/klassen.ts (reeds geïmporteerd in OnboardingWizard.tsx):
  klassenState: { klassen: Record<string, Klas>; activeKlasId: string | null }
  Klas bevat: students: any[] (bevestigd via utils/klassen.ts regel 72)
  Gebruik in App.tsx: Object.values(klassenState.klassen).some((k: any) => k.students?.length > 0)

Huidige OnboardingWizardProps (src/components/OnboardingWizard.tsx regel 8-10):
  interface OnboardingWizardProps {
    onComplete: (klasId: string) => void;
  }

  Uitbreiden met optionele onAbort:
  interface OnboardingWizardProps {
    onComplete: (klasId: string) => void;
    onAbort?: () => void;
  }

App.tsx regel 14 — lazy init te vervangen:
  () => Object.keys(klassenState.klassen).length === 0 ? 'onboarding' : 'import'
  wordt:
  () => Object.values(klassenState.klassen).some((k: any) => k.students?.length > 0) ? 'import' : 'onboarding'

App.tsx regel 154 — OnboardingWizard JSX te vervangen:
  <OnboardingWizard onComplete={handleOnboardingComplete} />
  wordt:
  <OnboardingWizard onComplete={handleOnboardingComplete} onAbort={() => setView('import')} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Breid OnboardingWizard.tsx uit naar 6 stappen (GAP 1, 3, 4)</name>
  <files>src/components/OnboardingWizard.tsx</files>
  <read_first>
    - src/components/OnboardingWizard.tsx (lees volledig voor de huidige structuur)
    - utils/verzuimDrempels.ts (bevestig import-namen en signatures)
  </read_first>
  <action>
Pas src/components/OnboardingWizard.tsx aan op de onderstaande punten. Lees het bestand eerst volledig.

IMPORTS — voeg toe na regel 6 (na de bpv-import):
  import { useEffect } from 'react';
  import { loadVerzuimDrempels, saveVerzuimDrempels, DEFAULT_VERZUIM_DREMPELS } from '../../utils/verzuimDrempels';

  Wijzig ook de React-import op regel 1 zodat useEffect erin zit, of voeg de losse import toe als named import. Huidige import is: import React, { useState, useRef } from 'react'; — vervang door: import React, { useState, useRef, useEffect } from 'react';

PROPS INTERFACE (regel 8-10) — voeg onAbort toe:
  interface OnboardingWizardProps {
    onComplete: (klasId: string) => void;
    onAbort?: () => void;
  }

DESTRUCTURING (regel 28) — voeg onAbort toe naast onComplete:
  export default function OnboardingWizard({ onComplete, onAbort }: OnboardingWizardProps)

STATE — voeg twee extra state-variabelen toe direct na de bestaande state-declaraties (na stepErr):
  const [geoorloofdHours, setGeoorloofdHours] = useState<number>(() => Math.round(DEFAULT_VERZUIM_DREMPELS.geoorloofd / 60));
  const [ongeoorloofdHours, setOngeoorloofdHours] = useState<number>(() => Math.round(DEFAULT_VERZUIM_DREMPELS.ongeoorloofd / 60));

STEP TYPE — verbreed het step-type en de nextStep-functie:
  Vervang: const [step, setStep] = useState<1 | 2 | 3 | 4 | 5>(1);
  door:     const [step, setStep] = useState<1 | 2 | 3 | 4 | 5 | 6>(1);

  Vervang in nextStep(): Math.min(5, s + 1) door Math.min(6, s + 1), pas het cast-type aan naar 1|2|3|4|5|6.

STEP_TITLES — voeg stap 5 en 6 toe, hernoemd stap 5 naar instellingen:
  5: 'Instellingen (optioneel)'
  6: 'Klaar om te beginnen'
  (Verwijder de oude stap 5 titel 'Klaar om te beginnen' of overschrijf index 5.)

STEP_SUBS — idem:
  5: 'Stel drempelwaarden in voor verzuimwaarschuwingen. Je kunt dit overslaan — de standaardwaarden worden dan gebruikt.'
  6: 'Je klas is aangemaakt en je gegevens zijn geladen. Je kunt nu aan de slag!'

PROGRESS INDICATOR — vervang "Stap {step} van 5" door "Stap {step} van 6" en genereer 6 balkjes in de map (vervang het array [1,2,3,4,5] door [1,2,3,4,5,6]).

USE EFFECT — laad opgeslagen drempels bij mount, voeg toe na de ref-declaraties:
  useEffect(() => {
    loadVerzuimDrempels().then(d => {
      setGeoorloofdHours(Math.round(d.geoorloofd / 60));
      setOngeoorloofdHours(Math.round(d.ongeoorloofd / 60));
    }).catch(() => { /* gebruik defaults */ });
  }, []);

HANDLER handleSaveDrempels — voeg toe als async functie na handleBpvExcel:
  Roept saveVerzuimDrempels({ geoorloofd: geoorloofdHours * 60, ongeoorloofd: ongeoorloofdHours * 60 }) aan.
  Na await: roept nextStep() aan.
  Wikkelt in setProcessing(true)/finally setProcessing(false) en clearFeedback().

STEP 5 CONTENT (GAP 1 — ONB-06) — vervang het huidige step === 5 blok (de 🎉 div) door een instellingen-formulier:
  Toon twee labeled number-inputs in een form-achtig div (geen form-element nodig):
    - Label "Geoorloofd verzuim (uren)" + input type="number" min="0" max="200" value={geoorloofdHours} onChange={e => setGeoorloofdHours(Number(e.target.value))}
    - Label "Ongeoorloofd verzuim (uren)" + input type="number" min="0" max="200" value={ongeoorloofdHours} onChange={e => setOngeoorloofdHours(Number(e.target.value))}
  Gebruik dezelfde inline input-stijl als de klasnaam-input in stap 1 (breedte 100%, border var(--border-default), etc.) maar max-width van 160px voor de number-inputs.
  Voeg een kleine subtekst toe onder elk veld: "Standaard: {Math.round(DEFAULT_VERZUIM_DREMPELS.geoorloofd / 60)} uur" resp. "Standaard: {Math.round(DEFAULT_VERZUIM_DREMPELS.ongeoorloofd / 60)} uur".

STEP 6 CONTENT (verschoven afrondscherm) — voeg nieuw blok toe:
  {step === 6 && (
    <div style={{ textAlign: 'center', padding: '1rem 0' }}>
      <div style={{ fontSize: '3rem', marginBottom: '0.75rem' }}>🎉</div>
      <p style={{ fontSize: '0.95rem' }}>
        Klas <strong>{klasNaamInput.trim()}</strong> is aangemaakt en je gegevens zijn geladen.
        Je kunt nu aan de slag!
      </p>
    </div>
  )}

ACTION BUTTONS — voeg stap 5 en 6 toe, verwijder de oude stap 5-knop:
  - Stap 5 (instellingen): twee knoppen — 'Overslaan' (detail-nav-btn, onClick=nextStep, disabled=processing) + 'Opslaan & Volgende →' (btn-primary-accent, onClick=handleSaveDrempels, disabled=processing).
  - Stap 6 (afrondscherm, GAP 3): vervang de oude onComplete(klasId!) aanroep door een guard:
      onClick={() => {
        if (!klasId) { setStepErr('Interne fout: klas ID ontbreekt. Herstart de wizard.'); return; }
        onComplete(klasId);
      }}
    Label: 'Naar het dashboard →'.

ABORT KNOP (GAP 4) — voeg toe bovenaan de action-buttons div, alleen zichtbaar voor stap 2 t/m 5 (NIET stap 1, niet stap 6):
  {step >= 2 && step <= 5 && (
    <button
      style={{ marginRight: 'auto', background: 'none', border: 'none', color: 'var(--text-muted)', fontSize: '0.8rem', cursor: 'pointer', padding: '0.25rem 0' }}
      onClick={() => { klasId ? onComplete(klasId) : onAbort?.(); }}
      disabled={processing}
    >
      Afbreken
    </button>
  )}
  Zet deze button LINKS uitgelijnd door marginRight: 'auto' (de rest van de buttons staan rechts via justifyContent: 'flex-end' op de flex-container). Voeg de Afbreken-button als EERSTE child van de flex-container in zodat de marginRight: 'auto' hem naar links duwt.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project D:\Downloads\get-shit-done-main\dashboard-2\tsconfig.json 2>&1 | Select-String "OnboardingWizard" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - grep "loadVerzuimDrempels" src/components/OnboardingWizard.tsx geeft minimaal 2 hits (import + useEffect aanroep)
    - grep "saveVerzuimDrempels" src/components/OnboardingWizard.tsx geeft minimaal 1 hit (handleSaveDrempels)
    - grep "step === 5" src/components/OnboardingWizard.tsx geeft een instellingen-blok terug (NIET de 🎉 emoji)
    - grep "step === 6" src/components/OnboardingWizard.tsx geeft het 🎉 afrondscherm terug
    - grep "Afbreken" src/components/OnboardingWizard.tsx geeft minimaal 1 hit
    - grep "onAbort" src/components/OnboardingWizard.tsx geeft minimaal 2 hits (prop + aanroep)
    - grep "if (!klasId)" src/components/OnboardingWizard.tsx geeft minimaal 1 hit (GAP 3 guard)
    - grep "van 6" src/components/OnboardingWizard.tsx geeft 1 hit (progress indicator bijgewerkt)
    - npx tsc --noEmit geeft geen fouten gerelateerd aan OnboardingWizard.tsx
  </acceptance_criteria>
  <done>OnboardingWizard.tsx heeft 6 stappen. Stap 5 toont drempelwaarden-inputs met Overslaan + Opslaan-knoppen (ONB-06). Stap 6 is het 🎉 afrondscherm met null-guard op klasId. Afbreken-knop zichtbaar op stappen 2-5. TypeScript compileert zonder fouten.</done>
</task>

<task type="auto">
  <name>Task 2: Herstel ghost-class lazy init en onAbort prop in App.tsx (GAP 2)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (lees volledig voor de huidige structuur)
  </read_first>
  <action>
Pas src/App.tsx aan op twee punten. Lees het bestand eerst volledig.

WIJZIGING 1 — lazy init (regel 14):
  Vervang:
    () => Object.keys(klassenState.klassen).length === 0 ? 'onboarding' : 'import'
  door:
    () => Object.values(klassenState.klassen).some((k: any) => k.students?.length > 0) ? 'import' : 'onboarding'

  Toelichting: controleert op aanwezige studenten in plaats van louter klasbestaan. Voorkomt dat een gebruiker die na stap 1 is gestopt bij de volgende start het dashboard ziet zonder data (ghost class).

WIJZIGING 2 — OnboardingWizard JSX (rond regel 154):
  Vervang:
    <OnboardingWizard onComplete={handleOnboardingComplete} />
  door:
    <OnboardingWizard onComplete={handleOnboardingComplete} onAbort={() => setView('import')} />

  De onAbort callback navigeert naar 'import' zodat de gebruiker het dashboard toch kan openen als ze de wizard afbreken zonder klassen of studenten.

Geen andere wijzigingen in App.tsx.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project D:\Downloads\get-shit-done-main\dashboard-2\tsconfig.json 2>&1 | Measure-Object -Line | Select-Object -ExpandProperty Lines</automated>
  </verify>
  <acceptance_criteria>
    - grep "students?.length" src/App.tsx geeft minimaal 1 hit (nieuwe guard)
    - grep "Object.keys(klassenState.klassen).length" src/App.tsx geeft 0 hits (oude guard verwijderd)
    - grep "onAbort" src/App.tsx geeft minimaal 1 hit (prop doorgegeven)
    - grep "setView\('import'\)" src/App.tsx geeft minimaal 2 hits (import-navigatie aanwezig inclusief onAbort callback)
    - npx tsc --noEmit geeft geen fouten gerelateerd aan App.tsx
  </acceptance_criteria>
  <done>App.tsx lazy init controleert op students in plaats van klassen (ghost-class fix). OnboardingWizard krijgt onAbort={() => setView('import')} prop mee. TypeScript compileert zonder fouten.</done>
</task>

<task type="auto">
  <name>Task 3: Verifieer en commit gap-closure wijzigingen</name>
  <files></files>
  <read_first>
    - Geen bestanden — alleen CLI-commando's uitvoeren
  </read_first>
  <action>
Voer de volgende verificaties uit in volgorde:

1. TypeScript compileer-check (geen build nodig, alleen type-controle):
   npx tsc --noEmit --project D:\Downloads\get-shit-done-main\dashboard-2\tsconfig.json
   Verwacht: geen uitvoer (exit 0). Bij fouten: herstel eerst voor je verdergaat.

2. Grep-gates op OnboardingWizard.tsx (gebruik PowerShell Select-String):
   - "loadVerzuimDrempels" — minimaal 2 overeenkomsten
   - "saveVerzuimDrempels" — minimaal 1 overeenkomst
   - "step === 6" — minimaal 1 overeenkomst
   - "Afbreken" — minimaal 1 overeenkomst
   - "if (!klasId)" — minimaal 1 overeenkomst
   - "van 6" — minimaal 1 overeenkomst

3. Grep-gates op App.tsx:
   - "students?.length" — minimaal 1 overeenkomst
   - "onAbort" — minimaal 1 overeenkomst

4. Bestaande tests draaien:
   cd D:\Downloads\get-shit-done-main\dashboard-2 && npx vitest run --reporter=verbose 2>&1 | tail -20
   Verwacht: alle bestaande tests slagen. De wizard heeft geen eigen unit tests (UI-component); verificatie is via tsc + grep.

5. Commit als alle controles slagen:
   git add src/components/OnboardingWizard.tsx src/App.tsx
   git commit -m "feat(24-03): ONB-06 settings step, ghost-class fix, klasId guard, abort flow (GAP 1-4)"
  </action>
  <verify>
    <automated>npx tsc --noEmit --project D:\Downloads\get-shit-done-main\dashboard-2\tsconfig.json; echo "TSC exit: $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit geeft exit 0 (geen TypeScript-fouten)
    - Alle vier grep-gates voor OnboardingWizard.tsx slagen
    - Beide grep-gates voor App.tsx slagen
    - npx vitest run toont geen regressies in bestaande tests
    - git log toont een nieuwe commit met boodschap inclusief "ONB-06"
  </acceptance_criteria>
  <done>TypeScript compileert foutloos. Alle grep-gates slagen. Bestaande tests slagen. Wijzigingen gecommit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> file input | User-supplied PDF en Excel bestanden worden lokaal geparsed (ongewijzigd t.o.v. plan 01) |
| wizard state -> plugin-store | Drempelwaarden worden via saveVerzuimDrempels naar Tauri plugin-store geschreven |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | parseSinglePDF / parseExcelFile | accept | Inherited from plan 01 — bestanden worden lokaal in het Tauri-proces geparsed; geen netwerkblootstelling. Mislukte bestanden worden per bestand gevangen en overgeslagen. |
| T-24-02 | Denial of Service | handlePDFs loop | accept | Inherited from plan 01 — sequentiële verwerking; processing-flag voorkomt her-entry. |
| T-24-03 | Tampering | saveVerzuimDrempels input | accept | number-inputs zijn type="number" met min/max attribuut. Waarden worden als integer vermenigvuldigd met 60 voor opslag. Geen server-side exposure; lokale desktop-app. |
| T-24-04 | Elevation of Privilege | onAbort callback | accept | onAbort navigeert naar 'import' view — geen privilige-escalatie mogelijk. klasId-guard voorkomt dat onComplete wordt aangeroepen met null. |
</threat_model>

<verification>
Voer de volgende checks uit na uitvoering van alle drie taken:

1. TypeScript:
   npx tsc --noEmit --project D:\Downloads\get-shit-done-main\dashboard-2\tsconfig.json
   Verwacht: exit 0, geen uitvoer.

2. ONB-06 content aanwezig:
   Select-String -Path "src\components\OnboardingWizard.tsx" -Pattern "loadVerzuimDrempels|saveVerzuimDrempels|geoorloofdHours|ongeoorloofdHours" | Measure-Object
   Verwacht: Count >= 6

3. Stap 6 afrondscherm bestaat:
   Select-String -Path "src\components\OnboardingWizard.tsx" -Pattern "step === 6"
   Verwacht: minimaal 1 hit

4. Null-guard aanwezig (GAP 3):
   Select-String -Path "src\components\OnboardingWizard.tsx" -Pattern "if \(!klasId\)"
   Verwacht: minimaal 1 hit

5. Afbreken aanwezig (GAP 4):
   Select-String -Path "src\components\OnboardingWizard.tsx" -Pattern "Afbreken"
   Verwacht: minimaal 1 hit

6. Ghost-class fix (GAP 2):
   Select-String -Path "src\App.tsx" -Pattern "students\?\.length"
   Verwacht: minimaal 1 hit

7. Vitest regressietest:
   npx vitest run --reporter=verbose 2>&1 | Select-String "FAIL|fail"
   Verwacht: geen FAIL-regels
</verification>

<success_criteria>
- ONB-06 is gesloten: stap 5 toont twee number-inputs voor geoorloofd/ongeoorloofd drempelwaarden (uren), met Overslaan en Opslaan-knoppen; saveVerzuimDrempels wordt aangeroepen bij opslaan
- Stap 6 is het afrondscherm met 🎉 en 'Naar het dashboard' knop
- De 'Naar het dashboard' knop heeft een expliciete null-guard op klasId (geen non-null assertion meer)
- Afbreken-knop zichtbaar op stappen 2-5; roept onComplete(klasId) aan als klasId bekend is, anders onAbort?.()
- App.tsx lazy init gebruikt students-check in plaats van klassen-count
- OnboardingWizard ontvangt onAbort={() => setView('import')} prop van App.tsx
- npx tsc --noEmit geeft geen fouten
- Alle bestaande vitest-tests slagen
</success_criteria>

<output>
Na voltooiing, maak aan: .planning/phases/24-onboarding-wizard/24-03-SUMMARY.md
</output>
