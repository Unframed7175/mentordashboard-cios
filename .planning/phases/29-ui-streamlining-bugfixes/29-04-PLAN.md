---
phase: 29
plan: 04
type: execute
wave: 2
depends_on:
  - 29-02
files_modified:
  - src/components/DetailWeergave.tsx
  - src/components/DeelgebiedenMatrix.tsx
  - src/components/FeedbackActiepuntenSection.tsx
  - src/components/AanvullendSection.tsx
  - src/components/RekenenNederlandsSection.tsx
  - src/components/VerzuimSection.tsx
  - src/components/SettingsPage.tsx
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04
must_haves:
  truths:
    - "DetailWeergave no longer imports or renders LeerlijnenSection, VakkenSection, or NotitiesTextarea"
    - "SpiderChartCard container in DetailWeergave is full-width (no width constraint)"
    - "DetailWeergave outer wrapper div has className including 'view-fade-in'"
    - "DetailWeergave print header color uses var(--text-secondary) not '#475569'"
    - "DeelgebiedenMatrix, FeedbackActiepuntenSection, AanvullendSection, RekenenNederlandsSection, VerzuimSection, SettingsPage contain no hardcoded hex values in inline styles"
  artifacts:
    - path: "src/components/DetailWeergave.tsx"
      provides: "section removal + full-width spider + view-fade-in + hex fix"
      contains: "view-fade-in"
    - path: "src/components/VerzuimSection.tsx"
      provides: "UI-03 hex fix — 7 replacements"
      contains: "var(--rag-groen)"
  key_links:
    - from: "src/components/DetailWeergave.tsx"
      to: "src/index.css"
      via: "view-fade-in class"
      pattern: "view-fade-in"
---

<objective>
Two tasks: (1) DetailWeergave.tsx cleanup — remove LeerlijnenSection/VakkenSection/NotitiesTextarea imports and JSX, make spider full-width, add view-fade-in class, fix print header hex. (2) Hex-fix pass on 6 remaining component files — replace all hardcoded inline-style colors with CSS variables.

Purpose: Clean up the detail view section order per D-DETAIL-01..04, and close the UI-03 dark mode violations across all affected components. Both tasks are independent of Plan 03 (no shared files).
Output: DetailWeergave.tsx with 8-section order; 6 component files with zero hardcoded hex in inline styles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-ui-streamlining-bugfixes/29-CONTEXT.md
@.planning/phases/29-ui-streamlining-bugfixes/29-RESEARCH.md
@.planning/phases/29-ui-streamlining-bugfixes/29-PATTERNS.md
@.planning/phases/29-ui-streamlining-bugfixes/29-UI-SPEC.md

<interfaces>
<!-- DetailWeergave.tsx current import block (from PATTERNS.md, lines 1–13): -->
  import VakkenSection from './VakkenSection';           // REMOVE import + JSX
  import NotitiesTextarea from './NotitiesTextarea';     // REMOVE import + JSX
  import LeerlijnenSection from './LeerlijnenSection';   // REMOVE import + JSX

JSX blocks to remove (from PATTERNS.md):
  <LeerlijnenSection prognose={status.prognose} />       — line ~135
  <VakkenSection student={student} />                    — line ~175–176
  <NotitiesTextarea student={student} leerlingId={leerlingId} />  — line ~178

Spider full-width (from PATTERNS.md):
  Current: <div className="spider-charts-row" style={{ display: 'flex', flexWrap: 'wrap', gap: '16px', justifyContent: 'flex-start' }}>
  Fix: remove any fixed width or max-width from the container; the inline style already has flex + flexWrap which is correct

Outer div hex fix (from PATTERNS.md, line 66):
  Before: <p style={{ margin: '0.25rem 0 0', fontSize: '0.9rem', color: '#475569' }}>
  After:  <p style={{ margin: '0.25rem 0 0', fontSize: '0.9rem', color: 'var(--text-secondary)' }}>

Outer div view-fade-in (from PATTERNS.md, line 62):
  Before: <div className="print-target" style={{ ... }}>
  After:  <div className="print-target view-fade-in" style={{ ... }}>

<!-- RESEARCH.md UI-03 hex audit — exact replacements per file: -->

DeelgebiedenMatrix.tsx:
  Line 46: '#16a34a' → 'var(--status-groen-text)'
  Line 47: '#dc2626' → 'var(--status-rood-text)'
  Line 48: '#9ca3af' → 'var(--text-muted)'
  Line 97: '#9ca3af' → 'var(--text-muted)'

FeedbackActiepuntenSection.tsx:
  Line 177: '#9ca3af' → 'var(--text-muted)'
  Line 190: '#dc2626' → 'var(--status-rood-text)'

AanvullendSection.tsx:
  Line 68: '#10b981' → 'var(--status-groen-text)'

RekenenNederlandsSection.tsx:
  Line 36: '#10b981' → 'var(--status-groen-text)'
  Line 83: '#10b981' → 'var(--status-groen-text)'

VerzuimSection.tsx:
  Line 25: '#9ca3af' → 'var(--text-muted)'
  Lines 58, 72, 86: '#22c55e' → 'var(--rag-groen)', '#f97316' → 'var(--rag-oranje)', '#ef4444' → 'var(--rag-rood)'
  Lines 108, 119, 130: same replacements for legend dots
  Line 135: '#991b1b' → 'var(--status-rood-text)'

SettingsPage.tsx:
  Lines 422, 430: '#EF4444' → 'var(--status-rood-text)'

<!-- New section order after cleanup (UI-SPEC.md Detail View Section Order Contract): -->
  1. Header
  2. DoortstroomPrognoseSection (rewritten in Plan 05)
  3. RekenenNederlandsSection
  4. FeedbackActiepuntenSection
  5. SpiderChartCard row (full-width)
  6. DeelgebiedenMatrix
  7. VerzuimSection
  8. BpvProgressSection
  Removed: LeerlijnenSection, VakkenSection, NotitiesTextarea
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DetailWeergave cleanup — section removal, full-width spider, fade-in, hex fix</name>
  <files>src/components/DetailWeergave.tsx</files>
  <read_first>
    - src/components/DetailWeergave.tsx — full read; map the complete import block, all JSX section elements, the spider-charts-row container, and the outer wrapper div to identify exact line locations for each change
  </read_first>
  <action>
    Make four independent changes to src/components/DetailWeergave.tsx:

    CHANGE 1 — Remove three imports (per D-DETAIL-01..03):
    Delete the import lines for VakkenSection, NotitiesTextarea, and LeerlijnenSection. Remove only the import lines; do not touch other imports.

    CHANGE 2 — Remove three JSX blocks (per D-DETAIL-01..03):
    Delete the JSX element `<LeerlijnenSection prognose={status.prognose} />` and its surrounding whitespace/comment.
    Delete the JSX element `<VakkenSection student={student} />` and its surrounding whitespace/comment.
    Delete the JSX element `<NotitiesTextarea student={student} leerlingId={leerlingId} />` and its surrounding whitespace/comment.
    Do NOT remove the surrounding section <div> wrappers if they contain other content. If LeerlijnenSection/VakkenSection/NotitiesTextarea are each the sole child of a wrapper div, remove that wrapper div too.

    CHANGE 3 — SpiderChartCard full-width (per D-DETAIL-04):
    Find the `.spider-charts-row` container div. If it has an inline style with a fixed `width:` or `maxWidth:` value, remove that property from the inline style. The `display: 'flex'`, `flexWrap: 'wrap'`, `gap: '16px'`, `justifyContent: 'flex-start'` inline style properties stay. If no fixed width is present on this element, note that in the SUMMARY (no change needed).

    CHANGE 4 — Outer wrapper div: add view-fade-in class and fix hex color:
    On the outermost return div (the one with `className="print-target"`), change className to `"print-target view-fade-in"`.
    On the `<p>` element near the print header that uses inline `color: '#475569'`, change to `color: 'var(--text-secondary)'`.

    The section order after these removals must match the UI-SPEC contract: Header → DoortstroomPrognoseSection → RekenenNederlandsSection → FeedbackActiepuntenSection → SpiderChartCard row → DeelgebiedenMatrix → VerzuimSection → BpvProgressSection. Verify the order is correct after removals.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - DetailWeergave.tsx does NOT contain "import LeerlijnenSection", "import VakkenSection", "import NotitiesTextarea"
    - DetailWeergave.tsx does NOT contain "<LeerlijnenSection", "<VakkenSection", "<NotitiesTextarea"
    - DetailWeergave.tsx contains "view-fade-in" in the outermost wrapper className
    - DetailWeergave.tsx does NOT contain the string '#475569' anywhere
    - npm test exits 0
  </acceptance_criteria>
  <done>Three sections removed (imports + JSX); spider container full-width; view-fade-in class added to wrapper; #475569 replaced with var(--text-secondary); npm test green</done>
</task>

<task type="auto">
  <name>Task 2: Hex-fix pass — 6 component files (UI-03)</name>
  <files>
    src/components/DeelgebiedenMatrix.tsx,
    src/components/FeedbackActiepuntenSection.tsx,
    src/components/AanvullendSection.tsx,
    src/components/RekenenNederlandsSection.tsx,
    src/components/VerzuimSection.tsx,
    src/components/SettingsPage.tsx
  </files>
  <read_first>
    - src/components/DeelgebiedenMatrix.tsx — read lines 40–55 (GrowthBadge component) and line ~97 (empty state) to confirm exact hex values before editing
    - src/components/FeedbackActiepuntenSection.tsx — read lines 170–195 to find '#9ca3af' and '#dc2626'
    - src/components/AanvullendSection.tsx — read lines 60–75 to find the '#10b981' inline style
    - src/components/RekenenNederlandsSection.tsx — read lines 30–45 and 78–90 to find both '#10b981' occurrences
    - src/components/VerzuimSection.tsx — read lines 20–30, 50–95, 100–140 to find all 7 hex occurrences
    - src/components/SettingsPage.tsx — read lines 415–435 to find both '#EF4444' occurrences
  </read_first>
  <action>
    Apply the exact hex-to-variable replacements from the RESEARCH.md UI-03 audit. These are all inline style `color:` or `background:` property values inside JSX — not CSS class definitions.

    DeelgebiedenMatrix.tsx (4 replacements):
    - GrowthBadge component, ↑ arrow span: `color: '#16a34a'` → `color: 'var(--status-groen-text)'`
    - GrowthBadge component, ↓ arrow span: `color: '#dc2626'` → `color: 'var(--status-rood-text)'`
    - GrowthBadge component, = span: `color: '#9ca3af'` → `color: 'var(--text-muted)'`
    - Empty state td: `color: '#9ca3af'` → `color: 'var(--text-muted)'`

    FeedbackActiepuntenSection.tsx (2 replacements):
    - Empty state p: `color: '#9ca3af'` → `color: 'var(--text-muted)'`
    - Save error span: `color: '#dc2626'` → `color: 'var(--status-rood-text)'`

    AanvullendSection.tsx (1 replacement):
    - Saved hint p: `color: hint === 'saved' ? '#10b981' : undefined` → `color: hint === 'saved' ? 'var(--status-groen-text)' : undefined`

    RekenenNederlandsSection.tsx (2 replacements):
    - Line 36: in the kleur variable or inline style, `'#10b981'` → `'var(--status-groen-text)'`
    - Line 83: same pattern, `'#10b981'` → `'var(--status-groen-text)'`

    VerzuimSection.tsx (7 replacements):
    - Empty state line 25: `color: '#9ca3af'` → `color: 'var(--text-muted)'`
    - Verzuim bar backgrounds (lines 58, 72, 86): `'#22c55e'` → `'var(--rag-groen)'`, `'#f97316'` → `'var(--rag-oranje)'`, `'#ef4444'` → `'var(--rag-rood)'`
    - Legend dots (lines 108, 119, 130): same three replacements for the dot background/color
    - High-absentee warning line 135: `color: '#991b1b'` → `color: 'var(--status-rood-text)'`

    SettingsPage.tsx (2 replacements):
    - Lines 422 and 430: `color: '#EF4444'` → `color: 'var(--status-rood-text)'`

    IMPORTANT: Do NOT change:
    - VakkenSection.tsx and StageSection.tsx — low priority per RESEARCH.md (their sections are removed/not rendered). If you edit them incidentally, fine, but they are not required.
    - NotitiesTextarea.tsx — optional, component file kept but not rendered.
    - Any CSS class definitions in .css files — only inline style JSX values.
    - Any hex value that is a gradient stop or SVG stroke color (leave spider chart stroke colors alone).
    - DoortstroomPrognoseSection.tsx — being fully rewritten in Plan 05.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - DeelgebiedenMatrix.tsx does NOT contain '#16a34a', '#dc2626' (in inline styles), '#9ca3af'
    - FeedbackActiepuntenSection.tsx does NOT contain '#9ca3af', '#dc2626' (as inline style values)
    - AanvullendSection.tsx does NOT contain '#10b981' as an inline style value
    - RekenenNederlandsSection.tsx does NOT contain '#10b981' as an inline style value
    - VerzuimSection.tsx does NOT contain '#9ca3af', '#22c55e', '#f97316', '#ef4444', '#991b1b' as inline style values
    - SettingsPage.tsx does NOT contain '#EF4444' as an inline style value
    - npm test exits 0
  </acceptance_criteria>
  <done>All 16 hex violations across 6 files replaced with CSS variables; npm test green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component render → DOM | Presentational changes only; removing sections does not delete stored data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Information Disclosure | Section removal | accept | D-DETAIL-01..03 explicitly confirms data remains in store — only the JSX render is removed; no data destruction |
| T-29-04-SC | Tampering | No new packages | accept | No npm installs; no slopcheck needed |
</threat_model>

<verification>
After both tasks:
- `npm test` exits 0
- Grep check: `grep -n "#[0-9a-fA-F]" src/components/DeelgebiedenMatrix.tsx src/components/FeedbackActiepuntenSection.tsx src/components/AanvullendSection.tsx src/components/RekenenNederlandsSection.tsx src/components/VerzuimSection.tsx src/components/SettingsPage.tsx src/components/DetailWeergave.tsx` — output should contain only hex values that are acceptable (gradient literals, SVG strokes) and nothing in inline style color/background properties
- `grep "LeerlijnenSection\|VakkenSection\|NotitiesTextarea" src/components/DetailWeergave.tsx` returns empty
- `grep "view-fade-in" src/components/DetailWeergave.tsx` returns a match
</verification>

<success_criteria>
- DetailWeergave: 3 sections removed (imports + JSX), spider full-width, view-fade-in added, #475569 fixed
- 6 component files: zero hardcoded hex in inline styles (16 total replacements)
- Full test suite green (158+ tests)
</success_criteria>

<output>
Create `.planning/phases/29-ui-streamlining-bugfixes/29-04-SUMMARY.md` when done
</output>
