---
phase: 29
plan: 03
type: execute
wave: 2
depends_on:
  - 29-02
files_modified:
  - src/components/BpvProgressSection.tsx
  - src/components/KlasTabStrip.tsx
autonomous: true
requirements:
  - FIX-01
  - FIX-02
must_haves:
  truths:
    - "BpvProgressSection renders 'BPV-data laden…' while Promise.all is pending"
    - "BpvProgressSection renders 'Geen stage-data — importeer de BPV Excel via het importscherm.' when record is null after loading"
    - "No hardcoded hex colors remain in BpvProgressSection.tsx"
    - "KlasTabStrip renders a <div className='nav-stripe'> element inside #main-nav"
    - "Nav stripe DOM element has aria-hidden='true'"
  artifacts:
    - path: "src/components/BpvProgressSection.tsx"
      provides: "FIX-02 — loading state + empty state + hex fixes"
      contains: "loading"
    - path: "src/components/KlasTabStrip.tsx"
      provides: "FIX-01 — nav-stripe DOM element"
      contains: "nav-stripe"
  key_links:
    - from: "src/components/BpvProgressSection.tsx"
      to: "utils/bpv.ts"
      via: "getBpvConfig + getBpvData"
      pattern: "setLoading\\(false\\)"
    - from: "src/components/KlasTabStrip.tsx"
      to: "src/index.css"
      via: ".nav-stripe class"
      pattern: "nav-stripe"
---

<objective>
Fix FIX-02 in BpvProgressSection (add loading boolean state, update empty state text, replace hardcoded hex colors) and fix FIX-01 in KlasTabStrip (add nav-stripe DOM element). Both files are independent — no shared imports, no shared CSS changes. Wave 1 CSS is already in place.

Purpose: Two targeted component fixes that close FIX-01 and FIX-02 and turn the Wave 0 RED BpvProgressSection tests GREEN.
Output: BpvProgressSection.tsx with 3-state render; KlasTabStrip.tsx with nav-stripe div. Wave 0 BpvProgressSection tests turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-ui-streamlining-bugfixes/29-CONTEXT.md
@.planning/phases/29-ui-streamlining-bugfixes/29-RESEARCH.md
@.planning/phases/29-ui-streamlining-bugfixes/29-PATTERNS.md
@.planning/phases/29-ui-streamlining-bugfixes/29-UI-SPEC.md

<interfaces>
<!-- BpvProgressSection.tsx current state (from PATTERNS.md pattern assignment) -->

Current state declarations (lines 20–21):
  const [bpvConfig, setBpvConfig] = useState<BpvConfig | null>(null);
  const [record, setRecord] = useState<BpvStudentRecord | null>(null);

Current useEffect (lines 23–30): Promise.all([getBpvConfig(), getBpvData()]).then([cfg, data] => { setBpvConfig(cfg); setRecord(data[leerlingId] ?? null); }).catch(err => console.warn(...));

Current JSX conditional (lines 36–40): single ternary — null state shows "Nog geen stage-data — importeer de stage Excel via het importscherm.", populated shows progress bar

Hardcoded hex to fix in BpvProgressSection.tsx (from RESEARCH.md UI-03 audit):
  Line 38: '#9ca3af' → var(--text-muted)
  Line 61: '#22C55E' (overshoot bar color) → var(--rag-groen)
  Line 126: '#22C55E' (goedgekeurd cell) → var(--rag-groen)
  Line 129: '#F59E0B' (in behandeling) → var(--rag-oranje)

<!-- KlasTabStrip.tsx current state (from PATTERNS.md) -->

Current return (lines 59–158): <nav id="main-nav"> ... </nav>
The nav-stripe div must be the LAST child before </nav>.
#main-nav has position: relative already (confirmed line ~270 in index.css).
.nav-tab elements use z-index: 1 — nav-stripe must use z-index: 0 (via CSS class .nav-stripe added in Plan 02).

<!-- CSS class .nav-stripe from Plan 02 output (already in index.css after Wave 1): -->
  position: absolute; top: 0; right: 0; width: 140px; height: 52px;
  background: linear-gradient(to bottom-left, #009FE3 0%, #009FE3 25%, transparent 70%);
  pointer-events: none; z-index: 0;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BpvProgressSection FIX-02 — loading state + empty state + hex fixes</name>
  <files>src/components/BpvProgressSection.tsx</files>
  <read_first>
    - src/components/BpvProgressSection.tsx — full read; map every line that references bpvConfig, record, the useEffect, the JSX ternary, and every inline style with a hex value (lines 38, 61, 126, 129 per the RESEARCH audit)
  </read_first>
  <behavior>
    - When Promise.all is pending (loading === true): component renders text "BPV-data laden…" and does NOT render progress bar or empty-state text
    - When loading === false and bpvConfig === null or record === null: component renders "Geen stage-data — importeer de BPV Excel via het importscherm." and does NOT render "BPV-data laden…"
    - When loading === false and both bpvConfig and record are non-null: component renders the existing progress bar; neither loading text nor empty state text is visible
    - No inline style in the file contains a hex color value after the fix
  </behavior>
  <action>
    Edit src/components/BpvProgressSection.tsx with three independent changes:

    CHANGE 1 — Add loading state (per D-FIX-02a):
    In the useState declarations block, add: `const [loading, setLoading] = useState<boolean>(true);`
    Initialize to true — this is critical (per RESEARCH Pitfall 3: initializing to false means loading message never shows).

    CHANGE 2 — Update useEffect to set loading to false (per D-FIX-02a, D-FIX-02b):
    In the existing Promise.all .then handler, add `setLoading(false);` BEFORE setBpvConfig and setRecord — or after, either order is correct since React batches state updates. Add `setLoading(false);` in the .catch handler as well.

    CHANGE 3 — Replace the JSX ternary with three-state conditional render (per D-FIX-02a/b/c):
    The current single ternary at lines 36–40 must become three separate early-return guards:
    First guard: if (loading) return the loading JSX — a div.detail-section containing p.detail-section-title "BPV-uren" and a p with text "BPV-data laden…" using style={{ fontSize: '0.875rem', color: 'var(--text-muted)' }}.
    Second guard: if (bpvConfig === null || record === null) return the empty-state JSX — same shell, p text "Geen stage-data — importeer de BPV Excel via het importscherm." using same font-size/color style.
    Third: fall through to the existing populated JSX (progress bar + breakdown) — no changes to that branch's structure.

    CHANGE 4 — Fix hardcoded hex colors (per UI-03, D-FIX-02b overlap):
    Line 38: if this inline style '#9ca3af' remains in any of the new JSX, it has already been replaced by var(--text-muted) in Changes 1–3 above. Verify it is gone.
    Line 61: find the inline style or JSX expression using '#22C55E' on the overshoot bar background — change to 'var(--rag-groen)'.
    Line 126: find '#22C55E' on the goedgekeurd cell color — change to 'var(--rag-groen)'.
    Line 129: find '#F59E0B' on the in-behandeling color — change to 'var(--rag-oranje)'.

    Do NOT change the component's props interface, export name, or any part of the populated-state JSX beyond the hex color fixes.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose --grep BpvProgress</automated>
  </verify>
  <acceptance_criteria>
    - BpvProgressSection.tsx contains "useState<boolean>(true)" on the loading state declaration
    - BpvProgressSection.tsx contains "setLoading(false)" in both the .then and .catch branches
    - BpvProgressSection.tsx contains the text literal "BPV-data laden" in JSX
    - BpvProgressSection.tsx contains the text literal "Geen stage-data — importeer de BPV Excel via het importscherm."
    - BpvProgressSection.tsx does NOT contain '#9ca3af', '#22C55E', '#22c55e', '#F59E0B', '#f59e0b' as inline style values
    - npm test -- --grep BpvProgress passes (Wave 0 BpvProgressSection tests turn GREEN)
    - npm test (full suite) exits 0
  </acceptance_criteria>
  <done>BpvProgressSection renders 3 distinct states; Wave 0 BpvProgressSection tests GREEN; no hardcoded hex remains; full suite green</done>
</task>

<task type="auto">
  <name>Task 2: KlasTabStrip FIX-01 — add nav-stripe DOM element</name>
  <files>src/components/KlasTabStrip.tsx</files>
  <read_first>
    - src/components/KlasTabStrip.tsx — full read; locate the <nav id="main-nav"> JSX element and its closing </nav>; confirm the exact last child before the closing tag
  </read_first>
  <action>
    Edit src/components/KlasTabStrip.tsx:

    Inside the return statement's `<nav id="main-nav">` element, add `<div aria-hidden="true" className="nav-stripe" />` as the LAST child before the closing `</nav>` tag.

    The exact placement is: after all existing children (tab buttons, modal trigger, gear icon, feedback button — whatever is currently the last child), add the nav-stripe div.

    Do NOT change any existing children, props, event handlers, or any other part of the component.

    The .nav-stripe CSS class was added to src/index.css in Plan 02. The div requires no inline styles — the class provides position: absolute, top: 0, right: 0, width: 140px, height: 52px, the gradient background, pointer-events: none, and z-index: 0.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/components/KlasTabStrip.tsx contains the string "nav-stripe" inside a JSX element
    - src/components/KlasTabStrip.tsx contains 'aria-hidden="true"' or `aria-hidden={true}` on the nav-stripe div
    - The nav-stripe div appears inside the `<nav id="main-nav">` element (not outside it)
    - npm test exits 0
  </acceptance_criteria>
  <done>nav-stripe div added as last child of #main-nav in KlasTabStrip; npm test green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component render → DOM | Presentational changes only; no user input or data handling changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Information Disclosure | nav-stripe div | accept | aria-hidden="true" ensures screen readers skip decorative element |
| T-29-03-SC | Tampering | No new packages | accept | No npm installs; no slopcheck needed |
</threat_model>

<verification>
After both tasks:
- `npm test -- --grep BpvProgress` passes (Wave 0 tests GREEN)
- `npm test` full suite exits 0
- Grep: `grep -v "^//" src/components/BpvProgressSection.tsx | grep "#[0-9a-fA-F]"` returns empty
- Grep: `grep "nav-stripe" src/components/KlasTabStrip.tsx` returns a match
</verification>

<success_criteria>
- BpvProgressSection.tsx: loading state, 3-state render, no hardcoded hex
- KlasTabStrip.tsx: nav-stripe div as last child inside #main-nav
- Wave 0 BpvProgressSection tests GREEN
- Full test suite green (158+ tests)
</success_criteria>

<output>
Create `.planning/phases/29-ui-streamlining-bugfixes/29-03-SUMMARY.md` when done
</output>
