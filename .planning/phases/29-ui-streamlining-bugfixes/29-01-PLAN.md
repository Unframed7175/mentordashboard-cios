---
phase: 29
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/BpvProgressSection.test.tsx
  - tests/DoortstroomPrognoseSection.test.tsx
autonomous: true
requirements:
  - FIX-02
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - "BpvProgressSection test file exists with 3 RED tests covering loading/empty/data states"
    - "DoortstroomPrognoseSection test file exists with RED tests covering BJ1 and BJ2 block rendering"
    - "npm test exits non-zero (RED) for new tests, existing 158+ tests remain green"
  artifacts:
    - path: "tests/BpvProgressSection.test.tsx"
      provides: "FIX-02 gate — loading state, empty state, data state"
      min_lines: 40
    - path: "tests/DoortstroomPrognoseSection.test.tsx"
      provides: "PROG-01 gate — block layout for BJ1 and BJ2"
      min_lines: 60
  key_links:
    - from: "tests/BpvProgressSection.test.tsx"
      to: "src/components/BpvProgressSection.tsx"
      via: "import BpvProgressSection"
      pattern: "BpvProgressSection"
    - from: "tests/DoortstroomPrognoseSection.test.tsx"
      to: "src/components/DoortstroomPrognoseSection.tsx"
      via: "import DoortstroomPrognoseSection"
      pattern: "DoortstroomPrognoseSection"
---

<objective>
Create two RED test files that gate the Wave 2 and Wave 3 implementations. These tests fail NOW against the current components; they turn GREEN when the implementations ship.

Purpose: Establish automated verification for FIX-02 (BPV loading state) and PROG-01 (doorstroomnorm block layout) so that no wave can claim completion without passing a concrete test.
Output: tests/BpvProgressSection.test.tsx, tests/DoortstroomPrognoseSection.test.tsx — both RED until Wave 2/3 implementations ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-ui-streamlining-bugfixes/29-CONTEXT.md
@.planning/phases/29-ui-streamlining-bugfixes/29-RESEARCH.md
@.planning/phases/29-ui-streamlining-bugfixes/29-PATTERNS.md
@.planning/phases/29-ui-streamlining-bugfixes/29-VALIDATION.md

<interfaces>
<!-- Existing test pattern from analogous tests in this project. -->
<!-- BpvProgressSection currently lacks a loading state; DoortstroomPrognoseSection lacks block layout. -->

From src/components/BpvProgressSection.tsx (current state — BEFORE FIX-02):
- Props: { leerlingId: string }
- States: bpvConfig (null or BpvConfig), record (null or BpvStudentRecord) — no loading state yet
- Render: one conditional — null state shows "Nog geen stage-data", populated shows progress bar

From src/components/DoortstroomPrognoseSection.tsx (current state — BEFORE PROG-01 rewrite):
- Props: { student: any; status: StatusResult }
- Does NOT render .prognose-block elements or .prognose-criterion-row elements
- Does NOT render block names "SBL", "SBC", "Negatief"

From utils/prognosis.ts — berekenPrognose() return shape (used via status.prognose):
- status.prognose.traject: 'bj1' | 'bj2'
- status.prognose.totaalVoldoendeOfHoger: number
- status.prognose.totaalOnvoldoende: number
- status.prognose.isNegatief: boolean
- status.prognose.gaps.nodigSBL: number
- status.prognose.gaps.nodigSBC_deelgebieden: number
- status.prognose.gaps.nodigSBC_kern: string[]
- status.prognose.leerlijnen: Array<{ leerlijn, totaal, voldoendeOfHoger, onvoldoende, ... }>
- status.prognose.label: string

Mock pattern from existing tests (see tests/storage.test.ts, tests/klassen.test.ts):
- vi.mock('../src/utils/bpv', () => ({ getBpvConfig: vi.fn(), getBpvData: vi.fn() }))
- Use renderToStaticMarkup or @testing-library/react render
- Check tests/spider.test.tsx for JSX-aware test patterns already established
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: BpvProgressSection RED test scaffold</name>
  <files>tests/BpvProgressSection.test.tsx</files>
  <read_first>
    - src/components/BpvProgressSection.tsx — full read, understand current props, state declarations, useEffect pattern, and exact render output for null vs populated states
    - tests/spider.test.tsx — established pattern for rendering React components in this test suite (renderToStaticMarkup or @testing-library/react usage)
    - vitest.config.ts or vite.config.ts — confirm jsdom environment is configured
  </read_first>
  <behavior>
    - Test 1 "loading state": mock getBpvConfig + getBpvData to return promises that never resolve (or delay), render BpvProgressSection, assert rendered HTML contains text "BPV-data laden" — this test MUST be RED now (current component has no loading state)
    - Test 2 "empty state text": mock both to resolve with null/empty data, render, assert rendered HTML contains "Geen stage-data — importeer de BPV Excel via het importscherm." — verify current text "Nog geen stage-data" does NOT match (RED)
    - Test 3 "data state does not show empty text": mock both to resolve with valid BpvConfig and BpvStudentRecord (stub objects with doelUren: 200, gerealiseerdeUren: 120, plaatsingen: []), render, assert rendered HTML does NOT contain "BPV-data laden" and does NOT contain "Geen stage-data"
  </behavior>
  <action>
    Write tests/BpvProgressSection.test.tsx. Import BpvProgressSection from '../src/components/BpvProgressSection'. Mock the bpv utils: vi.mock('../src/utils/bpv', () => ({ getBpvConfig: vi.fn(), getBpvData: vi.fn() })). Use renderToStaticMarkup (from react-dom/server) for synchronous rendering assertions OR use @testing-library/react with waitFor if the project already uses it for async components — check spider.test.tsx to pick the same approach.

    For the loading test: set getBpvConfig to return a promise that does not resolve synchronously (new Promise(() => {}) or a delayed mock). Since renderToStaticMarkup only captures initial render, the loading state (useState(true)) will be visible in the initial render snapshot — assert the text "BPV-data laden" is present.

    For the empty test: set getBpvConfig to vi.fn().mockResolvedValue(null) and getBpvData to vi.fn().mockResolvedValue({}) — in the current component this shows "Nog geen stage-data" (old text), so assert that the NEW text "Geen stage-data — importeer de BPV Excel via het importscherm." is present. This will be RED against the current component.

    For the data test: mock with a valid BpvConfig object (doelUren: 200) and getBpvData returning { 'leerling-1': { leerlingId: 'leerling-1', naam: 'Test', gerealiseerdeUren: 120, doelUren: 200, plaatsingen: [] } }. Render with leerlingId="leerling-1". Assert neither loading text nor empty text appears. This will be RED against the current component because the loading state does not exist.

    Do NOT write full implementations inside test files — only test assertions.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | grep -E "BpvProgress|PASS|FAIL|✓|✗|×"</automated>
  </verify>
  <done>tests/BpvProgressSection.test.tsx exists with 3 tests; at least 2 tests are RED (failing) against the current BpvProgressSection implementation; all existing 158+ tests remain green</done>
</task>

<task type="tdd">
  <name>Task 2: DoortstroomPrognoseSection RED test scaffold</name>
  <files>tests/DoortstroomPrognoseSection.test.tsx</files>
  <read_first>
    - src/components/DoortstroomPrognoseSection.tsx — full read, understand current props interface (student, status), what it currently renders, and what it does NOT render (block layout elements)
    - src/utils/status.ts — StatusResult type and berekenStatus return shape to build valid stub status objects
    - utils/normen.ts — getNormenSync() return shape to know what n.sbl, n.sbc, n.negatiefTotaal, n.negatiefPerLeerlijn fields look like
    - tests/spider.test.tsx — rendering pattern for JSX components
  </read_first>
  <behavior>
    BJ2 tests (traject='bj2'):
    - Test 1 "BJ2 renders SBL block name": render with a BJ2 student stub, assert rendered output contains text "SBL" — RED now (current component does not render block names)
    - Test 2 "BJ2 renders SBC block name": assert rendered output contains text "SBC"
    - Test 3 "BJ2 renders Negatief block name": assert rendered output contains "Negatief"
    - Test 4 "BJ2 SBL criterion row shows score vs threshold": assert rendered output contains "/" with numbers (e.g. pattern /\d+ \/ \d+/) in a .prognose-criterion-row element

    BJ1 tests (traject='bj1'):
    - Test 5 "BJ1 renders BJ2 doorstroom block": assert rendered output contains "BJ2 doorstroom"
    - Test 6 "BJ1 renders Versneld SBC block": assert rendered output contains "Versneld SBC"

    Empty/grijs tests:
    - Test 7 "no scores shows no scores message": render with status where totaalVoldoendeOfHoger===0 && totaalOnvoldoende===0, assert "Nog geen scores beschikbaar" appears in output

    All tests must be RED against current implementation.
  </behavior>
  <action>
    Write tests/DoortstroomPrognoseSection.test.tsx. Import DoortstroomPrognoseSection from '../src/components/DoortstroomPrognoseSection'. Mock getNormenSync: vi.mock('../utils/normen', () => ({ getNormenSync: vi.fn().mockReturnValue({ sbl: 13, sbc: 15, negatiefTotaal: 6, negatiefPerLeerlijn: 2, bj1Positief: 10, bj1VersneldLesgeven: 4, bj1VersneldOrganiseren: 3, bj1VersneldProfHandelen: 5 }) })).

    Build stub objects for the status prop:
    - BJ2 stub: status.prognose = { traject: 'bj2', label: 'sbl', isNegatief: false, totaalVoldoendeOfHoger: 14, totaalOnvoldoende: 2, leerlijnen: [{ leerlijn: 'lesgeven', totaal: 8, voldoendeOfHoger: 5, goedOfHoger: 3, onvoldoende: 1, onbeoordeeld: 2 }, ...], gaps: { nodigSBL: 0, nodigSBC_deelgebieden: 1, nodigSBC_kern: [], onvoldoendeRuimte: 0, onvoldoendeRuimtePerLeerlijn: { lesgeven: 1, organiseren: 2, prof_handelen: 0 } } }
    - BJ1 stub: status.prognose = { traject: 'bj1', label: 'naar_bj2', isNegatief: false, totaalVoldoendeOfHoger: 8, totaalOnvoldoende: 1, leerlijnen: [...], gaps: { nodigBJ2: 2, nodigVersneld_lesgeven: 1, nodigVersneld_organiseren: 0, nodigVersneld_profHandelen: 2, onvoldoendeRuimte: 0, onvoldoendeRuimtePerLeerlijn: { lesgeven: 1, organiseren: 2, prof_handelen: 0 } } }
    - student stub: { jaarlaag: 'BJ2' } for BJ2, { jaarlaag: 'BJ1' } for BJ1

    Use renderToStaticMarkup and assert with .includes() or .match() for the expected text/pattern. Do not write implementations inside the test.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | grep -E "DoortstroomPrognose|PASS|FAIL|✓|✗|×"</automated>
  </verify>
  <done>tests/DoortstroomPrognoseSection.test.tsx exists with 7 tests; all 7 are RED against the current implementation; all 158+ existing tests remain green; total failing count = exactly the new RED tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → component | Tests import production components; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-SC | Tampering | npm test scaffolds | accept | No new packages installed; test files only reference existing source imports |
</threat_model>

<verification>
Run `npm test` after both tasks. Confirm:
1. The new test files appear in output
2. New tests are RED (failing) — do not proceed to Wave 1 if all new tests pass (that would mean the components already implement the target behavior, which is unexpected)
3. All pre-existing 158+ tests remain green
</verification>

<success_criteria>
- tests/BpvProgressSection.test.tsx exists, 3 tests, at minimum 2 RED
- tests/DoortstroomPrognoseSection.test.tsx exists, 7 tests, all 7 RED
- npm test total: 158+ green + the RED tests isolated to phase 29 test files
- No changes to any src/ file in this plan
</success_criteria>

<output>
Create `.planning/phases/29-ui-streamlining-bugfixes/29-01-SUMMARY.md` when done
</output>
