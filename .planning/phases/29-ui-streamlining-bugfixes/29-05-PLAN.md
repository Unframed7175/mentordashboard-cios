---
phase: 29
plan: 05
type: execute
wave: 3
depends_on:
  - 29-03
  - 29-04
files_modified:
  - src/components/DoortstroomPrognoseSection.tsx
  - src/index.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - FIX-01
  - FIX-02
must_haves:
  truths:
    - "DoortstroomPrognoseSection renders three blocks (SBL, SBC, Negatief) for BJ2 students"
    - "DoortstroomPrognoseSection renders BJ2 doorstroom + Versneld SBC + Negatief blocks for BJ1 students"
    - "Each block contains a header with block name and overall status chip"
    - "Each criterion row shows label, score/threshold, and a 3-state status chip"
    - "Thresholds come from getNormenSync() — no hardcoded numbers"
    - "When totaalVoldoendeOfHoger === 0 and totaalOnvoldoende === 0, blocks show 'Nog geen scores beschikbaar'"
    - "No hardcoded hex color values in the rewritten component"
    - ".prognose-block and .prognose-criterion-row CSS classes exist in index.css"
    - "Wave 0 DoortstroomPrognoseSection tests turn GREEN"
  artifacts:
    - path: "src/components/DoortstroomPrognoseSection.tsx"
      provides: "PROG-01 — per-traject block layout rewrite"
      contains: "prognose-block"
    - path: "src/index.css"
      provides: "PROG-01 CSS classes: .prognose-block, .prognose-block-header, .prognose-criterion-row"
      contains: "prognose-block"
  key_links:
    - from: "src/components/DoortstroomPrognoseSection.tsx"
      to: "utils/normen.ts"
      via: "getNormenSync()"
      pattern: "getNormenSync"
    - from: "src/components/DoortstroomPrognoseSection.tsx"
      to: "utils/status.ts"
      via: "status.prognose + detectTraject"
      pattern: "detectTraject"
    - from: "src/components/DoortstroomPrognoseSection.tsx"
      to: "src/index.css"
      via: ".prognose-block CSS classes"
      pattern: "prognose-block"
---

<objective>
Fully rewrite DoortstroomPrognoseSection.tsx with the per-traject block layout (D-PROG-01..05) and add the required CSS classes to index.css. The rewrite uses status.prognose (pre-computed) and getNormenSync() for thresholds — no new async calls.

Purpose: Replace the current traject-tag + gap-items approach with three structured blocks (SBL, SBC, Negatief for BJ2; BJ2 doorstroom + Versneld SBC + Negatief for BJ1). Each block shows its criteria with score vs threshold and 3-state color. Turns Wave 0 DoortstroomPrognoseSection tests GREEN.
Output: Fully rewritten DoortstroomPrognoseSection.tsx; new CSS block classes in index.css. All Phase 29 requirements fulfilled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-ui-streamlining-bugfixes/29-CONTEXT.md
@.planning/phases/29-ui-streamlining-bugfixes/29-RESEARCH.md
@.planning/phases/29-ui-streamlining-bugfixes/29-PATTERNS.md
@.planning/phases/29-ui-streamlining-bugfixes/29-UI-SPEC.md

<interfaces>
<!-- Props interface to PRESERVE from current DoortstroomPrognoseSection.tsx (PATTERNS.md): -->

interface DoortstroomPrognoseSectionProps {
  student: any;
  status: StatusResult;
}

<!-- Key rule from existing component (PATTERNS.md, line 109 comment): -->
// Use pre-computed prognose from status — do NOT call berekenPrognose again
const p = status.prognose;
const traject = detectTraject(student);
// Alternative: status.prognose.traject also available as fallback

<!-- LEERLIJN_LABEL map to keep (PATTERNS.md lines 17–21): -->
const LEERLIJN_LABEL: Record<string, string> = {
  lesgeven: 'Lesgeven',
  organiseren: 'Organiseren',
  prof_handelen: 'Professioneel handelen',
};

<!-- getNormenSync() return shape (from RESEARCH.md + utils/normen.ts): -->
n.sbl, n.sbc, n.negatiefTotaal, n.negatiefPerLeerlijn, n.bj1Positief,
n.bj1VersneldLesgeven, n.bj1VersneldOrganiseren, n.bj1VersneldProfHandelen

<!-- status.prognose shape (from RESEARCH.md berekenPrognose Output Structure): -->
p.traject: 'bj1' | 'bj2'
p.isNegatief: boolean
p.totaalVoldoendeOfHoger: number
p.totaalOnvoldoende: number
p.leerlijnen: Array<{ leerlijn, totaal, voldoendeOfHoger, goedOfHoger, onvoldoende, onbeoordeeld }>
p.gaps.nodigSBL: number           (BJ2)
p.gaps.nodigSBC_deelgebieden: number  (BJ2)
p.gaps.nodigSBC_kern: string[]    (BJ2)
p.gaps.nodigBJ2: number           (BJ1)
p.gaps.nodigVersneld_lesgeven: number   (BJ1)
p.gaps.nodigVersneld_organiseren: number (BJ1)
p.gaps.nodigVersneld_profHandelen: number (BJ1)
p.gaps.onvoldoendeRuimtePerLeerlijn: { lesgeven: number, organiseren: number, prof_handelen: number }

<!-- CSS classes to USE (already dark-mode safe — from PATTERNS.md): -->
.status-groen { background: var(--status-groen-bg); color: var(--status-groen-text); }
.status-oranje { background: var(--status-oranje-bg); color: var(--status-oranje-text); }
.status-rood   { background: var(--status-rood-bg);   color: var(--status-rood-text); }
.gap-ok, .gap-warn, .gap-danger — have body.dark overrides; safe to use for criterion rows

<!-- CSS classes to ADD to index.css for block layout: -->
.prognose-block — container per block
.prognose-block-header — header row inside block
.prognose-criterion-row — individual criterion row

<!-- Block layout spec (UI-SPEC.md Component Contracts): -->
Block container (.prognose-block):
  border: 1px solid var(--border-default)
  border-radius: var(--radius-md)
  background: var(--bg-surface-alt)
  Left accent: border-left: 3px solid [block-status-color: var(--rag-groen)/var(--rag-oranje)/var(--rag-rood)]
  The border-left color is applied via an inline style or a modifier class, as needed.

Block header (.prognose-block-header):
  Block name: font-size: 0.75rem; font-weight: 700; text-transform: uppercase; color: var(--text-faint)
  Overall status chip: .status-groen / .status-oranje / .status-rood class

Criterion row (.prognose-criterion-row):
  display: flex; justify-content: space-between; align-items: center; padding: 4px 0
  Left: label — font-size: 0.875rem; color: var(--text-secondary)
  Center: score / threshold — font-size: 0.875rem; font-weight: 700; color: var(--text-primary)
  Right: status chip — .status-groen / .status-oranje / .status-rood

<!-- 3-state coloring rules for criteria (UI-SPEC.md Color table): -->
groen: nodig === 0
oranje: nodig > 0 && nodig <= 2
rood: nodig > 2

<!-- Data mapping for blocks (UI-SPEC.md Component Contracts table): -->
SBL block (BJ2 only):
  Criterion: "≥{n.sbl} deelgebieden ≥V"
  Score: totaalVoldoendeOfHoger / n.sbl
  nodig for status: gaps.nodigSBL

SBC block (BJ2 only):
  Criterion 1: "≥{n.sbc} deelgebieden ≥V", score totaalVoldoendeOfHoger / n.sbc, nodig: gaps.nodigSBC_deelgebieden
  Criterion 2: "Kerndeelgebieden ≥V", score (4 - gaps.nodigSBC_kern.length) / 4, nodig: gaps.nodigSBC_kern.length (groen: 0, rood: >0)

Negatief block (both BJ1 and BJ2):
  Criterion 1: "≤{n.negatiefTotaal} O totaal", score totaalOnvoldoende vs n.negatiefTotaal
    nodig: isNegatief based on totaal? Use: groen = !isNegatief (for total), else rood
  Criterion 2–4: per leerlijn "≤{n.negatiefPerLeerlijn} O {leerlijn}", nodig: -gaps.onvoldoendeRuimtePerLeerlijn[l] when negative, else 0

BJ2 doorstroom block (BJ1 only):
  Criterion: "≥{n.bj1Positief} deelgebieden ≥V", score totaalVoldoendeOfHoger / n.bj1Positief, nodig: gaps.nodigBJ2

Versneld SBC block (BJ1 only):
  Criterion 1: "≥{n.bj1VersneldLesgeven} ≥G lesgeven", nodig: gaps.nodigVersneld_lesgeven
  Criterion 2: "≥{n.bj1VersneldOrganiseren} ≥G organiseren", nodig: gaps.nodigVersneld_organiseren
  Criterion 3: "≥{n.bj1VersneldProfHandelen} ≥G professioneel handelen", nodig: gaps.nodigVersneld_profHandelen

<!-- Copywriting contract (UI-SPEC.md Copywriting Contract): -->
Empty state: "Nog geen scores beschikbaar" inside each block body when totaalVoldoendeOfHoger === 0 && totaalOnvoldoende === 0
Block names (text content, uppercased via CSS): "SBL", "SBC", "Negatief", "BJ2 doorstroom", "Versneld SBC"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .prognose-block CSS classes to index.css</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css lines 580–620 — read the existing .detail-section and .detail-section-title block to understand where to insert the new prognose block classes (add in the same section, after .detail-section-title)
    - src/index.css lines 155–165 — confirm .status-groen/.status-oranje/.status-rood classes and their dark mode overrides for reference (these are reused; no changes needed)
    - src/index.css `:root` block — locate --border-default, --radius-md, --bg-surface-alt, --rag-groen, --rag-oranje, --rag-rood token names
  </read_first>
  <action>
    Add three new CSS rule blocks to src/index.css, placed after the existing `.detail-section-title` rule in the detail-section area (section 14 or wherever .detail-section is defined).

    .prognose-blocks-container:
      display: flex
      flex-wrap: wrap
      gap: 0.75rem

    .prognose-block:
      border: 1px solid var(--border-default)
      border-radius: var(--radius-md)
      background: var(--bg-surface-alt)
      padding: 0.75rem 1rem
      flex: 1 1 260px
      min-width: 0
      (The left accent border-left is applied via inline style in the TSX for each block, not a separate CSS class, because the color depends on block status at runtime.)

    .prognose-block-header:
      display: flex
      justify-content: space-between
      align-items: center
      margin-bottom: 0.5rem

    .prognose-block-name:
      font-size: 0.75rem
      font-weight: 700
      text-transform: uppercase
      color: var(--text-faint)
      letter-spacing: 0.05em

    .prognose-criterion-row:
      display: flex
      justify-content: space-between
      align-items: center
      padding: 4px 0
      gap: 0.5rem

    .prognose-criterion-label:
      font-size: 0.875rem
      color: var(--text-secondary)
      flex: 1 1 auto

    .prognose-criterion-score:
      font-size: 0.875rem
      font-weight: 700
      color: var(--text-primary)
      white-space: nowrap

    .prognose-status-chip:
      font-size: 0.75rem
      font-weight: 700
      padding: 2px 6px
      border-radius: 4px
      white-space: nowrap
      (This class is not needed if .status-groen/.status-oranje/.status-rood are used directly — use .status-* for the chips.)

    Do NOT add any hardcoded hex values. Use only CSS custom property references. Do NOT add dark-mode overrides — the .status-* classes already handle dark mode.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains ".prognose-block {" as a class definition
    - src/index.css contains ".prognose-criterion-row {" as a class definition
    - src/index.css contains ".prognose-block-header {" as a class definition
    - No new hardcoded hex values introduced
    - npm test exits 0
  </acceptance_criteria>
  <done>5+ .prognose-* CSS classes added to index.css using only CSS variables; npm test green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite DoortstroomPrognoseSection.tsx with block layout</name>
  <files>src/components/DoortstroomPrognoseSection.tsx</files>
  <read_first>
    - src/components/DoortstroomPrognoseSection.tsx — full read of the current implementation to understand: props interface, imports, LEERLIJN_LABEL map, getNormenSync() usage, detectTraject() usage, and what state/rendering currently exists (so the rewrite preserves the correct data access patterns)
    - utils/normen.ts — confirm getNormenSync() return type and field names (n.sbl, n.sbc, n.negatiefTotaal, n.negatiefPerLeerlijn, n.bj1Positief, n.bj1VersneldLesgeven, n.bj1VersneldOrganiseren, n.bj1VersneldProfHandelen)
    - src/utils/status.ts — confirm StatusResult type and that detectTraject(student) returns 'bj1' | 'bj2'
    - src/index.css (the classes added in Task 1) — confirm exact class names before using them in JSX
  </read_first>
  <behavior>
    BJ2 rendering:
    - Component renders a div.detail-section containing p.detail-section-title "Doorstroomprognose"
    - Component renders a .prognose-blocks-container with 3 blocks: SBL, SBC, Negatief
    - SBL block header shows block name "SBL" and an overall status chip (.status-groen when nodigSBL===0, .status-oranje when <=2, .status-rood when >2)
    - SBL block has one criterion row: label "≥{n.sbl} deelgebieden ≥V", score "totaalVoldoendeOfHoger / n.sbl", status chip based on nodigSBL
    - SBC block shows 2 criterion rows per data mapping
    - Negatief block shows 4 criterion rows (1 totaal + 3 per leerlijn)

    BJ1 rendering:
    - Renders "BJ2 doorstroom" block + "Versneld SBC" block + Negatief block
    - "Versneld SBC" shows 3 criterion rows (lesgeven, organiseren, professioneel handelen) using bj1Versneld* thresholds

    No-scores state:
    - When totaalVoldoendeOfHoger === 0 AND totaalOnvoldoende === 0: each block body renders "Nog geen scores beschikbaar" in var(--text-muted) at 0.875rem; block headers still render with block name; no status chip shown

    No hardcoded hex:
    - All color values use CSS variables or .status-* classes
  </behavior>
  <action>
    Fully rewrite src/components/DoortstroomPrognoseSection.tsx. The file is replaced entirely (not patched). Preserve:
    - The same import for React
    - The same props interface: { student: any; status: StatusResult }
    - The LEERLIJN_LABEL constant
    - Import of detectTraject from '../utils/status' and getNormenSync from '../../utils/normen' (or the relative path used in the current file — check the current import paths)

    Do NOT import or call berekenPrognose(). Do NOT call getDrempelwaarden() (async). Use only status.prognose (pre-computed) and getNormenSync() (sync).

    Structure the rewrite as follows:

    1. Helper function criterionStatus(nodig: number): 'groen' | 'oranje' | 'rood'
       - Returns 'groen' if nodig === 0, 'oranje' if nodig <= 2, 'rood' if nodig > 2

    2. Helper component CriterionRow({ label, scoreDisplay, nodig }: { label: string; scoreDisplay: string; nodig: number })
       - Renders a div.prognose-criterion-row with: span.prognose-criterion-label (label text), span.prognose-criterion-score (scoreDisplay), span with className matching the status chip classes ("status-" + criterionStatus(nodig)) (content = "✓" for groen, "△" for oranje, "✗" for rood, or just leave empty — any readable indicator)
       - Note: the chip className should be built as `status-${criterionStatus(nodig)}` which maps to .status-groen/.status-oranje/.status-rood

    3. Helper component PrognoseBlock({ name, overallNodig, children, isEmpty }: { name: string; overallNodig: number; children: React.ReactNode; isEmpty: boolean })
       - Renders a div.prognose-block with inline style `borderLeft: '3px solid var(--rag-' + (overallNodig === 0 ? 'groen' : overallNodig <= 2 ? 'oranje' : 'rood') + ')'`
       - Header: div.prognose-block-header containing span.prognose-block-name (name text) and (if !isEmpty) a span with .status-* chip class showing the block name overall status
       - Body: if isEmpty, render `<p style={{ fontSize: '0.875rem', color: 'var(--text-muted)' }}>Nog geen scores beschikbaar</p>`, else render children

    4. Main component DoortstroomPrognoseSection:
       - Compute: const p = status.prognose; const n = getNormenSync(); const traject = detectTraject(student); (or use p.traject as fallback)
       - Compute: const isEmpty = p.totaalVoldoendeOfHoger === 0 && p.totaalOnvoldoende === 0;
       - Build per-leerlijn onvoldoende ruimte: for each leerlijn l in ['lesgeven', 'organiseren', 'prof_handelen'], the "nodig" for the negatief per-leerlijn row is: Math.max(0, -(p.gaps.onvoldoendeRuimtePerLeerlijn[l] ?? 0)) — negative ruimte = exceeded
       - For overall Negatief block status: nodig = p.isNegatief ? 3 : 0 (binary — no oranje for the Negatief block overall header; simplify to rood = isNegatief)

       BJ2 JSX:
         If traject === 'bj2' (or p.traject === 'bj2'):
           PrognoseBlock "SBL" with overallNodig=gaps.nodigSBL, isEmpty:
             CriterionRow label=`≥${n.sbl} deelgebieden ≥V` scoreDisplay=`${p.totaalVoldoendeOfHoger} / ${n.sbl}` nodig=gaps.nodigSBL
           PrognoseBlock "SBC" with overallNodig=Math.max(gaps.nodigSBC_deelgebieden, gaps.nodigSBC_kern.length > 0 ? 3 : 0), isEmpty:
             CriterionRow label=`≥${n.sbc} deelgebieden ≥V` scoreDisplay=`${p.totaalVoldoendeOfHoger} / ${n.sbc}` nodig=gaps.nodigSBC_deelgebieden
             CriterionRow label="Kerndeelgebieden ≥V" scoreDisplay=`${4 - gaps.nodigSBC_kern.length} / 4` nodig=gaps.nodigSBC_kern.length
           PrognoseBlock "Negatief" with overallNodig=p.isNegatief ? 3 : 0, isEmpty:
             CriterionRow label=`≤${n.negatiefTotaal} O totaal` scoreDisplay=`${p.totaalOnvoldoende} / ${n.negatiefTotaal}` nodig=p.isNegatief ? 3 : 0
             [for each leerlijn l in ['lesgeven', 'organiseren', 'prof_handelen']]:
               CriterionRow label=`≤${n.negatiefPerLeerlijn} O ${LEERLIJN_LABEL[l].toLowerCase()}` scoreDisplay=computed nodig=leerlijnenNodigForL
       
       BJ1 JSX:
         If traject === 'bj1' (or p.traject === 'bj1'):
           PrognoseBlock "BJ2 doorstroom" with overallNodig=gaps.nodigBJ2, isEmpty:
             CriterionRow label=`≥${n.bj1Positief} deelgebieden ≥V` scoreDisplay=`${p.totaalVoldoendeOfHoger} / ${n.bj1Positief}` nodig=gaps.nodigBJ2
           PrognoseBlock "Versneld SBC" with overallNodig=Math.max(gaps.nodigVersneld_lesgeven, gaps.nodigVersneld_organiseren, gaps.nodigVersneld_profHandelen), isEmpty:
             CriterionRow label=`≥${n.bj1VersneldLesgeven} ≥G lesgeven` scoreDisplay computed from leerlijnen nodig=gaps.nodigVersneld_lesgeven
             CriterionRow label=`≥${n.bj1VersneldOrganiseren} ≥G organiseren` nodig=gaps.nodigVersneld_organiseren
             CriterionRow label=`≥${n.bj1VersneldProfHandelen} ≥G professioneel handelen` nodig=gaps.nodigVersneld_profHandelen
           PrognoseBlock "Negatief" — same as BJ2

    Wrap all blocks in a div.prognose-blocks-container inside div.detail-section. Add p.detail-section-title "Doorstroomprognose" at the top.

    CRITICAL: Do NOT add any hardcoded hex values. Use CSS classes and var() references only.
    CRITICAL: The component's export name must remain `DoortstroomPrognoseSection` (default export).
    CRITICAL: The props interface must remain `{ student: any; status: StatusResult }`.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose --grep DoortstroomPrognose</automated>
  </verify>
  <acceptance_criteria>
    - DoortstroomPrognoseSection.tsx does NOT import berekenPrognose
    - DoortstroomPrognoseSection.tsx contains "getNormenSync" and "detectTraject"
    - DoortstroomPrognoseSection.tsx contains "prognose-block" in JSX
    - DoortstroomPrognoseSection.tsx contains "prognose-criterion-row" in JSX
    - DoortstroomPrognoseSection.tsx contains "Nog geen scores beschikbaar" for the empty state
    - DoortstroomPrognoseSection.tsx does NOT contain any hex color literals (no '#[0-9a-fA-F]' patterns in the rewritten file)
    - npm test -- --grep DoortstroomPrognose passes (Wave 0 DoortstroomPrognoseSection tests turn GREEN)
    - npm test (full suite) exits 0
  </acceptance_criteria>
  <done>DoortstroomPrognoseSection fully rewritten with block layout; BJ1 and BJ2 paths implemented; empty state handled; Wave 0 DoortstroomPrognoseSection tests GREEN; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component → status.prognose prop | Data flows from DetailWeergave parent; component is read-only (no writes) |
| Component → getNormenSync() | Reads from in-memory store; no user input crosses this call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Spoofing | getNormenSync in render path | accept | getNormenSync reads from validated settings store (Phase 25 validation guards); no user-controlled value enters render without passing through store layer |
| T-29-05-02 | Tampering | DoortstroomPrognoseSection rewrite | mitigate | Props interface unchanged; parent (DetailWeergave) still passes same status + student — no regression risk for upstream callers |
| T-29-05-SC | Tampering | No new packages | accept | No npm installs; no slopcheck needed |
</threat_model>

<verification>
Final wave checks:
- `npm test -- --grep DoortstroomPrognose` exits 0 (Wave 0 tests GREEN)
- `npm test` full suite exits 0 (all 158+ original tests + all Phase 29 new tests pass)
- `grep "berekenPrognose" src/components/DoortstroomPrognoseSection.tsx` returns empty
- `grep "prognose-block" src/index.css` returns a match
- `grep -v "^//" src/components/DoortstroomPrognoseSection.tsx | grep "#[0-9a-fA-F]"` returns empty (no hardcoded hex in rewritten file)

Manual verification checklist (run in `npm run vite-dev` or `tauri dev`):
- Open a BJ2 student detail view — verify SBL, SBC, Negatief blocks render with headers and criterion rows
- Open a BJ1 student detail view — verify BJ2 doorstroom, Versneld SBC, Negatief blocks render
- Open a student with no scores (grijs tegel) — verify "Nog geen scores beschikbaar" appears in blocks
- Toggle dark mode — verify all block headers, criterion rows, status chips are readable
- Navigate klasoverzicht → detail → back — verify fade-in transition fires (150–200ms)
- Run `tauri dev` — verify nav-stripe gradient is visible in the top-right of the nav bar
</verification>

<success_criteria>
- DoortstroomPrognoseSection fully rewritten with per-traject block layout
- BJ2: SBL + SBC + Negatief blocks with criterion rows and 3-state color
- BJ1: BJ2 doorstroom + Versneld SBC + Negatief blocks
- Empty state: "Nog geen scores beschikbaar" per block
- Zero hardcoded hex in the rewritten component
- CSS block classes in index.css
- Wave 0 DoortstroomPrognoseSection tests GREEN
- Full test suite green
- All 6 requirements (UI-01, UI-02, UI-03, UI-04, FIX-01, FIX-02) have at least one implementation across Phase 29 plans
</success_criteria>

<output>
Create `.planning/phases/29-ui-streamlining-bugfixes/29-05-SUMMARY.md` when done
</output>
