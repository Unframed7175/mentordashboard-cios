---
phase: 29
plan: 02
type: execute
wave: 1
depends_on:
  - 29-01
files_modified:
  - src/index.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - FIX-01
must_haves:
  truths:
    - "#main-nav::after CSS block is deleted from index.css"
    - ".nav-stripe CSS class exists in index.css with correct geometry and gradient"
    - ".klas-tile padding is 1.25rem 1.5rem and gap is 0.75rem"
    - ".view-fade-in animation class exists with viewFadeIn keyframe"
    - "No new hardcoded hex values introduced in index.css"
  artifacts:
    - path: "src/index.css"
      provides: "nav-stripe class, tile padding update, fade-in animation, typography rules"
      contains: ".nav-stripe"
  key_links:
    - from: "src/index.css"
      to: "KlasTabStrip.tsx"
      via: ".nav-stripe class applied to DOM element added in Wave 2"
      pattern: "nav-stripe"
    - from: "src/index.css"
      to: "KlasOverzicht + DetailWeergave"
      via: ".view-fade-in class applied in Wave 2"
      pattern: "view-fade-in"
---

<objective>
Apply all CSS-only changes to src/index.css: delete the broken #main-nav::after block (FIX-01 CSS side), add the .nav-stripe replacement class, increase .klas-tile whitespace (UI-02), add .view-fade-in transition (UI-04), and confirm typography token rules across section titles (UI-01). This wave has zero TSX changes — executors touch only index.css.

Purpose: Establish the CSS foundation that Wave 2 TSX changes depend on. Keeping CSS changes isolated in Wave 1 makes any regressions easy to attribute.
Output: Updated src/index.css with 5 targeted changes. npm test remains green throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-ui-streamlining-bugfixes/29-CONTEXT.md
@.planning/phases/29-ui-streamlining-bugfixes/29-RESEARCH.md
@.planning/phases/29-ui-streamlining-bugfixes/29-PATTERNS.md
@.planning/phases/29-ui-streamlining-bugfixes/29-UI-SPEC.md

<interfaces>
<!-- Key CSS facts extracted from source audit in RESEARCH.md and PATTERNS.md -->

src/index.css confirmed values:
- Line ~270: #main-nav { position: relative; } — already set, nav-stripe will use position: absolute
- Lines 275–285: #main-nav::after block to DELETE entirely
- Line ~298: .nav-tab { z-index: 1; } — tabs must remain above nav-stripe (z-index: 0)
- Lines ~467–480: .klas-tile { padding: 1rem 1.125rem; gap: 0.5rem; } — increase to 1.25rem 1.5rem / 0.75rem
- Line ~74: --transition-base: 200ms ease; --transition-fast: 150ms ease; — use these tokens
- Line ~587: .detail-section-title { font-size: 0.6875rem; font-weight: 700; text-transform: uppercase; color: var(--text-faint); letter-spacing: 0.07em; } — canonical section title rule
- Lines ~1336+: .view-slide-in-right + @keyframes slideInFromRight — existing animation pattern to follow for new .view-fade-in
- Existing prefers-reduced-motion block — must add view-fade-in + viewFadeIn keyframe to it

Design token system (`:root` block):
- --text-primary, --text-secondary, --text-muted, --text-faint
- --bg-page, --bg-surface, --bg-surface-alt
- --status-groen-bg/text, --status-oranje-bg/text, --status-rood-bg/text
- --rag-groen, --rag-oranje, --rag-rood
- --transition-base: 200ms ease
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete #main-nav::after and add .nav-stripe class</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css lines 265–295 — read the exact #main-nav block and ::after block to understand the boundaries of what to delete and where to insert .nav-stripe
  </read_first>
  <action>
    In src/index.css:

    1. FIND and DELETE the entire `#main-nav::after` CSS rule block (lines ~275–285). The block starts with `#main-nav::after {` and ends with the matching `}`. Delete all lines including both braces. Do not touch adjacent rules.

    2. ADD the `.nav-stripe` CSS class immediately after the `#main-nav` rule block (within the same section of the file, section 4 nav styles). The class must contain exactly:
       - position: absolute
       - top: 0
       - right: 0
       - width: 140px
       - height: 52px
       - background: linear-gradient(to bottom-left, #009FE3 0%, #009FE3 25%, transparent 70%)
       - pointer-events: none
       - z-index: 0

    The gradient uses #009FE3 as a color literal (per UI-SPEC.md note: accent color literals are acceptable in gradient stops for predictable WebView2 rendering — do not replace with var(--accent)).

    Do NOT change any other CSS rules. Do NOT add or remove any other blocks.
  </action>
  <verify>
    <automated>grep -q "\.nav-stripe" src/index.css && grep -v "#main-nav::after" src/index.css > /dev/null && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css does NOT contain the string "#main-nav::after" anywhere
    - src/index.css contains ".nav-stripe {" followed by "position: absolute"
    - src/index.css contains "linear-gradient(to bottom-left, #009FE3"
    - src/index.css contains "pointer-events: none" within the .nav-stripe block
    - npm test exits 0
  </acceptance_criteria>
  <done>#main-nav::after block deleted; .nav-stripe class added with correct geometry, gradient, pointer-events: none, z-index: 0; npm test green</done>
</task>

<task type="auto">
  <name>Task 2: Tile padding/gap increase, view fade-in animation, typography pass</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css lines 460–500 — read current .klas-tile rule to confirm exact padding and gap values before modifying
    - src/index.css lines 1330–1360 — read current .view-slide-in-right and @keyframes slideInFromRight to use as the exact analog for .view-fade-in
    - src/index.css — search for @media (prefers-reduced-motion: reduce) block to know where to add the animation: none override for viewFadeIn
    - src/index.css lines 580–600 — read .detail-section-title to confirm current values before deciding whether a correction is needed
  </read_first>
  <action>
    Make three targeted edits to src/index.css:

    CHANGE 1 — .klas-tile padding and gap (UI-02, per D-UI-02 and UI-SPEC.md spacing table):
    Find the `.klas-tile` rule. Change `padding: 1rem 1.125rem` to `padding: 1.25rem 1.5rem`. Change `gap: 0.5rem` to `gap: 0.75rem`. No other properties on this rule change.

    Note: CONTEXT.md D-UI-02 refers to this as "leerling-tegel" (the component name). The actual CSS class is `.klas-tile`, verified at index.css line ~471. The plan targets the correct CSS class name.

    CHANGE 2 — .view-fade-in animation (UI-04, per D-UI-04):
    After the existing `.view-slide-in-right` + `@keyframes slideInFromRight` block, add:
    - CSS class `.view-fade-in` with property: `animation: viewFadeIn var(--transition-base) ease forwards`
    - Keyframe `@keyframes viewFadeIn`: from { opacity: 0; transform: translateY(4px); } to { opacity: 1; transform: translateY(0); }
    Use var(--transition-base) — do NOT hardcode 200ms.

    CHANGE 3 — prefers-reduced-motion coverage (UI-04):
    Find the existing `@media (prefers-reduced-motion: reduce)` block in index.css. Inside it, add:
    `.view-fade-in { animation: none; }`
    This ensures the new animation respects the OS reduced-motion preference, consistent with the existing pattern for .view-slide-in-right.

    TYPOGRAPHY CHECK (UI-01 — source audit resolved):
    The RESEARCH.md source audit confirms that body text (0.875rem/400) and label tokens (0.75rem/600) are already consistent across all views via CSS custom properties — no corrections are needed for those categories.

    The only corrective action for UI-01 is `.detail-section-title`. Read the existing rule. If it matches the canonical spec exactly:
      font-size: 0.6875rem; font-weight: 700; text-transform: uppercase; color: var(--text-faint); letter-spacing: 0.06em
    leave it unchanged. If any property deviates from this spec (including if letter-spacing differs — 0.07em in the current source is a deviation from the 0.06em spec), correct it to match exactly. Document the finding (match or correction) in the SUMMARY.

    Do NOT add any new font-size declarations or override any other typography rules. The audit scope for UI-01 is limited to .detail-section-title only.
  </action>
  <verify>
    <automated>grep -q "padding: 1\.25rem 1\.5rem" src/index.css && grep -q "viewFadeIn" src/index.css && grep -q "font-size: 0\.6875rem" src/index.css && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css .klas-tile block contains "padding: 1.25rem 1.5rem" and "gap: 0.75rem"
    - src/index.css contains ".view-fade-in {" with "animation: viewFadeIn var(--transition-base)"
    - src/index.css contains "@keyframes viewFadeIn" with "from { opacity: 0" and "to { opacity: 1"
    - src/index.css prefers-reduced-motion block contains ".view-fade-in { animation: none"
    - Grep confirms .detail-section-title in index.css contains font-size: 0.6875rem and letter-spacing: 0.06em
    - npm test exits 0
    - src/index.css does NOT contain new hardcoded hex values (any new hex introduced in this task must be flagged as a deviation in the SUMMARY)
  </acceptance_criteria>
  <done>.klas-tile updated to 1.25rem/1.5rem/0.75rem; .view-fade-in + @keyframes viewFadeIn added after .view-slide-in-right; prefers-reduced-motion override added; .detail-section-title verified/corrected to 0.6875rem/0.06em spec; npm test green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file → browser render | CSS changes are purely visual; no user data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Tampering | src/index.css z-index stack | mitigate | .nav-stripe must have z-index: 0, .nav-tab must retain z-index: 1 — verify both values present after edit |
| T-29-02-SC | Tampering | No new packages | accept | No npm installs in this plan; no slopcheck needed |
</threat_model>

<verification>
After both tasks: `npm test` exits 0. The wave is considered complete only when:
- `grep "#main-nav::after" src/index.css` returns empty
- `grep "\.nav-stripe" src/index.css` returns a match
- `grep "padding: 1\.25rem 1\.5rem" src/index.css` returns a match inside .klas-tile
- `grep "viewFadeIn" src/index.css` returns two matches (class + keyframe)
- `grep "font-size: 0\.6875rem" src/index.css` and `grep "letter-spacing: 0\.06em" src/index.css` both return a match on the .detail-section-title rule
</verification>

<success_criteria>
- src/index.css has #main-nav::after deleted, .nav-stripe class added
- .klas-tile padding: 1.25rem 1.5rem; gap: 0.75rem
- .view-fade-in + @keyframes viewFadeIn added, prefers-reduced-motion covered
- .detail-section-title confirmed/corrected to font-size: 0.6875rem; letter-spacing: 0.06em
- No new hardcoded hex values added
- npm test green (158+ tests)
</success_criteria>

<output>
Create `.planning/phases/29-ui-streamlining-bugfixes/29-02-SUMMARY.md` when done
</output>
