---
phase: 02-excel-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - parsers/excel.js
  - utils/datamodel.js
autonomous: true
requirements:
  - XLS-02
  - XLS-03
  - XLS-04

must_haves:
  truths:
    - "parseVerzuimTime('107u24m') returns 6444 (107*60+24)"
    - "parseVerzuimTime('0u00m') returns 0"
    - "parseVerzuimTime('2u05m') returns 125"
    - "normalizeNaam('van den Dool, A.B.C. (Abigail)') matches 'van den dool, a.b.c. (abigail)'"
    - "parseExcelFile extracts verzuim fields for each row: aanwezigheid, geoorloofd, ongeoorloofd, totaal, laatsteMelding"
    - "mergeVerzuim links verzuim records to existing students via leerlingId or normalized naam"
  artifacts:
    - path: "parsers/excel.js"
      provides: "Excel parser with SheetJS, time format parsing, column extraction"
      contains: "window.parseExcelFile"
    - path: "utils/datamodel.js"
      provides: "Verzuim merge function and name normalization"
      contains: "window.mergeVerzuim"
  key_links:
    - from: "parsers/excel.js"
      to: "utils/datamodel.js"
      via: "window.mergeVerzuim called after parsing"
      pattern: "window\\.mergeVerzuim"
    - from: "utils/datamodel.js"
      to: "window.appState.students"
      via: "matching by leerlingId or normalizeNaam"
      pattern: "normalizeNaam"
---

<objective>
Create the Excel verzuim parser and extend the data model with verzuim fields and student matching.

Purpose: This is the core logic for Phase 2 — parsing Dutch school absence Excel exports and linking verzuim data to students already imported from PDFs.
Output: parsers/excel.js (Excel parser) and updated utils/datamodel.js (verzuim merge + name normalization)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@utils/datamodel.js
@parsers/pdf.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parsers/excel.js — Excel verzuim parser</name>
  <files>parsers/excel.js</files>
  <read_first>
    - D:/Downloads/dashboard-2/parsers/pdf.js (pattern reference — how parsers are structured in this project)
    - D:/Downloads/dashboard-2/utils/datamodel.js (StudentRecord typedef — understand existing data shape)
    - D:/Downloads/dashboard-2/utils/schema.js (global pattern reference)
  </read_first>
  <action>
Create parsers/excel.js as a classic script (NOT ES module — consistent with schema.js and datamodel.js pattern, loaded via plain script tag).

The file must:

1. Define window.parseVerzuimTime(str) that converts Dutch time format to total minutes:
   - Input: string like "107u24m", "0u00m", "2u05m"
   - Parse: extract hours before "u" and minutes before "m"
   - Return: (hours * 60) + minutes as integer
   - Edge cases: null/undefined/empty string returns 0, string without "u" returns 0

2. Define window.parseExcelFile(file) as an async function:
   - Takes a File object (from file input)
   - Reads the file as ArrayBuffer using FileReader wrapped in a Promise
   - Calls XLSX.read(data, { type: 'array' }) to parse (XLSX loaded globally from CDN)
   - Gets the first sheet: workbook.Sheets[workbook.SheetNames[0]]
   - Converts to JSON: XLSX.utils.sheet_to_json(sheet)
   - For each row, extract a verzuim record object:
     ```
     {
       naam: row['Naam'] || row['naam'] || '',
       leerlingnummer: String(row['Leerlingnummer'] || row['leerlingnummer'] || row['Llnr'] || ''),
       aanwezigheid: window.parseVerzuimTime(String(row['Aanwezigheid'] || row['aanwezigheid'] || '')),
       geoorloofd: window.parseVerzuimTime(String(row['Geoorloofd'] || row['geoorloofd'] || row['Geoorloofd verzuim'] || '')),
       ongeoorloofd: window.parseVerzuimTime(String(row['Ongeoorloofd'] || row['ongeoorloofd'] || row['Ongeoorloofd verzuim'] || '')),
       totaal: window.parseVerzuimTime(String(row['Totaal'] || row['totaal'] || row['Totaal verzuim'] || '')),
       laatsteMelding: String(row['Laatste melding'] || row['laatste melding'] || row['Laatst gemeld'] || '')
     }
     ```
   - Return array of verzuim record objects
   - Throw Error with descriptive Dutch message if XLSX is not loaded or file cannot be parsed

3. Add console.log('[excel.js] Excel parser loaded') at end of file.

IMPORTANT: Do NOT use ES module import/export. Use window.* globals consistent with the rest of the codebase (except parsers/pdf.js which needs ESM for PDF.js).
  </action>
  <verify>
    <automated>cd "D:/Downloads/dashboard-2" && grep -c "window.parseExcelFile" parsers/excel.js && grep -c "window.parseVerzuimTime" parsers/excel.js && grep -c "XLSX.read" parsers/excel.js && grep -c "XLSX.utils.sheet_to_json" parsers/excel.js</automated>
  </verify>
  <acceptance_criteria>
    - parsers/excel.js exists and is NOT an ES module (no import/export statements)
    - Contains window.parseVerzuimTime function that handles "107u24m" format
    - Contains window.parseExcelFile async function that uses XLSX global
    - parseVerzuimTime handles: "107u24m" -> 6444, "0u00m" -> 0, "2u05m" -> 125, "" -> 0, null -> 0
    - parseExcelFile reads File as ArrayBuffer, parses with XLSX.read, extracts rows with column name fallbacks
    - Each verzuim record has: naam, leerlingnummer, aanwezigheid, geoorloofd, ongeoorloofd, totaal, laatsteMelding
    - File ends with console.log('[excel.js] Excel parser loaded')
  </acceptance_criteria>
  <done>parsers/excel.js created with parseVerzuimTime and parseExcelFile functions exposed as window globals</done>
</task>

<task type="auto">
  <name>Task 2: Extend datamodel.js — verzuim fields, normalizeNaam, mergeVerzuim</name>
  <files>utils/datamodel.js</files>
  <read_first>
    - D:/Downloads/dashboard-2/utils/datamodel.js (MUST read current state before modifying)
  </read_first>
  <action>
Add the following to utils/datamodel.js, AFTER the existing code (do not modify existing functions):

1. Add a Verzuim typedef in JSDoc above the new code:
   ```
   /**
    * @typedef {Object} Verzuim
    * @property {number} aanwezigheid - Attendance in minutes
    * @property {number} geoorloofd - Excused absence in minutes
    * @property {number} ongeoorloofd - Unexcused absence in minutes
    * @property {number} totaal - Total absence in minutes
    * @property {string} laatsteMelding - Date of last absence report
    */
   ```

2. Add window.normalizeNaam(naam) function:
   - Convert to lowercase
   - Replace multiple whitespace with single space
   - Trim leading/trailing whitespace
   - Return the normalized string
   - This handles "van den Dool" vs "van den dool" matching (per XLS-04 requirement)

3. Add window.mergeVerzuim(verzuimRecords) function:
   - Takes array of verzuim records (output of parseExcelFile)
   - For each verzuim record, find matching student in window.appState.students:
     a. First try: match by leerlingnummer === student.leerlingId (exact string match)
     b. If no match: match by normalizeNaam(verzuim.naam) === normalizeNaam(student.naam)
   - If match found: set student.verzuim = { aanwezigheid, geoorloofd, ongeoorloofd, totaal, laatsteMelding }
   - Track results: { matched: number, unmatched: string[] } where unmatched contains names of verzuim records that could not be linked
   - Return the results object
   - Console.log each match for debugging: `[mergeVerzuim] Gekoppeld: ${naam} -> ${student.naam}`
   - Console.warn each unmatched: `[mergeVerzuim] Niet gekoppeld: ${naam}`

4. Add window.getVerzuim(leerlingId) convenience function:
   - Returns student.verzuim for the student with matching leerlingId, or null if not found

Do NOT change any existing code in the file. Append all new code after the existing console.log line.
  </action>
  <verify>
    <automated>cd "D:/Downloads/dashboard-2" && grep -c "window.normalizeNaam" utils/datamodel.js && grep -c "window.mergeVerzuim" utils/datamodel.js && grep -c "window.getVerzuim" utils/datamodel.js && grep -c "student.verzuim" utils/datamodel.js</automated>
  </verify>
  <acceptance_criteria>
    - utils/datamodel.js contains all original code unchanged (window.appState, window.addStudent, window.getStudentScores)
    - New Verzuim typedef added with aanwezigheid, geoorloofd, ongeoorloofd, totaal, laatsteMelding fields
    - window.normalizeNaam exists: lowercases, collapses whitespace, trims
    - window.mergeVerzuim exists: accepts verzuim array, matches by leerlingId first then by normalized naam, sets student.verzuim object, returns { matched, unmatched }
    - window.getVerzuim exists: returns student.verzuim by leerlingId or null
    - grep "normalizeNaam" returns at least 2 hits (definition + usage in mergeVerzuim)
  </acceptance_criteria>
  <done>datamodel.js extended with Verzuim type, normalizeNaam, mergeVerzuim, and getVerzuim — all as window globals, no existing code changed</done>
</task>

</tasks>

<verification>
- parsers/excel.js exists with window.parseVerzuimTime and window.parseExcelFile
- utils/datamodel.js has window.normalizeNaam, window.mergeVerzuim, window.getVerzuim added without breaking existing code
- No ES module syntax in either file (no import/export)
- All functions use window.* global pattern
</verification>

<success_criteria>
- parseVerzuimTime correctly converts "107u24m" to 6444 minutes
- parseExcelFile reads .xls/.xlsx files via SheetJS and returns structured verzuim records
- mergeVerzuim links verzuim records to students by leerlingId or case-insensitive name match
- normalizeNaam handles Dutch name variations (case, whitespace)
</success_criteria>

<output>
After completion, create `.planning/phases/02-excel-import/02-01-SUMMARY.md`
</output>
