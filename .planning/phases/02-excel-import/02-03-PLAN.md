---
phase: 02-excel-import
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app.js
autonomous: true
gap_closure: true
requirements:
  - XLS-01

must_haves:
  truths:
    - "If SheetJS CDN failed to load, the Excel button is disabled and shows Dutch error text instead of being clickable"
  artifacts:
    - path: "app.js"
      provides: "XLSX availability guard before Excel button click handler"
      contains: "typeof window.XLSX"
  key_links:
    - from: "app.js"
      to: "excelChooseBtn"
      via: "XLSX guard disabling button if CDN fails"
      pattern: "window\\.XLSX.*undefined"
---

<objective>
Close the single verification gap from 02-VERIFICATION.md: add the missing XLSX CDN load guard in app.js.

Purpose: Plan 02-02 required that if SheetJS CDN fails to load, `excelChooseBtn` is disabled with Dutch error text. The current code unconditionally wires the click handler (line 418). This plan adds the guard.
Output: Updated app.js with XLSX availability check before attaching the click handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-excel-import/02-VERIFICATION.md
@.planning/phases/02-excel-import/02-02-SUMMARY.md

<interfaces>
<!-- The fix targets app.js lines 412-418. Current code: -->

Line 412: const excelFileInput = document.getElementById('excel-file-input');
Line 413: const excelChooseBtn = document.getElementById('excel-choose-btn');
Line 414: const excelResults   = document.getElementById('excel-import-results');
Line 415: const excelResultTxt = document.getElementById('excel-result-text');
Line 416: const excelUnmatched = document.getElementById('excel-unmatched-list');
Line 417: (blank)
Line 418: excelChooseBtn.addEventListener('click', () => excelFileInput.click());

<!-- Line 418 must be REPLACED with the XLSX guard block. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add XLSX CDN load guard before Excel button click handler</name>
  <files>app.js</files>
  <read_first>
    - D:/Downloads/dashboard-2/app.js (lines 408-420 — the Excel import section header and DOM references)
  </read_first>
  <action>
In app.js, REPLACE line 418:

```javascript
  excelChooseBtn.addEventListener('click', () => excelFileInput.click());
```

WITH this guard block:

```javascript
  // Guard: if SheetJS CDN failed to load, disable button with Dutch error
  if (typeof window.XLSX === 'undefined') {
    excelChooseBtn.disabled = true;
    excelChooseBtn.textContent = 'SheetJS niet geladen. Ververs de pagina.';
  } else {
    excelChooseBtn.addEventListener('click', () => excelFileInput.click());
  }
```

This is a single-line replacement expanding to 6 lines. The surrounding code (DOM references above on lines 412-416, and the change handler starting at line 420) stays untouched.

Use the Edit tool targeting line 418. The old text to match is:
```
  excelChooseBtn.addEventListener('click', () => excelFileInput.click());
```

The new text is the 6-line guard block above.

Do NOT modify any other lines in app.js.
  </action>
  <verify>
    <automated>cd "D:/Downloads/dashboard-2" && grep -n "typeof window.XLSX" app.js && grep -n "SheetJS niet geladen" app.js && grep -n "excelChooseBtn.disabled = true" app.js</automated>
  </verify>
  <acceptance_criteria>
    - app.js contains the string `typeof window.XLSX === 'undefined'` (grep pattern: `typeof window.XLSX`)
    - app.js contains the string `SheetJS niet geladen. Ververs de pagina.` (grep pattern: `SheetJS niet geladen`)
    - app.js contains `excelChooseBtn.disabled = true` inside the XLSX undefined branch
    - The click handler `excelChooseBtn.addEventListener('click'` is inside an `else` block, only wired when XLSX is available
    - The change handler on `excelFileInput` (line 420+) is NOT inside the if/else — it remains unconditional (it has its own internal parseExcelFile guard)
    - No other lines in app.js are modified
  </acceptance_criteria>
  <done>app.js has XLSX CDN load guard: button disabled with Dutch error text when window.XLSX is undefined, click handler only attached when XLSX is available</done>
</task>

</tasks>

<verification>
- `grep "typeof window.XLSX" app.js` returns a match
- `grep "SheetJS niet geladen" app.js` returns a match
- `grep "excelChooseBtn.disabled = true" app.js` returns a match
- The click handler is inside an else block (only fires when XLSX loaded)
- Human verification: open index.html with CDN blocked, button should be disabled with error text
</verification>

<success_criteria>
- The XLSX CDN load guard is present in app.js
- When XLSX is undefined: button is disabled, text reads "SheetJS niet geladen. Ververs de pagina."
- When XLSX is defined: click handler wired normally (existing behavior preserved)
- All other app.js code unchanged
- Closes the single gap from 02-VERIFICATION.md (truth 5 of plan 02-02)
</success_criteria>

<output>
After completion, create `.planning/phases/02-excel-import/02-03-SUMMARY.md`
</output>
