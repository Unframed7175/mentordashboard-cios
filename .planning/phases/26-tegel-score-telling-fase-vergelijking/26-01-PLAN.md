---
phase: 26-tegel-score-telling-fase-vergelijking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/LeerlingTegel.tsx
autonomous: true
requirements:
  - TEGEL-01
  - TEGEL-02

must_haves:
  truths:
    - "Score-telling row renders inside the tile for students with a non-grijs status"
    - "Score-telling is completely absent from DOM when status.kleur === 'grijs'"
    - "Tile layout order is: naam → badge → score-telling → verzuim bar"
    - "LeerlingTegel accepts optional 'trend' prop without breaking existing renders"
    - "CSS classes .score-telling, .trend-pijl, .trend-op, .trend-neer exist in index.css"
  artifacts:
    - path: src/components/LeerlingTegel.tsx
      provides: "score-telling render + trend prop interface"
      contains: "trend?: 'op' | 'neer' | null"
    - path: src/index.css
      provides: "CSS classes for score-telling row and trend arrows"
      contains: ".score-telling"
  key_links:
    - from: src/components/LeerlingTegel.tsx
      to: src/index.css
      via: "className references"
      pattern: "className=\"score-telling\""
---

<objective>
Extend LeerlingTegel.tsx with the score-telling row and add the four required CSS classes to index.css.

Purpose: Delivers the full tile rendering capability for TEGEL-01 and TEGEL-02. The tile will render "14/19 ≥V · 1 O" below the status badge and accept an optional trend prop (arrow not yet wired to live data — that is Plan 02).

Output: Updated LeerlingTegel.tsx with new prop + render logic; four new CSS classes appended to src/index.css after the .mvb-* block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-tegel-score-telling-fase-vergelijking/26-CONTEXT.md
@.planning/phases/26-tegel-score-telling-fase-vergelijking/26-UI-SPEC.md
@.planning/phases/26-tegel-score-telling-fase-vergelijking/26-PATTERNS.md

<interfaces>
<!-- Read-only reference: src/utils/status.ts exports consumed by LeerlingTegel -->

STATUS_VOLGORDE: Record<string, number> = { rood: 0, oranje: 1, groen: 2, blauw: 3, grijs: 4 }
StatusKleur = 'groen' | 'oranje' | 'rood' | 'blauw' | 'grijs'

interface StatusResult {
  kleur:    StatusKleur;
  label:    string;
  prognose: any;  // .totaalVoldoendeOfHoger: number, .totaalOnvoldoende: number
}

<!-- Current LeerlingTegelProps (lines 10-14 of LeerlingTegel.tsx): -->
interface LeerlingTegelProps {
  student: any;
  status: StatusResult;
  onClick: () => void;
}

<!-- CSS tile classes in src/index.css (insertion point after line 503): -->
<!-- .mini-verzuim-bar ends at line 499, .mvb-* segment lines 500-503 -->
<!-- New classes slot in after line 503 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CSS classes for score-telling row and trend arrows</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css — read lines 430–510 to confirm the exact end of the .mvb-* block and the comment header that follows; new classes append after line 503 (after .mvb-ongeoorloofd rule)
    - .planning/phases/26-tegel-score-telling-fase-vergelijking/26-PATTERNS.md — lines 234–267 contain the exact CSS to append verbatim
    - .planning/phases/26-tegel-score-telling-fase-vergelijking/26-UI-SPEC.md — lines 117–156 contain the canonical CSS spec
  </read_first>
  <behavior>
    - .score-telling class exists in src/index.css with display:flex, align-items:center, gap:4px, font-size:0.75rem, font-weight:400, line-height:1.4, color:var(--text-muted), padding-left:0.375rem
    - .trend-pijl class exists with display:inline-block, flex-shrink:0, width:0, height:0, vertical-align:middle
    - .trend-op class exists with border-left:4px solid transparent, border-right:4px solid transparent, border-bottom:5px solid var(--rag-groen)
    - .trend-neer class exists with border-left:4px solid transparent, border-right:4px solid transparent, border-top:5px solid var(--rag-rood)
  </behavior>
  <action>
    Append the following four CSS rule blocks to src/index.css immediately after the .mvb-ongeoorloofd rule (after the line ".mvb-ongeoorloofd{ background: #F97316; }"), before the next section comment block:

    Section comment: "/* -- Phase 26: Score-telling & Trend-pijl */"

    .score-telling: display:flex; align-items:center; gap:4px; font-size:0.75rem; font-weight:400; line-height:1.4; color:var(--text-muted); padding-left:0.375rem (matches .klas-tile-naam indentation — pre-existing non-4pt value from existing codebase, not a new spacing contract value).

    .trend-pijl: display:inline-block; flex-shrink:0; width:0; height:0; vertical-align:middle. No margin-top — flex parent's align-items:center handles vertical centering.

    .trend-op: border-left:4px solid transparent; border-right:4px solid transparent; border-bottom:5px solid var(--rag-groen). Use CSS variable, not hardcoded #22C55E.

    .trend-neer: border-left:4px solid transparent; border-right:4px solid transparent; border-top:5px solid var(--rag-rood). Use CSS variable, not hardcoded #EF4444.

    Do NOT add dark-mode overrides — per UI-SPEC.md RAG colors are stable across modes.
    Do NOT add margin-top to .score-telling — the .klas-tile gap:0.5rem (line 442) handles vertical spacing.
  </action>
  <verify>
    <automated>grep -c "score-telling" src/index.css</automated>
  </verify>
  <done>
    src/index.css contains all four new rule blocks: .score-telling, .trend-pijl, .trend-op, .trend-neer. Each uses CSS custom properties (var(--rag-groen), var(--rag-rood), var(--text-muted)) rather than hardcoded hex values.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend LeerlingTegel with trend prop and score-telling render</name>
  <files>src/components/LeerlingTegel.tsx</files>
  <read_first>
    - src/components/LeerlingTegel.tsx — read entire file (65 lines); understand the existing miniBar null-node pattern (lines 18-39) which the scoreTelling variable must mirror exactly
    - .planning/phases/26-tegel-score-telling-fase-vergelijking/26-PATTERNS.md — lines 23-110 contain the exact insertion pattern including the null-node approach and the IIFE alternative; prefer the null-node variable style (let scoreTelling: React.ReactNode = null) to match the existing miniBar pattern in the same file
    - .planning/phases/26-tegel-score-telling-fase-vergelijking/26-UI-SPEC.md — lines 88-113 contain the exact HTML structure spec; lines 196-232 contain the copywriting contract (exact Unicode characters and aria-label format)
  </read_first>
  <behavior>
    - LeerlingTegelProps has new optional field: trend?: 'op' | 'neer' | null
    - Component destructures trend from props alongside student, status, onClick
    - When status.kleur === 'grijs': scoreTelling is null; score-telling div is NOT in DOM
    - When status.kleur !== 'grijs' and trend === 'op': div.score-telling contains span.trend-pijl.trend-op (aria-hidden="true") + span.score-telling-tekst
    - When status.kleur !== 'grijs' and trend === 'neer': div.score-telling contains span.trend-pijl.trend-neer (aria-hidden="true") + span.score-telling-tekst
    - When status.kleur !== 'grijs' and trend is null/undefined: div.score-telling contains only span.score-telling-tekst (no arrow span in DOM)
    - Score text format: "{v}/{totaal} ≥V · {o} O" using U+2265 for ≥ and U+00B7 for · and uppercase O
    - div.score-telling carries aria-label: "Trend omhoog: {v} van {totaal} deelgebieden voldoende of hoger, {o} onvoldoende" (trend=op), "Trend omlaag: ..." (trend=neer), or "{v} van {totaal} deelgebieden voldoende of hoger, {o} onvoldoende" (no trend)
    - totaalDeelgebieden = status.prognose.totaalVoldoendeOfHoger + status.prognose.totaalOnvoldoende
    - JSX order inside .klas-tile: span.klas-tile-naam → span.status-badge → {scoreTelling} → {miniBar}
    - Existing behavior (student naam, status badge, miniBar, onClick, keyboard handler) is unchanged
  </behavior>
  <action>
    Modify src/components/LeerlingTegel.tsx:

    1. Add trend to LeerlingTegelProps interface after onClick: `trend?: 'op' | 'neer' | null;`

    2. Add trend to function destructuring parameter: `{ student, status, onClick, trend }`

    3. After the miniBar block and before the handleKeyDown function, declare the scoreTelling null-node variable using the same pattern as miniBar:
       - `let scoreTelling: React.ReactNode = null;`
       - Inside `if (status.kleur !== 'grijs')` block: compute totaalDeelgebieden, v (totaalVoldoendeOfHoger), o (totaalOnvoldoende), ariaLabel string, then assign scoreTelling the JSX.
       - ariaLabel when trend === 'op': "Trend omhoog: {v} van {totaalDeelgebieden} deelgebieden voldoende of hoger, {o} onvoldoende"
       - ariaLabel when trend === 'neer': "Trend omlaag: {v} van {totaalDeelgebieden} deelgebieden voldoende of hoger, {o} onvoldoende"
       - ariaLabel otherwise: "{v} van {totaalDeelgebieden} deelgebieden voldoende of hoger, {o} onvoldoende"
       - Score text uses the exact characters: ≥ (U+2265), · (U+00B7 interpunct, NOT hyphen/bullet), uppercase O. Single space before and after the interpunct.
       - Arrow span: `<span className="trend-pijl trend-op" aria-hidden="true" />` (only when trend === 'op')
       - Arrow span: `<span className="trend-pijl trend-neer" aria-hidden="true" />` (only when trend === 'neer')
       - Score span: `<span className="score-telling-tekst">{v}/{totaalDeelgebieden} ≥V{' · '}{o} O</span>`

    4. In the return JSX, insert `{scoreTelling}` between the status-badge span and `{miniBar}`.

    Do NOT use an IIFE pattern — use the null-node variable style to match existing miniBar code style.
    Do NOT add any inline styles to the score-telling row — all styling is handled by CSS classes.
    Do NOT import any new modules — StatusResult is already imported from '../utils/status'.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `npx tsc --noEmit` passes with 0 errors. LeerlingTegelProps includes `trend?: 'op' | 'neer' | null`. The component renders the score-telling div only when status.kleur !== 'grijs'. Trend arrows are present/absent based on trend prop value. Existing tile behavior (click, keyboard, miniBar) is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| berekenStatus output → render | Computed prognose values flow into displayed numbers; malformed prognose could render NaN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | score-telling text | accept | Values derived from prognose computation already displayed in DetailWeergave; no new data exposure |
| T-26-02 | Tampering | CSS class injection | accept | className values are hardcoded string literals, not user-supplied |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — pure CSS + React prop extension |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (0 errors)
- `grep -c "score-telling" src/index.css` returns >= 1
- `grep -c "trend-pijl" src/index.css` returns >= 1
- `grep -c "trend?: " src/components/LeerlingTegel.tsx` returns >= 1
- `npm test` passes (all existing tests remain green — no behavior changes to tested code)
</verification>

<success_criteria>
- LeerlingTegel accepts trend prop without TypeScript error
- When rendered with a non-grijs status, the tile shows "{v}/{total} ≥V · {o} O" below the badge
- When rendered with status.kleur === 'grijs', no score-telling div is in the DOM
- CSS classes .score-telling, .trend-pijl, .trend-op, .trend-neer exist in src/index.css
- All existing tests pass (npx vitest run)
</success_criteria>

<output>
Create `.planning/phases/26-tegel-score-telling-fase-vergelijking/26-01-SUMMARY.md` when done
</output>
