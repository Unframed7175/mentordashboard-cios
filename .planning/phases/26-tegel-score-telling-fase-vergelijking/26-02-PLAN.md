---
phase: 26-tegel-score-telling-fase-vergelijking
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - src/components/KlasOverzicht.tsx
autonomous: true
requirements:
  - TREND-01
  - TREND-02
  - TREND-03
  - TREND-04

must_haves:
  truths:
    - "KlasOverzicht computes trendMap via useMemo using getAllRecordsForStudent + berekenStatus + STATUS_VOLGORDE"
    - "Trend is 'op' when rank(fase2) > rank(fase1) and neither fase is grijs"
    - "Trend is 'neer' when rank(fase2) < rank(fase1) and neither fase is grijs"
    - "Trend is null when fewer than 2 records, same rank, or either fase is grijs"
    - "Trend is null when records[0].periode equals records[last].periode (duplicate import guard)"
    - "Every LeerlingTegel receives trend={trendMap.get(s.leerlingId) ?? null}"
  artifacts:
    - path: src/components/KlasOverzicht.tsx
      provides: "trendMap useMemo computation + trend prop wiring"
      contains: "trendMap"
  key_links:
    - from: src/components/KlasOverzicht.tsx
      to: utils/klassen.ts
      via: "getAllRecordsForStudent import"
      pattern: "getAllRecordsForStudent"
    - from: src/components/KlasOverzicht.tsx
      to: src/components/LeerlingTegel.tsx
      via: "trend prop"
      pattern: "trend=\\{trendMap\\.get"
---

<objective>
Wire live trend data into KlasOverzicht.tsx by adding the trendMap useMemo computation and passing the trend prop to each LeerlingTegel.

Purpose: Completes the phase — connects the rendering capability delivered in Plan 01 to real per-student data. The trend arrows and score-telling will now respond to actual multi-fase student records.

Output: Updated KlasOverzicht.tsx with getAllRecordsForStudent import, trendMap useMemo (same dependency array as statusMap), and trend prop on every LeerlingTegel render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-tegel-score-telling-fase-vergelijking/26-CONTEXT.md
@.planning/phases/26-tegel-score-telling-fase-vergelijking/26-UI-SPEC.md
@.planning/phases/26-tegel-score-telling-fase-vergelijking/26-PATTERNS.md
@.planning/phases/26-tegel-score-telling-fase-vergelijking/26-01-SUMMARY.md

<interfaces>
<!-- Read-only reference: utils/klassen.ts -->
export function getAllRecordsForStudent(leerlingId: string): any[]
  // Returns all period records for leerlingId in the active klas, sorted oldest-first
  // by s.periode (localeCompare). records[0] = fase 1, records[length-1] = fase 2.
  // Returns [] if no active klas or no matching records.

<!-- STATUS_VOLGORDE from src/utils/status.ts (already imported in KlasOverzicht) -->
export const STATUS_VOLGORDE: Record<string, number> = {
  rood: 0, oranje: 1, groen: 2, blauw: 3, grijs: 4
}

<!-- berekenStatus from src/utils/status.ts (already imported) -->
export function berekenStatus(student: any): StatusResult
  // .kleur is 'grijs' when student has no scores

<!-- Existing statusMap useMemo in KlasOverzicht.tsx (lines 31-37) -->
const statusMap = useMemo(() => {
  const students = getActiveStudents();
  const m = new Map<string, ReturnType<typeof berekenStatus>>();
  for (const s of students) m.set(s.leerlingId, berekenStatus(s));
  return m;
// eslint-disable-next-line react-hooks/exhaustive-deps
}, [refreshKey, allStudents.length]);

<!-- Existing LeerlingTegel render (lines 183-193) -->
{sorted.map(s => {
  const status = statusMap.get(s.leerlingId)!;
  return (
    <LeerlingTegel
      key={s.leerlingId}
      student={s}
      status={status}
      onClick={() => onSelectStudent(s.leerlingId, sorted.map(r => r.leerlingId))}
    />
  );
})}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add getAllRecordsForStudent import and trendMap useMemo to KlasOverzicht</name>
  <files>src/components/KlasOverzicht.tsx</files>
  <read_first>
    - src/components/KlasOverzicht.tsx — read entire file; confirm current import on line 8 (getActiveStudents, klassenState, deleteKlas from utils/klassen) and statusMap useMemo block (lines 31-37); trendMap must use identical dependency array pattern
    - .planning/phases/26-tegel-score-telling-fase-vergelijking/26-PATTERNS.md — lines 113-196 contain the exact trendMap computation to implement, including the full algorithm with grijs guards and rank comparison
    - utils/klassen.ts — confirm getAllRecordsForStudent export exists and sort order (oldest-first by periode localeCompare)
  </read_first>
  <behavior>
    - Import line for utils/klassen adds getAllRecordsForStudent: `import { getActiveStudents, getAllRecordsForStudent, klassenState, deleteKlas } from '../../utils/klassen';`
    - trendMap useMemo exists immediately after statusMap useMemo in the file
    - trendMap type: Map<string, 'op' | 'neer' | null>
    - For each student: getAllRecordsForStudent(s.leerlingId) called; if records.length < 2 → null
    - Distinct-period guard: if records[0].periode === records[records.length - 1].periode → null (prevents a student imported twice in the same periode from producing a false two-fase comparison)
    - berekenStatus called on records[0] (fase1Status) and records[records.length - 1] (fase2Status)
    - If fase1Status.kleur === 'grijs' OR fase2Status.kleur === 'grijs' → null (per D-09)
    - rank1 = STATUS_VOLGORDE[fase1Status.kleur], rank2 = STATUS_VOLGORDE[fase2Status.kleur]
    - rank2 > rank1 → 'op'; rank2 < rank1 → 'neer'; rank2 === rank1 → null
    - trendMap dependency array is identical to statusMap: [refreshKey, allStudents.length] with the same eslint-disable-next-line comment
    - Two records with the same periode string produce trend null (the distinct-period guard fires before berekenStatus is called)
  </behavior>
  <action>
    Make two changes to src/components/KlasOverzicht.tsx:

    1. On the existing utils/klassen import line (line 8), add getAllRecordsForStudent to the destructured imports. The import path stays '../../utils/klassen'. Result: `import { getActiveStudents, getAllRecordsForStudent, klassenState, deleteKlas } from '../../utils/klassen';`

    2. Directly after the closing of the statusMap useMemo block (after the `}, [refreshKey, allStudents.length]);` line), add the trendMap useMemo:

    - const trendMap = useMemo(() => { ... }, [refreshKey, allStudents.length])
    - Inner body: declare m as new Map<string, 'op' | 'neer' | null>(); iterate getActiveStudents() with a for...of loop (NOT forEach — match the statusMap style); for each student:

      Step A — length guard: call getAllRecordsForStudent(s.leerlingId); if records.length < 2 → m.set(s.leerlingId, null); continue;

      Step B — distinct-period guard: if records[0].periode === records[records.length - 1].periode → m.set(s.leerlingId, null); continue; (This covers the case where the same periode was imported twice, which would give records.length >= 2 but is not a genuine two-fase comparison.)

      Step C — compute: call berekenStatus on records[0] and records[records.length - 1]; if either .kleur === 'grijs' → m.set(s.leerlingId, null); continue; compute rank1 and rank2 via STATUS_VOLGORDE; set 'op', 'neer', or null based on comparison; return m.

    - Extract the per-student trend logic into a named const inside the useMemo body — e.g., `const computeTrend = (records: any[]): 'op' | 'neer' | null => { ... }` — so the algorithm is readable and could be extracted to a standalone pure helper in a future refactor without changing any call sites.

    - Add `// eslint-disable-next-line react-hooks/exhaustive-deps` comment before the dependency array, matching the statusMap pattern exactly.

    - Dependency array invariant: [refreshKey, allStudents.length] is intentionally the same as statusMap. This is safe because every import operation (the only source of new record data) increments refreshKey via the store. A comment above the useMemo closing brace must document this: `// refreshKey increments on every import — ensures trendMap recomputes whenever new record data arrives (same invariant as statusMap above)`

    STATUS_VOLGORDE and berekenStatus are already imported — do NOT add new imports for them.
    Do NOT use grijs rank (4) in the comparison — the explicit grijs guard (if kleur === 'grijs') fires before the rank comparison, so grijs never reaches STATUS_VOLGORDE lookup in the trend path.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `npx tsc --noEmit` passes. KlasOverzicht.tsx imports getAllRecordsForStudent. trendMap useMemo exists with Map<string, 'op' | 'neer' | null> type, correct algorithm, and [refreshKey, allStudents.length] dependency array.

    The distinct-period guard is present: two records with the same periode string produce trend null (guard fires at Step B, before berekenStatus is called).

    A comment explaining the refreshKey dependency invariant is present above the trendMap useMemo closing brace.

    The trend computation logic is extracted into a named const (e.g., computeTrend) inside the useMemo body.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire trend prop to every LeerlingTegel in the tile grid</name>
  <files>src/components/KlasOverzicht.tsx</files>
  <read_first>
    - src/components/KlasOverzicht.tsx — read the tile grid map block (around lines 183-193) to see the current LeerlingTegel JSX; add trend prop after the status prop
    - src/components/LeerlingTegel.tsx — verify trend prop is accepted (delivered by Plan 01) so TypeScript does not reject the new prop
  </read_first>
  <behavior>
    - Every LeerlingTegel in the sorted.map render has prop: trend={trendMap.get(s.leerlingId) ?? null}
    - The ?? null fallback handles the case where trendMap has no entry (should not occur but defensive)
    - All existing props (key, student, status, onClick) are unchanged
  </behavior>
  <action>
    In the sorted.map render block of KlasOverzicht.tsx, add `trend={trendMap.get(s.leerlingId) ?? null}` as a prop on every LeerlingTegel element. Place it between the status prop and the onClick prop for readability. The full prop set becomes: key, student, status, trend, onClick.

    No other changes to this file beyond adding the trend prop.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    `npx vitest run` passes (all existing tests green). Every LeerlingTegel in KlasOverzicht receives the trend prop. TypeScript reports 0 errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| klassen store → trendMap | Student records from store feed berekenStatus; malformed records produce grijs status which is safely guarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Information Disclosure | trendMap computation | accept | Trend direction derived from already-visible RAG status; no new data exposure |
| T-26-04 | Denial of Service | getAllRecordsForStudent in useMemo loop | accept | Called once per student per refreshKey change; same computational cost as existing statusMap loop |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (0 errors)
- `npx vitest run` passes (all tests green)
- `grep -c "getAllRecordsForStudent" src/components/KlasOverzicht.tsx` returns >= 2 (import + usage in useMemo)
- `grep -c "trendMap" src/components/KlasOverzicht.tsx` returns >= 3 (declaration, computation, prop usage)
- `grep "trend=" src/components/KlasOverzicht.tsx` shows `trend={trendMap.get(s.leerlingId) ?? null}`
</verification>

<success_criteria>
- trendMap is computed in KlasOverzicht via useMemo with [refreshKey, allStudents.length] dependency array
- Trend is 'op' when fase2 RAG rank > fase1 RAG rank (grijs excluded per D-09)
- Trend is 'neer' when fase2 RAG rank < fase1 RAG rank
- Trend is null for single-fase students, equal-rank students, students with grijs in either fase, and students whose two records share the same periode string
- Every LeerlingTegel in the tile grid receives the computed trend value
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/26-tegel-score-telling-fase-vergelijking/26-02-SUMMARY.md` when done
</output>
