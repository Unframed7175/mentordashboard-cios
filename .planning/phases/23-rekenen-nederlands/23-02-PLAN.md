---
phase: 23-rekenen-nederlands
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/components/RekenenNederlandsSection.tsx
  - src/components/DetailWeergave.tsx
autonomous: true
requirements: [RNL-01, RNL-02, RNL-03, RNL-04]

must_haves:
  truths:
    - "DetailWeergave shows a 'Rekenen & Nederlands' section between AanvullendSection and StageSection"
    - "Each field shows a dropdown with options: (blank) / 3F / 2F (norm) / 1F — onder norm"
    - "Selecting a value persists via saveKlassen() using the AanvullendSection pattern"
    - "A norm badge renders next to the label: '3F — goed' / '2F — voldoende (norm)' / 'Onder norm' in green/red"
    - "Old records without rekenResultaat/nederlandsResultaat render empty dropdowns without crashing"
    - "All existing tests still pass"
  artifacts:
    - path: "src/components/RekenenNederlandsSection.tsx"
      provides: "Rekenen & Nederlands section component"
      exports: ["default RekenenNederlandsSection"]
    - path: "src/components/DetailWeergave.tsx"
      provides: "Component mount point between AanvullendSection and StageSection"
      contains: "RekenenNederlandsSection"
  key_links:
    - from: "src/components/RekenenNederlandsSection.tsx"
      to: "utils/schema.ts"
      via: "import { normalizeRekenScore }"
      pattern: "normalizeRekenScore"
    - from: "src/components/RekenenNederlandsSection.tsx"
      to: "utils/klassen.ts"
      via: "import { saveKlassen, klassenState }"
      pattern: "saveKlassen"
    - from: "src/components/DetailWeergave.tsx"
      to: "src/components/RekenenNederlandsSection.tsx"
      via: "JSX mount between AanvullendSection and StageSection"
      pattern: "<RekenenNederlandsSection"
---

<objective>
Create `src/components/RekenenNederlandsSection.tsx` and mount it in `DetailWeergave.tsx` between AanvullendSection and StageSection.

Purpose: Delivers RNL-01, RNL-02, RNL-03 — mentor can record Rekenen and Nederlands scores per student and see the MBO-3 norm status (2F = voldoende) as a colour-coded badge inline with the label. RNL-04 is scaffolded: the fields exist and the component is wired; PDF extraction is additive once a real sample file is available.

Output: New component file + one import + one JSX mount in DetailWeergave.tsx. No new CSS required — reuses existing `.detail-section`, `.detail-section-title`, `.aanvullend-grid`, `.aanvullend-veld` classes.
</objective>

<execution_context>
@D:/Downloads/get-shit-done-main/dashboard-2/.claude/get-shit-done/workflows/execute-plan.md
@D:/Downloads/get-shit-done-main/dashboard-2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/ROADMAP.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/STATE.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/23-rekenen-nederlands/23-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — extracted from codebase. Do not explore further. -->

From utils/klassen.ts (pattern confirmed via AanvullendSection):
```typescript
export let klassenState: { klassen: Record<string, Klas>; activeKlasId: string | null };
export async function saveKlassen(): Promise<boolean | undefined>;
```

From utils/schema.ts (created in Plan 23-01):
```typescript
export function normalizeRekenScore(raw: unknown): 'goed' | 'voldoende' | 'onvoldoende' | null;
```

From src/components/AanvullendSection.tsx — handleChange pattern to follow exactly:
```typescript
async function handleChange(field: 'taalniveauSchrijven' | 'taalniveauGesprekken' | 'rekenniveau', value: string) {
  const klas = klassenState.klassen[klassenState.activeKlasId!];
  const rec = klas?.students?.find((s: any) => s.leerlingId === student.leerlingId);
  if (!rec) return;
  rec[field] = value || null;
  const saved = await saveKlassen();
  if (saved === false) return;
  if (timerRef.current) clearTimeout(timerRef.current);
  setHint('saved');
  timerRef.current = setTimeout(() => setHint('idle'), 1500);
}
```

From src/components/DetailWeergave.tsx (lines 12-13, insertion coordinates):
```typescript
import AanvullendSection from './AanvullendSection';
import StageSection from './StageSection';
// New import goes after AanvullendSection import line
```

From src/components/DetailWeergave.tsx (lines 136-140, mount coordinates):
```tsx
{/* Section 2: AanvullendSection */}
<AanvullendSection student={student} />

{/* Section 3: StageSection — stageData passed as prop (WR-08) */}
<StageSection student={student} stageData={stageData} />
// New component mounts between lines 137 and 140
```

From src/index.css (classes to reuse — no new CSS):
  .detail-section         — card container with border/padding
  .detail-section-title   — section heading
  .aanvullend-grid        — two-column CSS grid
  .aanvullend-veld        — grid cell with label + select
  .aanvullend-hint        — small helper text line below grid
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/components/RekenenNederlandsSection.tsx</name>
  <files>src/components/RekenenNederlandsSection.tsx, src/components/RekenenNederlandsSection.test.tsx</files>
  <behavior>
    - Renders a div.detail-section with title "Rekenen & Nederlands"
    - Renders two .aanvullend-veld cells inside .aanvullend-grid: "Rekenen" and "Nederlands"
    - Each cell has a label and a select with options: '' (— niet ingevuld —), '3F', '2F', '1F'
    - Select value is controlled: student.rekenResultaat ?? '' and student.nederlandsResultaat ?? ''
    - normBadge(null) returns null — no badge rendered when no value selected
    - normBadge('goed') returns span with text "3F — goed" in green (#10b981)
    - normBadge('voldoende') returns span with text "2F — voldoende (norm)" in green (#10b981)
    - normBadge('onvoldoende') returns span with text "Onder norm" in red (var(--status-rood-text))
    - When both statuses are null and hint is idle: hint paragraph shows norm text "Norm: Rekenen 2F · Nederlands 2F (MBO-3 landelijk)"
    - When hint is 'saved': hint paragraph shows "Opgeslagen" in green
    - onChange calls handleChange with correct field name and the select's value
    - handleChange finds the record via leerlingId in klassenState, mutates rec[field], calls saveKlassen()
    - If saveKlassen() returns false, does NOT set hint to 'saved'
    - useEffect cleanup clears the timer ref on unmount
  </behavior>
  <action>
    Write tests first in src/components/RekenenNederlandsSection.test.tsx covering the behavior cases above (mock saveKlassen and klassenState). Run tests — they MUST fail (RED).

    Then create src/components/RekenenNederlandsSection.tsx with the following structure (do not deviate from this contract):

    Imports:
      import React, { useState, useRef, useEffect } from 'react';
      import { saveKlassen, klassenState } from '../../utils/klassen';
      import { normalizeRekenScore } from '../../utils/schema';

    Props interface:
      interface RekenenNederlandsSectionProps { student: any; }

    State:
      const [hint, setHint] = useState<'idle' | 'saved'>('idle');
      const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null);

    useEffect:
      Cleanup only — clears timerRef.current on unmount.

    handleChange(field: 'rekenResultaat' | 'nederlandsResultaat', value: string):
      Follow the AanvullendSection pattern exactly (see interfaces block above).
      rec[field] = value || null;  — empty string becomes null in storage.

    Derived state:
      const rekenStatus = normalizeRekenScore(student.rekenResultaat ?? null);
      const nederlandsStatus = normalizeRekenScore(student.nederlandsResultaat ?? null);

    normBadge helper function (defined inside the component):
      Accepts status: ReturnType<typeof normalizeRekenScore>
      Returns null when status === null.
      For 'onvoldoende': color = 'var(--status-rood-text)', label = 'Onder norm'
      For 'voldoende': color = '#10b981', label = '2F — voldoende (norm)'
      For 'goed': color = '#10b981', label = '3F — goed'
      Returns a <span> with fontSize '0.75rem', fontWeight 600, marginLeft '0.5rem'.

    JSX structure:
      <div className="detail-section">
        <p className="detail-section-title">Rekenen &amp; Nederlands</p>
        <div className="aanvullend-grid">
          Rekenen .aanvullend-veld:
            id="rnl-rekenen"
            select value={student.rekenResultaat ?? ''}
            options: value='' text="— niet ingevuld —", value='3F' text='3F', value='2F' text='2F (norm)', value='1F' text='1F — onder norm'
            normBadge(rekenStatus) inside the label after the text "Rekenen"
          Nederlands .aanvullend-veld:
            id="rnl-nederlands"
            select value={student.nederlandsResultaat ?? ''}
            same options pattern
            normBadge(nederlandsStatus) inside the label after the text "Nederlands"
        </div>
        <p className="aanvullend-hint" style={{ color: hint === 'saved' ? '#10b981' : undefined }}>
          When hint === 'saved': "Opgeslagen"
          When hint === 'idle' AND rekenStatus === null AND nederlandsStatus === null:
            <span style={{ color: 'var(--text-muted)', fontSize: '0.8rem' }}>Norm: Rekenen 2F · Nederlands 2F (MBO-3 landelijk)</span>
          Otherwise: nothing (empty)
        </p>
      </div>

    Run tests — they MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>RekenenNederlandsSection.tsx created; tests cover badge rendering, handleChange mutation pattern, hint state transitions; all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Mount RekenenNederlandsSection in DetailWeergave.tsx and commit</name>
  <files>src/components/DetailWeergave.tsx</files>
  <action>
    Two edits to src/components/DetailWeergave.tsx:

    1. ADD IMPORT — after the existing `import AanvullendSection from './AanvullendSection';` line, insert:
         import RekenenNederlandsSection from './RekenenNederlandsSection';

    2. ADD JSX MOUNT — after the `<AanvullendSection student={student} />` line (currently line 137) and before the `<StageSection` line, insert:
         {/* Section 2b: RekenenNederlandsSection — RNL-01..03 */}
         <RekenenNederlandsSection student={student} />

    Do not touch any other code in DetailWeergave.tsx. The component receives only `student` as a prop (no stageData, no status).

    After verifying tests pass, commit:
      git add utils/schema.ts utils/datamodel.ts src/components/RekenenNederlandsSection.tsx src/components/RekenenNederlandsSection.test.tsx src/components/DetailWeergave.tsx
      git commit -m "feat(rnl): add Rekenen & Nederlands section with 2F norm indicator (RNL-01..03)"
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>DetailWeergave.tsx imports and mounts RekenenNederlandsSection; npm test exits 0; git commit created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| select onChange → handleChange | User-supplied dropdown value enters mutation path |
| handleChange → klassenState mutation | Mutates in-memory record then calls saveKlassen() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | handleChange field parameter | accept | Field is a union type literal ('rekenResultaat' | 'nederlandsResultaat') — TypeScript enforces at call sites; no user-supplied field name |
| T-23-04 | Tampering | select value → rec[field] | mitigate | value || null coerces empty string to null; options are controlled enum values; no freetext input |
| T-23-05 | Denial of Service | saveKlassen failure | accept | On false return, hint is not set — UI stays consistent; no crash; standard pattern used across all section components |
| T-23-06 | Information Disclosure | student prop (any type) | accept | Component reads only rekenResultaat/nederlandsResultaat/leerlingId; no PII rendered beyond what DetailWeergave already exposes |
</threat_model>

<verification>
- `grep -n "RekenenNederlandsSection" src/components/DetailWeergave.tsx` returns both the import and the JSX mount line
- `grep -n "normalizeRekenScore" src/components/RekenenNederlandsSection.tsx` returns the import and two call sites
- `grep -n "saveKlassen" src/components/RekenenNederlandsSection.tsx` returns the import and the call in handleChange
- `npm test` exits 0 — all 93+ tests pass
- Component renders between AanvullendSection and StageSection in DevTools component tree
</verification>

<success_criteria>
- RekenenNederlandsSection.tsx created, imports normalizeRekenScore from utils/schema and saveKlassen/klassenState from utils/klassen
- Component reuses .detail-section / .aanvullend-grid / .aanvullend-veld CSS — no new classes added
- Norm badge renders inline with label: "3F — goed" (green) / "2F — voldoende (norm)" (green) / "Onder norm" (red)
- Dropdowns default to '' (empty) when student has no rekenResultaat or nederlandsResultaat — no crash
- onChange persists via saveKlassen() with the same rec[field] = value || null mutation pattern as AanvullendSection
- DetailWeergave.tsx mounts the component between AanvullendSection and StageSection
- git commit created with all five files staged
- npm test exits 0 — all 93+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-rekenen-nederlands/23-02-SUMMARY.md`
</output>
