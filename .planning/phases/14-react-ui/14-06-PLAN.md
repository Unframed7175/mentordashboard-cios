---
phase: 14-react-ui
plan: "06"
type: execute
wave: 0
depends_on: ["14-04", "14-05"]
files_modified:
  - src/components/AanvullendSection.tsx
  - src/components/StageSection.tsx
  - src/components/LeerlijnenSection.tsx
  - src/components/DetailWeergave.tsx
autonomous: true
gap_closure: true
requirements: [DET-V2-01]

must_haves:
  truths:
    - "Section 2 (Aanvullende gegevens) renders taalniveau and rekenniveau selects with current student values; change saves via saveKlassen()"
    - "Section 3 (Stage) renders stage stat-cards for organisatie, periode, and stage-uren when stageData exists; renders empty-state message when absent"
    - "Section 5 (Per leerlijn) renders one leerlijn-row per leerlijn in berekenPrognose().leerlijnen with bar track, voldoendeOfHoger/totaal counts, and onvoldoende count"
    - "No 'Komt in Plan 05' text appears anywhere in DetailWeergave.tsx or its rendered output"
  artifacts:
    - path: "src/components/AanvullendSection.tsx"
      provides: "Taalniveau + rekenniveau selects wired to student + saveKlassen()"
      exports: ["AanvullendSection"]
    - path: "src/components/StageSection.tsx"
      provides: "Stage stat-cards from klassenState activeKlas stageData"
      exports: ["StageSection"]
    - path: "src/components/LeerlijnenSection.tsx"
      provides: "Per-leerlijn breakdown bars from berekenPrognose().leerlijnen"
      exports: ["LeerlijnenSection"]
    - path: "src/components/DetailWeergave.tsx"
      provides: "All 10 sections rendered without any stub text"
      contains: "AanvullendSection"
  key_links:
    - from: "src/components/DetailWeergave.tsx"
      to: "src/components/AanvullendSection.tsx"
      via: "import AanvullendSection; replace Section 2 stub div"
      pattern: "AanvullendSection"
    - from: "src/components/AanvullendSection.tsx"
      to: "utils/klassen.ts"
      via: "saveKlassen() after onChange"
      pattern: "saveKlassen"
    - from: "src/components/StageSection.tsx"
      to: "utils/klassen.ts"
      via: "klassenState.activeKlasId + klassenState.klassen read"
      pattern: "klassenState"
    - from: "src/components/LeerlijnenSection.tsx"
      to: "utils/prognosis.ts"
      via: "berekenPrognose(student).leerlijnen"
      pattern: "berekenPrognose"
---

<objective>
Replace three stub divs in DetailWeergave.tsx ("Komt in Plan 05") with real component implementations that are functionally identical to the legacy app.js sections:

- Section 2: AanvullendSection — taalniveau/rekenniveau selects, persists via saveKlassen()
- Section 3: StageSection — stage stat-cards from stageData on the active klas object
- Section 5: LeerlijnenSection — per-leerlijn breakdown bars from berekenPrognose().leerlijnen

Purpose: Closes the SC-3 / DET-V2-01 gap identified in VERIFICATION.md. The legacy app renders all three sections; the React version shows placeholder text. This plan eliminates the ownership gap.

Output: Three new component files + updated DetailWeergave.tsx with all 10 sections rendering real data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-react-ui/14-CONTEXT.md
@.planning/phases/14-react-ui/14-PATTERNS.md

<!-- Prior plan summaries needed for interface context -->
@.planning/phases/14-react-ui/14-04-SUMMARY.md
@.planning/phases/14-react-ui/14-05-SUMMARY.md

<interfaces>
<!-- Extracted from src/components/DetailWeergave.tsx (current state, lines 1-10, 97-115) -->
<!-- Three stub positions to replace: -->

Section 2 stub (lines 96-100):
  <div className="detail-section">
    <p className="detail-section-title">Aanvullende gegevens</p>
    <p style={{ fontSize: '0.875rem', color: 'var(--text-faint)' }}>Komt in Plan 05</p>
  </div>

Section 3 stub (lines 102-106):
  <div className="detail-section">
    <p className="detail-section-title">Stage</p>
    <p style={{ fontSize: '0.875rem', color: 'var(--text-faint)' }}>Komt in Plan 05</p>
  </div>

Section 5 stub (lines 111-115):
  <div className="detail-section">
    <p className="detail-section-title">Per leerlijn</p>
    <p style={{ fontSize: '0.875rem', color: 'var(--text-faint)' }}>Komt in Plan 05</p>
  </div>

<!-- DetailWeergave.tsx variables available in scope at stub locations (line 20-41): -->
  leerlingId: string          -- from props
  student: any                -- most-recent record after verzuim inheritance
  status: StatusResult        -- berekenStatus(student)
  status.prognose             -- berekenPrognose result (already computed, same object as in DoortstroomPrognoseSection)

<!-- From utils/klassen.ts (exported): -->
  klassenState: { klassen: Record<string, any>; activeKlasId: string | null }
  saveKlassen(): Promise<boolean>

  Active klas object access pattern:
    const klas = klassenState.activeKlasId ? klassenState.klassen[klassenState.activeKlasId] : null;
    const sd = klas?.stageData?.[student.leerlingId]; // StageData | undefined

<!-- From utils/prognosis.ts (berekenPrognose return shape, lines 102-105, 218): -->
  leerlijnen: Array<{
    leerlijn: 'lesgeven' | 'organiseren' | 'prof_handelen',
    totaal: number,
    voldoendeOfHoger: number,
    onvoldoende: number,
    onbeoordeeld: number,
  }>

<!-- Legacy app.js reference implementations: -->
  buildDetailAanvullend: app.js lines 1891-1920
    - student.taalniveau field (not taalniveauSchrijven — the simple field, may be undefined)
    - student.rekenniveau field (may be undefined)
    - taal options: ['2F', '3F']
    - rekenen options: ['MBO 3', 'MBO 4']
    - empty option: value="" text="— niet ingevuld —"
    - saves via event delegation in legacy; in React use onChange on each select
    - shows aanvullend-hint paragraph for save confirmation

  buildDetailStage: app.js lines 1930-1951
    - stageData on the klas object: klas.stageData[student.leerlingId]
    - StageData shape: { organisatie, startdatum, einddatum, urenGoedgekeurd, urenIngeleverd }
    - formatDutchDate(iso): splits on '-', returns DD-MM-YYYY (app.js lines 1924-1928)
    - empty state: "Geen stage-data — importeer de stage-Excel via het Import-tabblad."
    - stat-cards grid: 2-column grid with class "stats-grid" style gridTemplateColumns:1fr 1fr
    - cards: Organisatie, Periode (startdatum t/m einddatum), Stage-uren (span 2 cols)
    - uren string: urenGoedgekeurd ?? '—' + ' / ' + urenIngeleverd ?? '—' + ' uren goedgekeurd'

  buildDetailLeerlijnen: app.js lines 1649-1666
    - receives p (berekenPrognose result) — maps p.leerlijnen
    - LEERLIJN_LABEL map: { lesgeven: 'Lesgeven', organiseren: 'Organiseren', prof_handelen: 'Prof. handelen' }
    - per row: leerlijn-row div containing:
        span.leerlijn-naam: "{label} ({totaal})"
        span.leerlijn-stat: "{voldoendeOfHoger}/{totaal} ≥V"
        span.leerlijn-stat (conditional): onvoldoende count — color var(--status-rood-text), fontWeight:700 when onvoldoende > 2
        span.leerlijn-stat style color:var(--text-faint): "{onbeoordeeld} ?"
        div.leerlijn-bar-track > div.leerlijn-bar-fill style width:{pct}%
          where pct = totaal > 0 ? Math.round((voldoendeOfHoger / totaal) * 100) : 0
    - container: div.leerlijn-rows wrapping all rows

<!-- Import path convention (Pattern E from 14-PATTERNS.md): -->
  utils imports: ../../utils/
  component-to-component imports: ./

<!-- All new component files follow Pattern C (props interface before function): -->
  interface ComponentProps { ... }
  export default function ComponentName({ ... }: ComponentProps) { ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AanvullendSection, StageSection, and LeerlijnenSection components</name>
  <files>
    src/components/AanvullendSection.tsx,
    src/components/StageSection.tsx,
    src/components/LeerlijnenSection.tsx
  </files>
  <read_first>
    - src/components/DetailWeergave.tsx — understand the student and status objects available at stub locations
    - src/components/FeedbackActiepuntenSection.tsx — Pattern B: async handler + saveKlassen() pattern to follow for AanvullendSection onChange
    - app.js lines 1891-1920 — buildDetailAanvullend: exact select options, field names (taalniveau / rekenniveau), hint paragraph
    - app.js lines 1924-1951 — formatDutchDate helper + buildDetailStage: stageData shape, stat-cards layout
    - app.js lines 1649-1666 — buildDetailLeerlijnen: leerlijn-row structure, LEERLIJN_LABEL, bar-fill width formula
    - utils/klassen.ts lines 31-34 — klassenState shape to access active klas stageData
  </read_first>
  <behavior>
    AanvullendSection:
    - Renders div.detail-section with p.detail-section-title "Aanvullende gegevens"
    - Renders div.aanvullend-grid containing two div.aanvullend-veld children
    - First veld: label "Taalniveau", select with options [""/"— niet ingevuld —", "2F"/"2F", "3F"/"3F"], value from student.taalniveau || ''
    - Second veld: label "Rekenniveau", select with options [""/"— niet ingevuld —", "MBO 3"/"MBO 3", "MBO 4"/"MBO 4"], value from student.rekenniveau || ''
    - onChange for each select: mutate student[field] = newValue, await saveKlassen(), show 'Opgeslagen' hint for 1500ms then clear (same hint pattern as NotitiesTextarea)
    - Hint paragraph: p.aanvullend-hint — empty string when idle, 'Opgeslagen' when saved; color #10b981 when saved
    - Props: { student: any }

    StageSection:
    - Reads active klas stageData: const klas = klassenState.activeKlasId ? klassenState.klassen[klassenState.activeKlasId] : null; const sd = klas?.stageData?.[student.leerlingId]
    - When sd is undefined/null: render div.detail-section with title "Stage" and p "Geen stage-data — importeer de stage-Excel via het Import-tabblad." (color #9ca3af, fontSize 0.875rem)
    - When sd exists: render div.detail-section with title "Stage" and div.stats-grid (style: gridTemplateColumns '1fr 1fr')
    - formatDutchDate(iso): if falsy return '—'; split on '-'; if parts.length === 3 return `${parts[2]}-${parts[1]}-${parts[0]}`; else return iso as-is
    - Stat-cards: div.stat-card for Organisatie (sd.organisatie || '—'), div.stat-card for Periode (formatDutchDate(sd.startdatum) + ' t/m ' + formatDutchDate(sd.einddatum)), div.stat-card style gridColumn 'span 2' for Stage-uren (show `${sd.urenGoedgekeurd ?? '—'} / ${sd.urenIngeleverd ?? '—'} uren goedgekeurd`)
    - Each stat-card: div.stat-label above div.stat-value (fontSize 1rem)
    - Props: { student: any }

    LeerlijnenSection:
    - Receives p (berekenPrognose result) via prop; maps p.leerlijnen array
    - LEERLIJN_LABEL map defined inline: { lesgeven: 'Lesgeven', organiseren: 'Organiseren', prof_handelen: 'Prof. handelen' }
    - For each ll in p.leerlijnen: render div.leerlijn-row containing:
        span.leerlijn-naam: text "{LEERLIJN_LABEL[ll.leerlijn] || ll.leerlijn} ({ll.totaal})"
        span.leerlijn-stat: "{ll.voldoendeOfHoger}/{ll.totaal} ≥V" (use React entity &ge; or literal ≥)
        span.leerlijn-stat with conditional style: "{ll.onvoldoende} O" — apply color 'var(--status-rood-text)' and fontWeight 700 only when ll.onvoldoende > 2
        span.leerlijn-stat style color 'var(--text-faint)': "{ll.onbeoordeeld} ?"
        div.leerlijn-bar-track > div.leerlijn-bar-fill with style width: `${pct}%` where pct = ll.totaal > 0 ? Math.round((ll.voldoendeOfHoger / ll.totaal) * 100) : 0
    - Wrap all rows in div.leerlijn-rows
    - Container: div.detail-section with p.detail-section-title "Per leerlijn"
    - Empty state when p.leerlijnen.length === 0: render nothing inside leerlijn-rows
    - Props: { prognose: any }

    All three files follow Pattern C (interface before function) and Pattern E (../../utils/ import paths).
    AanvullendSection uses useState for hint state and useRef for timer (same shape as NotitiesTextarea.tsx).
    StageSection and LeerlijnenSection are pure presentational (no state, no async).
  </behavior>
  <action>
    Create src/components/AanvullendSection.tsx:
    Import React, useState, useRef from 'react'. Import saveKlassen from '../../utils/klassen'.
    Props interface: { student: any }.
    Component body: useState hint ('idle'|'saved'), useRef timer. Two controlled selects with value bound to student.taalniveau||'' and student.rekenniveau||''. Each onChange handler: mutate student field, await saveKlassen(), setHint('saved'), setTimeout 1500ms → setHint('idle'). Render div.detail-section, p.detail-section-title "Aanvullende gegevens", div.aanvullend-grid with two div.aanvullend-veld children (label + select), p.aanvullend-hint (text 'Opgeslagen' when hint==='saved', style color #10b981 when saved).

    Create src/components/StageSection.tsx:
    Import React from 'react'. Import klassenState from '../../utils/klassen'.
    Props interface: { student: any }.
    Define formatDutchDate(iso: string | null | undefined): string inline.
    Read sd: const klas = klassenState.activeKlasId ? klassenState.klassen[klassenState.activeKlasId] : null; const sd = klas?.stageData?.[student.leerlingId]; 
    Render div.detail-section with p.detail-section-title "Stage". When !sd: render p with empty-state copy. When sd: render div.stats-grid (style gridTemplateColumns '1fr 1fr') with three div.stat-card children (Organisatie, Periode, Stage-uren with gridColumn 'span 2').

    Create src/components/LeerlijnenSection.tsx:
    Import React from 'react'. Import berekenPrognose from '../../utils/prognosis'.
    Note: LeerlijnenSection receives the already-computed prognose result as a prop — it does NOT call berekenPrognose itself (same pattern as DoortstroomPrognoseSection using status.prognose).
    Props interface: { prognose: any }.
    Define LEERLIJN_LABEL constant inline. Map prognose.leerlijnen to div.leerlijn-row elements. Render div.detail-section, p.detail-section-title "Per leerlijn", div.leerlijn-rows.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    Three files exist; npx tsc --noEmit exits 0; each file exports a default function with the correct props interface; AanvullendSection imports saveKlassen; StageSection imports klassenState; LeerlijnenSection does not call berekenPrognose (receives prognose prop).
  </done>
  <acceptance_criteria>
    - src/components/AanvullendSection.tsx exists and exports default function AanvullendSection
    - src/components/StageSection.tsx exists and exports default function StageSection
    - src/components/LeerlijnenSection.tsx exists and exports default function LeerlijnenSection
    - grep -c "saveKlassen" src/components/AanvullendSection.tsx returns at least 1 (wired to save)
    - grep -c "klassenState" src/components/StageSection.tsx returns at least 1 (reads active klas)
    - grep -c "berekenPrognose" src/components/LeerlijnenSection.tsx returns 0 (receives prop, does not call)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace the three stub divs in DetailWeergave.tsx with real component renders</name>
  <files>
    src/components/DetailWeergave.tsx
  </files>
  <read_first>
    - src/components/DetailWeergave.tsx — current full file; identify stub locations at lines 96-100 (Section 2), 102-106 (Section 3), 111-115 (Section 5) and the existing imports at lines 1-10
    - src/components/AanvullendSection.tsx — confirm exported name and props interface created in Task 1
    - src/components/StageSection.tsx — confirm exported name and props interface
    - src/components/LeerlijnenSection.tsx — confirm exported name and props interface
  </read_first>
  <action>
    Edit src/components/DetailWeergave.tsx:

    1. Add three import statements after the existing imports (after line 10):
       import AanvullendSection from './AanvullendSection';
       import StageSection from './StageSection';
       import LeerlijnenSection from './LeerlijnenSection';

    2. Replace Section 2 stub div (the entire div.detail-section block for "Aanvullende gegevens" containing the "Komt in Plan 05" paragraph) with:
       {/* Section 2: AanvullendSection */}
       <AanvullendSection student={student} />

    3. Replace Section 3 stub div (the entire div.detail-section block for "Stage" containing the "Komt in Plan 05" paragraph) with:
       {/* Section 3: StageSection */}
       <StageSection student={student} />

    4. Replace Section 5 stub div (the entire div.detail-section block for "Per leerlijn" containing the "Komt in Plan 05" paragraph) with:
       {/* Section 5: LeerlijnenSection */}
       <LeerlijnenSection prognose={status.prognose} />

    The variable status.prognose is already computed at line 40 (berekenStatus(student)) and is the berekenPrognose result — pass it directly as the prognose prop. No new berekenPrognose call needed.

    All other sections (1, 4, 6, 7, 8, 9, 10) and all other code in the file are left unchanged.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | head -10 && grep -c "Komt in Plan 05" src/components/DetailWeergave.tsx</automated>
  </verify>
  <done>
    npm test exits 0 with all previously-passing tests still passing; npx tsc --noEmit exits 0; grep "Komt in Plan 05" src/components/DetailWeergave.tsx returns 0 matches.
  </done>
  <acceptance_criteria>
    - grep -c "Komt in Plan 05" src/components/DetailWeergave.tsx returns 0
    - grep -c "AanvullendSection" src/components/DetailWeergave.tsx returns at least 2 (import + usage)
    - grep -c "StageSection" src/components/DetailWeergave.tsx returns at least 2 (import + usage)
    - grep -c "LeerlijnenSection" src/components/DetailWeergave.tsx returns at least 2 (import + usage)
    - grep "prognose={status.prognose}" src/components/DetailWeergave.tsx returns a match (passing pre-computed prognose)
    - npm test exits 0 with 43 passing (no regressions)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student object → select value | student.taalniveau / student.rekenniveau are string fields from imported PDFs; rendered as controlled select values (no innerHTML) |
| klassenState → StageSection render | stageData comes from stage-Excel import; rendered as JSX string children (React auto-escapes) |
| berekenPrognose output → LeerlijnenSection | leerlijnen data is numeric computation from deelgebiedScores; rendered as JSX string interpolations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Injection | AanvullendSection selects | accept | select values are React-controlled value prop; user can only pick from predefined option values ['2F','3F','MBO 3','MBO 4','']; no innerHTML |
| T-14-06-02 | Injection | StageSection stat-card render | accept | sd.organisatie, sd.startdatum, sd.einddatum, sd.urenGoedgekeurd, sd.urenIngeleverd rendered as JSX string children — React auto-escapes all values |
| T-14-06-03 | Injection | LeerlijnenSection leerlijn-row | accept | LEERLIJN_LABEL values are hardcoded strings; numeric fields (totaal, voldoendeOfHoger, onvoldoende, onbeoordeeld) rendered via arithmetic/interpolation; no external input reflected as raw HTML |
| T-14-06-04 | Tampering | AanvullendSection saveKlassen | accept | onChange mutates student object in-memory then calls saveKlassen(); no validation needed beyond select constraint — options are hardcoded and value is the selected option string |
</threat_model>

<verification>
After both tasks complete, run:

1. grep -c "Komt in Plan 05" src/components/DetailWeergave.tsx
   Expected: 0

2. npm test
   Expected: 43 passing, 5 skipped, 0 failures (identical to pre-gap-closure baseline)

3. npx tsc --noEmit
   Expected: exits 0, no errors

4. grep -c "AanvullendSection\|StageSection\|LeerlijnenSection" src/components/DetailWeergave.tsx
   Expected: at least 6 (3 import lines + 3 usage lines)

5. grep -c "saveKlassen" src/components/AanvullendSection.tsx
   Expected: at least 1

6. grep -c "klassenState" src/components/StageSection.tsx
   Expected: at least 1
</verification>

<success_criteria>
- All three "Komt in Plan 05" stubs removed from DetailWeergave.tsx (0 grep matches)
- AanvullendSection renders taalniveau/rekenniveau selects bound to student fields; onChange persists via saveKlassen()
- StageSection renders empty-state when no stageData; renders stat-cards for Organisatie, Periode, Stage-uren when stageData exists
- LeerlijnenSection renders one leerlijn-row per entry in berekenPrognose().leerlijnen with bar-fill proportional to voldoendeOfHoger/totaal
- npm test: 43 passing, 0 failures
- npx tsc --noEmit: 0 errors
- Requirement DET-V2-01 status changes from BLOCKED to SATISFIED
</success_criteria>

<output>
After completion, create `.planning/phases/14-react-ui/14-06-SUMMARY.md` using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
