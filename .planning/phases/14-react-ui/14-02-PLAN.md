---
phase: 14-react-ui
plan: 02
type: execute
wave: 1
depends_on:
  - "14-01"
files_modified:
  - src/App.tsx
  - src/components/ImportPage.tsx
  - src/components/KlasTabStrip.tsx
  - src/components/KlasModal.tsx
autonomous: true
requirements:
  - KOV-01
  - KOV-02
must_haves:
  truths:
    - "App.tsx routes between 'import', 'klas', and 'detail' views via useState"
    - "refreshKey increments after every successful import, klas switch, or klas create/delete"
    - "ImportPage calls onImportComplete() after each successful saveKlassen() in handlePDFs, handleExcel, handleBackup"
    - "KlasTabStrip renders all klassen as nav-tab buttons inside #main-nav, with active klas highlighted"
    - "KlasModal controlled form creates a new klas via createKlas(), validates name, shows duplicate error"
  artifacts:
    - path: "src/App.tsx"
      provides: "View routing state (view, refreshKey, activeStudentId, detailStudentList), KlasTabStrip + KlasModal mount"
      exports: ["App"]
    - path: "src/components/ImportPage.tsx"
      provides: "onImportComplete?: () => void prop called after saveKlassen() success"
      exports: ["ImportPage"]
    - path: "src/components/KlasTabStrip.tsx"
      provides: "Tab strip rendering klassenState.klassen, switching active klas"
      exports: ["KlasTabStrip"]
    - path: "src/components/KlasModal.tsx"
      provides: "Modal form for new klas creation with validation and error display"
      exports: ["KlasModal"]
  key_links:
    - from: "src/App.tsx"
      to: "src/components/KlasTabStrip.tsx"
      via: "onSwitch prop → switchActiveKlas() + setRefreshKey"
      pattern: "KlasTabStrip"
    - from: "src/components/ImportPage.tsx"
      to: "src/App.tsx"
      via: "onImportComplete() → setRefreshKey(k+1); setView('klas')"
      pattern: "onImportComplete"
    - from: "src/components/KlasModal.tsx"
      to: "utils/klassen.ts"
      via: "createKlas() + saveKlassen()"
      pattern: "createKlas"
---

<objective>
Wire App.tsx view routing, add onImportComplete prop to ImportPage, and create KlasTabStrip + KlasModal components. After this wave, App.tsx manages view state ('import'|'klas'|'detail'), import completion navigates to klas view, and the user can create and switch between klassen.

Purpose: This wave produces the navigational skeleton that Wave 2 plans (KlasOverzicht and DetailWeergave) will slot into. No Wave 2 plan can run until App.tsx exposes the correct routing callbacks.

Output: Updated App.tsx + ImportPage.tsx + new KlasTabStrip.tsx + new KlasModal.tsx. npm test stays green.
</objective>

<execution_context>
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\phases\14-react-ui\14-RESEARCH.md
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\phases\14-react-ui\14-PATTERNS.md
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\phases\14-react-ui\14-UI-SPEC.md
</execution_context>

<context>
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\ROADMAP.md
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\phases\14-react-ui\14-CONTEXT.md
@D:\Downloads\get-shit-done-main\dashboard-2\.planning\phases\14-react-ui\14-01-SUMMARY.md

<interfaces>
<!-- From src/App.tsx (current — read before modifying) -->
Current App.tsx (25 lines total):
  - Imports: React, useEffect, ImportPage
  - Has drop-guard useEffect (lines 5-15) — keep verbatim
  - Has <div id="storage-error-banner"> — keep verbatim
  - Has <ImportPage /> with no props — add onImportComplete prop

<!-- From src/components/ImportPage.tsx (current — lines 1-289) -->
ImportPage signature to change:
  Current: export default function ImportPage()
  New:     interface ImportPageProps { onImportComplete?: () => void }
           export default function ImportPage({ onImportComplete }: ImportPageProps)
  Call onImportComplete?.() at end of three success paths:
    - handlePDFs: after setImportState with status:'done' (around line 75-79)
    - handleExcel: after setImportState with status:'done' (around line 106-114)
    - handleBackup: after setImportState with status:'done', result.success=true (around line 140)

<!-- From utils/klassen.ts (key exports for App.tsx and KlasTabStrip) -->
export const klassenState: { klassen: Record<string, any>; activeKlasId: string | null }
export async function createKlas(naam: string): Promise<any>
  // Returns: { id: string, ...klas } on success | { error: 'duplicate' } on name collision | { error: string } on other errors
export async function switchActiveKlas(klasId: string): Promise<boolean>
export async function deleteKlas(klasId: string): Promise<boolean>
export async function saveKlassen(): Promise<boolean>
export function getActiveStudents(): any[]

<!-- From src/utils/status.ts (created in Wave 0 — needed for App.tsx type reference) -->
No direct use in App.tsx or KlasTabStrip — consumed in Wave 2 by KlasOverzicht.

<!-- CSS classes for KlasTabStrip — from 14-UI-SPEC.md -->
Header: #site-header height:56px; background:var(--bg-header)=#000000
Nav: #main-nav display:flex; gap:0.25rem (contains tab buttons directly — no wrapper div that breaks flex)
Tab: .nav-tab; 0.9rem; padding 0.4rem 0.9rem; radius 6px
Active tab: .nav-tab.active; background:rgba(255,255,255,0.15); color:#fff
Add button: .nav-tab style; color:#3b82f6; title="Nieuwe klas aanmaken"; content "+"

<!-- CSS classes for KlasModal — from 14-UI-SPEC.md -->
Overlay: position:fixed; inset:0; background:rgba(0,0,0,0.5); display:flex; align-items:center; justify-content:center; z-index:1000
Modal panel: background:var(--bg-surface); border-radius:12px; padding:1.5rem; width:100%; max-width:400px
Fields: Klasnaam (required), Schooljaar (optional)
Error display: 0.85rem; color:var(--status-rood-text)
Actions: "Annuleren" (.btn.btn-ghost) + "Klas aanmaken" (.btn.btn-primary), right-aligned
Keyboard: Enter in klasnaam field submits; Escape closes modal
Validation error: "Voer een klasnaam in." / "Er bestaat al een klas met de naam '{naam}'."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite App.tsx with view routing + mount KlasTabStrip + KlasModal placeholder</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current state — preserve drop-guard useEffect and storage-error-banner verbatim)
    - 14-PATTERNS.md §src/App.tsx section (import extension pattern, handleKlasSwitch, handleImportComplete)
    - 14-RESEARCH.md Pattern 2 (refreshKey increment pattern) and Pattern 1 (singleton read on render)
    - 14-UI-SPEC.md §Interaction Patterns — View Routing table
  </read_first>
  <action>
    Rewrite src/App.tsx. Add useState import alongside useEffect. Add four state variables: view ('import'|'klas'|'detail', initial 'import'), refreshKey (number, initial 0), activeStudentId (string|null, initial null), detailStudentList (string[], initial []). Keep the existing drop-guard useEffect verbatim. Add imports: KlasTabStrip from './components/KlasTabStrip', KlasModal from './components/KlasModal', and getActiveStudents + switchActiveKlas from '../utils/klassen'. Implement handleImportComplete() setting refreshKey(k+1) and view('klas'). Implement handleKlasSwitch(id) calling switchActiveKlas(id) then setRefreshKey(k+1) then setView based on getActiveStudents().length > 0. Implement handleStudentSelect(id, orderedList) setting activeStudentId and detailStudentList and view('detail'). Implement handleBack() setting view('klas') and activeStudentId(null). Add showModal useState(false) for KlasModal. Implement handleKlasCreated(klasId) calling switchActiveKlas(klasId) then setRefreshKey(k+1) then setView('klas') then setShowModal(false). In JSX: render KlasTabStrip (when klassenState.klassen has entries) with props activeKlasId, onSwitch, onCreateKlas; render showModal && KlasModal with onCreated and onCancel props; render ImportPage (view==='import') with onImportComplete; render placeholder div (view==='klas') with comment "KlasOverzicht goes here — Wave 2"; render placeholder div (view==='detail') with comment "DetailWeergave goes here — Wave 2". Keep the storage-error-banner div. Import klassenState from '../utils/klassen'.
  </action>
  <verify>
    <automated>cd "D:\Downloads\get-shit-done-main\dashboard-2" && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx imports useState alongside useEffect
    - App function declares: view, refreshKey, activeStudentId, detailStudentList state variables
    - App function declares: handleImportComplete, handleKlasSwitch, handleStudentSelect, handleBack functions
    - KlasTabStrip is rendered in JSX with activeKlasId, onSwitch, onCreateKlas props
    - showModal && KlasModal is rendered in JSX with onCreated, onCancel props
    - ImportPage is conditionally rendered with onImportComplete prop
    - Drop-guard useEffect is preserved (document.addEventListener dragover + drop)
    - storage-error-banner div is preserved
    - npm test exits 0 (no regression)
  </acceptance_criteria>
  <done>App.tsx routes view state, mounts KlasTabStrip and KlasModal, passes onImportComplete to ImportPage; npm test green</done>
</task>

<task type="auto">
  <name>Task 2: Add onImportComplete prop to ImportPage + create KlasTabStrip + KlasModal</name>
  <files>src/components/ImportPage.tsx, src/components/KlasTabStrip.tsx, src/components/KlasModal.tsx</files>
  <read_first>
    - src/components/ImportPage.tsx (full file — identify the three success paths where onImportComplete?.() must be called)
    - 14-PATTERNS.md §src/components/ImportPage.tsx section (exact change: add ImportPageProps interface, add prop call after each saveKlassen() success)
    - 14-PATTERNS.md §src/components/KlasTabStrip.tsx section (full pattern including no-wrapper-div constraint)
    - 14-PATTERNS.md §src/components/KlasModal.tsx section (controlled form pattern + error display)
    - 14-UI-SPEC.md §KlasTabStrip and §KlasModal for exact CSS classes and copy strings
    - index.html.bak (CSS definitions for .nav-tab, .nav-tab.active, .btn, .btn-primary, .btn-ghost)
  </read_first>
  <action>
    ImportPage.tsx: Add ImportPageProps interface with onImportComplete?: () => void before the component function. Change function signature to accept the prop. Add onImportComplete?.() call at the three success paths: (1) after the setImportState call in handlePDFs that sets status:'done' (the else branch after saved === false check), (2) after the setImportState call in handleExcel that sets status:'done', (3) after the setImportState call in handleBackup that sets status:'done' inside result.success block. No other changes to ImportPage.tsx.

    KlasTabStrip.tsx: Create new file. Import React from 'react'; import klassenState from '../../utils/klassen'. Define KlasTabStripProps interface with activeKlasId:string|null, onSwitch:(id:string)=>void, onCreateKlas:()=>void. Component reads klassen=Object.values(klassenState.klassen). Returns a fragment (or renders directly into a slot — NOT a wrapping div that breaks flex); render each klas as a button with className nav-tab or nav-tab active, key=klas.id, onClick=()=>onSwitch(klas.id), text=klas.naam. Render "+" button with className "nav-tab" style color:#3b82f6 and title="Nieuwe klas aanmaken" calling onCreateKlas. Return null if Object.values(klassenState.klassen).length === 0.

    KlasModal.tsx: Create new file. Import React, useState from 'react'; import createKlas, saveKlassen from '../../utils/klassen'. Define KlasModalProps with onCreated:(klasId:string)=>void, onCancel:()=>void. Implement with controlled state: naam, schooljaar, error (string|null), loading (boolean). handleSubmit validates naam.trim() non-empty (setError "Voer een klasnaam in." on fail). Calls await createKlas(naam.trim()) — if result.error==='duplicate' set error "Er bestaat al een klas met de naam '" + naam.trim() + "'."; if result.id exists call onCreated(result.id). Follow saveKlassen() pattern from ImportPage (saveKlassen is called internally by createKlas in klassen.ts — do not double-call it). Render fixed overlay div with z-index:1000. Modal panel max-width:400px. Form with Klasnaam input (required), Schooljaar input (optional), error paragraph, action row with Annuleren + Klas aanmaken buttons. Handle Escape key via onKeyDown on overlay. Handle Enter in klasnaam field via form onSubmit.
  </action>
  <verify>
    <automated>cd "D:\Downloads\get-shit-done-main\dashboard-2" && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ImportPage.tsx contains: interface ImportPageProps with onImportComplete?: () => void
    - ImportPage.tsx: onImportComplete?.() appears exactly 3 times (one per success path)
    - src/components/KlasTabStrip.tsx exists; exports default function KlasTabStrip
    - KlasTabStrip renders .nav-tab buttons for each klas; "+" button with title="Nieuwe klas aanmaken"
    - KlasTabStrip returns null when klassenState.klassen is empty (no tabs to show)
    - src/components/KlasModal.tsx exists; exports default function KlasModal
    - KlasModal error state shows "Voer een klasnaam in." for empty naam
    - KlasModal error state shows "Er bestaat al een klas met de naam '...'" for duplicate
    - npm test exits 0 (no regression)
  </acceptance_criteria>
  <done>ImportPage accepts onImportComplete prop and calls it after each successful import; KlasTabStrip renders klas tabs from klassenState singleton; KlasModal creates new klas with validation; npm test green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → KlasModal | Klasnaam and schooljaar inputs are user-supplied text |
| user action → App.tsx routing | View transitions triggered by user interactions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | KlasModal — klasnaam input | mitigate | Input rendered as controlled React value (not innerHTML). createKlas() handles duplicate detection. No XSS surface — klasnaam displayed via JSX string interpolation (auto-escaped). |
| T-14-02-02 | Tampering | KlasModal — schooljaar input | accept | Optional field; rendered in JSX (auto-escaped). No business logic depends on this value in Phase 14. |
| T-14-02-03 | Spoofing | App.tsx — view routing state | accept | Single-user local desktop app; no authentication or multi-user concern. View state is in-memory only. |
</threat_model>

<verification>
- npm test exits 0 after all changes
- src/App.tsx has view, refreshKey, activeStudentId, detailStudentList state
- KlasTabStrip renders without crashing when klassenState.klassen is empty (returns null)
- KlasModal shows duplicate error message matching exact Dutch copy from 14-UI-SPEC.md
- ImportPage.tsx has onImportComplete called at all 3 success paths (grep count: grep -c "onImportComplete?.()" src/components/ImportPage.tsx returns 3)
</verification>

<success_criteria>
- App.tsx compiles with no TypeScript errors
- KlasTabStrip renders in the app with tabs for each klas, "+" button visible
- Clicking a tab calls switchActiveKlas and increments refreshKey
- Clicking "+" opens KlasModal
- Klas aanmaken with a new name creates the klas and closes the modal
- Duplicate klas name shows the correct Dutch error message
- After successful PDF/Excel/backup import, App navigates to 'klas' view
</success_criteria>

<output>
After completion, create .planning/phases/14-react-ui/14-02-SUMMARY.md
</output>
