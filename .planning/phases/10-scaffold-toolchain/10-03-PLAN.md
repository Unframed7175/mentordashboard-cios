---
phase: 10-scaffold-toolchain
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - vitest.config.ts
  - tests/vitest-setup.js
  - tests/parseToetsplan.test.js
  - tests/jest.config.js
autonomous: true
requirements:
  - TCH-03

must_haves:
  truths:
    - "npm run test reports exactly 0 failures and 128 passed (TCH-03, D-05)"
    - "No test file has been rewritten — only vitest.config.ts and tests/vitest-setup.js are new files (D-04)"
    - "tests/jest.config.js is deleted — Vitest is the only test runner (D-06)"
    - "jest.fn() calls in actiepunten.test.js and backup.test.js resolve via the globalThis.jest shim in vitest-setup.js"
    - "parseToetsplan.test.js no longer imports jest-environment-jsdom (dead import removed)"
    - "All tests run in jsdom environment (window, document, localStorage available)"
    - "globals: true in vitest.config.ts so describe/it/expect are available without imports"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config: environment jsdom, globals true, setupFiles pointing to shim"
      contains: "environment: 'jsdom'"
    - path: "tests/vitest-setup.js"
      provides: "Jest API shim: globalThis.jest = { fn, spyOn, mock, resetModules, clearAllMocks, resetAllMocks }"
    - path: "tests/parseToetsplan.test.js"
      provides: "Dead import line removed (line 31: const { JSDOM } = require('jest-environment-jsdom'))"
  key_links:
    - from: "vitest.config.ts test.setupFiles"
      to: "tests/vitest-setup.js"
      via: "'./tests/vitest-setup.js'"
      pattern: "vitest-setup"
    - from: "tests/vitest-setup.js globalThis.jest.fn"
      to: "vi.fn"
      via: "vi.fn.bind(vi)"
      pattern: "vi\\.fn\\.bind"
    - from: "vitest.config.ts test.include"
      to: "tests/*.test.js"
      via: "'tests/**/*.test.{js,ts}'"
      pattern: "tests/\\*\\*/\\*\\.test"
---

<objective>
Configure Vitest to run all 128 existing tests without modifying any test files. Creates vitest.config.ts, the jest shim setupFile, removes the dead jest-environment-jsdom import from parseToetsplan.test.js (line 31, dead code), and deletes the old jest.config.js.

Purpose: TCH-03 requires `npm run test` to report 0 failures and 128 passed. Three obstacles block this without this plan: (1) jest.fn()/jest.resetModules() calls throw ReferenceError, (2) parseToetsplan.test.js crashes on module load due to dead import, (3) existing jest.config.js would conflict with Vitest. This plan resolves all three per D-04 (no test rewrites), D-05 (128 passed), and D-06 (jest removed).

Output:
- vitest.config.ts: Vitest configuration with jsdom environment, globals, setupFiles
- tests/vitest-setup.js: globalThis.jest shim mapping jest.* to vi.* equivalents
- tests/parseToetsplan.test.js: line 31 dead import removed
- tests/jest.config.js: deleted
</objective>

<execution_context>
@D:/Downloads/get-shit-done-main/dashboard-2/.claude/get-shit-done/workflows/execute-plan.md
@D:/Downloads/get-shit-done-main/dashboard-2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/ROADMAP.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/10-scaffold-toolchain/10-CONTEXT.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/10-scaffold-toolchain/10-RESEARCH.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/10-scaffold-toolchain/10-01-SUMMARY.md

<interfaces>
From package.json (Plan 01):
  scripts.test = "vitest run"
  devDependencies.vitest = "^4.1.6"
  devDependencies.jsdom = "^29.1.1"

From tests/actiepunten.test.js (line 18, 25):
  window.saveState = jest.fn();       ← needs globalThis.jest.fn shim
  jest.resetModules();                ← needs globalThis.jest.resetModules shim

From tests/backup.test.js (lines 18-19):
  window.loadKlassen = jest.fn();     ← needs globalThis.jest.fn shim
  window.saveKlassen = jest.fn(...);  ← needs globalThis.jest.fn shim

From tests/parseToetsplan.test.js (line 31 — DEAD CODE):
  const { JSDOM } = require('jest-environment-jsdom');
  JSDOM is imported but never called anywhere in the file.
  This line throws Cannot find module once jest-environment-jsdom is removed.
  Fix: delete line 31 only. Do NOT change any other line.

From tests/jest.config.js (full content):
  module.exports = {
    testEnvironment: 'jsdom',
    rootDir: '..',
    testMatch: ['**/*.test.js'],
    testPathIgnorePatterns: ['/node_modules/'],
  };
  This file must be DELETED (D-06). vitest.config.ts replaces it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vitest.config.ts and tests/vitest-setup.js</name>
  <read_first>
    - D:/Downloads/get-shit-done-main/dashboard-2/tests/jest.config.js (current Jest config — understand what to replicate: testEnvironment jsdom, rootDir .., testMatch **/*.test.js)
    - D:/Downloads/get-shit-done-main/dashboard-2/tests/actiepunten.test.js (lines 1-30 — confirm jest.fn() and jest.resetModules() usage locations)
    - D:/Downloads/get-shit-done-main/dashboard-2/tests/backup.test.js (lines 1-25 — confirm jest.fn() usage)
    - .planning/phases/10-scaffold-toolchain/10-RESEARCH.md (Pattern 2: vitest.config.ts, Pattern 3: jest shim setupFile, Pitfall 1: jest is not defined, Common Pitfalls section)
  </read_first>
  <action>
    Write vitest.config.ts at dashboard-2/vitest.config.ts with:
    - Import: defineConfig from 'vitest/config'
    - test.environment: 'jsdom' — required for window/document/localStorage (must NOT be 'node')
    - test.globals: true — required so describe/it/expect/beforeEach are available without imports
    - test.include: ['tests/**/*.test.{js,ts}'] — matches all existing .test.js files
    - test.setupFiles: ['./tests/vitest-setup.js'] — runs the jest shim before every test file
    - test.coverage.provider: 'v8'
    - test.coverage.include: ['utils/**', 'parsers/**']

    Write tests/vitest-setup.js at dashboard-2/tests/vitest-setup.js with:
    - ESM import: import { vi } from 'vitest'
    - globalThis.jest = { fn: vi.fn.bind(vi), spyOn: vi.spyOn.bind(vi), mock: vi.mock.bind(vi), resetModules: vi.resetModules.bind(vi), clearAllMocks: vi.clearAllMocks.bind(vi), resetAllMocks: vi.resetAllMocks.bind(vi), restoreAllMocks: vi.restoreAllMocks.bind(vi) }
    - Add a comment at the top: "Shim jest.* globals to vi equivalents. Required because actiepunten.test.js uses jest.fn()/jest.resetModules() and backup.test.js uses jest.fn(). Does NOT modify test files (D-04)."

    Do NOT add module aliases for jest-environment-jsdom in vitest.config.ts — that approach is NOT needed because the dead import will be removed from parseToetsplan.test.js directly (Task 2 handles this).
    Do NOT set test.pool or test.poolOptions — use Vitest 4.x defaults.
  </action>
  <verify>
    <automated>cd D:/Downloads/get-shit-done-main/dashboard-2 && node -e "const fs=require('fs'); const v=fs.readFileSync('vitest.config.ts','utf8'); const s=fs.readFileSync('tests/vitest-setup.js','utf8'); const ok = v.includes(\"environment: 'jsdom'\") && v.includes('globals: true') && v.includes('vitest-setup.js') && s.includes('vi.fn.bind(vi)') && s.includes('resetModules'); console.log(ok ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts contains: environment: 'jsdom'
    - vitest.config.ts contains: globals: true
    - vitest.config.ts contains: setupFiles: ['./tests/vitest-setup.js'] (or equivalent array with that path)
    - vitest.config.ts contains: include: ['tests/**/*.test.{js,ts}']
    - tests/vitest-setup.js contains: globalThis.jest = {
    - tests/vitest-setup.js contains: vi.fn.bind(vi)
    - tests/vitest-setup.js contains: resetModules
    - tests/vitest-setup.js uses ESM import syntax (import { vi } from 'vitest')
    - Verification node command above outputs "PASS"
  </acceptance_criteria>
  <done>vitest.config.ts created with jsdom environment, globals: true, setupFiles pointing to vitest-setup.js; tests/vitest-setup.js created with globalThis.jest shim mapping all jest.* APIs to vi.* equivalents</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead import from parseToetsplan.test.js and delete jest.config.js</name>
  <read_first>
    - D:/Downloads/get-shit-done-main/dashboard-2/tests/parseToetsplan.test.js (FULL FILE — read to confirm line 31 is the only JSDOM/jest-environment-jsdom reference, and that JSDOM variable is never used elsewhere in the file)
    - D:/Downloads/get-shit-done-main/dashboard-2/tests/jest.config.js (confirm content before deletion)
  </read_first>
  <action>
    Step 1 — Remove dead import from tests/parseToetsplan.test.js:
    Find line 31: const { JSDOM } = require('jest-environment-jsdom');
    Delete that single line. Do NOT change any other line in the file.
    Confirm by grep that "JSDOM" no longer appears in parseToetsplan.test.js.
    Confirm by grep that "jest-environment-jsdom" no longer appears in parseToetsplan.test.js.

    This line is dead code — JSDOM is imported but never called. Removing it does not change test behavior (confirmed by RESEARCH.md Pattern 3: "JSDOM is imported but never used in the test file — confirmed by grep"). This satisfies D-04 because removing dead code is not "rewriting" a test.

    Step 2 — Delete tests/jest.config.js:
    Delete the file tests/jest.config.js. This file is replaced by vitest.config.ts at the project root.
    Per D-06: Jest is fully removed; Vitest replaces it completely.

    Do NOT modify any other file in tests/. All other .test.js files remain exactly as-is.
  </action>
  <verify>
    <automated>cd D:/Downloads/get-shit-done-main/dashboard-2 && node -e "const fs=require('fs'); const pt=fs.readFileSync('tests/parseToetsplan.test.js','utf8'); const jcExists=fs.existsSync('tests/jest.config.js'); const ok = !pt.includes('jest-environment-jsdom') && !pt.includes('JSDOM') && !jcExists; console.log(ok ? 'PASS' : 'FAIL', {hasJsdomImport:pt.includes('jest-environment-jsdom'),hasJSDOM:pt.includes('JSDOM'),jestConfigExists:jcExists})"</automated>
  </verify>
  <acceptance_criteria>
    - tests/parseToetsplan.test.js does NOT contain the string "jest-environment-jsdom"
    - tests/parseToetsplan.test.js does NOT contain the string "JSDOM" (the import and the variable reference are both gone)
    - tests/jest.config.js does NOT exist (file deleted)
    - All other files in tests/ directory are unchanged (no other .test.js file was modified)
    - Verification node command above outputs "PASS"
  </acceptance_criteria>
  <done>Dead import line removed from parseToetsplan.test.js; tests/jest.config.js deleted; no other test files modified</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify 128 passed, 0 failures</name>
  <read_first>
    - D:/Downloads/get-shit-done-main/dashboard-2/vitest.config.ts (just created — confirm config before running)
    - D:/Downloads/get-shit-done-main/dashboard-2/tests/vitest-setup.js (just created — confirm shim before running)
    - .planning/phases/10-scaffold-toolchain/10-RESEARCH.md (Open Question 3: vi.resetModules() behavior in Vitest 4.x; if actiepunten.test.js fails, investigate pool config)
    - .planning/phases/10-scaffold-toolchain/10-CONTEXT.md (D-05: exact success definition — 0 failures, 128 passed; skipped tests do NOT count as passed)
  </read_first>
  <action>
    Run: npm run test (runs vitest run — no watch mode, CI-compatible)

    Expected output:
    - Test Files: X passed (X) — all test files pass
    - Tests: 128 passed (128)
    - "0 failures" visible in summary
    - No "ReferenceError: jest is not defined" errors
    - No "Cannot find module 'jest-environment-jsdom'" errors
    - No skipped tests (skipped ≠ passed per D-05)

    If any tests fail, diagnose and fix WITHOUT modifying test files:
    - "jest is not defined" → vitest-setup.js shim not loading; check setupFiles path in vitest.config.ts
    - "Cannot find module 'jest-environment-jsdom'" → dead import still present in parseToetsplan.test.js; re-check Task 2
    - "localStorage is not defined" → environment is not jsdom; check vitest.config.ts environment setting
    - "describe is not defined" → globals: true missing from vitest.config.ts
    - actiepunten.test.js failures after jest.resetModules() → Open Question 3 from RESEARCH.md; try adding test.pool: 'forks' to vitest.config.ts if resetModules causes module isolation issues

    Only fix configuration files (vitest.config.ts, tests/vitest-setup.js) and the one dead import line. No test file content may be changed.

    Once the run shows 128 passed, 0 failures, capture the terminal output.
  </action>
  <verify>
    <automated>cd D:/Downloads/get-shit-done-main/dashboard-2 && npm run test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm run test exits with code 0
    - Terminal output contains: "128 passed" (exact number)
    - Terminal output does NOT contain: "failed" (case-insensitive)
    - Terminal output does NOT contain: "skipped" (skipped tests invalidate D-05)
    - Terminal output does NOT contain: "ReferenceError: jest is not defined"
    - Terminal output does NOT contain: "Cannot find module 'jest-environment-jsdom'"
    - All 9 test files in tests/ directory are included in the run (actiepunten, aggregation, backup, feedback, parseStage, parseToetsplan, prognosis, spider, plus any others)
  </acceptance_criteria>
  <done>npm run test exits 0 with output showing exactly 128 passed, 0 failures, 0 skipped — TCH-03 satisfied per D-05</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vitest-setup.js → test globals | Setup file injects globalThis.jest before every test; must not pollute production code |
| test files → utils/parsers | Tests require() IIFE modules; jsdom environment provides window globals they depend on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Tampering | globalThis.jest shim introduces jest object in all test environments | accept | Shim only in setupFiles (test-only scope); not imported in production code; no production impact |
| T-10-10 | Tampering | parseToetsplan.test.js line deletion removes too much | mitigate | Read full file before editing; confirm JSDOM is never used (grep); delete only line 31; verify node command checks exact absence |
| T-10-11 | Denial of Service | vi.resetModules() in Vitest 4.x changes module cache differently than Jest — could cause test isolation failures | accept | Acceptance criteria includes npm run test exit 0 with 128 passed; if this occurs, add test.pool: 'forks' as documented fallback in Task 3 action |
</threat_model>

<verification>
After all three tasks complete:

1. vitest.config.ts exists with environment: 'jsdom', globals: true, setupFiles pointing to vitest-setup.js
2. tests/vitest-setup.js exists with globalThis.jest shim containing fn, resetModules, spyOn, mock, clearAllMocks, resetAllMocks
3. tests/parseToetsplan.test.js does NOT contain "jest-environment-jsdom" or "JSDOM"
4. tests/jest.config.js does NOT exist
5. npm run test exits 0 with "128 passed" in output and no failures or skipped
</verification>

<success_criteria>
- TCH-03 fully satisfied: npm run test reports exactly 128 passed, 0 failures, 0 skipped
- D-04 honored: no test file content was rewritten (only dead import line removed from parseToetsplan.test.js)
- D-05 honored: 128 passed — not 127, not "127 passed 1 skipped"
- D-06 honored: tests/jest.config.js deleted; jest not in package.json
</success_criteria>

<output>
After completion, create .planning/phases/10-scaffold-toolchain/10-03-SUMMARY.md
</output>
