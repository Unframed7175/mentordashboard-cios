---
phase: 12-versleutelde-opslag
plan: 04
type: execute
wave: 3
depends_on:
  - 12-02
  - 12-03
files_modified:
  - tests/storage.test.ts
autonomous: true
requirements:
  - STO-01
  - STO-02
  - STO-03
  - STO-04

must_haves:
  truths:
    - "npm run test -- tests/storage.test.ts slaagt met alle 4 tests groen"
    - "Test STO-01: saveKlassen + loadKlassen round-trip herstelt klassenState correct"
    - "Test STO-02: store bevat mock_encrypted ciphertext, niet de plaintext klasnaam"
    - "Test STO-03: loadKlassen bij aanwezige localStorage 'mentordashboard_klassen_v1' triggert migratie en verwijdert legacy key"
    - "Test STO-04: deleteStudent verwijdert leerling L1, behoudt L2, en store bevat updated ciphertext"
    - "npm run test (volledige suite) slaagt zonder regressies"
  artifacts:
    - path: "tests/storage.test.ts"
      provides: "Vitest unit tests voor STO-01, STO-02, STO-03, STO-04"
      contains: "vi.mock('@tauri-apps/plugin-store'"
  key_links:
    - from: "tests/storage.test.ts"
      to: "utils/klassen.ts saveKlassen, loadKlassen, deleteStudent"
      via: "directe import"
      pattern: "from '../utils/klassen'"
    - from: "tests/storage.test.ts vi.mock"
      to: "@tauri-apps/plugin-store LazyStore"
      via: "in-memory Map als stub"
      pattern: "_storeData"
---

<objective>
Schrijf tests/storage.test.ts met Vitest unit tests die STO-01 t/m STO-04 aantonen. Plugin-store en invoke() worden gemockt zodat tests zonder Tauri runtime draaien.

Purpose: Sluit de fase af met geautomatiseerd bewijs dat alle vier requirements werken. De mock-strategie verifieert de storage-contracten (ciphertext opgeslagen, niet plaintext) zonder echte AES of keychain.
Output: tests/storage.test.ts aangemaakt; npm run test -- tests/storage.test.ts slaagt; npm run test (volledig) slaagt.
</objective>

<execution_context>
@D:/Downloads/get-shit-done-main/dashboard-2/.claude/get-shit-done/workflows/execute-plan.md
@D:/Downloads/get-shit-done-main/dashboard-2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/ROADMAP.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/STATE.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-CONTEXT.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-RESEARCH.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-PATTERNS.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-02-SUMMARY.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-03-SUMMARY.md

<interfaces>
<!-- utils/klassen.ts exports na Plans 02/03 (lees het bijgewerkte bestand): -->
<!-- export const klassenState: { klassen: Record<string, any>; activeKlasId: string | null } -->
<!-- export async function saveKlassen(): Promise<boolean> -->
<!-- export async function loadKlassen(): Promise<boolean> -->
<!-- export async function deleteStudent(klasId: string, leerlingId: string): Promise<boolean> -->
<!-- export async function switchActiveKlas(klasId: string): Promise<boolean> -->
<!-- export async function createKlas(naam: string): Promise<any> -->
<!-- export async function deleteKlas(klasId: string): Promise<boolean> -->
<!-- export function getActiveStudents(): any[] -->
<!-- export function getAllRecordsForStudent(leerlingId: string): any[] -->

<!-- Mock strategie (RESEARCH.md Pattern 5 + PATTERNS.md §tests/storage.test.ts): -->
<!-- _storeData: Map<string, unknown> — in-memory store stub -->
<!-- vi.mock('@tauri-apps/plugin-store') — LazyStore mock met get/set/save/delete -->
<!-- vi.mock('@tauri-apps/api/core') — invoke mock: encrypt_klassen prefix 'mock_encrypted:' -->
<!--                                              decrypt_klassen strip 'mock_encrypted:' -->

<!-- tests/backup.test.ts structuur (analog — lees dit bestand als structuurpatroon): -->
<!-- beforeEach reset klassenState.klassen = {}; klassenState.activeKlasId = null -->
<!-- test() blokken met async/await -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: tests/storage.test.ts aanmaken met alle vier STO-tests</name>
  <files>tests/storage.test.ts</files>
  <read_first>
    - utils/klassen.ts (volledig — lees de bijgewerkte versie na Plans 02/03 om exacte exports te kennen)
    - tests/backup.test.ts (volledig — structuurpatroon: imports, beforeEach, test blokken)
    - .planning/phases/12-versleutelde-opslag/12-PATTERNS.md §tests/storage.test.ts (volledig — mock patroon en test bodies)
    - .planning/phases/12-versleutelde-opslag/12-RESEARCH.md §Pattern 5 (Vitest mock voor plugin-store)
    - .planning/phases/12-versleutelde-opslag/12-RESEARCH.md §Validation Architecture §Phase Requirements → Test Map
    - .planning/phases/12-versleutelde-opslag/12-CONTEXT.md §D-12-13
  </read_first>
  <behavior>
    - Test STO-01: na saveKlassen() + state-reset + loadKlassen() → klassenState.klassen['klas_1'].naam === 'Test Klas'
    - Test STO-02: na saveKlassen() → _storeData.get('klassen') bevat 'mock_encrypted:' en NIET de klasnaam 'Geheim'
    - Test STO-03: loadKlassen() bij lege _storeData + localStorage 'mentordashboard_klassen_v1' → klassenState.klassen['klas_old'] defined; _storeData.has('klassen') === true; localStorage.getItem('mentordashboard_klassen_v1') === null
    - Test STO-04: deleteStudent('klas_1', 'L1') → students.find(s => s.leerlingId === 'L1') === undefined; students.find(s => s.leerlingId === 'L2') !== undefined; _storeData.has('klassen') === true
  </behavior>
  <action>
Maak tests/storage.test.ts aan als nieuw bestand. Het bestand bestaat nog niet.

**Structuur van het bestand (in volgorde):**

1. Bestandsheader commentaar (patroon van backup.test.ts):
   // storage.test.ts — saveKlassen, loadKlassen, deleteStudent unit tests
   // Phase 12 Plan 04 — STO-01, STO-02, STO-03, STO-04
   // Plugin-store en invoke() zijn gemockt — tests draaien zonder Tauri runtime

2. vi.mock aanroepen (MOETEN op module top-level staan — Vitest hoist ze):
   - vi.mock('@tauri-apps/plugin-store', factory) met in-memory _storeData Map:
     LazyStore: vi.fn().mockImplementation(() => ({
       get: vi.fn(async (key: string) => _storeData.get(key) ?? null),
       set: vi.fn(async (key: string, value: unknown) => { _storeData.set(key, value); }),
       save: vi.fn(async () => {}),
       delete: vi.fn(async (key: string) => { _storeData.delete(key); }),
     }))
   - vi.mock('@tauri-apps/api/core', factory):
     invoke: vi.fn(async (cmd: string, args: any) => {
       if (cmd === 'encrypt_klassen') return `mock_encrypted:${args.plaintext}`;
       if (cmd === 'decrypt_klassen') return args.ciphertext.replace('mock_encrypted:', '');
       return null;
     })

3. Imports (na vi.mock — Vitest hoist mocks voor imports):
   import { vi, describe, it, expect, beforeEach, test } from 'vitest';
   import { klassenState, saveKlassen, loadKlassen, deleteStudent } from '../utils/klassen';

4. Declareer _storeData VOOR vi.mock (let _storeData: Map<string, unknown>;) — initialiseer in beforeEach.

   LETOP: vi.mock factory closures moeten over _storeData kunnen, maar de Map mag nog niet bestaan als vi.mock wordt uitgevoerd (Vitest hoist). Gebruik een module-level declaratie en initialiseer in beforeEach:
     let _storeData = new Map<string, unknown>();
   In de factory closures gebruik je direct _storeData (geen const in de factory).

5. beforeEach blok:
   - _storeData.clear() (of reassign: _storeData = new Map())
   - vi.clearAllMocks()
   - klassenState.klassen = {};
   - klassenState.activeKlasId = null;
   - localStorage.clear();

6. Vier test blokken (gebruik test() niet describe/it voor consistentie met backup.test.ts):

   **STO-01 — save/load round-trip:**
     klassenState.klassen = { klas_1: { id: 'klas_1', naam: 'Test Klas', students: [] } };
     klassenState.activeKlasId = 'klas_1';
     await saveKlassen();
     klassenState.klassen = {};
     klassenState.activeKlasId = null;
     const result = await loadKlassen();
     expect(result).toBe(true);
     expect(klassenState.klassen['klas_1']).toBeDefined();
     expect(klassenState.klassen['klas_1'].naam).toBe('Test Klas');

   **STO-02 — ciphertext, niet plaintext:**
     klassenState.klassen = { klas_1: { id: 'klas_1', naam: 'Geheim', students: [] } };
     await saveKlassen();
     const stored = _storeData.get('klassen') as string;
     expect(stored).toContain('mock_encrypted:');
     expect(stored).not.toContain('Geheim');

   **STO-03 — localStorage migratie:**
     localStorage.setItem('mentordashboard_klassen_v1', JSON.stringify({
       klassen: { klas_old: { id: 'klas_old', naam: 'Oude Klas', students: [] } },
       activeKlasId: 'klas_old',
     }));
     const result = await loadKlassen();
     expect(result).toBe(true);
     expect(klassenState.klassen['klas_old']).toBeDefined();
     expect(_storeData.has('klassen')).toBe(true);
     expect(localStorage.getItem('mentordashboard_klassen_v1')).toBeNull();

   **STO-04 — deleteStudent:**
     klassenState.klassen = {
       klas_1: { id: 'klas_1', naam: 'Klas 1', students: [
         { leerlingId: 'L1', naam: 'De Vries, A.' },
         { leerlingId: 'L2', naam: 'Jansen, B.' },
       ]},
     };
     klassenState.activeKlasId = 'klas_1';
     const result = await deleteStudent('klas_1', 'L1');
     expect(result).toBe(true);
     const remaining = klassenState.klassen['klas_1'].students;
     expect(remaining.find((s: any) => s.leerlingId === 'L1')).toBeUndefined();
     expect(remaining.find((s: any) => s.leerlingId === 'L2')).toBeDefined();
     expect(_storeData.has('klassen')).toBe(true);

Draai na aanmaken: npm run test -- tests/storage.test.ts
Als een test faalt, diagnosticeer het probleem in utils/klassen.ts (niet in de test) en herstel de implementatie. Tests falen alleen als de implementatie incorrect is.

Draai tot slot: npm run test (volledige suite)
  </action>
  <verify>
    <automated>npm run test -- tests/storage.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/storage.test.ts bestaat
    - tests/storage.test.ts bevat vi.mock('@tauri-apps/plugin-store'
    - tests/storage.test.ts bevat vi.mock('@tauri-apps/api/core'
    - tests/storage.test.ts bevat 4 test() blokken
    - npm run test -- tests/storage.test.ts slaagt met 4 passed, 0 failed
    - Testuitvoer bevat: "STO-01" of "save/load round-trip" en "STO-04" of "deleteStudent" in test namen
    - npm run test (volledige suite) slaagt (exit 0) — geen regressies in bestaande tests
  </acceptance_criteria>
  <done>Alle 4 STO-requirements hebben geautomatiseerde tests; volledige test suite groen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grens | Beschrijving |
|-------|-------------|
| Test → gemockte store | Mock vervangt file I/O volledig; geen echte sleutels of bestanden tijdens tests |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatie |
|-----------|-----------|-----------|------------|-----------|
| T-12-12 | Information Disclosure | Test fixtures met leerlingdata | accept | Test-data is fictief (L1: 'De Vries, A.', L2: 'Jansen, B.'); geen echte PII in testbestand |
| T-12-13 | Tampering | Mock bypass van echte AES | accept | Mock verifieert storage-contract (ciphertext !== plaintext); echte AES-correctheid wordt getest door Rust cargo test encrypt_decrypt_roundtrip (Plan 01) |
</threat_model>

<verification>
Na uitvoering van dit plan:

1. npm run test -- tests/storage.test.ts slaagt: "4 passed"
2. npm run test (volledige suite) slaagt: geen failed tests
3. grep "vi.mock.*plugin-store" tests/storage.test.ts geeft een resultaat
4. grep "mock_encrypted" tests/storage.test.ts geeft een resultaat
5. npm run typecheck slaagt (exit 0) — storage.test.ts introduceert geen type-fouten
</verification>

<success_criteria>
- tests/storage.test.ts bestaat met vier tests voor STO-01, STO-02, STO-03, STO-04
- Alle vier tests slagen: npm run test -- tests/storage.test.ts exits 0
- Volledige test suite slaagt: npm run test exits 0
- Geen bestaande tests zijn gebroken
</success_criteria>

<output>
Na voltooiing, maak aan: .planning/phases/12-versleutelde-opslag/12-04-SUMMARY.md
</output>
