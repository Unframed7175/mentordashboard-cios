---
phase: 12-versleutelde-opslag
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - utils/leerlijnen.ts
  - utils/datamodel.ts
autonomous: true
requirements:
  - STO-01

must_haves:
  truths:
    - "getLeerlijnenMapping() is async en leest uit plugin-store onder key 'leerlijnen'"
    - "saveLeerlijnenMapping() is async en roept store.save() aan na store.set()"
    - "resetLeerlijnenMapping() is async en verwijdert de 'leerlijnen' store-entry"
    - "Leerlijnen auto-migratie: bij aanwezige localStorage-data wordt deze naar plugin-store gemigreerd en legacy key verwijderd"
    - "saveState() in datamodel.ts is een no-op (retourneert true, doet niets)"
    - "loadState() in datamodel.ts retourneert false met deprecation waarschuwing"
  artifacts:
    - path: "utils/leerlijnen.ts"
      provides: "Async leerlijnen-opslag via plugin-store (plain JSON, onversleuteld per D-12-06)"
      exports: ["getLeerlijnenMapping", "saveLeerlijnenMapping", "resetLeerlijnenMapping"]
    - path: "utils/datamodel.ts"
      provides: "Deprecated saveState/loadState; appState en overige exports ongewijzigd"
      contains: "Deprecated"
  key_links:
    - from: "utils/leerlijnen.ts saveLeerlijnenMapping"
      to: "LazyStore store.json"
      via: "store.set('leerlijnen', JSON.stringify(mapping))"
      pattern: "store\\.set.*leerlijnen"
    - from: "utils/leerlijnen.ts getLeerlijnenMapping"
      to: "localStorage legacy key"
      via: "localStorage.getItem(LEERLIJNEN_LEGACY_KEY)"
      pattern: "LEERLIJNEN_LEGACY_KEY"
---

<objective>
Migreer utils/leerlijnen.ts van synchrone localStorage naar async plugin-store (onversleuteld, plain JSON per D-12-06). Depreceer saveState() en verwijder loadState() in utils/datamodel.ts per D-12-07.

Purpose: Voltooiet de storage-migratie voor de niet-persoonsgebonden leerlijnen-data en verwijdert de verouderde dual-write constructie. Kan parallel uitgevoerd worden met Plan 02 omdat er geen gedeelde bestanden zijn.
Output: utils/leerlijnen.ts volledig async; utils/datamodel.ts saveState als no-op; npm run typecheck slaagt.
</objective>

<execution_context>
@D:/Downloads/get-shit-done-main/dashboard-2/.claude/get-shit-done/workflows/execute-plan.md
@D:/Downloads/get-shit-done-main/dashboard-2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/ROADMAP.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/STATE.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-CONTEXT.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-RESEARCH.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-PATTERNS.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-01-SUMMARY.md

<interfaces>
<!-- Huidig utils/leerlijnen.ts (volledig gelezen voor implementatie): -->
<!-- const STORAGE_KEY = 'mentordashboard_leerlijnen_v1' -->
<!-- let _cachedMapping: Record<string, string> | null = null -->
<!-- function buildDefault(): Record<string, string> — ongewijzigd -->
<!-- function isValid(mapping: any): boolean — ongewijzigd -->
<!-- export function getLeerlijnenMapping(): Record<string, string> — wordt async -->
<!-- export function saveLeerlijnenMapping(mapping: any): boolean — wordt async -->
<!-- export function resetLeerlijnenMapping(): void — wordt async -->

<!-- Huidig utils/datamodel.ts (relevant gedeelte): -->
<!-- var STORAGE_KEY = 'mentordashboard_v1' — behouden voor clearState() -->
<!-- export function saveState(): boolean — wordt no-op -->
<!-- export function loadState(): boolean — wordt deprecated stub -->
<!-- export function clearState(): void — ONGEWIJZIGD -->
<!-- export const appState — ONGEWIJZIGD -->
<!-- export function addStudent, mergeVerzuim, getVerzuim, normalizeNaam — ONGEWIJZIGD -->

<!-- Plugin-store API (zelfde store.json als klassen.ts — beide LazyStore instanties delen hetzelfde bestand): -->
<!-- const store = new LazyStore('store.json', { autoSave: false }) -->
<!-- await store.get<string>('leerlijnen') -> string | null -->
<!-- await store.set('leerlijnen', JSON.stringify(mapping)) -->
<!-- await store.save() -->
<!-- await store.delete('leerlijnen') -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: utils/leerlijnen.ts async-ificeren naar plugin-store</name>
  <files>utils/leerlijnen.ts</files>
  <read_first>
    - utils/leerlijnen.ts (volledig — alle 92 regels; lees voor je begint)
    - .planning/phases/12-versleutelde-opslag/12-PATTERNS.md §utils/leerlijnen.ts (alle subsecties)
    - .planning/phases/12-versleutelde-opslag/12-RESEARCH.md §Anti-Patterns to Avoid (store.save() verplicht)
    - .planning/phases/12-versleutelde-opslag/12-CONTEXT.md §D-12-06 (leerlijnen blijft plain JSON)
  </read_first>
  <behavior>
    - getLeerlijnenMapping() aangeroepen na saveLeerlijnenMapping(mapping) → retourneert opgeslagen mapping (via cache)
    - getLeerlijnenMapping() bij lege store + aanwezige localStorage 'mentordashboard_leerlijnen_v1' → migreert, legacy key verwijderd, retourneert mapping
    - getLeerlijnenMapping() bij lege store + lege localStorage → retourneert buildDefault()
    - saveLeerlijnenMapping(mapping) → store.set + store.save aangeroepen; _cachedMapping genullificeerd
    - resetLeerlijnenMapping() → store.delete('leerlijnen') + store.save aangeroepen; cache genullificeerd
  </behavior>
  <action>
Pas utils/leerlijnen.ts aan. Lees het bestand volledig voor je begint. Wijzig UITSLUITEND wat hieronder staat.

**Imports (voeg toe na bestaande import DEELGEBIEDEN):**
  import { LazyStore } from '@tauri-apps/plugin-store';

**Module-scope constanten (vervang STORAGE_KEY declaratie):**
  Verwijder: const STORAGE_KEY = 'mentordashboard_leerlijnen_v1';
  Voeg toe:
    const store = new LazyStore('store.json', { autoSave: false });
    const LEERLIJNEN_STORE_KEY = 'leerlijnen';
    const LEERLIJNEN_LEGACY_KEY = 'mentordashboard_leerlijnen_v1';

**buildDefault() en isValid():** ONGEWIJZIGD laten.

**getLeerlijnenMapping (vervang regels 40–58):**
  export async function getLeerlijnenMapping(): Promise<Record<string, string>>
  Body (per PATTERNS.md §getLeerlijnenMapping async conversion):
    if (_cachedMapping !== null) return _cachedMapping;
    try {
      const stored = await store.get<string>(LEERLIJNEN_STORE_KEY);
      if (stored) {
        const parsed = JSON.parse(stored);
        if (isValid(parsed)) { _cachedMapping = parsed; return _cachedMapping!; }
      }
      // Legacy migration
      const legacy = localStorage.getItem(LEERLIJNEN_LEGACY_KEY);
      if (legacy) {
        const parsed = JSON.parse(legacy);
        if (isValid(parsed)) {
          await saveLeerlijnenMapping(parsed);    // persist naar store
          localStorage.removeItem(LEERLIJNEN_LEGACY_KEY);
          _cachedMapping = parsed;
          return _cachedMapping!;
        }
      }
    } catch (e: any) {
      console.warn('[leerlijnen.ts] read error:', e);
    }
    _cachedMapping = buildDefault();
    return _cachedMapping;

**saveLeerlijnenMapping (vervang regels 66–75):**
  export async function saveLeerlijnenMapping(mapping: any): Promise<boolean>
  Body:
    try {
      await store.set(LEERLIJNEN_STORE_KEY, JSON.stringify(mapping));
      await store.save();   // VERPLICHT: set() is alleen in-memory
      _cachedMapping = null;
      return true;
    } catch (e: any) {
      console.warn('[leerlijnen.ts] plugin-store write error:', e);
      return false;
    }

**resetLeerlijnenMapping (vervang regels 82–89):**
  export async function resetLeerlijnenMapping(): Promise<void>
  Body:
    try {
      await store.delete(LEERLIJNEN_STORE_KEY);
      await store.save();
    } catch (e: any) {
      console.warn('[leerlijnen.ts] plugin-store remove error:', e);
    }
    _cachedMapping = null;

Draai na implementatie: npm run typecheck
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - utils/leerlijnen.ts importeert LazyStore uit '@tauri-apps/plugin-store'
    - utils/leerlijnen.ts bevat NIET meer: localStorage.setItem(STORAGE_KEY
    - utils/leerlijnen.ts bevat NIET meer: localStorage.getItem(STORAGE_KEY (originele opslag-aanroep)
    - utils/leerlijnen.ts bevat: export async function getLeerlijnenMapping(): Promise<Record<string, string>>
    - utils/leerlijnen.ts bevat: export async function saveLeerlijnenMapping(mapping: any): Promise<boolean>
    - utils/leerlijnen.ts bevat: export async function resetLeerlijnenMapping(): Promise<void>
    - utils/leerlijnen.ts bevat: LEERLIJNEN_STORE_KEY = 'leerlijnen'
    - utils/leerlijnen.ts bevat: LEERLIJNEN_LEGACY_KEY = 'mentordashboard_leerlijnen_v1'
    - utils/leerlijnen.ts bevat: await store.save() in saveLeerlijnenMapping
    - npm run typecheck slaagt (exit 0)
  </acceptance_criteria>
  <done>leerlijnen.ts volledig async via plugin-store; legacy localStorage migratie aanwezig; typecheck groen.</done>
</task>

<task type="auto">
  <name>Taak 2: saveState en loadState depreceren in utils/datamodel.ts</name>
  <files>utils/datamodel.ts</files>
  <read_first>
    - utils/datamodel.ts (volledig — alle 261 regels; lees voor je begint)
    - .planning/phases/12-versleutelde-opslag/12-PATTERNS.md §utils/datamodel.ts
    - .planning/phases/12-versleutelde-opslag/12-CONTEXT.md §D-12-07
  </read_first>
  <action>
Pas utils/datamodel.ts aan. Lees het bestand volledig voor je begint.

Wijzig UITSLUITEND de functies saveState() (regels 218–230) en loadState() (regels 236–251).
Alle overige exports (appState, addStudent, mergeVerzuim, normalizeNaam, getVerzuim, clearState) blijven ONGEWIJZIGD.
STORAGE_KEY (regel 212) blijft ongewijzigd — clearState() gebruikt die nog voor localStorage.removeItem.

**saveState (vervang regels 218–230):**
Vervang de volledige function body door een no-op:
  /** @deprecated Phase 12: klassen.ts is de enige writer. Deze functie doet niets. D-12-07 */
  export function saveState(): boolean {
    // Deprecated per D-12-07 — vervangen door saveKlassen() in utils/klassen.ts
    return true;
  }

**loadState (vervang regels 236–251):**
Vervang door een deprecation stub die false retourneert:
  /** @deprecated Phase 12: gebruik loadKlassen() uit utils/klassen.ts. D-12-07 */
  export function loadState(): boolean {
    console.warn('[datamodel] loadState is deprecated — gebruik loadKlassen() uit klassen.ts');
    return false;
  }

Draai na implementatie: npm run typecheck
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - utils/datamodel.ts bevat: export function saveState(): boolean met body bestaande uitsluitend uit return true en @deprecated JSDoc
    - utils/datamodel.ts bevat NIET meer: localStorage.setItem(STORAGE_KEY in saveState (body is no-op)
    - utils/datamodel.ts bevat: export function loadState(): boolean met body bestaande uitsluitend uit console.warn + return false
    - utils/datamodel.ts bevat NIET meer: localStorage.getItem(STORAGE_KEY in loadState
    - utils/datamodel.ts clearState() is ONGEWIJZIGD (bevat nog steeds localStorage.removeItem(STORAGE_KEY))
    - utils/datamodel.ts appState, addStudent, mergeVerzuim, normalizeNaam, getVerzuim zijn ONGEWIJZIGD
    - npm run typecheck slaagt (exit 0)
  </acceptance_criteria>
  <done>saveState() is een no-op; loadState() retourneert false met waarschuwing; alle overige exports ongewijzigd; typecheck groen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grens | Beschrijving |
|-------|-------------|
| utils/leerlijnen.ts → plugin-store | Plain JSON opgeslagen (geen PII — D-12-06); geen encryptie vereist |
| utils/datamodel.ts legacy callers | saveState() callers ontvangen true (no-op); loadState() callers ontvangen false — geen data-lek |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatie |
|-----------|-----------|-----------|------------|-----------|
| T-12-09 | Information Disclosure | leerlijnen plain JSON | accept | Leerlijnen-mapping bevat geen persoonsdata (dgId→leerlijn mapping); plain storage is bewust per D-12-06 |
| T-12-10 | Tampering | saveLeerlijnenMapping fire-and-forget | mitigate | store.save() altijd awaited na store.set() in saveLeerlijnenMapping en resetLeerlijnenMapping |
| T-12-11 | Spoofing | deprecated loadState callers | accept | loadState() retourneert false; callers zien een lege state en vallen terug op loadKlassen() — geen data-corruptie mogelijk |
</threat_model>

<verification>
Na uitvoering van dit plan:

1. npm run typecheck slaagt (exit 0)
2. grep "async function getLeerlijnenMapping" utils/leerlijnen.ts geeft een resultaat
3. grep "localStorage.setItem" utils/leerlijnen.ts geeft GEEN resultaat
4. grep "localStorage.setItem" utils/datamodel.ts | grep "saveState" geeft GEEN resultaat (saveState is no-op)
5. grep "@deprecated" utils/datamodel.ts geeft minstens 2 resultaten (saveState + loadState)
</verification>

<success_criteria>
- utils/leerlijnen.ts gebruikt uitsluitend plugin-store; legacy localStorage alleen gelezen tijdens eenmalige migratie
- utils/datamodel.ts saveState is een gedocumenteerde no-op; loadState is een deprecated stub
- npm run typecheck groen
- Geen bestaande exports zijn verwijderd (clearState, appState, etc. ongewijzigd)
</success_criteria>

<output>
Na voltooiing, maak aan: .planning/phases/12-versleutelde-opslag/12-03-SUMMARY.md
</output>
