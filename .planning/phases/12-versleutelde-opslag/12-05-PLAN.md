---
phase: 12-versleutelde-opslag
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/crypto.rs
  - utils/leerlijnen.ts
  - src-tauri/capabilities/default.json
autonomous: true
requirements:
  - STO-01
  - STO-02
  - STO-03

must_haves:
  truths:
    - "get_or_init_key() en get_key() retourneren Err als decoded key != 32 bytes (geen panic)"
    - "decrypt_klassen() retourneert Err voor inputs < 28 bytes (12 nonce + 16 auth tag)"
    - "getLeerlijnenMapping() verwijdert localStorage legacy key alleen na bevestigde store write"
    - "capabilities/default.json bevat 'secure-storage:default'"
    - "cargo check slaagt zonder fouten na alle Rust-wijzigingen"
  artifacts:
    - path: "src-tauri/src/crypto.rs"
      provides: "AES-256-GCM commands met sleutellengte-validatie en correct minimumlengte-guard"
    - path: "utils/leerlijnen.ts"
      provides: "Leerlijnen-migratie met write-confirm guard voor localStorage removal"
    - path: "src-tauri/capabilities/default.json"
      provides: "Tauri capabilities inclusief secure-storage:default"
---

<objective>
Gap closure for Phase 12 (Versleutelde Opslag). Three targeted fixes identified in 12-VERIFICATION.md and 12-REVIEW.md. No existing functionality is rewritten — only the three specific defects are patched.

Purpose: Close the verification gaps CR-01, CR-03, CR-04, and WR-02 so the phase passes all must-have checks.

Output:
- src-tauri/src/crypto.rs with 32-byte key length guards in get_or_init_key() and get_key(), and a corrected 28-byte minimum in decrypt_klassen().
- utils/leerlijnen.ts with a write-confirm guard before localStorage.removeItem() in the legacy migration path.
- src-tauri/capabilities/default.json with "secure-storage:default" added to the permissions array.
</objective>

<execution_context>
@D:/Downloads/get-shit-done-main/dashboard-2/.claude/get-shit-done/workflows/execute-plan.md
@D:/Downloads/get-shit-done-main/dashboard-2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/ROADMAP.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/STATE.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-CONTEXT.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-VERIFICATION.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/12-versleutelde-opslag/12-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: crypto.rs — key-length guard + decrypt minimum (CR-01, CR-04)</name>
  <files>src-tauri/src/crypto.rs</files>
  <read_first>
    Read src-tauri/src/crypto.rs fully before editing. Relevant lines:
    - Lines 37–41: get_or_init_key() — the STANDARD.decode one-liner that currently returns directly without a length check.
    - Lines 78–82: get_key() — the match arm for Some(key_b64) that decodes and returns directly without a length check.
    - Line 122: decrypt_klassen() — the `if decoded.len() < 12` guard that uses the wrong minimum.
  </read_first>
  <action>
    Apply three surgical edits to src-tauri/src/crypto.rs. Do NOT touch any other lines.

    Edit 1 — get_or_init_key(), lines 37–41.
    Replace the current one-liner `STANDARD.decode(&key_b64).map_err(|e| format!("Key decode error: {e}"))` with a two-step form:
    First decode into a named binding `decoded`, then check `decoded.len() != 32` and return `Err(format!("Sleutellengte ongeldig: {} bytes (verwacht 32)", decoded.len()))` if the check fails, otherwise return `Ok(decoded)`.
    The return type of this branch must remain `Result<Vec<u8>, String>`.

    Edit 2 — get_key(), lines 78–82.
    Replace the `Some(key_b64)` match arm body. Currently it decodes and returns in one expression. Change it to the same two-step pattern as Edit 1: decode into `decoded`, check `decoded.len() != 32`, return Err with the Dutch message "Sleutellengte ongeldig: {} bytes (verwacht 32)" if false, return `Ok(decoded)` if true.

    Edit 3 — decrypt_klassen(), line 122.
    Change the guard condition from `decoded.len() < 12` to `decoded.len() < 28`.
    Change the error string from `"Ciphertext too short"` to `format!("Ciphertext te kort: {} bytes (minimum 28: 12 nonce + 16 auth tag)", decoded.len())`.
    The `return Err(...)` structure stays the same; only the condition value and message change.

    After all three edits, run: `cargo check --manifest-path src-tauri/Cargo.toml`
    The command must exit 0 with no errors. Fix any compile errors before marking done.
  </action>
  <verify>
    <automated>cargo check --manifest-path D:/Downloads/get-shit-done-main/dashboard-2/src-tauri/Cargo.toml</automated>
  </verify>
  <done>
    - get_or_init_key() contains a `decoded.len() != 32` check that returns Err before calling from_slice.
    - get_key() contains the same `decoded.len() != 32` check in the Some(key_b64) match arm.
    - decrypt_klassen() guard condition is `decoded.len() < 28` with a Dutch format message.
    - `cargo check` exits 0 with no errors or warnings about these lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: leerlijnen.ts — write-confirm guard before localStorage removal (WR-02)</name>
  <files>utils/leerlijnen.ts</files>
  <read_first>
    Read utils/leerlijnen.ts fully before editing. Relevant lines:
    - Lines 56–60: the legacy migration block inside getLeerlijnenMapping(). Currently calls `await saveLeerlijnenMapping(parsed)` but discards the return value, then unconditionally calls `localStorage.removeItem(LEERLIJNEN_LEGACY_KEY)`.
    - Lines 76–86: saveLeerlijnenMapping() signature — returns `Promise<boolean>` (true on success, false on error). This is the return value that must be captured.
    - For reference pattern, note that klassen.ts _migrateLocalStorageToStore() uses the same guard pattern.
  </read_first>
  <action>
    Apply one targeted edit to utils/leerlijnen.ts, lines 56–61.

    Current block (inside the `if (isValid(parsed))` branch, lines 57–60):
      `await saveLeerlijnenMapping(parsed);` — return value discarded
      `localStorage.removeItem(LEERLIJNEN_LEGACY_KEY);` — runs unconditionally
      `_cachedMapping = parsed;`
      `return _cachedMapping!;`

    Replace with a write-confirm guard:
    Capture the return value: `const saved = await saveLeerlijnenMapping(parsed);`
    Then wrap the removeItem and cache assignment inside `if (saved)` so that:
    - If saved is true: call `localStorage.removeItem(LEERLIJNEN_LEGACY_KEY)`, set `_cachedMapping = parsed`, and return `_cachedMapping!`.
    - If saved is false: do NOT remove the legacy key and do NOT return here — fall through to the `buildDefault()` call at line 66.

    The outer `if (legacy)` and `if (isValid(parsed))` blocks are unchanged. Only the inner four lines change.

    After the edit, run: `npm run typecheck` from the project root.
    The command must exit 0. Fix any type errors before marking done.
  </action>
  <verify>
    <automated>npm run typecheck --prefix D:/Downloads/get-shit-done-main/dashboard-2</automated>
  </verify>
  <done>
    - Lines 57–60 of getLeerlijnenMapping() use `const saved = await saveLeerlijnenMapping(parsed)`.
    - `localStorage.removeItem(LEERLIJNEN_LEGACY_KEY)` is inside an `if (saved)` block.
    - If saved is false, execution falls through to buildDefault() without removing the legacy key.
    - `npm run typecheck` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: capabilities/default.json — add secure-storage:default (CR-03)</name>
  <files>src-tauri/capabilities/default.json</files>
  <read_first>
    Read src-tauri/capabilities/default.json. Current permissions array contains exactly two entries: "core:default" and "store:default". The file is 10 lines and valid JSON.
  </read_first>
  <action>
    Add "secure-storage:default" as a third entry in the permissions array of src-tauri/capabilities/default.json.

    The resulting permissions array must be:
    ["core:default", "store:default", "secure-storage:default"]

    All other fields ($schema, identifier, description, windows) are unchanged.

    After editing, verify the file parses as valid JSON by running:
    `node -e "JSON.parse(require('fs').readFileSync('src-tauri/capabilities/default.json','utf8'))" `
    from the project root. Must exit 0 with no output.
  </action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('D:/Downloads/get-shit-done-main/dashboard-2/src-tauri/capabilities/default.json','utf8')); if(!j.permissions.includes('secure-storage:default')) throw new Error('missing'); console.log('OK');"</automated>
  </verify>
  <done>
    - permissions array in default.json contains "secure-storage:default".
    - File is valid JSON (node parse exits 0).
    - No other fields in the file are changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust key decode path | Externally-stored Base64 key bytes enter Rust before being passed to from_slice() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | get_or_init_key / get_key | mitigate | 32-byte length check added by Task 1 — truncated or padded keychain values cause Err, not panic |
| T-12-05-02 | Denial of Service | decrypt_klassen | mitigate | Minimum 28-byte guard (Task 1) prevents split_at panic on short inputs |
| T-12-05-03 | Tampering | getLeerlijnenMapping legacy migration | mitigate | Write-confirm guard (Task 2) prevents silently losing legacy data when store write fails |
</threat_model>

<verification>
Run all three checks after executing the tasks:

1. Rust compile check — must exit 0:
   cargo check --manifest-path D:/Downloads/get-shit-done-main/dashboard-2/src-tauri/Cargo.toml

2. TypeScript type check — must exit 0:
   npm run typecheck (from D:/Downloads/get-shit-done-main/dashboard-2)

3. capabilities JSON contains secure-storage:default:
   node -e "const j=JSON.parse(require('fs').readFileSync('D:/Downloads/get-shit-done-main/dashboard-2/src-tauri/capabilities/default.json','utf8')); if(!j.permissions.includes('secure-storage:default')) throw new Error('missing'); console.log('OK');"

4. Key-length guard present in crypto.rs (both functions):
   grep -n "decoded.len() != 32" D:/Downloads/get-shit-done-main/dashboard-2/src-tauri/src/crypto.rs
   — must return 2 matches (one in get_or_init_key, one in get_key)

5. Decrypt minimum guard updated:
   grep -n "decoded.len() < 28" D:/Downloads/get-shit-done-main/dashboard-2/src-tauri/src/crypto.rs
   — must return 1 match

6. Write-confirm guard present in leerlijnen.ts:
   grep -n "const saved = await saveLeerlijnenMapping" D:/Downloads/get-shit-done-main/dashboard-2/utils/leerlijnen.ts
   — must return 1 match

   grep -n "if (saved)" D:/Downloads/get-shit-done-main/dashboard-2/utils/leerlijnen.ts
   — must return 1 match
</verification>

<success_criteria>
All three gaps from 12-VERIFICATION.md are closed when:

- CR-01: get_or_init_key() contains a `decoded.len() != 32` guard that returns Err before passing bytes to Key::from_slice(). No panic possible on malformed keychain values.
- CR-04: get_key() contains the same `decoded.len() != 32` guard in the Some(key_b64) match arm.
- CR-04 (decrypt): decrypt_klassen() guard is `decoded.len() < 28` (was 12), preventing split_at panic when auth tag bytes are missing.
- WR-02: getLeerlijnenMapping() captures `saveLeerlijnenMapping` return value; localStorage.removeItem is inside `if (saved)` — legacy data is not silently lost on store write failure.
- CR-03: src-tauri/capabilities/default.json permissions array includes "secure-storage:default".
- cargo check exits 0 with no errors.
- npm run typecheck exits 0 with no errors.
</success_criteria>

<output>
Na voltooiing, maak aan: .planning/phases/12-versleutelde-opslag/12-05-SUMMARY.md
</output>
