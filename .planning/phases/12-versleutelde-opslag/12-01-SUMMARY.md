---
phase: 12-versleutelde-opslag
plan: 01
subsystem: infra
tags: [rust, aes-gcm, tauri, keychain, encryption, tauri-plugin-store, tauri-plugin-secure-storage]

# Dependency graph
requires:
  - phase: 10-tauri-scaffold
    provides: "src-tauri/ project structure, Cargo.toml foundation, lib.rs greet scaffold"
  - phase: 11-typescript-migratie
    provides: "TypeScript modules (klassen.ts, datamodel.ts, leerlijnen.ts) that Wave 1 will migrate"
provides:
  - "AES-256-GCM Tauri commands: encrypt_klassen and decrypt_klassen in src-tauri/src/crypto.rs"
  - "OS keychain key lifecycle via tauri-plugin-secure-storage (key identifier: nl.cios.mentordashboard.key)"
  - "tauri-plugin-store and tauri-plugin-secure-storage plugins registered in lib.rs"
  - "store:default capability in capabilities/default.json"
  - "cargo check + cargo test passing (2 Rust unit tests)"
affects:
  - "12-02 (TypeScript saveKlassen/loadKlassen async rewrite depends on encrypt_klassen/decrypt_klassen)"
  - "12-03 (leerlijnen.ts plugin-store migration)"
  - "12-04 (deleteStudent + storage.test.ts)"
  - "15-packaging (OS keychain cross-platform testing)"

# Tech tracking
tech-stack:
  added:
    - "tauri-plugin-store = 2 (durable key-value JSON persistence)"
    - "tauri-plugin-secure-storage = 1.5.0 (OS keychain — Windows Credential Manager)"
    - "aes-gcm = 0.10.3 (AES-256-GCM authenticated encryption, NCC Group audited)"
    - "base64 = 0.22 (nonce+ciphertext wire format encoding)"
  patterns:
    - "Rust Tauri commands return Result<String, String> for TS invoke() interop"
    - "AES-256-GCM wire format: Base64(12-byte-nonce || ciphertext || 16-byte-auth-tag)"
    - "OsRng nonce via aes_gcm::aead::OsRng re-export — no separate rand dependency"
    - "SecureStorage API: OptionsRequest { prefixed_key, data, .. } struct for get_item/set_item"
    - "GetItemResponse.data is Option<String> — None means key absent (not Err)"

key-files:
  created:
    - "src-tauri/src/crypto.rs — AES-256-GCM encrypt/decrypt Tauri commands + keychain key management"
    - "src-tauri/Cargo.lock — generated by cargo on dependency resolution"
  modified:
    - "src-tauri/Cargo.toml — 4 new dependencies added"
    - "src-tauri/src/lib.rs — mod crypto + 2 plugin registrations + 2 commands in generate_handler!"
    - "src-tauri/capabilities/default.json — store:default added"

key-decisions:
  - "GetItemResponse.data field name corrected from assumed .value — RESEARCH.md A1 was wrong"
  - "No explicit rand dependency — aes_gcm::aead::OsRng re-export avoids version conflicts (A4 confirmed correct)"
  - "get_item returns Ok(data: None) when key absent, not Err — handled via Option match, not match on Err"
  - "secure-storage:default NOT added to capabilities (plugin has no JS bindings — no capability needed, A2 confirmed)"
  - "Non-ASCII em-dash in byte string literal fixed (Rust compiler error) — use plain ASCII in b-strings"

patterns-established:
  - "Pattern: OptionsRequest struct for SecureStorage calls — both prefixed_key and data fields required for set, only prefixed_key for get"
  - "Pattern: get_or_init_key separates key-not-found (data: None) from keychain error (Err)"
  - "Pattern: get_key returns Dutch error message for UI display on keychain failure (D-12-16)"

requirements-completed: [STO-02]

# Metrics
duration: 25min
completed: 2026-05-14
---

# Phase 12 Plan 01: Versleutelde Opslag — Rust Crypto Layer Summary

**AES-256-GCM Tauri commands (encrypt_klassen/decrypt_klassen) with OS keychain key management; cargo check + cargo test passing, ready for TypeScript invoke() in Wave 1**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-05-14T16:30:00Z
- **Completed:** 2026-05-14T16:55:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Implemented `src-tauri/src/crypto.rs` with two `#[tauri::command]` functions: `encrypt_klassen` and `decrypt_klassen` using AES-256-GCM with per-call OsRng nonces
- AES key stored exclusively in OS keychain (Windows Credential Manager) under `nl.cios.mentordashboard.key` via tauri-plugin-secure-storage; key never returned to TypeScript
- Registered both plugins (plugin-store, secure-storage) and both commands in lib.rs; added `store:default` to capabilities
- All 4 Rust dependencies downloaded and compiled; `cargo check` exits 0; `cargo test` passes 2 unit tests

## Task Commits

Each task was committed atomically:

1. **Task 1: Rust-afhankelijkheden toevoegen** - `e5c85bc` (chore)
2. **Task 2: crypto.rs aanmaken + lib.rs + capabilities** - `0b54250` (feat)

## Files Created/Modified

- `src-tauri/src/crypto.rs` — AES-256-GCM encrypt/decrypt commands + keychain key lifecycle helpers
- `src-tauri/src/lib.rs` — Added `mod crypto`, plugin-store + secure-storage plugin init, both crypto commands in generate_handler!
- `src-tauri/Cargo.toml` — 4 new dependencies: tauri-plugin-store, tauri-plugin-secure-storage, aes-gcm, base64
- `src-tauri/Cargo.lock` — Generated lock file (committed)
- `src-tauri/capabilities/default.json` — Added `store:default` permission

## Decisions Made

- GetItemResponse field is `.data: Option<String>` (verified from source), not `.value` as RESEARCH.md assumed
- `get_item` returns `Ok(GetItemResponse { data: None })` when key is absent (not `Err`) — key generation path handled via `Option` match
- `secure-storage:default` NOT added to capabilities (plugin registers JS IPC commands but no frontend capability needed for Rust-side-only operations)
- No explicit `rand` crate dependency — used `aes_gcm::aead::OsRng` re-export as planned in RESEARCH.md A4

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] GetItemResponse field name was .data not .value**
- **Found during:** Task 2 pre-implementation source inspection
- **Issue:** RESEARCH.md Assumption A1 stated field would likely be `.value` or `.password`. Actual source (tauri-plugin-secure-storage 1.5.0 models.rs) shows `pub data: Option<String>`
- **Fix:** Used `response.data` with `Option<String>` handling throughout crypto.rs; added note in file header
- **Files modified:** src-tauri/src/crypto.rs
- **Verification:** `cargo check` exits 0 — field name compiles correctly
- **Committed in:** 0b54250 (Task 2 commit)

**2. [Rule 1 - Bug] SecureStorage API takes OptionsRequest struct, not plain string key**
- **Found during:** Task 2 pre-implementation source inspection
- **Issue:** RESEARCH.md Pattern 2 showed `storage.get_item(KEY_IDENTIFIER)` with a plain string. Actual API is `get_item(app: AppHandle<R>, payload: OptionsRequest)` where `OptionsRequest { prefixed_key: Option<String>, .. }`
- **Fix:** Built `OptionsRequest` struct for each call with appropriate fields
- **Files modified:** src-tauri/src/crypto.rs
- **Verification:** `cargo check` exits 0; both tests pass
- **Committed in:** 0b54250 (Task 2 commit)

**3. [Rule 1 - Bug] Non-ASCII em-dash in Rust byte string literal**
- **Found during:** Task 2 — first cargo check after creating crypto.rs
- **Issue:** `b"test payload — mentordashboard klassendata"` uses U+2014 (em-dash) which is not valid in Rust byte string literals
- **Fix:** Replaced with `b"test payload mentordashboard klassendata"` (plain ASCII)
- **Files modified:** src-tauri/src/crypto.rs
- **Verification:** `cargo check` exits 0 after fix
- **Committed in:** 0b54250 (Task 2 commit)

---

**Total deviations:** 3 auto-fixed (3x Rule 1 — bug/incorrect assumption in research)
**Impact on plan:** All fixes necessary for compilation correctness. No scope creep. The SecureStorage API shape was the most significant finding — executor verified from source before writing code rather than discovering at compile time.

## Issues Encountered

- RESEARCH.md Assumption A1 (GetItemResponse field name) was incorrect — caught proactively by inspecting the crate source before writing crypto.rs
- RESEARCH.md Pattern 2 showed incorrect `get_item` call signature — actual API requires `OptionsRequest` struct
- Both issues were auto-corrected before cargo check; no failed compile cycles needed

## User Setup Required

None — no external service configuration required. OS keychain (Windows Credential Manager) is a built-in Windows 11 service.

## Known Stubs

None — crypto.rs is fully wired. No placeholder data in return types. The AES key is generated on first use and stored in the keychain.

## Threat Flags

No new threat surface beyond what was planned. All STRIDE threats from the plan's threat model are mitigated:
- T-12-01: Key in OS keychain only, never in store.json or TypeScript layer
- T-12-02: OsRng nonce generated fresh on every `encrypt_klassen` call
- T-12-03: AES-GCM 16-byte auth tag — `decrypt` returns Err on any bit flip
- T-12-04: Not applicable at Rust layer — sequential call order enforced in Wave 1 TypeScript

## Next Phase Readiness

- Wave 1 (Plan 12-02) can now call `invoke('encrypt_klassen', { plaintext })` and `invoke('decrypt_klassen', { ciphertext })` from TypeScript
- Plugin-store is registered and ready for LazyStore initialization in klassen.ts
- No blockers for Wave 1 execution

---
*Phase: 12-versleutelde-opslag*
*Completed: 2026-05-14*
