---
phase: 30-documentatie-help-ci
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - INSTRUCTIES.md
  - TESTPLAN.md
autonomous: true
requirements:
  - HELP-03
  - HELP-04
  - TEST-04
  - TEST-05

must_haves:
  truths:
    - "INSTRUCTIES.md exists in repo root with installation steps for Windows and macOS"
    - "INSTRUCTIES.md contains the developer contact email ralvarezstam@cioszuidwest.nl and refers to the bug-report button"
    - "INSTRUCTIES.md documents known limitations (BPV kolom-matching, RNL PDF, no auto-updates)"
    - "TESTPLAN.md exists in repo root with 13 scenario tables in Dutch"
    - "Every scenario table has columns: Stap | Actie | Verwacht resultaat | Geslaagd?"
    - "Each table row has a checkbox (☐) in the Geslaagd? column"
  artifacts:
    - path: "INSTRUCTIES.md"
      provides: "Installation + usage guide for collega-testers"
      contains: "ralvarezstam@cioszuidwest.nl"
    - path: "INSTRUCTIES.md"
      provides: "Known limitations section"
      contains: "BPV"
    - path: "TESTPLAN.md"
      provides: "Manual test scenario checklists"
      contains: "Geslaagd?"
    - path: "TESTPLAN.md"
      provides: "Scenario coverage"
      contains: "Testscenario 13"
  key_links:
    - from: "INSTRUCTIES.md"
      to: "FeedbackModal (src/components/FeedbackModal.tsx)"
      via: "prose reference to bug icon in nav bar + email address"
      pattern: "ralvarezstam@cioszuidwest.nl"
    - from: "TESTPLAN.md"
      to: "INSTRUCTIES.md"
      via: "testers follow INSTRUCTIES.md then execute TESTPLAN.md scenarios"
      pattern: "Testscenario 1: Installatie"
---

<objective>
Create INSTRUCTIES.md and TESTPLAN.md in the repo root — the two documentation files that make
collega-testers self-sufficient without developer assistance.

Purpose: HELP-03 (installation + usage guide), HELP-04 (contact info + bug report reference),
TEST-04 (scenario-based test checklist), TEST-05 (expected-behaviour column per step) fulfilled.
Output: INSTRUCTIES.md (~120 lines) and TESTPLAN.md (~200 lines), both Dutch, both in repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-documentatie-help-ci/30-RESEARCH.md

<interfaces>
<!-- Key facts verified from codebase and prior phase plans -->

Developer contact email (from utils/feedback.ts, verified in 28-01-PLAN.md):
  ralvarezstam@cioszuidwest.nl

FeedbackModal trigger (from src/components/KlasTabStrip.tsx, verified):
  Bug icon button in nav bar, aria-label "Fout melden", opens email client via mailto:

App name: Mentordashboard CIOS
GitHub Releases URL format: https://github.com/[owner]/[repo]/releases (use a placeholder
  "Releases pagina op GitHub" if the exact URL is not known — the executor should NOT fabricate
  a URL but rather write "zie de Releases pagina" with no specific URL)

Known limitations (from RESEARCH.md §Documentation Content):
  - BPV kolom-matchers vereisen specifiek SomToday BPV-formaat
  - Rekenen/Nederlands sectie is optioneel in SomToday PDFs — als de sectie ontbreekt, worden
    die scores niet getoond
  - Geen automatische updates — nieuwe versies moeten handmatig gedownload worden

TESTPLAN.md scenario list (from RESEARCH.md §Documentation Content):
  Scenario 1:  Installatie Windows
  Scenario 2:  Installatie macOS
  Scenario 3:  Onboarding wizard (eerste keer)
  Scenario 4:  PDF importeren (SomToday voortgangsrapport)
  Scenario 5:  Verzuim Excel importeren
  Scenario 6:  BPV stage Excel importeren
  Scenario 7:  Detailweergave bekijken (deelgebieden, prognose, verzuim)
  Scenario 8:  Fase-vergelijking (fase 1 + fase 2 PDFs, trend pijl)
  Scenario 9:  Afdrukken / Print-to-PDF
  Scenario 10: Bug melden via bug-knop
  Scenario 11: Instellingen aanpassen (drempelwaarden, dark mode)
  Scenario 12: Klas hernoemen / verwijderen
  Scenario 13: Help pagina openen en sluiten
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create INSTRUCTIES.md in repo root</name>
  <files>INSTRUCTIES.md</files>
  <read_first>
    - .planning/phases/30-documentatie-help-ci/30-RESEARCH.md section "Documentation Content"
      (INSTRUCTIES.md Outline) — lines 441–468, which specify the exact 8 sections required
  </read_first>
  <action>
Create INSTRUCTIES.md in the repository root (alongside package.json, not inside .planning/).

The file is written in Dutch. Use standard Markdown: ## for H2 sections, ### for H3 subsections,
numbered lists for step-by-step instructions, backticks for terminal commands.

Required sections IN ORDER:

## Wat is dit?
One short paragraph: Mentordashboard CIOS is een desktopapplicatie voor mentoren om de voortgang,
het verzuim en de doorstroomprognose van leerlingen te bekijken. De app draait als los programma
op Windows en macOS en vereist geen internetverbinding.

## Installatie op Windows
Steps (numbered list):
  1. Ga naar de Releases pagina op GitHub en download het nieuwste .exe-bestand.
  2. Dubbelklik op het gedownloade .exe-bestand om de installer te starten.
  3. Als Windows SmartScreen een popup toont ("Windows heeft deze pc beschermd"), klik dan op
     "Meer info" en vervolgens "Toch uitvoeren".
  4. Volg de installer wizard. Installatie vereist geen beheerdersrechten (installeert voor
     de huidige gebruiker).
  5. Na installatie is de app beschikbaar via het Startmenu als "Mentordashboard CIOS".

## Installatie op macOS
Steps:
  1. Ga naar de Releases pagina op GitHub en download het nieuwste .dmg-bestand.
  2. Dubbelklik op het .dmg-bestand en sleep de app naar de map Programma's (Applications).
  3. Dubbelklik op de app om te starten. Als macOS meldt "app is beschadigd" of "kan niet worden
     geopend":
     a. Open Terminal en voer het volgende commando uit:
        xattr -c "/Applications/Mentordashboard CIOS.app"
     b. Of ga naar Systeeminstellingen → Privacy en beveiliging → scroll omlaag → klik "Toch openen".

## Eerste gebruik
Short paragraph: De eerste keer dat de app wordt gestart, verschijnt een korte onboarding wizard.
Volg de stappen om de basisinstellingen in te stellen. Daarna kom je direct in het hoofdscherm
terecht. De wizard verschijnt niet meer bij volgende starts.

## Bestanden importeren

### SomToday voortgang (PDF)
Steps:
  1. Klik op de knop "PDF importeren" (of sleep bestanden naar het scherm).
  2. Selecteer een of meerdere PDF-voortgangsrapporten van SomToday.
  3. De app verwerkt de PDFs en toont de leerlingen in een klas-tab.
  4. Meerdere imports worden samengevoegd — eerder geïmporteerde leerlingen blijven behouden.

### Verzuim (Excel)
Steps:
  1. Exporteer het verzuimoverzicht uit SomToday als .xls-bestand.
  2. Klik op "Verzuim importeren" en selecteer het .xls-bestand.
  3. Verzuimgegevens worden gekoppeld aan de bestaande leerlingen op basis van naam.

### BPV stage (Excel)
Steps:
  1. Exporteer het BPV-overzicht als Excel-bestand vanuit SomToday.
  2. Klik op "BPV importeren" en selecteer het bestand.
  3. Zorg dat het bestand de kolommen bevat die de app verwacht (zie Bekende beperkingen).

## Dashboard bekijken
Short paragraph covering: klas-tabs bovenin voor wisselen tussen klassen, tegels per leerling
met kleurcodering (groen/oranje/rood op basis van doorstroomprognose), klikken op een tegel opent
de detailweergave met deelgebieden, verzuim en prognose. De print-knop in de detailweergave
stuurt de pagina naar de printer of slaat op als PDF.

## Bekende beperkingen
Bullet list:
  - BPV-kolom-herkenning: de app verwacht specifieke kolomnamen uit het SomToday BPV-exportformaat.
    Als kolommen anders heten, worden BPV-gegevens niet ingelezen.
  - Rekenen en Nederlands: deze sectie is optioneel in SomToday PDF-rapporten. Als de sectie
    ontbreekt in het rapport, worden die scores niet getoond in de app.
  - Geen automatische updates: nieuwe versies moeten handmatig worden gedownload via de Releases
    pagina op GitHub.

## Bug melden
Paragraph: Gebruik de **🐛 Fout melden**-knop in de navigatiebalk bovenin de app. Dit opent je
e-mailprogramma met het volgende adres al ingevuld: ralvarezstam@cioszuidwest.nl

Geef in je e-mail zo duidelijk mogelijk aan:
  - Wat je deed op het moment van de fout
  - Wat er mis ging (foutmelding, onverwacht gedrag)
  - Op welk besturingssysteem je werkt (Windows / macOS)

REQUIRED: The exact string "ralvarezstam@cioszuidwest.nl" must appear in the file (HELP-04).
The section heading "Bug melden" must appear as a level-2 heading (HELP-04).
The phrase "Fout melden" must appear (reference to the nav button).
  </action>
  <verify>
    <automated>
      if (Test-Path "INSTRUCTIES.md") {
        $content = Get-Content "INSTRUCTIES.md" -Raw
        $hasEmail = $content -match "ralvarezstam@cioszuidwest\.nl"
        $hasBug = $content -match "Fout melden"
        $hasBpv = $content -match "BPV"
        $hasWindows = $content -match "Installatie op Windows"
        $hasMac = $content -match "Installatie op macOS"
        $hasBeperking = $content -match "Bekende beperkingen"
        Write-Host "email:$hasEmail foutMelden:$hasBug bpv:$hasBpv windows:$hasWindows mac:$hasMac beperkingen:$hasBeperking"
        if ($hasEmail -and $hasBug -and $hasBpv -and $hasWindows -and $hasMac -and $hasBeperking) { exit 0 } else { exit 1 }
      } else { Write-Host "INSTRUCTIES.md missing"; exit 1 }
    </automated>
  </verify>
  <done>
    INSTRUCTIES.md exists in repo root. Verification script exits 0 with all six checks true.
    File has 8 H2 sections in order: Wat is dit?, Installatie op Windows, Installatie op macOS,
    Eerste gebruik, Bestanden importeren, Dashboard bekijken, Bekende beperkingen, Bug melden.
    Email address ralvarezstam@cioszuidwest.nl appears verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TESTPLAN.md in repo root</name>
  <files>TESTPLAN.md</files>
  <read_first>
    - .planning/phases/30-documentatie-help-ci/30-RESEARCH.md section "TESTPLAN.md Outline"
      (lines 473–500) — scenario list and table format example
    - INSTRUCTIES.md (just created) — verify the install steps to keep TESTPLAN steps consistent
  </read_first>
  <action>
Create TESTPLAN.md in the repository root (alongside INSTRUCTIES.md).

The file is written in Dutch. Start with a short intro paragraph, then 13 ## Testscenario sections.

Intro (before first scenario):
  # Testplan Mentordashboard CIOS
  Dit testplan bevat handmatige testscenario's voor collega-testers. Doorloop de stappen in volgorde
  en markeer elke stap als geslaagd (✓) of niet geslaagd (✗) in de kolom "Geslaagd?". Meld
  afwijkingen via de 🐛 Fout melden-knop in de app of via ralvarezstam@cioszuidwest.nl.

Table format for EVERY scenario (this exact column structure, no deviation):

  | Stap | Actie | Verwacht resultaat | Geslaagd? |
  |------|-------|--------------------|-----------|
  | 1    | ...   | ...                | ☐         |

Each row: Stap = integer, Actie = wat de tester doet, Verwacht resultaat = wat de app moet tonen
of doen, Geslaagd? = always ☐ (Unicode U+2610 empty checkbox).

Required scenarios (all 13, in order):

## Testscenario 1: Installatie Windows
Stappen: download .exe van Releases pagina → SmartScreen popup → klik "Toch uitvoeren" →
installer wizard voltooien → app verschijnt in Startmenu → app starten → onboarding wizard
verschijnt. (~5 rows)

## Testscenario 2: Installatie macOS
Stappen: download .dmg → sleep naar Applications → eerste start → "beschadigd" waarschuwing
aanpak (xattr of Privacy instellingen) → app start succesvol → onboarding wizard verschijnt.
(~5 rows)

## Testscenario 3: Onboarding wizard (eerste keer)
Stappen: wizard verschijnt bij eerste start → doorloop alle stappen → wizard sluit af →
hoofdscherm zichtbaar → wizard verschijnt NIET bij herstart. (~4 rows)

## Testscenario 4: PDF importeren
Stappen: klik "PDF importeren" → selecteer 1 SomToday voortgangs-PDF → verwerking →
leerlingen verschijnen als tegels in klas-tab → tegel toont naam + kleurcode. (~4 rows)

## Testscenario 5: Verzuim Excel importeren
Stappen: klik "Verzuim importeren" → selecteer .xls-bestand → verwerking →
open detailweergave van een leerling → verzuimgegevens zijn zichtbaar. (~4 rows)

## Testscenario 6: BPV stage Excel importeren
Stappen: klik "BPV importeren" → selecteer BPV Excel-bestand → verwerking →
open detailweergave → BPV-gegevens zijn zichtbaar. (~4 rows)

## Testscenario 7: Detailweergave bekijken
Stappen: klik op een leerlingtegel → detailweergave opent → deelgebieden zichtbaar →
doorstroomprognose zichtbaar → verzuim zichtbaar (als geïmporteerd) →
klik "← Terug" → klasoverzicht zichtbaar. (~5 rows)

## Testscenario 8: Fase-vergelijking
Stappen: importeer PDF fase 1 → importeer tweede PDF voor zelfde leerling (fase 2) →
open detailweergave → trend-pijl (↑/↓/→) zichtbaar naast deelgebiedsscore. (~4 rows)

## Testscenario 9: Afdrukken / Print-to-PDF
Stappen: open detailweergave van een leerling → klik printknop → printdialoog verschijnt →
kies "Opslaan als PDF" → PDF wordt opgeslagen. (~4 rows)

## Testscenario 10: Bug melden via bug-knop
Stappen: klik 🐛-knop in navigatiebalk → e-mailprogramma opent →
adres ralvarezstam@cioszuidwest.nl is vooringevuld → onderwerpregel is ingevuld → e-mail kan
worden verstuurd of gesloten. (~3 rows)

## Testscenario 11: Instellingen aanpassen
Stappen: klik tandwiel-knop → instellingenpagina opent → wijzig een drempelwaarde →
ga terug → tegel-kleur reflecteert de nieuwe drempel → schakel dark mode in →
interface wordt donker → schakel dark mode uit → interface terug normaal. (~5 rows)

## Testscenario 12: Klas hernoemen en verwijderen
Stappen: dubbelklik op klas-tab naam → naam wordt bewerkbaar → typ nieuwe naam → bevestig →
tab toont nieuwe naam. Dan: klik verwijder-knop op klas → bevestigingsdialoog →
bevestig → klas verdwenen uit tabs. (~5 rows)

## Testscenario 13: Help pagina openen en sluiten
Stappen: klik "?"-knop in navigatiebalk → help pagina opent → inhoud zichtbaar
(Importeren, Bekijken, Afdrukken, Fout melden) → klik "← Terug" →
vorig scherm (klasoverzicht of detailweergave) is hersteld. (~4 rows)

REQUIRED structural constraints (verified by acceptance criteria):
- The header line "| Stap | Actie | Verwacht resultaat | Geslaagd? |" must appear at least once
  (the verify script checks for "Geslaagd?" which appears in every table header).
- "Testscenario 13" must appear as a heading (verifies all 13 scenarios are present).
- Each table row must end with "| ☐ |" or "| ☐         |" — use ☐ (U+2610).

Do NOT use ✓ or ✗ in the template rows — those are for testers to fill in manually.
Do NOT number rows as "1." in markdown list style — use the table row Stap column only.
  </action>
  <verify>
    <automated>
      if (Test-Path "TESTPLAN.md") {
        $content = Get-Content "TESTPLAN.md" -Raw
        $hasHeader = $content -match "Geslaagd\?"
        $hasScenario13 = $content -match "Testscenario 13"
        $hasCheckbox = $content -match [char]0x2610
        $hasActie = $content -match "Actie"
        $hasVerwacht = $content -match "Verwacht resultaat"
        Write-Host "header:$hasHeader scenario13:$hasScenario13 checkbox:$hasCheckbox actie:$hasActie verwacht:$hasVerwacht"
        if ($hasHeader -and $hasScenario13 -and $hasCheckbox -and $hasActie -and $hasVerwacht) { exit 0 } else { exit 1 }
      } else { Write-Host "TESTPLAN.md missing"; exit 1 }
    </automated>
  </verify>
  <done>
    TESTPLAN.md exists in repo root. Verification script exits 0 with all five checks true.
    File contains 13 ## Testscenario sections (1 through 13). Every scenario has a Markdown table
    with columns Stap | Actie | Verwacht resultaat | Geslaagd?. Every table row has ☐ in the
    last column. No row contains ✓ or ✗ (those are for testers to complete manually).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root files → GitHub rendering | INSTRUCTIES.md and TESTPLAN.md are rendered as HTML by GitHub; Markdown injection risk is negligible as content is developer-authored prose |
| INSTRUCTIES.md → tester action | Tester follows steps; no code execution in the doc itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Information Disclosure | ralvarezstam@cioszuidwest.nl in INSTRUCTIES.md | accept | Email is intentionally published for bug reporting; already in FeedbackModal source code |
| T-30-07 | Tampering | INSTRUCTIES.md xattr terminal command | accept | Command is read-only (removes quarantine attribute); does not grant elevated privileges; macOS standard workaround for unsigned apps |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan; pure documentation files |
</threat_model>

<verification>
After both tasks complete:

  PowerShell checks:
    Test-Path "INSTRUCTIES.md"   → True
    Test-Path "TESTPLAN.md"      → True

  Content checks:
    Select-String -Path "INSTRUCTIES.md" -Pattern "ralvarezstam@cioszuidwest\.nl" → match found
    Select-String -Path "INSTRUCTIES.md" -Pattern "Fout melden"                   → match found
    Select-String -Path "INSTRUCTIES.md" -Pattern "BPV"                           → match found
    Select-String -Path "TESTPLAN.md"    -Pattern "Geslaagd\?"                    → match found
    Select-String -Path "TESTPLAN.md"    -Pattern "Testscenario 13"               → match found

  Visual check (optional):
    Open both files in a Markdown previewer (VS Code, GitHub web) to confirm tables render
    correctly and all 13 scenario sections are readable.
</verification>

<success_criteria>
- INSTRUCTIES.md exists in repo root with 8 H2 sections in the specified order
- INSTRUCTIES.md contains "ralvarezstam@cioszuidwest.nl" verbatim (HELP-04)
- INSTRUCTIES.md references the "🐛 Fout melden" nav button (HELP-04)
- INSTRUCTIES.md has a "Bekende beperkingen" section covering BPV, RNL, no auto-updates (HELP-03)
- TESTPLAN.md exists in repo root with exactly 13 ## Testscenario sections (TEST-04)
- Every scenario table has "Stap | Actie | Verwacht resultaat | Geslaagd?" columns (TEST-05)
- Every table data row ends with ☐ in the Geslaagd? column (TEST-05)
- Both verification scripts exit 0
</success_criteria>

<output>
Create .planning/phases/30-documentatie-help-ci/30-04-SUMMARY.md when done
</output>
