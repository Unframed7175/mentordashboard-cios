---
phase: 30-documentatie-help-ci
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/HelpPage.test.tsx
  - tests/KlasTabStrip.test.tsx
autonomous: true
requirements:
  - HELP-01
  - HELP-02

must_haves:
  truths:
    - "4 tests in HelpPage.test.tsx all FAIL (RED) — HelpPage component does not exist yet"
    - "2 new tests in KlasTabStrip.test.tsx FAIL (RED) — onHelp/isHelpActive props not yet on component"
    - "All existing tests outside these files continue to pass"
  artifacts:
    - path: "tests/HelpPage.test.tsx"
      provides: "RED test gate for HelpPage component"
      contains: "describe('HelpPage'"
    - path: "tests/KlasTabStrip.test.tsx"
      provides: "RED gate for ? button"
      contains: "Help openen"
  key_links:
    - from: "tests/HelpPage.test.tsx"
      to: "src/components/HelpPage.tsx"
      via: "import HelpPage from '../src/components/HelpPage'"
      pattern: "import HelpPage"
    - from: "tests/KlasTabStrip.test.tsx"
      to: "src/components/KlasTabStrip.tsx"
      via: "makeProps factory updated with onHelp/isHelpActive"
      pattern: "onHelp"
---

<objective>
Write failing tests (RED) that gate the Wave 1 GREEN implementation.

Purpose: Enforce TDD cycle — no HelpPage.tsx or KlasTabStrip ? button changes exist yet, so tests
must fail before implementation and pass after.
Output: tests/HelpPage.test.tsx (4 RED tests) + tests/KlasTabStrip.test.tsx extended with 2 RED
tests and makeProps updated with onHelp/isHelpActive defaults.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-documentatie-help-ci/30-RESEARCH.md
@.planning/phases/30-documentatie-help-ci/30-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create tests/HelpPage.test.tsx — 4 RED tests</name>
  <files>tests/HelpPage.test.tsx</files>
  <read_first>
    - tests/KlasTabStrip.test.tsx — import block style, describe/it structure, toBeTruthy pattern
    - src/components/SettingsPage.tsx lines 1–10 — to confirm no existing HelpPage import exists
  </read_first>
  <behavior>
    - Test 1: renders a heading with text matching /help/i
    - Test 2: calls onBack exactly once when the "← Terug" button is clicked
    - Test 3: renders a section or element containing text matching /importeren/i
    - Test 4: renders a section or element containing text matching /fout melden/i
  </behavior>
  <action>
Create tests/HelpPage.test.tsx. The file does NOT need to exist beforehand — create fresh.

Import block (mirror KlasTabStrip.test.tsx lines 5–8):
  import React from 'react';
  import { render, screen, fireEvent } from '@testing-library/react';
  import { vi, describe, it, expect } from 'vitest';
  import HelpPage from '../src/components/HelpPage';

The import will cause a TypeScript/module-not-found error at runtime because HelpPage.tsx does not
exist yet. That is the expected RED state.

describe block: 'HelpPage' with four it() tests:

  it('renders help heading') — render(<HelpPage onBack={() => {}} />) then
    expect(screen.getByRole('heading', { name: /help/i })).toBeTruthy()

  it('calls onBack when Terug is clicked') — const onBack = vi.fn(); render; fireEvent.click on
    screen.getByRole('button', { name: /terug/i }); expect(onBack).toHaveBeenCalledTimes(1)

  it('renders importeren section') — render; expect(screen.getByText(/importeren/i)).toBeTruthy()

  it('renders fout melden section') — render;
    expect(screen.getByText(/fout melden/i)).toBeTruthy()

Use .toBeTruthy() assertions (NOT .toBeInTheDocument()) — mirror KlasTabStrip.test.tsx assertion
style (line 26) to avoid @testing-library/jest-dom setup dependency.

Do NOT create src/components/HelpPage.tsx in this task — that is Wave 1.
  </action>
  <verify>
    <automated>npx vitest run tests/HelpPage.test.tsx 2>&1 | Select-String -Pattern "FAIL|Cannot find module|failed"</automated>
  </verify>
  <done>
    npx vitest run tests/HelpPage.test.tsx exits non-zero (module not found) AND the test file
    exists at tests/HelpPage.test.tsx with 4 it() blocks. Existing suite npm test still green
    (HelpPage.test.tsx is excluded from full suite because the file errors at import, not at
    test execution — or vitest reports it as a collection error for that file only).
  </done>
</task>

<task type="tdd">
  <name>Task 2: Extend tests/KlasTabStrip.test.tsx — makeProps + 2 RED tests</name>
  <files>tests/KlasTabStrip.test.tsx</files>
  <read_first>
    - tests/KlasTabStrip.test.tsx — full file; locate makeProps factory (lines 88–105), locate
      Phase 28 describe block (feedback button tests) as the insertion point after which to append
  </read_first>
  <behavior>
    - makeProps factory must provide onHelp: vi.fn() and isHelpActive: false as defaults
    - Test A: KlasTabStrip renders a button with aria-label "Help openen"
    - Test B: clicking that button calls onHelp exactly once
    Before adding onHelp/isHelpActive to the actual component (Wave 1), these tests FAIL because
    KlasTabStrip does not accept those props and the button does not exist.
  </behavior>
  <action>
Read tests/KlasTabStrip.test.tsx. Make two changes:

CHANGE 1 — Update makeProps factory (around line 88–104). Add two fields to the returned object
so all existing tests compile without error after the component props are extended in Wave 1.
The factory must include:
  onHelp: vi.fn(),
  isHelpActive: false,
These sit alongside the existing onSettings, onFeedback, isSettingsActive, isDark lines.

NOTE: The makeProps factory currently does NOT include onHelp/isHelpActive, so existing tests
using makeProps will get TypeScript errors in Wave 1 after the interface is extended. Adding them
now prevents that breakage.

CHANGE 2 — Append a new describe block AFTER the Phase 28 feedback button describe block
(end of file). The describe name is 'KlasTabStrip — help button (Phase 30)':

  describe('KlasTabStrip — help button (Phase 30)', () => {
    it('renders a button with aria-label "Help openen"', () => {
      render(<KlasTabStrip {...makeProps()} />);
      expect(screen.getByRole('button', { name: 'Help openen' })).toBeTruthy();
    });
    it('calls onHelp when ? button is clicked', () => {
      const onHelp = vi.fn();
      render(<KlasTabStrip {...makeProps({ onHelp })} />);
      fireEvent.click(screen.getByRole('button', { name: 'Help openen' }));
      expect(onHelp).toHaveBeenCalledTimes(1);
    });
  });

These 2 tests FAIL before Wave 1 ships because the component does not yet accept onHelp /
does not render an aria-label="Help openen" button. That RED state is correct.

The Phase 17 describe block (lines 10–85) still passes because those tests pass inline props
that do NOT include onHelp — they will remain valid until Wave 1 changes the interface (at
which point TypeScript will require onHelp; but Vitest runtime is lenient about extra props).
The makeProps update ensures Phase 27 and Phase 28 describe blocks keep working.
  </action>
  <verify>
    <automated>npx vitest run tests/KlasTabStrip.test.tsx 2>&1 | Select-String -Pattern "help button|FAIL|passed"</automated>
  </verify>
  <done>
    npx vitest run tests/KlasTabStrip.test.tsx output shows: existing Phase 17/27/28 tests pass,
    AND the 2 new "help button (Phase 30)" tests FAIL with "Unable to find an accessible element
    with the role 'button' and name 'Help openen'" (because the ? button does not yet exist in
    the component).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → component | Test imports component under test — no external network, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all test utilities (vitest, @testing-library/react) already in package.json from prior phases |
</threat_model>

<verification>
Run after both tasks complete:
  npx vitest run tests/HelpPage.test.tsx   → expected: FAIL (module not found)
  npx vitest run tests/KlasTabStrip.test.tsx → expected: 2 FAIL (help button), rest PASS
  npm test → all previously-passing tests still pass (HelpPage.test.tsx excluded or errors at
  collection only, not contaminating the overall result)
</verification>

<success_criteria>
- tests/HelpPage.test.tsx exists with exactly 4 it() blocks covering heading, onBack callback,
  importeren section, fout melden section
- tests/KlasTabStrip.test.tsx makeProps factory includes onHelp: vi.fn() and isHelpActive: false
- KlasTabStrip "help button (Phase 30)" describe block appended with 2 failing tests
- npm test exits with at most the 2 new KlasTabStrip tests failing; no regressions in the 204+
  previously-passing tests
</success_criteria>

<output>
Create .planning/phases/30-documentatie-help-ci/30-01-SUMMARY.md when done
</output>
