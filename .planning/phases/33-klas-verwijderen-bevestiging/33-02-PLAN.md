---
phase: 33-klas-verwijderen-bevestiging
plan: 02
title: GREEN implementatie — KlasVerwijderenModal + KlasTabStrip + App.tsx wiring
type: tdd
wave: 1
depends_on: [33-01]
files_modified:
  - src/components/KlasVerwijderenModal.tsx
  - src/components/KlasTabStrip.tsx
  - src/App.tsx
autonomous: true
requirements:
  - KLS-04
  - KLS-05
  - KLS-06
  - KLS-07

must_haves:
  truths:
    - "KlasVerwijderenModal.tsx bestaat en toont klasnaam + leerlingaantal + checkbox + disabled confirm"
    - "× knop is zichtbaar voor ALLE klassen in KlasTabStrip — geen canDelete guard meer"
    - "App.tsx opent KlasVerwijderenModal in plaats van window.confirm"
    - "Na verwijdering van de laatste klas navigeert de app naar setView('import') (KLS-07)"
    - "npm test exitcode 0 — alle tests groen inclusief de 6 RED tests uit Plan 01"
  artifacts:
    - path: "src/components/KlasVerwijderenModal.tsx"
      provides: "Modal component: klasnaam + count, checkbox, disabled confirm, onConfirm/onCancel callbacks"
    - path: "src/components/KlasTabStrip.tsx"
      provides: "canDelete guard verwijderd — × altijd zichtbaar"
    - path: "src/App.tsx"
      provides: "showDeleteModal state, handleDeleteKlas opent modal, handleConfirmDeleteKlas, KLS-07 navigatie"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/KlasVerwijderenModal.tsx"
      via: "import + conditional render"
      pattern: "showDeleteModal"
    - from: "src/components/KlasTabStrip.tsx"
      to: "src/App.tsx"
      via: "onDeleteKlas prop callback"
      pattern: "onDeleteKlas"
---

<objective>
Implementeer KlasVerwijderenModal en wire alles op in KlasTabStrip + App.tsx. Doelen:

1. **KlasVerwijderenModal.tsx (nieuw)** — ~50 regels, volgt KlasModal structuur (inline styles,
   modal-overlay + modal-box). Props: klasNaam, leerlingCount, onConfirm, onCancel.
   Checkbox "Ik begrijp dat alle leerlingdata wordt verwijderd" disabled de confirm-knop totdat
   aangevinkt (KLS-06). Body toont: `Klas '{klasNaam}' bevat {leerlingCount} leerlingen.` (KLS-05).

2. **KlasTabStrip.tsx (aanpassing)** — verwijder `{klas.canDelete && ...}` guard. × knop altijd
   renderen voor elk klas-tabblad (D-01). canDelete mag optioneel blijven in de interface —
   het prop wordt genegeerd.

3. **App.tsx (aanpassing)** — drie veranderingen:
   a. `showDeleteModal` state: `{ klasId: string; naam: string; count: number } | null`
   b. `handleDeleteKlas` opent de modal in plaats van window.confirm te tonen
   c. `handleConfirmDeleteKlas` verwijdert de klas en navigeert naar import als geen klassen resten (KLS-07)
   d. canDelete in klassen.map → true voor alle klassen
   e. `<KlasVerwijderenModal>` mounten wanneer showDeleteModal !== null

Output: npm test exitcode 0, alle 6 RED tests uit Plan 01 nu groen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-klas-verwijderen-bevestiging/33-CONTEXT.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (GREEN): Maak src/components/KlasVerwijderenModal.tsx aan</name>
  <files>src/components/KlasVerwijderenModal.tsx</files>

  <read_first>
    - src/components/KlasModal.tsx (volledig — exact dezelfde modal overlay/box structuur, inline styles, btn klassen)
    - tests/KlasVerwijderenModal.test.tsx (de 5 tests: wat de component moet renderen en hoe callbacks aangeroepen worden)
    - .planning/phases/33-klas-verwijderen-bevestiging/33-CONTEXT.md §D-06 t/m D-10 (modal stijl en inhoud)
  </read_first>

  <behavior>
    Interface Props:
      interface KlasVerwijderenModalProps {
        klasNaam: string
        leerlingCount: number
        onConfirm: () => void
        onCancel: () => void
      }

    State: `const [checked, setChecked] = useState(false)`

    Overlay click handler (sluit modal bij klik op backdrop — identiek aan KlasModal.handleOverlayClick):
      function handleOverlayClick(e: React.MouseEvent<HTMLDivElement>) {
        if (e.target === e.currentTarget) onCancel()
      }

    Overlay keydown handler (Escape sluit modal):
      function handleOverlayKeyDown(e: React.KeyboardEvent<HTMLDivElement>) {
        if (e.key === 'Escape') onCancel()
      }

    JSX structuur (identiek aan KlasModal — zelfde inline styles voor overlay en box):
      <div style={{ position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.5)', display: 'flex',
                    alignItems: 'center', justifyContent: 'center', zIndex: 1000 }}
           onKeyDown={handleOverlayKeyDown} onClick={handleOverlayClick}>
        <div style={{ background: 'var(--bg-surface, #ffffff)', borderRadius: '12px',
                      padding: '1.5rem', width: '100%', maxWidth: '400px',
                      boxShadow: '0 8px 32px rgba(0,0,0,0.18)' }}>

          <h2 style={{ margin: '0 0 1rem 0', fontSize: '1.1rem', fontWeight: 700 }}>
            Klas verwijderen
          </h2>

          <p style={{ margin: '0 0 1rem 0', fontSize: '0.9rem' }}>
            Klas '{klasNaam}' bevat {leerlingCount} leerlingen.
          </p>

          <label style={{ display: 'flex', alignItems: 'flex-start', gap: '0.5rem',
                          marginBottom: '1.25rem', fontSize: '0.875rem', cursor: 'pointer' }}>
            <input type="checkbox" checked={checked} onChange={e => setChecked(e.target.checked)}
                   style={{ marginTop: '2px', flexShrink: 0 }} />
            Ik begrijp dat alle leerlingdata wordt verwijderd
          </label>

          <div style={{ display: 'flex', justifyContent: 'flex-end', gap: '0.5rem' }}>
            <button type="button" className="btn btn-ghost" onClick={onCancel}>
              Annuleren
            </button>
            <button type="button" className="btn btn-primary" onClick={onConfirm} disabled={!checked}>
              Verwijderen
            </button>
          </div>

        </div>
      </div>

    Geen hardcoded kleuren in de component-specifieke logica. style={{ opacity }} op de
    disabled knop wordt NIET handmatig toegevoegd — de btn-primary CSS regelt dit al.
  </behavior>

  <action>
    Maak src/components/KlasVerwijderenModal.tsx aan met bovenstaande implementatie.
    Importeer React en useState bovenaan.
    Exporteer de component als default export.
    Voer npm test uit na aanmaken en controleer dat de 5 KlasVerwijderenModal tests nu groen zijn.
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npm test -- --reporter=verbose 2>&1 | grep -E "KlasVerwijderenModal|passed|failed"</automated>
  </verify>

  <acceptance_criteria>
    - src/components/KlasVerwijderenModal.tsx bestaat
    - grep -n "KlasVerwijderenModalProps" src/components/KlasVerwijderenModal.tsx toont de interface
    - grep -n "leerlingCount" src/components/KlasVerwijderenModal.tsx geeft minstens 2 matches (interface + render)
    - grep -n "disabled={!checked}" src/components/KlasVerwijderenModal.tsx geeft exact 1 match
    - grep -n "Ik begrijp" src/components/KlasVerwijderenModal.tsx geeft exact 1 match
    - De 5 KlasVerwijderenModal tests in npm test zijn nu groen
  </acceptance_criteria>

  <done>KlasVerwijderenModal.tsx aangemaakt. 5 tests groen. Overige tests onveranderd.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Verwijder canDelete guard in src/components/KlasTabStrip.tsx</name>
  <files>src/components/KlasTabStrip.tsx</files>

  <read_first>
    - src/components/KlasTabStrip.tsx (volledig — focus op regels 110-119: de canDelete guard en delete button render)
    - tests/KlasTabStrip.test.tsx (TAB-01 describe-blok — verwacht nu 2 × knoppen voor k1 en k2)
    - .planning/phases/33-klas-verwijderen-bevestiging/33-CONTEXT.md §D-01 en §D-02
  </read_first>

  <behavior>
    Huidige code in KlasTabStrip.tsx (regels 110-119):
      {klas.canDelete && (
        <button
          className="delete-tab-btn"
          title="Klas verwijderen"
          aria-label={`Klas ${klas.naam} verwijderen`}
          onClick={e => { e.stopPropagation(); onDeleteKlas(klas.id); }}
        >
          ×
        </button>
      )}

    Verwijder de `{klas.canDelete && ...}` wrapper zodat de knop altijd gerenderd wordt:
      <button
        className="delete-tab-btn"
        title="Klas verwijderen"
        aria-label={`Klas ${klas.naam} verwijderen`}
        onClick={e => { e.stopPropagation(); onDeleteKlas(klas.id); }}
      >
        ×
      </button>

    De `canDelete` prop in de interface mag blijven staan (als optional?: boolean) om bestaande
    call sites niet te breken. Het prop wordt simpelweg niet meer gebruikt in de render logica.
  </behavior>

  <action>
    1. Lees src/components/KlasTabStrip.tsx volledig.
    2. Verwijder de `{klas.canDelete && (...)}` wrapper — render de delete-button altijd.
    3. Maak canDelete optioneel in de interface: verander `canDelete: boolean` naar `canDelete?: boolean`
       (of laat het als boolean staan — beide werken).
    4. Voer npm test uit. De TAB-01 test moet nu GROEN zijn (2 × knoppen gevonden).
       Alle andere KlasTabStrip tests en overige tests moeten ook groen zijn.
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npm test -- --reporter=verbose 2>&1 | grep -E "TAB-01|KlasTabStrip|passed|failed"</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "canDelete &&" src/components/KlasTabStrip.tsx geeft nul matches (guard verwijderd)
    - grep -n "delete-tab-btn" src/components/KlasTabStrip.tsx geeft exact 1 match (button altijd aanwezig)
    - npm test: TAB-01 test groen, alle KlasTabStrip tests groen
    - Geen andere wijzigingen in KlasTabStrip.tsx (TAB-02, TAB-03 rename flow intact)
  </acceptance_criteria>

  <done>canDelete guard verwijderd. TAB-01 test groen. Alle overige tests groen.</done>
</task>

<task type="tdd">
  <name>Task 3 (GREEN): Update src/App.tsx — modal state, handleDeleteKlas, canDelete=true, KLS-07 navigatie</name>
  <files>src/App.tsx</files>

  <read_first>
    - src/App.tsx volledig (focus: regel 23 useState imports, regel 128-135 handleDeleteKlas, regel 153-157 klassen.map met canDelete, regel 170-175 modal rendering)
    - src/components/KlasVerwijderenModal.tsx (de props interface: klasNaam, leerlingCount, onConfirm, onCancel)
    - utils/klassen.ts regels 93-115 (deleteKlas — behandelt automatisch wisselen bij actieve klas; retourneert boolean)
    - .planning/phases/33-klas-verwijderen-bevestiging/33-CONTEXT.md §D-03 t/m D-05, §"Integration Points"
  </read_first>

  <behavior>
    Vier veranderingen in App.tsx:

    **Verandering 1 — import toevoegen** (bovenaan bij de andere component imports):
      import KlasVerwijderenModal from './components/KlasVerwijderenModal'

    **Verandering 2 — showDeleteModal state toevoegen** (na de bestaande useState declaraties, rond regel 26):
      const [showDeleteModal, setShowDeleteModal] = useState<{ klasId: string; naam: string; count: number } | null>(null)

    **Verandering 3 — handleDeleteKlas vervangen** (huidige: window.confirm + direct deleteKlas):
      Vervang de hele handleDeleteKlas functie (regels 128-135) door:

        function handleDeleteKlas(klasId: string): void {
          const klas = klassenState.klassen[klasId]
          const naam = klas?.naam ?? klasId
          const count = Array.isArray(klas?.students) ? klas.students.length : 0
          setShowDeleteModal({ klasId, naam, count })
        }

      Voeg daarna een nieuwe handleConfirmDeleteKlas functie toe (direct ná handleDeleteKlas):

        async function handleConfirmDeleteKlas(): Promise<void> {
          if (!showDeleteModal) return
          const { klasId } = showDeleteModal
          await deleteKlas(klasId)
          setShowDeleteModal(null)
          setRefreshKey(k => k + 1)
          if (Object.keys(klassenState.klassen).length === 0) {
            setView('import')
          }
        }

    **Verandering 4 — canDelete in klassen.map** (regel 156):
      Verander:
        canDelete: Array.isArray(klas.students) && klas.students.length === 0,
      naar:
        canDelete: true,

    **Verandering 5 — KlasVerwijderenModal renderen** (na het bestaande {feedbackOpen && ...} blok, rond regel 176):
      Voeg toe:
        {showDeleteModal && (
          <KlasVerwijderenModal
            klasNaam={showDeleteModal.naam}
            leerlingCount={showDeleteModal.count}
            onConfirm={handleConfirmDeleteKlas}
            onCancel={() => setShowDeleteModal(null)}
          />
        )}

    Geen andere wijzigingen. De bestaande handleRenameKlas, handleKlasSwitch en andere handlers
    blijven volledig intact.
  </behavior>

  <action>
    1. Lees src/App.tsx volledig.
    2. Voeg KlasVerwijderenModal import toe bovenaan (bij de andere component imports).
    3. Voeg showDeleteModal useState toe na de bestaande state-declaraties.
    4. Vervang handleDeleteKlas door de nieuwe versie die modal opent.
    5. Voeg handleConfirmDeleteKlas toe direct ná handleDeleteKlas.
    6. Verander canDelete: ... → canDelete: true in klassen.map.
    7. Voeg {showDeleteModal && <KlasVerwijderenModal .../>} toe in de JSX na feedbackOpen block.
    8. Voer npm test uit — alle tests moeten groen zijn inclusief de 6 RED tests uit Plan 01.
    9. Voer npm run typecheck uit (of npx tsc --noEmit) — nul TypeScript fouten.
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npm test 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "showDeleteModal" src/App.tsx geeft minstens 5 matches (state decl, setter in handleDelete, setter in confirm, check in confirm, JSX render)
    - grep -n "window.confirm" src/App.tsx geeft nul matches (vervangen)
    - grep -n "KlasVerwijderenModal" src/App.tsx geeft minstens 2 matches (import + JSX)
    - grep -n "canDelete: true" src/App.tsx geeft exact 1 match
    - grep -n "setView('import')" src/App.tsx toont de KLS-07 navigatie na deleteKlas
    - npm test exitcode 0 — ALLE tests groen inclusief de 6 RED tests uit Plan 01
    - npm run typecheck exitcode 0 (nul TypeScript fouten)
  </acceptance_criteria>

  <done>
    App.tsx bijgewerkt: KlasVerwijderenModal geïmporteerd en gemount, showDeleteModal state,
    handleDeleteKlas opent modal, handleConfirmDeleteKlas verwijdert + navigeert (KLS-07),
    canDelete: true voor alle klassen. npm test groen.
  </done>
</task>

<task type="auto">
  <name>Task 4 (VERIFY): Volledige testsuite + typecheck + acceptance check</name>
  <files></files>

  <read_first>
    - src/components/KlasVerwijderenModal.tsx (eindtoestand)
    - src/App.tsx regels 1-30 en de handleDeleteKlas/handleConfirmDeleteKlas sectie (eindtoestand)
  </read_first>

  <action>
    Voer de volgende commando's uit en bevestig dat elk slaagt:

    1. npm test — volledige testsuite groen (alle vorige tests + 6 nieuwe/aangepaste tests)
    2. npm run typecheck 2>&1 | tail -10 — nul TypeScript fouten
       (alternatief: npx tsc --noEmit als typecheck script ontbreekt in package.json)
    3. grep -c "window.confirm" src/App.tsx — geeft 0 (vervangen door modal)
    4. grep -c "canDelete: true" src/App.tsx — geeft 1
    5. grep -c "canDelete &&" src/components/KlasTabStrip.tsx — geeft 0 (guard verwijderd)

    Als een commando faalt: diagnosticeer en repareer vóór het aanmaken van de SUMMARY.
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npm test 2>&1 | grep -E "passed|failed"</automated>
  </verify>

  <acceptance_criteria>
    - npm test exitcode 0, alle tests groen (minstens 210+6 = 216 tests)
    - TypeScript typecheck exitcode 0
    - window.confirm afwezig in App.tsx
    - canDelete: true aanwezig in App.tsx (1 match)
    - canDelete guard verwijderd uit KlasTabStrip.tsx (0 matches voor "canDelete &&")
    - KlasVerwijderenModal geëxporteerd als default uit src/components/KlasVerwijderenModal.tsx
    - KLS-04: × knop opent modal (handleDeleteKlas → setShowDeleteModal)
    - KLS-05: modal body toont klasnaam en leerlingaantal
    - KLS-06: confirm knop disabled tot checkbox aangevinkt
    - KLS-07: na deleteKlas + lege klassen → setView('import') wordt aangeroepen
  </acceptance_criteria>

  <done>
    Volledige testsuite groen. TypeScript clean. KLS-04 t/m KLS-07 aantoonbaar geleverd.
    SUMMARY aangemaakt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gebruiker → modal confirm | Mentor klikt bevestig na checkbox — dit triggert deleteKlas() op de aangegeven klasId |
| klassenState.klassen[klasId] | klasId afkomstig van KlasTabStrip onDeleteKlas callback — altijd een bestaande klas (guard in deleteKlas zelf) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | handleDeleteKlas klasId | accept | klasId afkomstig van klassen.map via KlasTabStrip — altijd een geldige key uit klassenState. deleteKlas() returnt false voor onbekende ids. |
| T-33-02 | Denial of Service | deleteKlas + setView nav | accept | Modal vereist expliciete checkbox + klikactie — geen accidentele verwijdering mogelijk. Navigatie naar import is graceful fallback. |
| T-33-SC | Tampering | npm installs | accept | Geen nieuwe npm packages in dit plan. |
</threat_model>

<verification>
Na uitvoering:
- npm test exitcode 0 — alle 210+ tests groen inclusief 6 nieuwe (5 KlasVerwijderenModal + 1 TAB-01)
- TypeScript typecheck exitcode 0
- KLS-04: × knop zichtbaar voor ALLE klassen, opent modal (geen window.confirm)
- KLS-05: modal toont `Klas '{naam}' bevat {count} leerlingen.`
- KLS-06: confirm knop disabled totdat checkbox aangevinkt
- KLS-07: na verwijdering laatste klas → setView('import')
- Geen hardcoded kleuren toegevoegd in KlasVerwijderenModal.tsx
</verification>

<success_criteria>
1. KLS-04: Niet-lege klas verwijderen via modal met checkbox-bevestiging werkt correct
2. KLS-05: Modal toont klasnaam en exact leerlingaantal
3. KLS-06: Confirm-knop uitgeschakeld tot checkbox aangevinkt
4. KLS-07: Na verwijdering laatste klas navigeert app naar importscherm
5. Alle bestaande tests (210+) blijven groen — nul regressies
6. TypeScript typecheck clean
</success_criteria>

<output>
Maak .planning/phases/33-klas-verwijderen-bevestiging/33-02-SUMMARY.md aan wanneer klaar.
</output>
