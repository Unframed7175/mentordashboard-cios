---
phase: 33-klas-verwijderen-bevestiging
plan: 03
type: execute
wave: 1
depends_on: [33-02]
files_modified:
  - utils/klassen.ts
  - src/App.tsx
  - tests/klassen.uniekeLeerlingen.test.ts
autonomous: true
requirements: [KLS-05]
gap_closure: true

must_haves:
  truths:
    - "De verwijdermodal toont het werkelijke unieke leerlingaantal — niet verdubbeld bij leerlingen met 2 fases/periodes"
  artifacts:
    - path: "utils/klassen.ts"
      provides: "countUniekeLeerlingen() helper — telt unieke leerlingId's"
      contains: "export function countUniekeLeerlingen"
    - path: "src/App.tsx"
      provides: "handleDeleteKlas gebruikt countUniekeLeerlingen voor count"
      contains: "countUniekeLeerlingen"
    - path: "tests/klassen.uniekeLeerlingen.test.ts"
      provides: "Test die dubbeltelling bij multi-periode leerlingen afvangt"
      contains: "countUniekeLeerlingen"
  key_links:
    - from: "src/App.tsx"
      to: "utils/klassen.ts"
      via: "import countUniekeLeerlingen"
      pattern: "countUniekeLeerlingen"
---

<objective>
Gap closure voor Phase 33, test 3 (UAT issue, severity: major).

De verwijdermodal verdubbelde het leerlingaantal wanneer een leerling 2 fases/periodes had.
Oorzaak: `App.tsx:133` berekende `count` als `klas.students.length`, maar de `students` array bevat
één record per periode per leerling. Een leerling met 2 periodes telde dus als 2.

Fix: tel unieke `leerlingId`'s in plaats van de ruwe array-lengte.

Purpose: De modal toont het werkelijke aantal leerlingen dat verwijderd wordt (KLS-05).
Output: Pure helper `countUniekeLeerlingen()` in utils, gebruikt door `handleDeleteKlas`, met test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-klas-verwijderen-bevestiging/33-UAT.md

<interfaces>
<!-- Bestaande feiten uit de codebase — executor hoeft niet te exploreren. -->

Uit src/App.tsx (regel 130-135, de te fixen functie):
```typescript
function handleDeleteKlas(klasId: string): void {
  const klas = klassenState.klassen[klasId];
  const naam = klas?.naam ?? klasId;
  const count = Array.isArray(klas?.students) ? klas.students.length : 0;  // BUG: telt periode-records, niet leerlingen
  setShowDeleteModal({ klasId, naam, count });
}
```

App.tsx importeert al uit ../utils/klassen (regel 12):
```typescript
import { klassenState, switchActiveKlas, getActiveStudents, saveOnboardingCompleted, deleteKlas, renameKlas } from '../utils/klassen';
```

Student-records dragen het veld `leerlingId` (zie AanvullendSection.tsx, DeelgebiedenMatrix.tsx:
`s.leerlingId`). Eén record per periode per leerling → dubbele leerlingId bij 2 fases.

utils/klassen.ts is de bestaande data-laag met named ES-module exports (createKlas,
deleteKlas, getActiveStudents, getAllRecordsForStudent, etc.). Helper hoort hier thuis.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add countUniekeLeerlingen helper + write test</name>
  <files>utils/klassen.ts, tests/klassen.uniekeLeerlingen.test.ts</files>
  <behavior>
    countUniekeLeerlingen(students):
    - Test 1: students met 2 records voor dezelfde leerlingId (2 periodes) → telt als 1.
    - Test 2: 3 leerlingen elk met 2 periodes (6 records) → resultaat 3.
    - Test 3: niet-array input (undefined / null) → resultaat 0.
    - Test 4: lege array → resultaat 0.
    - Test 5: 3 records met unieke leerlingId's → resultaat 3.
  </behavior>
  <action>
    Voeg in utils/klassen.ts een geëxporteerde pure functie `countUniekeLeerlingen` toe (geen
    LazyStore, geen async, geen side-effects). Signatuur: `export function countUniekeLeerlingen(students: any): number`.
    Gedrag: als `students` geen array is, return 0; anders return het aantal unieke `leerlingId`-waarden
    via `new Set(students.map((s: any) => s.leerlingId)).size`. Plaats de functie naast de andere
    pure read-helpers (bijv. vlak bij getActiveStudents / getAllRecordsForStudent), niet binnen een
    bestaande functie.

    Schrijf eerst tests/klassen.uniekeLeerlingen.test.ts dat `countUniekeLeerlingen` importeert uit
    '../utils/klassen' en de 5 cases uit <behavior> dekt. Gebruik Vitest (import { describe, it, expect }
    from 'vitest') conform de bestaande test-stijl. Geen mocks nodig — de helper is puur. Bouw test-records
    als plain objects met enkel een `leerlingId` veld (bijv. { leerlingId: 'a' }).
  </action>
  <verify>
    <automated>npx vitest run tests/klassen.uniekeLeerlingen.test.ts</automated>
  </verify>
  <done>countUniekeLeerlingen geëxporteerd uit utils/klassen.ts; 5 testcases groen; multi-periode leerling telt als 1.</done>
</task>

<task type="auto">
  <name>Task 2: Wire helper into handleDeleteKlas in App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Voeg `countUniekeLeerlingen` toe aan de bestaande import uit '../utils/klassen' (regel 12).
    Vervang in `handleDeleteKlas` (regel 133) de regel
    `const count = Array.isArray(klas?.students) ? klas.students.length : 0;`
    door `const count = countUniekeLeerlingen(klas?.students);`.
    Geen andere wijzigingen — naam-afleiding en setShowDeleteModal blijven identiek.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit -p tsconfig.migrated.json</automated>
  </verify>
  <done>App.tsx importeert en gebruikt countUniekeLeerlingen; volledige test-suite groen (incl. bestaande KlasVerwijderenModal.test.tsx); geen typecheck-fouten.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — volledige suite groen, geen regressies in de bestaande 26 testbestanden.
- Bestaande tests/KlasVerwijderenModal.test.tsx blijft ongewijzigd en groen (modal krijgt count nog steeds als prop).
- Een leerling met 2 periodes telt nu als 1 in de modal-count.
</verification>

<success_criteria>
- KLS-05 / UAT test 3 gap gesloten: modal toont werkelijk uniek leerlingaantal, niet verdubbeld.
- countUniekeLeerlingen is een pure, geteste helper.
- handleDeleteKlas gebruikt de helper; geen ruwe `.length` meer.
</success_criteria>

<output>
Create `.planning/phases/33-klas-verwijderen-bevestiging/33-03-SUMMARY.md` when done
</output>
