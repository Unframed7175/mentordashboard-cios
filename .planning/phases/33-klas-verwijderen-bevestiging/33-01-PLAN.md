---
phase: 33-klas-verwijderen-bevestiging
plan: 01
title: RED test scaffolds — KlasVerwijderenModal + TAB-01 update
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/KlasVerwijderenModal.test.tsx
  - tests/KlasTabStrip.test.tsx
autonomous: true
requirements:
  - KLS-04
  - KLS-05
  - KLS-06
  - KLS-07

must_haves:
  truths:
    - "tests/KlasVerwijderenModal.test.tsx bestaat met 5 RED tests"
    - "TAB-01 tests in KlasTabStrip.test.tsx verwachten × voor ALLE klassen (niet alleen canDelete=true)"
    - "Alle eerder groene tests blijven groen — alleen de nieuwe/aangepaste tests zijn RED"
  artifacts:
    - path: "tests/KlasVerwijderenModal.test.tsx"
      provides: "5 RED tests: klasnaam+count weergave, checkbox disabled/enabled, onCancel, onConfirm"
    - path: "tests/KlasTabStrip.test.tsx"
      provides: "TAB-01 describe-blok herschreven — verwacht × voor alle klassen"
  key_links:
    - from: "tests/KlasVerwijderenModal.test.tsx"
      to: "src/components/KlasVerwijderenModal.tsx"
      via: "import (nog niet aanwezig — RED)"
      pattern: "KlasVerwijderenModal"
---

<objective>
Schrijf falende (RED) tests als gate voor Plan 02. Twee doelen:

1. **KlasVerwijderenModal.test.tsx (nieuw)** — 5 tests die de modal-interface beschrijven:
   klasnaam + leerlingaantal in body, confirm disabled tot checkbox aangevinkt, enabled ná aanvinken,
   Annuleren roept onCancel aan, bevestigen roept onConfirm aan.
   RED omdat KlasVerwijderenModal.tsx nog niet bestaat.

2. **KlasTabStrip.test.tsx (update)** — TAB-01 describe-blok herschrijven zodat het verwacht dat de
   × knop voor ALLE klassen verschijnt (D-01: canDelete verwijderd als concept). De huidige tests
   verwachten dat canDelete=false de knop verbergt — die verwachting klopt niet meer na Plan 02.
   RED omdat de component de canDelete guard nog niet verwijderd heeft.

Output:
- tests/KlasVerwijderenModal.test.tsx (nieuw)
- tests/KlasTabStrip.test.tsx (aangepast — TAB-01 describe-blok)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-klas-verwijderen-bevestiging/33-CONTEXT.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Maak tests/KlasVerwijderenModal.test.tsx aan — 5 falende tests</name>
  <files>tests/KlasVerwijderenModal.test.tsx</files>

  <read_first>
    - src/components/KlasModal.tsx (modal-patroon: overlay-click, cancel, confirm flow)
    - tests/FeedbackModal.test.tsx (component test-patroon: makeProps, fireEvent.click, disabled check)
  </read_first>

  <behavior>
    Maak tests/KlasVerwijderenModal.test.tsx aan. Importeer:
      import React from 'react'
      import { render, screen, fireEvent } from '@testing-library/react'
      import { vi, describe, it, expect } from 'vitest'
      import KlasVerwijderenModal from '../src/components/KlasVerwijderenModal'

    Hulpfunctie boven de describe-blokken:
      function makeProps(overrides: Record<string, unknown> = {}) {
        return { klasNaam: 'CSD2A', leerlingCount: 19, onConfirm: vi.fn(), onCancel: vi.fn(), ...overrides }
      }

    Vijf tests in describe('KlasVerwijderenModal'):

    Test 1 — KLS-05: toont klasnaam en leerlingaantal in modal body:
      render(<KlasVerwijderenModal {...makeProps()} />)
      expect(screen.getByText(/CSD2A/)).toBeTruthy()
      expect(screen.getByText(/19/)).toBeTruthy()

    Test 2 — KLS-06: confirm-knop is disabled bij initiële render (checkbox niet aangevinkt):
      render(<KlasVerwijderenModal {...makeProps()} />)
      const btn = screen.getByRole('button', { name: /Verwijderen/i })
      expect((btn as HTMLButtonElement).disabled).toBe(true)

    Test 3 — KLS-06: confirm-knop wordt enabled na aanvinken checkbox:
      render(<KlasVerwijderenModal {...makeProps()} />)
      fireEvent.click(screen.getByRole('checkbox'))
      const btn = screen.getByRole('button', { name: /Verwijderen/i })
      expect((btn as HTMLButtonElement).disabled).toBe(false)

    Test 4 — KLS-04: klikken op Annuleren roept onCancel aan:
      const onCancel = vi.fn()
      render(<KlasVerwijderenModal {...makeProps({ onCancel })} />)
      fireEvent.click(screen.getByRole('button', { name: /Annuleren/i }))
      expect(onCancel).toHaveBeenCalledTimes(1)

    Test 5 — KLS-04: aanvinken checkbox dan klikken Verwijderen roept onConfirm aan:
      const onConfirm = vi.fn()
      render(<KlasVerwijderenModal {...makeProps({ onConfirm })} />)
      fireEvent.click(screen.getByRole('checkbox'))
      fireEvent.click(screen.getByRole('button', { name: /Verwijderen/i }))
      expect(onConfirm).toHaveBeenCalledTimes(1)
  </behavior>

  <action>
    Maak tests/KlasVerwijderenModal.test.tsx aan met exact de bovenstaande 5 tests.
    Voer na aanmaken npm test uit en bevestig dat de 5 nieuwe tests ROOD zijn
    (import error of "Cannot find module" — beide zijn acceptabele RED-uitkomsten).
    Alle eerder groene tests moeten nog groen zijn.
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npm test -- --reporter=verbose 2>&1 | grep -E "KlasVerwijderenModal|FAIL|passed|failed"</automated>
  </verify>

  <acceptance_criteria>
    - tests/KlasVerwijderenModal.test.tsx bestaat
    - Bevat describe('KlasVerwijderenModal', ...) met exact 5 it()-blokken
    - grep -c "onConfirm" tests/KlasVerwijderenModal.test.tsx geeft minstens 2
    - grep -c "onCancel" tests/KlasVerwijderenModal.test.tsx geeft minstens 2
    - grep -c "disabled" tests/KlasVerwijderenModal.test.tsx geeft minstens 2 (KLS-06 tests)
    - npm test toont de 5 nieuwe tests als FAILING — alle overige tests groen
  </acceptance_criteria>

  <done>5 RED tests in tests/KlasVerwijderenModal.test.tsx. Alle overige tests groen.</done>
</task>

<task type="tdd">
  <name>Task 2 (RED): Update TAB-01 in tests/KlasTabStrip.test.tsx — × voor ALLE klassen</name>
  <files>tests/KlasTabStrip.test.tsx</files>

  <read_first>
    - tests/KlasTabStrip.test.tsx (volledig — begrijp de huidige TAB-01 describe-blok op regels 117-131 en de makeProps helper op regels 96-115)
    - .planning/phases/33-klas-verwijderen-bevestiging/33-CONTEXT.md §D-01 (× knop voor ALLE klassen — canDelete verwijderd als concept)
  </read_first>

  <behavior>
    Vervang het TAB-01 describe-blok (regels 117-131) door onderstaand blok.
    Het makeProps helper (regels 96-115) blijft ongewijzigd — canDelete mag nog in de props zitten
    als optioneel veld; de test controleert nu gedrag, niet de prop-waarde.

    Oud TAB-01 describe-blok (vervangen):
      describe('KlasTabStrip — TAB-01: delete button visibility (Phase 27)', () => {
        it('TAB-01: renders × button only for klas with canDelete === true', ...
        it('TAB-01: no × button for klas with canDelete === false', ...
      });

    Nieuw TAB-01 describe-blok (ter vervanging):
      describe('KlasTabStrip — TAB-01: delete button visibility (Phase 33)', () => {
        it('TAB-01: renders × button voor ALLE klassen ongeacht canDelete prop', () => {
          render(<KlasTabStrip {...makeProps()} />);
          // makeProps heeft k1 (canDelete: true) en k2 (canDelete: false)
          // Na Phase 33 toont de component × voor BEIDE klassen
          const deleteButtons = screen.getAllByRole('button', { name: /verwijderen/i });
          expect(deleteButtons).toHaveLength(2);
          expect(deleteButtons[0].getAttribute('aria-label')).toMatch(/Klas A verwijderen/i);
          expect(deleteButtons[1].getAttribute('aria-label')).toMatch(/Klas B verwijderen/i);
        });
      });

    Dit maakt de TAB-01 test RED omdat de component de canDelete guard nog heeft (toont slechts 1 ×).
    Plan 02 verwijdert de guard zodat de test GREEN wordt.
  </behavior>

  <action>
    1. Lees tests/KlasTabStrip.test.tsx volledig.
    2. Vervang het bestaande TAB-01 describe-blok (regels 117-131) door het nieuwe blok hierboven.
    3. Voer npm test uit. De TAB-01 test moet nu ROOD zijn (getAllByRole gooit omdat er slechts 1 button is,
       of de length-assertion faalt). Alle overige KlasTabStrip tests en alle andere testfiles moeten groen blijven.
  </action>

  <verify>
    <automated>cd /d/Downloads/get-shit-done-main/dashboard-2 && npm test -- --reporter=verbose 2>&1 | grep -E "TAB-01|KlasTabStrip|FAIL|passed|failed"</automated>
  </verify>

  <acceptance_criteria>
    - tests/KlasTabStrip.test.tsx TAB-01 describe-blok bevat nu exact 1 it()-blok (was 2)
    - grep -c "toHaveLength(2)" tests/KlasTabStrip.test.tsx geeft minstens 1
    - grep -c "Klas B verwijderen" tests/KlasTabStrip.test.tsx geeft minstens 1
    - npm test toont TAB-01 test als FAILING — alle overige tests groen
    - De makeProps helper is ongewijzigd (k1 heeft canDelete: true, k2 heeft canDelete: false)
  </acceptance_criteria>

  <done>TAB-01 test RED. Alle overige tests groen. Plan 02 maakt beide RED tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Geen nieuwe vertrouwensgrenzen — dit plan schrijft uitsluitend testcode.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-SC | Tampering | npm installs | accept | Geen nieuwe npm packages in dit plan. |
</threat_model>

<verification>
Na uitvoering:
- tests/KlasVerwijderenModal.test.tsx bestaat met 5 RED tests
- TAB-01 describe-blok in KlasTabStrip.test.tsx is herschreven — 1 test, verwacht 2 × knoppen
- npm test: 6 nieuwe/aangepaste tests ROOD, alle overige tests groen
</verification>

<success_criteria>
1. tests/KlasVerwijderenModal.test.tsx bestaat met exact 5 RED tests (KLS-04..07 gedekt)
2. KlasTabStrip.test.tsx TAB-01 beschrijft het nieuwe gedrag: × voor ALLE klassen
3. Nul regressies in de overige testsuite
</success_criteria>

<output>
Maak .planning/phases/33-klas-verwijderen-bevestiging/33-01-SUMMARY.md aan wanneer klaar.
</output>
