---
phase: 11-typescript-migratie
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - utils/schema.ts
  - utils/datamodel.ts
  - utils/leerlijnen.ts
autonomous: true
requirements:
  - MIG-03

must_haves:
  truths:
    - "utils/schema.ts exporteert DEELGEBIEDEN, SCORE_LEVELS, detectColumnMapping, normalizeScore als named exports"
    - "utils/datamodel.ts exporteert appState als mutable object, plus addStudent, saveState, loadState, clearState, normalizeNaam, mergeVerzuim, getVerzuim"
    - "utils/leerlijnen.ts exporteert getLeerlijnenMapping, saveLeerlijnenMapping, resetLeerlijnenMapping"
    - "Geen window.* assignments meer in schema.ts, datamodel.ts of leerlijnen.ts"
    - "npm run typecheck exit 0 na alle drie migraties"
    - "npm run typecheck-migrated exit 0 na alle drie migraties (noImplicitAny:true voor utils/**)"
  artifacts:
    - path: "utils/schema.ts"
      provides: "Domain constants als named ES module exports; geen IIFE; geen window.*"
      exports: ["DEELGEBIEDEN", "SCORE_LEVELS", "detectColumnMapping", "normalizeScore"]
    - path: "utils/datamodel.ts"
      provides: "In-memory data model met mutable appState export"
      exports: ["appState", "addStudent", "saveState", "loadState", "clearState", "normalizeNaam", "mergeVerzuim", "getVerzuim"]
    - path: "utils/leerlijnen.ts"
      provides: "Leerlijn mapping persistentie; importeert DEELGEBIEDEN van ./schema"
      exports: ["getLeerlijnenMapping", "saveLeerlijnenMapping", "resetLeerlijnenMapping"]
  key_links:
    - from: "utils/leerlijnen.ts"
      to: "utils/schema.ts"
      via: "import { DEELGEBIEDEN } from './schema'"
      pattern: "import.*DEELGEBIEDEN.*from.*schema"
    - from: "utils/klassen.ts (later Plan 03)"
      to: "utils/datamodel.ts"
      via: "import { appState, saveState } from './datamodel'"
      pattern: "export const appState"
---

<objective>
Migreer de drie bodem-laag utils (geen lokale imports, simpelste bestanden) van JavaScript naar TypeScript. Dit zijn de fundament-modules die alle andere utils importeren — schema.ts heeft nul imports, datamodel.ts heeft nul lokale imports, leerlijnen.ts importeert alleen schema.

Purpose: Door de bodem-laag als eerste te migreren ontstaan correcte TypeScript exports die Plan 03 (klassen, actiepunten, prognosis) direct kan importeren. Elke fout hier zou doorslaan naar alle afhankelijke modules.

Output: utils/schema.ts, utils/datamodel.ts, utils/leerlijnen.ts — alle drie zonder window.* globals, met named exports, TypeScript-compileerbaar. Na dit plan moet `npm run typecheck-migrated` (noImplicitAny:true) ook exit 0 geven voor deze drie bestanden.
</objective>

<execution_context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-02-PLAN.md
</execution_context>

<context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/ROADMAP.md
@D:/Downloads/get-shit-done-main/dashboard-2/tsconfig.json
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-PATTERNS.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-RESEARCH.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-01-SUMMARY.md

<interfaces>
<!-- Migratiepatronen uit 11-PATTERNS.md (samenvatting voor executor): -->

<!-- Pattern B: window.* removal zonder IIFE (schema.js, datamodel.js):
  Verwijder alle regels die beginnen met "window.X = ..."
  Voeg "export" keyword toe voor const/function declaraties
  schema.js heeft GEEN IIFE — alleen window.* assignments verwijderen

  schema.js bottom (te verwijderen):
    window.SCORE_LEVELS = SCORE_LEVELS;
    window.DEELGEBIEDEN = DEELGEBIEDEN;
    window.detectColumnMapping = detectColumnMapping;
    window.normalizeScore = normalizeScore;

  datamodel.js: window.appState = { ... } → export const appState = { ... }
  datamodel.js: window.addStudent = function → export function addStudent
  (zelfde voor saveState, loadState, clearState, normalizeNaam, mergeVerzuim, getVerzuim)
  Interne window.appState self-referenties → appState (lokale naam)
-->

<!-- Pattern A: IIFE removal (leerlijnen.js):
  Verwijder: (function() { aan het begin
  Verwijder: })(); aan het einde
  var binnenin IIFE → let/const op module niveau
  window.X = function → export function X

  leerlijnen.js window.* dependencies:
    window.DEELGEBIEDEN (2x in buildDefault en isValid) → DEELGEBIEDEN (na import)
  leerlijnen.js exports:
    window.getLeerlijnenMapping = function() → export function getLeerlijnenMapping
    window.saveLeerlijnenMapping = function → export function saveLeerlijnenMapping
    window.resetLeerlijnenMapping = function → export function resetLeerlijnenMapping
-->

<!-- TypeScript type aanpak (D-11-21):
  Gebruik as any met // TODO: type comment voor onbekende parameter types
  Geen strictNullChecks — global tsconfig heeft strict: false
  schema.ts kan concrete types toevoegen (DEELGEBIEDEN array is goed typeerbaar):
    export interface Deelgebied { id: string; label: string; group: string }
    export const DEELGEBIEDEN: Deelgebied[] = [...]
    export const SCORE_LEVELS = ['onvoldoende', 'voldoende', 'goed', 'excellent'] as const

  BELANGRIJK (D-11-05 via tsconfig.migrated.json): npm run typecheck-migrated
  gebruikt noImplicitAny:true. Alle parameters moeten expliciete types hebben —
  gebruik as any of : any maar NIET impliciet untyped parameters.
  Correct: function foo(x: any): void   — expliciete any, OK
  Fout:    function foo(x): void        — impliciete any, fails typecheck-migrated
-->

<!-- appState test-reset patroon (kritisch voor actiepunten.test.js update later):
  appState moet als mutable geëxporteerd object beschikbaar zijn zodat tests
  appState.students = [...] kunnen schrijven. Gebruik:
    export const appState = {
      students: [] as any[],
      lastImportErrors: [] as any[],
      importing: false,
    }
  NIET als frozen/readonly — tests moeten .students kunnen muteren.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: Migreer utils/schema.js → utils/schema.ts</name>
  <files>utils/schema.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/utils/schema.js
  </read_first>
  <behavior>
    - utils/schema.ts bestaat; utils/schema.js bestaat niet meer (hernoemd per D-11-01)
    - Exporteert DEELGEBIEDEN als array met Deelgebied interface objecten (id, label, group)
    - Exporteert SCORE_LEVELS als const array
    - Exporteert detectColumnMapping en normalizeScore als named functions
    - Geen window.* regels meer in het bestand
    - Alle parameters hebben expliciete types (geen impliciete any)
    - npm run typecheck exit 0
    - npm run typecheck-migrated exit 0
  </behavior>
  <action>
    Hernoem utils/schema.js naar utils/schema.ts (of maak utils/schema.ts aan met de volledige inhoud van schema.js en verwijder daarna schema.js).

    Voer de volgende specifieke wijzigingen door:

    1. Verwijder de vier window.* assignment regels onderaan het bestand:
       - window.SCORE_LEVELS = SCORE_LEVELS;
       - window.DEELGEBIEDEN = DEELGEBIEDEN;
       - window.detectColumnMapping = detectColumnMapping;
       - window.normalizeScore = normalizeScore;

    2. Voeg export keyword toe aan de const declaraties:
       - const SCORE_LEVELS → export const SCORE_LEVELS
       - const DEELGEBIEDEN → export const DEELGEBIEDEN
       - Voeg eventueel toe: export type ScoreLevel = typeof SCORE_LEVELS[number]
       - Voeg eventueel toe: export interface Deelgebied { id: string; label: string; group: string }

    3. Voeg export keyword toe aan de twee functies:
       - function detectColumnMapping → export function detectColumnMapping
       - function normalizeScore → export function normalizeScore

    4. Voeg expliciete parameter types toe aan alle functies (vereist door tsconfig.migrated.json noImplicitAny:true):
       - Iedere function parameter krijgt een type annotatie: (headers: string[]) of (raw: unknown) of (x: any) etc.
       - Gebruik as any met // TODO: type comment voor parameters die TypeScript niet kan infereren.

    5. Geen andere inhoud wijzigen — alle implementatielogica blijft byte-identiek.

    schema.ts heeft NULIMPORTEN — importeer NIETS van andere locale modules (anders circulaire afhankelijkheid).
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "const fs=require('fs'); if(fs.existsSync('utils/schema.js')) throw new Error('schema.js still exists!'); fs.statSync('utils/schema.ts'); console.log('schema.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "export const DEELGEBIEDEN" utils/schema.ts && grep -c "export const SCORE_LEVELS" utils/schema.ts && grep -c "export function normalizeScore" utils/schema.ts && grep -c "export function detectColumnMapping" utils/schema.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "window\." utils/schema.ts | xargs -I{} sh -c 'if [ {} -ne 0 ]; then echo "FAIL: window.* still present"; exit 1; else echo "OK: no window.*"; fi'</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck-migrated 2>&1 | tail -5</automated>
  </verify>
  <done>utils/schema.ts bestaat met export const DEELGEBIEDEN, export const SCORE_LEVELS, export function detectColumnMapping, export function normalizeScore. utils/schema.js bestaat niet meer. Geen window.* regels. npm run typecheck exit 0. npm run typecheck-migrated exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: Migreer utils/datamodel.js → utils/datamodel.ts</name>
  <files>utils/datamodel.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/utils/datamodel.js
  </read_first>
  <behavior>
    - utils/datamodel.ts bestaat; utils/datamodel.js bestaat niet meer
    - Exporteert appState als mutable const object met students, lastImportErrors, importing velden
    - Exporteert addStudent, saveState, loadState, clearState, normalizeNaam, mergeVerzuim, getVerzuim als named functions
    - Alle interne window.appState self-referenties zijn vervangen door appState (lokaal)
    - Geen window.* regels meer
    - Alle function parameters hebben expliciete types (: any is OK, impliciet is niet OK)
    - npm run typecheck exit 0
    - npm run typecheck-migrated exit 0
    - npm run test exit 0 (bestaande 9 tests ongewijzigd)
  </behavior>
  <action>
    Hernoem utils/datamodel.js naar utils/datamodel.ts.

    Voer de volgende specifieke wijzigingen door:

    1. Vervang window.appState = { ... } → export const appState = { students: [] as any[], lastImportErrors: [] as any[], importing: false }
       appState moet mutable zijn (niet Object.freeze) zodat tests appState.students = [...] kunnen schrijven.

    2. Vervang alle window.addStudent = function → export function addStudent
       Vervang window.saveState = function → export function saveState
       Vervang window.loadState = function → export function loadState
       Vervang window.clearState = function → export function clearState
       Vervang window.normalizeNaam = function → export function normalizeNaam
       Vervang window.mergeVerzuim = function → export function mergeVerzuim
       Vervang window.getVerzuim = function → export function getVerzuim

    3. Vervang interne window.appState referenties door appState (de lokale geëxporteerde const).
       Grep hiervoor: grep -n "window\.appState" utils/datamodel.js om alle locaties te vinden.

    4. Voeg expliciete parameter types toe aan alle functies (vereist door tsconfig.migrated.json noImplicitAny:true):
       function addStudent(student: any): void
       function normalizeNaam(naam: string): string
       function mergeVerzuim(records: any[]): { matched: number; unmatched: string[] }
       Gebruik as any met // TODO: type comment voor alle student/record parameters.

    5. JSDoc @typedef comments mogen blijven — ze worden niet omgezet naar TypeScript interfaces in Phase 11 (D-11-21).

    Geen lokale imports nodig in datamodel.ts — dit bestand heeft nul externe afhankelijkheden.
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "const fs=require('fs'); if(fs.existsSync('utils/datamodel.js')) throw new Error('datamodel.js still exists!'); fs.statSync('utils/datamodel.ts'); console.log('datamodel.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "export const appState" utils/datamodel.ts && grep -c "export function saveState" utils/datamodel.ts && grep -c "export function addStudent" utils/datamodel.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "window\." utils/datamodel.ts | xargs -I{} sh -c 'if [ {} -ne 0 ]; then echo "FAIL: window.* still present"; exit 1; else echo "OK: no window.*"; fi'</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5 && npm run typecheck-migrated 2>&1 | tail -5 && npm run test 2>&1 | tail -5</automated>
  </verify>
  <done>utils/datamodel.ts bestaat met export const appState (mutable), export function saveState, loadState, clearState, addStudent, normalizeNaam, mergeVerzuim, getVerzuim. utils/datamodel.js bestaat niet meer. Geen window.*. npm run typecheck-migrated exit 0. npm run test 9/9 groen.</done>
</task>

<task type="auto" tdd="true">
  <name>Taak 3: Migreer utils/leerlijnen.js → utils/leerlijnen.ts</name>
  <files>utils/leerlijnen.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/utils/leerlijnen.js
    D:/Downloads/get-shit-done-main/dashboard-2/utils/schema.ts
  </read_first>
  <behavior>
    - utils/leerlijnen.ts bestaat; utils/leerlijnen.js bestaat niet meer
    - Eerste regel is: import { DEELGEBIEDEN } from './schema'
    - Exporteert getLeerlijnenMapping, saveLeerlijnenMapping, resetLeerlijnenMapping
    - IIFE wrapper verwijderd (geen (function() { })() meer)
    - window.DEELGEBIEDEN vervangen door DEELGEBIEDEN (geïmporteerd)
    - Alle function parameters hebben expliciete types
    - npm run typecheck exit 0
    - npm run typecheck-migrated exit 0
  </behavior>
  <action>
    Hernoem utils/leerlijnen.js naar utils/leerlijnen.ts.

    Voer de volgende specifieke wijzigingen door:

    1. Voeg bovenaan het bestand toe (vóór alle bestaande code):
       import { DEELGEBIEDEN } from './schema';

    2. Verwijder de IIFE wrapper:
       - Verwijder de opening: (function() { of (function () {
       - Verwijder de sluiting: })(); onderaan het bestand
       - Alle variabelen binnenin (var STORAGE_KEY, var _cachedMapping) worden module-scope let/const

    3. Vervang var → let voor _cachedMapping (mutable), const voor STORAGE_KEY.

    4. Vervang window.DEELGEBIEDEN door DEELGEBIEDEN op alle plaatsen (grep meldt 2 locaties: buildDefault en isValid).

    5. Vervang de drie window.* exports:
       - window.getLeerlijnenMapping = function() → export function getLeerlijnenMapping(): ...
       - window.saveLeerlijnenMapping = function(mapping) → export function saveLeerlijnenMapping(mapping: any): void
       - window.resetLeerlijnenMapping = function() → export function resetLeerlijnenMapping(): void

    6. Voeg expliciete types toe aan alle functie parameters (noImplicitAny vereiste):
       saveLeerlijnenMapping(mapping: any): boolean of void
       Interne helpers: ook expliciete types (: any is OK, ongetypeerd is niet OK)

    WAARSCHUWING: schema.ts mag NIET worden geïmporteerd van leerlijnen.ts als leerlijnen.ts ooit geïmporteerd wordt door schema.ts — dit zou een circulaire dependency zijn. schema.ts heeft echter nul imports (geverifieerd) dus dit risico bestaat niet.
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "const fs=require('fs'); if(fs.existsSync('utils/leerlijnen.js')) throw new Error('leerlijnen.js still exists!'); fs.statSync('utils/leerlijnen.ts'); console.log('leerlijnen.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "import { DEELGEBIEDEN } from './schema'" utils/leerlijnen.ts && grep -c "export function getLeerlijnenMapping" utils/leerlijnen.ts && grep -c "export function saveLeerlijnenMapping" utils/leerlijnen.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "window\." utils/leerlijnen.ts | xargs -I{} sh -c 'if [ {} -ne 0 ]; then echo "FAIL: window.* still present"; exit 1; else echo "OK: no window.*"; fi'</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5 && npm run typecheck-migrated 2>&1 | tail -5 && npm run test 2>&1 | tail -5</automated>
  </verify>
  <done>utils/leerlijnen.ts bestaat met import { DEELGEBIEDEN } from './schema', geen IIFE wrapper, export function getLeerlijnenMapping/saveLeerlijnenMapping/resetLeerlijnenMapping. utils/leerlijnen.js bestaat niet meer. npm run typecheck exit 0. npm run typecheck-migrated exit 0. npm run test 9/9 groen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grens | Beschrijving |
|-------|-------------|
| localStorage → datamodel.ts | loadState leest van localStorage (jsdom in tests); geen validatie van opgeslagen data shape |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatie |
|-----------|-----------|-----------|------------|-----------|
| T-11-02-01 | Tampering | utils/datamodel.ts appState | accept | appState is in-memory state; versleutelde persistentie komt in Phase 12 |
| T-11-02-02 | Information Disclosure | utils/leerlijnen.ts localStorage | accept | Leerlijnen-mapping bevat geen PII; sleutel is mentordashboard_leerlijnen_v1 |
</threat_model>

<verification>
Na alle drie taken:
- npm run typecheck exit 0
- npm run typecheck-migrated exit 0 (noImplicitAny:true voor utils/schema.ts, utils/datamodel.ts, utils/leerlijnen.ts)
- npm run test exit 0 (minimaal 9 tests slagen)
- utils/schema.js, utils/datamodel.js, utils/leerlijnen.js bestaan NIET meer
- utils/schema.ts, utils/datamodel.ts, utils/leerlijnen.ts bestaan WEL
- Geen window.* in een van de drie .ts bestanden
</verification>

<success_criteria>
Plan 02 is klaar wanneer:
1. utils/schema.ts exporteert DEELGEBIEDEN, SCORE_LEVELS, detectColumnMapping, normalizeScore
2. utils/datamodel.ts exporteert mutable appState object, alle datamodel functies
3. utils/leerlijnen.ts importeert DEELGEBIEDEN van ./schema en exporteert de drie leerlijn functies
4. Alle drie .js bestanden zijn verwijderd
5. npm run typecheck exit 0
6. npm run typecheck-migrated exit 0
7. npm run test exit 0 (9/9 groen)
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/11-typescript-migratie/11-02-SUMMARY.md` aan met:
- Gemigreerde bestanden en het aantal verwijderde window.* regels per bestand
- TypeScript fouten gevonden en opgelost (as any toevoegingen)
- Test resultaten na elke taak
</output>
</output>
