---
phase: 11-typescript-migratie
plan: 04
type: execute
wave: 2
depends_on: ["11-02", "11-03"]
files_modified:
  - utils/aggregation.ts
  - utils/backup.ts
  - utils/spider.ts
autonomous: true
requirements:
  - MIG-03

must_haves:
  truths:
    - "utils/aggregation.ts exporteert aggregateDeelgebiedScores met signatuur (datapunten: any[]) => { aggregationDetail: Record<string, string | null> }"
    - "utils/backup.ts exporteert buildBackupPayload (geeft Uint8Array terug) en applyBackupRestore"
    - "utils/spider.ts exporteert SpiderChart object met buildSpiderSVG methode"
    - "tests/aggregation.test.ts gaat van import-fout naar slagende tests"
    - "tests/backup.test.ts gaat van import-fout naar slagende tests"
    - "tests/spider.test.ts gaat van import-fout naar slagende tests"
    - "npm run typecheck exit 0"
  artifacts:
    - path: "utils/aggregation.ts"
      provides: "Modus-berekening over datapunten per deelgebied"
      exports: ["aggregateDeelgebiedScores"]
    - path: "utils/backup.ts"
      provides: "fflate-gebaseerde backup aanmaak en herstel"
      exports: ["buildBackupPayload", "applyBackupRestore"]
    - path: "utils/spider.ts"
      provides: "SVG spider chart berekeningen"
      exports: ["SpiderChart"]
  key_links:
    - from: "utils/backup.ts"
      to: "fflate"
      via: "import { zipSync, unzipSync, strToU8, strFromU8 } from 'fflate'"
      pattern: "from 'fflate'"
    - from: "utils/backup.ts"
      to: "utils/klassen.ts"
      via: "import { klassenState } from './klassen'"
      pattern: "import.*klassenState.*from.*klassen"
    - from: "utils/aggregation.ts"
      to: "utils/schema.ts"
      via: "import { SCORE_LEVELS } from './schema'"
      pattern: "import.*SCORE_LEVELS.*from.*schema"
---

<objective>
Recreëer de drie verloren utility modules direct als TypeScript (per D-11-12). De API contracten zijn afgeleid uit app.js (D-11-14). De tests in Plan 01 zijn aangemaakt als stubs — dit plan maakt ze groen.

Purpose: aggregation.ts, backup.ts en spider.ts waren verloren door scaffold --force in Phase 10. Ze worden nu direct als TypeScript geschreven — geen .js tussenversie.

Output: Drie nieuwe .ts bestanden die de test stubs uit Plan 01 groen maken.
</objective>

<execution_context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-04-PLAN.md
</execution_context>

<context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/ROADMAP.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-PATTERNS.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-RESEARCH.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-02-SUMMARY.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-03-SUMMARY.md
@D:/Downloads/get-shit-done-main/dashboard-2/index.html.bak

<interfaces>
<!-- API contracten uit app.js (geverifieerd in 11-RESEARCH.md §5): -->

<!-- aggregateDeelgebiedScores:
  app.js regel 1674-1676: window.aggregateDeelgebiedScores(student.datapunten || [])
  Retourneert: { aggregationDetail: Record<string, string | null> }
  Gedrag: modus-score per deelgebied label over alle datapunten
  Invoer: datapunten is array van objecten met minimaal een scores property: { scores: Record<string, string|null> }
  SCORE_LEVELS ordening: ['onvoldoende', 'voldoende', 'goed', 'excellent']
  Tie-break: hogere score wint bij gelijkspel
-->

<!-- buildBackupPayload / applyBackupRestore:
  app.js regel 841: window.buildBackupPayload()  → retourneert Uint8Array (zip)
  app.js regel 945: window.applyBackupRestore(payload, mode)
  mode: 'overschrijven' | 'samenvoegen'
  ZIP bestandsnaam intern: 'mentordashboard-backup.json'
  Payload JSON structuur: { version: 1, klassen: {}, activeKlasId: string|null, exportedAt: string }
  Retourneert: { success: boolean; message: string }
  fflate sync API: zipSync, unzipSync, strToU8, strFromU8 (alle geïnstalleerd in Plan 01)
-->

<!-- SpiderChart.buildSpiderSVG:
  app.js regel 1534, 1553: window.SpiderChart.buildSpiderSVG(axes, scores, fillVar, strokeVar)
  axes: Array<{ key: string; label: string }>
  scores: Record<string, string | null>
  fillVar: CSS variable string (bijv. '--color-spider-fill')
  strokeVar: CSS variable string
  Retourneert: SVG markup string (volledig <svg>...</svg> element)
  Gedrag: score → radius mapping: null=0, onvoldoende=0.25, voldoende=0.5, goed=0.75, excellent=1.0
  Coördinaten: polar → cartesian, polygon points berekening
-->

<!-- Beschikbare exports na Plans 02+03: -->
<!-- utils/schema.ts: SCORE_LEVELS, DEELGEBIEDEN -->
<!-- utils/klassen.ts: klassenState -->
<!-- fflate: zipSync, unzipSync, strToU8, strFromU8 (na Plan 01 npm install) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: Maak utils/aggregation.ts aan</name>
  <files>utils/aggregation.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/tests/aggregation.test.ts
    D:/Downloads/get-shit-done-main/dashboard-2/utils/schema.ts
    D:/Downloads/get-shit-done-main/dashboard-2/index.html.bak
  </read_first>
  <behavior>
    - tests/aggregation.test.ts slaagt volledig (alle 5 tests groen)
    - aggregateDeelgebiedScores([]) retourneert { aggregationDetail: {} }
    - aggregateDeelgebiedScores([{ scores: { 'V&A': 'goed' } }]) retourneert { aggregationDetail: { 'V&A': 'goed' } }
    - Bij twee datapunten: 2x 'goed' + 1x 'voldoende' voor 'V&A' → aggregationDetail['V&A'] === 'goed'
    - Bij gelijkspel: hogere score in SCORE_LEVELS ordening wint
    - null scores tellen niet als stemmen
    - npm run typecheck exit 0
  </behavior>
  <action>
    Maak utils/aggregation.ts aan als nieuw bestand.

    Import: import { SCORE_LEVELS } from './schema';

    Implementeer aggregateDeelgebiedScores(datapunten: any[]): { aggregationDetail: Record<string, string | null> }

    Algoritme:
    1. Bouw een frequentie-map per deelgebied label: { [label: string]: { [score: string]: number } }
    2. Itereer over alle datapunten; elk datapunt heeft een scores object (Record<string, string|null>)
    3. Voor elke score die niet null is: verhoog de teller in de frequentie-map
    4. Kies de modus (meest frequente niet-null score) per deelgebied
    5. Tie-break bij gelijke frequentie: kies de score die later in SCORE_LEVELS staat (hogere score wint)
    6. Als een deelgebied alleen null scores heeft: aggregationDetail[label] = null

    Return: { aggregationDetail } waarbij elke sleutel een deelgebied label is.

    Voeg TypeScript parameter types toe: datapunten: any[]; interne helpers zijn privé (geen export).
    Exporteer ALLEEN aggregateDeelgebiedScores.
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "require('fs').statSync('utils/aggregation.ts'); console.log('aggregation.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "export function aggregateDeelgebiedScores" utils/aggregation.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run test -- tests/aggregation.test.ts 2>&1 | tail -10</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>utils/aggregation.ts bestaat. tests/aggregation.test.ts slaagt (alle tests groen). npm run typecheck exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: Maak utils/backup.ts aan</name>
  <files>utils/backup.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/tests/backup.test.ts
    D:/Downloads/get-shit-done-main/dashboard-2/utils/klassen.ts
    D:/Downloads/get-shit-done-main/dashboard-2/index.html.bak
  </read_first>
  <behavior>
    - tests/backup.test.ts slaagt volledig (alle 4 tests groen)
    - buildBackupPayload() retourneert een Uint8Array met lengte > 0
    - Round-trip: buildBackupPayload() → applyBackupRestore(zip, 'overschrijven') herstelt klassenState.klassen
    - applyBackupRestore met 'samenvoegen' voegt nieuwe klassen toe zonder bestaande te verwijderen
    - applyBackupRestore met ongeldige data retourneert { success: false, message: '...' }
    - npm run typecheck exit 0
  </behavior>
  <action>
    Maak utils/backup.ts aan als nieuw bestand.

    Imports:
      import { zipSync, unzipSync, strToU8, strFromU8 } from 'fflate';
      import { klassenState } from './klassen';

    Constante: const BACKUP_FILENAME = 'mentordashboard-backup.json';

    Implementeer buildBackupPayload(): Uint8Array
      - Serialiseer { version: 1, klassen: klassenState.klassen, activeKlasId: klassenState.activeKlasId, exportedAt: new Date().toISOString() } als JSON string
      - Gebruik zipSync({ [BACKUP_FILENAME]: strToU8(jsonString) })
      - Retourneer de Uint8Array

    Implementeer applyBackupRestore(zipData: Uint8Array, mode: 'overschrijven' | 'samenvoegen'): { success: boolean; message: string }
      - Wikkel in try-catch; bij fout: retourneer { success: false, message: err.message }
      - unzipSync(zipData) → extracted
      - strFromU8(extracted[BACKUP_FILENAME]) → JSON string
      - JSON.parse → payload ({ version, klassen, activeKlasId })
      - mode 'overschrijven': klassenState.klassen = payload.klassen; klassenState.activeKlasId = payload.activeKlasId
      - mode 'samenvoegen': Object.assign(klassenState.klassen, payload.klassen) (nieuwe klassen toevoegen, bestaande updaten)
      - Retourneer { success: true, message: 'Backup hersteld' }

    Exporteer ALLEEN buildBackupPayload en applyBackupRestore.

    NOOT: AES encryptie is NIET geïmplementeerd (fflate ondersteunt dit niet; wachtwoord-optie is dead code in Phase 11 — Phase 12 voegt encryptie toe via OS keychain).
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "require('fs').statSync('utils/backup.ts'); console.log('backup.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "export function buildBackupPayload" utils/backup.ts && grep -c "export function applyBackupRestore" utils/backup.ts && grep -c "from 'fflate'" utils/backup.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run test -- tests/backup.test.ts 2>&1 | tail -10</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>utils/backup.ts bestaat met fflate imports en klassenState import. tests/backup.test.ts slaagt (alle tests groen). npm run typecheck exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Taak 3: Maak utils/spider.ts aan</name>
  <files>utils/spider.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/tests/spider.test.ts
    D:/Downloads/get-shit-done-main/dashboard-2/index.html.bak
  </read_first>
  <behavior>
    - tests/spider.test.ts slaagt volledig (alle 4 tests groen)
    - SpiderChart.buildSpiderSVG(axes, {}, '--fill', '--stroke') retourneert een niet-lege string
    - Output bevat '<svg'
    - Output bevat 'polygon' wanneer niet alle scores null zijn
    - Lege scores object gooit geen fout
    - npm run typecheck exit 0
  </behavior>
  <action>
    Maak utils/spider.ts aan als nieuw bestand. Geen lokale imports nodig (pure wiskunde).

    Exporteer SpiderChart als const object:

    export const SpiderChart = {
      buildSpiderSVG(
        axes: Array<{ key: string; label: string }>,
        scores: Record<string, string | null>,
        fillVar: string,
        strokeVar: string
      ): string { ... }
    };

    Implementeer buildSpiderSVG:
    1. Score → radius mapping:
       null = 0, 'onvoldoende' = 0.25, 'voldoende' = 0.5, 'goed' = 0.75, 'excellent' = 1.0
    2. SVG canvas: gebruik viewBox="0 0 200 200", centrum (100, 100), maximale radius 80
    3. Voor elk as-punt (index i van n totaal):
       hoek = (2 * Math.PI * i / n) - (Math.PI / 2)  (start bovenaan)
       radius = scoreRadius * 80
       x = 100 + Math.cos(hoek) * radius
       y = 100 + Math.sin(hoek) * radius
    4. Bouw polygon points string: alle (x, y) coördinaten samengevoegd als "x1,y1 x2,y2 ..."
    5. Return volledige SVG string:
       '<svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">' +
       '<polygon points="' + points + '" ' +
       'fill="var(' + fillVar + ')" stroke="var(' + strokeVar + ')" stroke-width="2" fill-opacity="0.4"/>' +
       '</svg>'

    Als axes leeg is: retourneer '<svg viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg"></svg>'.

    Exporteer ALLEEN SpiderChart. Intern gebruik van scoreToRadius helper is privé.
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "require('fs').statSync('utils/spider.ts'); console.log('spider.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "export const SpiderChart" utils/spider.ts && grep -c "buildSpiderSVG" utils/spider.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run test -- tests/spider.test.ts 2>&1 | tail -10</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>utils/spider.ts bestaat met export SpiderChart en buildSpiderSVG. tests/spider.test.ts slaagt (alle tests groen). npm run typecheck exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grens | Beschrijving |
|-------|-------------|
| zip Uint8Array → applyBackupRestore | Backup data kan van onbekende bron komen (bijv. gedeeld bestand); JSON.parse van onbekende inhoud |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatie |
|-----------|-----------|-----------|------------|-----------|
| T-11-04-01 | Tampering | utils/backup.ts applyBackupRestore | mitigate | try-catch omhult de volledige parse en state update; ongeldige data geeft success:false terug zonder state te muteren |
| T-11-04-02 | Denial of Service | utils/spider.ts buildSpiderSVG | accept | Puur rekenkundig; max 19 as-punten (DEELGEBIEDEN); O(n) complexiteit |
| T-11-04-03 | Information Disclosure | utils/backup.ts buildBackupPayload | accept | Backup bevat leerlingdata; encryptie volgt in Phase 12; backup is gebruikersactie niet automatisch |
</threat_model>

<verification>
Na alle drie taken:
- npm run typecheck exit 0
- npm run test -- tests/aggregation.test.ts exit 0 (alle tests groen)
- npm run test -- tests/backup.test.ts exit 0 (alle tests groen)
- npm run test -- tests/spider.test.ts exit 0 (alle tests groen)
- utils/aggregation.ts, utils/backup.ts, utils/spider.ts bestaan
</verification>

<success_criteria>
Plan 04 is klaar wanneer:
1. utils/aggregation.ts exporteert aggregateDeelgebiedScores; tests groen
2. utils/backup.ts exporteert buildBackupPayload + applyBackupRestore via fflate; tests groen
3. utils/spider.ts exporteert SpiderChart.buildSpiderSVG; tests groen
4. npm run typecheck exit 0
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/11-typescript-migratie/11-04-SUMMARY.md` aan met:
- Recreëerde bestanden en hun implementatiekeuzes
- Test resultaten (verwacht: alle 3 test files groen)
- Eventuele afwijkingen van de API contracten uit app.js
</output>
