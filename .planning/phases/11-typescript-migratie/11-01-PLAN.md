---
phase: 11-typescript-migratie
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tsconfig.json
  - tests/prognosis.test.ts
  - tests/aggregation.test.ts
  - tests/backup.test.ts
  - tests/spider.test.ts
  - tests/feedback.test.ts
  - tests/parseStage.test.ts
autonomous: false
requirements:
  - MIG-01
  - MIG-02
  - MIG-03

must_haves:
  truths:
    - "npm run typecheck verwerkt utils/, parsers/ en tests/ — niet alleen src/"
    - "fflate is aanwezig in node_modules"
    - "Alle test stub-bestanden bestaan in tests/ met describe.skipIf guard"
    - "tests/fixtures/ map bestaat"
    - "Fixture bestanden zijn gecommit of tests overslaan correct wanneer ze ontbreken"
  artifacts:
    - path: "tsconfig.json"
      provides: "TypeScript project scope uitgebreid naar utils, parsers, tests"
      contains: '"utils", "parsers", "tests"'
    - path: "node_modules/fflate"
      provides: "fflate npm package voor utils/backup.ts"
    - path: "tests/prognosis.test.ts"
      provides: "Failing test stubs voor berekenPrognose (MIG-03)"
    - path: "tests/aggregation.test.ts"
      provides: "Failing test stubs voor aggregateDeelgebiedScores"
    - path: "tests/backup.test.ts"
      provides: "Failing test stubs voor buildBackupPayload + applyBackupRestore"
    - path: "tests/spider.test.ts"
      provides: "Failing test stubs voor SpiderChart.buildSpiderSVG"
    - path: "tests/feedback.test.ts"
      provides: "Test stubs voor feedback logica"
    - path: "tests/parseStage.test.ts"
      provides: "Integration test stub met fixture guard (MIG-01)"
    - path: "tests/fixtures/"
      provides: "Map voor fixture bestanden"
  key_links:
    - from: "tsconfig.json include array"
      to: "utils/, parsers/, tests/"
      via: "JSON array uitbreiding"
      pattern: '"utils"'
    - from: "tests/parseStage.test.ts"
      to: "tests/fixtures/sample-voortgang.pdf"
      via: "describe.skipIf(!FIXTURE_EXISTS)"
      pattern: "skipIf"
---

<objective>
Wave 0 prerequisites zodat alle latere migratieplans kunnen starten: TypeScript scope uitbreiden, fflate installeren, test stubs aanmaken met skip guards, en fixture bestanden beschikbaar stellen via een human checkpoint.

Purpose: Zonder deze stap ziet `npm run typecheck` de gemigreerde bestanden nooit (tsconfig pitfall #1). De test stubs bepalen de "rode" fase van de TDD-aanpak — elke migratiegolf heeft failing tests die ze groen maakt.

Output: Bijgewerkt tsconfig.json, fflate in node_modules, 6 test stub-bestanden met skip guards, tests/fixtures/ aangemaakt.
</objective>

<execution_context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-01-PLAN.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-VALIDATION.md
</execution_context>

<context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/ROADMAP.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/STATE.md
@D:/Downloads/get-shit-done-main/dashboard-2/tsconfig.json
@D:/Downloads/get-shit-done-main/dashboard-2/vitest.config.ts
@D:/Downloads/get-shit-done-main/dashboard-2/tests/actiepunten.test.js
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-PATTERNS.md

<interfaces>
<!-- Bestaande tsconfig.json structuur — uitbreiding is additive -->
<!-- tsconfig.json huidig: "include": ["src"] — uitbreiden naar ["src", "utils", "parsers", "tests"] -->
<!-- vitest.config.ts include: ['tests/**/*.test.{js,ts}'] — geen wijziging nodig -->

<!-- Testbestand structuur (uit actiepunten.test.js patroon): -->
<!-- Alle nieuwe test stubs volgen dit patroon:
  - Geen 'use strict', geen global.window
  - Import uit de te-migrereren module
  - describe.skipIf guard voor bestanden die fixture vereisen
  - beforeEach reset van module state via geëxporteerde objecten
  - Concrete assertions (geen placeholder comments)
-->

<!-- fflate sync API (na installatie):
  import { zipSync, unzipSync, strToU8, strFromU8 } from 'fflate'
  zipSync({ 'file.json': strToU8(jsonString) }) → Uint8Array
  unzipSync(uint8Array) → { 'file.json': Uint8Array }
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: tsconfig.json uitbreiden + fflate installeren</name>
  <files>tsconfig.json</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/tsconfig.json
    D:/Downloads/get-shit-done-main/dashboard-2/package.json
  </read_first>
  <behavior>
    - npm run typecheck slaagt na wijziging (exit 0)
    - tsconfig.json "include" array bevat exact: ["src", "utils", "parsers", "tests"]
    - node_modules/fflate/ bestaat na npm install
    - package.json dependencies bevat "fflate" entry
  </behavior>
  <action>
    Stap 1 — Wijzig tsconfig.json: vervang de "include" array van ["src"] naar ["src", "utils", "parsers", "tests"]. Alle andere velden in compilerOptions blijven ongewijzigd (strict: false, noImplicitAny: false, etc. — per D-11-05).

    Stap 2 — Installeer fflate via: npm install fflate
    Dit voegt fflate aan dependencies toe in package.json (niet devDependencies — backup is productie logica).

    Stap 3 — Valideer: npm run typecheck moet nog steeds exit 0 geven. Als er nieuwe typefouten verschijnen door de verbreding van de include, zijn dat typefouten in bestaande .js bestanden die TypeScript nu meeneemt — dit is verwacht en acceptabel; ze worden opgelost in de migratieplannen.
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "const t=JSON.parse(require('fs').readFileSync('tsconfig.json','utf8')); const inc=t.include; ['src','utils','parsers','tests'].forEach(p=>{if(!inc.includes(p))throw new Error('Missing: '+p);}); console.log('tsconfig OK:', inc.join(', '))"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "require('fs').statSync('node_modules/fflate'); console.log('fflate OK')"</automated>
  </verify>
  <done>tsconfig.json "include" bevat ["src", "utils", "parsers", "tests"]; node_modules/fflate bestaat; package.json bevat fflate dependency.</done>
</task>

<task type="auto">
  <name>Taak 2: Test stub-bestanden aanmaken (failing stubs met skip guards)</name>
  <files>
    tests/prognosis.test.ts
    tests/aggregation.test.ts
    tests/backup.test.ts
    tests/spider.test.ts
    tests/feedback.test.ts
    tests/parseStage.test.ts
  </files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/tests/actiepunten.test.js
    D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-PATTERNS.md
    D:/Downloads/get-shit-done-main/dashboard-2/tests/vitest-setup.js
  </read_first>
  <action>
    Maak de volgende test stub-bestanden aan in tests/. Elk bestand importeert de nog-niet-bestaande module — Vitest slaat de test over met describe.skip totdat de module bestaat, MAAR de import moet werken. Gebruik daarom een try-catch import patroon of schrijf de stubs zo dat ze pas falen als de module daadwerkelijk ontbreekt na de migratiegolf.

    BETER patroon: schrijf elke stub als echte tests met concrete assertions. Ze mogen falen in Wave 0 zolang de import een ModuleNotFoundError geeft — Vitest toont dan "1 failed" maar niet "syntax error". De tests worden groen als de module beschikbaar is na migratie.

    tests/prognosis.test.ts — importeert berekenPrognose, berekenAllePrognoses uit '../utils/prognosis'; DEELGEBIEDEN uit '../utils/schema'. Tests: (1) negatief label wanneer >6 deelgebieden onvoldoende zijn; (2) neutraal label wanneer <13 voldoende; (3) sbl label wanneer >=13 voldoende maar <15; (4) berekenPrognose met leeg scores object geeft een geldig label terug; (5) berekenAllePrognoses met lege array geeft lege array. Minimaal 5 tests.

    tests/aggregation.test.ts — importeert aggregateDeelgebiedScores uit '../utils/aggregation'. Tests: (1) lege datapunten geeft leeg aggregationDetail object; (2) één datapunt — scores worden direct doorgegeven; (3) twee datapunten — modus wint (2x goed > 1x voldoende); (4) gelijke scores — hogere score wint bij gelijkspel; (5) null scores tellen niet mee als stemmen. Minimaal 5 tests.

    tests/backup.test.ts — importeert buildBackupPayload, applyBackupRestore uit '../utils/backup'; klassenState uit '../utils/klassen'. Tests: (1) buildBackupPayload geeft Uint8Array terug; (2) round-trip: build + restore overschrijven herstelt klassenState; (3) samenvoegen voegt nieuwe klassen toe zonder bestaande te verwijderen; (4) ongeldige zip data geeft success: false terug. Minimaal 4 tests.

    tests/spider.test.ts — importeert SpiderChart uit '../utils/spider'; DEELGEBIEDEN uit '../utils/schema'. Tests: (1) buildSpiderSVG geeft een string terug; (2) output bevat '<svg'; (3) alle excellent scores geeft polygon terug; (4) lege scores object gooit geen fout. Minimaal 4 tests.

    tests/feedback.test.ts — Omdat feedback logica in actiepunten.ts zit (geen aparte utils/feedback.js in index.html.bak), importeert dit bestand actiepuntenStore, normalizeOnderwerp uit '../utils/actiepunten' en appState uit '../utils/datamodel'. Tests dekken de store.add met herhaling status — dit is de "feed forward" functionaliteit. Minimaal 3 tests die de herhaling-detectie vanuit actiepunten.ts testen (aanvullend op actiepunten.test.js).

    tests/parseStage.test.ts — Gebruikt existsSync + describe.skipIf patroon. Importeert existsSync uit 'fs'; berekent FIXTURE_PATH als new URL('./fixtures/sample-voortgang.pdf', import.meta.url).pathname; FIXTURE_EXISTS check. describe.skipIf(!FIXTURE_EXISTS)('parseStageFile integration', ...) met minimaal 2 tests: (1) fixture PDF wordt geparsed en geeft een array van records terug; (2) elk record heeft een naam eigenschap. Voeg ook een tests/fixtures/ map toe via mkdir als die niet bestaat.

    Maak ook de tests/fixtures/ directory aan (leeg bestand .gitkeep erin zodat git de map bijhoudt).
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "const fs=require('fs'); ['tests/prognosis.test.ts','tests/aggregation.test.ts','tests/backup.test.ts','tests/spider.test.ts','tests/feedback.test.ts','tests/parseStage.test.ts'].forEach(f=>{fs.statSync(f); console.log('OK:', f)}); fs.statSync('tests/fixtures'); console.log('fixtures dir OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Alle 6 test stub-bestanden bestaan. tests/fixtures/ map bestaat. npm run test geeft geen syntax errors — stubs kunnen falen door ontbrekende modules maar niet door syntaxfouten. actiepunten.test.js (9 tests) blijft slagen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Fixture bestanden aanleveren</name>
  <what-built>
    Wave 0 toolchain is klaar: tsconfig uitgebreid, fflate geïnstalleerd, test stubs aangemaakt.
    tests/fixtures/ map bestaat maar is leeg (alleen .gitkeep).

    De parser integration tests (tests/parseStage.test.ts en toekomstig tests/excel.test.ts) vereisen echte fixture bestanden om te draaien. Zolang de fixtures ontbreken slaan de tests correct over via describe.skipIf — dit is geen blocker voor de migratiegolven, maar de MIG-01 en MIG-02 requirements kunnen pas volledig worden geverifieerd met echte fixtures.
  </what-built>
  <how-to-verify>
    Stap 1 — Controleer dat de Wave 0 setup werkt:
      cd D:/Downloads/get-shit-done-main/dashboard-2
      npm run test
    Verwacht: 9 tests slagen (actiepunten.test.js), nieuwe stub-bestanden falen door ontbrekende modules, geen syntax errors.

    Stap 2 — Lever fixture bestanden aan via ÉÉN van de volgende opties:

    OPTIE A (aanbevolen): Gebruik een echte CIOS voortgang-PDF met geanonimiseerde data.
      - Naam leerling vervangen door "Test Leerling", leerlingnummer door "000000"
      - Sla op als: tests/fixtures/sample-voortgang.pdf

    OPTIE B: Laat de fixture tests overgeslagen — migratie kan volledig worden uitgevoerd zonder fixture bestanden. MIG-01 wordt dan geverifieerd via handmatige import na Phase 13.

    Voor de Excel fixture (MIG-02):
      - Maak een minimaal .xls bestand met kolommen: "Leerlingnummer", "Naam", "Afwezig" en één rij data met Nederlandse tekens (bijv. "Müller")
      - Sla op als: tests/fixtures/sample-verzuim.xls

    Stap 3 — Als fixtures zijn toegevoegd: npm run test opnieuw uitvoeren en bevestigen dat parseStage.test.ts nu draait (niet skippt).
  </how-to-verify>
  <resume-signal>
    Typ "fixtures geleverd" als je tests/fixtures/sample-voortgang.pdf en/of tests/fixtures/sample-verzuim.xls hebt toegevoegd.
    Typ "fixtures overgeslagen" als je doorgaat zonder fixtures (MIG-01/MIG-02 worden later geverifieerd).
    In beide gevallen ga je verder met Plan 02 (utils migratie).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grens | Beschrijving |
|-------|-------------|
| fixture bestanden → test runner | Fixture bestanden zijn lezer-gecontroleerde input; kunnen geen code uitvoeren — enkel gelezen door parsers |
| npm install → node_modules | Package registry vertrouwen; fflate is een bekende, breed gebruikte library |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatie |
|-----------|-----------|-----------|------------|-----------|
| T-11-00-01 | Information Disclosure | tests/fixtures/ | mitigate | .gitignore voegt geen fixture toe — fixture moet geanonimiseerde data bevatten; checkpoint vraagt expliciet om anonimisering |
| T-11-00-02 | Tampering | tsconfig.json include wijziging | accept | include uitbreiding is additive; bestaande src/ scope ongewijzigd; laag risico |
</threat_model>

<verification>
Na Wave 0 taak 1 en 2:
- npm run typecheck exit 0
- npm run test exit 0 (9 tests slagen, stubs kunnen falen door ontbrekende modules maar niet door syntax)
- tsconfig.json "include" bevat "utils", "parsers", "tests"
- node_modules/fflate/ bestaat
- tests/fixtures/ map bestaat
</verification>

<success_criteria>
Wave 0 is klaar wanneer:
1. tsconfig.json "include": ["src", "utils", "parsers", "tests"]
2. node_modules/fflate aanwezig; package.json bevat fflate entry
3. Alle 6 test stubs bestaan: prognosis.test.ts, aggregation.test.ts, backup.test.ts, spider.test.ts, feedback.test.ts, parseStage.test.ts
4. tests/fixtures/ map bestaat
5. npm run test exit 0 (actiepunten.test.js 9/9 slagen)
6. Human heeft beslissing genomen over fixture bestanden
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/11-typescript-migratie/11-01-SUMMARY.md` aan met:
- Wat er is gebouwd (tsconfig wijziging, fflate versie, test stubs aangemaakt)
- Fixture beslissing (geleverd / overgeslagen)
- Eventuele afwijkingen van het plan
</output>
