---
phase: 11-typescript-migratie
plan: 03
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - utils/klassen.ts
  - utils/actiepunten.ts
  - utils/prognosis.ts
autonomous: true
requirements:
  - MIG-03

must_haves:
  truths:
    - "utils/klassen.ts importeert appState, saveState van ./datamodel en exporteert klassenState, alle klas-functies"
    - "utils/actiepunten.ts importeert appState, saveState van ./datamodel en exporteert actiepuntenStore, normalizeOnderwerp, isHerhaling"
    - "utils/prognosis.ts importeert DEELGEBIEDEN van ./schema en getLeerlijnenMapping van ./leerlijnen; exporteert berekenPrognose, berekenAllePrognoses"
    - "Geen window.* assignments in klassen.ts, actiepunten.ts of prognosis.ts"
    - "npm run typecheck exit 0"
    - "npm run test exit 0 — actiepunten.test.js 9/9 groen (ook al is de import nog require-based)"
  artifacts:
    - path: "utils/klassen.ts"
      provides: "Multi-klas state management met geïmporteerde datamodel afhankelijkheden"
      exports: ["klassenState", "switchActiveKlas", "createKlas", "deleteKlas", "saveKlassen", "loadKlassen", "getActiveStudents", "getAllRecordsForStudent"]
    - path: "utils/actiepunten.ts"
      provides: "Actiepunten store met IIFE verwijderd en ESM imports"
      exports: ["actiepuntenStore", "normalizeOnderwerp", "isHerhaling"]
    - path: "utils/prognosis.ts"
      provides: "Doorstroomnorm engine met ESM imports van schema en leerlijnen"
      exports: ["berekenPrognose", "berekenAllePrognoses"]
  key_links:
    - from: "utils/klassen.ts"
      to: "utils/datamodel.ts"
      via: "import { appState, saveState } from './datamodel'"
      pattern: "import.*appState.*saveState.*from.*datamodel"
    - from: "utils/actiepunten.ts"
      to: "utils/datamodel.ts"
      via: "import { appState, saveState } from './datamodel'"
      pattern: "import.*appState.*saveState.*from.*datamodel"
    - from: "utils/prognosis.ts"
      to: "utils/schema.ts"
      via: "import { DEELGEBIEDEN } from './schema'"
      pattern: "import.*DEELGEBIEDEN.*from.*schema"
    - from: "utils/prognosis.ts"
      to: "utils/leerlijnen.ts"
      via: "import { getLeerlijnenMapping } from './leerlijnen'"
      pattern: "import.*getLeerlijnenMapping.*from.*leerlijnen"
---

<objective>
Migreer de drie afhankelijke utils (wave 1, parallel met plan 02 maar vereist dat plan 02 klaar is voor de imports). klassen.ts en actiepunten.ts importeren beide van datamodel.ts; prognosis.ts importeert van schema.ts en leerlijnen.ts.

Purpose: Na dit plan is de volledige utils laag beschikbaar als TypeScript modules. Plan 04 (verloren utils recreëren) en Plan 05 (parsers) kunnen dan direct bouwen op de schone TypeScript exports.

Output: utils/klassen.ts, utils/actiepunten.ts, utils/prognosis.ts — alle drie zonder window.* globals, met named exports, TypeScript-compileerbaar.
</objective>

<execution_context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-03-PLAN.md
</execution_context>

<context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/ROADMAP.md
@D:/Downloads/get-shit-done-main/dashboard-2/tsconfig.json
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-PATTERNS.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-RESEARCH.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-02-SUMMARY.md

<interfaces>
<!-- Beschikbare exports na Plan 02 (vereiste imports voor dit plan): -->

<!-- utils/schema.ts exporteert:
  export const SCORE_LEVELS
  export const DEELGEBIEDEN: Deelgebied[]
  export function detectColumnMapping(headers: string[]): ...
  export function normalizeScore(raw: unknown): string | null
-->

<!-- utils/datamodel.ts exporteert:
  export const appState = { students: any[], lastImportErrors: any[], importing: boolean }
  export function addStudent(student: any): void
  export function saveState(): boolean
  export function loadState(): boolean
  export function clearState(): void
  export function normalizeNaam(naam: string): string
  export function mergeVerzuim(records: any[]): { matched: number; unmatched: string[] }
  export function getVerzuim(leerlingId: string): any
-->

<!-- utils/leerlijnen.ts exporteert:
  export function getLeerlijnenMapping(): Record<string, string>
  export function saveLeerlijnenMapping(mapping: any): boolean
  export function resetLeerlijnenMapping(): void
-->

<!-- Kritische window.* vervanging tabel (uit 11-PATTERNS.md):
  klassen.ts:
    window.klassenState (self-ref) → klassenState
    window.appState → appState (from ./datamodel)
    window.saveState() → saveState() (from ./datamodel)
    window.saveKlassen() → saveKlassen() (lokale functie call)
    window.switchActiveKlas → switchActiveKlas (lokale functie call)

  actiepunten.ts:
    window.appState && window.appState.students → appState.students
    window.saveState() → saveState() (from ./datamodel)
    window.actiepuntenStore → actiepuntenStore (lokale export)
    window.normalizeOnderwerp → normalizeOnderwerp (lokale export)
    window.isHerhaling → isHerhaling (lokale export)

  prognosis.ts:
    window.DEELGEBIEDEN → DEELGEBIEDEN (from ./schema)
    window.getLeerlijnenMapping ? window.getLeerlijnenMapping() : {} → getLeerlijnenMapping()
    window.appState.students → appState.students (from ./datamodel) — in berekenAllePrognoses
    window.berekenPrognose → berekenPrognose (lokale export)
    window.berekenAllePrognoses → berekenAllePrognoses (lokale export)
    window.debugPrognose → optioneel export (of weggooien)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: Migreer utils/klassen.js → utils/klassen.ts</name>
  <files>utils/klassen.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/utils/klassen.js
    D:/Downloads/get-shit-done-main/dashboard-2/utils/datamodel.ts
  </read_first>
  <behavior>
    - utils/klassen.ts bestaat; utils/klassen.js bestaat niet meer
    - Importeert appState, saveState van './datamodel' als eerste imports
    - Exporteert klassenState als mutable const object
    - Exporteert: switchActiveKlas, createKlas, deleteKlas, saveKlassen, loadKlassen, getActiveStudents, getAllRecordsForStudent (plus eventueel _migrateV1ToKlassen)
    - Geen window.* regels
    - npm run typecheck exit 0
  </behavior>
  <action>
    Hernoem utils/klassen.js naar utils/klassen.ts.

    1. Voeg bovenaan toe:
       import { appState, saveState } from './datamodel';

    2. Vervang window.klassenState = { ... } → export const klassenState: { klassen: Record<string, any>; activeKlasId: string | null } = { klassen: {}, activeKlasId: null }

    3. Vervang alle window.X = function(...) → export function X(...)
       Functies: switchActiveKlas, createKlas, deleteKlas, saveKlassen, loadKlassen, getActiveStudents, getAllRecordsForStudent, _migrateV1ToKlassen (als die bestaat).

    4. Vervang interne window.klassenState referenties → klassenState (lokaal).

    5. Vervang interne window.appState → appState (geïmporteerd).

    6. Vervang interne window.saveState() → saveState() (geïmporteerd).

    7. Vervang interne calls zoals window.saveKlassen() → saveKlassen() (lokale functie).

    8. Grep resultaat: grep -n "window\." utils/klassen.js geeft alle locaties. Vervang elk.

    9. Voeg : any type toe aan parameters. Gebruik as any voor klasObject shapes.
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "const fs=require('fs'); if(fs.existsSync('utils/klassen.js')) throw new Error('klassen.js still exists!'); fs.statSync('utils/klassen.ts'); console.log('klassen.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "import { appState" utils/klassen.ts && grep -c "export const klassenState" utils/klassen.ts && grep -c "export function switchActiveKlas" utils/klassen.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "window\." utils/klassen.ts | xargs -I{} sh -c 'if [ {} -ne 0 ]; then echo "FAIL: window.* still present"; exit 1; else echo "OK: no window.*"; fi'</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5 && npm run test 2>&1 | tail -5</automated>
  </verify>
  <done>utils/klassen.ts bestaat met import van datamodel, export klassenState en alle klas-functies. utils/klassen.js bestaat niet meer. Geen window.*. npm run test 9/9 groen.</done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: Migreer utils/actiepunten.js → utils/actiepunten.ts</name>
  <files>utils/actiepunten.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/utils/actiepunten.js
    D:/Downloads/get-shit-done-main/dashboard-2/utils/datamodel.ts
    D:/Downloads/get-shit-done-main/dashboard-2/tests/actiepunten.test.js
  </read_first>
  <behavior>
    - utils/actiepunten.ts bestaat; utils/actiepunten.js bestaat niet meer
    - Importeert appState, saveState van './datamodel' als eerste imports
    - Exporteert actiepuntenStore als const object met list, add, update, remove methoden
    - Exporteert normalizeOnderwerp als named function
    - Exporteert isHerhaling als named function
    - actiepuntenStore.add gebruikt saveState() (geïmporteerd, niet window.saveState)
    - actiepunten worden opgeslagen op appState.students (geïmporteerd, niet window.appState)
    - npm run test exit 0 — de 9 bestaande tests in actiepunten.test.js slagen NIET na deze migratie (want ze gebruiken nog require + window.*), maar er zijn geen syntax fouten
  </behavior>
  <action>
    Hernoem utils/actiepunten.js naar utils/actiepunten.ts.

    1. Voeg bovenaan toe:
       import { appState, saveState } from './datamodel';

    2. Verwijder de IIFE wrapper:
       - Verwijder (function() { aan het begin
       - Verwijder })(); aan het einde

    3. Vervang alle window.appState → appState (geïmporteerd).
       window.appState && window.appState.students → appState.students (appState is altijd beschikbaar via import)

    4. Vervang window.saveState() → saveState() (geïmporteerd).

    5. Vervang de window.* exports onderaan:
       window.normalizeOnderwerp = normalizeOnderwerp → export function normalizeOnderwerp (of verplaats export naar de functie declaratie)
       window.isHerhaling = isHerhaling → export function isHerhaling (of verplaats export)
       window.actiepuntenStore = store → export const actiepuntenStore = store

    6. Voeg : string parameter types toe aan normalizeOnderwerp(s: string), isHerhaling(listOrStudent: any, nieuwOnderwerp: string, excludeId?: string).

    7. KRITISCH voor test-compatibiliteit: appState.students moet muteerbaar zijn (is al zo in datamodel.ts als mutable const). Controleer dat getStudent(leerlingId) de appState.students array uitleest.

    NOOT over actiepunten.test.js: De bestaande 9 tests gebruiken nog require() + window.*. Ze FALEN na deze migratie omdat de test de module anders laadt. Dit is VERWACHT en wordt opgelost in Plan 06 (tests update). Controleer dat het falen met "Cannot use import statement" of "ReferenceError: require is not defined" is — NIET met een TypeScript syntax fout.
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "const fs=require('fs'); if(fs.existsSync('utils/actiepunten.js')) throw new Error('actiepunten.js still exists!'); fs.statSync('utils/actiepunten.ts'); console.log('actiepunten.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "import { appState" utils/actiepunten.ts && grep -c "export const actiepuntenStore" utils/actiepunten.ts && grep -c "export function normalizeOnderwerp" utils/actiepunten.ts && grep -c "export function isHerhaling" utils/actiepunten.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "window\." utils/actiepunten.ts | xargs -I{} sh -c 'if [ {} -ne 0 ]; then echo "FAIL: window.* still present"; exit 1; else echo "OK: no window.*"; fi'</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>utils/actiepunten.ts bestaat met import van datamodel, export actiepuntenStore/normalizeOnderwerp/isHerhaling. utils/actiepunten.js bestaat niet meer. Geen window.*. npm run typecheck exit 0. (actiepunten.test.js mag nu falen — wordt gerepareerd in Plan 06.)</done>
</task>

<task type="auto" tdd="true">
  <name>Taak 3: Migreer utils/prognosis.js → utils/prognosis.ts</name>
  <files>utils/prognosis.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/utils/prognosis.js
    D:/Downloads/get-shit-done-main/dashboard-2/utils/schema.ts
    D:/Downloads/get-shit-done-main/dashboard-2/utils/leerlijnen.ts
    D:/Downloads/get-shit-done-main/dashboard-2/utils/datamodel.ts
  </read_first>
  <behavior>
    - utils/prognosis.ts bestaat; utils/prognosis.js bestaat niet meer
    - Importeert DEELGEBIEDEN van './schema'; getLeerlijnenMapping van './leerlijnen'
    - Optioneel importeert appState van './datamodel' (alleen als berekenAllePrognoses dat gebruikt)
    - Exporteert berekenPrognose als named function
    - Exporteert berekenAllePrognoses als named function
    - IIFE wrapper verwijderd
    - Interne telLeerlijnen gebruikt DEELGEBIEDEN (geïmporteerd) en getLeerlijnenMapping() (geïmporteerd)
    - npm run typecheck exit 0
    - tests/prognosis.test.ts gaat van "module not found" naar échte test failures (of slagen als logica correct is)
  </behavior>
  <action>
    Hernoem utils/prognosis.js naar utils/prognosis.ts.

    1. Voeg bovenaan toe (vóór alle bestaande code):
       import { DEELGEBIEDEN } from './schema';
       import { getLeerlijnenMapping } from './leerlijnen';
       Als berekenAllePrognoses window.appState.students gebruikt, voeg ook toe:
       import { appState } from './datamodel';

    2. Verwijder de IIFE wrapper (opening (function() { en sluiting })(); ).

    3. Vervang in de interne telLeerlijnen functie:
       var deelgebieden = window.DEELGEBIEDEN → const deelgebieden = DEELGEBIEDEN
       var mapping = window.getLeerlijnenMapping ? window.getLeerlijnenMapping() : {} → const mapping = getLeerlijnenMapping()

    4. Vervang in berekenAllePrognoses:
       window.appState.students → appState.students (als dat van toepassing is)

    5. Vervang de window.* exports onderaan:
       window.berekenPrognose = function(student, traject) → export function berekenPrognose(student: any, traject?: string): any
       window.berekenAllePrognoses = function → export function berekenAllePrognoses(traject?: string): any[]
       window.debugPrognose — optioneel: export function debugPrognose of weggooien (ontwikkel helper)

    6. Controleer alle interne window.* referenties — grep -n "window\." utils/prognosis.js voor een complete lijst.

    7. Voeg any types toe aan parameters en return types. Interne helpers (isVoldoendeOfHoger, telLeerlijnen) zijn privé — geen export nodig.
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "const fs=require('fs'); if(fs.existsSync('utils/prognosis.js')) throw new Error('prognosis.js still exists!'); fs.statSync('utils/prognosis.ts'); console.log('prognosis.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "import { DEELGEBIEDEN } from './schema'" utils/prognosis.ts && grep -c "import { getLeerlijnenMapping } from './leerlijnen'" utils/prognosis.ts && grep -c "export function berekenPrognose" utils/prognosis.ts && grep -c "export function berekenAllePrognoses" utils/prognosis.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "window\." utils/prognosis.ts | xargs -I{} sh -c 'if [ {} -ne 0 ]; then echo "FAIL: window.* still present"; exit 1; else echo "OK: no window.*"; fi'</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>utils/prognosis.ts bestaat met imports van schema en leerlijnen, export berekenPrognose en berekenAllePrognoses. utils/prognosis.js bestaat niet meer. Geen window.*. npm run typecheck exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grens | Beschrijving |
|-------|-------------|
| localStorage → klassen.ts | loadKlassen leest van localStorage (jsdom in tests); opgeslagen JSON is niet gevalideerd |
| appState.students → actiepunten.ts | Actiepunten worden opgeslagen op het student object; student shape is any |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatie |
|-----------|-----------|-----------|------------|-----------|
| T-11-03-01 | Tampering | utils/klassen.ts localStorage | accept | Phase 12 vervangt localStorage door plugin-store met encryptie |
| T-11-03-02 | Tampering | utils/actiepunten.ts actiepuntenStore.add | accept | Input validatie voor onderwerp/datum/status is functioneel behouden uit .js origineel |
| T-11-03-03 | Denial of Service | utils/prognosis.ts berekenAllePrognoses | accept | Verwerkt in-memory data; geen I/O; max payload is aantal studenten per klas (~35) |
</threat_model>

<verification>
Na alle drie taken:
- npm run typecheck exit 0
- utils/klassen.js, utils/actiepunten.js, utils/prognosis.js bestaan NIET meer
- utils/klassen.ts, utils/actiepunten.ts, utils/prognosis.ts bestaan WEL
- Geen window.* in een van de drie .ts bestanden
- Alle zes imports die in de tabel staan (plan interfaces) zijn aanwezig
</verification>

<success_criteria>
Plan 03 is klaar wanneer:
1. utils/klassen.ts importeert appState+saveState van ./datamodel; exporteert klassenState en klas-functies
2. utils/actiepunten.ts importeert appState+saveState van ./datamodel; exporteert actiepuntenStore, normalizeOnderwerp, isHerhaling
3. utils/prognosis.ts importeert DEELGEBIEDEN van ./schema en getLeerlijnenMapping van ./leerlijnen; exporteert berekenPrognose, berekenAllePrognoses
4. Alle drie .js bestanden zijn verwijderd
5. npm run typecheck exit 0
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/11-typescript-migratie/11-03-SUMMARY.md` aan met:
- Gemigreerde bestanden en window.* vervangingen per bestand
- Of actiepunten.test.js nu faalt (verwacht) en de foutmelding
- TypeScript compile fouten gevonden en opgelost
</output>
