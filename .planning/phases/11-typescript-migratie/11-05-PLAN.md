---
phase: 11-typescript-migratie
plan: 05
type: execute
wave: 2
depends_on: ["11-02", "11-03"]
files_modified:
  - parsers/pdf.ts
  - parsers/excel.ts
autonomous: true
requirements:
  - MIG-01
  - MIG-02

must_haves:
  truths:
    - "parsers/pdf.ts importeert DEELGEBIEDEN en normalizeScore van '../utils/schema' — geen window.DEELGEBIEDEN meer"
    - "parsers/excel.ts gebruikt import * as XLSX from 'xlsx' — geen window.XLSX meer"
    - "parsers/excel.ts registreert cpexcel bij module load (vóór XLSX.read)"
    - "Geen window.* assignments onderaan pdf.ts of excel.ts"
    - "npm run typecheck exit 0"
    - "npm run typecheck-migrated exit 0 (noImplicitAny:true voor parsers/pdf.ts en parsers/excel.ts)"
  artifacts:
    - path: "parsers/pdf.ts"
      provides: "PDF parser als ESM module; exporteert parseSinglePDF als named export"
      exports: ["parseSinglePDF"]
    - path: "parsers/excel.ts"
      provides: "Excel parser als ESM module; exporteert parseExcelFile, parseVerzuimTime"
      exports: ["parseExcelFile", "parseVerzuimTime"]
  key_links:
    - from: "parsers/pdf.ts"
      to: "utils/schema.ts"
      via: "import { DEELGEBIEDEN, normalizeScore } from '../utils/schema'"
      pattern: "import.*DEELGEBIEDEN.*normalizeScore.*from.*schema"
    - from: "parsers/excel.ts"
      to: "xlsx npm package"
      via: "import * as XLSX from 'xlsx'"
      pattern: "import \\* as XLSX from 'xlsx'"
    - from: "parsers/excel.ts"
      to: "xlsx/dist/cpexcel.full.mjs"
      via: "import * as cpexcel from 'xlsx/dist/cpexcel.full.mjs'; XLSX.set_cptable(cpexcel.cptable)"
      pattern: "set_cptable"
---

<objective>
Migreer de twee parsers van JavaScript naar TypeScript. pdf.ts heeft minimale wijzigingen (al grotendeels ESM); excel.ts vervangt de window.XLSX CDN-aanname door een directe npm import.

Purpose: Na dit plan zijn alle utils EN parsers beschikbaar als TypeScript modules. Plan 06 (tests) kan dan de parser integration tests schrijven die MIG-01 en MIG-02 bewijzen. Beide parsers moeten ook `npm run typecheck-migrated` (noImplicitAny:true voor parsers/**) laten slagen.

Output: parsers/pdf.ts en parsers/excel.ts — beide zonder window.* globals, TypeScript-compileerbaar, noImplicitAny-compliant.
</objective>

<execution_context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-05-PLAN.md
</execution_context>

<context>
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/ROADMAP.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-PATTERNS.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-RESEARCH.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-02-SUMMARY.md
@D:/Downloads/get-shit-done-main/dashboard-2/.planning/phases/11-typescript-migratie/11-03-SUMMARY.md

<interfaces>
<!-- Beschikbare exports na Plans 02+03: -->
<!-- utils/schema.ts: export const DEELGEBIEDEN, export function normalizeScore -->

<!-- pdf.js analyse (uit 11-PATTERNS.md — exacte regels met window.*): -->
<!-- Regel 427: window.DEELGEBIEDEN.map(...) → DEELGEBIEDEN.map(...) -->
<!-- Regel 492: window.DEELGEBIEDEN.map(...) → DEELGEBIEDEN.map(...) -->
<!-- Regel 500: window.DEELGEBIEDEN[dgIdx].label → DEELGEBIEDEN[dgIdx].label -->
<!-- Regel 608: window.normalizeScore(item.str) → normalizeScore(item.str) -->
<!-- Regel 642: for (const dg of window.DEELGEBIEDEN) → for (const dg of DEELGEBIEDEN) -->
<!-- Regels 751-761: window.* assignments → VERWIJDEREN (export block al aanwezig op regels 725-748) -->

<!-- pdfjs vendor import patroon (geen .d.ts beschikbaar): -->
<!-- // @ts-ignore — vendor bundle heeft geen TypeScript declaraties -->
<!-- import * as pdfjsLib from '../vendor/pdf.min.mjs'; -->
<!-- Alle pdfjsLib API calls: (pdfjsLib as any).GlobalWorkerOptions.workerSrc = ... -->
<!-- const pdf = await (pdfjsLib as any).getDocument({ data: arrayBuffer }).promise; -->

<!-- excel.js window.* analyse (uit 11-PATTERNS.md): -->
<!-- Geen ESM header — alles via window.* globals -->
<!-- window.parseVerzuimTime = function → export function parseVerzuimTime -->
<!-- window.parseExcelFile = async function → export async function parseExcelFile -->
<!-- window.debugExcelBestand = function → export async function debugExcelBestand -->
<!-- if (typeof XLSX === 'undefined') guard → VERWIJDEREN -->
<!-- FileReader patroon → file.arrayBuffer() (moderne API) -->

<!-- xlsx cpexcel registratie (D-11 research, geverifieerd): -->
<!-- import * as cpexcel from 'xlsx/dist/cpexcel.full.mjs'; -->
<!-- XLSX.set_cptable((cpexcel as any).cptable); -->
<!-- Dit EENMALIG bij module load — niet in de functie body -->

<!-- noImplicitAny vereiste (D-11-05 via tsconfig.migrated.json):
  tsconfig.migrated.json include: ["utils/**", "parsers/**"]
  Alle parameters in parsers/pdf.ts en parsers/excel.ts moeten expliciete types hebben.
  Gebruik as any voor pdfjs API calls (vendor bundle zonder .d.ts).
  Correct: function foo(x: any): void
  Fout:    function foo(x): void  ← fails typecheck-migrated
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: Migreer parsers/pdf.js → parsers/pdf.ts</name>
  <files>parsers/pdf.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/parsers/pdf.js
    D:/Downloads/get-shit-done-main/dashboard-2/utils/schema.ts
  </read_first>
  <behavior>
    - parsers/pdf.ts bestaat; parsers/pdf.js bestaat niet meer
    - Tweede import regel is: import { DEELGEBIEDEN, normalizeScore } from '../utils/schema'
    - Eerste import blijft: // @ts-ignore gevolgd door import * as pdfjsLib from '../vendor/pdf.min.mjs'
    - Vijf window.DEELGEBIEDEN en window.normalizeScore verwijzingen zijn vervangen
    - Regels 751-761 (window.* assignments) zijn verwijderd
    - De bestaande export block (regels 725-748) blijft intact
    - Alle function parameters hebben expliciete types (as any is OK voor pdfjs shapes)
    - npm run typecheck exit 0
    - npm run typecheck-migrated exit 0
  </behavior>
  <action>
    Hernoem parsers/pdf.js naar parsers/pdf.ts.

    Voer de volgende MINIMALE wijzigingen door (D-11-06: vendor bestanden ongewijzigd, D-11-09: identieke API):

    1. Voeg toe aan de import sectie bovenaan (na de bestaande pdfjsLib import):
       import { DEELGEBIEDEN, normalizeScore } from '../utils/schema';

    2. Voeg @ts-ignore toe vóór de vendor import:
       // @ts-ignore — vendor bundle heeft geen TypeScript declaraties; pdfjs-dist npm types gelden hier niet
       import * as pdfjsLib from '../vendor/pdf.min.mjs';

    3. Vervang alle vijf window.* verwijzingen (uit de patterns tabel):
       - Regel ~427: window.DEELGEBIEDEN.map → DEELGEBIEDEN.map
       - Regel ~492: window.DEELGEBIEDEN.map → DEELGEBIEDEN.map
       - Regel ~500: window.DEELGEBIEDEN[dgIdx] → DEELGEBIEDEN[dgIdx]
       - Regel ~608: window.normalizeScore( → normalizeScore(
       - Regel ~642: window.DEELGEBIEDEN) → DEELGEBIEDEN)
       Gebruik grep -n "window\." parsers/pdf.js om de exacte regelnummers te vinden vóór hernoeming.

    4. Verwijder de window.* assignments blok (regels ~751-761):
       window.parseSinglePDF = parseSinglePDF;
       window.extractAllTextItems = extractAllTextItems;
       (en eventuele andere window.* regels in dat blok)

    5. De bestaande export block (export { parseSinglePDF, ... }) BLIJFT staan — dit is al aanwezig.

    6. Voeg as any casts toe aan pdfjsLib API aanroepen die TypeScript niet kan infereren:
       (pdfjsLib as any).GlobalWorkerOptions.workerSrc = ...
       (pdfjsLib as any).getDocument(...).promise
       Gebruik grep -n "pdfjsLib\." parsers/pdf.ts om alle locaties te vinden.

    7. Voeg expliciete types toe aan alle function parameters (noImplicitAny vereiste):
       Gebruik : any voor parameters met onbekende shape uit de pdfjs callbacks.
       Overige TypeScript typefouten: gebruik as any met // TODO: type comment.
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "const fs=require('fs'); if(fs.existsSync('parsers/pdf.js')) throw new Error('pdf.js still exists!'); fs.statSync('parsers/pdf.ts'); console.log('pdf.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "import { DEELGEBIEDEN, normalizeScore } from '../utils/schema'" parsers/pdf.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "window\." parsers/pdf.ts | xargs -I{} sh -c 'if [ {} -ne 0 ]; then echo "FAIL: window.* still present (count: {})"; exit 1; else echo "OK: no window.*"; fi'</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5 && npm run typecheck-migrated 2>&1 | tail -5</automated>
  </verify>
  <done>parsers/pdf.ts bestaat met import { DEELGEBIEDEN, normalizeScore } van schema, geen window.*, export block intact. parsers/pdf.js bestaat niet meer. npm run typecheck exit 0. npm run typecheck-migrated exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: Migreer parsers/excel.js → parsers/excel.ts</name>
  <files>parsers/excel.ts</files>
  <read_first>
    D:/Downloads/get-shit-done-main/dashboard-2/parsers/excel.js
  </read_first>
  <behavior>
    - parsers/excel.ts bestaat; parsers/excel.js bestaat niet meer
    - Eerste twee imports zijn: import * as XLSX from 'xlsx' en import * as cpexcel from 'xlsx/dist/cpexcel.full.mjs'
    - Derde regel is XLSX.set_cptable((cpexcel as any).cptable) (module-level registratie)
    - Geen window.XLSX referentie, geen "typeof XLSX === 'undefined'" guard
    - FileReader patroon vervangen door file.arrayBuffer()
    - Exporteert parseVerzuimTime, parseExcelFile, debugExcelBestand als named exports
    - Alle function parameters hebben expliciete types
    - npm run typecheck exit 0
    - npm run typecheck-migrated exit 0
  </behavior>
  <action>
    Hernoem parsers/excel.js naar parsers/excel.ts.

    excel.js is GEEN ESM module — het is een pure window.* script. Herschrijf het als TypeScript module:

    1. Verwijder alle window.X = function(...) { ... } structuren.

    2. Voeg bovenaan het bestand toe:
       import * as XLSX from 'xlsx';
       import * as cpexcel from 'xlsx/dist/cpexcel.full.mjs';

       // Registreer cpexcel bij module load voor correcte Nederlandse tekens (cp1252)
       XLSX.set_cptable((cpexcel as any).cptable);

    3. Schrijf de functies als named exports met expliciete parameter types:
       export function parseVerzuimTime(str: unknown): number { ... }  (originele implementatie)
       export async function parseExcelFile(file: File): Promise<any[]> { ... }
       export async function debugExcelBestand(file: File): Promise<void> { ... }

    4. In parseExcelFile:
       - Verwijder: if (typeof XLSX === 'undefined') { throw new Error(...) } guard — niet meer nodig
       - Vervang het FileReader patroon door: const arrayBuffer = await file.arrayBuffer();
       - Behoud de rest van de functie logica identiek (XLSX.read, sheet_to_json, etc.)
       - Voeg try-catch toe rond XLSX.read als die er nog niet is

    5. Alle interne XLSX.read/XLSX.utils aanroepen blijven ongewijzigd — SheetJS types zijn ingebouwd.

    6. Voeg as any casts toe alleen waar TypeScript klaagt (bijv. XLSX.read argument type).

    7. Controleer alle interne helper functies op impliciete parameter types — voeg : any of concrete types toe.
  </action>
  <verify>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && node -e "const fs=require('fs'); if(fs.existsSync('parsers/excel.js')) throw new Error('excel.js still exists!'); fs.statSync('parsers/excel.ts'); console.log('excel.ts OK')"</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "import \* as XLSX from 'xlsx'" parsers/excel.ts && grep -c "set_cptable" parsers/excel.ts && grep -c "export function parseVerzuimTime" parsers/excel.ts && grep -c "export async function parseExcelFile" parsers/excel.ts</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && grep -c "window\." parsers/excel.ts | xargs -I{} sh -c 'if [ {} -ne 0 ]; then echo "FAIL: window.* still present"; exit 1; else echo "OK: no window.*"; fi'</automated>
    <automated>cd "D:/Downloads/get-shit-done-main/dashboard-2" && npm run typecheck 2>&1 | tail -5 && npm run typecheck-migrated 2>&1 | tail -5</automated>
  </verify>
  <done>parsers/excel.ts bestaat met XLSX npm import + cpexcel registratie + named exports. parsers/excel.js bestaat niet meer. Geen window.*. npm run typecheck exit 0. npm run typecheck-migrated exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grens | Beschrijving |
|-------|-------------|
| File (Uint8Array) → parsers/pdf.ts | PDF bestanden van de gebruiker; onbetrouwbare binaire input |
| File (ArrayBuffer) → parsers/excel.ts | Excel bestanden van de gebruiker (SomToday export); onbetrouwbare binaire input |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatie |
|-----------|-----------|-----------|------------|-----------|
| T-11-05-01 | Tampering | parsers/pdf.ts vendor parse | accept | pdfjs-dist vendor v5.5.207 parseert in een eigen context; malformed PDFs gooien een exception die door de caller wordt afgehandeld |
| T-11-05-02 | Tampering | parsers/excel.ts XLSX.read | mitigate | try-catch in parseExcelFile; ongeldige bestanden gooien een Error met beschrijvende melding |
| T-11-05-03 | Information Disclosure | parsers/excel.ts verzuimdata | accept | Verzuimdata blijft in memory; Phase 12 regelt persistentie-encryptie |
</threat_model>

<verification>
Na beide taken:
- npm run typecheck exit 0
- npm run typecheck-migrated exit 0 (noImplicitAny:true voor parsers/pdf.ts en parsers/excel.ts)
- parsers/pdf.js en parsers/excel.js bestaan NIET meer
- parsers/pdf.ts en parsers/excel.ts bestaan WEL
- Geen window.* in de twee .ts bestanden
- pdf.ts heeft import van utils/schema; excel.ts heeft import van xlsx + cpexcel registratie
</verification>

<success_criteria>
Plan 05 is klaar wanneer:
1. parsers/pdf.ts importeert DEELGEBIEDEN+normalizeScore van utils/schema; exporteert parseSinglePDF
2. parsers/excel.ts importeert XLSX van xlsx; registreert cpexcel bij module load; exporteert parseExcelFile, parseVerzuimTime
3. Beide .js bestanden zijn verwijderd
4. npm run typecheck exit 0
5. npm run typecheck-migrated exit 0
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/11-typescript-migratie/11-05-SUMMARY.md` aan met:
- Gemigreerde bestanden en het aantal window.* vervangingen
- TypeScript as any casts voor pdfjs vendor calls
- Of TypeScript klaagde over XLSX.read argument type (en de gekozen oplossing)
- npm run typecheck-migrated resultaat
</output>
</output>
