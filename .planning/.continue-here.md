---
context: default
phase: security-hardening
task: 11
total_tasks: 11
status: paused
last_updated: 2026-06-08T07:50:48Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_Geen blocking constraints geïdentificeerd deze sessie._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Legacy plaintext migratie | BPV en backup accepteren nog plaintext voor backwards compat — test dit pad vóór je het verwijdert | advisory | Houd migratiecode minimaal 1 release-cyclus actief |

<current_state>
Beveiligingssessie volledig afgerond. Alle 11 bevindingen gefixed.
Bestanden zijn gewijzigd maar NOG NIET GECOMMIT.
TypeScript check: schoon (0 nieuwe errors).
Volgende stap: commit alle beveiligingswijzigingen.
</current_state>

<completed_work>

Alle 11 beveiligingsfixes uitgevoerd op basis van OWASP Top 10 + MITRE ATT&CK analyse:

**Sessie 1 (gisteren) — 7 fixes:**
- [x] BPV data versleuteld via encrypt_klassen/decrypt_klassen (utils/bpv.ts)
- [x] Backup export/restore versleuteld + version-validatie (utils/backup.ts)
- [x] ImportPage: await applyBackupRestore (src/components/ImportPage.tsx)
- [x] localStorage notities bulk-migratie bij app-start (src/main.tsx)
- [x] tag_name semver whitelist + encodeURIComponent (utils/updateCheck.ts)
- [x] entitlements.plist: allow-unsigned-executable-memory verwijderd
- [x] xlsx verschoven van devDependencies naar dependencies

**Sessie 2 (vandaag) — 4 fixes:**
- [x] GitHub Actions SHA-gepind op commit-hashes (release.yml + ci.yml)
- [x] Bestandsgrootte-limieten: PDF 50MB, Excel 10MB, ZIP 100MB (ImportPage.tsx)
- [x] Magic bytes validatie: %PDF, PK (ZIP/xlsx), OLE2 (xls) (ImportPage.tsx)
- [x] PII verwijderd uit console.log/warn: namen → leerlingId (datamodel.ts, prognosis.ts)
</completed_work>

<remaining_work>

**Open LOW-bevindingen (bewust geaccepteerd, geen actie vereist):**
- style-src 'unsafe-inline' in CSP (tauri.conf.json) — Tauri desktop, beperkt risico; vereist Tailwind refactor
- xlsx via CDN-tarball — SHA-512 hash aanwezig, SheetJS 0.20.x niet op npm

**Pending menselijke actie:**
- [ ] Testen dat macOS app nog werkt zonder allow-unsigned-executable-memory
- [ ] COMMIT uitvoeren (zie next_action)
</remaining_work>

<decisions_made>

- BPV encrypt via bestaande Rust commands (consistentie, geen nieuwe sleutels)
- Legacy plaintext transparant migreren bij eerste lees (backwards compat)
- xlsx CDN accepteren als geaccepteerd risico (SHA-512 hash, geen npm-alternatief)
- style-src unsafe-inline open laten (LOW, Tailwind refactor buiten scope)
- GitHub Actions SHA-pinned op 2026-06-08 — periodiek vernieuwen bij major releases
</decisions_made>

<blockers>

Geen blockers. Alle fixes zijn technisch compleet.
</blockers>

## Required Reading (in order)
1. `REVIEW.md` — volledige beveiligingsaudit die de basis vormt voor alle fixes
2. `.planning/HANDOFF.json` — machine-readable state met alle gewijzigde bestanden

## Infrastructure State
- TypeScript: schoon (`npx tsc --noEmit` geeft 0 nieuwe errors)
- npm audit: 0 vulnerabilities
- Git: 13 bestanden gewijzigd, niet gecommit

<context>
Twee sessies beveiligingswerk afgerond. Eerste sessie: crypto-inconsistenties (BPV plaintext, backup plaintext, localStorage leak), URL-injection en entitlements. Tweede sessie: supply chain (GitHub Actions SHA-pinning), input validation (file size + magic bytes) en PII logging. Alle fixes zijn minimaal en backwards-compatibel. TypeScript strict check slaagt.
</context>

<next_action>
Start met: git commit -m "security: harden app — OWASP/MITRE analyse fixes (11 bevindingen)" voor de gewijzigde bestanden:
  .github/workflows/ci.yml .github/workflows/release.yml package.json package-lock.json
  src-tauri/entitlements.plist src/components/ImportPage.tsx src/main.tsx
  tests/backup.test.ts utils/backup.ts utils/bpv.ts utils/datamodel.ts
  utils/prognosis.ts utils/updateCheck.ts
</next_action>
